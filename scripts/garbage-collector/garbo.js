'use strict';

var kolmafia = require('kolmafia');

function _arrayLikeToArray(r, a) {
  (null == a || a > r.length) && (a = r.length);
  for (var e = 0, n = Array(a); e < a; e++) n[e] = r[e];
  return n;
}
function _arrayWithHoles(r) {
  if (Array.isArray(r)) return r;
}
function _arrayWithoutHoles(r) {
  if (Array.isArray(r)) return _arrayLikeToArray(r);
}
function _assertClassBrand(e, t, n) {
  if ("function" == typeof e ? e === t : e.has(t)) return arguments.length < 3 ? t : n;
  throw new TypeError("Private element is not present on this object");
}
function _assertThisInitialized(e) {
  if (void 0 === e) throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
  return e;
}
function _callSuper(t, o, e) {
  return o = _getPrototypeOf(o), _possibleConstructorReturn(t, _isNativeReflectConstruct() ? Reflect.construct(o, e || [], _getPrototypeOf(t).constructor) : o.apply(t, e));
}
function _checkPrivateRedeclaration(e, t) {
  if (t.has(e)) throw new TypeError("Cannot initialize the same private elements twice on an object");
}
function _classCallCheck(a, n) {
  if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function");
}
function _classPrivateFieldGet2(s, a) {
  return s.get(_assertClassBrand(s, a));
}
function _classPrivateFieldInitSpec(e, t, a) {
  _checkPrivateRedeclaration(e, t), t.set(e, a);
}
function _classPrivateFieldSet2(s, a, r) {
  return s.set(_assertClassBrand(s, a), r), r;
}
function _construct(t, e, r) {
  if (_isNativeReflectConstruct()) return Reflect.construct.apply(null, arguments);
  var o = [null];
  o.push.apply(o, e);
  var p = new (t.bind.apply(t, o))();
  return r && _setPrototypeOf(p, r.prototype), p;
}
function _defineProperties(e, r) {
  for (var t = 0; t < r.length; t++) {
    var o = r[t];
    o.enumerable = o.enumerable || false, o.configurable = true, "value" in o && (o.writable = true), Object.defineProperty(e, _toPropertyKey(o.key), o);
  }
}
function _createClass(e, r, t) {
  return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", {
    writable: false
  }), e;
}
function _createForOfIteratorHelper(r, e) {
  var t = "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"];
  if (!t) {
    if (Array.isArray(r) || (t = _unsupportedIterableToArray(r)) || e) {
      t && (r = t);
      var n = 0,
        F = function () {};
      return {
        s: F,
        n: function () {
          return n >= r.length ? {
            done: true
          } : {
            done: false,
            value: r[n++]
          };
        },
        e: function (r) {
          throw r;
        },
        f: F
      };
    }
    throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
  }
  var o,
    a = true,
    u = false;
  return {
    s: function () {
      t = t.call(r);
    },
    n: function () {
      var r = t.next();
      return a = r.done, r;
    },
    e: function (r) {
      u = true, o = r;
    },
    f: function () {
      try {
        a || null == t.return || t.return();
      } finally {
        if (u) throw o;
      }
    }
  };
}
function _defineProperty(e, r, t) {
  return (r = _toPropertyKey(r)) in e ? Object.defineProperty(e, r, {
    value: t,
    enumerable: true,
    configurable: true,
    writable: true
  }) : e[r] = t, e;
}
function _get() {
  return _get = "undefined" != typeof Reflect && Reflect.get ? Reflect.get.bind() : function (e, t, r) {
    var p = _superPropBase(e, t);
    if (p) {
      var n = Object.getOwnPropertyDescriptor(p, t);
      return n.get ? n.get.call(arguments.length < 3 ? e : r) : n.value;
    }
  }, _get.apply(null, arguments);
}
function _getPrototypeOf(t) {
  return _getPrototypeOf = Object.setPrototypeOf ? Object.getPrototypeOf.bind() : function (t) {
    return t.__proto__ || Object.getPrototypeOf(t);
  }, _getPrototypeOf(t);
}
function _inherits(t, e) {
  if ("function" != typeof e && null !== e) throw new TypeError("Super expression must either be null or a function");
  t.prototype = Object.create(e && e.prototype, {
    constructor: {
      value: t,
      writable: true,
      configurable: true
    }
  }), Object.defineProperty(t, "prototype", {
    writable: false
  }), e && _setPrototypeOf(t, e);
}
function _isNativeFunction(t) {
  try {
    return -1 !== Function.toString.call(t).indexOf("[native code]");
  } catch (n) {
    return "function" == typeof t;
  }
}
function _isNativeReflectConstruct() {
  try {
    var t = !Boolean.prototype.valueOf.call(Reflect.construct(Boolean, [], function () {}));
  } catch (t) {}
  return (_isNativeReflectConstruct = function () {
    return !!t;
  })();
}
function _iterableToArray(r) {
  if ("undefined" != typeof Symbol && null != r[Symbol.iterator] || null != r["@@iterator"]) return Array.from(r);
}
function _iterableToArrayLimit(r, l) {
  var t = null == r ? null : "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"];
  if (null != t) {
    var e,
      n,
      i,
      u,
      a = [],
      f = true,
      o = false;
    try {
      if (i = (t = t.call(r)).next, 0 === l) {
        if (Object(t) !== t) return;
        f = !1;
      } else for (; !(f = (e = i.call(t)).done) && (a.push(e.value), a.length !== l); f = !0);
    } catch (r) {
      o = true, n = r;
    } finally {
      try {
        if (!f && null != t.return && (u = t.return(), Object(u) !== u)) return;
      } finally {
        if (o) throw n;
      }
    }
    return a;
  }
}
function _nonIterableRest() {
  throw new TypeError("Invalid attempt to destructure non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
}
function _nonIterableSpread() {
  throw new TypeError("Invalid attempt to spread non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
}
function ownKeys$1(e, r) {
  var t = Object.keys(e);
  if (Object.getOwnPropertySymbols) {
    var o = Object.getOwnPropertySymbols(e);
    r && (o = o.filter(function (r) {
      return Object.getOwnPropertyDescriptor(e, r).enumerable;
    })), t.push.apply(t, o);
  }
  return t;
}
function _objectSpread2(e) {
  for (var r = 1; r < arguments.length; r++) {
    var t = null != arguments[r] ? arguments[r] : {};
    r % 2 ? ownKeys$1(Object(t), true).forEach(function (r) {
      _defineProperty(e, r, t[r]);
    }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys$1(Object(t)).forEach(function (r) {
      Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r));
    });
  }
  return e;
}
function _possibleConstructorReturn(t, e) {
  if (e && ("object" == typeof e || "function" == typeof e)) return e;
  if (void 0 !== e) throw new TypeError("Derived constructors may only return object or undefined");
  return _assertThisInitialized(t);
}
function _setPrototypeOf(t, e) {
  return _setPrototypeOf = Object.setPrototypeOf ? Object.setPrototypeOf.bind() : function (t, e) {
    return t.__proto__ = e, t;
  }, _setPrototypeOf(t, e);
}
function _slicedToArray(r, e) {
  return _arrayWithHoles(r) || _iterableToArrayLimit(r, e) || _unsupportedIterableToArray(r, e) || _nonIterableRest();
}
function _superPropBase(t, o) {
  for (; !{}.hasOwnProperty.call(t, o) && null !== (t = _getPrototypeOf(t)););
  return t;
}
function _superPropGet(t, o, e, r) {
  var p = _get(_getPrototypeOf(t.prototype ), o, e);
  return 2 & r && "function" == typeof p ? function (t) {
    return p.apply(e, t);
  } : p;
}
function _toConsumableArray(r) {
  return _arrayWithoutHoles(r) || _iterableToArray(r) || _unsupportedIterableToArray(r) || _nonIterableSpread();
}
function _toPrimitive(t, r) {
  if ("object" != typeof t || !t) return t;
  var e = t[Symbol.toPrimitive];
  if (void 0 !== e) {
    var i = e.call(t, r);
    if ("object" != typeof i) return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (String )(t);
}
function _toPropertyKey(t) {
  var i = _toPrimitive(t, "string");
  return "symbol" == typeof i ? i : i + "";
}
function _unsupportedIterableToArray(r, a) {
  if (r) {
    if ("string" == typeof r) return _arrayLikeToArray(r, a);
    var t = {}.toString.call(r).slice(8, -1);
    return "Object" === t && r.constructor && (t = r.constructor.name), "Map" === t || "Set" === t ? Array.from(r) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray(r, a) : void 0;
  }
}
function _wrapNativeSuper(t) {
  var r = "function" == typeof Map ? new Map() : void 0;
  return _wrapNativeSuper = function (t) {
    if (null === t || !_isNativeFunction(t)) return t;
    if ("function" != typeof t) throw new TypeError("Super expression must either be null or a function");
    if (void 0 !== r) {
      if (r.has(t)) return r.get(t);
      r.set(t, Wrapper);
    }
    function Wrapper() {
      return _construct(t, arguments, _getPrototypeOf(this).constructor);
    }
    return Wrapper.prototype = Object.create(t.prototype, {
      constructor: {
        value: Wrapper,
        enumerable: false,
        writable: true,
        configurable: true
      }
    }), _setPrototypeOf(Wrapper, t);
  }, _wrapNativeSuper(t);
}

var Args = /*#__PURE__*/function () {
  function Args() {
    _classCallCheck(this, Args);
  }
  return _createClass(Args, null, [{
    key: "custom",
    value: function custom(spec, _parser, valueHelpName) {
      var _a, _b;
      var raw_options = (_a = spec.options) === null || _a === void 0 ? void 0 : _a.map(option => option[0]);
      // Check that the default value actually appears in the options.
      if ("default" in spec && raw_options) {
        if (!raw_options.includes(spec.default)) {
          throw `Invalid default value ${spec.default}`;
        }
      }
      return _objectSpread2(_objectSpread2({}, spec), {}, {
        valueHelpName: valueHelpName,
        parser: value => {
          var parsed_value = _parser(value);
          if (parsed_value === undefined || parsed_value instanceof ParseError) return parsed_value;
          if (raw_options) {
            if (!raw_options.includes(parsed_value)) {
              return new ParseError(`received ${value} which was not in the allowed options`);
            }
          }
          return parsed_value;
        },
        options: (_b = spec.options) === null || _b === void 0 ? void 0 : _b.map(a => [`${a[0]}`, a[1]])
      });
    }
  }, {
    key: "arrayFromArg",
    value: function arrayFromArg(spec, argFromSpec) {
      var _a, _b, _c;
      // First, construct a non-array version of this argument.
      // We do this by calling argFromSpec in order to extract the parser and
      // valueHelpName (to make it easier to define the functions below).
      //
      // The default argument of an ArraySpec is of type T[], which causes
      // problems, so we must remove it.
      var spec_without_default = _objectSpread2({}, spec); // Avoid "the operand of a 'delete' operator must be optional"
      if ("default" in spec_without_default) delete spec_without_default["default"];
      var arg = argFromSpec.call(this, spec_without_default);
      // Next, check that all default values actually appear in the options.
      var raw_options = (_a = spec.options) === null || _a === void 0 ? void 0 : _a.map(option => option[0]);
      if ("default" in spec && raw_options) {
        var _iterator = _createForOfIteratorHelper(spec.default),
          _step;
        try {
          for (_iterator.s(); !(_step = _iterator.n()).done;) {
            var default_entry = _step.value;
            if (!raw_options.includes(default_entry)) throw `Invalid default value ${spec.default}`;
          }
        } catch (err) {
          _iterator.e(err);
        } finally {
          _iterator.f();
        }
      }
      var separator = (_b = spec.separator) !== null && _b !== void 0 ? _b : ",";
      var arrayParser = value => {
        // Split the array
        var values = value.split(separator);
        if (!spec.noTrim) values = values.map(v => v.trim());
        // Parse all values, return the first error found if any
        var result = values.map(v => arg.parser(v));
        var error = result.find(v => v instanceof ParseError);
        if (error) return error;
        var failure_index = result.indexOf(undefined);
        if (failure_index !== -1) return new ParseError(`components expected ${arg.valueHelpName} but could not parse ${values[failure_index]}`);
        // Otherwise, all values are good
        return result;
      };
      return _objectSpread2(_objectSpread2({}, spec), {}, {
        valueHelpName: `${arg.valueHelpName}${separator} ${arg.valueHelpName}${separator} ...`,
        parser: arrayParser,
        options: (_c = spec.options) === null || _c === void 0 ? void 0 : _c.map(a => [`${a[0]}`, a[1]])
      });
    }
  }, {
    key: "string",
    value: function string(spec) {
      return this.custom(spec, value => value, "TEXT");
    }
  }, {
    key: "strings",
    value: function strings(spec) {
      return this.arrayFromArg(spec, this.string);
    }
  }, {
    key: "number",
    value: function number(spec) {
      return this.custom(spec, value => isNaN(Number(value)) ? undefined : Number(value), "NUMBER");
    }
  }, {
    key: "numbers",
    value: function numbers(spec) {
      return this.arrayFromArg(spec, this.number);
    }
  }, {
    key: "boolean",
    value: function boolean(spec) {
      return this.custom(spec, value => {
        if (value.toLowerCase() === "true") return true;
        if (value.toLowerCase() === "false") return false;
        return undefined;
      }, "BOOLEAN");
    }
  }, {
    key: "booleans",
    value: function booleans(spec) {
      return this.arrayFromArg(spec, this.boolean);
    }
  }, {
    key: "flag",
    value: function flag(spec) {
      return this.custom(spec, value => {
        if (value.toLowerCase() === "true") return true;
        if (value.toLowerCase() === "false") return false;
        return undefined;
      }, "FLAG");
    }
  }, {
    key: "class",
    value: function _class(spec) {
      return this.custom(spec, value => {
        var match = kolmafia.Class.get(value);
        // Class.get does fuzzy matching:
        //  e.g. Class.get("sc") returns disco bandit.
        // To avoid this foot-gun, only return exact matches or id lookups.
        if (match.toString().toUpperCase() === value.toString().toUpperCase()) return match;
        if (!isNaN(Number(value))) return match;
        return undefined;
      }, "CLASS");
    }
  }, {
    key: "classes",
    value: function classes(spec) {
      return this.arrayFromArg(spec, this.class);
    }
  }, {
    key: "effect",
    value: function effect(spec) {
      return this.custom(spec, kolmafia.Effect.get, "EFFECT");
    }
  }, {
    key: "effects",
    value: function effects(spec) {
      return this.arrayFromArg(spec, this.effect);
    }
  }, {
    key: "familiar",
    value: function familiar(spec) {
      return this.custom(spec, kolmafia.Familiar.get, "FAMILIAR");
    }
  }, {
    key: "familiars",
    value: function familiars(spec) {
      return this.arrayFromArg(spec, this.familiar);
    }
  }, {
    key: "item",
    value: function item(spec) {
      return this.custom(spec, kolmafia.Item.get, "ITEM");
    }
  }, {
    key: "items",
    value: function items(spec) {
      return this.arrayFromArg(spec, this.item);
    }
  }, {
    key: "location",
    value: function location(spec) {
      return this.custom(spec, kolmafia.Location.get, "LOCATION");
    }
  }, {
    key: "locations",
    value: function locations(spec) {
      return this.arrayFromArg(spec, this.location);
    }
  }, {
    key: "monster",
    value: function monster(spec) {
      return this.custom(spec, kolmafia.Monster.get, "MONSTER");
    }
  }, {
    key: "monsters",
    value: function monsters(spec) {
      return this.arrayFromArg(spec, this.monster);
    }
  }, {
    key: "path",
    value: function path(spec) {
      return this.custom(spec, kolmafia.Path.get, "PATH");
    }
  }, {
    key: "paths",
    value: function paths(spec) {
      return this.arrayFromArg(spec, this.path);
    }
  }, {
    key: "skill",
    value: function skill(spec) {
      return this.custom(spec, kolmafia.Skill.get, "SKILL");
    }
  }, {
    key: "skills",
    value: function skills(spec) {
      return this.arrayFromArg(spec, this.skill);
    }
    /**
     * Create a group of arguments that will be printed separately in the help.
     *
     * Note that keys in the group must still be globally distinct.
     *
     * @param groupName The display name for the group in help.
     * @param args A JS object specifying the script arguments. Its values should
     *    be {@link Arg} objects (created by Args.string, Args.number, or others)
     *    or groups of arguments (created by Args.group).
     */
  }, {
    key: "group",
    value: function group(groupName, args) {
      return {
        name: groupName,
        args: args
      };
    }
    /**
     * Create a set of input arguments for a script.
     * @param scriptName Prefix for property names; often the name of the script.
     * @param scriptHelp Brief description of this script, for the help message.
     * @param args A JS object specifying the script arguments. Its values should
     *    be {@link Arg} objects (created by Args.string, Args.number, or others)
     *    or groups of arguments (created by Args.group).
     * @param options Config options for the args and arg parser.
     * @returns An object which can hold parsed argument values. The keys of this
     *    object are identical to the keys in 'args'.
     */
  }, {
    key: "create",
    value: function create(scriptName, scriptHelp, args, options) {
      _traverse(args, (keySpec, key) => {
        if (key === "help" || keySpec.key === "help") throw `help is a reserved argument name`;
      });
      var argsWithHelp = _objectSpread2(_objectSpread2({}, args), {}, {
        help: this.flag({
          help: "Show this message and exit.",
          setting: ""
        })
      });
      // Create an object to hold argument results, with a default value for
      // each argument.
      var res = _objectSpread2(_objectSpread2({}, _loadDefaultValues(argsWithHelp)), {}, {
        [specSymbol]: argsWithHelp,
        [scriptSymbol]: scriptName,
        [scriptHelpSymbol]: scriptHelp,
        [optionsSymbol]: options !== null && options !== void 0 ? options : {}
      });
      if (options === null || options === void 0 ? void 0 : options.positionalArgs) {
        var keys = [];
        var metadata = Args.getMetadata(res);
        metadata.traverse((keySpec, key) => {
          var _a;
          keys.push((_a = keySpec.key) !== null && _a !== void 0 ? _a : key);
        });
        var _iterator2 = _createForOfIteratorHelper(options.positionalArgs),
          _step2;
        try {
          for (_iterator2.s(); !(_step2 = _iterator2.n()).done;) {
            var arg = _step2.value;
            if (!keys.includes(arg)) throw `Unknown key for positional arg: ${arg}`;
          }
        } catch (err) {
          _iterator2.e(err);
        } finally {
          _iterator2.f();
        }
      }
      return res;
    }
    /**
     * Parse the command line input into the provided script arguments.
     * @param args An object to hold the parsed argument values, from Args.create(*).
     * @param command The command line input.
     * @param includeSettings If true, parse values from settings as well.
     */
  }, {
    key: "fill",
    value: function fill(args, command) {
      var includeSettings = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : true;
      var _a;
      var metadata = Args.getMetadata(args);
      // Load the list of keys and flags from the arg spec
      var keys = new Set();
      var flags = new Set();
      metadata.traverse((keySpec, key) => {
        var _a;
        var name = (_a = keySpec.key) !== null && _a !== void 0 ? _a : key;
        if (flags.has(name) || keys.has(name)) throw `Duplicate arg key ${name} is not allowed`;
        if (keySpec.valueHelpName === "FLAG") flags.add(name);else keys.add(name);
      });
      // Parse values from settings.
      if (includeSettings) {
        metadata.traverseAndMaybeSet(args, (keySpec, key) => {
          var _a, _b;
          var setting = (_a = keySpec.setting) !== null && _a !== void 0 ? _a : `${metadata.scriptName}_${(_b = keySpec.key) !== null && _b !== void 0 ? _b : key}`;
          if (setting === "") return undefined; // no setting
          var value_str = kolmafia.getProperty(setting);
          if (value_str === "") return undefined; // no setting
          return parseAndValidate(keySpec, `Setting ${setting}`, value_str);
        });
      }
      // Parse new argments from the command line
      if (command === undefined || command === "") return;
      var parsed = new CommandParser(command, keys, flags, (_a = metadata.options.positionalArgs) !== null && _a !== void 0 ? _a : []).parse();
      metadata.traverseAndMaybeSet(args, (keySpec, key) => {
        var _a;
        var argKey = (_a = keySpec.key) !== null && _a !== void 0 ? _a : key;
        var value_str = parsed.get(argKey);
        if (value_str === undefined) return undefined; // no setting
        return parseAndValidate(keySpec, `Argument ${argKey}`, value_str);
      });
    }
    /**
     * Parse command line input into a new set of script arguments.
     * @param scriptName Prefix to use in property names; typically the name of the script.
     * @param scriptHelp Brief description of this script, for the help message.
     * @param spec An object specifying the script arguments.
     * @param command The command line input.
     * @param options Config options for the args and arg parser.
     */
  }, {
    key: "parse",
    value: function parse(scriptName, scriptHelp, spec, command, options) {
      var args = this.create(scriptName, scriptHelp, spec, options);
      this.fill(args, command);
      return args;
    }
    /**
     * Print a description of the script arguments to the CLI.
     *
     * First, all top-level argument descriptions are printed in the order they
     * were defined. Afterwards, descriptions for groups of arguments are printed
     * in the order they were defined.
     *
     * @param args An object of parsed arguments, from Args.create(*).
     * @param maxOptionsToDisplay If given, do not list more than this many options for each arg.
     */
  }, {
    key: "showHelp",
    value: function showHelp(args, maxOptionsToDisplay) {
      var _a;
      var metadata = Args.getMetadata(args);
      kolmafia.printHtml(`${metadata.scriptHelp}`);
      kolmafia.printHtml("");
      kolmafia.printHtml(`<b>${(_a = metadata.options.defaultGroupName) !== null && _a !== void 0 ? _a : "Options"}:</b>`);
      metadata.traverse((arg, key) => {
        var _a, _b, _c, _d, _e;
        if (arg.hidden) return;
        var nameText = `<font color='${kolmafia.isDarkMode() ? "yellow" : "blue"}'>${(_a = arg.key) !== null && _a !== void 0 ? _a : key}</font>`;
        var valueText = arg.valueHelpName === "FLAG" ? "" : `<font color='purple'>${arg.valueHelpName}</font>`;
        var helpText = (_b = arg.help) !== null && _b !== void 0 ? _b : "";
        var defaultText = "default" in arg ? `<font color='#888888'>[default: ${arg.default}]</font>` : "";
        var settingText = arg.setting === "" ? "" : `<font color='#888888'>[setting: ${(_c = arg.setting) !== null && _c !== void 0 ? _c : `${metadata.scriptName}_${(_d = arg.key) !== null && _d !== void 0 ? _d : key}`}]</font>`;
        kolmafia.printHtml(`&nbsp;&nbsp;${[nameText, valueText, "-", helpText, defaultText, settingText].join(" ")}`);
        var valueOptions = (_e = arg.options) !== null && _e !== void 0 ? _e : [];
        if (valueOptions.length < (maxOptionsToDisplay !== null && maxOptionsToDisplay !== void 0 ? maxOptionsToDisplay : Number.MAX_VALUE)) {
          var _iterator3 = _createForOfIteratorHelper(valueOptions),
            _step3;
          try {
            for (_iterator3.s(); !(_step3 = _iterator3.n()).done;) {
              var option = _step3.value;
              if (option.length === 1 || option[1] === undefined) {
                kolmafia.printHtml(`&nbsp;&nbsp;&nbsp;&nbsp;<font color='blue'>${nameText}</font> ${option[0]}`);
              } else {
                kolmafia.printHtml(`&nbsp;&nbsp;&nbsp;&nbsp;<font color='blue'>${nameText}</font> ${option[0]} - ${option[1]}`);
              }
            }
          } catch (err) {
            _iterator3.e(err);
          } finally {
            _iterator3.f();
          }
        }
      }, group => {
        kolmafia.printHtml("");
        kolmafia.printHtml(`<b>${group.name}:</b>`);
      });
    }
    /**
     * Load the metadata information for a set of arguments. Only for advanced usage.
     *
     * @param args A JS object specifying the script arguments. Its values should
     *    be {@link Arg} objects (created by Args.string, Args.number, or others)
     *    or groups of arguments (created by Args.group).
     * @returns A class containing metadata information.
     */
  }, {
    key: "getMetadata",
    value: function getMetadata(args) {
      return new WrappedArgMetadata(args);
    }
  }]);
}();
var ParseError = /*#__PURE__*/_createClass(function ParseError(message) {
  _classCallCheck(this, ParseError);
  this.message = message;
});
/**
 * Metadata for the parsed arguments.
 *
 * This information is hidden within the parsed argument object so that it
 * is invisible to the user but available to fill(*) and showHelp(*).
 */
var specSymbol = Symbol("spec");
var scriptSymbol = Symbol("script");
var scriptHelpSymbol = Symbol("scriptHelp");
var optionsSymbol = Symbol("options");
/**
 * Parse a string into a value for a given argument, throwing if the parsing fails.
 * @param arg An argument that takes values in T.
 * @param source A description of where this value came from, for the error message.
 * @param value The value to parse.
 * @returns the parsed value.
 */
function parseAndValidate(arg, source, value) {
  var parsed_value;
  try {
    parsed_value = arg.parser(value);
  } catch (_a) {
    parsed_value = undefined;
  }
  if (parsed_value === undefined) throw `${source} expected ${arg.valueHelpName} but could not parse ${value}`;
  if (parsed_value instanceof ParseError) throw `${source} ${parsed_value.message}`;
  return parsed_value;
}
/**
 * A class that reveals the hidden metadata and specs for arguments.
 *
 * Only for advanced usage.
 */
var WrappedArgMetadata = /*#__PURE__*/function () {
  function WrappedArgMetadata(args) {
    _classCallCheck(this, WrappedArgMetadata);
    this.spec = args[specSymbol];
    this.scriptName = args[scriptSymbol];
    this.scriptHelp = args[scriptHelpSymbol];
    this.options = args[optionsSymbol];
  }
  /**
   * Create a parsed args object from this spec using all default values.
   */
  return _createClass(WrappedArgMetadata, [{
    key: "loadDefaultValues",
    value: function loadDefaultValues() {
      return _loadDefaultValues(this.spec);
    }
    /**
     * Traverse the spec and possibly generate a value for each argument.
     *
     * @param result The object to hold the resulting argument values, typically
     *    the result of loadDefaultValues().
     * @param setTo A function to generate an argument value from each arg spec.
     *    If this function returns undefined, then the argument value is unchanged.
     */
  }, {
    key: "traverseAndMaybeSet",
    value: function traverseAndMaybeSet(result, setTo) {
      return _traverseAndMaybeSet(this.spec, result, setTo);
    }
    /**
     * Traverse the spec and call a method for each argument.
     *
     * @param process A function to call at each arg spec.
     */
  }, {
    key: "traverse",
    value: function traverse(process, onGroup) {
      return _traverse(this.spec, process, onGroup);
    }
  }]);
}();
/**
 * Create a parsed args object from a spec using all default values.
 *
 * @param spec The spec for all arguments.
 */
function _loadDefaultValues(spec) {
  var result = {};
  for (var k in spec) {
    var argSpec = spec[k];
    if ("args" in argSpec) {
      result[k] = _loadDefaultValues(argSpec.args);
    } else {
      if ("default" in argSpec) result[k] = argSpec.default;else result[k] = undefined;
    }
  }
  return result;
}
/**
 * Traverse the spec and possibly generate a value for each argument.
 *
 * @param spec The spec for all arguments.
 * @param result The object to hold the resulting argument values.
 * @param setTo A function to generate an argument value from each arg spec.
 *    If this function returns undefined, then the argument value is unchanged.
 */
function _traverseAndMaybeSet(spec, result, setTo) {
  var groups = [];
  for (var k in spec) {
    var argSpec = spec[k];
    if ("args" in argSpec) {
      groups.push([argSpec, k]);
    } else {
      var value = setTo(argSpec, k);
      if (value === undefined) continue;
      result[k] = value;
    }
  }
  for (var _i = 0, _groups = groups; _i < _groups.length; _i++) {
    var group_and_key = _groups[_i];
    _traverseAndMaybeSet(group_and_key[0].args, result[group_and_key[1]], setTo);
  }
}
/**
 * Traverse the spec and possibly generate a value for each argument.
 *
 * @param spec The spec for all arguments.
 * @param process A function to call at each arg spec.
 */
function _traverse(spec, process, onGroup) {
  var groups = [];
  for (var k in spec) {
    var argSpec = spec[k];
    if ("args" in argSpec) {
      groups.push([argSpec, k]);
    } else {
      process(argSpec, k);
    }
  }
  for (var _i2 = 0, _groups2 = groups; _i2 < _groups2.length; _i2++) {
    var group_and_key = _groups2[_i2];
    onGroup === null || onGroup === void 0 ? void 0 : onGroup(group_and_key[0], group_and_key[1]);
    _traverse(group_and_key[0].args, process, onGroup);
  }
}
/**
 * A parser to extract key/value pairs from a command line input.
 * @member command The command line input.
 * @member keys The set of valid keys that can appear.
 * @member flags The set of valid flags that can appear.
 * @member index An internal marker for the progress of the parser over the input.
 */
var CommandParser = /*#__PURE__*/function () {
  function CommandParser(command, keys, flags, positionalArgs) {
    _classCallCheck(this, CommandParser);
    this.command = command;
    this.index = 0;
    this.keys = keys;
    this.flags = flags;
    this.positionalArgs = positionalArgs;
    this.positionalArgsParsed = 0;
  }
  /**
   * Perform the parsing of (key, value) pairs.
   * @returns The set of extracted (key, value) pairs.
   */
  return _createClass(CommandParser, [{
    key: "parse",
    value: function parse() {
      var _a, _b, _c, _d;
      this.index = 0; // reset the parser
      var result = new Map();
      while (!this.finished()) {
        // A flag F may appear as !F to be parsed as false.
        var parsing_negative_flag = false;
        if (this.peek() === "!") {
          parsing_negative_flag = true;
          this.consume(["!"]);
        }
        var startIndex = this.index;
        var key = this.parseKey();
        if (result.has(key)) {
          throw `Duplicate key ${key} (first set to ${(_a = result.get(key)) !== null && _a !== void 0 ? _a : ""})`;
        }
        if (this.flags.has(key)) {
          // The key corresponds to a flag.
          // Parse [key] as true and ![key] as false.
          result.set(key, parsing_negative_flag ? "false" : "true");
          if (this.peek() === "=") throw `Flag ${key} cannot be assigned a value`;
          if (!this.finished()) this.consume([" "]);
          this.prevUnquotedKey = undefined;
        } else if (this.keys.has(key)) {
          // Parse [key]=[value] or [key] [value]
          this.consume(["=", " "]);
          var value = this.parseValue();
          if (["'", '"'].includes((_b = this.prev()) !== null && _b !== void 0 ? _b : "")) this.prevUnquotedKey = undefined;else this.prevUnquotedKey = key;
          if (!this.finished()) this.consume([" "]);
          result.set(key, value);
        } else if (this.positionalArgsParsed < this.positionalArgs.length && this.peek() !== "=") {
          // Parse [value] as the next positional arg
          var positionalKey = this.positionalArgs[this.positionalArgsParsed];
          this.positionalArgsParsed++;
          this.index = startIndex; // back up to reparse the key as a value
          var _value = this.parseValue();
          if (["'", '"'].includes((_c = this.prev()) !== null && _c !== void 0 ? _c : "")) this.prevUnquotedKey = undefined;else this.prevUnquotedKey = key;
          if (!this.finished()) this.consume([" "]);
          if (result.has(positionalKey)) throw `Cannot assign ${_value} to ${positionalKey} (positionally) since ${positionalKey} was already set to ${(_d = result.get(positionalKey)) !== null && _d !== void 0 ? _d : ""}`;
          result.set(positionalKey, _value);
        } else {
          // Key not found; include a better error message if it is possible for quotes to have been missed
          if (this.prevUnquotedKey && this.peek() !== "=") throw `Unknown argument: ${key} (if this should have been parsed as part of ${this.prevUnquotedKey}, you should surround the entire value in quotes)`;else throw `Unknown argument: ${key}`;
        }
      }
      return result;
    }
    /**
     * @returns True if the entire command has been parsed.
     */
  }, {
    key: "finished",
    value: function finished() {
      return this.index >= this.command.length;
    }
    /**
     * @returns The next character to parse, if it exists.
     */
  }, {
    key: "peek",
    value: function peek() {
      if (this.index >= this.command.length) return undefined;
      return this.command.charAt(this.index);
    }
    /**
     * @returns The character just parsed, if it exists.
     */
  }, {
    key: "prev",
    value: function prev() {
      if (this.index <= 0) return undefined;
      if (this.index >= this.command.length + 1) return undefined;
      return this.command.charAt(this.index - 1);
    }
    /**
     * Advance the internal marker over the next expected character.
     * Throws an error on unexpected characters.
     *
     * @param allowed Characters that are expected.
     */
  }, {
    key: "consume",
    value: function consume(allowed) {
      var _a;
      if (this.finished()) throw `Expected ${allowed}`;
      if (allowed.includes((_a = this.peek()) !== null && _a !== void 0 ? _a : "")) {
        this.index += 1;
      }
    }
    /**
     * Find the next occurance of one of the provided characters, or the end of
     * the string if the characters never appear again.
     *
     * @param searchValue The characters to locate.
     */
  }, {
    key: "findNext",
    value: function findNext(searchValue) {
      var result = this.command.length;
      var _iterator4 = _createForOfIteratorHelper(searchValue),
        _step4;
      try {
        for (_iterator4.s(); !(_step4 = _iterator4.n()).done;) {
          var value = _step4.value;
          var index = this.command.indexOf(value, this.index);
          if (index !== -1 && index < result) result = index;
        }
      } catch (err) {
        _iterator4.e(err);
      } finally {
        _iterator4.f();
      }
      return result;
    }
    /**
     * Starting from the internal marker, parse a single key.
     * This also advances the internal marker.
     *
     * @returns The next key.
     */
  }, {
    key: "parseKey",
    value: function parseKey() {
      var keyEnd = this.findNext(["=", " "]);
      var key = this.command.substring(this.index, keyEnd);
      this.index = keyEnd;
      return key;
    }
    /**
     * Starting from the internal marker, parse a single value.
     * This also advances the internal marker.
     *
     * Values are a single word or enclosed in matching quotes, i.e. one of:
     *    "[^"]*"
     *    '[^']*"
     *    [^'"][^ ]*
     *
     * @returns The next value.
     */
  }, {
    key: "parseValue",
    value: function parseValue() {
      var _a, _b;
      var valueEnder = " ";
      var quotes = ["'", '"'];
      if (quotes.includes((_a = this.peek()) !== null && _a !== void 0 ? _a : "")) {
        valueEnder = (_b = this.peek()) !== null && _b !== void 0 ? _b : ""; // The value is everything until the next quote
        this.consume([valueEnder]); // Consume opening quote
      }
      var valueEnd = this.findNext([valueEnder]);
      var value = this.command.substring(this.index, valueEnd);
      if (valueEnder !== " " && valueEnd === this.command.length) {
        throw `No closing ${valueEnder} found for ${valueEnder}${value}`;
      }
      // Consume the value (and closing quote)
      this.index = valueEnd;
      if (valueEnder !== " ") this.consume([valueEnder]);
      return value;
    }
  }]);
}();

var LogLevels;
(function (LogLevels) {
  LogLevels[LogLevels["NONE"] = 0] = "NONE";
  LogLevels[LogLevels["ERROR"] = 1] = "ERROR";
  LogLevels[LogLevels["WARNING"] = 2] = "WARNING";
  LogLevels[LogLevels["INFO"] = 3] = "INFO";
  LogLevels[LogLevels["DEBUG"] = 4] = "DEBUG";
})(LogLevels || (LogLevels = {}));
var defaultHandlers = {
  [LogLevels.INFO]: message => {
    kolmafia.printHtml(`<b>[Libram Info]</b> ${message}`);
    kolmafia.logprint(`[Libram] ${message}`);
    return;
  },
  [LogLevels.WARNING]: message => {
    kolmafia.printHtml(`<span style="background: orange; color: white;"><b>[Libram Warning]</b> ${message}</span>`);
    kolmafia.logprint(`[Libram] ${message}`);
    return;
  },
  [LogLevels.ERROR]: error => {
    kolmafia.printHtml(`<span style="background: red; color: white;"><b>[Libram Error]</b> ${error.toString()}</span>`);
    kolmafia.logprint(`[Libram] ${error}`);
    return;
  },
  [LogLevels.DEBUG]: message => {
    kolmafia.printHtml(`<span style="background: red; color: white;"><b>[Libram Debug]</b> ${message}</span>`);
    kolmafia.logprint(`[Libram] ${message}`);
    return;
  }
};
var Logger = /*#__PURE__*/function () {
  function Logger() {
    _classCallCheck(this, Logger);
    _defineProperty(this, "handlers", defaultHandlers);
  }
  return _createClass(Logger, [{
    key: "level",
    get: function get() {
      return Logger.currentLevel;
    }
  }, {
    key: "setLevel",
    value: function setLevel(level) {
      Logger.currentLevel = level;
    }
  }, {
    key: "setHandler",
    value: function setHandler(level, callback) {
      this.handlers[level] = callback;
    }
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
  }, {
    key: "log",
    value: function log(level, message) {
      if (this.level >= level) this.handlers[level](message);
    }
  }, {
    key: "info",
    value: function info(message) {
      this.log(LogLevels.INFO, message);
    }
  }, {
    key: "warning",
    value: function warning(message) {
      this.log(LogLevels.WARNING, message);
    }
  }, {
    key: "error",
    value: function error(message) {
      this.log(LogLevels.ERROR, message);
    }
  }, {
    key: "debug",
    value: function debug(message) {
      this.log(LogLevels.DEBUG, message);
    }
  }]);
}();
_defineProperty(Logger, "currentLevel", LogLevels.ERROR);
var logger = new Logger();

/** THIS FILE IS AUTOMATICALLY GENERATED. See tools/parseDefaultProperties.ts for more information */
var booleanProperties = ["abortOnChoiceWhenNotInChoice", "addChatCommandLine", "addCreationQueue", "addStatusBarToFrames", "allowCloseableDesktopTabs", "allowNegativeTally", "allowNonMoodBurning", "allowSummonBurning", "autoHighlightOnFocus", "broadcastEvents", "cacheMallSearches", "chatBeep", "chatLinksUseRelay", "compactChessboard", "copyAsHTML", "customizedTabs", "debugBuy", "debugConsequences", "debugFoxtrotRemoval", "debugPathnames", "debugTopMenuStyle", "gapProtection", "gitInstallDependencies", "gitShowCommitMessages", "gitUpdateOnLogin", "greenScreenProtection", "guiUsesOneWindow", "hideServerDebugText", "logAcquiredItems", "logBattleAction", "logBrowserInteractions", "logChatMessages", "logChatRequests", "logCleanedHTML", "logDecoratedResponses", "logFamiliarActions", "logGainMessages", "logReadableHTML", "logPreferenceChange", "logMonsterHealth", "logReverseOrder", "logStatGains", "logStatusEffects", "logStatusOnLogin", "macroDebug", "macroLens", "mementoListActive", "mergeHobopolisChat", "printStackOnAbort", "proxySet", "relayAddSounds", "relayAddsCustomCombat", "relayAddsDiscoHelper", "relayAddsGraphicalCLI", "relayAddsQuickScripts", "relayAddsRestoreLinks", "relayAddsUpArrowLinks", "relayAddsUseLinks", "relayAddsWikiLinks", "relayAllowRemoteAccess", "relayBrowserOnly", "relayCacheUncacheable", "relayFormatsChatText", "relayHidesJunkMallItems", "relayMaintainsEffects", "relayMaintainsHealth", "relayMaintainsMana", "relayOverridesImages", "relayRunsAfterAdventureScript", "relayRunsBeforeBattleScript", "relayRunsBeforePVPScript", "relayScriptButtonFirst", "relayTextualizesEffects", "relayTrimsZapList", "relayUsesInlineLinks", "relayUsesIntegratedChat", "relayWarnOnRecoverFailure", "removeMalignantEffects", "retryFailedNetworkRequests", "saveSettingsOnSet", "separateTitleAndMenuBar", "sharePriceData", "showAllRequests", "showExceptionalRequests", "stealthLogin", "svnAlwaysAdd", "svnAlwaysOverwrite", "svnInstallDependencies", "svnShowCommitMessages", "svnUpdateOnLogin", "switchEquipmentForBuffs", "syncAfterSvnUpdate", "useChatToolbar", "useContactsFrame", "useDevServer", "useDockIconBadge", "useHugglerChannel", "useImageCache", "useLastUserAgent", "useSystemTrayIcon", "useTabbedChatFrame", "useToolbars", "useCachedVolcanoMaps", "useZoneComboBox", "verboseSpeakeasy", "verboseFloundry", "wrapLongLines", "_faxDataChanged", "_gitUpdated", "_svnRepoFileFetched", "_svnUpdated", "antagonisticSnowmanKitAvailable", "arcadeGameHints", "armoryUnlocked", "autoForbidIgnoringStores", "autoCraft", "autoQuest", "autoEntangle", "autoGarish", "autoManaRestore", "autoFillMayoMinder", "autoPinkyRing", "autoPlantHardcore", "autoPlantSoftcore", "autoPotionID", "autoRepairBoxServants", "autoSatisfyWithCloset", "autoSatisfyWithCoinmasters", "autoSatisfyWithMall", "autoSatisfyWithNPCs", "autoSatisfyWithStash", "autoSatisfyWithStorage", "autoSatisfyWithShop", "autoSetConditions", "autoSteal", "autoTuxedo", "backupCameraReverserEnabled", "badMoonEncounter01", "badMoonEncounter02", "badMoonEncounter03", "badMoonEncounter04", "badMoonEncounter05", "badMoonEncounter06", "badMoonEncounter07", "badMoonEncounter08", "badMoonEncounter09", "badMoonEncounter10", "badMoonEncounter11", "badMoonEncounter12", "badMoonEncounter13", "badMoonEncounter14", "badMoonEncounter15", "badMoonEncounter16", "badMoonEncounter17", "badMoonEncounter18", "badMoonEncounter19", "badMoonEncounter20", "badMoonEncounter21", "badMoonEncounter22", "badMoonEncounter23", "badMoonEncounter24", "badMoonEncounter25", "badMoonEncounter26", "badMoonEncounter27", "badMoonEncounter28", "badMoonEncounter29", "badMoonEncounter30", "badMoonEncounter31", "badMoonEncounter32", "badMoonEncounter33", "badMoonEncounter34", "badMoonEncounter35", "badMoonEncounter36", "badMoonEncounter37", "badMoonEncounter38", "badMoonEncounter39", "badMoonEncounter40", "badMoonEncounter41", "badMoonEncounter42", "badMoonEncounter43", "badMoonEncounter44", "badMoonEncounter45", "badMoonEncounter46", "badMoonEncounter47", "badMoonEncounter48", "barrelShrineUnlocked", "batWingsBatHoleEntrance", "batWingsBatratBurrow", "batWingsBeanbatChamber", "batWingsGuanoJunction", "bigBrotherRescued", "blackBartsBootyAvailable", "bondAdv", "bondBeach", "bondBeat", "bondBooze", "bondBridge", "bondDesert", "bondDR", "bondDrunk1", "bondDrunk2", "bondHoney", "bondHP", "bondInit", "bondItem1", "bondItem2", "bondItem3", "bondJetpack", "bondMartiniDelivery", "bondMartiniPlus", "bondMartiniTurn", "bondMeat", "bondMox1", "bondMox2", "bondMPregen", "bondMus1", "bondMus2", "bondMys1", "bondMys2", "bondSpleen", "bondStat", "bondStat2", "bondStealth", "bondStealth2", "bondSymbols", "bondWar", "bondWeapon2", "bondWpn", "bookOfIronyAvailable", "booPeakLit", "bootsCharged", "breakfastCompleted", "burlyBodyguardReceivedBonus", "burrowgrubHiveUsed", "calzoneOfLegendEaten", "candyCaneSwordApartmentBuilding", "candyCaneSwordBlackForest", "candyCaneSwordBowlingAlley", "candyCaneSwordCopperheadClub", "candyCaneSwordDailyDungeon", "candyCaneSwordDefiledCranny", "candyCaneSwordFunHouse", "candyCaneSwordShore", "candyCaneSwordWarFratRoom", "candyCaneSwordWarFratZetas", "candyCaneSwordWarHippyBait", "candyCaneSwordWarHippyLine", "canteenUnlocked", "chaosButterflyThrown", "chatbotScriptExecuted", "chateauAvailable", "chatLiterate", "chatServesUpdates", "checkJackassHardcore", "checkJackassSoftcore", "clanAttacksEnabled", "coldAirportAlways", "considerShadowNoodles", "controlRoomUnlock", "concertVisited", "controlPanel1", "controlPanel2", "controlPanel3", "controlPanel4", "controlPanel5", "controlPanel6", "controlPanel7", "controlPanel8", "controlPanel9", "corralUnlocked", "crAlways", "crimbo23ArmoryAtWar", "crimbo23BarAtWar", "crimbo23CafeAtWar", "crimbo23CottageAtWar", "crimbo23FoundryAtWar", "cyberDatastickCollected", "dailyDungeonDone", "dampOldBootPurchased", "daycareOpen", "deepDishOfLegendEaten", "demonSummoned", "dinseyAudienceEngagement", "dinseyGarbagePirate", "dinseyRapidPassEnabled", "dinseyRollercoasterNext", "dinseySafetyProtocolsLoose", "doghouseBoarded", "dontStopForCounters", "drippingHallUnlocked", "drippyShieldUnlocked", "edUsedLash", "eldritchFissureAvailable", "eldritchHorrorAvailable", "enqueueForConsumption", "errorOnAmbiguousFold", "essenceOfAnnoyanceAvailable", "essenceOfBearAvailable", "expressCardUsed", "falloutShelterChronoUsed", "falloutShelterCoolingTankUsed", "fireExtinguisherBatHoleUsed", "fireExtinguisherChasmUsed", "fireExtinguisherCyrptUsed", "fireExtinguisherDesertUsed", "fireExtinguisherHaremUsed", "fistTeachingsHaikuDungeon", "fistTeachingsPokerRoom", "fistTeachingsBarroomBrawl", "fistTeachingsConservatory", "fistTeachingsBatHole", "fistTeachingsFunHouse", "fistTeachingsMenagerie", "fistTeachingsSlums", "fistTeachingsFratHouse", "fistTeachingsRoad", "fistTeachingsNinjaSnowmen", "flickeringPixel1", "flickeringPixel2", "flickeringPixel3", "flickeringPixel4", "flickeringPixel5", "flickeringPixel6", "flickeringPixel7", "flickeringPixel8", "floristFriarAvailable", "floristFriarChecked", "frAlways", "frCemetaryUnlocked", "friarsBlessingReceived", "frMountainsUnlocked", "frSwampUnlocked", "frVillageUnlocked", "frWoodUnlocked", "getawayCampsiteUnlocked", "ghostPencil1", "ghostPencil2", "ghostPencil3", "ghostPencil4", "ghostPencil5", "ghostPencil6", "ghostPencil7", "ghostPencil8", "ghostPencil9", "gingerAdvanceClockUnlocked", "gingerBlackmailAccomplished", "gingerbreadCityAvailable", "gingerExtraAdventures", "gingerNegativesDropped", "gingerSewersUnlocked", "gingerSubwayLineUnlocked", "gingerRetailUnlocked", "glitchItemAvailable", "grabCloversHardcore", "grabCloversSoftcore", "grandpaUnlockedBlankPrescriptionSheet", "grandpaUnlockedEelSauce", "grandpaUnlockedFishyWand", "grandpaUnlockedGlowingSyringe", "grandpaUnlockedGroupieSpangles", "grandpaUnlockedHairOfTheFish", "grandpaUnlockedHalibut", "grandpaUnlockedHeavilyInvestedInPunFutures", "grandpaUnlockedJellyfishGel", "grandpaUnlockedMarineAquamarine", "grandpaUnlockedMidgetClownfish", "grandpaUnlockedSeaRadish", "grandpaUnlockedTrophyFish", "grandpaUnlockedWaterPoloCap", "grandpaUnlockedWaterPoloMitt", "guideToSafariAvailable", "guyMadeOfBeesDefeated", "hallowienerDefiledNook", "hallowienerGuanoJunction", "hallowienerKnollGym", "hallowienerMadnessBakery", "hallowienerMiddleChamber", "hallowienerOvergrownLot", "hallowienerSkeletonStore", "hallowienerSmutOrcs", "hallowienerSonofaBeach", "hallowienerVolcoino", "hardcorePVPWarning", "harvestBatteriesHardcore", "harvestBatteriesSoftcore", "hasAutumnaton", "hasBartender", "hasChef", "hasCocktailKit", "hasCosmicBowlingBall", "hasDetectiveSchool", "hasMaydayContract", "hasInterestingCoin", "hasOven", "hasRange", "hasShaker", "hasShrunkenHead", "hasSushiMat", "hasTwinkleVision", "haveBoxingDaydreamHardcore", "haveBoxingDaydreamSoftcore", "heartstoneBanishUnlocked", "heartstoneBuffUnlocked", "heartstoneKillUnlocked", "heartstoneLuckUnlocked", "heartstonePalsUnlocked", "heartstoneStunUnlocked", "hermitHax0red", "holidayHalsBookAvailable", "horseryAvailable", "hotAirportAlways", "includeCodpieceGemsInOutfits", "intenseCurrents", "isMerkinGladiatorChampion", "isMerkinHighPriest", "itemBoughtPerAscension637", "itemBoughtPerAscension8266", "itemBoughtPerAscension10790", "itemBoughtPerAscension10794", "itemBoughtPerAscension10795", "itemBoughtPerAscension12293", "itemBoughtPerAscension12298", "itemBoughtPerAscension12299", "itemBoughtPerAscension12300", "itemBoughtPerAscension12301", "itemBoughtPerAscension12303", "itemBoughtPerCharacter6423", "itemBoughtPerCharacter6428", "itemBoughtPerCharacter6429", "kingLiberated", "lastPirateInsult1", "lastPirateInsult2", "lastPirateInsult3", "lastPirateInsult4", "lastPirateInsult5", "lastPirateInsult6", "lastPirateInsult7", "lastPirateInsult8", "lawOfAveragesAvailable", "leafletCompleted", "ledCandleDropped", "libraryCardUsed", "lockPicked", "logBastilleBattalionBattles", "loginRecoveryHardcore", "loginRecoverySoftcore", "lovebugsUnlocked", "loveTunnelAvailable", "lowerChamberUnlock", "madnessBakeryAvailable", "makeHandheldRadiosHardcore", "makeHandheldRadiosSoftcore", "makePocketWishesHardcore", "makePocketWishesSoftcore", "manualOfNumberologyAvailable", "mappingMonsters", "mapToAnemoneMinePurchased", "mapToKokomoAvailable", "mapToMadnessReefPurchased", "mapToTheDiveBarPurchased", "mapToTheMarinaraTrenchPurchased", "mapToTheSkateParkPurchased", "maraisBeaverUnlock", "maraisCorpseUnlock", "maraisDarkUnlock", "maraisVillageUnlock", "maraisWildlifeUnlock", "maraisWizardUnlock", "maximizerAlwaysCurrent", "maximizerCreateOnHand", "maximizerCurrentMallPrices", "maximizerFoldables", "maximizerIncludeAll", "maximizerNoAdventures", "maximizerUseScope", "merkinElementaryBathroomUnlock", "merkinElementaryJanitorUnlock", "merkinElementaryTeacherUnlock", "middleChamberUnlock", "milkOfMagnesiumActive", "moonTuned", "neverendingPartyAlways", "noncombatForcerActive", "oasisAvailable", "odeBuffbotCheck", "oilPeakLit", "oscusSodaUsed", "outrageousSombreroUsed", "overgrownLotAvailable", "ownsFloristFriar", "ownsReplicaFloristFriar", "ownsSpeakeasy", "pathedSummonsHardcore", "pathedSummonsSoftcore", "permitScriptNotify", "pirateRealmUnlockedAnemometer", "pirateRealmUnlockedBlunderbuss", "pirateRealmUnlockedBreastplate", "pirateRealmUnlockedClipper", "pirateRealmUnlockedCrabsicle", "pirateRealmUnlockedFlag", "pirateRealmUnlockedFork", "pirateRealmUnlockedGoldRing", "pirateRealmUnlockedManOWar", "pirateRealmUnlockedPlushie", "pirateRealmUnlockedRadioRing", "pirateRealmUnlockedRhum", "pirateRealmUnlockedScurvySkillbook", "pirateRealmUnlockedShavingCream", "pirateRealmUnlockedSpyglass", "pirateRealmUnlockedTattoo", "pirateRealmUnlockedThirdCrewmate", "pirateRealmUnlockedTikiSkillbook", "pizzaOfLegendEaten", "popularTartUnlocked", "potatoAlarmClockUsed", "prAlways", "prayedForGlamour", "prayedForProtection", "prayedForVigor", "primaryLabCheerCoreGrabbed", "pumpkinSpiceWhorlUsed", "pyramidBombUsed", "rageGlandVented", "readManualHardcore", "readManualSoftcore", "relayDecorateJsCommands", "relayShowSpoilers", "relayShowWarnings", "rememberDesktopSize", "replicaChateauAvailable", "replicaNeverendingPartyAlways", "replicaWitchessSetAvailable", "requireBoxServants", "requireSewerTestItems", "restUsingCampAwayTent", "restUsingChateau", "ROMOfOptimalityAvailable", "safePickpocket", "schoolOfHardKnocksDiplomaAvailable", "scriptCascadingMenus", "serverAddsCustomCombat", "serverAddsBothCombat", "SHAWARMAInitiativeUnlocked", "showForbiddenStores", "showGainsPerUnit", "showIgnoringStorePrices", "showNoSummonOnly", "showTurnFreeOnly", "shubJigguwattDefeated", "skeletonStoreAvailable", "sleazeAirportAlways", "snojoAvailable", "sortByEffect", "sortByRoom", "spacegateAlways", "spacegateVaccine1", "spacegateVaccine2", "spacegateVaccine3", "spaceInvaderDefeated", "spelunkyHints", "spiceMelangeUsed", "spookyAirportAlways", "stenchAirportAlways", "stopForFixedWanderer", "stopForUltraRare", "styxPixieVisited", "superconductorDefeated", "suppressCyberRealmDarkMode", "suppressCyberRealmGreenImages", "suppressInappropriateNags", "suppressPowerPixellation", "suppressMallPriceCacheMessages", "telegraphOfficeAvailable", "telescopeLookedHigh", "timeTowerAvailable", "trackLightsOut", "uneffectWithHotTub", "universalSeasoningActive", "universalSeasoningAvailable", "useBookOfEverySkillHardcore", "useBookOfEverySkillSoftcore", "useCrimboToysHardcore", "useCrimboToysSoftcore", "verboseMaximizer", "visitLoungeHardcore", "visitLoungeSoftcore", "visitRumpusHardcore", "visitRumpusSoftcore", "voteAlways", "wildfireBarrelCaulked", "wildfireDusted", "wildfireFracked", "wildfirePumpGreased", "wildfireSprinkled", "yearbookCameraPending", "yogUrtDefeated", "youRobotScavenged", "_2002MrStoreCreditsCollected", "_adjustedJokestersWig", "_affirmationCookieEaten", "_affirmationHateUsed", "_airFryerUsed", "_akgyxothUsed", "_alienAnimalMilkUsed", "_alienPlantPodUsed", "_allYearSucker", "_alliedRadioMaterielIntel", "_alliedRadioWildsunBoon", "_aprilShower", "_aprilShowerGlobsCollected", "_aprilShowerLungingThrustSmack", "_aprilShowerNorthernExplosion", "_aprilShowerSimmer", "_armyToddlerCast", "_aug1Cast", "_aug2Cast", "_aug3Cast", "_aug4Cast", "_aug5Cast", "_aug6Cast", "_aug7Cast", "_aug8Cast", "_aug9Cast", "_aug10Cast", "_aug11Cast", "_aug12Cast", "_aug13Cast", "_aug14Cast", "_aug15Cast", "_aug16Cast", "_aug17Cast", "_aug18Cast", "_aug19Cast", "_aug20Cast", "_aug21Cast", "_aug22Cast", "_aug23Cast", "_aug24Cast", "_aug25Cast", "_aug26Cast", "_aug27Cast", "_aug28Cast", "_aug29Cast", "_aug30Cast", "_aug31Cast", "_augTodayCast", "_authorsInkUsed", "_baconMachineUsed", "_bagOfCandy", "_bagOfCandyUsed", "_bagOTricksUsed", "_ballastTurtleUsed", "_ballInACupUsed", "_ballpit", "_barrelPrayer", "_bastilleLastBattleWon", "_brandishMossMedalCast", "_beachCombing", "_bearlyHeard", "_bendHellUsed", "_blackMonolithUsed", "_blankoutUsed", "_bloodBagDoctorBag", "_bloodBagCloake", "_bloodBankIntimidated", "_bloodBankVisited", "_bonersSummoned", "_bookOfEverySkillUsed", "_borrowedTimeUsed", "_bowleggedSwaggerUsed", "_bowlFullOfJellyUsed", "_boxOfHammersUsed", "_brainPreservationFluidUsed", "_brassDreadFlaskUsed", "_cameraUsed", "_canSeekBirds", "_candyCaneSwordBackAlley", "_candyCaneSwordHauntedBedroom", "_candyCaneSwordHauntedLibrary", "_candyCaneSwordLyle", "_candyCaneSwordMadnessBakery", "_candyCaneSwordOvergrownLot", "_candyCaneSwordOvergrownShrine", "_candyCaneSwordPalindome", "_candyCaneSwordSouthOfTheBorder", "_candyCaneSwordSpookyForest", "_carboLoaded", "_cargoPocketEmptied", "_ceciHatUsed", "_chateauDeskHarvested", "_chateauMonsterFought", "_chibiChanged", "_chronerCrossUsed", "_chronerTriggerUsed", "_chubbyAndPlumpUsed", "_circadianRhythmsRecalled", "_circleDrumUsed", "_clanFortuneBuffUsed", "_clanRumpusSpot1Visited", "_clanRumpusSpot2Visited", "_clanRumpusSpot3Visited", "_clanRumpusSpot4Visited", "_clanRumpusSpot5Visited", "_clanRumpusSpot7Visited", "_clanRumpusSpot9Visited", "_claraBellUsed", "_coalPaperweightUsed", "_cocoaDispenserUsed", "_cocktailShakerUsed", "_coldAirportToday", "_coldOne", "_communismUsed", "_confusingLEDClockUsed", "_controlPanelUsed", "_cookbookbatRecipeDrops", "_coolerYetiAdventures", "_corruptedStardustUsed", "_cosmicSixPackConjured", "_crappyCameraUsed", "_creepyVoodooDollUsed", "_crimboPastDailySpecial", "_crimboPastMedicalGruel", "_crimboPastPrizeTurkey", "_crimboPastSmokingPope", "_crimboTraining", "_crimboTree", "_crToday", "_cursedKegUsed", "_cursedMicrowaveUsed", "_cyberTrashCollected", "_dailyDungeonMalwareUsed", "_darkChocolateHeart", "_daycareFights", "_daycareNap", "_daycareSpa", "_daycareToday", "_defectiveTokenChecked", "_defectiveTokenUsed", "_dinseyGarbageDisposed", "_discoKnife", "_distentionPillUsed", "_dnaHybrid", "_docClocksThymeCocktailDrunk", "_douseFoeSuccess", "_drippingHallDoor1", "_drippingHallDoor2", "_drippingHallDoor3", "_drippingHallDoor4", "_drippyCaviarUsed", "_drippyNuggetUsed", "_drippyPilsnerUsed", "_drippyPlumUsed", "_drippyWineUsed", "_eldritchHorrorEvoked", "_eldritchTentacleFought", "_eleventRestEffectGained", "_elfGuardHangoverCureUsed", "_emberingHulkFought", "_entauntaunedToday", "_envyfishEggUsed", "_epicMcTwistUsed", "_essentialTofuUsed", "_etchedHourglassUsed", "_eternalCarBatteryUsed", "_everfullGlassUsed", "_extraGreasySliderEaten", "_eyeAndATwistUsed", "_fancyChessSetUsed", "_falloutShelterSpaUsed", "_fancyHotDogEaten", "_faradayCageRestEffectGained", "_farmerItemsCollected", "_favoriteBirdVisited", "_firedJokestersGun", "_fireExtinguisherRefilled", "_fireStartingKitUsed", "_fireworksShop", "_fireworksShopHatBought", "_fireworksShopEquipmentBought", "_fireworkUsed", "_fishyPipeUsed", "_flagellateFlagonUsed", "_fleekMascaraUsed", "_floundryItemCreated", "_floundryItemUsed", "_freePillKeeperUsed", "_frToday", "_frostyMugUsed", "_fudgeSporkUsed", "_garbageItemChanged", "_giantGnawingBoneUsed", "_gingerBiggerAlligators", "_gingerbreadCityToday", "_gingerbreadClockAdvanced", "_gingerbreadClockVisited", "_gingerbreadColumnDestroyed", "_gingerbreadHouseRestEffectGained", "_gingerbreadMobHitUsed", "_glennGoldenDiceUsed", "_glitchItemImplemented", "_gnollEyeUsed", "_gnomePart", "_governmentPerDiemUsed", "_grimBuff", "_guildManualUsed", "_guzzlrQuestAbandoned", "_hardKnocksDiplomaUsed", "_heartstoneLuckUsed", "_hippyMeatCollected", "_hobbyHorseUsed", "_hodgmansBlanketDrunk", "_holidayFunUsed", "_holoWristCrystal", "_hotAirportToday", "_hungerSauceUsed", "_hyperinflatedSealLungUsed", "_iceHotelRoomsRaided", "_iceSculptureUsed", "_incredibleSelfEsteemCast", "_infernoDiscoVisited", "_infiniteJellyUsed", "_internetDailyDungeonMalwareBought", "_internetGallonOfMilkBought", "_internetPlusOneBought", "_internetPrintScreenButtonBought", "_internetViralVideoBought", "_interviewIsabella", "_interviewMasquerade", "_interviewVlad", "_inquisitorsUnidentifiableObjectUsed", "_interestingCoinHeads", "_ironicMoustache", "_jackassPlumberGame", "_jarlsCheeseSummoned", "_jarlsCreamSummoned", "_jarlsDoughSummoned", "_jarlsEggsSummoned", "_jarlsFruitSummoned", "_jarlsMeatSummoned", "_jarlsPotatoSummoned", "_jarlsVeggiesSummoned", "_jingleBellUsed", "_jukebox", "_kgbFlywheelCharged", "_kgbLeftDrawerUsed", "_kgbOpened", "_kgbRightDrawerUsed", "_kneecappingOrdered", "_kolConSixPackUsed", "_kolhsCutButNotDried", "_kolhsIsskayLikeAnAshtray", "_kolhsPoeticallyLicenced", "_kolhsSchoolSpirited", "_kudzuSaladEaten", "_lastCombatLost", "_lastCombatWon", "_latteBanishUsed", "_latteCopyUsed", "_latteDrinkUsed", "_leafAntEggCrafted", "_leafDayShortenerCrafted", "_leafTattooCrafted", "_leavesJumped", "_legendaryBeat", "_legendaryNoodlesSpleen", "_legendaryPastaWaveCast", "_legendarySpiceGhostFood", "_licenseToChillUsed", "_lightningRodCast", "_lodestoneUsed", "_lookingGlass", "_loveTunnelToday", "_loveTunnelUsed", "_lunchBreak", "_lupineHormonesUsed", "_lyleFavored", "_madLiquorDrunk", "_madTeaParty", "_mafiaMiddleFingerRingUsed", "_managerialManipulationUsed", "_mansquitoSerumUsed", "_mapToACandyRichBlockUsed", "_maydayDropped", "_mayoDeviceRented", "_mayoTankSoaked", "_meatballMachineUsed", "_meatifyMatterUsed", "_milkOfMagnesiumUsed", "_mimeArmyShotglassUsed", "_miniKiwiIntoxicatingSpiritsBought", "_miniKiwiTipiDrop", "_missGravesVermouthDrunk", "_missileLauncherUsed", "_mobiusRingPrimed", "_molehillMountainUsed", "_momFoodReceived", "_mrBurnsgerEaten", "_muffinOrderedToday", "_mulliganStewEaten", "_mushroomGardenVisited", "_mushroomHouseRestEffectGained", "_neverendingPartyToday", "_newYouQuestCompleted", "_olympicSwimmingPool", "_olympicSwimmingPoolItemFound", "_overflowingGiftBasketUsed", "_partyHard", "_pastaAdditive", "_perfectFreezeUsed", "_perfectlyFairCoinUsed", "_petePartyThrown", "_peteRiotIncited", "_photocopyUsed", "_pickyTweezersUsed", "_pickleJuiceDrunk", "_pingPongGame", "_pirateBellowUsed", "_pirateDinghyUsed", "_pirateForkUsed", "_pirateRealmSoldCompass", "_pirateRealmWindicleUsed", "_pixelOrbUsed", "_plumbersMushroomStewEaten", "_pneumaticityPotionUsed", "_porkElfMedicineCabinetUsed", "_porkElfNetiPotUsed", "_porkElfSinkUsed", "_porkElfToiletriesKitUsed", "_porkElfToiletUsed", "_portableSteamUnitUsed", "_pottedTeaTreeUsed", "_prToday", "_psychoJarFilled", "_psychoJarUsed", "_psychokineticHugUsed", "_pumpkinRestEffectGained", "_punchingMirrorUsed", "_rainCoatCast", "_rainStickUsed", "_redwoodRainStickUsed", "_replicaSnowconeTomeUsed", "_replicaResolutionLibramUsed", "_replicaSmithsTomeUsed", "_requestSandwichSucceeded", "_residenceCubeRestEffectGained", "_rhinestonesAcquired", "_saladForkUsed", "_seadentWaveUsed", "_seaJellyHarvested", "_septEmberBalanceChecked", "_setOfJacksUsed", "_sewingKitUsed", "_sexChanged", "_shadowAffinityToday", "_shadowForestLooted", "_shrubDecorated", "_silverDreadFlaskUsed", "_sitCourseCompleted", "_skateBuff1", "_skateBuff2", "_skateBuff3", "_skateBuff4", "_skateBuff5", "_sleazeAirportToday", "_snowballFactoryUsed", "_snowFortRestEffectGained", "_sobrieTeaUsed", "_softwareGlitchTurnReceived", "_sotParcelReturned", "_spacegateMurderbot", "_spacegateRuins", "_spacegateSpant", "_spacegateToday", "_spacegateVaccine", "_spaghettiBreakfast", "_spaghettiBreakfastEaten", "_spinmasterLatheVisited", "_spinningWheel", "_spookyAirportToday", "_stabonicScrollUsed", "_steelyEyedSquintUsed", "_stenchAirportToday", "_stinkyCheeseBanisherUsed", "_strangeStalagmiteUsed", "_streamsCrossed", "_structuralEmberUsed", "_stuffedPocketwatchUsed", "_styxSprayUsed", "_summonAnnoyanceUsed", "_summonCarrotUsed", "_summonResortPassUsed", "_sweetToothUsed", "_syntheticDogHairPillUsed", "_tacoFlierUsed", "_takerSpaceSuppliesDelivered", "_telegraphOfficeToday", "_templeHiddenPower", "_tempuraAirUsed", "_thesisDelivered", "_thunderDownUnderwearCast", "_tiedUpFlamingLeafletFought", "_tiedUpFlamingMonsteraFought", "_tiedUpLeaviathanFought", "_timeSpinnerReplicatorUsed", "_toastSummoned", "_tonicDjinn", "_treasuryEliteMeatCollected", "_treasuryHaremMeatCollected", "_trivialAvocationsGame", "_tryptophanDartUsed", "_turtlePowerCast", "_twelveNightEnergyUsed", "_ultraMegaSourBallUsed", "_unblemishedPearlAnemoneMine", "_unblemishedPearlDiveBar", "_unblemishedPearlMadnessReef", "_unblemishedPearlMarinaraTrench", "_unblemishedPearlTheBriniestDeepests", "_victorSpoilsUsed", "_villainLairCanLidUsed", "_villainLairColorChoiceUsed", "_villainLairDoorChoiceUsed", "_villainLairFirecrackerUsed", "_villainLairSymbologyChoiceUsed", "_villainLairWebUsed", "_vmaskBanisherUsed", "_voraciTeaUsed", "_volcanoItemRedeemed", "_volcanoSuperduperheatedMetal", "_voodooSnuffUsed", "_voteToday", "_VYKEACafeteriaRaided", "_VYKEALoungeRaided", "_walfordQuestStartedToday", "_warbearBankUsed", "_warbearBreakfastMachineUsed", "_warbearGyrocopterUsed", "_warbearSodaMachineUsed", "_wildfireBarrelHarvested", "_witchessBuff", "_workshedItemUsed", "_yamBatteryUsed", "_zombieClover", "_preventScurvy", "lockedItem4637", "lockedItem4638", "lockedItem4639", "lockedItem4646", "lockedItem4647", "unknownRecipe3542", "unknownRecipe3543", "unknownRecipe3544", "unknownRecipe3545", "unknownRecipe3546", "unknownRecipe3547", "unknownRecipe3548", "unknownRecipe3749", "unknownRecipe3751", "unknownRecipe4172", "unknownRecipe4173", "unknownRecipe4174", "unknownRecipe5060", "unknownRecipe5061", "unknownRecipe5062", "unknownRecipe5063", "unknownRecipe5064", "unknownRecipe5066", "unknownRecipe5067", "unknownRecipe5069", "unknownRecipe5070", "unknownRecipe5072", "unknownRecipe5073", "unknownRecipe5670", "unknownRecipe5671", "unknownRecipe6501", "unknownRecipe6564", "unknownRecipe6565", "unknownRecipe6566", "unknownRecipe6567", "unknownRecipe6568", "unknownRecipe6569", "unknownRecipe6570", "unknownRecipe6571", "unknownRecipe6572", "unknownRecipe6573", "unknownRecipe6574", "unknownRecipe6575", "unknownRecipe6576", "unknownRecipe6577", "unknownRecipe6578", "unknownRecipe7752", "unknownRecipe7753", "unknownRecipe7754", "unknownRecipe7755", "unknownRecipe7756", "unknownRecipe7757", "unknownRecipe7758", "unknownRecipe10970", "unknownRecipe10971", "unknownRecipe10972", "unknownRecipe10973", "unknownRecipe10974", "unknownRecipe10975", "unknownRecipe10976", "unknownRecipe10977", "unknownRecipe10978", "unknownRecipe10988", "unknownRecipe10989", "unknownRecipe10990", "unknownRecipe10991", "unknownRecipe10992", "unknownRecipe11000"];
var numericProperties = ["coinMasterIndex", "dailyDeedsVersion", "defaultDropdown1", "defaultDropdown2", "defaultDropdownSplit", "defaultLimit", "fixedThreadPoolSize", "itemManagerIndex", "lastBuffRequestType", "lastGlobalCounterDay", "lastImageCacheClear", "pingDefaultTestPings", "previousUpdateRevision", "relayDelayForSVN", "relaySkillButtonCount", "scriptButtonPosition", "statusDropdown", "svnThreadPoolSize", "toolbarPosition", "_beachTides", "_g9Effect", "8BitBonusTurns", "8BitScore", "addingScrolls", "adventurerMeatsWorldPoints", "affirmationCookiesEaten", "aminoAcidsUsed", "antagonisticSnowmanKitCost", "ascensionsToday", "asolDeferredPoints", "asolPointsPigSkinner", "asolPointsCheeseWizard", "asolPointsJazzAgent", "autoAbortThreshold", "autoAntidote", "autoBuyPriceLimit", "autopsyTweezersUsed", "autumnatonQuestTurn", "availableCandyCredits", "availableDimes", "availableFunPoints", "availableMrStore2002Credits", "availableQuarters", "availableSeptEmbers", "availableStoreCredits", "availableSwagger", "avantGuardPoints", "averageSwagger", "awolMedicine", "awolPointsBeanslinger", "awolPointsCowpuncher", "awolPointsSnakeoiler", "awolDeferredPointsBeanslinger", "awolDeferredPointsCowpuncher", "awolDeferredPointsSnakeoiler", "awolVenom", "bagOTricksCharges", "ballpitBonus", "bankedKarma", "bartenderTurnsUsed", "basementMallPrices", "basementSafetyMargin", "batmanFundsAvailable", "batmanBonusInitialFunds", "batmanTimeLeft", "bearSwagger", "beeCounter", "beGregariousCharges", "beGregariousFightsLeft", "birdformCold", "birdformHot", "birdformRoc", "birdformSleaze", "birdformSpooky", "birdformStench", "blackBartsBootyCost", "blackPuddingsDefeated", "blackForestProgress", "blankOutUsed", "blessingShieldColdTier", "blessingShieldHotTier", "blessingShieldPhysicalTier", "blessingShieldSleazeTier", "blessingShieldSpookyTier", "blessingShieldStenchTier", "bloodweiserDrunk", "bodyguardCharge", "bondPoints", "bondVillainsDefeated", "boneAbacusVictories", "bookOfFactsGummi", "bookOfFactsPinata", "bookOfIronyCost", "booPeakProgress", "borisPoints", "breakableHandling", "breakableHandling1964", "breakableHandling9691", "breakableHandling9692", "breakableHandling9699", "breathitinCharges", "brodenBacteria", "brodenSprinkles", "buffBotMessageDisposal", "buffBotPhilanthropyType", "buffJimmyIngredients", "burnoutsDefeated", "burrowgrubSummonsRemaining", "bwApronMealsEaten", "camelSpit", "camerasUsed", "campAwayDecoration", "candyWitchTurnsUsed", "candyWitchCandyTotal", "carboLoading", "catBurglarBankHeists", "cellarLayout", "charitableDonations", "chasmBridgeProgress", "chefTurnsUsed", "chessboardsCleared", "chibiAlignment", "chibiBirthday", "chibiFitness", "chibiIntelligence", "chibiLastVisit", "chibiSocialization", "chilledToTheBone", "cinchoSaltAndLime", "cinderellaMinutesToMidnight", "cinderellaScore", "clubEmNextWeekMonsterTurn", "cocktailSummons", "commerceGhostCombats", "cookbookbatIngredientsCharge", "controlPanelOmega", "cornucopiasOpened", "cosmicBowlingBallReturnCombats", "cozyCounter6332", "cozyCounter6333", "cozyCounter6334", "craftingClay", "craftingLeather", "craftingPlansCharges", "craftingStraw", "crimbo16BeardChakraCleanliness", "crimbo16BootsChakraCleanliness", "crimbo16BungChakraCleanliness", "crimbo16CrimboHatChakraCleanliness", "crimbo16GutsChakraCleanliness", "crimbo16HatChakraCleanliness", "crimbo16JellyChakraCleanliness", "crimbo16LiverChakraCleanliness", "crimbo16NippleChakraCleanliness", "crimbo16NoseChakraCleanliness", "crimbo16ReindeerChakraCleanliness", "crimbo16SackChakraCleanliness", "crimboTrainingSkill", "crimboTreeDays", "cubelingProgress", "cupidBowFights", "currentExtremity", "currentHedgeMazeRoom", "currentMojoFilters", "currentNunneryMeat", "currentPortalEnergy", "currentReplicaStoreYear", "cursedMagnifyingGlassCount", "cyrptAlcoveEvilness", "cyrptCrannyEvilness", "cyrptNicheEvilness", "cyrptNookEvilness", "cyrptTotalEvilness", "darkGyfftePoints", "dartsThrown", "daycareEquipment", "daycareInstructorItemQuantity", "daycareInstructors", "daycareLastScavenge", "daycareToddlers", "dbNemesisSkill1", "dbNemesisSkill2", "dbNemesisSkill3", "desertExploration", "desktopHeight", "desktopWidth", "dinseyFilthLevel", "dinseyFunProgress", "dinseyNastyBearsDefeated", "dinseySocialJusticeIProgress", "dinseySocialJusticeIIProgress", "dinseyTouristsFed", "dinseyToxicMultiplier", "doctorBagQuestLights", "doctorBagUpgrades", "dreadScroll1", "dreadScroll2", "dreadScroll3", "dreadScroll4", "dreadScroll5", "dreadScroll6", "dreadScroll7", "dreadScroll8", "dripAdventuresSinceAscension", "drippingHallAdventuresSinceAscension", "drippingTreesAdventuresSinceAscension", "drippyBatsUnlocked", "drippyJuice", "drippyOrbsClaimed", "droneSelfDestructChipsUsed", "drunkenSwagger", "edDefeatAbort", "edPoints", "eldritchTentaclesFought", "electricKoolAidEaten", "elfGratitude", "encountersUntilDMTChoice", "encountersUntilYachtzeeChoice", "encountersUntilNEPChoice", "encountersUntilSRChoice", "ensorceleeLevel", "entauntaunedColdRes", "essenceOfAnnoyanceCost", "essenceOfBearCost", "exerciseLiquidityCharges", "extraRolloverAdventures", "falloutShelterLevel", "familiarSweat", "fingernailsClipped", "fistSkillsKnown", "flyeredML", "fossilB", "fossilD", "fossilN", "fossilP", "fossilS", "fossilW", "fratboysDefeated", "frenchGuardTurtlesFreed", "funGuyMansionKills", "garbageChampagneCharge", "garbageFireProgress", "garbageShirtCharge", "garbageTreeCharge", "garlandUpgrades", "getsYouDrunkTurnsLeft", "ghostPepperTurnsLeft", "gingerDigCount", "gingerLawChoice", "gingerMuscleChoice", "gingerTrainScheduleStudies", "gladiatorBallMovesKnown", "gladiatorBladeMovesKnown", "gladiatorNetMovesKnown", "glitchItemCost", "glitchItemImplementationCount", "glitchItemImplementationLevel", "glitchSwagger", "gloverPoints", "gnasirProgress", "goldenMrAccessories", "gongPath", "gooseDronesRemaining", "goreCollected", "gourdItemCount", "greyYouPoints", "grimoire1Summons", "grimoire2Summons", "grimoire3Summons", "grimstoneCharge", "guardTurtlesFreed", "guideToSafariCost", "guyMadeOfBeesCount", "guzzlrBronzeDeliveries", "guzzlrDeliveryProgress", "guzzlrGoldDeliveries", "guzzlrPlatinumDeliveries", "haciendaLayout", "hallowiener8BitRealm", "hallowienerCoinspiracy", "handfulOfTipsMeat", "hareMillisecondsSaved", "hareTurnsUsed", "heavyRainsStartingThunder", "heavyRainsStartingRain", "heavyRainsStartingLightning", "heroDonationBoris", "heroDonationJarlsberg", "heroDonationSneakyPete", "hiddenApartmentProgress", "hiddenBowlingAlleyProgress", "hiddenHospitalProgress", "hiddenOfficeProgress", "hiddenTavernUnlock", "highTopPumped", "hippiesDefeated", "holidayHalsBookCost", "holidaySwagger", "homemadeRobotUpgrades", "homebodylCharges", "hpAutoRecovery", "hpAutoRecoveryTarget", "iceSwagger", "intangibleAssetCharges", "ironicSwagger", "jarlsbergPoints", "juicyGarbageUsed", "jungCharge", "junglePuns", "knownAscensions", "kolhsTotalSchoolSpirited", "lassoTrainingCount", "lastAnticheeseDay", "lastArcadeAscension", "lastBadMoonReset", "lastBangPotionReset", "lastBattlefieldReset", "lastBeardBuff", "lastBreakfast", "lastCartographyBooPeak", "lastCartographyCastleTop", "lastCartographyDarkNeck", "lastCartographyDefiledNook", "lastCartographyFratHouse", "lastCartographyFratHouseVerge", "lastCartographyGuanoJunction", "lastCartographyHauntedBilliards", "lastCartographyHippyCampVerge", "lastCartographyZeppelinProtesters", "lastCastleGroundUnlock", "lastCastleTopUnlock", "lastCellarReset", "lastChanceThreshold", "lastChasmReset", "lastColosseumRoundWon", "lastCouncilVisit", "lastCounterDay", "lastDesertUnlock", "lastDispensaryOpen", "lastDMTDuplication", "lastDwarfFactoryReset", "lastEVHelmetValue", "lastEVHelmetReset", "lastEmptiedStorage", "lastFilthClearance", "lastGoofballBuy", "lastGuildStoreOpen", "lastGuyMadeOfBeesReset", "lastFratboyCall", "lastFriarCeremonyAscension", "lastFriarsElbowNC", "lastFriarsHeartNC", "lastFriarsNeckNC", "lastHippyCall", "lastIslandUnlock", "lastKeyotronUse", "lastKingLiberation", "lastLightsOutTurn", "lastMushroomPlot", "lastMiningReset", "lastNemesisReset", "lastPaperStripReset", "lastPirateEphemeraReset", "lastPirateInsultReset", "lastPlusSignUnlock", "lastQuartetAscension", "lastQuartetRequest", "lastSecondFloorUnlock", "lastShadowForgeUnlockAdventure", "lastKOLHSArtClassUnlockAdventure", "lastKOLHSChemClassUnlockAdventure", "lastKOLHSShopClassUnlockAdventure", "lastSkateParkReset", "lastStillBeatingSpleen", "lastTavernAscension", "lastTavernSquare", "lastTelescopeReset", "lastTempleAdventures", "lastTempleButtonsUnlock", "lastTempleUnlock", "lastThingWithNoNameDefeated", "lastTowelAscension", "lastTr4pz0rQuest", "lastTrainsetConfiguration", "lastVioletFogMap", "lastVoteMonsterTurn", "lastWartDinseyDefeated", "lastWuTangDefeated", "lastYearbookCameraAscension", "lastZapperWand", "lastZapperWandExplosionDay", "lawOfAveragesCost", "legacyPoints", "legendaryNoodlesAmygdala", "legendaryNoodlesSkin", "legendaryNoodlesStomach", "leprecondoLastNeedChange", "libramSummons", "lightsOutAutomation", "louvreDesiredGoal", "louvreGoal", "lovebugsAridDesert", "lovebugsBeachBuck", "lovebugsBooze", "lovebugsChroner", "lovebugsCoinspiracy", "lovebugsCyrpt", "lovebugsFreddy", "lovebugsFunFunds", "lovebugsHoboNickel", "lovebugsItemDrop", "lovebugsMeat", "lovebugsMeatDrop", "lovebugsMoxie", "lovebugsMuscle", "lovebugsMysticality", "lovebugsOilPeak", "lovebugsOrcChasm", "lovebugsPowder", "lovebugsWalmart", "lttQuestDifficulty", "lttQuestStageCount", "manaBurnSummonThreshold", "manaBurningThreshold", "manaBurningTrigger", "manorDrawerCount", "manualOfNumberologyCost", "mapToKokomoCost", "markYourTerritoryCharges", "masksUnlocked", "maximizerMRUSize", "maximizerCombinationLimit", "maximizerEquipmentLevel", "maximizerEquipmentScope", "maximizerMaxPrice", "maximizerPriceLevel", "maxManaBurn", "mayflyExperience", "mayoLevel", "meansuckerPrice", "mechanicalSongbirdProgress", "merkinVocabularyMastery", "miniAdvClass", "miniKiwiAiolisUsed", "miniMartinisDrunk", "mixedBerryJellyUses", "moleTunnelLevel", "momSeaMonkeeProgress", "mothershipProgress", "mpAutoRecovery", "mpAutoRecoveryTarget", "munchiesPillsUsed", "mushroomGardenCropLevel", "nanopolymerSpiderWebsUsed", "nextAprilBandTurn", "nextParanormalActivity", "nextQuantumFamiliarOwnerId", "nextQuantumFamiliarTurn", "noobPoints", "noobDeferredPoints", "noodleSummons", "nsContestants1", "nsContestants2", "nsContestants3", "nuclearAutumnPoints", "numericSwagger", "nunsVisits", "oilPeakProgress", "optimalSwagger", "optimisticCandleProgress", "palindomeDudesDefeated", "parasolUsed", "peaceTurkeyIndex", "pendingMapReflections", "phosphorTracesUses", "pingpongSkill", "pirateRealmPlasticPiratesDefeated", "pirateRealmShipsDestroyed", "pirateRealmStormsEscaped", "pirateSwagger", "plantingDay", "plumberBadgeCost", "plumberCostumeCost", "plumberPoints", "pokefamPoints", "poolSharkCount", "poolSkill", "powerPillProgress", "preworkoutPowderUses", "primaryLabGooIntensity", "prismaticSummons", "procrastinatorLanguageFluency", "promptAboutCrafting", "puzzleChampBonus", "pyramidPosition", "quantumPoints", "reagentSummons", "reanimatorArms", "reanimatorLegs", "reanimatorSkulls", "reanimatorWeirdParts", "reanimatorWings", "recentLocations", "redSnapperProgress", "relayPort", "relocatePygmyJanitor", "relocatePygmyLawyer", "rockinRobinProgress", "romanCandelabraRedCasts", "romanCandelabraBlueCasts", "romanCandelabraYellowCasts", "romanCandelabraGreenCasts", "romanCandelabraPurpleCasts", "ROMOfOptimalityCost", "rumpelstiltskinKidsRescued", "rumpelstiltskinTurnsUsed", "rwbMonsterCount", "safariSwagger", "sausageGrinderUnits", "schoolOfHardKnocksDiplomaCost", "schoolSwagger", "scrapbookCharges", "screechCombats", "scriptMRULength", "seadentConstructKills", "seadentLevel", "seaodesFound", "seaPoints", "SeasoningSwagger", "sexChanges", "shenInitiationDay", "shockingLickCharges", "shrunkenHeadZombieHP", "singleFamiliarRun", "skillBurn3", "skillBurn90", "skillBurn153", "skillBurn154", "skillBurn155", "skillBurn236", "skillBurn237", "skillBurn1019", "skillBurn5017", "skillBurn6014", "skillBurn6015", "skillBurn6016", "skillBurn6020", "skillBurn6021", "skillBurn6022", "skillBurn6023", "skillBurn6024", "skillBurn6026", "skillBurn6028", "skillBurn7323", "skillBurn14008", "skillBurn14028", "skillBurn14038", "skillBurn15011", "skillBurn15028", "skillBurn17005", "skillBurn22034", "skillBurn22035", "skillBurn23301", "skillBurn23302", "skillBurn23303", "skillBurn23304", "skillBurn23305", "skillBurn23306", "skillLevel46", "skillLevel47", "skillLevel48", "skillLevel117", "skillLevel118", "skillLevel121", "skillLevel128", "skillLevel134", "skillLevel135", "skillLevel144", "skillLevel180", "skillLevel188", "skillLevel227", "skillLevel245", "skillLevel7254", "slimelingFullness", "slimelingStacksDropped", "slimelingStacksDue", "smoresEaten", "smutOrcNoncombatProgress", "sneakyPetePoints", "snojoMoxieWins", "snojoMuscleWins", "snojoMysticalityWins", "sourceAgentsDefeated", "sourceEnlightenment", "sourceInterval", "sourcePoints", "sourceTerminalGram", "sourceTerminalPram", "sourceTerminalSpam", "soybeanFuturesEaten", "spaceBabyLanguageFluency", "spacePirateLanguageFluency", "spelunkyNextNoncombat", "spelunkySacrifices", "spelunkyWinCount", "spookyPuttyCopiesMade", "spookyVHSTapeMonsterTurn", "statbotUses", "stockCertificateTurn", "sugarCounter4178", "sugarCounter4179", "sugarCounter4180", "sugarCounter4181", "sugarCounter4182", "sugarCounter4183", "sugarCounter4191", "summonAnnoyanceCost", "sweat", "tacoDanCocktailSauce", "tacoDanFishMeat", "takerSpaceAnchor", "takerSpaceGold", "takerSpaceMast", "takerSpaceRum", "takerSpaceSilk", "takerSpaceSpice", "telescopeUpgrades", "tempuraSummons", "timeposedTopHats", "timeSpinnerMedals", "timesRested", "tomeSummons", "totalCharitableDonations", "toxicAssetCharges", "trainsetPosition", "tryToRememberCharges", "turtleBlessingTurns", "twinPeakProgress", "twoCRSPoints", "unicornHornInflation", "universalSeasoningCost", "usable1HWeapons", "usable1xAccs", "usable2HWeapons", "usable3HWeapons", "usableAccessories", "usableHats", "usableOffhands", "usableOther", "usablePants", "usableShirts", "valueOfAdventure", "valueOfInventory", "valueOfStill", "valueOfTome", "vintnerCharge", "vintnerWineLevel", "violetFogGoal", "walfordBucketProgress", "warehouseProgress", "welcomeBackAdv", "wereProfessorBite", "wereProfessorKick", "wereProfessorLiver", "wereProfessorPoints", "wereProfessorRend", "wereProfessorResearchPoints", "wereProfessorStomach", "wereProfessorTransformTurns", "whetstonesUsed", "wolfPigsEvicted", "wolfTurnsUsed", "writingDesksDefeated", "xoSkeleltonXProgress", "xoSkeleltonOProgress", "yearbookCameraAscensions", "yearbookCameraUpgrades", "youRobotBody", "youRobotBottom", "youRobotLeft", "youRobotPoints", "youRobotRight", "youRobotTop", "zeppelinProgress", "zeppelinProtestors", "zigguratLianas", "zombiePoints", "zootSpecimensPrepared", "zootomistPoints", "_absintheDrops", "_abstractionDropsCrown", "_aguaDrops", "_xenomorphCharge", "_alliedRadioDropsUsed", "_ancestralRecallCasts", "_antihangoverBonus", "_aprilShowerDiscoNap", "_aprilBandInstruments", "_aprilBandSaxophoneUses", "_aprilBandTomUses", "_aprilBandTubaUses", "_aprilBandStaffUses", "_aprilBandPiccoloUses", "_archSpadeDigs", "_astralDrops", "_augSkillsCast", "_assertYourAuthorityCast", "_automatedFutureManufactures", "_autumnatonQuests", "_backUpUses", "_badlyRomanticArrows", "_badgerCharge", "_balefulHowlUses", "_banderRunaways", "_baseballInnings", "_bastilleCheese", "_bastilleGames", "_bastilleGameTurn", "_bastilleLastCheese", "_batWingsCauldronUsed", "_batWingsFreeFights", "_batWingsRestUsed", "_batWingsSwoopUsed", "_bczBloodGeyserCasts", "_bczRefractedGazeCasts", "_bczSweatBulletsCasts", "_bczBloodBathCasts", "_bczDialitupCasts", "_bczSweatEquityCasts", "_bczBloodThinnerCasts", "_bczSpinalTapasCasts", "_bczPheromoneCocktailCasts", "_beanCannonUses", "_bearHugs", "_beerLensDrops", "_bellydancerPickpockets", "_benettonsCasts", "_beretBlastUses", "_beretBoastUses", "_beretBuskingUses", "_birdsSoughtToday", "_bookOfFactsWishes", "_bookOfFactsTatters", "_boomBoxFights", "_boomBoxSongsLeft", "_bootStomps", "_boxingGloveArrows", "_brickoEyeSummons", "_brickoFights", "_campAwayCloudBuffs", "_campAwaySmileBuffs", "_candyEggsDeviled", "_candySummons", "_captainHagnkUsed", "_carnieCandyDrops", "_carnivorousPottedPlantWins", "_carrotNoseDrops", "_catBurglarCharge", "_catBurglarHeistsComplete", "_cheerleaderSteam", "_chestXRayUsed", "_chibiAdventures", "_chipBags", "_chocolateCigarsUsed", "_chocolateCoveredPingPongBallsUsed", "_chocolateSculpturesUsed", "_chocolatesUsed", "_chronolithActivations", "_chronolithNextCost", "_cinchUsed", "_cinchoRests", "_circadianRhythmsAdventures", "_clanFortuneConsultUses", "_clipartSummons", "_clocksUsed", "_cloversPurchased", "_clubEmBattlefieldUsed", "_clubEmNextWeekUsed", "_clubEmTimeUsed", "_coldMedicineConsults", "_coldMedicineEquipmentTaken", "_companionshipCasts", "_concoctionDatabaseRefreshes", "_cookbookbatCrafting", "_cookbookbatCombatsUntilNewQuest", "_cosmicBowlingSkillsUsed", "_crimbo21ColdResistance", "_crimboPastDailySpecialPrice", "_cupOf13sCharges", "_cupOf13sDrops", "_cupOf13sJewels", "_cyberFreeFights", "_cyberZone1Turns", "_cyberZone2Turns", "_cyberZone3Turns", "_dailySpecialPrice", "_dartsLeft", "_daycareGymScavenges", "_daycareRecruits", "_deckCardsDrawn", "_deluxeKlawSummons", "_demandSandwich", "_detectiveCasesCompleted", "_disavowed", "_dnaPotionsMade", "_donhosCasts", "_douseFoeUses", "_dreamJarDrops", "_drunkPygmyBanishes", "_durableDolphinWhistleUsed", "_edDefeats", "_edLashCount", "_eldritchTentaclesFoughtToday", "_elfGuardCookingUsed", "_elronsCasts", "_enamorangs", "_energyCollected", "_ensorcelUsed", "_expertCornerCutterUsed", "_experienceSafariUsed", "_extraTimeUsed", "_favorRareSummons", "_feastUsed", "_feelinTheRhythm", "_feelPrideUsed", "_feelExcitementUsed", "_feelHatredUsed", "_feelLonelyUsed", "_feelNervousUsed", "_feelEnvyUsed", "_feelDisappointedUsed", "_feelSuperiorUsed", "_feelLostUsed", "_feelNostalgicUsed", "_feelPeacefulUsed", "_fingertrapArrows", "_fireExtinguisherCharge", "_fitnessTrackingSteps", "_fragrantHerbsUsed", "_freeBeachWalksUsed", "_frButtonsPressed", "_fudgeWaspFights", "_gapBuffs", "_garbageFireDrops", "_garbageFireDropsCrown", "_generateIronyUsed", "_genieFightsUsed", "_genieWishesUsed", "_gibbererAdv", "_gibbererCharge", "_gingerbreadCityTurns", "_glarkCableUses", "_glitchMonsterFights", "_gnomeAdv", "_godLobsterFights", "_goldenMoneyCharge", "_gongDrops", "_gothKidCharge", "_gothKidFights", "_greyYouAdventures", "_grimBrotherCharge", "_grimFairyTaleDrops", "_grimFairyTaleDropsCrown", "_grimoireConfiscatorSummons", "_grimoireGeekySummons", "_grimstoneMaskDrops", "_grimstoneMaskDropsCrown", "_grooseCharge", "_grooseDrops", "_grubbyWoolDrops", "_guzzlrDeliveries", "_guzzlrGoldDeliveries", "_guzzlrPlatinumDeliveries", "_hareAdv", "_hareCharge", "_heartstoneBanishUsed", "_heartstoneBuffUsed", "_heartstoneKillUsed", "_heartstonePalsUsed", "_heartstoneStunUsed", "_highTopPumps", "_hipsterAdv", "_hoardedCandyDropsCrown", "_hoboUnderlingSummons", "_holidayMultitaskingUsed", "_holoWristDrops", "_holoWristProgress", "_hoboFortRestEffectsGained", "_hotAshesDrops", "_hotJellyUses", "_hotTubSoaks", "_humanMuskUses", "_iceballUses", "_inigosCasts", "_itemBoughtPerDay12292", "_itemBoughtPerDay12294", "_itemBoughtPerDay12295", "_itemBoughtPerDay12305", "_itemBoughtPerDay12306", "_itemBoughtPerDay12307", "_itemBoughtPerDay12308", "_itemBoughtPerDay12310", "_itemBoughtPerDay12311", "_itemBoughtPerDay12312", "_ironTricornHeadbuttUsed", "_jerksHealthMagazinesUsed", "_jiggleCheese", "_jiggleCream", "_jiggleLife", "_jiggleSteak", "_jitbCharge", "_juneCleaverAdvs", "_juneCleaverFightsLeft", "_juneCleaverEncounters", "_juneCleaverStench", "_juneCleaverSpooky", "_juneCleaverSleaze", "_juneCleaverHot", "_juneCleaverCold", "_juneCleaverSkips", "_jungDrops", "_kgbClicksUsed", "_kgbDispenserUses", "_kgbTranquilizerDartUses", "_klawSummons", "_kloopCharge", "_kloopDrops", "_knuckleboneDrops", "_knuckleboneRests", "_kolhsAdventures", "_kolhsSavedByTheBell", "_lastDailyDungeonRoom", "_lastFitzsimmonsHatch", "_lastMobiusStripTurn", "_lastSausageMonsterTurn", "_lastZomboEye", "_latteRefillsUsed", "_laughingStockCharges", "_laughingStockFruitDropped", "_lawOfAveragesUsed", "_leafblowerML", "_leafLassosCrafted", "_leafMonstersFought", "_leavesBurned", "_legendaryLasagmbieMana", "_legendaryPastaWandCrafting", "_legendaryVermincelliFreeRats", "_legionJackhammerCrafting", "_leprecondoRearrangements", "_leprecondoFurniture", "_llamaCharge", "_longConUsed", "_lovebugsBeachBuck", "_lovebugsChroner", "_lovebugsCoinspiracy", "_lovebugsFreddy", "_lovebugsFunFunds", "_lovebugsHoboNickel", "_lovebugsWalmart", "_loveChocolatesUsed", "_luckyGoldRingBeachBuck", "_luckyGoldRingBit", "_luckyGoldRingCoinspiracy", "_luckyGoldRingFreddy", "_luckyGoldRingFunFunds", "_luckyGoldRingHoboNickel", "_luckyGoldRingMeat", "_luckyGoldRingRubee", "_luckyGoldRingSandDollar", "_luckyGoldRingVolcoino", "_luckyGoldRingWalmart", "_lynyrdSnareUses", "_machineTunnelsAdv", "_macrometeoriteUses", "_mafiaThumbRingAdvs", "_mapToACandyRichBlockDrops", "_mayamRests", "_mayflowerDrops", "_mayflySummons", "_mcHugeLargeAvalancheUses", "_mcHugeLargeSkiPlowUses", "_mcHugeLargeSlashUses", "_meatCuteUsed", "_meatLoafUsed", "_mediumSiphons", "_meteoriteAdesUsed", "_meteorShowerUses", "_micrometeoriteUses", "_mildEvilPerpetrated", "_mimicEggsDonated", "_mimicEggsObtained", "_miniKiwiDrops", "_miniMartiniDrops", "_mobiusRingPrimedTurn", "_mobiusStripEncounters", "_monkeyPawWishesUsed", "_monsterHabitatsFightsLeft", "_monsterHabitatsRecalled", "_monstersMapped", "_mushroomGardenFights", "_nanorhinoCharge", "_navelRunaways", "_neverendingPartyFreeTurns", "_newYouQuestSharpensDone", "_newYouQuestSharpensToDo", "_nextColdMedicineConsult", "_nextQuantumAlignment", "_nightmareFuelCharges", "_noobSkillCount", "_nuclearStockpileUsed", "_oilExtracted", "_oldSchoolCocktailCraftingUsed", "_olfactionsUsed", "_optimisticCandleDropsCrown", "_oreDropsCrown", "_otoscopeUsed", "_oysterEggsFound", "_pantsgivingBanish", "_pantsgivingCount", "_pantsgivingCrumbs", "_pantsgivingFullness", "_pasteDrops", "_perilsForeseen", "_peteJukeboxFixed", "_peteJumpedShark", "_petePeeledOut", "_photoBoothEffects", "_photoBoothEquipment", "_pieDrops", "_piePartsCount", "_pirateRealmGold", "_pirateRealmGlue", "_pirateRealmGrog", "_pirateRealmGrub", "_pirateRealmGuns", "_pirateRealmIslandMonstersDefeated", "_pirateRealmSailingTurns", "_pirateRealmShipSpeed", "_pixieCharge", "_pocketProfessorLectures", "_poisonArrows", "_pokeGrowFertilizerDrops", "_poolGames", "_powderedGoldDrops", "_powderedMadnessUses", "_powerfulGloveBatteryPowerUsed", "_powerPillDrops", "_powerPillUses", "_precisionCasts", "_pyramidRestEffectsGained", "_questPartyFairItemsOpened", "_radlibSummons", "_raindohCopiesMade", "_rapidPrototypingUsed", "_raveStealCount", "_reflexHammerUsed", "_resolutionAdv", "_resolutionRareSummons", "_riftletAdv", "_robinEggDrops", "_roboDrops", "_rogueProgramCharge", "_romanticFightsLeft", "_saberForceMonsterCount", "_saberForceUses", "_saberMod", "_saltGrainsConsumed", "_sandwormCharge", "_saplingsPlanted", "_sausageFights", "_sausagesEaten", "_sausagesMade", "_seadentLightningUsed", "_sealFigurineUses", "_sealScreeches", "_sealsSummoned", "_shadowBricksUsed", "_shadowRiftCombats", "_shatteringPunchUsed", "_shortOrderCookCharge", "_shrubCharge", "_slimeVialsHarvested", "_sloppyDinerBeachBucks", "_smilesOfMrA", "_smithsnessSummons", "_smolderingSkeletonsDefeated", "_smoochArmyHQCombats", "_snojoFreeFights", "_snojoParts", "_snokebombUsed", "_snowconeSummons", "_snowglobeDrops", "_snowmanHatPlaceUsed", "_snowSuitCount", "_sourceTerminalDigitizeMonsterCount", "_sourceTerminalDigitizeUses", "_sourceTerminalDuplicateUses", "_sourceTerminalEnhanceUses", "_sourceTerminalExtrudes", "_sourceTerminalPortscanUses", "_spaceFurDropsCrown", "_spacegatePlanetIndex", "_spacegateTurnsLeft", "_spaceJellyfishDrops", "_speakeasyDrinksDrunk", "_speakeasyFreeFights", "_spelunkerCharges", "_spelunkingTalesDrops", "_spikolodonSpikeUses", "_spiritOfTheMountainsAdvs", "_spookyJellyUses", "_stackLumpsUses", "_steamCardDrops", "_stickerSummons", "_stinkyCheeseCount", "_stressBallSqueezes", "_sugarSummons", "_summonResortPassesUsed", "_surprisinglySweetSlashUsed", "_surprisinglySweetStabUsed", "_sweatOutSomeBoozeUsed", "_swordOfSWordsKills", "_swordOfSWordsMonsterChanged", "_taffyRareSummons", "_taffyYellowSummons", "_tearawayPantsAdvs", "_thanksgettingFoodsEaten", "_thingfinderCasts", "_thinknerdPackageDrops", "_thorsPliersCrafting", "_timeHelmetAdv", "_timeCopsFoughtToday", "_timeSpinnerMinutesUsed", "_tokenDrops", "_transponderDrops", "_turkeyBlastersUsed", "_turkeyBooze", "_turkeyMuscle", "_turkeyMyst", "_turkeyMoxie", "_unaccompaniedMinerUsed", "_unblemishedPearlAnemoneMineProgress", "_unblemishedPearlDiveBarProgress", "_unblemishedPearlMadnessReefProgress", "_unblemishedPearlMarinaraTrenchProgress", "_unblemishedPearlTheBriniestDeepestsProgress", "_unconsciousCollectiveCharge", "_universalSeasoningsUsed", "_universeCalculated", "_universeImploded", "_usedReplicaBatoomerang", "_vampyreCloakeFormUses", "_villainLairProgress", "_vitachocCapsulesUsed", "_vmaskAdv", "_voidFreeFights", "_volcanoItem1", "_volcanoItem2", "_volcanoItem3", "_volcanoItemCount1", "_volcanoItemCount2", "_volcanoItemCount3", "_voteFreeFights", "_VYKEACompanionLevel", "_wandOfPigificationUsed", "_warbearAutoAnvilCrafting", "_waxGlobDrops", "_whiteRiceDrops", "_witchessFights", "_xoHugsUsed", "_yellowPixelDropsCrown", "_zapCount", "_zombieSmashPocketsUsed", "lastNoncombat15", "lastNoncombat207", "lastNoncombat257", "lastNoncombat270", "lastNoncombat273", "lastNoncombat280", "lastNoncombat283", "lastNoncombat297", "lastNoncombat322", "lastNoncombat323", "lastNoncombat324", "lastNoncombat341", "lastNoncombat343", "lastNoncombat384", "lastNoncombat386", "lastNoncombat391", "lastNoncombat392", "lastNoncombat394", "lastNoncombat405", "lastNoncombat406", "lastNoncombat408", "lastNoncombat439", "lastNoncombat440", "lastNoncombat441", "lastNoncombat450", "lastNoncombat528", "lastNoncombat533", "lastNoncombat539", "lastNoncombat540", "lastNoncombat541", "lastNoncombat588", "lastNoncombat589", "lastNoncombat590", "lastNoncombat591", "lastNoncombat592"];
var monsterProperties = ["beGregariousMonster", "bodyguardChatMonster", "cameraMonster", "chateauMonster", "clubEmNextWeekMonster", "clumsinessGroveBoss", "crappyCameraMonster", "crudeMonster", "enamorangMonster", "envyfishMonster", "glacierOfJerksBoss", "holdHandsMonster", "iceSculptureMonster", "lastCopyableMonster", "lastBlueVsRedNCMonster", "longConMonster", "maelstromOfLoversBoss", "makeFriendsMonster", "merkinLockkeyMonster", "monkeyPointMonster", "motifMonster", "nosyNoseMonster", "olfactedMonster", "photocopyMonster", "rainDohMonster", "romanticTarget", "rufusDesiredEntity", "rwbMonster", "screencappedMonster", "shrunkenHeadZombieMonster", "spookyPuttyMonster", "spookyVHSTapeMonster", "stenchCursedMonster", "superficiallyInterestedMonster", "swordOfSWordsMonster", "waxMonster", "yearbookCameraTarget", "_afterimageMonster", "_beanballMonster", "_chainedRelativityMonster", "_chainedPurpleCandleMonster", "_chainedAfterimageMonster", "_cookbookbatQuestMonster", "_curveballMonster", "_gallapagosMonster", "_jiggleCreamedMonster", "_latteMonster", "_monsterHabitatsMonster", "_nanorhinoBanishedMonster", "_newYouQuestMonster", "_prankCardMonster", "_relativityMonster", "_saberForceMonster", "_screwballMonster", "_skullballMonster", "_sourceTerminalDigitizeMonster", "_trickCoinMonster", "_voteMonster"];
var monsterNumericProperties = ["lastBlueVsRedNCMonster", "swordOfSWordsMonster"];
var locationProperties = ["autumnatonQuestLocation", "currentJunkyardLocation", "doctorBagQuestLocation", "ghostLocation", "guzzlrQuestLocation", "holdHandsLocation", "lastAdventure", "nextAdventure", "nextSpookyravenElizabethRoom", "nextSpookyravenStephenRoom", "rwbLocation", "sourceOracleTarget", "_citizenZone", "_cookbookbatQuestLastLocation", "_floundryBassLocation", "_floundryCarpLocation", "_floundryCodLocation", "_floundryHatchetfishLocation", "_floundryTroutLocation", "_floundryTunaLocation", "_lastPirateRealmIsland", "_seadentWaveZone", "_sotParcelLocation"];
var stringProperties = ["autoLogin", "browserBookmarks", "chatFontSize", "combatHotkey0", "combatHotkey1", "combatHotkey2", "combatHotkey3", "combatHotkey4", "combatHotkey5", "combatHotkey6", "combatHotkey7", "combatHotkey8", "combatHotkey9", "commandBufferGCLI", "commandBufferTabbedChat", "commandLineNamespace", "dailyDeedsOptions", "defaultBorderColor", "displayName", "externalEditor", "getBreakfast", "gitConflictPriority", "headerStates", "highlightList", "http.proxyHost", "http.proxyPassword", "http.proxyPort", "http.proxyUser", "https.proxyHost", "https.proxyPassword", "https.proxyPort", "https.proxyUser", "initialDesktop", "initialFrames", "lastRelayUpdate", "lastUserAgent", "lastUsername", "logPreferenceChangeFilter", "loginScript", "loginServerName", "loginWindowLogo", "logoutScript", "pingDefaultTestPage", "pingLatest", "previousNotifyList", "previousUpdateVersion", "saveState", "saveStateActive", "scriptList", "swingLookAndFeel", "userAgent", "8BitColor", "afterAdventureScript", "antiScientificMethod", "autoOlfact", "autoPutty", "autumnatonUpgrades", "backupCameraMode", "banishedMonsters", "banishedPhyla", "banishingShoutMonsters", "baseballTeam", "batmanStats", "batmanZone", "batmanUpgrades", "battleAction", "beachHeadsUnlocked", "beastSkillsAvailable", "beastSkillsKnown", "beforePVPScript", "betweenBattleScript", "blueVsRedTeam", "boomBoxSong", "breakfastAlways", "breakfastHardcore", "breakfastSoftcore", "buffBotCasting", "buyScript", "cargoPocketsEmptied", "cargoPocketScraps", "chatbotScript", "chatPlayerScript", "chibiName", "choiceAdventureScript", "chosenTrip", "clanFortuneReply1", "clanFortuneReply2", "clanFortuneReply3", "clanFortuneWord1", "clanFortuneWord2", "clanFortuneWord3", "coolerYetiMode", "counterScript", "copperheadClubHazard", "crimbo23ArmoryControl", "crimbo23BarControl", "crimbo23CafeControl", "crimbo23CottageControl", "crimbo23FoundryControl", "crimbotChassis", "crimbotArm", "crimbotPropulsion", "crystalBallPredictions", "csServicesPerformed", "currentAstralTrip", "currentDistillateMods", "currentEasyBountyItem", "currentHardBountyItem", "currentHippyStore", "currentJunkyardTool", "currentLlamaForm", "currentMood", "currentPVPSeason", "currentPvpVictories", "currentSpecialBountyItem", "currentSITSkill", "customCombatScript", "cyrusAdjectives", "dailyDungeonRooms", "defaultFlowerLossMessage", "defaultFlowerWinMessage", "demonName1", "demonName2", "demonName3", "demonName4", "demonName5", "demonName6", "demonName7", "demonName8", "demonName9", "demonName10", "demonName11", "demonName12", "demonName13", "demonName14", "demonName14Segments", "dinseyGatorStenchDamage", "dinseyRollercoasterStats", "dreadScrollGuesses", "duckAreasCleared", "duckAreasSelected", "edPiece", "enamorangMonsterTurn", "ensorcelee", "EVEDirections", "everfullDartPerks", "extraCosmeticModifiers", "familiarScript", "flagellateFlagonsActive", "forbiddenStores", "gameProBossSpecialPower", "gooseReprocessed", "grimoireSkillsHardcore", "grimoireSkillsSoftcore", "grimstoneMaskPath", "guzzlrQuestClient", "guzzlrQuestTier", "harvestGardenHardcore", "harvestGardenSoftcore", "heartstoneAttunementMods", "heartstoneAttunementWord", "heartstoneLetters", "holdHandsMonsterCount", "hpAutoRecoveryItems", "invalidBuffMessage", "jickSwordModifier", "juneCleaverQueue", "kingLiberatedScript", "lassoTraining", "lastAdventureContainer", "lastAdventureTrail", "lastBangPotion819", "lastBangPotion820", "lastBangPotion821", "lastBangPotion822", "lastBangPotion823", "lastBangPotion824", "lastBangPotion825", "lastBangPotion826", "lastBangPotion827", "lastChanceBurn", "lastChessboard", "lastCombatEnvironments", "lastDwarfDiceRolls", "lastDwarfDigitRunes", "lastDwarfEquipmentRunes", "lastDwarfFactoryItem118", "lastDwarfFactoryItem119", "lastDwarfFactoryItem120", "lastDwarfFactoryItem360", "lastDwarfFactoryItem361", "lastDwarfFactoryItem362", "lastDwarfFactoryItem363", "lastDwarfFactoryItem364", "lastDwarfFactoryItem365", "lastDwarfFactoryItem910", "lastDwarfFactoryItem3199", "lastDwarfOfficeItem3208", "lastDwarfOfficeItem3209", "lastDwarfOfficeItem3210", "lastDwarfOfficeItem3211", "lastDwarfOfficeItem3212", "lastDwarfOfficeItem3213", "lastDwarfOfficeItem3214", "lastDwarfOreRunes", "lastDwarfHopper1", "lastDwarfHopper2", "lastDwarfHopper3", "lastDwarfHopper4", "lastEncounter", "lastMacroError", "lastMessageId", "lastPaperStrip3144", "lastPaperStrip4138", "lastPaperStrip4139", "lastPaperStrip4140", "lastPaperStrip4141", "lastPaperStrip4142", "lastPaperStrip4143", "lastPaperStrip4144", "lastPirateEphemera", "lastPorkoBoard", "lastPorkoPayouts", "lastPorkoExpected", "lastSlimeVial3885", "lastSlimeVial3886", "lastSlimeVial3887", "lastSlimeVial3888", "lastSlimeVial3889", "lastSlimeVial3890", "lastSlimeVial3891", "lastSlimeVial3892", "lastSlimeVial3893", "lastSlimeVial3894", "lastSlimeVial3895", "lastSlimeVial3896", "lastSelectedFaxbot", "lastSuccessfulFaxbot", "latteIngredients", "latteModifier", "latteUnlocks", "ledCandleMode", "leprecondoCurrentNeed", "leprecondoDiscovered", "leprecondoInstalled", "leprecondoNeedOrder", "libramSkillsHardcore", "libramSkillsSoftcore", "louvreOverride", "lovePotion", "lttQuestName", "maximizerList", "maximizerMRUList", "maximizerLastFilters", "mayoInMouth", "mayoMinderSetting", "merkinCatalogChoices", "merkinQuestPath", "mimicEggMonsters", "mineLayout1", "mineLayout2", "mineLayout3", "mineLayout4", "mineLayout5", "mineLayout6", "mineState1", "mineState2", "mineState3", "mineState4", "mineState5", "mineState6", "mpAutoRecoveryItems", "nextDistillateMods", "nextQuantumFamiliarName", "nextQuantumFamiliarOwner", "noncombatForcers", "nsChallenge2", "nsChallenge3", "nsChallenge4", "nsChallenge5", "nsTowerDoorKeysUsed", "oceanAction", "oceanDestination", "parkaMode", "pastaThrall1", "pastaThrall2", "pastaThrall3", "pastaThrall4", "pastaThrall5", "pastaThrall6", "pastaThrall7", "pastaThrall8", "peteMotorbikeTires", "peteMotorbikeGasTank", "peteMotorbikeHeadlight", "peteMotorbikeCowling", "peteMotorbikeMuffler", "peteMotorbikeSeat", "pieStuffing", "plantingDate", "plantingLength", "plantingScript", "plumberCostumeWorn", "pokefamBoosts", "postAscensionScript", "preAscensionScript", "questClumsinessGrove", "questDoctorBag", "questECoBucket", "questESlAudit", "questESlBacteria", "questESlCheeseburger", "questESlCocktail", "questESlDebt", "questESlFish", "questESlMushStash", "questESlSalt", "questESlSprinkles", "questESpClipper", "questESpEVE", "questESpFakeMedium", "questESpGore", "questESpJunglePun", "questESpOutOfOrder", "questESpSerum", "questESpSmokes", "questEStFishTrash", "questEStGiveMeFuel", "questEStNastyBears", "questEStSocialJusticeI", "questEStSocialJusticeII", "questEStSuperLuber", "questEStWorkWithFood", "questEStZippityDooDah", "questEUNewYou", "questF01Primordial", "questF02Hyboria", "questF03Future", "questF04Elves", "questF05Clancy", "questG01Meatcar", "questG02Whitecastle", "questG03Ego", "questG04Nemesis", "questG05Dark", "questG06Delivery", "questG07Myst", "questG08Moxie", "questG09Muscle", "questGlacierOfJerks", "questGuzzlr", "questI01Scapegoat", "questI02Beat", "questL02Larva", "questL03Rat", "questL04Bat", "questL05Goblin", "questL06Friar", "questL07Cyrptic", "questL08Trapper", "questL09Topping", "questL10Garbage", "questL11Black", "questL11Business", "questL11Curses", "questL11Desert", "questL11Doctor", "questL11MacGuffin", "questL11Manor", "questL11Palindome", "questL11Pyramid", "questL11Ron", "questL11Shen", "questL11Spare", "questL11Worship", "questL12HippyFrat", "questL12War", "questL13Final", "questL13Warehouse", "questLTTQuestByWire", "questM01Untinker", "questM02Artist", "questM03Bugbear", "questM05Toot", "questM06Gourd", "questM07Hammer", "questM08Baker", "questM09Rocks", "questM10Azazel", "questM11Postal", "questM12Pirate", "questM13Escape", "questM14Bounty", "questM15Lol", "questM16Temple", "questM17Babies", "questM18Swamp", "questM19Hippy", "questM20Necklace", "questM21Dance", "questM22Shirt", "questM23Meatsmith", "questM24Doc", "questM25Armorer", "questM26Oracle", "questMaelstromOfLovers", "questPAGhost", "questRufus", "questS01OldGuy", "questS02Monkees", "raveCombo1", "raveCombo2", "raveCombo3", "raveCombo4", "raveCombo5", "raveCombo6", "recoveryScript", "relayChatCLITrigger", "relayCounters", "retroCapeSuperhero", "retroCapeWashingInstructions", "royalty", "rufusQuestTarget", "rufusQuestType", "scriptMRUList", "seahorseName", "shadowLabyrinthGoal", "shadowRiftIngress", "shrubGarland", "shrubGifts", "shrubLights", "shrubTopper", "shrunkenHeadZombieAbilities", "sideDefeated", "sidequestArenaCompleted", "sidequestFarmCompleted", "sidequestJunkyardCompleted", "sidequestLighthouseCompleted", "sidequestNunsCompleted", "sidequestOrchardCompleted", "skateParkStatus", "snowsuit", "sourceTerminalChips", "sourceTerminalEducate1", "sourceTerminalEducate2", "sourceTerminalEnquiry", "sourceTerminalEducateKnown", "sourceTerminalEnhanceKnown", "sourceTerminalEnquiryKnown", "sourceTerminalExtrudeKnown", "spadingData", "spadingScript", "speakeasyName", "spelunkyStatus", "spelunkyUpgrades", "spookyravenRecipeUsed", "stationaryButton1", "stationaryButton2", "stationaryButton3", "stationaryButton4", "stationaryButton5", "stockCertificateTurns", "streamCrossDefaultTarget", "sweetSynthesisBlacklist", "tavernLayout", "telescope1", "telescope2", "telescope3", "telescope4", "telescope5", "testudinalTeachings", "textColors", "thanksMessage", "tomeSkillsHardcore", "tomeSkillsSoftcore", "trackVoteMonster", "trackedMonsters", "trackedPhyla", "trainsetConfiguration", "umbrellaState", "umdLastObtained", "vintnerWineEffect", "vintnerWineName", "vintnerWineType", "violetFogLayout", "volcanoMaze1", "volcanoMaze2", "volcanoMaze3", "volcanoMaze4", "volcanoMaze5", "walfordBucketItem", "warProgress", "watchedPreferences", "wereProfessorAdvancedResearch", "workteaClue", "yourFavoriteBird", "yourFavoriteBirdMods", "youRobotCPUUpgrades", "zootGraftedMods", "zootMilkCrueltyMods", "zootMilkKindnessMods", "_automatedFutureSide", "_bastilleBoosts", "_bastilleChoice1", "_bastilleChoice2", "_bastilleChoice3", "_bastilleCurrentStyles", "_bastilleEnemyCastle", "_bastilleEnemyName", "_bastilleLastBattleResults", "_bastilleLastEncounter", "_bastilleStats", "_beachHeadsUsed", "_beachLayout", "_beachMinutes", "_birdOfTheDay", "_birdOfTheDayMods", "_bittycar", "_campAwaySmileBuffSign", "_citizenZoneMods", "_cloudTalkMessage", "_cloudTalkSmoker", "_coatOfPaintModifier", "_cupidBowFamiliars", "_currentDartboard", "_curveballFightsLeft", "_cyberZone1Defense", "_cyberZone1Hacker", "_cyberZone1Owner", "_cyberZone2Defense", "_cyberZone2Hacker", "_cyberZone2Owner", "_cyberZone3Defense", "_cyberZone3Hacker", "_cyberZone3Owner", "_deckCardsSeen", "_feastedFamiliars", "_floristPlantsUsed", "_frAreasUnlocked", "_frHoursLeft", "_frMonstersKilled", "_futuristicCollarModifier", "_futuristicHatModifier", "_futuristicShirtModifier", "_horsery", "_horseryCrazyMox", "_horseryCrazyMus", "_horseryCrazyMys", "_horseryCrazyName", "_horseryCurrentName", "_horseryDarkName", "_horseryNormalName", "_horseryPaleName", "_jickJarAvailable", "_jiggleCheesedMonsters", "_lastCombatActions", "_lastCombatStarted", "_locketMonstersFought", "_mayamSymbolsUsed", "_mummeryMods", "_mummeryUses", "_newYouQuestSkill", "_noHatModifier", "_pantogramModifier", "_perilLocations", "_pirateRealmCrewmate", "_pirateRealmCrewmate1", "_pirateRealmCrewmate2", "_pirateRealmCrewmate3", "_pirateRealmShip", "_pottedPowerPlant", "_questESp", "_questPartyFair", "_questPartyFairProgress", "_questPartyFairQuest", "_questPirateRealm", "_roboDrinks", "_roninStoragePulls", "_savageBeastMods", "_spacegateAnimalLife", "_spacegateCoordinates", "_spacegateGear", "_spacegateHazards", "_spacegateIntelligentLife", "_spacegatePlanetName", "_spacegatePlantLife", "_stolenAccordions", "_tempRelayCounters", "_timeSpinnerFoodAvailable", "_trickOrTreatBlock", "_unknownEasyBountyItem", "_unknownHardBountyItem", "_unknownSpecialBountyItem", "_untakenEasyBountyItem", "_untakenHardBountyItem", "_untakenSpecialBountyItem", "_userMods", "_villainLairColor", "_villainLairKey", "_voteLocal1", "_voteLocal2", "_voteLocal3", "_voteLocal4", "_voteMonster1", "_voteMonster2", "_voteModifier", "_VYKEACompanionType", "_VYKEACompanionRune", "_VYKEACompanionName"];
var numericOrStringProperties = ["statusEngineering", "statusGalley", "statusMedbay", "statusMorgue", "statusNavigation", "statusScienceLab", "statusSonar", "statusSpecialOps", "statusWasteProcessing", "choiceAdventure2", "choiceAdventure3", "choiceAdventure4", "choiceAdventure5", "choiceAdventure6", "choiceAdventure7", "choiceAdventure8", "choiceAdventure9", "choiceAdventure10", "choiceAdventure11", "choiceAdventure12", "choiceAdventure14", "choiceAdventure15", "choiceAdventure16", "choiceAdventure17", "choiceAdventure18", "choiceAdventure19", "choiceAdventure20", "choiceAdventure21", "choiceAdventure22", "choiceAdventure23", "choiceAdventure24", "choiceAdventure25", "choiceAdventure26", "choiceAdventure27", "choiceAdventure28", "choiceAdventure29", "choiceAdventure40", "choiceAdventure41", "choiceAdventure42", "choiceAdventure45", "choiceAdventure46", "choiceAdventure47", "choiceAdventure71", "choiceAdventure72", "choiceAdventure73", "choiceAdventure74", "choiceAdventure75", "choiceAdventure76", "choiceAdventure77", "choiceAdventure86", "choiceAdventure87", "choiceAdventure88", "choiceAdventure89", "choiceAdventure90", "choiceAdventure91", "choiceAdventure105", "choiceAdventure106", "choiceAdventure107", "choiceAdventure108", "choiceAdventure109", "choiceAdventure110", "choiceAdventure111", "choiceAdventure112", "choiceAdventure113", "choiceAdventure114", "choiceAdventure115", "choiceAdventure116", "choiceAdventure117", "choiceAdventure118", "choiceAdventure120", "choiceAdventure123", "choiceAdventure125", "choiceAdventure126", "choiceAdventure127", "choiceAdventure129", "choiceAdventure131", "choiceAdventure132", "choiceAdventure135", "choiceAdventure136", "choiceAdventure137", "choiceAdventure138", "choiceAdventure139", "choiceAdventure140", "choiceAdventure141", "choiceAdventure142", "choiceAdventure143", "choiceAdventure144", "choiceAdventure145", "choiceAdventure146", "choiceAdventure147", "choiceAdventure148", "choiceAdventure149", "choiceAdventure151", "choiceAdventure152", "choiceAdventure153", "choiceAdventure154", "choiceAdventure155", "choiceAdventure156", "choiceAdventure157", "choiceAdventure158", "choiceAdventure159", "choiceAdventure160", "choiceAdventure161", "choiceAdventure162", "choiceAdventure163", "choiceAdventure164", "choiceAdventure165", "choiceAdventure166", "choiceAdventure167", "choiceAdventure168", "choiceAdventure169", "choiceAdventure170", "choiceAdventure171", "choiceAdventure172", "choiceAdventure177", "choiceAdventure178", "choiceAdventure180", "choiceAdventure181", "choiceAdventure182", "choiceAdventure184", "choiceAdventure185", "choiceAdventure186", "choiceAdventure187", "choiceAdventure188", "choiceAdventure189", "choiceAdventure191", "choiceAdventure197", "choiceAdventure198", "choiceAdventure199", "choiceAdventure200", "choiceAdventure201", "choiceAdventure202", "choiceAdventure203", "choiceAdventure204", "choiceAdventure205", "choiceAdventure206", "choiceAdventure207", "choiceAdventure208", "choiceAdventure211", "choiceAdventure212", "choiceAdventure213", "choiceAdventure214", "choiceAdventure215", "choiceAdventure216", "choiceAdventure217", "choiceAdventure218", "choiceAdventure219", "choiceAdventure220", "choiceAdventure221", "choiceAdventure222", "choiceAdventure223", "choiceAdventure224", "choiceAdventure225", "choiceAdventure230", "choiceAdventure272", "choiceAdventure273", "choiceAdventure276", "choiceAdventure277", "choiceAdventure278", "choiceAdventure279", "choiceAdventure280", "choiceAdventure281", "choiceAdventure282", "choiceAdventure283", "choiceAdventure284", "choiceAdventure285", "choiceAdventure286", "choiceAdventure287", "choiceAdventure288", "choiceAdventure289", "choiceAdventure290", "choiceAdventure291", "choiceAdventure292", "choiceAdventure293", "choiceAdventure294", "choiceAdventure295", "choiceAdventure296", "choiceAdventure297", "choiceAdventure298", "choiceAdventure299", "choiceAdventure302", "choiceAdventure303", "choiceAdventure304", "choiceAdventure305", "choiceAdventure306", "choiceAdventure307", "choiceAdventure308", "choiceAdventure309", "choiceAdventure310", "choiceAdventure311", "choiceAdventure317", "choiceAdventure318", "choiceAdventure319", "choiceAdventure320", "choiceAdventure321", "choiceAdventure322", "choiceAdventure326", "choiceAdventure327", "choiceAdventure328", "choiceAdventure329", "choiceAdventure330", "choiceAdventure331", "choiceAdventure332", "choiceAdventure333", "choiceAdventure334", "choiceAdventure335", "choiceAdventure336", "choiceAdventure337", "choiceAdventure338", "choiceAdventure339", "choiceAdventure340", "choiceAdventure341", "choiceAdventure342", "choiceAdventure343", "choiceAdventure344", "choiceAdventure345", "choiceAdventure346", "choiceAdventure347", "choiceAdventure348", "choiceAdventure349", "choiceAdventure350", "choiceAdventure351", "choiceAdventure352", "choiceAdventure353", "choiceAdventure354", "choiceAdventure355", "choiceAdventure356", "choiceAdventure357", "choiceAdventure358", "choiceAdventure360", "choiceAdventure361", "choiceAdventure362", "choiceAdventure363", "choiceAdventure364", "choiceAdventure365", "choiceAdventure366", "choiceAdventure367", "choiceAdventure372", "choiceAdventure376", "choiceAdventure387", "choiceAdventure388", "choiceAdventure389", "choiceAdventure390", "choiceAdventure391", "choiceAdventure392", "choiceAdventure393", "choiceAdventure395", "choiceAdventure396", "choiceAdventure397", "choiceAdventure398", "choiceAdventure399", "choiceAdventure400", "choiceAdventure401", "choiceAdventure402", "choiceAdventure403", "choiceAdventure423", "choiceAdventure424", "choiceAdventure425", "choiceAdventure426", "choiceAdventure427", "choiceAdventure428", "choiceAdventure429", "choiceAdventure430", "choiceAdventure431", "choiceAdventure432", "choiceAdventure433", "choiceAdventure435", "choiceAdventure438", "choiceAdventure439", "choiceAdventure442", "choiceAdventure444", "choiceAdventure445", "choiceAdventure446", "choiceAdventure447", "choiceAdventure448", "choiceAdventure449", "choiceAdventure451", "choiceAdventure452", "choiceAdventure453", "choiceAdventure454", "choiceAdventure455", "choiceAdventure456", "choiceAdventure457", "choiceAdventure458", "choiceAdventure460", "choiceAdventure461", "choiceAdventure462", "choiceAdventure463", "choiceAdventure464", "choiceAdventure465", "choiceAdventure467", "choiceAdventure468", "choiceAdventure469", "choiceAdventure470", "choiceAdventure471", "choiceAdventure472", "choiceAdventure473", "choiceAdventure474", "choiceAdventure475", "choiceAdventure477", "choiceAdventure478", "choiceAdventure480", "choiceAdventure483", "choiceAdventure484", "choiceAdventure485", "choiceAdventure486", "choiceAdventure488", "choiceAdventure489", "choiceAdventure490", "choiceAdventure491", "choiceAdventure496", "choiceAdventure497", "choiceAdventure502", "choiceAdventure503", "choiceAdventure504", "choiceAdventure505", "choiceAdventure506", "choiceAdventure507", "choiceAdventure509", "choiceAdventure510", "choiceAdventure511", "choiceAdventure512", "choiceAdventure513", "choiceAdventure514", "choiceAdventure515", "choiceAdventure517", "choiceAdventure518", "choiceAdventure519", "choiceAdventure521", "choiceAdventure522", "choiceAdventure523", "choiceAdventure527", "choiceAdventure528", "choiceAdventure529", "choiceAdventure530", "choiceAdventure531", "choiceAdventure532", "choiceAdventure533", "choiceAdventure534", "choiceAdventure535", "choiceAdventure536", "choiceAdventure538", "choiceAdventure539", "choiceAdventure542", "choiceAdventure543", "choiceAdventure544", "choiceAdventure546", "choiceAdventure548", "choiceAdventure549", "choiceAdventure550", "choiceAdventure551", "choiceAdventure552", "choiceAdventure553", "choiceAdventure554", "choiceAdventure556", "choiceAdventure557", "choiceAdventure558", "choiceAdventure559", "choiceAdventure560", "choiceAdventure561", "choiceAdventure562", "choiceAdventure563", "choiceAdventure564", "choiceAdventure565", "choiceAdventure566", "choiceAdventure567", "choiceAdventure568", "choiceAdventure569", "choiceAdventure571", "choiceAdventure572", "choiceAdventure573", "choiceAdventure574", "choiceAdventure575", "choiceAdventure576", "choiceAdventure577", "choiceAdventure578", "choiceAdventure579", "choiceAdventure581", "choiceAdventure582", "choiceAdventure583", "choiceAdventure584", "choiceAdventure594", "choiceAdventure595", "choiceAdventure596", "choiceAdventure597", "choiceAdventure598", "choiceAdventure599", "choiceAdventure600", "choiceAdventure603", "choiceAdventure604", "choiceAdventure616", "choiceAdventure634", "choiceAdventure640", "choiceAdventure654", "choiceAdventure655", "choiceAdventure656", "choiceAdventure657", "choiceAdventure658", "choiceAdventure664", "choiceAdventure669", "choiceAdventure670", "choiceAdventure671", "choiceAdventure672", "choiceAdventure673", "choiceAdventure674", "choiceAdventure675", "choiceAdventure676", "choiceAdventure677", "choiceAdventure678", "choiceAdventure679", "choiceAdventure681", "choiceAdventure683", "choiceAdventure684", "choiceAdventure685", "choiceAdventure686", "choiceAdventure687", "choiceAdventure688", "choiceAdventure689", "choiceAdventure690", "choiceAdventure691", "choiceAdventure692", "choiceAdventure693", "choiceAdventure694", "choiceAdventure695", "choiceAdventure696", "choiceAdventure697", "choiceAdventure698", "choiceAdventure700", "choiceAdventure701", "choiceAdventure705", "choiceAdventure706", "choiceAdventure707", "choiceAdventure708", "choiceAdventure709", "choiceAdventure710", "choiceAdventure711", "choiceAdventure712", "choiceAdventure713", "choiceAdventure714", "choiceAdventure715", "choiceAdventure716", "choiceAdventure717", "choiceAdventure721", "choiceAdventure725", "choiceAdventure729", "choiceAdventure733", "choiceAdventure737", "choiceAdventure741", "choiceAdventure745", "choiceAdventure749", "choiceAdventure753", "choiceAdventure771", "choiceAdventure778", "choiceAdventure780", "choiceAdventure781", "choiceAdventure783", "choiceAdventure784", "choiceAdventure785", "choiceAdventure786", "choiceAdventure787", "choiceAdventure788", "choiceAdventure789", "choiceAdventure791", "choiceAdventure793", "choiceAdventure794", "choiceAdventure795", "choiceAdventure796", "choiceAdventure797", "choiceAdventure803", "choiceAdventure805", "choiceAdventure808", "choiceAdventure809", "choiceAdventure813", "choiceAdventure815", "choiceAdventure830", "choiceAdventure832", "choiceAdventure833", "choiceAdventure834", "choiceAdventure835", "choiceAdventure837", "choiceAdventure838", "choiceAdventure839", "choiceAdventure840", "choiceAdventure841", "choiceAdventure842", "choiceAdventure851", "choiceAdventure852", "choiceAdventure853", "choiceAdventure854", "choiceAdventure855", "choiceAdventure856", "choiceAdventure857", "choiceAdventure858", "choiceAdventure866", "choiceAdventure873", "choiceAdventure875", "choiceAdventure876", "choiceAdventure877", "choiceAdventure878", "choiceAdventure879", "choiceAdventure880", "choiceAdventure881", "choiceAdventure882", "choiceAdventure888", "choiceAdventure889", "choiceAdventure918", "choiceAdventure919", "choiceAdventure920", "choiceAdventure921", "choiceAdventure923", "choiceAdventure924", "choiceAdventure925", "choiceAdventure926", "choiceAdventure927", "choiceAdventure928", "choiceAdventure929", "choiceAdventure930", "choiceAdventure931", "choiceAdventure932", "choiceAdventure940", "choiceAdventure941", "choiceAdventure942", "choiceAdventure943", "choiceAdventure944", "choiceAdventure945", "choiceAdventure946", "choiceAdventure950", "choiceAdventure955", "choiceAdventure957", "choiceAdventure958", "choiceAdventure959", "choiceAdventure960", "choiceAdventure961", "choiceAdventure962", "choiceAdventure963", "choiceAdventure964", "choiceAdventure965", "choiceAdventure966", "choiceAdventure970", "choiceAdventure973", "choiceAdventure974", "choiceAdventure975", "choiceAdventure976", "choiceAdventure977", "choiceAdventure979", "choiceAdventure980", "choiceAdventure981", "choiceAdventure982", "choiceAdventure983", "choiceAdventure988", "choiceAdventure989", "choiceAdventure993", "choiceAdventure998", "choiceAdventure1000", "choiceAdventure1003", "choiceAdventure1005", "choiceAdventure1006", "choiceAdventure1007", "choiceAdventure1008", "choiceAdventure1009", "choiceAdventure1010", "choiceAdventure1011", "choiceAdventure1012", "choiceAdventure1013", "choiceAdventure1015", "choiceAdventure1016", "choiceAdventure1017", "choiceAdventure1018", "choiceAdventure1019", "choiceAdventure1020", "choiceAdventure1021", "choiceAdventure1022", "choiceAdventure1023", "choiceAdventure1026", "choiceAdventure1027", "choiceAdventure1028", "choiceAdventure1029", "choiceAdventure1030", "choiceAdventure1031", "choiceAdventure1032", "choiceAdventure1033", "choiceAdventure1034", "choiceAdventure1035", "choiceAdventure1036", "choiceAdventure1037", "choiceAdventure1038", "choiceAdventure1039", "choiceAdventure1040", "choiceAdventure1041", "choiceAdventure1042", "choiceAdventure1044", "choiceAdventure1045", "choiceAdventure1046", "choiceAdventure1048", "choiceAdventure1051", "choiceAdventure1052", "choiceAdventure1053", "choiceAdventure1054", "choiceAdventure1055", "choiceAdventure1056", "choiceAdventure1057", "choiceAdventure1059", "choiceAdventure1060", "choiceAdventure1061", "choiceAdventure1062", "choiceAdventure1065", "choiceAdventure1067", "choiceAdventure1068", "choiceAdventure1069", "choiceAdventure1070", "choiceAdventure1071", "choiceAdventure1073", "choiceAdventure1077", "choiceAdventure1080", "choiceAdventure1081", "choiceAdventure1082", "choiceAdventure1083", "choiceAdventure1084", "choiceAdventure1085", "choiceAdventure1091", "choiceAdventure1094", "choiceAdventure1095", "choiceAdventure1096", "choiceAdventure1097", "choiceAdventure1102", "choiceAdventure1106", "choiceAdventure1107", "choiceAdventure1108", "choiceAdventure1110", "choiceAdventure1114", "choiceAdventure1115", "choiceAdventure1116", "choiceAdventure1118", "choiceAdventure1119", "choiceAdventure1120", "choiceAdventure1121", "choiceAdventure1122", "choiceAdventure1123", "choiceAdventure1171", "choiceAdventure1172", "choiceAdventure1173", "choiceAdventure1174", "choiceAdventure1175", "choiceAdventure1193", "choiceAdventure1195", "choiceAdventure1196", "choiceAdventure1197", "choiceAdventure1198", "choiceAdventure1199", "choiceAdventure1202", "choiceAdventure1203", "choiceAdventure1204", "choiceAdventure1205", "choiceAdventure1206", "choiceAdventure1207", "choiceAdventure1208", "choiceAdventure1209", "choiceAdventure1210", "choiceAdventure1211", "choiceAdventure1212", "choiceAdventure1213", "choiceAdventure1214", "choiceAdventure1215", "choiceAdventure1219", "choiceAdventure1222", "choiceAdventure1223", "choiceAdventure1224", "choiceAdventure1225", "choiceAdventure1226", "choiceAdventure1227", "choiceAdventure1228", "choiceAdventure1229", "choiceAdventure1236", "choiceAdventure1237", "choiceAdventure1238", "choiceAdventure1239", "choiceAdventure1240", "choiceAdventure1241", "choiceAdventure1242", "choiceAdventure1243", "choiceAdventure1244", "choiceAdventure1245", "choiceAdventure1246", "choiceAdventure1247", "choiceAdventure1248", "choiceAdventure1249", "choiceAdventure1250", "choiceAdventure1251", "choiceAdventure1252", "choiceAdventure1253", "choiceAdventure1254", "choiceAdventure1255", "choiceAdventure1256", "choiceAdventure1266", "choiceAdventure1280", "choiceAdventure1281", "choiceAdventure1282", "choiceAdventure1283", "choiceAdventure1284", "choiceAdventure1285", "choiceAdventure1286", "choiceAdventure1287", "choiceAdventure1288", "choiceAdventure1289", "choiceAdventure1290", "choiceAdventure1291", "choiceAdventure1292", "choiceAdventure1293", "choiceAdventure1294", "choiceAdventure1295", "choiceAdventure1296", "choiceAdventure1297", "choiceAdventure1298", "choiceAdventure1299", "choiceAdventure1300", "choiceAdventure1301", "choiceAdventure1302", "choiceAdventure1303", "choiceAdventure1304", "choiceAdventure1305", "choiceAdventure1307", "choiceAdventure1310", "choiceAdventure1312", "choiceAdventure1313", "choiceAdventure1314", "choiceAdventure1315", "choiceAdventure1316", "choiceAdventure1317", "choiceAdventure1318", "choiceAdventure1319", "choiceAdventure1321", "choiceAdventure1322", "choiceAdventure1323", "choiceAdventure1324", "choiceAdventure1325", "choiceAdventure1326", "choiceAdventure1327", "choiceAdventure1328", "choiceAdventure1332", "choiceAdventure1333", "choiceAdventure1335", "choiceAdventure1340", "choiceAdventure1341", "choiceAdventure1345", "choiceAdventure1389", "choiceAdventure1392", "choiceAdventure1397", "choiceAdventure1399", "choiceAdventure1405", "choiceAdventure1411", "choiceAdventure1415", "choiceAdventure1427", "choiceAdventure1428", "choiceAdventure1429", "choiceAdventure1430", "choiceAdventure1431", "choiceAdventure1432", "choiceAdventure1433", "choiceAdventure1434", "choiceAdventure1436", "choiceAdventure1460", "choiceAdventure1461", "choiceAdventure1467", "choiceAdventure1468", "choiceAdventure1469", "choiceAdventure1470", "choiceAdventure1471", "choiceAdventure1472", "choiceAdventure1473", "choiceAdventure1474", "choiceAdventure1475", "choiceAdventure1486", "choiceAdventure1487", "choiceAdventure1488", "choiceAdventure1489", "choiceAdventure1491", "choiceAdventure1494", "choiceAdventure1505", "choiceAdventure1528", "choiceAdventure1534", "choiceAdventure1538", "choiceAdventure1539", "choiceAdventure1540", "choiceAdventure1541", "choiceAdventure1542", "choiceAdventure1545", "choiceAdventure1546", "choiceAdventure1547", "choiceAdventure1548", "choiceAdventure1549", "choiceAdventure1550", "choiceAdventure1591", "choiceAdventure1604", "choiceAdventure1605", "choiceAdventure1606", "choiceAdventure1607", "choiceAdventure1608", "choiceAdventure1609", "choiceAdventure1610", "choiceAdventure1611", "choiceAdventure1612", "choiceAdventure1613", "choiceAdventure1614", "choiceAdventure1615", "choiceAdventure1616", "choiceAdventure1617", "choiceAdventure1618", "choiceAdventure1619", "choiceAdventure1620", "choiceAdventure1621", "choiceAdventure1622", "choiceAdventure1623", "choiceAdventure1624", "choiceAdventure1625", "choiceAdventure1626", "choiceAdventure1627", "choiceAdventure1628", "choiceAdventure1629", "choiceAdventure1630", "choiceAdventure1631", "choiceAdventure1632", "choiceAdventure1633", "choiceAdventure1634", "choiceAdventure1635", "choiceAdventure1636"];
var familiarProperties = ["commaFamiliar", "cupidBowLastFamiliar", "nextQuantumFamiliar", "stillsuitFamiliar", "zootGraftedButtCheekLeftFamiliar", "zootGraftedButtCheekRightFamiliar", "zootGraftedFootLeftFamiliar", "zootGraftedFootRightFamiliar", "zootGraftedHandLeftFamiliar", "zootGraftedHandRightFamiliar", "zootGraftedHeadFamiliar", "zootGraftedNippleLeftFamiliar", "zootGraftedNippleRightFamiliar", "zootGraftedShoulderLeftFamiliar", "zootGraftedShoulderRightFamiliar"];
var familiarNumericProperties = ["cupidBowLastFamiliar", "zootGraftedButtCheekLeftFamiliar", "zootGraftedButtCheekRightFamiliar", "zootGraftedFootLeftFamiliar", "zootGraftedFootRightFamiliar", "zootGraftedHandLeftFamiliar", "zootGraftedHandRightFamiliar", "zootGraftedHeadFamiliar", "zootGraftedNippleLeftFamiliar", "zootGraftedNippleRightFamiliar", "zootGraftedShoulderLeftFamiliar", "zootGraftedShoulderRightFamiliar"];
var statProperties = ["nsChallenge1", "snojoSetting"];
var phylumProperties = ["dnaSyringe", "locketPhylum", "redSnapperPhylum", "_circadianRhythmsPhylum"];
var itemProperties = ["commerceGhostItem", "daycareInstructorItem", "doctorBagQuestItem", "dolphinItem", "eweItem", "guzzlrQuestBooze", "implementGlitchItem", "muffinOnOrder", "rufusDesiredArtifact", "rufusDesiredItems", "shenQuestItem", "trapperOre", "_cookbookbatQuestIngredient", "_crimboPastDailySpecialItem", "_dailySpecial", "_pirateRealmCurio"];
var itemNumericProperties = ["daycareInstructorItem", "_crimboPastDailySpecialItem"];

var booleanPropertiesSet = new Set(booleanProperties);
var numericPropertiesSet = new Set(numericProperties);
var numericOrStringPropertiesSet = new Set(numericOrStringProperties);
var stringPropertiesSet = new Set(stringProperties);
var locationPropertiesSet = new Set(locationProperties);
var monsterPropertiesSet = new Set(monsterProperties);
var familiarPropertiesSet = new Set(familiarProperties);
var statPropertiesSet = new Set(statProperties);
var phylumPropertiesSet = new Set(phylumProperties);
var itemPropertiesSet = new Set(itemProperties);
/**
 * Determine whether a property has a boolean value
 *
 * @param property Property to check
 * @returns Whether the supplied property has a boolean value
 */
function isBooleanProperty(property) {
  return booleanPropertiesSet.has(property);
}
/**
 * Determine whether a property has a numeric value
 *
 * @param property Property to check
 * @returns Whether the supplied property has a numeric value
 */
function isNumericProperty(property) {
  return numericPropertiesSet.has(property);
}
/**
 * Determine whether a property has a numeric or string value
 *
 * @param property Property to check
 * @returns Whether the supplied property has a numeric or string value
 */
function isNumericOrStringProperty(property) {
  return numericOrStringPropertiesSet.has(property);
}
/**
 * Determine whether a property has a string value
 *
 * @param property Property to check
 * @returns Whether the supplied property has a string value
 */
function isStringProperty(property) {
  return stringPropertiesSet.has(property);
}
/**
 * Determine whether a property has a Location value
 *
 * @param property Property to check
 * @returns Whether the supplied property has a Location value
 */
function isLocationProperty(property) {
  return locationPropertiesSet.has(property);
}
/**
 * Determine whether a property has a Monster value
 *
 * @param property Property to check
 * @returns Whether the supplied property has a Monster value
 */
function isMonsterProperty(property) {
  return monsterPropertiesSet.has(property);
}
/**
 * Determine whether a property has a Familiar value
 *
 * @param property Property to check
 * @returns Whether the supplied property has a Familiar value
 */
function isFamiliarProperty(property) {
  return familiarPropertiesSet.has(property);
}
/**
 * Determine whether a property has a Stat value
 *
 * @param property Property to check
 * @returns Whether the supplied property has a Stat value
 */
function isStatProperty(property) {
  return statPropertiesSet.has(property);
}
/**
 * Determine whether a property has a Phylum value
 *
 * @param property Property to check
 * @returns Whether the supplied property has a Phylum value
 */
function isPhylumProperty(property) {
  return phylumPropertiesSet.has(property);
}
/**
 * Determine whether a property has an Item value
 *
 * @param property Property to check
 * @returns Whether the supplied property has an Item value
 */
function isItemProperty(property) {
  return itemPropertiesSet.has(property);
}

var createPropertyGetter = transform => (property, default_) => {
  var value = kolmafia.getProperty(property);
  if (default_ !== undefined && value === "") {
    return default_;
  }
  return transform(value, property);
};
function createMafiaClassPropertyGetter(Type,
// eslint-disable-next-line @typescript-eslint/no-explicit-any
toType) {
  var numericPropertyNames = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : [];
  return createPropertyGetter((value, property) => {
    if (value === "") return null;
    var v = numericPropertyNames.includes(property) ? value.match(/^[0-9]+$/) ? toType(parseInt(value)) : null : toType(value);
    return v === Type.none ? null : v;
  });
}
var getString = createPropertyGetter(value => value);
var getBoolean = createPropertyGetter(value => value === "true");
var getNumber = createPropertyGetter(value => Number(value));
var getFamiliar = createMafiaClassPropertyGetter(kolmafia.Familiar, kolmafia.toFamiliar, familiarNumericProperties);
var getItem = createMafiaClassPropertyGetter(kolmafia.Item, kolmafia.toItem, itemNumericProperties);
var getLocation$1 = createMafiaClassPropertyGetter(kolmafia.Location, kolmafia.toLocation);
var getMonster = createMafiaClassPropertyGetter(kolmafia.Monster, kolmafia.toMonster, monsterNumericProperties);
var getPhylum = createMafiaClassPropertyGetter(kolmafia.Phylum, kolmafia.toPhylum);
var getStat = createMafiaClassPropertyGetter(kolmafia.Stat, kolmafia.toStat);
/**
 * Gets the value of a mafia property, either built in or custom
 *
 * @param property Name of the property
 * @param _default Default value for the property to take if not set
 * @returns Value of the mafia property
 */
function get$2(property, _default) {
  var value = getString(property);
  // Handle known properties.
  if (isBooleanProperty(property)) {
    return getBoolean(property, _default) ?? false;
  } else if (isNumericProperty(property)) {
    return getNumber(property, _default) ?? 0;
  } else if (isNumericOrStringProperty(property)) {
    return value.match(/^\d+$/) ? parseInt(value) : value;
  } else if (isLocationProperty(property)) {
    return getLocation$1(property, _default);
  } else if (isMonsterProperty(property)) {
    return getMonster(property, _default);
  } else if (isFamiliarProperty(property)) {
    return getFamiliar(property, _default);
  } else if (isStatProperty(property)) {
    return getStat(property, _default);
  } else if (isPhylumProperty(property)) {
    return getPhylum(property, _default);
  } else if (isItemProperty(property)) {
    return getItem(property, _default);
  } else if (isStringProperty(property)) {
    return value === "" && _default !== undefined ? _default : value;
  }
  // Not a KnownProperty from here on out.
  if (_default instanceof kolmafia.Location) {
    return getLocation$1(property, _default);
  } else if (_default instanceof kolmafia.Monster) {
    return getMonster(property, _default);
  } else if (_default instanceof kolmafia.Familiar) {
    return getFamiliar(property, _default);
  } else if (_default instanceof kolmafia.Stat) {
    return getStat(property, _default);
  } else if (_default instanceof kolmafia.Phylum) {
    return getPhylum(property, _default);
  } else if (_default instanceof kolmafia.Item) {
    return getItem(property, _default);
  } else if (typeof _default === "boolean") {
    return value === "true" ? true : value === "false" ? false : _default;
  } else if (typeof _default === "number") {
    return value === "" ? _default : parseInt(value);
  } else if (value === "") {
    return _default === undefined ? "" : _default;
  } else {
    return value;
  }
}
/**
 * Sets the value of a mafia property, either built in or custom
 *
 * @param property Name of the property
 * @param value Value to give the property
 * @returns Value that was set
 */
function _set(property, value) {
  var stringValue = value === null ? "" : value.toString();
  kolmafia.setProperty(property, stringValue);
  return value;
}
/**
 * Sets the value of a set of mafia properties
 *
 * @param properties Set of properties
 */
function setProperties(properties) {
  for (var _i = 0, _Object$entries = Object.entries(properties); _i < _Object$entries.length; _i++) {
    var _Object$entries$_i = _slicedToArray(_Object$entries[_i], 2),
      prop = _Object$entries$_i[0],
      value = _Object$entries$_i[1];
    _set(prop, value);
  }
}
/**
 * Carries out a callback during which a set of properties will be set as supplied
 *
 * @param properties Properties to set during callback
 * @param callback Callback to execute with set properties
 * @returns Return value of the supplied callback
 */
function withProperties(properties, callback) {
  var propertiesBackup = Object.fromEntries(Object.entries(properties).map(_ref => {
    var _ref2 = _slicedToArray(_ref, 1),
      prop = _ref2[0];
    return [prop, get$2(prop)];
  }));
  setProperties(properties);
  try {
    return callback();
  } finally {
    setProperties(propertiesBackup);
  }
}
/**
 * Carries out a callback during which a property will be set as supplied
 *
 * @param property Property to set during callback
 * @param value Value to set property during callback
 * @param callback Callback to execute with set properties
 * @returns Return value of the supplied callback
 */
function withProperty(property, value, callback) {
  return withProperties({
    [property]: value
  }, callback);
}
/**
 * Carries out a callback during which a set of choices will be handled as supplied
 *
 * @param choices Choices to set during callback
 * @param callback Callback to execute with set choices
 * @returns Return value of the supplied callback
 */
function withChoices(choices, callback) {
  var properties = Object.fromEntries(Object.entries(choices).map(_ref3 => {
    var _ref4 = _slicedToArray(_ref3, 2),
      choice = _ref4[0],
      option = _ref4[1];
    return [`choiceAdventure${choice}`, option];
  }));
  return withProperties(properties, callback);
}
/**
 * Carries out a callback during which a choice will be handled as supplied
 *
 * @param choice Choice to set during callback
 * @param value How to handle choice during callback
 * @param callback Callback to execute with set properties
 * @returns Return value of the supplied callback
 */
function withChoice(choice, value, callback) {
  return withChoices({
    [choice]: value
  }, callback);
}
var PropertiesManager = /*#__PURE__*/function () {
  function PropertiesManager() {
    _classCallCheck(this, PropertiesManager);
    _defineProperty(this, "properties", {});
  }
  return _createClass(PropertiesManager, [{
    key: "storedValues",
    get: function get() {
      return this.properties;
    }
    /**
     * Sets a collection of properties to the given values, storing the old values.
     *
     * @param propertiesToSet A Properties object, keyed by property name.
     */
  }, {
    key: "set",
    value: function set(propertiesToSet) {
      for (var _i2 = 0, _Object$entries2 = Object.entries(propertiesToSet); _i2 < _Object$entries2.length; _i2++) {
        var _Object$entries2$_i = _slicedToArray(_Object$entries2[_i2], 2),
          propertyName = _Object$entries2$_i[0],
          propertyValue = _Object$entries2$_i[1];
        if (!(propertyName in this.properties)) {
          this.properties[propertyName] = kolmafia.propertyExists(propertyName) ? get$2(propertyName) : PropertiesManager.EMPTY_PREFERENCE;
        }
        _set(propertyName, propertyValue);
      }
    }
    /**
     * Sets a collection of choice adventure properties to the given values, storing the old values.
     *
     * @param choicesToSet An object keyed by choice adventure number.
     */
  }, {
    key: "setChoices",
    value: function setChoices(choicesToSet) {
      this.set(Object.fromEntries(Object.entries(choicesToSet).map(_ref5 => {
        var _ref6 = _slicedToArray(_ref5, 2),
          choiceNumber = _ref6[0],
          choiceValue = _ref6[1];
        return [`choiceAdventure${choiceNumber}`, choiceValue];
      })));
    }
    /**
     * Sets a single choice adventure property to the given value, storing the old value.
     *
     * @param choiceToSet The number of the choice adventure to set the property for.
     * @param value The value to assign to that choice adventure.
     */
  }, {
    key: "setChoice",
    value: function setChoice(choiceToSet, value) {
      this.setChoices({
        [choiceToSet]: value
      });
    }
    /**
     * Resets the given properties to their original stored value. Does not delete entries from the manager.
     *
     * @param properties Collection of properties to reset.
     */
  }, {
    key: "reset",
    value: function reset() {
      for (var _len = arguments.length, properties = new Array(_len), _key = 0; _key < _len; _key++) {
        properties[_key] = arguments[_key];
      }
      for (var _i3 = 0, _properties = properties; _i3 < _properties.length; _i3++) {
        var property = _properties[_i3];
        if (!(property in this.properties)) continue;
        var value = this.properties[property];
        if (value === PropertiesManager.EMPTY_PREFERENCE) {
          kolmafia.removeProperty(property);
        } else {
          _set(property, value);
        }
      }
    }
    /**
     * Iterates over all stored values, setting each property back to its original stored value. Does not delete entries from the manager.
     */
  }, {
    key: "resetAll",
    value: function resetAll() {
      this.reset.apply(this, _toConsumableArray(Object.keys(this.properties)));
    }
    /**
     * Stops storing the original values of inputted properties.
     *
     * @param properties Properties for the manager to forget.
     */
  }, {
    key: "clear",
    value: function clear() {
      for (var _len2 = arguments.length, properties = new Array(_len2), _key2 = 0; _key2 < _len2; _key2++) {
        properties[_key2] = arguments[_key2];
      }
      for (var _i4 = 0, _properties2 = properties; _i4 < _properties2.length; _i4++) {
        var property = _properties2[_i4];
        if (this.properties[property]) {
          delete this.properties[property];
        }
      }
    }
    /**
     * Clears all properties.
     */
  }, {
    key: "clearAll",
    value: function clearAll() {
      this.properties = {};
    }
    /**
     * Increases a numeric property to the given value if necessary.
     *
     * @param property The numeric property we want to potentially raise.
     * @param value The minimum value we want that property to have.
     * @returns Whether we needed to change the property.
     */
  }, {
    key: "setMinimumValue",
    value: function setMinimumValue(property, value) {
      if (get$2(property, 0) < value) {
        this.set({
          [property]: value
        });
        return true;
      }
      return false;
    }
    /**
     * Decrease a numeric property to the given value if necessary.
     *
     * @param property The numeric property we want to potentially lower.
     * @param value The maximum value we want that property to have.
     * @returns Whether we needed to change the property.
     */
  }, {
    key: "setMaximumValue",
    value: function setMaximumValue(property, value) {
      if (get$2(property, 0) > value) {
        this.set({
          [property]: value
        });
        return true;
      }
      return false;
    }
    /**
     * Creates a new PropertiesManager with identical stored values to this one.
     *
     * @returns A new PropertiesManager, with identical stored values to this one.
     */
  }, {
    key: "clone",
    value: function clone() {
      var newGuy = new PropertiesManager();
      newGuy.properties = this.storedValues;
      return newGuy;
    }
    /**
     * Clamps a numeric property, modulating it up or down to fit within a specified range
     *
     * @param property The numeric property to clamp
     * @param min The lower bound for what we want the property to be allowed to be.
     * @param max The upper bound for what we want the property to be allowed to be.
     * @returns Whether we ended up changing the property or not.
     */
  }, {
    key: "clamp",
    value: function clamp(property, min, max) {
      if (max < min) return false;
      var start = get$2(property);
      this.setMinimumValue(property, min);
      this.setMaximumValue(property, max);
      return start !== get$2(property);
    }
    /**
     * Determines whether this PropertiesManager has identical stored values to another.
     *
     * @param other The PropertiesManager to compare to this one.
     * @returns Whether their StoredValues are identical.
     */
  }, {
    key: "equals",
    value: function equals(other) {
      var thisProps = Object.entries(this.storedValues);
      var otherProps = new Map(Object.entries(other.storedValues));
      if (thisProps.length !== otherProps.size) return false;
      for (var _i5 = 0, _thisProps = thisProps; _i5 < _thisProps.length; _i5++) {
        var _thisProps$_i = _slicedToArray(_thisProps[_i5], 2),
          propertyName = _thisProps$_i[0],
          propertyValue = _thisProps$_i[1];
        if (otherProps.get(propertyName) === propertyValue) return false;
      }
      return true;
    }
    /**
     * Merges a PropertiesManager onto this one, letting the input win in the event that both PropertiesManagers have a value stored.
     *
     * @param other The PropertiesManager to be merged onto this one.
     * @returns A new PropertiesManager with stored values from both its parents.
     */
  }, {
    key: "merge",
    value: function merge(other) {
      var newGuy = new PropertiesManager();
      newGuy.properties = _objectSpread2(_objectSpread2({}, this.properties), other.properties);
      return newGuy;
    }
    /**
     * Merges an arbitrary collection of PropertiesManagers, letting the rightmost PropertiesManager win in the event of verlap.
     *
     * @param mergees The PropertiesManagers to merge together.
     * @returns A PropertiesManager that is just an amalgam of all the constituents.
     */
  }], [{
    key: "merge",
    value: function merge() {
      for (var _len3 = arguments.length, mergees = new Array(_len3), _key3 = 0; _key3 < _len3; _key3++) {
        mergees[_key3] = arguments[_key3];
      }
      if (mergees.length === 0) return new PropertiesManager();
      return mergees.reduce((a, b) => a.merge(b));
    }
  }]);
}();
_defineProperty(PropertiesManager, "EMPTY_PREFERENCE", Symbol("empty preference"));

/**
 * Type guard against null value
 *
 * @param value Value that could be null
 * @returns Confirmation that the value is not null
 */
function notNull(value) {
  return value !== null;
}
/**
 * Type guard against nullish value
 *
 * @param value Value that could be null or undefined
 * @returns Confirmation that the value is not null nor undefined
 */
function notNullish(value) {
  return notNull(value) && value !== undefined;
}
/**
 * Parse string to number, stripping commas
 *
 * @param n Numberical string to parse
 * @returns Numerical value of string
 */
function parseNumber(n) {
  return Number.parseInt(n.replace(/,/g, ""));
}
/**
 * Clamp a number between lower and upper bounds.
 *
 * @param n Number to clamp.
 * @param min Lower bound.
 * @param max Upper bound.
 * @returns Clamped value
 */
function clamp(n, min, max) {
  return Math.max(min, Math.min(max, n));
}
/**
 * Split an array into chunkSize sized chunks
 *
 * @param array Array to split
 * @param chunkSize Size of chunk
 * @returns Split array
 */
function chunk(array, chunkSize) {
  var result = [];
  for (var i = 0; i < array.length; i += chunkSize) {
    result.push(array.slice(i, i + chunkSize));
  }
  return result;
}
/**
 * Count distinct values in an array
 *
 * @param array Array of values
 * @returns Map of distinct values to count
 */
function arrayToCountedMap(array) {
  if (!Array.isArray(array)) return array;
  var map = new Map();
  array.forEach(item => {
    map.set(item, (map.get(item) || 0) + 1);
  });
  return map;
}
/**
 * Turn map of distinct values to count into array of values
 *
 * @param map Map to turn into array
 * @returns Array of values
 */
function countedMapToArray(map) {
  var _ref;
  return (_ref = []).concat.apply(_ref, _toConsumableArray(_toConsumableArray(map).map(_ref2 => {
    var _ref3 = _slicedToArray(_ref2, 2),
      item = _ref3[0],
      quantity = _ref3[1];
    return Array(quantity).fill(item);
  })));
}
/**
 * Stringify a counted map
 *
 * @param map Map of counted values
 * @returns String representing map of counted values
 */
function countedMapToString(map) {
  return _toConsumableArray(map).map(_ref4 => {
    var _ref5 = _slicedToArray(_ref4, 2),
      item = _ref5[0],
      quantity = _ref5[1];
    return `${quantity} x ${item}`;
  }).join(", ");
}
/**
 * Sum an array of numbers.
 *
 * @param addends Addends to sum.
 * @param x Property or mapping function of addends to sum
 * @returns Sum of numbers
 */
function sum(addends, x) {
  return addends.reduce((subtotal, element) => subtotal + (typeof x === "function" ? x(element) : element[x]), 0);
}
/**
 * Sum array of numbers
 *
 * @param addends Numbers to sum
 * @returns Sum of numbers
 */
function sumNumbers(addends) {
  return sum(addends, x => x);
}
/**
 * Checks if a given item is in a readonly array, acting as a typeguard.
 *
 * @param item Needle
 * @param array Readonly array haystack
 * @returns Whether the item is in the array, and narrows the type of the item.
 */
function arrayContains(item, array) {
  return array.includes(item);
}
/**
 * Checks if two arrays contain the same elements in the same quantity.
 *
 * @param a First array for comparison
 * @param b Second array for comparison
 * @returns Whether the two arrays are equal, irrespective of order.
 */
function setEqual(a, b) {
  var sortedA = _toConsumableArray(a).sort();
  var sortedB = _toConsumableArray(b).sort();
  return a.length === b.length && sortedA.every((item, index) => item === sortedB[index]);
}
/**
 * Reverses keys and values for a given map
 *
 * @param map Map to invert
 * @returns Inverted map
 */
function invertMap(map) {
  var returnValue = new Map();
  var _iterator = _createForOfIteratorHelper(map),
    _step;
  try {
    for (_iterator.s(); !(_step = _iterator.n()).done;) {
      var _step$value = _slicedToArray(_step.value, 2),
        key = _step$value[0],
        value = _step$value[1];
      returnValue.set(value, key);
    }
  } catch (err) {
    _iterator.e(err);
  } finally {
    _iterator.f();
  }
  return returnValue;
}
/**
 * Splits a string by commas while also respecting escaping commas with a backslash
 *
 * @param str String to split
 * @returns List of tokens
 */
function splitByCommasWithEscapes(str) {
  var returnValue = [];
  var ignoreNext = false;
  var currentString = "";
  var _iterator2 = _createForOfIteratorHelper(str.split("")),
    _step2;
  try {
    for (_iterator2.s(); !(_step2 = _iterator2.n()).done;) {
      var char = _step2.value;
      if (char === "\\") {
        ignoreNext = true;
      } else {
        if (char == "," && !ignoreNext) {
          returnValue.push(currentString.trim());
          currentString = "";
        } else {
          currentString += char;
        }
        ignoreNext = false;
      }
    }
  } catch (err) {
    _iterator2.e(err);
  } finally {
    _iterator2.f();
  }
  returnValue.push(currentString.trim());
  return returnValue;
}
/**
 * Find the best element of an array, where "best" is defined by some given criteria.
 *
 * @param array The array to traverse and find the best element of.
 * @param optimizer Either a key on the objects we're looking at that corresponds to numerical values, or a function for mapping these objects to numbers. Essentially, some way of assigning value to the elements of the array.
 * @param reverse Make this true to find the worst element of the array, and false to find the best. Defaults to false.
 * @returns Best element by optimizer function
 */
function maxBy(array, optimizer) {
  var reverse = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : false;
  if (!array.length) throw new Error("Cannot call maxBy on an empty array!");
  if (typeof optimizer === "function") {
    return _toConsumableArray(array).reduce((_ref6, other) => {
      var value = _ref6.value,
        item = _ref6.item;
      var otherValue = optimizer(other);
      return value >= otherValue !== reverse ? {
        value,
        item
      } : {
        value: otherValue,
        item: other
      };
    }, {
      item: array[0],
      value: optimizer(array[0])
    }).item;
  } else {
    return array.reduce((a, b) => a[optimizer] >= b[optimizer] !== reverse ? a : b);
  }
}
/**
 * Let Typescript see that you're working with tuples!
 *
 * @param args A spread array to interpret as a tuple
 * @returns The given arg, its type now interpreted as a tuple
 */
// eslint-disable-next-line @typescript-eslint/no-explicit-any
function tuple() {
  for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
    args[_key] = arguments[_key];
  }
  return args;
}
/**
 * Compare arrays shallowly
 *
 * @param left One array to compare
 * @param right The other array to compare
 * @returns Whether the two arrays are shallowly equal
 */
function arrayEquals$1(left, right) {
  if (left.length !== right.length) return false;
  return left.every((element, index) => element === right[index]);
}
/**
 * Used to collapse a Delayed<T, S> object into an entity of type "T" as represented by the object.
 *
 * @param delayedObject Object of type Delayed<T, S> that represents either a value of type T or a function returning a value of type T.
 * @param args The arguments to pass to the delay function
 * @returns The return value of the function, if delayedObject is a function. Otherwise, this returns the original element.
 */
// eslint-disable-next-line @typescript-eslint/no-explicit-any
function undelay(delayedObject) {
  for (var _len2 = arguments.length, args = new Array(_len2 > 1 ? _len2 - 1 : 0), _key2 = 1; _key2 < _len2; _key2++) {
    args[_key2 - 1] = arguments[_key2];
  }
  return typeof delayedObject === "function" ? delayedObject.apply(void 0, args) : delayedObject;
}
/**
 * Makes a byX function, like byStat or byClass
 *
 * @param source A method for finding your stat, or class, or whatever X is in this context
 * @returns A function akin to byStat or byClass; it accepts an object that either is "complete" in the sense that it has a key for every conceivable value, or contains a `default` parameter. If an inappropriate input is provided, returns undefined.
 */
function makeByXFunction(source) {
  return function (options, alternateSource) {
    var val = undelay(alternateSource ?? source);
    if ("default" in options) return options[val] ?? options.default;
    return options[val];
  };
}

var concatTemplateString = function concatTemplateString(literals) {
  for (var _len = arguments.length, placeholders = new Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
    placeholders[_key - 1] = arguments[_key];
  }
  return literals.raw.reduce((acc, literal, i) => acc + literal + (placeholders[i] ?? ""), "");
};
var handleTypeGetError = (Type, error) => {
  var message = `${error}`;
  var match = message.match(RegExp(`Bad ${Type.name.toLowerCase()} value: .*`));
  if (match) {
    kolmafia.print(`${match[0]}; if you're certain that this ${Type.name} exists and is spelled correctly, please update KoLMafia`, "red");
  } else {
    kolmafia.print(message);
  }
};
var createSingleConstant = (Type, converter) => {
  var tagFunction = function tagFunction(literals) {
    for (var _len2 = arguments.length, placeholders = new Array(_len2 > 1 ? _len2 - 1 : 0), _key2 = 1; _key2 < _len2; _key2++) {
      placeholders[_key2 - 1] = arguments[_key2];
    }
    var input = concatTemplateString.apply(void 0, [literals].concat(placeholders));
    try {
      return Type.get(input);
    } catch (error) {
      handleTypeGetError(Type, error);
    }
    kolmafia.abort();
  };
  tagFunction.cls = Type;
  tagFunction.none = Type.none;
  tagFunction.get = name => {
    var value = converter(name);
    return value === Type.none ? null : value;
  };
  return tagFunction;
};
var createPluralConstant = Type => {
  var tagFunction = function tagFunction(literals) {
    for (var _len3 = arguments.length, placeholders = new Array(_len3 > 1 ? _len3 - 1 : 0), _key3 = 1; _key3 < _len3; _key3++) {
      placeholders[_key3 - 1] = arguments[_key3];
    }
    var input = concatTemplateString.apply(void 0, [literals].concat(placeholders));
    if (input === "") {
      return Type.all();
    }
    try {
      return Type.get(splitByCommasWithEscapes(input));
    } catch (error) {
      handleTypeGetError(Type, error);
    }
    kolmafia.abort();
  };
  tagFunction.all = () => Type.all();
  return tagFunction;
};
/**
 * A Bounty specified by name.
 *
 * @category In-game constant
 */
createSingleConstant(kolmafia.Bounty, kolmafia.toBounty);
/**
 * A list of Bounties specified by a comma-separated list of names.
 * For a list of all possible Bounties, leave the template string blank.
 *
 * @category In-game constant
 */
createPluralConstant(kolmafia.Bounty);
/**
 * A Class specified by name.
 *
 * @category In-game constant
 */
var $class = createSingleConstant(kolmafia.Class, kolmafia.toClass);
/**
 * A list of Classes specified by a comma-separated list of names.
 * For a list of all possible Classes, leave the template string blank.
 *
 * @category In-game constant
 */
var $classes = createPluralConstant(kolmafia.Class);
/**
 * A Coinmaster specified by name.
 *
 * @category In-game constant
 */
var $coinmaster = createSingleConstant(kolmafia.Coinmaster, kolmafia.toCoinmaster);
/**
 * A list of Coinmasters specified by a comma-separated list of names.
 * For a list of all possible Coinmasters, leave the template string blank.
 *
 * @category In-game constant
 */
createPluralConstant(kolmafia.Coinmaster);
/**
 * An Effect specified by name.
 *
 * @category In-game constant
 */
var $effect = createSingleConstant(kolmafia.Effect, kolmafia.toEffect);
/**
 * A list of Effects specified by a comma-separated list of names.
 * For a list of all possible Effects, leave the template string blank.
 *
 * @category In-game constant
 */
var $effects = createPluralConstant(kolmafia.Effect);
/**
 * An Element specified by name.
 *
 * @category In-game constant
 */
var $element = createSingleConstant(kolmafia.Element, kolmafia.toElement);
/**
 * A list of Elements specified by a comma-separated list of names.
 * For a list of all possible Elements, leave the template string blank.
 *
 * @category In-game constant
 */
createPluralConstant(kolmafia.Element);
/**
 * A Familiar specified by name.
 *
 * @category In-game constant
 */
var $familiar = createSingleConstant(kolmafia.Familiar, kolmafia.toFamiliar);
/**
 * A list of Familiars specified by a comma-separated list of names.
 * For a list of all possible Familiars, leave the template string blank.
 *
 * @category In-game constant
 */
var $familiars = createPluralConstant(kolmafia.Familiar);
/**
 * An Item specified by name.
 *
 * @category In-game constant
 */
var $item = createSingleConstant(kolmafia.Item, kolmafia.toItem);
/**
 * A list of Items specified by a comma-separated list of names.
 * For a list of all possible Items, leave the template string blank.
 *
 * @category In-game constant
 */
var $items = createPluralConstant(kolmafia.Item);
/**
 * A Location specified by name.
 *
 * @category In-game constant
 */
var $location = createSingleConstant(kolmafia.Location, kolmafia.toLocation);
/**
 * A list of Locations specified by a comma-separated list of names.
 * For a list of all possible Locations, leave the template string blank.
 *
 * @category In-game constant
 */
var $locations = createPluralConstant(kolmafia.Location);
/**
 * A Modifier specified by name.
 *
 * @category In-game constant
 */
var $modifier = createSingleConstant(kolmafia.Modifier, kolmafia.toModifier);
/**
 * A list of Modifiers specified by a comma-separated list of names.
 * For a list of all possible Modifiers, leave the template string blank.
 *
 * @category In-game constant
 */
createPluralConstant(kolmafia.Modifier);
/**
 * A Monster specified by name.
 *
 * @category In-game constant
 */
var $monster = createSingleConstant(kolmafia.Monster, kolmafia.toMonster);
/**
 * A list of Monsters specified by a comma-separated list of names.
 * For a list of all possible Monsters, leave the template string blank.
 *
 * @category In-game constant
 */
var $monsters = createPluralConstant(kolmafia.Monster);
/**
 * A Path specified by name.
 *
 * @category In-game constant
 */
var $path = createSingleConstant(kolmafia.Path, kolmafia.toPath);
/**
 * A list of Paths specified by a comma-separated list of names.
 * For a list of all possible Paths, leave the template string blank.
 *
 * @category In-game constant
 */
createPluralConstant(kolmafia.Path);
/**
 * A Phylum specified by name.
 *
 * @category In-game constant
 */
var $phylum = createSingleConstant(kolmafia.Phylum, kolmafia.toPhylum);
/**
 * A list of Phyla specified by a comma-separated list of names.
 * For a list of all possible Phyla, leave the template string blank.
 *
 * @category In-game constant
 */
var $phyla = createPluralConstant(kolmafia.Phylum);
/**
 * A Servant specified by name.
 *
 * @category In-game constant
 */
createSingleConstant(kolmafia.Servant, kolmafia.toServant);
/**
 * A list of Servants specified by a comma-separated list of names.
 * For a list of all possible Servants, leave the template string blank.
 *
 * @category In-game constant
 */
createPluralConstant(kolmafia.Servant);
/**
 * A Skill specified by name.
 *
 * @category In-game constant
 */
var $skill = createSingleConstant(kolmafia.Skill, kolmafia.toSkill);
/**
 * A list of Skills specified by a comma-separated list of names.
 * For a list of all possible Skills, leave the template string blank.
 *
 * @category In-game constant
 */
var $skills = createPluralConstant(kolmafia.Skill);
/**
 * A Slot specified by name.
 *
 * @category In-game constant
 */
var $slot = createSingleConstant(kolmafia.Slot, kolmafia.toSlot);
/**
 * A list of Slots specified by a comma-separated list of names.
 * For a list of all possible Slots, leave the template string blank.
 *
 * @category In-game constant
 */
var $slots = createPluralConstant(kolmafia.Slot);
/**
 * A Stat specified by name.
 *
 * @category In-game constant
 */
var $stat = createSingleConstant(kolmafia.Stat, kolmafia.toStat);
/**
 * A list of Stats specified by a comma-separated list of names.
 * For a list of all possible Stats, leave the template string blank.
 *
 * @category In-game constant
 */
var $stats = createPluralConstant(kolmafia.Stat);
/**
 * A Thrall specified by name.
 *
 * @category In-game constant
 */
var $thrall = createSingleConstant(kolmafia.Thrall, kolmafia.toThrall);
/**
 * A list of Thralls specified by a comma-separated list of names.
 * For a list of all possible Thralls, leave the template string blank.
 *
 * @category In-game constant
 */
var $thralls = createPluralConstant(kolmafia.Thrall);

/**
 * Determines the current maximum Accordion Thief songs the player can have in their head
 *
 * @category General
 * @returns Maximum number of songs for player
 */
function getSongLimit() {
  return 3 + (kolmafia.booleanModifier("Four Songs") ? 1 : 0) + kolmafia.numericModifier("Additional Song");
}
/**
 * Determine whether the Skill or Effect provided is an Accordion Thief song
 *
 * @category General
 * @param skillOrEffect The Skill or Effect
 * @returns Whether it's a song
 */
function isSong(skillOrEffect) {
  if (skillOrEffect instanceof kolmafia.Effect && skillOrEffect.attributes.includes("song")) {
    return true;
  } else {
    var skill = skillOrEffect instanceof kolmafia.Effect ? kolmafia.toSkill(skillOrEffect) : skillOrEffect;
    return skill.class === $class`Accordion Thief` && skill.buff;
  }
}
/**
 * List all active Effects
 *
 * @category General
 * @returns List of Effects
 */
function getActiveEffects() {
  return Object.keys(kolmafia.myEffects()).map(e => kolmafia.Effect.get(e));
}
/**
 * List currently active Accordion Thief songs
 *
 * @category General
 * @returns List of song Effects
 */
function getActiveSongs() {
  return getActiveEffects().filter(isSong);
}
/**
 * List number of active Accordion Thief songs
 *
 * @category General
 * @returns Number of songs
 */
function getSongCount() {
  return getActiveSongs().length;
}
/**
 * Determine the player's remaining liver space
 *
 * @category General
 * @returns Remaining liver space
 */
function getRemainingLiver() {
  return kolmafia.inebrietyLimit() - kolmafia.myInebriety();
}
/**
 * Determine the player's remaining stomach space
 *
 * @category General
 * @returns Remaining stomach space
 */
function getRemainingStomach() {
  return kolmafia.fullnessLimit() - kolmafia.myFullness();
}
/**
 * Determine whether the player "has" any entity which one could feasibly "have".
 *
 * @category General
 * @param thing Thing to check
 * @param quantity Minimum quantity the player must have to pass
 * @returns Whether the player meets the requirements of owning the supplied thing
 */
function have$P(thing) {
  var quantity = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : 1;
  if (thing instanceof kolmafia.Effect) {
    return kolmafia.haveEffect(thing) >= quantity;
  }
  if (thing instanceof kolmafia.Familiar) {
    return kolmafia.haveFamiliar(thing);
  }
  if (thing instanceof kolmafia.Item) {
    return kolmafia.availableAmount(thing) >= quantity;
  }
  if (thing instanceof kolmafia.Servant) {
    return kolmafia.haveServant(thing);
  }
  if (thing instanceof kolmafia.Skill) {
    return kolmafia.haveSkill(thing);
  }
  if (thing instanceof kolmafia.Thrall) {
    var thrall = kolmafia.myThrall();
    return thrall.id === thing.id && thrall.level >= quantity;
  }
  return false;
}
/**
 * Determine whether a given item is in the player's campground
 *
 * @category General
 * @param item The Item KoLmafia uses to represent the campground item
 * @returns Whether the item is in the campground
 */
function haveInCampground(item) {
  return Object.keys(kolmafia.getCampground()).map(i => kolmafia.Item.get(i)).includes(item);
}
var Wanderer;
(function (Wanderer) {
  Wanderer["Digitize"] = "Digitize Monster";
  Wanderer["Enamorang"] = "Enamorang Monster";
  Wanderer["Familiar"] = "Familiar";
  Wanderer["Holiday"] = "Holiday Monster";
  Wanderer["Kramco"] = "Kramco";
  Wanderer["Nemesis"] = "Nemesis Assassin";
  Wanderer["Portscan"] = "portscan.edu";
  Wanderer["Romantic"] = "Romantic Monster";
  Wanderer["Vote"] = "Vote Monster";
})(Wanderer || (Wanderer = {}));
var deterministicWanderers = [Wanderer.Digitize, Wanderer.Portscan];
/**
 * Determine whether the player has the specified counter
 *
 * @param counterName Name of the counter
 * @param minTurns Minimum turns the counter is set to
 * @param maxTurns Maximum turns the counter is set to
 * @category General
 * @returns Whether player has the counter
 */
function haveCounter(counterName) {
  var minTurns = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : 0;
  var maxTurns = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : 500;
  return kolmafia.getCounters(counterName, minTurns, maxTurns) === counterName;
}
/**
 * Determine whether the player has the specified wanderer's counter
 *
 * @param wanderer Wanderer to check
 * @category Wanderers
 * @returns Whether player has the wanderer counter
 */
function haveWandererCounter(wanderer) {
  if (deterministicWanderers.includes(wanderer)) {
    return haveCounter(wanderer);
  }
  var begin = wanderer + " window begin";
  var end = wanderer + " window end";
  return haveCounter(begin) || haveCounter(end);
}
/**
 * Determines the chance the player will encounter a sausage goblin on the
 * next turn, providing the Kramco Sausage-o-Matic is equipped.
 *
 * @category Wanderers
 * @returns Chance that the sausage goblin is due (as a number between 0 and 1)
 */
function getKramcoWandererChance() {
  var fights = get$2("_sausageFights");
  var lastFight = get$2("_lastSausageMonsterTurn");
  var totalTurns = kolmafia.totalTurnsPlayed();
  if (fights < 1) {
    return lastFight === totalTurns && kolmafia.myTurncount() < 1 ? 0.5 : 1.0;
  }
  var turnsSinceLastFight = totalTurns - lastFight;
  return Math.min(1.0, (turnsSinceLastFight + 1) / (5 + fights * 3 + Math.max(0, fights - 5) ** 3));
}
var foldGroupCache = new Map();
/**
 * Determines the fold group (if any) of which the given item is a part
 *
 * @category General
 * @param item Item that is part of the required fold group
 * @param cache Whether to query the fold group cache
 * @returns List of items in the fold group
 */
function getFoldGroup(item) {
  var cache = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : true;
  if (cache) {
    var cached = foldGroupCache.get(item);
    if (cached !== undefined) return cached;
  }
  var result = Object.entries(kolmafia.getRelated(item, "fold")).sort((_ref, _ref2) => {
    var _ref3 = _slicedToArray(_ref, 2),
      a = _ref3[1];
    var _ref4 = _slicedToArray(_ref2, 2),
      b = _ref4[1];
    return a - b;
  }).map(_ref5 => {
    var _ref6 = _slicedToArray(_ref5, 1),
      i = _ref6[0];
    return kolmafia.Item.get(i);
  });
  var _iterator = _createForOfIteratorHelper(result),
    _step;
  try {
    for (_iterator.s(); !(_step = _iterator.n()).done;) {
      var fold = _step.value;
      foldGroupCache.set(fold, result);
    }
  } catch (err) {
    _iterator.e(err);
  } finally {
    _iterator.f();
  }
  return result;
}
var banishSource = banisher => {
  switch (banisher.toLowerCase()) {
    case "saber force":
      return $skill`Use the Force`;
    case "nanorhino":
      return $skill`Unleash Nanites`;
    case "sea *dent":
      return $skill`Sea *dent: Throw a Lightning Bolt`;
  }
  var item = kolmafia.toItem(banisher);
  if ($items`none, training scroll:  Snokebomb, tomayohawk-style reflex hammer`.includes(item)) {
    return kolmafia.toSkill(banisher);
  }
  return item;
};
/**
 * Translate mafia's multi-dimensional array prefs into a multi-dimensional array
 * @param prop The name of the mafia string property to process
 * @param outerDelimiter The "outer" delimiter, which separates tuples from eachother
 * @param innerDelimiter The "inner" delimieter, which separates the elements of tuples from eachother
 * @param mappers An array of string => whatever mapping functions that turn this into the actual objects we want
 * @returns An array of typed tuples, based on the given inputs
 */
// eslint-disable-next-line @typescript-eslint/no-explicit-any
function multiSplit(prop, outerDelimiter, innerDelimiter, mappers) {
  var initialSplit = get$2(prop).split(outerDelimiter).filter(Boolean);
  var multiDimensionalArray = outerDelimiter === innerDelimiter ? chunk(initialSplit, mappers.length) : initialSplit.map(entry => entry.split(innerDelimiter));
  return multiDimensionalArray.map(tup => mappers.map((func, index) => func(tup[index])));
}
/**
 * Get a map of banished monsters keyed by what banished them
 *
 * @category General
 * @returns Map of banished monsters
 */
function getBanishedMonsters() {
  return new Map(multiSplit("banishedMonsters", ":", ":", [kolmafia.toMonster, banishSource, Number]).map(_ref7 => {
    var _ref8 = _slicedToArray(_ref7, 2),
      monster = _ref8[0],
      source = _ref8[1];
    return [source, monster];
  }));
}
/**
 * Parse the sort of range that KoLmafia encodes as a string
 * @param range KoLmafia-style range string
 * @returns Tuple of integers representing range
 */
function getRange(range) {
  var _range$match;
  var _ref9 = ((_range$match = range.match(/^(-?\d+)(?:-(-?\d+))?$/)) === null || _range$match === void 0 ? void 0 : _range$match.slice(1, 3).map(v => parseInt(v))) ?? [0],
    _ref0 = _slicedToArray(_ref9, 2),
    lower = _ref0[0],
    upper = _ref0[1];
  return [lower, Number.isNaN(upper) || upper === undefined ? lower : upper];
}
/**
 * Determine the average value from the sort of range that KoLmafia encodes as a string
 *
 * @param range KoLmafia-style range string
 * @returns Average value for range
 */
function getAverage(range) {
  var _getRange = getRange(range),
    _getRange2 = _slicedToArray(_getRange, 2),
    min = _getRange2[0],
    max = _getRange2[1];
  return (min + max) / 2;
}
/**
 * Determine the average adventures expected from consuming an Item
 *
 * If item is not a consumable, will just return "0".
 *
 * @param item Consumable item
 * @returns Average aventures from consumable
 */
function getAverageAdventures(item) {
  return getAverage(item.adventures);
}
/**
 * Remove an effect
 *
 * @category General
 * @param effect Effect to remove
 * @returns Success
 */
function uneffect(effect) {
  return kolmafia.cliExecute(`uneffect ${effect.name}`);
}
/**
 * Determine the step as a number for a given quest property.
 *
 * @param questName Name of quest property to check.
 * @returns Quest step
 */
function questStep$1(questName) {
  var stringStep = get$2(questName);
  if (stringStep === "unstarted") return -1;else if (stringStep === "started") return 0;else if (stringStep === "finished" || stringStep === "") return 999;else {
    if (stringStep.substring(0, 4) !== "step") {
      throw new Error("Quest state parsing error.");
    }
    return parseInt(stringStep.substring(4), 10);
  }
}
var EnsureError = /*#__PURE__*/function (_Error) {
  function EnsureError(cause, reason) {
    var _this;
    _classCallCheck(this, EnsureError);
    _this = _callSuper(this, EnsureError, [`Failed to ensure ${cause.name}!${reason ? ` ${reason}` : ""}`]);
    _this.name = "Ensure Error";
    return _this;
  }
  _inherits(EnsureError, _Error);
  return _createClass(EnsureError);
}(/*#__PURE__*/_wrapNativeSuper(Error));
/**
 * Tries to get an effect using the default method
 *
 * @param ef effect to try to get
 * @param turns turns to aim for; default of 1
 * @throws {EnsureError} Throws an error if the effect cannot be guaranteed
 */
function ensureEffect(ef) {
  var turns = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : 1;
  if (kolmafia.haveEffect(ef) < turns) {
    if (ef.default === null) {
      throw new EnsureError(ef, "No default action");
    }
    if (!kolmafia.cliExecute(ef.default) || kolmafia.haveEffect(ef) === 0) {
      throw new EnsureError(ef);
    }
  }
}
var valueMap = new Map();
var MALL_VALUE_MODIFIER = 0.9;
/**
 * Determiens the average value (based on mallprice and autosell) of a collection of items
 *
 * @param items items whose value you care about
 * @returns Average value of items
 */
function getSaleValue() {
  for (var _len = arguments.length, items = new Array(_len), _key = 0; _key < _len; _key++) {
    items[_key] = arguments[_key];
  }
  return items.map(item => {
    if (valueMap.has(item)) return valueMap.get(item) || 0;
    if (item.discardable) {
      valueMap.set(item, kolmafia.mallPrice(item) > Math.max(2 * kolmafia.autosellPrice(item), 100) ? MALL_VALUE_MODIFIER * kolmafia.mallPrice(item) : kolmafia.autosellPrice(item));
    } else {
      valueMap.set(item, kolmafia.mallPrice(item) > 100 ? MALL_VALUE_MODIFIER * kolmafia.mallPrice(item) : 0);
    }
    return valueMap.get(item) || 0;
  }).reduce((s, price) => s + price, 0) / items.length;
}
var Environment = {
  Underwater: "underwater"
};
/**
 * Determines the weight-coefficient of any leprechaunning that this familiar may find itself doing
 * Assumes the familiar is nude and thus fails for hatrack & pantsrack
 * For the Mutant Cactus Bud, returns the efficacy-multiplier instead
 *
 * @param familiar The familiar whose leprechaun multiplier you're interested in
 * @returns Weight-coefficient
 */
function findLeprechaunMultiplier(familiar) {
  if (familiar === $familiar`Mutant Cactus Bud`) {
    return kolmafia.numericModifier(familiar, "Leprechaun Effectiveness", 1, $item.none);
  }
  if (familiar === $familiar`Reanimated Reanimator`) return 0;
  var meatBonus = kolmafia.numericModifier(familiar, "Meat Drop", 1, $item.none);
  if (meatBonus === 0) return 0;
  return Math.pow(Math.sqrt(meatBonus / 2 + 55 / 4 + 3) - Math.sqrt(55) / 2, 2);
}
/**
 * Determines the weight-coefficient of any baby gravy fairying that this familiar may find itself doing
 * Assumes the familiar is nude and thus fails for hatrack & pantsrack
 * For the Mutant Fire Ant, returns the efficacy-multiplier instead
 *
 * @param familiar The familiar whose fairy multiplier you're interested in
 * @returns Weight-coefficient
 */
function findFairyMultiplier(familiar) {
  if (familiar === $familiar`Mutant Fire Ant`) {
    return kolmafia.numericModifier(familiar, "Fairy Effectiveness", 1, $item.none);
  }
  if (familiar === $familiar`Reanimated Reanimator`) return 0;
  var itemBonus = kolmafia.numericModifier(familiar, "Item Drop", 1, $item.none);
  if (itemBonus === 0) return 0;
  // Assumes you're using LED candle; returns effective weight multiplier
  if (familiar === $familiar`Jill-of-All-Trades`) return 1.5;
  // Working out the multiplier based on the Item Drop at 1lb
  return Math.pow(Math.sqrt(itemBonus + 55 / 4 + 3) - Math.sqrt(55) / 2, 2);
}
var holidayWanderers = new Map([["El Dia De Los Muertos Borrachos", $monsters`Novia Cadáver, Novio Cadáver, Padre Cadáver, Persona Inocente Cadáver`], ["Feast of Boris", $monsters`Candied Yam Golem, Malevolent Tofurkey, Possessed Can of Cranberry Sauce, Stuffing Golem`], ["Talk Like a Pirate Day", $monsters`ambulatory pirate, migratory pirate, peripatetic pirate`]]);
/**
 * Get today's holiday wanderers
 *
 * @returns List of holiday wanderer Monsters
 */
function getTodaysHolidayWanderers() {
  return kolmafia.holiday().split("/").flatMap(holiday => holidayWanderers.get(holiday) ?? []);
}
/**
 * Determines whether or not we can safely call visitUrl(), based on whether we're in a fight, multi-fight, choice, etc
 *
 * @returns Whether urls can be safely visited
 */
function canVisitUrl() {
  if (kolmafia.currentRound()) {
    logger.debug(`Current round is ${kolmafia.currentRound()}; you're in combat.`);
    return false;
  }
  if (kolmafia.inMultiFight()) {
    logger.debug("You're in a multifight.");
    return false;
  }
  if (kolmafia.choiceFollowsFight()) {
    logger.debug("A choice follows this fight.");
    return false;
  }
  if (kolmafia.handlingChoice()) {
    logger.debug("You're currently in a choice adventure");
    return false;
  }
  return true;
}
new Map([["standing around flexing their muscles and using grip exercisers", $stat`Muscle`], ["sitting around playing chess and solving complicated-looking logic puzzles", $stat`Mysticality`], ["all wearing sunglasses and dancing", $stat`Moxie`]]);
new Map([["people, all of whom appear to be on fire", $element`hot`], ["people, surrounded by a cloud of eldritch mist", $element`spooky`], ["greasy-looking people furtively skulking around", $element`sleaze`], ["people, surrounded by garbage and clouds of flies", $element`stench`], ["people, clustered around a group of igloos", $element`cold`]]);
new Map([["smoldering bushes on the outskirts of a hedge maze", $element`hot`], ["creepy-looking black bushes on the outskirts of a hedge maze", $element`spooky`], ["purplish, greasy-looking hedges", $element`sleaze`], ["nasty-looking, dripping green bushes on the outskirts of a hedge maze", $element`stench`], ["frost-rimed bushes on the outskirts of a hedge maze", $element`cold`]]);
new Map([["smoke rising from deeper within the maze", $element`hot`], ["a miasma of eldritch vapors rising from deeper within the maze", $element`spooky`], ["a greasy purple cloud hanging over the center of the maze", $element`sleaze`], ["a cloud of green gas hovering over the maze", $element`stench`], ["wintry mists rising from deeper within the maze", $element`cold`]]);
new Map([["with lava slowly oozing out of it", $element`hot`], ["surrounded by creepy black mist", $element`spooky`], ["that occasionally vomits out a greasy ball of hair", $element`sleaze`], ["disgorging a really surprising amount of sewage", $element`stench`], ["occasionally disgorging a bunch of ice cubes", $element`cold`]]);
/**
 * Visit the desc_x.php page for a given thing
 *
 * @param thing Thing to examine
 * @returns Contents of desc_x.php page
 */
function examine(thing) {
  var url = thing instanceof kolmafia.Item ? `desc_item.php?whichitem=${thing.descid}` : thing instanceof kolmafia.Familiar ? `desc_familiar.php?which=${thing.id}` : thing instanceof kolmafia.Effect ? `desc_effect.php?whicheffect=${thing.descid}` : `desc_skill.php?whichskill=${thing.id}`;
  return kolmafia.visitUrl(url);
}
/**
 * Picks an option based on your primestat
 *
 * @param options An object keyed by stat; it must either contain all stats, or have a `default` parameter.
 * @param alternateSource An optional alternate way of determining which Stat to choose by.
 * @returns The option corresponding to your primestat.
 */
var byStat = makeByXFunction(() => kolmafia.myPrimestat().toString());
/**
 * Use an item with visitUrl instead of `use`; this is sometimes useful
 *
 * @param item The item you want to use
 * @returns The html of the resulting page
 */
function directlyUse(item) {
  return kolmafia.visitUrl(`inv_use.php?which=3&whichitem=${item.id}&pwd`);
}
/**
 * Empty a slot, or unequip all instances of a given equipped item
 *
 * @param thing The slot or item in question
 * @returns Whether we succeeded completely--`false` if we unequip some but not all instances of the item.
 */
function unequip(thing) {
  if (thing instanceof kolmafia.Slot) return kolmafia.equip(thing, $item.none);
  var failedSlots = kolmafia.Slot.all().filter(s => {
    // Filter the slot out if it doesn't contain the relevant item
    if (kolmafia.equippedItem(s) !== thing) return false;
    // Filter the slot out if we succeed at unequipping it
    return !unequip(s);
    // This leaves only slots that do contain the item but that we failed to unequip
  });
  if (failedSlots.length) logger.debug(`Failed to unequip ${thing} from slots ${failedSlots.join(", ")}`);
  return failedSlots.length === 0;
}
/**
 * @returns a Date object corresponding to the current in-game day, at midnight
 */
function gameDay() {
  var _map = (kolmafia.todayToString().match(/(\d{4})(\d{2})(\d{2})/) ?? []).map(Number),
    _map2 = _slicedToArray(_map, 4),
    year = _map2[1],
    month = _map2[2],
    day = _map2[3];
  return new Date(year, month - 1, day, 0, 0, 0);
}
/**
 * @param [type="all"] the type of crafting to check for free crafts
 * @returns the number of free crafts available of that type
 */
function freeCrafts() {
  var type = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : "all";
  var effectCrafts = effect => Math.floor(kolmafia.haveEffect(effect) / 5);
  var all = (have$P($skill`Rapid Prototyping`) ? 5 - get$2("_rapidPrototypingUsed") : 0) + (have$P($skill`Expert Corner-Cutter`) ? 5 - get$2("_expertCornerCutterUsed") : 0) + effectCrafts($effect`Inigo's Incantation of Inspiration`) + effectCrafts($effect`Craft Tea`) + effectCrafts($effect`Cooking Concentrate`);
  var food = type === "food" ? 5 - get$2("_cookbookbatCrafting") : 0;
  var smith = type === "smith" ? 5 - get$2("_thorsPliersCrafting") : 0;
  var booze = 0; // currently there is no booze specific free crafting skill
  return all + food + smith + booze;
}
var realmTypes = ["spooky", "stench", "hot", "cold", "sleaze", "fantasy", "pirate", "cyber"];
var todayOrAlways = x => get$2(`_${x}Today`) || get$2(`${x}Always`);
/**
 * @param identifier which realm to check for
 * @returns if that realm is available
 */
function realmAvailable(identifier) {
  switch (identifier) {
    case "fantasy":
      return todayOrAlways("fr");
    case "pirate":
      return todayOrAlways("pr");
    case "cyber":
      return todayOrAlways("cr");
    default:
      return todayOrAlways(`${identifier}Airport`);
  }
}
/**
 * Compute the currently available Lucky Gold Ring Currencies
 * @param realm the realm type to consider
 * @returns The currency for the given zone
 */
function realmCurrency(realm) {
  switch (realm) {
    case "sleaze":
      return $item`Beach Buck`;
    case "spooky":
      return $item`Coinspiracy`;
    case "stench":
      return $item`FunFunds™`;
    case "cold":
      return $item`Wal-Mart gift certificate`;
    case "hot":
      return $item`Volcoino`;
    case "fantasy":
      return $item`Rubee™`;
    case "cyber":
      return $item`0`;
    default:
      return null;
  }
}
/**
 * Compute which Lucky Gold Ring currencies are currently available
 * @returns a list of currently available currencies
 */
function lgrCurrencies() {
  return realmTypes.filter(realm => realmAvailable(realm) && !(realm === "sleaze" && get$2("_luckyGoldRingBeachBuck") >= 25) && !(realm === "spooky" && get$2("_luckyGoldRingCoinspiracy") >= 25) && !(realm === "stench" && get$2("_luckyGoldRingFunFunds") >= 15) && !(realm === "cold" && get$2("_luckyGoldRingWalmart") >= 15) && !(realm === "hot" && get$2("_luckyGoldRingVolcoino") >= 2) && !(realm === "fantasy" && get$2("_luckyGoldRingRubee") >= 10) && !(realm === "cyber" && get$2("_luckyGoldRingBit") >= 10)).map(realmCurrency).filter(notNull);
}
var ACCOUNT_COMBAT_FLAGS = ["aabosses", "wowbar", "bothcombatinterf", "compactmanuel", "eternalmrj", "disablelovebugs", "boringdarts"];
/**
 * Get the current value of all of your account's combat setting flags
 * @param flags An array of the flags you want to get, defaults to all of them
 * @returns An array of objects that contain the flags and their values as booleans
 */
function getCombatFlags() {
  var flags = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : [].concat(ACCOUNT_COMBAT_FLAGS);
  var accountPage = kolmafia.visitUrl("account.php?tab=combat");
  return flags.map(flag => ({
    flag,
    value: kolmafia.xpath(accountPage, `//*[@id="opt_flag_${flag}"]/label/input[@type='checkbox']@checked`)[0] === "checked"
  }));
}
/**
 * Sets the given combat setting flags on your account
 *
 * @param flags A spread array of objects that contain a flag and its desired value; these look like the return value of `getCombatFlags`
 * @returns the result of the associated `visitUrl` call
 */
function setCombatFlags() {
  for (var _len2 = arguments.length, flags = new Array(_len2), _key2 = 0; _key2 < _len2; _key2++) {
    flags[_key2] = arguments[_key2];
  }
  return kolmafia.visitUrl(`account.php?${[].concat(_toConsumableArray(flags.flatMap(_ref11 => {
    var flag = _ref11.flag,
      value = _ref11.value;
    return [`actions[]=flag_${flag}`, `flag_${flag}=${Number(value)}`];
  })), ["action=Update", "am=1", "ajax=1", "pwd"]).join("&"), true}`);
}
/**
 * Extracts a map of gained items from a string, for example from the result
 * of a combat.
 *
 * NOTE: Make sure you trust the source of that text.
 *
 * @param text The text to extract items from
 * @returns A map of items and their quantities
 */
function extractItems(text) {
  return new Map(Object.entries(kolmafia.extractItems(text)).map(_ref13 => {
    var _ref14 = _slicedToArray(_ref13, 2),
      itemName = _ref14[0],
      quantity = _ref14[1];
    return [kolmafia.Item.get(itemName), quantity];
  }));
}
/**
 * Calculate the total weight of a given familiar, including soup & modifiers
 * @param familiar The familiar to use--defaults to your current one
 * @param considerAdjustment Whether to include your `weightAdjustment` in the calculation
 * @returns The total weight of the given familiar
 */
function totalFamiliarWeight() {
  var familiar = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : kolmafia.myFamiliar();
  var considerAdjustment = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : true;
  return clamp(clamp(kolmafia.familiarWeight(familiar), have$P($effect`Fidoxene`) ? 20 : 0, Infinity) + familiar.soupWeight + (considerAdjustment ? kolmafia.weightAdjustment() : 0) + (familiar.feasted ? 10 : 0), 1, Infinity);
}
var regularFamiliarTags = Object.freeze(["animal", "insect", "haseyes", "haswings", "fast", "bite", "flies", "hashands", "wearsclothes", "organic", "vegetable", "hovers", "edible", "food", "sentient", "cute", "mineral", "polygonal", "object", "undead", "cantalk", "evil", "orb", "spooky", "sleaze", "aquatic", "swims", "isclothes", "phallic", "stench", "hot", "hasbeak", "haslegs", "robot", "technological", "hard", "cold", "hasbones", "hasclaws", "reallyevil", "good", "person", "humanoid", "animatedart", "software", "hasshell", "hasstinger"]);
new Set(regularFamiliarTags);
new Map([[$familiar`Nursine`, ["ult_bearhug"]], [$familiar`Caramel`, ["ult_sticktreats"]], [$familiar`Smashmoth`, ["ult_owlstare"]], [$familiar`Slotter`, ["ult_bloodbath"]], [$familiar`Cornbeefadon`, ["ult_pepperscorn"]], [$familiar`Mu`, ["ult_rainbowstorm"]]]);
/**
 * Determines the cost of acquiring an item taking into account your valueOfInventory preference
 *
 * @param item The item to check the price of
 * @param quantity the number of items to acquire
 * @returns The total value of the items
 */
function getAcquirePrice(item) {
  var quantity = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : 1;
  if (quantity <= 0) return 0;
  var currentAmount = kolmafia.availableAmount(item);
  var amountNeeded = Math.max(0, quantity - currentAmount);
  var retrieveCost = kolmafia.retrievePrice(item, currentAmount + quantity) - kolmafia.retrievePrice(item, currentAmount);
  var mallMinPrice = Math.max(100, 2 * kolmafia.autosellPrice(item));
  // If it's easy to meatpaste, just rely on retrieveCost
  if (kolmafia.craftType(item) === "Meatpasting" && retrieveCost > 0) {
    return retrieveCost;
  }
  if (kolmafia.isNpcItem(item) && kolmafia.npcPrice(item) > 0 && kolmafia.npcPrice(item) < kolmafia.mallPrice(item)) {
    // If it's best handled through NPC shops, handle it through NPC shops
    return quantity * kolmafia.npcPrice(item);
  }
  if (item.tradeable) {
    if (kolmafia.mallPrice(item) === mallMinPrice) {
      // Value the ones you have at autosell, because that's what you'd sell them at
      // Value the ones you need to buy at the price you'd buy them at
      return clamp(currentAmount, 0, quantity) * kolmafia.autosellPrice(item) + amountNeeded * kolmafia.mallPrice(item);
    }
    if (kolmafia.mallPrice(item) > mallMinPrice) {
      // Value them all at mall price
      // regardless of whether you already owned them
      return quantity * kolmafia.mallPrice(item);
    }
    // The fallthrough case here is that the mallprice is somehow below mall min
    // That shouldn't really happen
    return quantity * kolmafia.autosellPrice(item);
  }
  if (item.discardable) {
    return have$P(item, quantity) ? quantity * kolmafia.autosellPrice(item) : Infinity;
  }
  // If it can't be traded or discarded, and we're passing into this function, it's free
  // There might some day be specific items we don't want to value like this, because you receive only a limited number
  // We'll burn that bridge when we come to it.
  return 0;
}
/**
 *
 * @param target The place or Monster you expect to fight; accepts Monster, Location, or map of <Monster, number>
 * @returns A map of <Monster, number> defining for each Monster how many are present in target location
 */
function adventureTargetToWeightedMap(target) {
  if (target instanceof kolmafia.Monster) return new Map([[target, 1]]);
  if (target instanceof kolmafia.Location) {
    return new Map(Object.entries(kolmafia.appearanceRates(target, true)).map(_ref15 => {
      var _ref16 = _slicedToArray(_ref15, 2),
        monster = _ref16[0],
        rate = _ref16[1];
      return tuple(kolmafia.toMonster(monster), rate / 100);
    }).filter(_ref17 => {
      var _ref18 = _slicedToArray(_ref17, 2),
        weight = _ref18[1];
      return weight > 0;
    }));
  }
  return target;
}

/** THIS FILE IS AUTOMATICALLY GENERATED. See tools/parseItemSkillNames.ts for more information */
var overlappingItemNames = new Set(["spider web", "really sticky spider web", "dictionary", "NG", "Cloaca-Cola", "yo-yo", "top", "ball", "kite", "yo", "red potion", "blue potion", "bowling ball", "adder", "red button", "tennis ball", "pile of sand", "mushroom", "deluxe mushroom", "spoon"]);
var overlappingSkillNames = new Set(["Lightning Bolt", "Shoot", "Thrust-Smack", "Headbutt", "Toss", "Knife in the Dark", "Sing", "Disarm", "Static Shock", "LIGHT", "BURN", "Extract", "Meteor Shower", "Snipe", "Bite", "Kick", "Punt", "Howl", "Cleave", "Boil", "Slice", "Rainbow"]);

var MACRO_NAME = "Script Autoattack Macro";
/**
 * Get the KoL native ID of the macro with name name.
 *
 * @param name Name of the macro
 * @category Combat
 * @returns {number} The macro ID.
 */
function getMacroId() {
  var name = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : MACRO_NAME;
  var query = `//select[@name="macroid"]/option[text()="${name}"]/@value`;
  var macroText = kolmafia.visitUrl("account_combatmacros.php");
  var macroMatches = kolmafia.xpath(macroText, query);
  if (macroMatches.length === 0) {
    kolmafia.visitUrl("account_combatmacros.php?action=new");
    var newMacroText = kolmafia.visitUrl(`account_combatmacros.php?macroid=0&name=${name}&macrotext=abort&action=save`);
    macroMatches = kolmafia.xpath(newMacroText, query);
  }
  if (macroMatches.length === 0) {
    // We may have hit the macro cap
    if (kolmafia.xpath(macroText, '//select[@name="macroid"]/option').length >= 100) {
      throw new InvalidMacroError(`Please delete at least one existing macro to make some space for Libram`);
    }
    // Otherwise who knows why it failed
    throw new InvalidMacroError(`Could not find or create macro ${name}`);
  }
  return parseInt(macroMatches[0], 10);
}
/**
 * Converts an item name to a Item, or passes through an existing instance of Item
 *
 * @param itemOrName Item name or Item instance
 * @returns KoLmafia Item instance
 */
function itemOrNameToItem(itemOrName) {
  return typeof itemOrName === "string" ? kolmafia.Item.get(itemOrName) : itemOrName;
}
/**
 * Create a string of the item or items provided that is compatible with BALLS syntax and is non-ambiguous
 *
 * @param itemOrItems Item name, item instance, or 2-tuple of item name or item instance
 * @returns BALLS macro-compatible value for item or items provided
 */
function itemOrItemsBallsMacroName(itemOrItems) {
  if (Array.isArray(itemOrItems)) {
    return itemOrItems.map(itemOrItemsBallsMacroName).join(", ");
  } else {
    var item = itemOrNameToItem(itemOrItems);
    return !overlappingItemNames.has(item.name) ? item.name : item.id.toFixed(0);
  }
}
/**
 * Generate a BALLS macro condition to check wither the player has either a single or a 2-tuple of combat items
 *
 * @param itemOrItems Single or 2-tuple of combat items
 * @returns BALLS macro condition
 */
function itemOrItemsBallsMacroPredicate(itemOrItems) {
  if (Array.isArray(itemOrItems)) {
    if (itemOrItems[0] === itemOrItems[1]) return `hastwocombatitems ${itemOrItemsBallsMacroName(itemOrItems[0])}`;
    return itemOrItems.map(itemOrItemsBallsMacroPredicate).join(" && ");
  } else {
    return `hascombatitem ${itemOrItemsBallsMacroName(itemOrItems)}`;
  }
}
/**
 * Converts a skill name to a Skill, or passes through an existing instance of Skill
 *
 * @param skillOrName Skill name or Skill instance
 * @returns KoLmafia Skill instance
 */
function skillOrNameToSkill(skillOrName) {
  if (typeof skillOrName === "string") {
    return kolmafia.Skill.get(skillOrName);
  } else {
    return skillOrName;
  }
}
/**
 * Get a skill name in a form that is appropriate for BALLS macros
 *
 * @param skillOrName Skill name or Skill instance
 * @returns BALLS macro-suitable skill name
 */
function skillBallsMacroName(skillOrName) {
  var skill = skillOrNameToSkill(skillOrName);
  return skill.name.match(/^[A-Za-z ]+$/) && !overlappingSkillNames.has(skill.name) ? skill.name : skill.id;
}
/**
 * Reduces a list of items into pairs to funksling.
 *
 * @param items - The items to be reduced.
 * @returns The reduced list of items or item pairs.
 */
function funkslingReduce() {
  for (var _len = arguments.length, items = new Array(_len), _key = 0; _key < _len; _key++) {
    items[_key] = arguments[_key];
  }
  return items.reduce((acc, item, i, arr) => i % 2 === 0 ? acc.concat(i + 1 < arr.length ? [[item, arr[i + 1]]] : [item]) : acc, []);
}
var InvalidMacroError = /*#__PURE__*/function (_Error) {
  function InvalidMacroError() {
    _classCallCheck(this, InvalidMacroError);
    return _callSuper(this, InvalidMacroError, arguments);
  }
  _inherits(InvalidMacroError, _Error);
  return _createClass(InvalidMacroError);
}(/*#__PURE__*/_wrapNativeSuper(Error));
/**
 * BALLS macro builder for direct submission to KoL.
 * Create a new macro with `new Macro()` and add steps using the instance methods.
 * Uses a fluent interface, so each step returns the object for easy chaining of steps.
 * Each method is also defined as a static method that creates a new Macro with only that step.
 * For example, you can do `Macro.skill('Saucestorm').attack()`.
 */
var Macro$1 = /*#__PURE__*/function () {
  function Macro() {
    _classCallCheck(this, Macro);
    _defineProperty(this, "components", []);
    _defineProperty(this, "name", MACRO_NAME);
  }
  return _createClass(Macro, [{
    key: "toString",
    value:
    /**
     * Convert macro to string.
     *
     * @returns BALLS macro
     */
    function toString() {
      return (this.components.join(";") + ";").replace(/;;+/g, ";");
    }
    /**
     * Gives your macro a new name to be used when saving an autoattack.
     *
     * @param name The name to be used when saving as an autoattack.
     * @returns The macro in question
     */
  }, {
    key: "rename",
    value: function rename(name) {
      this.name = name;
      return this;
    }
    /**
     * Creates a new Macro with a name other than the default name.
     *
     * @param name The name to assign this macro.
     * @returns A new Macro with the assigned name.
     */
  }, {
    key: "save",
    value:
    /**
     * Save a macro to a Mafia property for use in a consult script.
     */
    function save() {
      _set(Macro.SAVED_MACRO_PROPERTY, this.toString());
    }
    /**
     * Load a saved macro from the Mafia property.
     *
     * @returns Loaded macro text
     */
  }, {
    key: "step",
    value:
    /**
     * Statefully add one or several steps to a macro.
     *
     * @param nextSteps The steps to add to the macro.
     * @returns {Macro} This object itself.
     */
    function step() {
      var _ref, _this$components;
      for (var _len2 = arguments.length, nextSteps = new Array(_len2), _key2 = 0; _key2 < _len2; _key2++) {
        nextSteps[_key2] = arguments[_key2];
      }
      var nextStepsStrings = (_ref = []).concat.apply(_ref, _toConsumableArray(nextSteps.map(x => x instanceof Macro ? x.components : [x])));
      (_this$components = this.components).push.apply(_this$components, _toConsumableArray(nextStepsStrings.filter(Boolean)));
      return this;
    }
    /**
     * Statefully add one or several steps to a macro.
     *
     * @param nextSteps The steps to add to the macro.
     * @returns {Macro} This object itself.
     */
  }, {
    key: "submit",
    value:
    /**
     * Submit the built macro to KoL. Only works inside combat.
     *
     * @returns Contents of the fight page after macro submission
     */
    function submit() {
      var final = this.toString();
      return kolmafia.visitUrl(`fight.php?action=macro&macrotext=${kolmafia.urlEncode(final)}`, true, true);
    }
    /**
     * Set this macro as a KoL native autoattack.
     */
  }, {
    key: "setAutoAttack",
    value: function setAutoAttack() {
      var id = Macro.cachedMacroIds.get(this.name);
      if (id === undefined) {
        id = getMacroId(this.name);
        Macro.cachedMacroIds.set(this.name, id);
      }
      if (kolmafia.getAutoAttack() === 99000000 + id && this.toString() === Macro.cachedAutoAttacks.get(this.name)) {
        // This macro is already set. Don"t make the server request.
        return;
      }
      kolmafia.visitUrl(`account_combatmacros.php?macroid=${id}&name=${kolmafia.urlEncode(this.name)}&macrotext=${kolmafia.urlEncode(this.toString())}&action=save`, true, true);
      kolmafia.visitUrl(`account.php?am=1&action=autoattack&value=${99000000 + id}&ajax=1`);
      Macro.cachedAutoAttacks.set(this.name, this.toString());
    }
    /**
     * Renames the macro, then sets it as an autoattack.
     *
     * @param name The name to save the macro under as an autoattack.
     */
  }, {
    key: "setAutoAttackAs",
    value: function setAutoAttackAs(name) {
      this.name = name;
      this.setAutoAttack();
    }
    /**
     * Clear all cached autoattacks, and delete all stored macros server-side.
     */
  }, {
    key: "abort",
    value:
    /**
     * Add an "abort" step to this macro.
     *
     * @returns {Macro} This object itself.
     */
    function abort() {
      return this.step("abort");
    }
    /**
     * Create a new macro with an "abort" step.
     *
     * @returns {Macro} This object itself.
     */
  }, {
    key: "abortWithWarning",
    value:
    /**
     * Adds an "abort" step to this macro, with a warning message to print
     *
     * @param warning The warning message to print
     * @returns  {Macro} This object itself.
     */
    function abortWithWarning(warning) {
      return this.step(`abort "${warning}"`);
    }
    /**
     * Create a new macro with an "abort" step to this macro, with a warning message to print
     *
     * @param warning The warning message to print
     * @returns {Macro} This object itself.
     */
  }, {
    key: "strict",
    value:
    /**
     * Add a "strict" step to this macro.
     *
     * @returns {Macro} This object itself.
     */
    function strict() {
      return this.step("strict");
    }
    /**
     * Add a "strict" step to this macro.
     *
     * @returns {Macro} This object itself.
     */
  }, {
    key: "lenient",
    value:
    /**
     * Add a "lenient" step to this macro.
     *
     * @returns {Macro} This object itself.
     */
    function lenient() {
      return this.step("lenient");
    }
    /**
     * Add a "lenient" step to this macro.
     *
     * @returns {Macro} This object itself.
     */
  }, {
    key: "scrollWhenDone",
    value:
    /**
     * Add a "scrollwhendone" step to this macro.
     *
     * @returns {Macro} This object itself.
     */
    function scrollWhenDone() {
      return this.step("scrollwhendone");
    }
    /**
     * Add a "scrollwhendone" step to this macro.
     *
     * @returns {Macro} This object itself.
     */
  }, {
    key: "icon",
    value:
    /**
     * Add an "icon [image]" step to this macro.
     *
     * @param item The item to use for the image.
     * @returns {Macro} This object itself.
     */
    function icon(item) {
      return this.step(`icon ${item.image.replace(/\.[^.]+$/, "")}`);
    }
    /**
     * Add an "icon [image]" step to this macro.
     *
     * @param item The item to use for the image.
     * @returns {Macro} This object itself.
     */
  }, {
    key: "runaway",
    value:
    /**
     * Add a "runaway" step to this macro.
     *
     * @returns {Macro} This object itself.
     */
    function runaway() {
      return this.step("runaway");
    }
    /**
     * Create a new macro with an "runaway" step.
     *
     * @returns {Macro} This object itself.
     */
  }, {
    key: "if_",
    value:
    /**
     * Add an "if" statement to this macro.
     *
     * @param condition The BALLS condition for the if statement.
     * @param ifTrue Continuation if the condition is true.
     * @param ifFalse Optional else-branch for the macro.
     * @returns {Macro} This object itself.
     */
    function if_(condition, ifTrue, ifFalse) {
      this.step(`if ${Macro.makeBALLSPredicate(condition)}`).step(ifTrue);
      if (ifFalse) {
        this.step("else").step(ifFalse);
      }
      return this.step("endif");
    }
    /**
     * Create a new macro with an "if" statement.
     *
     * @param condition The BALLS condition for the if statement.
     * @param ifTrue Continuation if the condition is true.
     * @param ifFalse Optional else-branch for the macro.
     * @returns {Macro} This object itself.
     */
  }, {
    key: "ifNot",
    value:
    /**
     * Add an "if" statement to this macro, inverting the condition.
     *
     * @param condition The BALLS condition for the if statement.
     * @param ifTrue Continuation if the negated condition is true.
     * @param ifFalse Optional else-branch for the macro.
     * @returns {Macro} This object itself.
     */
    function ifNot(condition, ifTrue, ifFalse) {
      return this.if_(`!${Macro.makeBALLSPredicate(condition)}`, ifTrue, ifFalse);
    }
    /**
     * Create a new macro with an "if" statement, inverting the condition.
     *
     * @param condition The BALLS condition for the if statement.
     * @param ifTrue Continuation if the negated condition is true.
     * @param ifFalse Optional else-branch for the macro.
     * @returns {Macro} This object itself.
     */
  }, {
    key: "while_",
    value:
    /**
     * Add a "while" statement to this macro.
     *
     * @param condition The BALLS condition for the while statement.
     * @param contents Loop to repeat while the condition is true.
     * @returns {Macro} This object itself.
     */
    function while_(condition, contents) {
      return this.step(`while ${Macro.makeBALLSPredicate(condition)}`).step(contents).step("endwhile");
    }
    /**
     * Create a new macro with a "while" statement.
     *
     * @param condition The BALLS condition for the while statement.
     * @param contents Loop to repeat while the condition is true.
     * @returns {Macro} This object itself.
     */
  }, {
    key: "externalIf",
    value:
    /**
     * Conditionally add a step to a macro based on a condition evaluated at the time of building the macro.
     *
     * @param condition The JS condition.
     * @param ifTrue Continuation to add if the condition is true.
     * @param ifFalse Optional input to turn this into an if...else statement.
     * @returns {Macro} This object itself.
     */
    function externalIf(condition, ifTrue, ifFalse) {
      if (condition) return this.step(ifTrue);else if (ifFalse) return this.step(ifFalse);else return this;
    }
    /**
     * Create a new macro with a condition evaluated at the time of building the macro.
     *
     * @param condition The JS condition.
     * @param ifTrue Continuation to add if the condition is true.
     * @param ifFalse Optional input to turn this into an if...else statement.
     * @returns {Macro} This object itself.
     */
  }, {
    key: "repeat",
    value:
    /**
     * Add a repeat step to the macro.
     *
     * @param condition The BALLS condition for the repeat statement, optional.
     * @returns {Macro} This object itself.
     */
    function repeat(condition) {
      return condition === undefined ? this.step("repeat") : this.step(`repeat ${Macro.makeBALLSPredicate(condition)}`);
    }
    /**
     * Add one or more skill cast steps to the macro.
     *
     * @param skills Skills to cast.
     * @returns {Macro} This object itself.
     */
  }, {
    key: "skill",
    value: function skill() {
      for (var _len3 = arguments.length, skills = new Array(_len3), _key3 = 0; _key3 < _len3; _key3++) {
        skills[_key3] = arguments[_key3];
      }
      return this.step.apply(this, _toConsumableArray(skills.map(skill => {
        return `skill ${skillBallsMacroName(skill)}`;
      })));
    }
    /**
     * Create a new macro with one or more skill cast steps.
     *
     * @param skills Skills to cast.
     * @returns {Macro} This object itself.
     */
  }, {
    key: "trySkill",
    value:
    /**
     * Add one or more skill cast steps to the macro, where each step checks if you have the skill first.
     *
     * @param skills Skills to try casting.
     * @returns {Macro} This object itself.
     */
    function trySkill() {
      for (var _len4 = arguments.length, skills = new Array(_len4), _key4 = 0; _key4 < _len4; _key4++) {
        skills[_key4] = arguments[_key4];
      }
      return this.step.apply(this, _toConsumableArray(skills.map(skillOrName => skillOrNameToSkill(skillOrName)).map(skill => {
        return Macro.if_(Macro.makeBALLSPredicate(skill), Macro.skill(skill));
      })));
    }
    /**
     * Create a new macro with one or more skill cast steps, where each step checks if you have the skill first.
     *
     * @param skills Skills to try casting.
     * @returns {Macro} This object itself.
     */
  }, {
    key: "trySkillRepeat",
    value:
    /**
     * Add one or more skill-cast-and-repeat steps to the macro, where each step checks if you have the skill first.
     *
     * @param skills Skills to try repeatedly casting.
     * @returns {Macro} This object itself.
     */
    function trySkillRepeat() {
      for (var _len5 = arguments.length, skills = new Array(_len5), _key5 = 0; _key5 < _len5; _key5++) {
        skills[_key5] = arguments[_key5];
      }
      return this.step.apply(this, _toConsumableArray(skills.map(skillOrName => skillOrNameToSkill(skillOrName)).map(skill => {
        return Macro.if_(Macro.makeBALLSPredicate(skill), Macro.skill(skill).repeat(skill));
      })));
    }
    /**
     * Create a new macro with one or more skill-cast-and-repeat steps, where each step checks if you have the skill first.
     *
     * @param skills Skills to try repeatedly casting.
     * @returns {Macro} This object itself.
     */
  }, {
    key: "item",
    value:
    /**
     * Add one or more item steps to the macro.
     *
     * @param items Items to use. Pass a tuple [item1, item2] to funksling.
     * @returns {Macro} This object itself.
     */
    function item() {
      for (var _len6 = arguments.length, items = new Array(_len6), _key6 = 0; _key6 < _len6; _key6++) {
        items[_key6] = arguments[_key6];
      }
      return this.step.apply(this, _toConsumableArray(items.map(itemOrItems => {
        return `use ${itemOrItemsBallsMacroName(itemOrItems)}`;
      })));
    }
    /**
     * Create a new macro with one or more item steps.
     *
     * @param items Items to use. Pass a tuple [item1, item2] to funksling.
     * @returns {Macro} This object itself.
     */
  }, {
    key: "itemQueue",
    value:
    /**
     * Create a new macro with one or more item queue steps.
     * Items will be funkslinged if available.
     *
     * @param items Items to use.
     * @param condition The BALLS condition for the usequeue statement, optional.
     * @param single Whether we should refrain from funkslinging: `true` if you want to avoid funkslinging, defaults to `false`.
     * @returns {Macro} This object itself.
     */
    function itemQueue(items, condition) {
      var single = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : false;
      if (items.length === 0) return this;
      return this.step(`${single ? "usesinglequeue" : "usequeue"} ${condition ? `until ${Macro.makeBALLSPredicate(condition)} :: ` : ""}${items.slice(0, 20).map(itemOrItemsBallsMacroName).join(", ")}`).itemQueue(items.slice(20), condition);
    }
    /**
     * Create a new macro with one or more item queue steps.
     * Items will be funkslinged if available.
     *
     * @param items Items to use.
     * @param condition The BALLS condition for the usequeue statement, optional.
     * @param single Whether we should refrain from funkslinging: `true` if you want to avoid funkslinging, defaults to `false`.
     * @returns {Macro} This object itself.
     */
  }, {
    key: "tryItem",
    value:
    /**
     * Add one or more item steps to the macro, where each step checks to see if you have the item first.
     *
     * @param items Items to try using. Pass a tuple [item1, item2] to funksling.
     * @returns {Macro} This object itself.
     */
    function tryItem() {
      for (var _len7 = arguments.length, items = new Array(_len7), _key7 = 0; _key7 < _len7; _key7++) {
        items[_key7] = arguments[_key7];
      }
      return this.step.apply(this, _toConsumableArray(items.map(item => {
        return Macro.if_(itemOrItemsBallsMacroPredicate(item), Macro.item(item));
      })));
    }
    /**
     * Create a new macro with one or more item steps, where each step checks to see if you have the item first.
     *
     * @param items Items to try using. Pass a tuple [item1, item2] to funksling.
     * @returns {Macro} This object itself.
     */
  }, {
    key: "funkslingItem",
    value:
    /**
     * Add one or more item steps to the macro, and automatically attempting to funksling as many of the items as possible.
     * This function does not check if you can funksling or not.
     *
     * @param items Items to use.
     * @returns {Macro} This object itself.
     */
    function funkslingItem() {
      return this.item.apply(this, _toConsumableArray(funkslingReduce.apply(void 0, arguments)));
    }
    /**
     * Create a new macro with one or more item steps, and automatically attempting to funksling as many of the items as possible.
     * This function does not check if you can funksling or not.
     *
     * @param items Items to use.
     * @returns {Macro} This object itself.
     */
  }, {
    key: "tryFunkslingItem",
    value:
    /**
     * Add one or more item steps to the macro, where each step checks to see if you have the item first,
     * and automatically attempting to funksling as many of the items as possible.
     * This function does not check if you can funksling or not.
     *
     * @param items Items to use.
     * @returns {Macro} This object itself.
     */
    function tryFunkslingItem() {
      return this.tryItem.apply(this, _toConsumableArray(funkslingReduce.apply(void 0, arguments)));
    }
    /**
     * Create a new macro with one or more item steps, where each step checks to see if you have the item first,
     * and automatically attempting to funksling as many of the items as possible.
     * This function does not check if you can funksling or not.
     *
     * @param items Items to use.
     * @returns {Macro} This object itself.
     */
  }, {
    key: "attack",
    value:
    /**
     * Add an attack step to the macro.
     *
     * @returns {Macro} This object itself.
     */
    function attack() {
      return this.step("attack");
    }
    /**
     * Create a new macro with an attack step.
     *
     * @returns {Macro} This object itself.
     */
  }, {
    key: "jiggle",
    value:
    /**
     * Add a "jiggle" step to the macro.
     *
     * @returns {Macro} This object itself.
     */
    function jiggle() {
      return this.step("jiggle");
    }
    /**
     * Create a new macro with a jiggle step.
     *
     * @returns {Macro} This object itself.
     */
  }, {
    key: "ifHolidayWanderer",
    value:
    /**
     * Create an if_ statement based on what holiday of loathing it currently is. On non-holidays, returns the original macro, unmutated.
     *
     * @param macro The macro to place in the if_ statement
     * @returns This macro with supplied macro wapped in if statement matching holiday wanderers
     */
    function ifHolidayWanderer(macro) {
      var todaysWanderers = getTodaysHolidayWanderers();
      if (todaysWanderers.length === 0) return this;
      return this.if_(todaysWanderers.map(monster => `monsterid ${monster.id}`).join(" || "), macro);
    }
    /**
     * Create a new macro starting with an ifHolidayWanderer step.
     *
     * @param macro The macro to place inside the if_ statement
     * @returns New macro with supplied macro wrapped in if statement matching holiday wanderers
     */
  }, {
    key: "ifNotHolidayWanderer",
    value:
    /**
     * Create an if_ statement based on what holiday of loathing it currently is. On non-holidays, returns the original macro, with the input macro appended.
     *
     * @param macro The macro to place in the if_ statement.
     * @returns This macro with supplied macro wrapped in if statement matching monsters that are not holiday wanderers
     */
    function ifNotHolidayWanderer(macro) {
      var todaysWanderers = getTodaysHolidayWanderers();
      if (todaysWanderers.length === 0) return this.step(macro);
      return this.if_(todaysWanderers.map(monster => `!monsterid ${monster.id}`).join(" && "), macro);
    }
    /**
     * Create a new macro starting with an ifNotHolidayWanderer step.
     *
     * @param macro The macro to place inside the if_ statement
     * @returns New macro with supplied macro wrapped in if statement matching monsters that are not holiday wanderers
     */
  }, {
    key: "beginif",
    value:
    /**
     * Create a longer if...elif block, starting with this if statement.
     * @param condition The BALLS condition for the if statement.
     * @param ifTrue Continuation if the condition is true.
     * @returns A MacroIfBlock that holds this macro on it, and will return to this macro upon calling either .else() or .endif()
     */
    function beginif(condition, ifTrue) {
      return new MacroIfBlock(this, condition, ifTrue);
    }
    /**
     * Create a Macro that begins with longer if...elif block, starting with this if statement.
     * @param condition The BALLS condition for the if statement.
     * @param ifTrue Continuation if the condition is true.
     * @returns A MacroIfBlock that holds this macro on it, and will return to this macro upon calling either .else() or .endif()
     */
  }], [{
    key: "rename",
    value: function rename(name) {
      return new this().rename(name);
    }
  }, {
    key: "load",
    value: function load() {
      var _this;
      return (_this = new this()).step.apply(_this, _toConsumableArray(get$2(Macro.SAVED_MACRO_PROPERTY).split(";")));
    }
    /**
     * Clear the saved macro in the Mafia property.
     */
  }, {
    key: "clearSaved",
    value: function clearSaved() {
      kolmafia.removeProperty(Macro.SAVED_MACRO_PROPERTY);
    }
  }, {
    key: "step",
    value: function step() {
      var _this2;
      return (_this2 = new this()).step.apply(_this2, arguments);
    }
  }, {
    key: "clearAutoAttackMacros",
    value: function clearAutoAttackMacros() {
      var _iterator = _createForOfIteratorHelper(Macro.cachedAutoAttacks.keys()),
        _step;
      try {
        for (_iterator.s(); !(_step = _iterator.n()).done;) {
          var name = _step.value;
          var id = Macro.cachedMacroIds.get(name) ?? getMacroId(name);
          kolmafia.visitUrl(`account_combatmacros.php?macroid=${id}&action=edit&what=Delete&confirm=1`);
          Macro.cachedAutoAttacks.delete(name);
          Macro.cachedMacroIds.delete(name);
        }
      } catch (err) {
        _iterator.e(err);
      } finally {
        _iterator.f();
      }
    }
  }, {
    key: "abort",
    value: function abort() {
      return new this().abort();
    }
  }, {
    key: "abortWithWarning",
    value: function abortWithWarning(warning) {
      return new this().abortWithWarning(warning);
    }
  }, {
    key: "strict",
    value: function strict() {
      return new this().strict();
    }
  }, {
    key: "lenient",
    value: function lenient() {
      return new this().lenient();
    }
  }, {
    key: "scrollWhenDone",
    value: function scrollWhenDone() {
      return new this().scrollWhenDone();
    }
  }, {
    key: "icon",
    value: function icon(item) {
      return new this().icon(item);
    }
  }, {
    key: "runaway",
    value: function runaway() {
      return new this().runaway();
    }
    /**
     *
     * @param condition The BALLS condition or a type to make a condition for (Monster, Item, Skill, etc.)
     * @returns {string} The BALLS condition string
     */
  }, {
    key: "makeBALLSPredicate",
    value: function makeBALLSPredicate(condition) {
      if (condition instanceof kolmafia.Monster) {
        return `monsterid ${condition.id}`;
      } else if (condition instanceof Array) {
        if (condition[0] instanceof kolmafia.Item) return itemOrItemsBallsMacroPredicate(condition);
        return `(${condition.map(entry => Macro.makeBALLSPredicate(entry)).join(" || ")})`;
      } else if (condition instanceof kolmafia.Effect) {
        return `haseffect ${condition.id}`;
      } else if (condition instanceof kolmafia.Skill) {
        return condition.combat ? `hasskill ${skillBallsMacroName(condition)}` : `knowsskill ${condition.id}`;
      } else if (condition instanceof kolmafia.Item) {
        if (!condition.combat) {
          throw new InvalidMacroError(`Item ${condition} cannot be made a valid BALLS predicate (it is not combat-usable)`);
        }
        return `hascombatitem ${itemOrItemsBallsMacroName(condition)}`;
      } else if (condition instanceof kolmafia.Location) {
        var snarfblat = condition.id;
        if (snarfblat < 1) {
          throw new InvalidMacroError(`Location ${condition} cannot be made a valid BALLS predicate (it has no location id)`);
        }
        return `snarfblat ${snarfblat}`;
      } else if (condition instanceof kolmafia.Class) {
        if (condition.id > 6) {
          throw new InvalidMacroError(`Class ${condition} cannot be made a valid BALLS predicate (it is not a standard class)`);
        }
        return condition.toString().replaceAll(" ", "").toLowerCase();
      } else if (condition instanceof kolmafia.Stat) {
        return `${condition.toString().toLowerCase()}class`;
      } else if (condition instanceof kolmafia.Phylum) {
        return `monsterphylum ${condition}`;
      } else if (condition instanceof kolmafia.Element) {
        return `monsterelement ${condition}`;
      } else if (["indoor", "outdoor", "underground", "underwater", "none"].includes(condition)) {
        return `environment ${condition}`;
      }
      return condition;
    }
  }, {
    key: "if_",
    value: function if_(condition, ifTrue, ifFalse) {
      return new this().if_(condition, ifTrue, ifFalse);
    }
  }, {
    key: "ifNot",
    value: function ifNot(condition, ifTrue, ifFalse) {
      return new this().ifNot(condition, ifTrue, ifFalse);
    }
  }, {
    key: "while_",
    value: function while_(condition, contents) {
      return new this().while_(condition, contents);
    }
  }, {
    key: "externalIf",
    value: function externalIf(condition, ifTrue, ifFalse) {
      return new this().externalIf(condition, ifTrue, ifFalse);
    }
  }, {
    key: "skill",
    value: function skill() {
      var _this3;
      return (_this3 = new this()).skill.apply(_this3, arguments);
    }
  }, {
    key: "trySkill",
    value: function trySkill() {
      var _this4;
      return (_this4 = new this()).trySkill.apply(_this4, arguments);
    }
  }, {
    key: "trySkillRepeat",
    value: function trySkillRepeat() {
      var _this5;
      return (_this5 = new this()).trySkillRepeat.apply(_this5, arguments);
    }
  }, {
    key: "item",
    value: function item() {
      var _this6;
      return (_this6 = new this()).item.apply(_this6, arguments);
    }
  }, {
    key: "itemQueue",
    value: function itemQueue(items, condition) {
      var single = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : false;
      return new this().itemQueue(items, condition, single);
    }
  }, {
    key: "tryItem",
    value: function tryItem() {
      var _this7;
      return (_this7 = new this()).tryItem.apply(_this7, arguments);
    }
  }, {
    key: "funkslingItem",
    value: function funkslingItem() {
      var _this8;
      return (_this8 = new this()).funkslingItem.apply(_this8, arguments);
    }
  }, {
    key: "tryFunkslingItem",
    value: function tryFunkslingItem() {
      var _this9;
      return (_this9 = new this()).tryFunkslingItem.apply(_this9, arguments);
    }
  }, {
    key: "attack",
    value: function attack() {
      return new this().attack();
    }
  }, {
    key: "jiggle",
    value: function jiggle() {
      return new this().jiggle();
    }
  }, {
    key: "ifHolidayWanderer",
    value: function ifHolidayWanderer(macro) {
      return new this().ifHolidayWanderer(macro);
    }
  }, {
    key: "ifNotHolidayWanderer",
    value: function ifNotHolidayWanderer(macro) {
      return new this().ifNotHolidayWanderer(macro);
    }
  }, {
    key: "beginif",
    value: function beginif(condition, ifTrue) {
      return new this().beginif(condition, ifTrue);
    }
  }]);
}();
/**
 * Adventure in a location and handle all combats with a given macro.
 * To use this function you will need to create a consult script that runs Macro.load().submit() and a CCS that calls that consult script.
 * See examples/consult.ts for an example.
 *
 * @category Combat
 * @param loc Location to adventure in.
 * @param macro Macro to execute.
 */
_defineProperty(Macro$1, "SAVED_MACRO_PROPERTY", "libram_savedMacro");
_defineProperty(Macro$1, "cachedMacroIds", new Map());
_defineProperty(Macro$1, "cachedAutoAttacks", new Map());
var StrictMacro = /*#__PURE__*/function (_Macro2) {
  function StrictMacro() {
    _classCallCheck(this, StrictMacro);
    return _callSuper(this, StrictMacro, arguments);
  }
  _inherits(StrictMacro, _Macro2);
  return _createClass(StrictMacro, [{
    key: "skill",
    value:
    /**
     * Add one or more skill cast steps to the macro.
     *
     * @param skills Skills to cast.
     * @returns {StrictMacro} This object itself.
     */
    function skill() {
      for (var _len8 = arguments.length, skills = new Array(_len8), _key8 = 0; _key8 < _len8; _key8++) {
        skills[_key8] = arguments[_key8];
      }
      return _superPropGet(StrictMacro, "skill", this, 3)(skills);
    }
    /**
     * Create a new macro with one or more skill cast steps.
     *
     * @param skills Skills to cast.
     * @returns {StrictMacro} This object itself.
     */
  }, {
    key: "item",
    value:
    /**
     * Add one or more item steps to the macro.
     *
     * @param items Items to use. Pass a tuple [item1, item2] to funksling.
     * @returns {StrictMacro} This object itself.
     */
    function item() {
      for (var _len9 = arguments.length, items = new Array(_len9), _key9 = 0; _key9 < _len9; _key9++) {
        items[_key9] = arguments[_key9];
      }
      return _superPropGet(StrictMacro, "item", this, 3)(items);
    }
    /**
     * Create a new macro with one or more item steps.
     *
     * @param items Items to use. Pass a tuple [item1, item2] to funksling.
     * @returns {StrictMacro} This object itself.
     */
  }, {
    key: "trySkill",
    value:
    /**
     * Add one or more skill cast steps to the macro, where each step checks if you have the skill first.
     *
     * @param skills Skills to try casting.
     * @returns {StrictMacro} This object itself.
     */
    function trySkill() {
      for (var _len0 = arguments.length, skills = new Array(_len0), _key0 = 0; _key0 < _len0; _key0++) {
        skills[_key0] = arguments[_key0];
      }
      return _superPropGet(StrictMacro, "trySkill", this, 3)(skills);
    }
    /**
     * Create a new macro with one or more skill cast steps, where each step checks if you have the skill first.
     *
     * @param skills Skills to try casting.
     * @returns {StrictMacro} This object itself.
     */
  }, {
    key: "tryItem",
    value:
    /**
     * Add one or more item steps to the macro, where each step checks to see if you have the item first.
     *
     * @param items Items to try using. Pass a tuple [item1, item2] to funksling.
     * @returns {StrictMacro} This object itself.
     */
    function tryItem() {
      for (var _len1 = arguments.length, items = new Array(_len1), _key1 = 0; _key1 < _len1; _key1++) {
        items[_key1] = arguments[_key1];
      }
      return _superPropGet(StrictMacro, "tryItem", this, 3)(items);
    }
    /**
     * Create a new macro with one or more item steps, where each step checks to see if you have the item first.
     *
     * @param items Items to try using. Pass a tuple [item1, item2] to funksling.
     * @returns {StrictMacro} This object itself.
     */
  }, {
    key: "funkslingItem",
    value:
    /**
     * Add one or more item steps to the macro, and automatically attempting to funksling as many of the items as possible.
     * This function does not check if you can funksling or not.
     *
     * @param items Items to use.
     * @returns {StrictMacro} This object itself.
     */
    function funkslingItem() {
      for (var _len10 = arguments.length, items = new Array(_len10), _key10 = 0; _key10 < _len10; _key10++) {
        items[_key10] = arguments[_key10];
      }
      return _superPropGet(StrictMacro, "funkslingItem", this, 3)(items);
    }
    /**
     * Create a new macro with one or more item steps, and automatically attempting to funksling as many of the items as possible.
     * This function does not check if you can funksling or not.
     *
     * @param items Items to use.
     * @returns {StrictMacro} This object itself.
     */
  }, {
    key: "tryFunkslingItem",
    value:
    /**
     * Add one or more item steps to the macro, where each step checks to see if you have the item first,
     * and automatically attempting to funksling as many of the items as possible.
     * This function does not check if you can funksling or not.
     *
     * @param items Items to use.
     * @returns {StrictMacro} This object itself.
     */
    function tryFunkslingItem() {
      for (var _len11 = arguments.length, items = new Array(_len11), _key11 = 0; _key11 < _len11; _key11++) {
        items[_key11] = arguments[_key11];
      }
      return _superPropGet(StrictMacro, "tryFunkslingItem", this, 3)(items);
    }
    /**
     * Create a new macro with one or more item steps, where each step checks to see if you have the item first,
     * and automatically attempting to funksling as many of the items as possible.
     * This function does not check if you can funksling or not.
     *
     * @param items Items to use.
     * @returns {StrictMacro} This object itself.
     */
  }, {
    key: "trySkillRepeat",
    value:
    /**
     * Add one or more skill-cast-and-repeat steps to the macro, where each step checks if you have the skill first.
     *
     * @param skills Skills to try repeatedly casting.
     * @returns {StrictMacro} This object itself.
     */
    function trySkillRepeat() {
      for (var _len12 = arguments.length, skills = new Array(_len12), _key12 = 0; _key12 < _len12; _key12++) {
        skills[_key12] = arguments[_key12];
      }
      return _superPropGet(StrictMacro, "trySkillRepeat", this, 3)(skills);
    }
    /**
     * Create a new macro with one or more skill-cast-and-repeat steps, where each step checks if you have the skill first.
     *
     * @param skills Skills to try repeatedly casting.
     * @returns {StrictMacro} This object itself.
     */
  }], [{
    key: "skill",
    value: function skill() {
      var _this0;
      return (_this0 = new this()).skill.apply(_this0, arguments);
    }
  }, {
    key: "item",
    value: function item() {
      var _this1;
      return (_this1 = new this()).item.apply(_this1, arguments);
    }
  }, {
    key: "trySkill",
    value: function trySkill() {
      var _this10;
      return (_this10 = new this()).trySkill.apply(_this10, arguments);
    }
  }, {
    key: "tryItem",
    value: function tryItem() {
      var _this11;
      return (_this11 = new this()).tryItem.apply(_this11, arguments);
    }
  }, {
    key: "funkslingItem",
    value: function funkslingItem() {
      var _this12;
      return (_this12 = new this()).funkslingItem.apply(_this12, arguments);
    }
  }, {
    key: "tryFunkslingItem",
    value: function tryFunkslingItem() {
      var _this13;
      return (_this13 = new this()).tryFunkslingItem.apply(_this13, arguments);
    }
  }, {
    key: "trySkillRepeat",
    value: function trySkillRepeat() {
      var _this14;
      return (_this14 = new this()).trySkillRepeat.apply(_this14, arguments);
    }
  }]);
}(Macro$1);
var MacroIfBlock = /*#__PURE__*/function () {
  function MacroIfBlock(macro, condition, ifTrue) {
    _classCallCheck(this, MacroIfBlock);
    _defineProperty(this, "root", void 0);
    _defineProperty(this, "components", []);
    _defineProperty(this, "baseClass", void 0);
    this.root = macro;
    this.baseClass = this.root.constructor;
    this.append(this.baseClass.if_(condition, ifTrue));
    this.components.pop(); // Remove "endif";
  }
  return _createClass(MacroIfBlock, [{
    key: "append",
    value: function append() {
      var _this$components2;
      for (var _len13 = arguments.length, components = new Array(_len13), _key13 = 0; _key13 < _len13; _key13++) {
        components[_key13] = arguments[_key13];
      }
      (_this$components2 = this.components).push.apply(_this$components2, _toConsumableArray(components.flatMap(component => component instanceof Macro$1 ? component.components : component)));
      return this;
    }
    /**
     * Append an elif statement to this if block.
     * @param condition The condition for the elif statement.
     * @param macro The macro within the elif statement.
     * @returns This same MacroIfBlock; call endif() or else() to return to your macro.
     */
  }, {
    key: "elseIf",
    value: function elseIf(condition, macro) {
      return this.append(`elif ${this.baseClass.makeBALLSPredicate(condition)}`, macro);
    }
    /**
     * End the elif block, and return to your initial macro.
     * @returns The macro that started this elif block, with all steps appended and the `if` statement closed.
     */
  }, {
    key: "endif",
    value: function endif() {
      var _this$root;
      return (_this$root = this.root).step.apply(_this$root, _toConsumableArray(this.components)).step("endif");
    }
    /**
     * End your elif block with a final `else`, and return to your initial macro.
     * @param macro The macro to pass into the `else` block.
     * @returns The macro that started this elif block, with all steps appended and the `if` statement closed.
     */
  }, {
    key: "else",
    value: function _else(macro) {
      return this.append("else", macro).endif();
    }
  }]);
}();

function toMaximizerName(_ref) {
  var name = _ref.name,
    id = _ref.id;
  return name.includes(";") ? `¶${id}` : name;
}
/**
 * Merges a partial set of maximizer options onto a full set maximizer options. We merge via overriding for all boolean properties and for onlySlot, and concat all other array properties.
 *
 * @param defaultOptions MaximizeOptions to use as a "base."
 * @param addendums Options to attempt to merge onto defaultOptions.
 * @returns Merged maximizer options
 */
function mergeMaximizeOptions(defaultOptions, addendums) {
  return {
    updateOnFamiliarChange: addendums.updateOnFamiliarChange ?? defaultOptions.updateOnFamiliarChange,
    updateOnCanEquipChanged: addendums.updateOnCanEquipChanged ?? defaultOptions.updateOnCanEquipChanged,
    updateOnLocationChange: addendums.updateOnLocationChange ?? defaultOptions.updateOnLocationChange,
    useOutfitCaching: addendums.useOutfitCaching ?? defaultOptions.useOutfitCaching,
    forceEquip: [].concat(_toConsumableArray(defaultOptions.forceEquip), _toConsumableArray(addendums.forceEquip ?? [])),
    preventEquip: [].concat(_toConsumableArray(defaultOptions.preventEquip), _toConsumableArray(addendums.preventEquip ?? [])).filter(item => {
      var _addendums$forceEquip;
      return !defaultOptions.forceEquip.includes(item) && !((_addendums$forceEquip = addendums.forceEquip) !== null && _addendums$forceEquip !== void 0 && _addendums$forceEquip.includes(item));
    }),
    bonusEquip: new Map([].concat(_toConsumableArray(defaultOptions.bonusEquip), _toConsumableArray(addendums.bonusEquip ?? []))),
    onlySlot: addendums.onlySlot ?? defaultOptions.onlySlot,
    preventSlot: [].concat(_toConsumableArray(defaultOptions.preventSlot), _toConsumableArray(addendums.preventSlot ?? [])),
    forceUpdate: addendums.forceUpdate ?? defaultOptions.forceUpdate,
    modes: _objectSpread2(_objectSpread2({}, defaultOptions.modes), addendums.modes ?? {})
  };
}
var defaultMaximizeOptions = {
  updateOnFamiliarChange: true,
  updateOnCanEquipChanged: true,
  updateOnLocationChange: false,
  useOutfitCaching: true,
  forceEquip: [],
  preventEquip: [],
  bonusEquip: new Map(),
  onlySlot: [],
  preventSlot: [],
  forceUpdate: false,
  modes: {}
};
/**
 *
 * @param options Default options for each maximizer run.
 * @param options.updateOnFamiliarChange Re-run the maximizer if familiar has changed. Default true.
 * @param options.updateOnCanEquipChanged Re-run the maximizer if stats have changed what can be equipped. Default true.
 * @param options.forceEquip Equipment to force-equip ("equip X").
 * @param options.preventEquip Equipment to prevent equipping ("-equip X").
 * @param options.bonusEquip Equipment to apply a bonus to ("200 bonus X").
 */
function setDefaultMaximizeOptions(options) {
  Object.assign(defaultMaximizeOptions, options);
}
var modeableCommands$1 = ["backupcamera", "umbrella", "snowsuit", "edpiece", "retrocape", "parka", "jillcandle"];
var modeableItems = {
  backupcamera: $item`backup camera`,
  umbrella: $item`unbreakable umbrella`,
  snowsuit: $item`Snow Suit`,
  edpiece: $item`The Crown of Ed the Undying`,
  retrocape: $item`unwrapped knock-off retro superhero cape`,
  parka: $item`Jurassic Parka`,
  jillcandle: $item`LED candle`
};
var modeableState = {
  backupcamera: () => kolmafia.getProperty("backupCameraMode"),
  umbrella: () => kolmafia.getProperty("umbrellaState"),
  snowsuit: () => kolmafia.getProperty("snowsuit"),
  edpiece: () => kolmafia.getProperty("edPiece"),
  retrocape: () => kolmafia.getProperty("retroCapeSuperhero") + " " + kolmafia.getProperty("retroCapeWashingInstructions"),
  parka: () => kolmafia.getProperty("parkaMode"),
  jillcandle: () => kolmafia.getProperty("ledCandleMode")
};
/**
 * Get set of current modes for modeables
 *
 * @returns Set of modes
 */
function getCurrentModes$1() {
  var modes = {};
  var _iterator = _createForOfIteratorHelper(modeableCommands$1),
    _step;
  try {
    for (_iterator.s(); !(_step = _iterator.n()).done;) {
      var key = _step.value;
      if (kolmafia.haveEquipped(modeableItems[key])) {
        modes[key] = modeableState[key]();
      }
    }
  } catch (err) {
    _iterator.e(err);
  } finally {
    _iterator.f();
  }
  return modes;
}
/**
 * Apply set of modes
 *
 * @param modes Modes to apply
 */
function applyModes(modes) {
  var _iterator2 = _createForOfIteratorHelper(modeableCommands$1),
    _step2;
  try {
    for (_iterator2.s(); !(_step2 = _iterator2.n()).done;) {
      var command = _step2.value;
      if (kolmafia.haveEquipped(modeableItems[command]) && modes[command] !== undefined) {
        if (modeableState[command]() !== modes[command]) {
          kolmafia.cliExecute(command + " " + modes[command]);
        }
      }
    }
  } catch (err) {
    _iterator2.e(err);
  } finally {
    _iterator2.f();
  }
}
// Subset of slots that are valid for caching.
var cachedSlots = $slots`hat, weapon, off-hand, back, shirt, pants, acc1, acc2, acc3, familiar`;
var CacheEntry = /*#__PURE__*/_createClass(function CacheEntry(equipment, rider, familiar, canEquipItemCount, modes) {
  _classCallCheck(this, CacheEntry);
  _defineProperty(this, "equipment", void 0);
  _defineProperty(this, "rider", void 0);
  _defineProperty(this, "familiar", void 0);
  _defineProperty(this, "canEquipItemCount", void 0);
  _defineProperty(this, "modes", void 0);
  this.equipment = equipment;
  this.rider = rider;
  this.familiar = familiar;
  this.canEquipItemCount = canEquipItemCount;
  this.modes = modes;
});
var _outfitSlots = /*#__PURE__*/new WeakMap();
var _useHistory = /*#__PURE__*/new WeakMap();
var _maxSize = /*#__PURE__*/new WeakMap();
var OutfitLRUCache = /*#__PURE__*/function () {
  function OutfitLRUCache(maxSize) {
    _classCallCheck(this, OutfitLRUCache);
    // Current outfits allocated
    _classPrivateFieldInitSpec(this, _outfitSlots, []);
    // Array of indices into #outfitSlots in order of use. Most recent at the front.
    _classPrivateFieldInitSpec(this, _useHistory, []);
    _classPrivateFieldInitSpec(this, _maxSize, void 0);
    _classPrivateFieldSet2(_maxSize, this, maxSize);
  }
  return _createClass(OutfitLRUCache, [{
    key: "checkConsistent",
    value: function checkConsistent() {
      if (_classPrivateFieldGet2(_useHistory, this).length !== _classPrivateFieldGet2(_outfitSlots, this).length || !_toConsumableArray(_classPrivateFieldGet2(_useHistory, this)).sort().every((value, index) => value === index)) {
        throw new Error("Outfit cache consistency failed.");
      }
    }
  }, {
    key: "promote",
    value: function promote(index) {
      _classPrivateFieldSet2(_useHistory, this, [index].concat(_toConsumableArray(_classPrivateFieldGet2(_useHistory, this).filter(i => i !== index))));
      this.checkConsistent();
    }
  }, {
    key: "get",
    value: function get(key) {
      var index = _classPrivateFieldGet2(_outfitSlots, this).indexOf(key);
      if (index < 0) return undefined;
      this.promote(index);
      return `${OutfitLRUCache.OUTFIT_PREFIX} ${index}`;
    }
  }, {
    key: "insert",
    value: function insert(key) {
      var lastUseIndex = undefined;
      if (_classPrivateFieldGet2(_outfitSlots, this).length >= _classPrivateFieldGet2(_maxSize, this)) {
        lastUseIndex = _classPrivateFieldGet2(_useHistory, this).pop();
        if (lastUseIndex === undefined) {
          throw new Error("Outfit cache consistency failed.");
        }
        _classPrivateFieldGet2(_useHistory, this).splice(0, 0, lastUseIndex);
        _classPrivateFieldGet2(_outfitSlots, this)[lastUseIndex] = key;
        this.checkConsistent();
        return `${OutfitLRUCache.OUTFIT_PREFIX} ${lastUseIndex}`;
      } else {
        var index = _classPrivateFieldGet2(_outfitSlots, this).push(key) - 1;
        _classPrivateFieldGet2(_useHistory, this).splice(0, 0, index);
        this.checkConsistent();
        return `${OutfitLRUCache.OUTFIT_PREFIX} ${index}`;
      }
    }
  }, {
    key: "clear",
    value: function clear() {
      _classPrivateFieldSet2(_outfitSlots, this, []);
      _classPrivateFieldSet2(_useHistory, this, []);
    }
  }]);
}();
/**
 * Save current equipment as KoL-native outfit.
 *
 * @param name Name of new outfit.
 */
_defineProperty(OutfitLRUCache, "OUTFIT_PREFIX", "Script Outfit");
function saveOutfit(name) {
  kolmafia.cliExecute(`outfit save ${name}`);
}
// Objective cache entries.
var cachedObjectives = {};
// Outfit cache entries. Keep 6 by default to avoid cluttering list.
var outfitCache$1 = new OutfitLRUCache(6);
// Cache to prevent rescanning all items unnecessarily
var cachedStats = [0, 0, 0];
var cachedCanEquipItemCount = 0;
/**
 * Count the number of unique items that can be equipped.
 *
 * @returns The count of unique items.
 */
function canEquipItemCount() {
  var stats = $stats`Muscle, Mysticality, Moxie`.map(stat => Math.min(kolmafia.myBasestat(stat), 300));
  if (stats.every((value, index) => value === cachedStats[index])) {
    return cachedCanEquipItemCount;
  }
  cachedStats = stats;
  cachedCanEquipItemCount = kolmafia.Item.all().filter(item => kolmafia.canEquip(item)).length;
  return cachedCanEquipItemCount;
}
/**
 * Checks the objective cache for a valid entry.
 *
 * @param cacheKey The cache key to check.
 * @param options Set of maximizer options
 * @returns A valid CacheEntry or null.
 */
function checkCache(cacheKey, options) {
  var entry = cachedObjectives[cacheKey];
  if (!entry) {
    return null;
  }
  if (options.updateOnFamiliarChange && kolmafia.myFamiliar() !== entry.familiar) {
    logger.warning("Equipment found in maximize cache but familiar is different.");
    return null;
  }
  if (options.updateOnCanEquipChanged && entry.canEquipItemCount !== canEquipItemCount()) {
    logger.warning("Equipment found in maximize cache but equippable item list is out of date.");
    return null;
  }
  return entry;
}
/**
 * Applies equipment that was found in the cache.
 *
 * @param entry The CacheEntry to apply
 * @param options Set of maximizer options
 */
function applyCached(entry, options) {
  var outfitName = options.useOutfitCaching ? outfitCache$1.get(entry) : undefined;
  if (outfitName) {
    if (!kolmafia.isWearingOutfit(outfitName)) {
      kolmafia.outfit(outfitName);
    }
    var familiarEquip = entry.equipment.get($slot`familiar`);
    if (familiarEquip) kolmafia.equip($slot`familiar`, familiarEquip);
  } else {
    var _iterator3 = _createForOfIteratorHelper(entry.equipment),
      _step3;
    try {
      for (_iterator3.s(); !(_step3 = _iterator3.n()).done;) {
        var _step3$value = _slicedToArray(_step3.value, 2),
          slot = _step3$value[0],
          item = _step3$value[1];
        if (kolmafia.equippedItem(slot) !== item && kolmafia.availableAmount(item) > 0) {
          kolmafia.equip(slot, item);
        }
      }
    } catch (err) {
      _iterator3.e(err);
    } finally {
      _iterator3.f();
    }
    if (verifyCached(entry) && options.useOutfitCaching) {
      var _outfitName = outfitCache$1.insert(entry);
      logger.info(`Saving equipment to outfit ${_outfitName}.`);
      saveOutfit(_outfitName);
    }
  }
  if (kolmafia.equippedAmount($item`Crown of Thrones`) > 0 && entry.rider.get($item`Crown of Thrones`)) {
    kolmafia.enthroneFamiliar(entry.rider.get($item`Crown of Thrones`) || $familiar.none);
  }
  if (kolmafia.equippedAmount($item`Buddy Bjorn`) > 0 && entry.rider.get($item`Buddy Bjorn`)) {
    kolmafia.bjornifyFamiliar(entry.rider.get($item`Buddy Bjorn`) || $familiar.none);
  }
  applyModes(_objectSpread2(_objectSpread2({}, entry.modes), options.modes));
}
var slotStructure = [$slots`hat`, $slots`back`, $slots`shirt`, $slots`weapon, off-hand`, $slots`pants`, $slots`acc1, acc2, acc3`, $slots`familiar`];
/**
 * Verifies that a CacheEntry was applied successfully.
 *
 * @param entry The CacheEntry to verify
 * @param warn Whether to warn if the cache could not be applied
 * @returns If all desired equipment was appliedn in the correct slots.
 */
function verifyCached(entry) {
  var warn = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : true;
  var success = true;
  var _iterator4 = _createForOfIteratorHelper(slotStructure),
    _step4;
  try {
    for (_iterator4.s(); !(_step4 = _iterator4.n()).done;) {
      var slotGroup = _step4.value;
      var desiredSlots = slotGroup.map(slot => [slot, entry.equipment.get(slot) ?? null]).filter(_ref2 => {
        var _ref3 = _slicedToArray(_ref2, 2),
          item = _ref3[1];
        return item !== null;
      });
      var desiredSet = desiredSlots.map(_ref4 => {
        var _ref5 = _slicedToArray(_ref4, 2),
          item = _ref5[1];
        return item;
      });
      var equippedSet = desiredSlots.map(_ref6 => {
        var _ref7 = _slicedToArray(_ref6, 1),
          slot = _ref7[0];
        return kolmafia.equippedItem(slot);
      });
      if (!setEqual(desiredSet, equippedSet)) {
        if (warn) {
          logger.warning(`Failed to apply cached ${desiredSet.join(", ")} in ${slotGroup.join(", ")}.`);
        }
        success = false;
      }
    }
  } catch (err) {
    _iterator4.e(err);
  } finally {
    _iterator4.f();
  }
  if (kolmafia.equippedAmount($item`Crown of Thrones`) > 0 && entry.rider.get($item`Crown of Thrones`)) {
    if (entry.rider.get($item`Crown of Thrones`) !== kolmafia.myEnthronedFamiliar()) {
      if (warn) {
        logger.warning(`Failed to apply ${entry.rider.get($item`Crown of Thrones`)} in ${$item`Crown of Thrones`}.`);
      }
      success = false;
    }
  }
  if (kolmafia.equippedAmount($item`Buddy Bjorn`) > 0 && entry.rider.get($item`Buddy Bjorn`)) {
    if (entry.rider.get($item`Buddy Bjorn`) !== kolmafia.myBjornedFamiliar()) {
      if (warn) {
        logger.warning(`Failed to apply${entry.rider.get($item`Buddy Bjorn`)} in ${$item`Buddy Bjorn`}.`);
      }
      success = false;
    }
  }
  return success;
}
/**
 * Save current equipment to the objective cache.
 *
 * @param cacheKey The cache key to save.
 * @param options Set of maximizer options
 */
function saveCached(cacheKey, options) {
  var equipment = new Map();
  var rider = new Map();
  var _iterator5 = _createForOfIteratorHelper(cachedSlots),
    _step5;
  try {
    for (_iterator5.s(); !(_step5 = _iterator5.n()).done;) {
      var _slot2 = _step5.value;
      equipment.set(_slot2, kolmafia.equippedItem(_slot2));
    }
  } catch (err) {
    _iterator5.e(err);
  } finally {
    _iterator5.f();
  }
  if (kolmafia.equippedAmount($item`card sleeve`) > 0) {
    equipment.set($slot`card-sleeve`, kolmafia.equippedItem($slot`card-sleeve`));
  }
  if (kolmafia.equippedAmount($item`Crown of Thrones`) > 0) {
    rider.set($item`Crown of Thrones`, kolmafia.myEnthronedFamiliar());
  }
  if (kolmafia.equippedAmount($item`Buddy Bjorn`) > 0) {
    rider.set($item`Buddy Bjorn`, kolmafia.myBjornedFamiliar());
  }
  if (options.preventSlot && options.preventSlot.length > 0) {
    var _iterator6 = _createForOfIteratorHelper(options.preventSlot),
      _step6;
    try {
      for (_iterator6.s(); !(_step6 = _iterator6.n()).done;) {
        var slot = _step6.value;
        equipment.delete(slot);
      }
    } catch (err) {
      _iterator6.e(err);
    } finally {
      _iterator6.f();
    }
    if (options.preventSlot.includes($slot`buddy-bjorn`)) {
      rider.delete($item`Buddy Bjorn`);
    }
    if (options.preventSlot.includes($slot`crown-of-thrones`)) {
      rider.delete($item`Crown of Thrones`);
    }
  }
  if (options.onlySlot && options.onlySlot.length > 0) {
    var _iterator7 = _createForOfIteratorHelper(kolmafia.Slot.all()),
      _step7;
    try {
      for (_iterator7.s(); !(_step7 = _iterator7.n()).done;) {
        var _slot = _step7.value;
        if (!options.onlySlot.includes(_slot)) {
          equipment.delete(_slot);
        }
      }
    } catch (err) {
      _iterator7.e(err);
    } finally {
      _iterator7.f();
    }
    if (!options.onlySlot.includes($slot`buddy-bjorn`)) {
      rider.delete($item`Buddy Bjorn`);
    }
    if (!options.onlySlot.includes($slot`crown-of-thrones`)) {
      rider.delete($item`Crown of Thrones`);
    }
  }
  var entry = new CacheEntry(equipment, rider, kolmafia.myFamiliar(), canEquipItemCount(), _objectSpread2(_objectSpread2({}, getCurrentModes$1()), options.modes));
  cachedObjectives[cacheKey] = entry;
  if (options.useOutfitCaching) {
    var outfitName = outfitCache$1.insert(entry);
    logger.info(`Saving equipment to outfit ${outfitName}.`);
    saveOutfit(outfitName);
  }
}
/**
 * Run the maximizer, but only if the objective and certain pieces of game state haven't changed since it was last run.
 *
 * @param objectives Objectives to maximize for.
 * @param options Options for this run of the maximizer.
 * @param options.updateOnFamiliarChange Re-run the maximizer if familiar has changed. Default true.
 * @param options.updateOnCanEquipChanged Re-run the maximizer if stats have changed what can be equipped. Default true.
 * @param options.forceEquip Equipment to force-equip ("equip X").
 * @param options.preventEquip Equipment to prevent equipping ("-equip X").
 * @param options.bonusEquip Equipment to apply a bonus to ("200 bonus X").
 * @returns Whether the maximize call succeeded.
 */
function maximizeCached(objectives) {
  var options = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {};
  var fullOptions = mergeMaximizeOptions(defaultMaximizeOptions, options);
  var forceEquip = fullOptions.forceEquip,
    preventEquip = fullOptions.preventEquip,
    bonusEquip = fullOptions.bonusEquip,
    onlySlot = fullOptions.onlySlot,
    preventSlot = fullOptions.preventSlot,
    forceUpdate = fullOptions.forceUpdate;
  // Sort each group in objective to ensure consistent ordering in string
  var objective = _toConsumableArray(new Set([].concat(_toConsumableArray(objectives.sort()), _toConsumableArray(forceEquip.map(item => `"equip ${toMaximizerName(item)}"`).sort()), _toConsumableArray(preventEquip.map(item => `-"equip ${toMaximizerName(item)}"`).sort()), _toConsumableArray(onlySlot.map(slot => `${slot}`).sort()), _toConsumableArray(preventSlot.map(slot => `-${slot}`).sort()), _toConsumableArray(Array.from(bonusEquip.entries()).filter(_ref8 => {
    var _ref9 = _slicedToArray(_ref8, 2),
      bonus = _ref9[1];
    return bonus !== 0;
  }).map(_ref0 => {
    var _ref1 = _slicedToArray(_ref0, 2),
      item = _ref1[0],
      bonus = _ref1[1];
    return `${Math.round(bonus * 100) / 100} "bonus ${toMaximizerName(item)}"`;
  }).sort())))).join(", ");
  // Items equipped in slots not touched by the maximizer must be in the cache key
  var untouchedSlots = cachedSlots.filter(slot => preventSlot.includes(slot) || onlySlot.length > 0 && !onlySlot.includes(slot));
  var cacheKey = [objective].concat(_toConsumableArray(untouchedSlots.map(slot => `${slot}:${kolmafia.equippedItem(slot)}`).sort()), [have$P($effect`Offhand Remarkable`), options.updateOnLocationChange && kolmafia.myLocation()]).join("; ");
  var cacheEntry = checkCache(cacheKey, fullOptions);
  if (cacheEntry && !forceUpdate) {
    if (verifyCached(cacheEntry, false)) return true;
    logger.info("Equipment found in maximize cache, equipping...");
    applyCached(cacheEntry, fullOptions);
    if (verifyCached(cacheEntry)) {
      logger.info(`Equipped cached ${cacheKey}`);
      return true;
    }
    logger.warning("Maximize cache application failed, maximizing...");
  }
  var result = kolmafia.maximize(objective, false);
  saveCached(cacheKey, fullOptions);
  return result;
}
function mergeOptionalOptions(optionsA, optionsB) {
  for (var _len = arguments.length, keys = new Array(_len > 2 ? _len - 2 : 0), _key = 2; _key < _len; _key++) {
    keys[_key - 2] = arguments[_key];
  }
  return keys.reduce((current, key) => _objectSpread2(_objectSpread2({}, current), (optionsA[key] || optionsB[key]) === undefined ? {} : {
    [key]: optionsA[key] || optionsB[key]
  }), {});
}
var _maximizeParameters = /*#__PURE__*/new WeakMap();
var _maximizeOptions = /*#__PURE__*/new WeakMap();
var Requirement = /*#__PURE__*/function () {
  /**
   * A convenient way of combining maximization parameters and options
   *
   * @param maximizeParameters Parameters you're attempting to maximize
   * @param maximizeOptions Object potentially containing forceEquips, bonusEquips, preventEquips, and preventSlots
   */
  function Requirement(maximizeParameters, maximizeOptions) {
    _classCallCheck(this, Requirement);
    _classPrivateFieldInitSpec(this, _maximizeParameters, void 0);
    _classPrivateFieldInitSpec(this, _maximizeOptions, void 0);
    _classPrivateFieldSet2(_maximizeParameters, this, maximizeParameters);
    _classPrivateFieldSet2(_maximizeOptions, this, maximizeOptions);
  }
  return _createClass(Requirement, [{
    key: "maximizeParameters",
    get: function get() {
      return _classPrivateFieldGet2(_maximizeParameters, this);
    }
  }, {
    key: "maximizeOptions",
    get: function get() {
      return _classPrivateFieldGet2(_maximizeOptions, this);
    }
    /**
     * Merges two requirements, concanating relevant arrays. Typically used in static form.
     *
     * @param other Requirement to merge with.
     * @returns A new merged Requirement
     */
  }, {
    key: "merge",
    value: function merge(other) {
      var _optionsA$bonusEquip, _optionsB$bonusEquip;
      var optionsA = this.maximizeOptions;
      var optionsB = other.maximizeOptions;
      var optionalBooleans = mergeOptionalOptions(optionsA, optionsB, "updateOnFamiliarChange", "updateOnCanEquipChanged", "updateOnLocationChange", "forceUpdate");
      return new Requirement([].concat(_toConsumableArray(this.maximizeParameters), _toConsumableArray(other.maximizeParameters)), _objectSpread2(_objectSpread2({}, optionalBooleans), {}, {
        forceEquip: [].concat(_toConsumableArray(optionsA.forceEquip ?? []), _toConsumableArray(other.maximizeOptions.forceEquip ?? [])).filter(x => {
          var _other$maximizeOption;
          return !((_other$maximizeOption = other.maximizeOptions.preventEquip) !== null && _other$maximizeOption !== void 0 && _other$maximizeOption.includes(x));
        }),
        preventEquip: [].concat(_toConsumableArray(optionsA.preventEquip ?? []), _toConsumableArray(other.maximizeOptions.preventEquip ?? [])).filter(x => {
          var _other$maximizeOption2;
          return !((_other$maximizeOption2 = other.maximizeOptions.forceEquip) !== null && _other$maximizeOption2 !== void 0 && _other$maximizeOption2.includes(x));
        }),
        bonusEquip: new Map([].concat(_toConsumableArray(((_optionsA$bonusEquip = optionsA.bonusEquip) === null || _optionsA$bonusEquip === void 0 ? void 0 : _optionsA$bonusEquip.entries()) ?? []), _toConsumableArray(((_optionsB$bonusEquip = optionsB.bonusEquip) === null || _optionsB$bonusEquip === void 0 ? void 0 : _optionsB$bonusEquip.entries()) ?? []))),
        onlySlot: [].concat(_toConsumableArray(optionsA.onlySlot ?? []), _toConsumableArray(optionsB.onlySlot ?? [])),
        preventSlot: [].concat(_toConsumableArray(optionsA.preventSlot ?? []), _toConsumableArray(optionsB.preventSlot ?? []))
      }));
    }
    /**
     * Merges a set of requirements together, starting with an empty requirement.
     *
     * @param allRequirements Requirements to merge
     * @returns Merged requirements
     */
  }, {
    key: "maximize",
    value:
    /**
     * Runs maximizeCached, using the maximizeParameters and maximizeOptions contained by this requirement.
     *
     * @returns Whether the maximize call succeeded.
     */
    function maximize() {
      return maximizeCached(this.maximizeParameters, this.maximizeOptions);
    }
    /**
     * Merges requirements, and then runs maximizeCached on the combined requirement.
     *
     * @param requirements Requirements to maximize on
     */
  }], [{
    key: "merge",
    value: function merge(allRequirements) {
      return allRequirements.reduce((x, y) => x.merge(y), new Requirement([], {}));
    }
  }, {
    key: "maximize",
    value: function maximize() {
      for (var _len2 = arguments.length, requirements = new Array(_len2), _key2 = 0; _key2 < _len2; _key2++) {
        requirements[_key2] = arguments[_key2];
      }
      Requirement.merge(requirements).maximize();
    }
  }]);
}();
/**
 * Clear all outfits cached by the maximizer.
 */
function clearMaximizerCache() {
  outfitCache$1.clear();
  for (var member in cachedObjectives) delete cachedObjectives[member];
}

/**
 * Merge a set of constraints into one
 *
 * @param allConstraints Constraints to mege
 * @returns Merged constraints
 */
function mergeConstraints() {
  for (var _len = arguments.length, allConstraints = new Array(_len), _key = 0; _key < _len; _key++) {
    allConstraints[_key] = arguments[_key];
  }
  var familiars = allConstraints.map(constraints => constraints.familiar).filter(Boolean);
  if (familiars.length > 1) {
    // Inconsistent requirements.
    return null;
  }
  var familiar = familiars.find(familiar => familiar);
  return _objectSpread2(_objectSpread2({
    equipmentRequirements: () => Requirement.merge(_toConsumableArray(allConstraints.map(constraints => {
      var _constraints$equipmen;
      return ((_constraints$equipmen = constraints.equipmentRequirements) === null || _constraints$equipmen === void 0 ? void 0 : _constraints$equipmen.call(constraints)) ?? new Requirement([], {});
    }))),
    preparation: () => {
      var success = true;
      for (var _i = 0, _allConstraints = allConstraints; _i < _allConstraints.length; _i++) {
        var constraints = _allConstraints[_i];
        success = success && (!constraints.preparation || constraints.preparation());
      }
      return success;
    }
  }, familiar ? {
    familiar
  } : {}), {}, {
    cost: () => sum(allConstraints, constraints => {
      var _constraints$cost;
      return ((_constraints$cost = constraints.cost) === null || _constraints$cost === void 0 ? void 0 : _constraints$cost.call(constraints)) ?? 0;
    })
  });
}
/**
 * A combat-based action resource in the game (e.g. a free run or free kill).
 */
var ActionSource = /*#__PURE__*/function () {
  /**
   * @param source Source(s) of the action (e.g. item, skill, or familiar needed).
   * @param potential Function returning how many times this action can be used.
   * @param macro Macro to execute this action in combat.
   * @param constraints Constraints required for this action to be available.
   */
  function ActionSource(source, potential, macro) {
    var constraints = arguments.length > 3 && arguments[3] !== undefined ? arguments[3] : {};
    _classCallCheck(this, ActionSource);
    _defineProperty(this, "source", void 0);
    _defineProperty(this, "potential", void 0);
    // Infinity: unlimited
    _defineProperty(this, "macro", void 0);
    _defineProperty(this, "constraints", void 0);
    this.source = source;
    this.potential = potential;
    this.macro = macro;
    this.constraints = constraints;
  }
  /**
   * @returns Name of the action source.
   */
  return _createClass(ActionSource, [{
    key: "name",
    value: function name() {
      return this.source.toString();
    }
    /**
     * @returns Whether the action is available.
     */
  }, {
    key: "available",
    value: function available() {
      return this.potential() > 0;
    }
    /**
     * @returns Cost in meat per usage of the action.
     */
  }, {
    key: "cost",
    value: function cost() {
      return this.constraints.cost ? this.constraints.cost() : 0;
    }
    /**
     * @returns Whether the action costs 0 meat to use.
     */
  }, {
    key: "isFree",
    value: function isFree() {
      return !this.cost || this.cost() === 0;
    }
    /**
     * @returns Whether unlimited uses of the action are available.
     */
  }, {
    key: "isUnlimited",
    value: function isUnlimited() {
      return this.potential() === Infinity;
    }
    /**
     * Create a compound action source with merged constraints.
     *
     * @param others Other actions to have available.
     * @returns Merged constraints, or null if they are inconsistent.
     */
  }, {
    key: "merge",
    value: function merge() {
      for (var _len2 = arguments.length, others = new Array(_len2), _key2 = 0; _key2 < _len2; _key2++) {
        others[_key2] = arguments[_key2];
      }
      var actions = [this].concat(others);
      var constraints = mergeConstraints.apply(void 0, _toConsumableArray(actions.map(action => action.constraints)));
      if (constraints === null) {
        // Inconsistent constraints - no path forward here.
        return null;
      }
      return new ActionSource(actions.flatMap(action => action.source), () => sum(actions, action => action.potential()), Macro$1.step.apply(Macro$1, _toConsumableArray(actions.map(action => action.macro))), constraints);
    }
    /**
     * Perform all preparation necessary to make this action available.
     *
     * @param otherRequirements Any other equipment requirements.
     * @returns Whether preparation succeeded.
     */
  }, {
    key: "prepare",
    value: function prepare(otherRequirements) {
      var _this$constraints$fam, _this$constraints;
      if ((_this$constraints$fam = (_this$constraints = this.constraints).familiar) !== null && _this$constraints$fam !== void 0 && _this$constraints$fam.call(_this$constraints)) {
        if (!kolmafia.useFamiliar(this.constraints.familiar())) return false;
      }
      if (this.constraints.equipmentRequirements) {
        var requirement = otherRequirements ? otherRequirements.merge(this.constraints.equipmentRequirements()) : this.constraints.equipmentRequirements();
        if (!requirement.maximize()) return false;
      }
      if (this.constraints.preparation) return this.constraints.preparation();
      return true;
    }
    /**
     * Perform all preparation necessary to make this action available.
     * Throws an error if preparation fails.
     *
     * @param otherRequirements Any other equipment requirements.
     */
  }, {
    key: "ensure",
    value: function ensure(otherRequirements) {
      if (!this.prepare(otherRequirements)) {
        throw new Error(`Failed to prepare action ${this.name()}.`);
      }
    }
  }]);
}();
/**
 * See if a supplied action meets a set of constraints
 *
 * @param action Action to test
 * @param constraints Constraints to apply
 * @returns Whether action meets constraints
 */
_defineProperty(ActionSource, "defaultPriceFunction", item => getAcquirePrice(item) > 0 ? getAcquirePrice(item) : Infinity);
function filterAction(action, constraints) {
  var _constraints$requireF, _constraints$requireU, _constraints$noFamili, _constraints$noRequir, _constraints$noPrepar, _constraints$maximumC;
  return action.available() && (constraints.allowedAction === undefined || constraints.allowedAction(action)) && !((_constraints$requireF = constraints.requireFamiliar) !== null && _constraints$requireF !== void 0 && _constraints$requireF.call(constraints) && !action.constraints.familiar) && !((_constraints$requireU = constraints.requireUnlimited) !== null && _constraints$requireU !== void 0 && _constraints$requireU.call(constraints) && !action.isUnlimited()) && !((_constraints$noFamili = constraints.noFamiliar) !== null && _constraints$noFamili !== void 0 && _constraints$noFamili.call(constraints) && action.constraints.familiar) && !((_constraints$noRequir = constraints.noRequirements) !== null && _constraints$noRequir !== void 0 && _constraints$noRequir.call(constraints) && action.constraints.equipmentRequirements) && !((_constraints$noPrepar = constraints.noPreparation) !== null && _constraints$noPrepar !== void 0 && _constraints$noPrepar.call(constraints) && action.constraints.preparation) && action.cost() <= (((_constraints$maximumC = constraints.maximumCost) === null || _constraints$maximumC === void 0 ? void 0 : _constraints$maximumC.call(constraints)) ?? 0);
}
/**
 * Find an available action source subject to constraints.
 *
 * @param actions Action source list.
 * @param constraints Preexisting constraints that restrict possible sources.
 * @returns Available action source satisfying constraints, or null.
 */
function findActionSource(actions) {
  var constraints = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {};
  var validActions = actions.filter(actions => filterAction(actions, constraints));
  if (validActions.length < 1) return null;
  return validActions.reduce((a, b) => a.cost() <= b.cost() ? a : b);
}

var PriceAge;
(function (PriceAge) {
  PriceAge[PriceAge["HISTORICAL"] = 0] = "HISTORICAL";
  PriceAge[PriceAge["RECENT"] = 1] = "RECENT";
  PriceAge[PriceAge["TODAY"] = 2] = "TODAY";
})(PriceAge || (PriceAge = {}));
/**
 * @returns Whether the Asdon is our current active workshed
 */
function installed$4() {
  return kolmafia.getWorkshed() === $item`Asdon Martin keyfob (on ring)`;
}
var fuelSkiplist = $items`cup of "tea", thermos of "whiskey", Lucky Lindy, Bee's Knees, Sockdollager, Ish Kabibble, Hot Socks, Phonus Balonus, Flivver, Sloppy Jalopy, glass of "milk"`;
/**
 * Internal function used to determine whether a historical price is recent enough
 *
 * @param item The item to check
 * @returns Whether a price is too old to trust
 */
function priceTooOld(item) {
  return kolmafia.historicalPrice(item) === 0 || kolmafia.historicalAge(item) >= 7;
}
/**
 * @param item The item in question
 * @returns Mall max if historicalPrice is -1; otherwise, the historical price
 */
function historicalPriceOrMax(item) {
  var historical = kolmafia.historicalPrice(item);
  return historical < 0 ? 999999999 : historical;
}
/**
 * @param item The item in question
 * @returns Mall max if historicalPrice is -1; otherwise, the mall price
 */
function mallPriceOrMax(item) {
  var mall = kolmafia.mallPrice(item);
  return mall < 0 ? 999999999 : mall;
}
/**
 * Combined internal function to determine the price of an item
 *
 * @param item The item in question
 * @param priceAge How do we decide when to use historical vs real mall prices?
 * @returns The price of the item in question
 */
function price(item, priceAge) {
  switch (priceAge) {
    case PriceAge.HISTORICAL:
      {
        var historical = historicalPriceOrMax(item);
        return historical === 0 ? mallPriceOrMax(item) : historical;
      }
    case PriceAge.RECENT:
      return priceTooOld(item) ? mallPriceOrMax(item) : historicalPriceOrMax(item);
    case PriceAge.TODAY:
      return mallPriceOrMax(item);
  }
}
function inventoryItems() {
  return kolmafia.Item.all().filter(isFuelItem).filter(item => have$P(item) && [100, kolmafia.autosellPrice(item)].includes(price(item, PriceAge.RECENT)));
}
/**
 * @param it The item in question
 * @param priceAge The PriceAge option to apply
 * @returns Meat per fuel of an item
 */
function calculateFuelUnitCost(it) {
  var priceAge = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : PriceAge.RECENT;
  var units = getAverageAdventures(it);
  return price(it, priceAge) / units;
}
/**
 * @param it the item in question
 * @returns Can `it` be used as Asdon fuel?
 */
function isFuelItem(it) {
  return !kolmafia.isNpcItem(it) && it.fullness + it.inebriety > 0 && getAverageAdventures(it) > 0 && it.tradeable && it.discardable && !fuelSkiplist.includes(it);
}
/**
 * @returns The best fuel options available to us at this time
 */
function getBestFuels() {
  // Three stages.
  // 1. Filter to reasonable items using historical cost (within 5x of historical best).
  var allFuel = kolmafia.Item.all().filter(isFuelItem);
  if (allFuel.filter(item => kolmafia.historicalPrice(item) === 0).length > 100) {
    kolmafia.mallPrices("food");
    kolmafia.mallPrices("booze");
  }
  var keyHistorical = item => calculateFuelUnitCost(item, PriceAge.HISTORICAL);
  allFuel.sort((x, y) => keyHistorical(x) - keyHistorical(y));
  var bestUnitCost = keyHistorical(allFuel[0]);
  var firstBadIndex = allFuel.findIndex(item => keyHistorical(item) > 5 * bestUnitCost);
  var potentialFuel = firstBadIndex > 0 ? allFuel.slice(0, firstBadIndex) : allFuel;
  // 2. Filter to top 10 candidates using prices at most a week old.
  if (potentialFuel.filter(item => priceTooOld(item)).length > 100) {
    kolmafia.mallPrices("food");
    kolmafia.mallPrices("booze");
  }
  var key1 = item => -getAverageAdventures(item);
  var key2 = item => calculateFuelUnitCost(item, PriceAge.RECENT);
  potentialFuel.sort((x, y) => key1(x) - key1(y));
  potentialFuel.sort((x, y) => key2(x) - key2(y));
  // 3. Find result using precise price for those top candidates.
  var candidates = potentialFuel.slice(0, 10);
  var key3 = item => calculateFuelUnitCost(item, PriceAge.TODAY);
  candidates.sort((x, y) => key3(x) - key3(y));
  if (calculateFuelUnitCost(candidates[0], PriceAge.TODAY) > 100) {
    throw new Error("Could not identify any fuel with efficiency better than 100 meat per fuel. " + "This means something went wrong.");
  }
  return candidates;
}
/**
 * Fuel your Asdon Martin with a given quantity of a given item
 *
 * @param it Item to fuel with.
 * @param quantity Number of items to fuel with.
 * @returns Whether we succeeded at fueling with the given items.
 */
function insertFuel(it) {
  var quantity = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : 1;
  var result = kolmafia.visitUrl(`campground.php?action=fuelconvertor&pwd&qty=${quantity}&iid=${it.id}&go=Convert%21`);
  return result.includes("The display updates with a");
}
/**
 * Fill your Asdon Martin to the given fuel level in the cheapest way possible
 *
 * @param targetUnits Fuel level to attempt to reach.
 * @returns Whether we succeeded at filling to the target fuel level.
 */
function fillTo(targetUnits) {
  if (!installed$4()) return false;
  // if in Hardcore/ronin, skip the price calculation and just use soda bread
  var bestFuels = kolmafia.canInteract() ? getBestFuels() : [$item`loaf of soda bread`];
  while (bestFuels.length > 0 && kolmafia.getFuel() < targetUnits) {
    var curFuel = bestFuels.shift();
    var nextFuel = bestFuels.at(0);
    var curEfficiency = kolmafia.mallPrice(curFuel) / getAverageAdventures(curFuel);
    var desiredEfficiency = nextFuel ? kolmafia.mallPrice(nextFuel) / getAverageAdventures(nextFuel) : curEfficiency;
    var ceiling = Math.floor(kolmafia.mallPrice(curFuel) * (1.0 + desiredEfficiency - curEfficiency));
    var count = Math.ceil(targetUnits / getAverageAdventures(curFuel));
    if (!kolmafia.canInteract()) {
      // If we can't access the bugbear bakery but do have access to all-purpose flower, use that to get soda bread
      if (kolmafia.npcPrice($item`wad of dough`) === 0 && kolmafia.npcPrice($item`all-purpose flower`) > 0) {
        var maxTries = Math.ceil(count / 35); // minimum amount of wad of dough created from all-purpose flower is 35
        for (var i = 0; i < maxTries && kolmafia.availableAmount($item`wad of dough`) < count; i++) {
          kolmafia.buy($item`all-purpose flower`);
          kolmafia.use($item`all-purpose flower`);
        }
        kolmafia.retrieveItem(count, curFuel);
      } else kolmafia.retrieveItem(count, curFuel);
    } else if (ceiling) kolmafia.buy(count, curFuel, ceiling);else kolmafia.buy(count, curFuel);
    if (kolmafia.itemAmount(curFuel) > 0 && !insertFuel(curFuel, Math.min(kolmafia.itemAmount(curFuel), count))) {
      throw new Error("Failed to fuel Asdon Martin.");
    }
  }
  return kolmafia.getFuel() >= targetUnits;
}
/**
 * @param targetUnits The fuel level we aim to achieve
 * @returns Whether we successfully filled our Asdon's tank
 */
function fillWithBestInventoryItem(targetUnits) {
  var options = inventoryItems().sort((a, b) => getAverageAdventures(b) / kolmafia.autosellPrice(b) - getAverageAdventures(a) / kolmafia.autosellPrice(a));
  if (options.length === 0) return false;
  var best = options[0];
  if (kolmafia.autosellPrice(best) / getAverageAdventures(best) > 100) return false;
  var amountToUse = clamp(Math.ceil(targetUnits / getAverageAdventures(best)), 0, kolmafia.itemAmount(best));
  return insertFuel(best, amountToUse);
}
/**
 * Fill your Asdon Martin by prioritizing mallmin items in your inventory. Default to the behavior of fillTo.
 *
 * @param targetUnits Fuel level to attempt to reach.
 * @returns Whether we succeeded at filling to the target fuel level.
 */
function fillWithInventoryTo(targetUnits) {
  if (!installed$4()) return false;
  var continueFuelingFromInventory = true;
  while (kolmafia.getFuel() < targetUnits && continueFuelingFromInventory) {
    continueFuelingFromInventory && (continueFuelingFromInventory = fillWithBestInventoryItem(targetUnits));
  }
  return fillTo(targetUnits);
}
/**
 * Object consisting of the various Asdon driving styles
 */
var Driving = {
  Obnoxiously: $effect`Driving Obnoxiously`,
  Stealthily: $effect`Driving Stealthily`,
  Wastefully: $effect`Driving Wastefully`,
  Safely: $effect`Driving Safely`,
  Recklessly: $effect`Driving Recklessly`,
  Intimidatingly: $effect`Driving Intimidatingly`,
  Quickly: $effect`Driving Quickly`,
  Observantly: $effect`Driving Observantly`,
  Waterproofly: $effect`Driving Waterproofly`
};
/**
 * Attempt to drive with a particular style for a particular number of turns.
 *
 * @param style The driving style to use.
 * @param turns The number of turns to attempt to get.
 * @param preferInventory Whether we should preferentially value items currently in our inventory.
 * @returns Whether we have at least as many turns as requested of said driving style.
 */
function drive(style) {
  var turns = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : 1;
  var preferInventory = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : false;
  if (!Object.values(Driving).includes(style)) return false;
  if (!installed$4()) return false;
  if (kolmafia.haveEffect(style) >= turns) return true;
  var fuelNeeded = 37 * Math.ceil((turns - kolmafia.haveEffect(style)) / 30);
  (preferInventory ? fillWithInventoryTo : fillTo)(fuelNeeded);
  while (kolmafia.getFuel() >= 37 && kolmafia.haveEffect(style) < turns) {
    kolmafia.cliExecute(`asdonmartin drive ${style.name.replace("Driving ", "")}`);
  }
  return kolmafia.haveEffect(style) >= turns;
}

// Value of _lastCombatStarted the last time we updated scrapbook charges.
var scrapbookChargesLastUpdated = get$2("_lastCombatStarted");
// Free unlimited source every 30 turns.
// Does not work on special monsters so needs a backup, see tryFindFreeRun.
// banishedMonsters isn't updated if the banish succeeds on an unbanishable monster
var asdonMartinSource = new ActionSource($skill`Asdon Martin: Spring-Loaded Front Bumper`, () => {
  if (!installed$4()) return 0;
  var banishes = get$2("banishedMonsters").split(":");
  var bumperIndex = banishes.map(string => string.toLowerCase()).indexOf("spring-loaded front bumper");
  if (bumperIndex === -1) return 1;
  return kolmafia.myTurncount() - parseInt(banishes[bumperIndex + 1]) > 30 ? 1 : 0;
}, Macro$1.trySkill($skill`Asdon Martin: Spring-Loaded Front Bumper`), {
  preparation: () => fillTo(50)
});
var banishSources = [
// Free limited sources
new ActionSource($skill`Snokebomb`, () => have$P($skill`Snokebomb`) ? 3 - get$2("_snokebombUsed") : 0, Macro$1.skill($skill`Snokebomb`), {
  preparation: () => kolmafia.restoreMp(50)
}), new ActionSource($skill`Emotionally Chipped`, () => have$P($skill`Emotionally Chipped`) ? 3 - get$2("_feelHatredUsed") : 0, Macro$1.skill($skill`Feel Hatred`)), new ActionSource($item`Kremlin's Greatest Briefcase`, () => have$P($item`Kremlin's Greatest Briefcase`) ? 3 - get$2("_kgbTranquilizerDartUses") : 0, Macro$1.skill($skill`KGB tranquilizer dart`), {
  equipmentRequirements: () => new Requirement([], {
    forceEquip: $items`Kremlin's Greatest Briefcase`
  })
}), new ActionSource($item`latte lovers member's mug`, () => have$P($item`latte lovers member's mug`) && !get$2("_latteBanishUsed") ? 1 : 0, Macro$1.skill($skill`Throw Latte on Opponent`), {
  equipmentRequirements: () => new Requirement([], {
    forceEquip: $items`latte lovers member's mug`
  })
}), new ActionSource($item`Lil' Doctor™ bag`, () => have$P($item`Lil' Doctor™ bag`) ? 3 - get$2("_reflexHammerUsed") : 0, Macro$1.skill($skill`Reflex Hammer`), {
  equipmentRequirements: () => new Requirement([], {
    forceEquip: $items`Lil' Doctor™ bag`
  })
}), new ActionSource($item`mafia middle finger ring`, () => have$P($item`mafia middle finger ring`) && kolmafia.canEquip($item`mafia middle finger ring`) && !get$2("_mafiaMiddleFingerRingUsed") ? 1 : 0, Macro$1.skill($skill`Show them your ring`), {
  equipmentRequirements: () => new Requirement([], {
    forceEquip: $items`mafia middle finger ring`
  })
}), new ActionSource($item`V for Vivala mask`, () => have$P($item`V for Vivala mask`) && !get$2("_vmaskBanisherUsed") ? 1 : 0, Macro$1.skill($skill`Creepy Grin`), {
  equipmentRequirements: () => new Requirement([], {
    forceEquip: $items`V for Vivala mask`
  }),
  preparation: () => kolmafia.restoreMp(30)
}), new ActionSource($item`stinky cheese eye`, () => getFoldGroup($item`stinky cheese eye`).some(item => have$P(item)) && !get$2("_stinkyCheeseBanisherUsed") ? 1 : 0, Macro$1.skill($skill`Give Your Opponent the Stinkeye`), {
  equipmentRequirements: () => new Requirement([], {
    forceEquip: $items`stinky cheese eye`
  }),
  preparation: () => {
    if (!have$P($item`stinky cheese eye`)) {
      kolmafia.cliExecute(`fold stinky cheese eye`);
    }
    return have$P($item`stinky cheese eye`);
  }
}), new ActionSource($skill`Show your boring familiar pictures`, () => {
  if (have$P($item`familiar scrapbook`)) {
    if (scrapbookChargesLastUpdated !== get$2("_lastCombatStarted")) {
      kolmafia.visitUrl("desc_item.php?whichitem=463063785");
      scrapbookChargesLastUpdated = get$2("_lastCombatStarted");
    }
    return Math.floor(get$2("scrapbookCharges") / 100);
  }
  return 0;
}, Macro$1.skill($skill`Show your boring familiar pictures`), {
  equipmentRequirements: () => new Requirement([], {
    forceEquip: $items`familiar scrapbook`
  })
}), new ActionSource($item`human musk`, () => Math.max(0, 3 - get$2("_humanMuskUses")), Macro$1.item($item`human musk`), {
  preparation: () => kolmafia.retrieveItem($item`human musk`),
  cost: () => ActionSource.defaultPriceFunction($item`human musk`)
})].concat(_toConsumableArray($items`Louder Than Bomb, divine champagne popper, tennis ball, stuffed yam stinkbomb, anchor bomb, handful of split pea soup`.map(item => new ActionSource(item, () => Infinity, Macro$1.item(item), {
  preparation: () => kolmafia.retrieveItem(item),
  cost: () => ActionSource.defaultPriceFunction(item)
}))));
/**
 * Find an available banish source subject to constraints.
 *
 * @param constraints Preexisting constraints that restrict possible sources.
 * @returns Banish source satisfying constraints, or null.
 */
function tryFindBanish(constraints) {
  var source = findActionSource(banishSources, constraints);
  // Always try to use Asdon Martin: Spring-Loaded Front Bumper first,
  // but only if another source has been found.
  if (source && asdonMartinSource.available()) {
    source = asdonMartinSource.merge(source);
  }
  return source;
}

var familiar$6 = $familiar`Frumious Bandersnatch`;
/**
 * Determines whether the player has the Frumious Bandersnatch in their
 * terrarium
 *
 * @returns Whether the player has a Frumious Bandersnatch
 */
function have$O() {
  return have$P(familiar$6);
}
/**
 * Get the number of free runaways that have already been used
 *
 * @see StompingBoots with which the Bandersnatch shares a counter
 * @returns Number of free runaways used today
 */
function getRunaways$1() {
  return get$2("_banderRunaways");
}
/**
 * Determine the total number of free runaways that the player can
 * get from their Bandersnatch
 *
 * @param considerWeightAdjustment Include familiar weight modifiers
 * @returns Current maximum runaways
 */
function getMaxRunaways$1() {
  var considerWeightAdjustment = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : true;
  return Math.floor(totalFamiliarWeight(familiar$6, considerWeightAdjustment) / 5);
}
/**
 * Determine the number of remaining free runaways the player can
 * get from their Bandersnatch
 *
 * @param considerWeightAdjustment Include familiar weight modifiers
 * @returns Current maximum runaways reamining
 */
function getRemainingRunaways$1() {
  var considerWeightAdjustment = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : true;
  return Math.max(0, getMaxRunaways$1(considerWeightAdjustment) - getRunaways$1());
}
/**
 * Determine whether the player could use their Bandersnatch to
 * get a free run in theory
 *
 * @param considerWeightAdjustment Include familiar weight modifiers
 * @returns Whether a free run is theoretically possible right now
 */
function couldRunaway$1() {
  var considerWeightAdjustment = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : true;
  return have$O() && getRemainingRunaways$1(considerWeightAdjustment) > 0;
}
$skill`The Ode to Booze`;
$effect`Ode to Booze`;

var familiar$5 = $familiar`Pair of Stomping Boots`;
/**
 * @returns whether the player has the Pair of Stomping Boots in their terrarium
 */
function have$N() {
  return have$P(familiar$5);
}
/**
 * @returns number of free runaways that have already been used today
 * @see Bandersnatch with which the Stomping Boots shares a counter
 */
function getRunaways() {
  return get$2("_banderRunaways");
}
/**
 * @param considerWeightAdjustment Include familiar weight modifiers
 * @returns total number of free runaways that the player can get from their Stomping Boots
 */
function getMaxRunaways() {
  var considerWeightAdjustment = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : true;
  return Math.floor(totalFamiliarWeight(familiar$5, considerWeightAdjustment) / 5);
}
/**
 * @param considerWeightAdjustment Whether to consider weight adjustment in free run calculation
 * @returns the number of remaining free runaways the player can get from their Stomping Boots
 */
function getRemainingRunaways() {
  var considerWeightAdjustment = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : true;
  return Math.max(0, getMaxRunaways(considerWeightAdjustment) - getRunaways());
}
/**
 * @param considerWeightAdjustment Include familiar weight modifiers
 * @returns whether the player could use their Stomping Boots to get a free run in theory
 */
function couldRunaway() {
  var considerWeightAdjustment = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : true;
  return have$N() && getRemainingRunaways(considerWeightAdjustment) > 0;
}

var everythingLooksGreen = function everythingLooksGreen() {
  var otherClause = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : () => true;
  return () => otherClause() && !have$P($effect`Everything Looks Green`) ? 1 : 0;
};
var freeRunSources = [
// Free unlimited source
new ActionSource($item`spring shoes`, everythingLooksGreen(() => have$P($item`spring shoes`)), Macro$1.skill($skill`Spring Away`), {
  equipmentRequirements: () => new Requirement([], {
    forceEquip: $items`spring shoes`
  })
}),
// Free limited sources
new ActionSource($familiar`Frumious Bandersnatch`, () => (have$P($effect`Ode to Booze`) || getSongCount() < getSongLimit()) && couldRunaway$1() ? getRemainingRunaways$1() : 0, Macro$1.step("runaway"), {
  equipmentRequirements: () => new Requirement(["Familiar Weight"], {}),
  preparation: () => {
    ensureEffect($effect`Ode to Booze`);
    return have$P($effect`Ode to Booze`);
  },
  familiar: () => $familiar`Frumious Bandersnatch`
}), new ActionSource($familiar`Pair of Stomping Boots`, () => couldRunaway() ? getRemainingRunaways() : 0, Macro$1.step("runaway"), {
  equipmentRequirements: () => new Requirement(["Familiar Weight"], {}),
  familiar: () => $familiar`Pair of Stomping Boots`
}), new ActionSource($item`navel ring of navel gazing`, () => have$P($item`navel ring of navel gazing`) ? Math.max(0, 3 - get$2("_navelRunaways")) : 0, Macro$1.step("runaway"), {
  equipmentRequirements: () => new Requirement([], {
    forceEquip: $items`navel ring of navel gazing`
  })
}), new ActionSource($item`Greatest American Pants`, () => have$P($item`Greatest American Pants`) ? Math.max(0, 3 - get$2("_navelRunaways")) : 0, Macro$1.step("runaway"), {
  equipmentRequirements: () => new Requirement([], {
    forceEquip: $items`Greatest American Pants`
  })
}), new ActionSource($item`peppermint parasol`, () => Math.max(0, 3 - get$2("_navelRunaways")), Macro$1.item($item`peppermint parasol`), {
  preparation: () => kolmafia.retrieveItem($item`peppermint parasol`),
  cost: () => ActionSource.defaultPriceFunction($item`peppermint parasol`) / 10 // Breaks after 10 successful runaways.
})].concat(_toConsumableArray($items`green smoke bomb, tattered scrap of paper, GOTO, T.U.R.D.S. Key`.map(item => new ActionSource(item, everythingLooksGreen(), Macro$1.item(item), {
  preparation: () => kolmafia.retrieveItem(item),
  cost: () => ActionSource.defaultPriceFunction(item)
}))), _toConsumableArray($items`fish-oil smoke bomb, giant eraser`.map(item => new ActionSource(item, () => !have$P(item) ? 0 : 1, Macro$1.item(item), {
  preparation: () => have$P(item),
  cost: () => 0
}))));
/**
 * Find an available free run source subject to constraints.
 *
 * @param constraints Preexisting constraints that restrict possible sources.
 * @returns Free run source satisfying constraints, or null.
 */
function tryFindFreeRun(constraints) {
  var source = findActionSource(freeRunSources, constraints);
  return source;
}
/**
 * Ensure an available free run source subject to constraints.
 * Throws an error if no source can be found.
 *
 * @param constraints Preexisting constraints that restrict possible sources.
 * @returns Free run source satisfying constraints.
 */
function ensureFreeRun(constraints) {
  var source = tryFindFreeRun(constraints);
  if (!source) {
    throw new Error("Failed to ensure Free Run source");
  }
  return source;
}

var familiar$4 = $familiar`Comma Chameleon`;
/**
 * Determines whether the player has the Comma Chameleon in their
 * terrarium
 *
 * @returns Whether the player has a Comma Chameleon
 */
function have$M() {
  return have$P(familiar$4);
}
/**
 * Transforms Comma Chameleon into the familiar of choice if you already have the appropriate familiar equipment
 * Will not transform if you do not
 * @param fam determines what to transform into
 * @returns Whether Comma has been successfully transformed
 */
function transform(fam) {
  if (currentFamiliar$1() === fam) {
    return true;
  }
  var equipment = kolmafia.familiarEquipment(fam);
  if (equipment === $item.none) return false;
  if (!have$P(equipment)) return false;
  kolmafia.visitUrl(`inv_equip.php?which=2&action=equip&whichitem=${kolmafia.toInt(equipment)}&pwd`);
  kolmafia.visitUrl("charpane.php"); // This is the only way to get Mafia to update it's pref
  if (currentFamiliar$1() !== fam) {
    return false;
  }
  return true;
}
/**
 * @returns The current familiar that Comma is behaving as
 */
function currentFamiliar$1() {
  return get$2("commaFamiliar");
}

var ridingFamiliars = [{
  familiar: $familiar`Puck Man`,
  drops: $items`yellow pixel`,
  probability: 0.25,
  dropPredicate: () => get$2("_yellowPixelDropsCrown") < 25
}, {
  familiar: $familiar`Ms. Puck Man`,
  drops: $items`yellow pixel`,
  probability: 0.25,
  dropPredicate: () => get$2("_yellowPixelDropsCrown") < 25
}, {
  familiar: $familiar`Grimstone Golem`,
  drops: $items`grimstone mask`,
  probability: 0.5,
  dropPredicate: () => get$2("_grimstoneMaskDropsCrown") < 1
}, {
  familiar: $familiar`Knob Goblin Organ Grinder`,
  drops: 30,
  probability: 1
}, {
  familiar: $familiar`Happy Medium`,
  drops: 30,
  probability: 1
}, {
  familiar: $familiar`Garbage Fire`,
  drops: $items`burning newspaper`,
  probability: 0.5,
  dropPredicate: () => get$2("_garbageFireDropsCrown") < 3
}, {
  familiar: $familiar`Machine Elf`,
  drops: $items`abstraction: sensation, abstraction: thought, abstraction: action, abstraction: category, abstraction: perception, abstraction: purpose`,
  probability: 0.2,
  dropPredicate: () => get$2("_abstractionDropsCrown") < 25
}, {
  familiar: $familiar`Trick-or-Treating Tot`,
  drops: $items`hoarded candy wad`,
  probability: 0.5,
  dropPredicate: () => get$2("_hoardedCandyDropsCrown") < 3
}, {
  familiar: $familiar`Warbear Drone`,
  drops: $items`warbear whosit`,
  probability: 1 / 4.5
}, {
  familiar: $familiar`Li'l Xenomorph`,
  drops: $items`lunar isotope`,
  probability: 0.05
}, {
  familiar: $familiar`Pottery Barn Owl`,
  drops: $items`volcanic ash`,
  probability: 0.1
}, {
  familiar: $familiar`Grim Brother`,
  drops: $items`grim fairy tale`,
  probability: 1,
  dropPredicate: () => get$2("_grimFairyTaleDropsCrown") < 2
}, {
  familiar: $familiar`Optimistic Candle`,
  drops: $items`glob of melted wax`,
  probability: 1,
  dropPredicate: () => get$2("_optimisticCandleDropsCrown") < 3
}, {
  familiar: $familiar`Adventurous Spelunker`,
  drops: $items`teflon ore, velcro ore, vinyl ore, cardboard ore, styrofoam ore, bubblewrap ore`,
  probability: 1,
  dropPredicate: () => get$2("_oreDropsCrown") < 6
}, {
  familiar: $familiar`Twitching Space Critter`,
  drops: $items`space beast fur`,
  probability: 1,
  dropPredicate: () => get$2("_spaceFurDropsCrown") < 1
}, {
  familiar: $familiar`Party Mouse`,
  drops: 50,
  probability: 0.05
}, {
  familiar: $familiar`Yule Hound`,
  drops: $items`candy cane`,
  probability: 1
}, {
  familiar: $familiar`Gluttonous Green Ghost`,
  drops: $items`bean burrito, enchanted bean burrito, jumping bean burrito`,
  probability: 1
}, {
  familiar: $familiar`Reassembled Blackbird`,
  drops: $items`blackberry`,
  probability: 1
}, {
  familiar: $familiar`Reconstituted Crow`,
  drops: $items`blackberry`,
  probability: 1
}, {
  familiar: $familiar`Hunchbacked Minion`,
  drops: new Map([[$item`disembodied brain`, 0.02], [$item`skeleton bone`, 0.98]]),
  probability: 1
}, {
  familiar: $familiar`Reanimated Reanimator`,
  drops: $items`hot wing, broken skull`,
  probability: 1
}, {
  familiar: $familiar`Attention-Deficit Demon`,
  drops: $items`chorizo brownies, white chocolate and tomato pizza, carob chunk noodles`,
  probability: 1
}, {
  familiar: $familiar`Piano Cat`,
  drops: $items`beertini, papaya slung, salty slug, tomato daiquiri`,
  probability: 1
}, {
  familiar: $familiar`Golden Monkey`,
  drops: $items`gold nuggets`,
  probability: 0.5
}, {
  familiar: $familiar`Robot Reindeer`,
  drops: $items`candy cane, eggnog, fruitcake, gingerbread bugbear`,
  probability: 0.3
}, {
  familiar: $familiar`Stocking Mimic`,
  drops: $items`Angry Farmer candy, Cold Hots candy, Rock Pops, Tasty Fun Good rice candy, Wint-O-Fresh mint`,
  probability: 0.3
}, {
  familiar: $familiar`BRICKO chick`,
  drops: $items`BRICKO brick`,
  probability: 1
}, {
  familiar: $familiar`Cotton Candy Carnie`,
  drops: $items`cotton candy pinch`,
  probability: 1
}, {
  familiar: $familiar`Untamed Turtle`,
  drops: $items`snailmail bits, turtlemail bits, turtle wax`,
  probability: 0.35
}, {
  familiar: $familiar`Astral Badger`,
  drops: $items`spooky mushroom, Knob mushroom, Knoll mushroom`,
  probability: 1
}, {
  familiar: $familiar`Green Pixie`,
  drops: $items`bottle of tequila`,
  probability: 0.2
}, {
  familiar: $familiar`Angry Goat`,
  drops: $items`goat cheese pizza`,
  probability: 1
}, {
  familiar: $familiar`Adorable Seal Larva`,
  drops: $items`stench nuggets, spooky nuggets, hot nuggets, cold nuggets, sleaze nuggets`,
  probability: 0.35
}, {
  familiar: $familiar`Ancient Yuletide Troll`,
  drops: $items`candy cane, eggnog, fruitcake, gingerbread bugbear`,
  probability: 0.3
}, {
  familiar: $familiar`Sweet Nutcracker`,
  drops: $items`candy cane, eggnog, fruitcake, gingerbread bugbear`,
  probability: 0.3
}, {
  familiar: $familiar`Casagnova Gnome`,
  drops: 0,
  probability: 0
}, {
  familiar: $familiar`Coffee Pixie`,
  drops: 0,
  probability: 0
}, {
  familiar: $familiar`Dancing Frog`,
  drops: 0,
  probability: 0
}, {
  familiar: $familiar`Grouper Groupie`,
  drops: 0,
  probability: 0
}, {
  familiar: $familiar`Hand Turkey`,
  drops: 30,
  probability: 1
}, {
  familiar: $familiar`Hippo Ballerina`,
  drops: 0,
  probability: 0
}, {
  familiar: $familiar`Jitterbug`,
  drops: 0,
  probability: 0
}, {
  familiar: $familiar`Leprechaun`,
  drops: 30,
  probability: 1
}, {
  familiar: $familiar`Obtuse Angel`,
  drops: 0,
  probability: 0
}, {
  familiar: $familiar`Psychedelic Bear`,
  drops: 0,
  probability: 0
}, {
  familiar: $familiar`Robortender`,
  drops: 0,
  probability: 0
}, {
  familiar: $familiar`Ghost of Crimbo Commerce`,
  drops: 30,
  probability: 1
}, {
  familiar: $familiar`Hobo Monkey`,
  drops: 0,
  probability: 0
}, {
  familiar: $familiar`Rockin' Robin`,
  drops: 60,
  probability: 1
}, {
  familiar: $familiar`Feral Kobold`,
  drops: 30,
  probability: 1
}, {
  familiar: $familiar`Oily Woim`,
  drops: 30,
  probability: 1
}, {
  familiar: $familiar`Cat Burglar`,
  drops: 0,
  probability: 0
}, {
  familiar: $familiar`Misshapen Animal Skeleton`,
  drops: 30,
  probability: 1
}, {
  familiar: $familiar`Gelatinous Cubeling`,
  drops: 0,
  probability: 0
}, {
  familiar: $familiar`Frozen Gravy Fairy`,
  drops: $items`cold nuggets`,
  probability: 1
}, {
  familiar: $familiar`Stinky Gravy Fairy`,
  drops: $items`stench nuggets`,
  probability: 1
}, {
  familiar: $familiar`Sleazy Gravy Fairy`,
  drops: $items`sleaze nuggets`,
  probability: 1
}, {
  familiar: $familiar`Spooky Gravy Fairy`,
  drops: $items`spooky nuggets`,
  probability: 1
}, {
  familiar: $familiar`Mini Kiwi`,
  drops: $items`mini kiwi`,
  probability: 0.08
}, {
  familiar: $familiar`Flaming Gravy Fairy`,
  // drops a hot nugget every combat, 5 of which can be used to make a hot wad
  drops: $items`hot nuggets`,
  probability: 1
}];
var FULL_RIDING_LIST = [].concat(ridingFamiliars, _toConsumableArray(kolmafia.Familiar.all().filter(f => !ridingFamiliars.some(_ref => {
  var familiar = _ref.familiar;
  return familiar === f;
})).map(familiar => ({
  familiar,
  drops: 0,
  probability: 1
}))));
/**
 * Value a specified familiar Crown rider
 *
 * @param rider Familiar to value
 * @param modifierValueFunction Value of the extra modifiers the familiar provides,
 * @param dropsValueFunction Value to assign the drops of the familiar gives
 * @param ignoreLimitedDrops Whether to ignore drops that are daily or otherwise limited
 * @returns Rider value (in meat)
 */
function valueRider(rider, modifierValueFunction, dropsValueFunction) {
  var ignoreLimitedDrops = arguments.length > 3 && arguments[3] !== undefined ? arguments[3] : false;
  var dropValue = !rider.dropPredicate || rider.dropPredicate() && !ignoreLimitedDrops ? rider.probability * (typeof rider.drops === "number" ? rider.drops : dropsValueFunction(rider.drops)) : 0;
  var modifierValue = modifierValueFunction(rider.familiar);
  return dropValue + modifierValue;
}
var riderModes = new Map();
var DEFAULTS = {
  modifierValueFunction: () => 0,
  dropsValueFunction: () => 0,
  ignoreLimitedDrops: false,
  excludeCurrentFamiliar: true
};
/**
 * Creates a rider mode for this session
 *
 * @param name Rider mode name
 * @param details An object consisting of various settings for the RiderMode:
 * @param details.modifierValueFunction Function to value a familiar itself, often using modifiers,
 * @param details.dropsValueFunction Function to value the drops of a familiar, which are stored as an `Item[]` or `Map<Item, number>`
 * @param details.ignoreLimitedDrops Whether to ignore daily or otherwise limited drops
 * @param details.excludeCurrentFamiliar Whether to exclude the player's current familiar
 * @returns Map of all rider modes created this session, including the one that was just made
 */
function createRiderMode(name, details) {
  return riderModes.set(name, _objectSpread2(_objectSpread2({}, DEFAULTS), details));
}
/**
 * @param name The name of the `RiderMode` to check
 * @returns Whether or not said `RiderMode` exists
 */
function hasRiderMode(name) {
  return riderModes.has(name);
}
var riderLists = new Map();
/**
 * Pick a rider
 *
 * @param mode Mode by which to select possible riders
 * @returns Picked faimiliar rider or null if no rider could be selected
 */
function pickRider(mode) {
  var modeData = riderModes.get(mode);
  if (!modeData) return null;
  var modifierValueFunction = modeData.modifierValueFunction,
    dropsValueFunction = modeData.dropsValueFunction,
    ignoreLimitedDrops = modeData.ignoreLimitedDrops,
    excludeCurrentFamiliar = modeData.excludeCurrentFamiliar;
  if (!riderLists.has(mode)) {
    riderLists.set(mode, FULL_RIDING_LIST.filter(_ref2 => {
      var familiar = _ref2.familiar;
      return have$P(familiar);
    }).map(rider => _objectSpread2(_objectSpread2({}, rider), {}, {
      value: valueRider(rider, modifierValueFunction, dropsValueFunction, ignoreLimitedDrops)
    })).sort((_ref3, _ref4) => {
      var a = _ref3.value;
      var b = _ref4.value;
      return b - a;
    }));
  }
  var list = riderLists.get(mode);
  if (list) {
    var riderToReturn = list.find(_ref5 => {
      var dropPredicate = _ref5.dropPredicate,
        familiar = _ref5.familiar;
      return ((dropPredicate === null || dropPredicate === void 0 ? void 0 : dropPredicate()) ?? true) && (!excludeCurrentFamiliar || kolmafia.myFamiliar() !== familiar);
    });
    return riderToReturn ?? null;
  }
  return null;
}
/**
 * Find the associated NumericModifier for a given familiar when enthroned or bjornified
 *
 * @param modifier The NumericModifier in question
 * @param familiar The Familiar in question
 * @returns The value of the given numeric modifier for the Crown of Thrones (or buddy bjorn) when the given familiar is encromulated
 */
function getModifier(modifier, familiar) {
  return kolmafia.numericModifier(`Throne:${familiar}`, modifier);
}
/**
 * Create a `modifierValueFunction` for a familiar.
 *
 * @param modifiers An array consisting of the `NumericModifier`s relevant to your valuation
 * @param functions An object keyed by `NumericModifier`s whose values are functions that map the the result of a modifier to its corresponding valuation
 * @returns A function that maps a familiar to the value of its modifiers in the crown of thrones or buddy bjorn.
 */
function createModifierValueFunction(modifiers, functions) {
  return familiar => sum(modifiers, modifier => functions[modifier](getModifier(modifier, familiar)));
}

var Copier = /*#__PURE__*/_createClass(function Copier(couldCopy, prepare, canCopy, copiedMonster, fightCopy) {
  _classCallCheck(this, Copier);
  _defineProperty(this, "couldCopy", void 0);
  _defineProperty(this, "prepare", void 0);
  _defineProperty(this, "canCopy", void 0);
  _defineProperty(this, "copiedMonster", void 0);
  _defineProperty(this, "fightCopy", null);
  this.couldCopy = couldCopy;
  this.prepare = prepare;
  this.canCopy = canCopy;
  this.copiedMonster = copiedMonster;
  if (fightCopy) this.fightCopy = fightCopy;
});

// THIS FILE IS AUTOMATICALLY GENERATED. See tools/parseModifiers.ts for more information
var stringModifiers = ["Class", "Intrinsic Effect", "Equalize", "Wiki Name", "Modifiers", "Outfit", "Stat Tuning", "Equips On", "Familiar Effect", "Jiggle", "Equalize Muscle", "Equalize Mysticality", "Equalize Moxie", "Avatar", "Skill", "Floor Buffed Muscle", "Floor Buffed Mysticality", "Floor Buffed Moxie", "Plumber Stat", "Recipe", "Evaluated Modifiers", "Last Available"];
var stringModifiersSet = new Set(stringModifiers);
var booleanModifiers = ["Softcore Only", "Single Equip", "Always Fumble", "Never Fumble", "Weakens Monster", "Free Pull", "Variable", "Nonstackable Watch", "Cold Immunity", "Hot Immunity", "Sleaze Immunity", "Spooky Immunity", "Stench Immunity", "Cold Vulnerability", "Hot Vulnerability", "Sleaze Vulnerability", "Spooky Vulnerability", "Stench Vulnerability", "Moxie Controls MP", "Moxie May Control MP", "Four Songs", "Adventure Randomly", "Adventure Underwater", "Underwater Familiar", "Generic", "Unarmed", "No Pull", "Lasts Until Rollover", "Alters Page Text", "Attacks Can't Miss", "Pirate", "Blind", "Breakable", "Drops Items", "Drops Meat", "Volleyball or Sombrero", "Extra Pickpocket", "Negative Status Resist"];
var booleanModifiersSet = new Set(booleanModifiers);
var numericModifiers = ["Familiar Weight", "Monster Level", "Combat Rate", "Initiative", "Experience", "Item Drop", "Meat Drop", "Damage Absorption", "Damage Reduction", "Cold Resistance", "Hot Resistance", "Sleaze Resistance", "Spooky Resistance", "Stench Resistance", "Mana Cost", "Moxie", "Moxie Percent", "Muscle", "Muscle Percent", "Mysticality", "Mysticality Percent", "Maximum HP", "Maximum HP Percent", "Maximum MP", "Maximum MP Percent", "Weapon Damage", "Ranged Damage", "Spell Damage", "Spell Damage Percent", "Cold Damage", "Hot Damage", "Sleaze Damage", "Spooky Damage", "Stench Damage", "Cold Spell Damage", "Hot Spell Damage", "Sleaze Spell Damage", "Spooky Spell Damage", "Stench Spell Damage", "Underwater Combat Rate", "Fumble", "HP Regen Min", "HP Regen Max", "MP Regen Min", "MP Regen Max", "Adventures", "Familiar Weight Percent", "Weapon Damage Percent", "Ranged Damage Percent", "Stackable Mana Cost", "Hobo Power", "Base Resting HP", "Resting HP Percent", "Bonus Resting HP", "Base Resting MP", "Resting MP Percent", "Bonus Resting MP", "Critical Hit Percent", "PvP Fights", "Volleyball", "Sombrero", "Leprechaun", "Fairy", "Meat Drop Penalty", "Hidden Familiar Weight", "Item Drop Penalty", "Initiative Penalty", "Food Drop", "Booze Drop", "Hat Drop", "Weapon Drop", "Offhand Drop", "Shirt Drop", "Pants Drop", "Accessory Drop", "Volleyball Effectiveness", "Sombrero Effectiveness", "Leprechaun Effectiveness", "Fairy Effectiveness", "Familiar Weight Cap", "Slime Resistance", "Slime Hates It", "Spell Critical Percent", "Muscle Experience", "Mysticality Experience", "Moxie Experience", "Effect Duration", "Candy Drop", "DB Combat Damage", "Sombrero Bonus", "Familiar Experience", "Sporadic Meat Drop", "Sporadic Item Drop", "Meat Bonus", "Pickpocket Chance", "Combat Mana Cost", "Muscle Experience Percent", "Mysticality Experience Percent", "Moxie Experience Percent", "Minstrel Level", "Muscle Limit", "Mysticality Limit", "Moxie Limit", "Song Duration", "Prismatic Damage", "Smithsness", "Supercold Resistance", "Reduce Enemy Defense", "Pool Skill", "Familiar Damage", "Gear Drop", "Maximum Hooch", "Water Level", "Crimbot Outfit Power", "Familiar Tuning Muscle", "Familiar Tuning Mysticality", "Familiar Tuning Moxie", "Random Monster Modifiers", "Luck", "Othello Skill", "Disco Style", "Rollover Effect Duration", "Sixgun Damage", "Fishing Skill", "Additional Song", "Sprinkle Drop", "Absorb Adventures", "Absorb Stats", "Rubee Drop", "Kruegerand Drop", "WarBear Armor Penetration", "Maximum PP", "Plumber Power", "Drippy Damage", "Drippy Resistance", "Energy", "Scrap", "Familiar Action Bonus", "Water", "Spleen Drop", "Potion Drop", "Sauce Spell Damage", "Monster Level Percent", "Food Fairy", "Booze Fairy", "Candy Fairy", "Food Fairy Effectiveness", "Booze Fairy Effectiveness", "Candy Fairy Effectiveness", "Damage Aura", "Sporadic Damage Aura", "Thorns", "Sporadic Thorns", "Stomach Capacity", "Liver Capacity", "Spleen Capacity", "Free Rests", "Leaves", "Elf Warfare Effectiveness", "Pirate Warfare Effectiveness", "MPC Drop", "Piece of Twelve Drop", "Combat Item Damage Percent", "Avoid Attack", "Damage vs. Bugbears", "Damage vs. Werewolves", "Damage vs. Zombies", "Damage vs. Ghosts", "Damage vs. Vampires", "Damage vs. Skeletons", "Damage vs. Undead", "RAM", "Lantern", "Raw Combat Rate"];
var numericModifiersSet = new Set(numericModifiers);
var multiStringModifiers = ["Effect", "Rollover Effect", "Conditional Skill (Equipped)", "Conditional Skill (Inventory)", "Lantern Element"];
var multiStringModifiersSet = new Set(multiStringModifiers);

/**
 * Type guard that determines if a given string is a boolean modifier
 * @param modifier The modifier in question
 * @returns Whether the string in question is a valid boolean modifier
 */
function isBooleanModifier(modifier) {
  return booleanModifiersSet.has(modifier);
}
/**
 * Type guard that determines if a given string is a numeric modifier
 * @param modifier The modifier in question
 * @returns Whether the string in question is a valid numeric modifier
 */
function isNumericModifier(modifier) {
  return numericModifiersSet.has(modifier);
}
/**
 * Type guard that determines if a given string is a string modifier
 * @param modifier The modifier in question
 * @returns Whether the string in question is a valid string modifier
 */
function isStringModifier(modifier) {
  return stringModifiersSet.has(modifier);
}
/**
 * Type guard that determines if a given string is a multistring modifier
 * @param modifier The modifier in question
 * @returns Whether the string in question is a valid multistring modifier
 */
function isMultiStringModifier(modifier) {
  return multiStringModifiersSet.has(modifier);
}
/**
 * Get the value of a modifier
 *
 * @param name Modifier name
 * @param subject Subject of modifier
 * @returns Value of modifier
 */
function get$1(name, subject) {
  if (isBooleanModifier(name)) {
    return subject === undefined ? kolmafia.booleanModifier(name) : kolmafia.booleanModifier(subject, name);
  }
  if (isNumericModifier(name)) {
    return subject === undefined ? kolmafia.numericModifier(name) : kolmafia.numericModifier(subject, name);
  }
  if (isStringModifier(name)) {
    return subject === undefined ? kolmafia.stringModifier(name) : kolmafia.stringModifier(subject, name);
  }
  if (isMultiStringModifier(name)) {
    return subject === undefined ? kolmafia.stringsModifier(name) : kolmafia.stringsModifier(subject, name);
  }
}
/**
 * Merge two Modifiers objects into one, summing all numeric modifiers, ||ing all boolean modifiers, and otherwise letting the second object overwrite the first.
 *
 * @param modifiers1 Modifiers objects to be merged onto.
 * @param modifiers2 Modifiers object to merge.
 * @returns A single Modifiers object obtained by merging.
 */
function pairwiseMerge(modifiers1, modifiers2) {
  var returnValue = _objectSpread2(_objectSpread2({}, modifiers1), modifiers2);
  for (var modifier in modifiers1) {
    if (Array.from(Object.values(modifiers2)).includes(modifier)) {
      if (isNumericModifier(modifier)) {
        returnValue[modifier] = (modifiers1[modifier] ?? 0) + (modifiers2[modifier] ?? 0);
      }
      if (isBooleanModifier(modifier)) {
        returnValue[modifier] = (modifiers1[modifier] ?? false) || (modifiers2[modifier] ?? false);
      }
      if (isMultiStringModifier(modifier)) {
        returnValue[modifier] = [].concat(_toConsumableArray(modifiers1[modifier] ?? []), _toConsumableArray(modifiers2[modifier] ?? []));
      }
    }
  }
  return returnValue;
}
/**
 * Merge arbitrarily many Modifiers objects into one, summing all numeric modifiers, and ||ing all boolean modifiers.
 *
 * @param modifiers Modifiers objects to be merged together.
 * @returns A single Modifiers object obtained by merging.
 */
function mergeModifiers() {
  for (var _len = arguments.length, modifiers = new Array(_len), _key = 0; _key < _len; _key++) {
    modifiers[_key] = arguments[_key];
  }
  return modifiers.reduce((a, b) => pairwiseMerge(a, b), {});
}

var Flower = /*#__PURE__*/function () {
  function Flower(name, id, environment, modifier) {
    var territorial = arguments.length > 4 && arguments[4] !== undefined ? arguments[4] : false;
    _classCallCheck(this, Flower);
    _defineProperty(this, "name", void 0);
    _defineProperty(this, "id", void 0);
    _defineProperty(this, "environment", void 0);
    _defineProperty(this, "modifier", void 0);
    _defineProperty(this, "territorial", void 0);
    this.name = name;
    this.id = id;
    this.environment = environment;
    this.modifier = modifier;
    this.territorial = territorial;
  }
  return _createClass(Flower, [{
    key: "isPlantedHere",
    value: function isPlantedHere() {
      var _Flower$plantNamesInZ;
      var location = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : get$2("lastAdventure") ?? $location.none;
      var plantedHere = (_Flower$plantNamesInZ = Flower.plantNamesInZone(location)) === null || _Flower$plantNamesInZ === void 0 ? void 0 : _Flower$plantNamesInZ.includes(this.name);
      return plantedHere !== undefined && plantedHere;
    }
  }, {
    key: "available",
    value: function available() {
      var location = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : get$2("lastAdventure") ?? $location.none;
      return this.environment === location.environment && !get$2("_floristPlantsUsed").includes(this.name) && !this.isPlantedHere(location);
    }
  }, {
    key: "dig",
    value: function dig() {
      if (!this.isPlantedHere()) return false;
      var flowers = Flower.plantNamesInZone();
      if (!flowers[2]) return false;
      var plantNumber = flowers.indexOf(this.name);
      Flower.visit();
      kolmafia.runChoice(2, `plnti=${plantNumber}`);
      return !this.isPlantedHere();
    }
  }, {
    key: "plant",
    value: function plant() {
      if (this.isPlantedHere()) return true;
      if (isFull()) return false;
      Flower.visit();
      kolmafia.runChoice(1, `plant=${this.id}`);
      return this.isPlantedHere();
    }
  }], [{
    key: "visit",
    value: function visit() {
      kolmafia.visitUrl("place.php?whichplace=forestvillage&action=fv_friar");
    }
  }, {
    key: "plantNamesInZone",
    value: function plantNamesInZone() {
      var location = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : get$2("lastAdventure") ?? $location.none;
      return kolmafia.getFloristPlants()[location.toString()] ?? [];
    }
  }, {
    key: "plantsInZone",
    value: function plantsInZone() {
      var location = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : get$2("lastAdventure") ?? $location.none;
      return this.plantNamesInZone(location).map(flowerName => toFlower(flowerName)).filter(notNull);
    }
  }, {
    key: "modifiersInZone",
    value: function modifiersInZone() {
      var location = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : get$2("lastAdventure") ?? $location.none;
      var plants = this.plantsInZone(location);
      if (!plants) return {};
      var modifiers = plants.map(plant => plant.modifier).map(modifier => typeof modifier === "string" ? {} : modifier);
      return mergeModifiers.apply(void 0, _toConsumableArray(modifiers));
    }
  }]);
}();
/**
 * @returns Whether or not the Florist is currently available
 */
function have$L() {
  return kolmafia.floristAvailable();
}
/**
 * Internal function used to convert strings to Flower instances
 *
 * @param name The flower name
 * @returns a Flower instance
 */
function toFlower(name) {
  return all.find(flower => name === flower.name) ?? null;
}
/**
 * @param location The location to check
 * @returns an array of the Flowers in that location
 */
function flowersIn(location) {
  var returnValue = [];
  Flower.plantNamesInZone(location).map(toFlower).forEach(flower => {
    if (flower) returnValue.push(flower);
  });
  return returnValue;
}
/**
 * @param location The location to check
 * @returns `true` if the location has 3 flowers in it; `false` otherwise
 */
function isFull() {
  var location = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : get$2("lastAdventure") ?? $location.none;
  return flowersIn(location).length === 3;
}
var RabidDogwood = new Flower("Rabid Dogwood", 1, "outdoor", {
  "Monster Level": 30
}, true);
var Rutabeggar = new Flower("Rutabeggar", 2, "outdoor", {
  "Item Drop": 25
}, true);
var RadishRadish = new Flower("Rad-ish Radish", 3, "outdoor", {
  "Moxie Experience": 5
}, true);
var Artichoker = new Flower("Artichoker", 4, "outdoor", "Delevels Enemy");
var SmokeRa = new Flower("Smoke-ra", 5, "outdoor", "Blocks Attacks");
var SkunkCabbage = new Flower("Skunk Cabbage", 6, "outdoor", {
  "Stench Damage": 12.5
});
var DeadlyCinnamon = new Flower("Deadly Cinnamon", 7, "outdoor", {
  "Hot Damage": 12.5
});
var CeleryStalker = new Flower("Celery Stalker", 8, "outdoor", {
  "Spooky Damage": 12.5
});
var LettuceSpray = new Flower("Lettus Spray", 9, "outdoor", {
  "HP Regen Min": 10,
  "HP Regen Max": 29
});
var SeltzerWatercress = new Flower("Seltzer Watercress", 10, "outdoor", {
  "MP Regen Min": 5,
  "MP Regen Max": 15
});
var WarLily = new Flower("War Lily", 11, "indoor", {
  "Monster Level": 30
}, true);
var StealingMagnolia = new Flower("Stealing Magnolia", 12, "indoor", {
  "Item Drop": 25
}, true);
var CannedSpinach = new Flower("Canned Spinach", 13, "indoor", {
  "Muscle Experience": 5
}, true);
var Impatiens = new Flower("Impatiens", 14, "indoor", {
  Initiative: 25
});
var SpiderPlant = new Flower("Spider Plant", 15, "indoor", "Poison");
var RedFern = new Flower("Red Fern", 16, "indoor", "Delevels Enemy");
var BamBoo = new Flower("BamBOO!", 17, "indoor", {
  "Spooky Damage": 12.5
});
var ArcticMoss = new Flower("Arctic Moss", 18, "indoor", {
  "Cold Damage": 12.5
});
var AloeGuvnor = new Flower("Aloe Guv'nor", 19, "indoor", {
  "HP Regen Min": 10,
  "HP Regen Max": 30
});
var PitcherPlant = new Flower("Pitcher Plant", 20, "indoor", {
  "MP Regen Min": 5,
  "MP Regen Max": 15
});
var BlusteryPuffball = new Flower("Blustery Puffball", 21, "underground", {
  "Monster Level": 30
}, true);
var HornOfPlenty = new Flower("Horn of Plenty", 22, "underground", {
  "Item Drop": 25
}, true);
var WizardsWig = new Flower("Wizard's Wig", 23, "underground", {
  "Mysticality Experience": 5
}, true);
var ShuffleTruffle = new Flower("Shuffle Truffle", 24, "underground", {
  Initiative: 25
});
var DisLichen = new Flower("Dis Lichen", 25, "underground", "Delevels Enemy");
var LooseMorels = new Flower("Loose Morels", 26, "underground", {
  "Sleaze Damage": 12.5
});
var FoulToadstool = new Flower("Foul Toadstool", 27, "underground", {
  "Stench Damage": 12.5
});
var Chillterelle = new Flower("Chillterelle", 28, "underground", {
  "Cold Damage": 12.5
});
var Portlybella = new Flower("Portlybella", 29, "underground", {
  "HP Regen Min": 10,
  "HP Regen Max": 30
});
var MaxHeadshroom = new Flower("Max Headshroom", 30, "underground", {
  "MP Regen Min": 5,
  "MP Regen Max": 15
});
var Spankton = new Flower("Spankton", 31, "underwater", "Delevels Enemy", true);
var Kelptomaniac = new Flower("Kelptomaniac", 32, "underwater", {
  "Item Drop": 40
}, true);
var Crookweed = new Flower("Crookweed", 33, "underwater", {
  "Meat Drop": 60
}, true);
var ElectricEelgrass = new Flower("Electric Eelgrass", 34, "underwater", "Blocks Attacks");
var Duckweed = new Flower("Duckweed", 35, "underwater", "Blocks Attacks");
var OrcaOrchid = new Flower("Orca Orchid", 36, "underwater", {
  "Weapon Damage": 12.5
});
var Sargassum = new Flower("Sargassum", 37, "underwater", {
  "Stench Damage": 12.5
});
var SubSeaRose = new Flower("Sub-Sea Rose", 38, "underwater", {
  "Cold Damage": 12.5
});
var Snori = new Flower("Snori", 39, "underwater", {
  "HP Regen Min": 20,
  "HP Regen Max": 30,
  "MP Regen Min": 10,
  "MP Regen Max": 20
});
var UpSeaDaisy = new Flower("Up Sea Daisy", 40, "underwater", {
  Experience: 30
});
var all = Object.freeze([RabidDogwood, Rutabeggar, RadishRadish, Artichoker, SmokeRa, SkunkCabbage, DeadlyCinnamon, CeleryStalker, LettuceSpray, SeltzerWatercress, WarLily, StealingMagnolia, CannedSpinach, Impatiens, SpiderPlant, RedFern, BamBoo, ArcticMoss, AloeGuvnor, PitcherPlant, BlusteryPuffball, HornOfPlenty, WizardsWig, ShuffleTruffle, DisLichen, LooseMorels, FoulToadstool, Chillterelle, Portlybella, MaxHeadshroom, Spankton, Kelptomaniac, Crookweed, ElectricEelgrass, Duckweed, OrcaOrchid, Sargassum, SubSeaRose, Snori, UpSeaDaisy]);

/**
 * @returns Whether we `have` the Crimbo Shrub in our terrarium
 */
function have$K() {
  return have$P($familiar`Crimbo Shrub`);
}
var Toppers = {
  Muscle: 1,
  Mysticality: 2,
  Moxie: 3
};
var Lights = {
  "Prismatic Damage": 1,
  "Hot Damage": 2,
  "Cold Damage": 3,
  "Stench Damage": 4,
  "Spooky Damage": 5,
  "Sleaze Damage": 6
};
var Garland = {
  "HP Regen": 1,
  "PvP Fights": 2,
  Blocking: 3
};
var Gifts = {
  "Yellow Ray": 1,
  "Red Ray": 2,
  Gifts: 3
};
var Prefs = {
  Muscle: "Muscle",
  Mysticality: "Mysticality",
  Moxie: "Moxie",
  Prismatic: "Prismatic Damage",
  Hot: "Hot Damage",
  Cold: "Cold Damage",
  Stench: "Stench Damage",
  Spooky: "Spooky Damage",
  Sleaze: "Sleaze Damage",
  HP: "HP Regen",
  PvP: "PvP Fights",
  blocking: "Blocking",
  yellow: "Yellow Ray",
  meat: "Red Ray",
  gifts: "Gifts"
};
/**
 * Internal function used to check whether decour perfectly matches what we want
 *
 * @param topper Topper to check for
 * @param lights Lights to check for
 * @param garland Garland to check for
 * @param gifts Gifts to check for
 * @returns Whether every single decour matches what we expect
 */
function isDecoratedWith(topper, lights, garland, gifts) {
  var decorations = [get$2("shrubTopper"), get$2("shrubLights"), get$2("shrubGarland"), get$2("shrubGifts")].map(x => Prefs[x]);
  return [topper, lights, garland, gifts].every((x, i) => x === decorations[i]);
}
/**
 * Decorates our Crimbo Shrub with the chosen decour, if able
 *
 * @param topper The shrub Topper to select
 * @param lights The shrub Lights to select
 * @param garland The shrub Garland to select
 * @param gifts The shrub Gifts to select
 * @returns Whether the shrub's decour perfectly matches the given decour
 */
function decorate(topper, lights, garland, gifts) {
  if (!have$K()) return false;
  if (get$2("_shrubDecorated")) return isDecoratedWith(topper, lights, garland, gifts);
  if (!have$P($item`box of old Crimbo decorations`)) {
    kolmafia.useFamiliar($familiar`Crimbo Shrub`);
  }
  directlyUse($item`box of old Crimbo decorations`);
  kolmafia.visitUrl(`choice.php?whichchoice=999&pwd=&option=1&topper=${Toppers[topper]}&lights=${Lights[lights]}&garland=${Garland[garland]}&gift=${Gifts[gifts]}`);
  return isDecoratedWith(topper, lights, garland, gifts);
}

var lab = $item`Little Geneticist DNA-Splicing Lab`;
/**
 * @returns Whether the DNA Lab is your currently active workshed
 */
function installed$3() {
  return kolmafia.getWorkshed() === lab;
}
var phylaEffects = new Map([[$phylum`beast`, $effect`Human-Beast Hybrid`], [$phylum`bug`, $effect`Human-Insect Hybrid`], [$phylum`constellation`, $effect`Human-Constellation Hybrid`], [$phylum`construct`, $effect`Human-Machine Hybrid`], [$phylum`demon`, $effect`Human-Demon Hybrid`], [$phylum`dude`, $effect`Human-Human Hybrid`], [$phylum`elemental`, $effect`Human-Elemental Hybrid`], [$phylum`elf`, $effect`Human-Elf Hybrid`], [$phylum`fish`, $effect`Human-Fish Hybrid`], [$phylum`goblin`, $effect`Human-Goblin Hybrid`], [$phylum`hippy`, $effect`Human-Hobo Hybrid`], [$phylum`horror`, $effect`Human-Horror Hybrid`], [$phylum`humanoid`, $effect`Human-Humanoid Hybrid`], [$phylum`mer-kin`, $effect`Human-Mer-kin Hybrid`], [$phylum`orc`, $effect`Human-Orc Hybrid`], [$phylum`penguin`, $effect`Human-Penguin Hybrid`], [$phylum`pirate`, $effect`Human-Pirate Hybrid`], [$phylum`plant`, $effect`Human-Plant Hybrid`], [$phylum`slime`, $effect`Human-Slime Hybrid`], [$phylum`undead`, $effect`Human-Undead Hybrid`], [$phylum`weird`, $effect`Human-Weird Thing Hybrid`]]);
var phylaTonics = new Map([[$phylum`beast`, $item`Gene Tonic: Beast`], [$phylum`bug`, $item`Gene Tonic: Insect`], [$phylum`constellation`, $item`Gene Tonic: Constellation`], [$phylum`construct`, $item`Gene Tonic: Construct`], [$phylum`demon`, $item`Gene Tonic: Demon`], [$phylum`dude`, $item`Gene Tonic: Humanoid`], [$phylum`elemental`, $item`Gene Tonic: Elemental`], [$phylum`elf`, $item`Gene Tonic: Elf`], [$phylum`fish`, $item`Gene Tonic: Fish`], [$phylum`goblin`, $item`Gene Tonic: Goblin`], [$phylum`hippy`, $item`Gene Tonic: Hobo`], [$phylum`horror`, $item`Gene Tonic: Horror`], [$phylum`humanoid`, $item`Gene Tonic: Humanoid`], [$phylum`mer-kin`, $item`Gene Tonic: Mer-kin`], [$phylum`orc`, $item`Gene Tonic: Orc`], [$phylum`penguin`, $item`Gene Tonic: Penguin`], [$phylum`pirate`, $item`Gene Tonic: Pirate`], [$phylum`plant`, $item`Gene Tonic: Plant`], [$phylum`slime`, $item`Gene Tonic: Slime`], [$phylum`undead`, $item`Gene Tonic: Undead`], [$phylum`weird`, $item`Gene Tonic: Weird`]]);
Array.from(phylaEffects.values());
/**
 * Returns the tonic item associated with a particular phylum.
 *
 * @param phylum The phylum in question.
 * @returns The tonic item associated with that phylum; returns $item.none for $phylum.none.
 */
function getTonic(phylum) {
  return phylaTonics.get(phylum) ?? $item.none;
  //return $item.none rather than null because it should never happen.
}
/**
 * Makes tonics with whatever phylum is currently in your syringe
 *
 * @param {number} [amount=1] the number of tonics to make
 * @returns Whether we successfully made tonics; returns true if we made as many as we could, regardless of whether that was the number requested
 */
function makeTonic() {
  var amount = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : 1;
  if (!installed$3()) return false;
  var currentSyringe = get$2("dnaSyringe");
  if (!currentSyringe) return false;
  var tonicPotion = getTonic(currentSyringe);
  var amountToMake = clamp(amount, 0, tonicsLeft());
  var startingAmount = kolmafia.itemAmount(tonicPotion);
  kolmafia.cliExecute(`camp dnapotion ${amountToMake}`);
  return kolmafia.itemAmount(tonicPotion) - startingAmount === amountToMake;
}
/**
 * Tells you how many tonics you can make the rest of the day.
 *
 * @returns The remaining tonics you can make
 */
function tonicsLeft() {
  return clamp(3 - get$2("_dnaPotionsMade"), 0, 3);
}

/**
 * @returns Whether we own the Chateau Mantegna
 */
function have$J() {
  return get$2("chateauAvailable");
}
/**
 * @returns `null` for an empty painting; otherwise, the monster trapped in our painting
 */
function paintingMonster() {
  return get$2("chateauMonster");
}
/**
 * @returns Whether or not we've fought our painted monster today
 */
function paintingFought() {
  return get$2("_chateauMonsterFought");
}
/**
 * Fights your currently installed painting monster if able
 *
 * @param combatParams Any parameters you'd like to pass to `runCombat`
 * @returns The result of `runCombat`, which is the page html of the final round of combat
 */
function fightPainting() {
  kolmafia.visitUrl("place.php?whichplace=chateau&action=chateau_painting", false);
  return kolmafia.runCombat.apply(void 0, arguments);
}

var Mayo$1 = {
  nex: $item`Mayonex`,
  diol: $item`Mayodiol`,
  zapine: $item`Mayozapine`,
  flex: $item`Mayoflex`
};
/**
 * @returns Whether the Mayo Clinic is our current active workshed
 */
function installed$2() {
  return kolmafia.getWorkshed() === $item`portable Mayo Clinic`;
}
/**
 * Sets mayo minder to a particular mayo, and ensures you have enough of it.
 *
 * @param mayo Mayo to use
 * @param quantity Quantity to ensure
 * @returns Whether we succeeded in this endeavor; a trivial `false` for people without the clinic `installed`
 */
function setMayoMinder(mayo) {
  var quantity = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : 1;
  if (kolmafia.getWorkshed() !== $item`portable Mayo Clinic`) return false;
  if (!Object.values(Mayo$1).includes(mayo)) {
    logger.error("Invalid mayo selected");
    return false;
  }
  if (get$2("mayoInMouth") && get$2("mayoInMouth") !== mayo.name) {
    logger.error("Currently have incorrect mayo in mouth");
    return false;
  }
  kolmafia.retrieveItem(quantity, mayo);
  if (!have$P($item`Mayo Minder™`)) kolmafia.buy($item`Mayo Minder™`);
  if (get$2("mayoMinderSetting") !== mayo.name) {
    withChoice(1076, mayo.id - 8260, () => kolmafia.use($item`Mayo Minder™`));
  }
  return get$2("mayoMinderSetting") === mayo.name;
}

/**
 * @returns Whether or not you can currently access Gingerbread City
 */
function available$3() {
  return (get$2("gingerbreadCityAvailable") || get$2("_gingerbreadCityToday")) && turns() < availableTurns();
}
function turns() {
  return get$2("_gingerbreadCityTurns") + (get$2("_gingerbreadClockAdvanced") ? 5 : 0);
}
function availableTurns() {
  return 20 + (get$2("gingerExtraAdventures") ? 10 : 0);
}
/**
 * @returns The number of Gingerbread encounters until it's Midnight in the city; this may be negative if the time has passed
 */
function minutesToMidnight() {
  return 19 - turns();
}
/**
 * @returns The number of Gingerbread encounters until it's Noon in the city; this may be negative if the time has passed
 */
function minutesToNoon() {
  return 9 - turns();
}
var LOCATIONS = Object.freeze($locations`Gingerbread Civic Center, Gingerbread Train Station, Gingerbread Industrial Zone, Gingerbread Upscale Retail District, Gingerbread Sewers`);
new Map([[$location`Gingerbread Train Station`, 1204], [$location`Gingerbread Civic Center`, 1202], [$location`Gingerbread Industrial Zone`, 1206], [$location`Gingerbread Upscale Retail District`, 1208]]);
new Map([[$location`Gingerbread Train Station`, 1205], [$location`Gingerbread Civic Center`, 1203], [$location`Gingerbread Industrial Zone`, 1207], [$location`Gingerbread Upscale Retail District`, 1209]]);
/**
 * @returns Whether or not it is possible for you to fight Judge Fudge today
 */
function canJudgeFudge() {
  if (minutesToNoon() >= 0) {
    return true;
  }
  if (minutesToMidnight() >= 0 && get$2("_gingerbreadColumnDestroyed")) {
    return true;
  }
  return false;
}

var item$b = $item`Source terminal`;
/**
 * @returns Is the terminal currently installed & available in our campground?
 */
function have$I() {
  return haveInCampground(item$b);
}
/**
 * Buffs that can be acquired from Enhance
 *
 * - Items: +30% Item Drop
 * - Meat: +60% Meat Drop
 * - Init: +50% Initiative
 * - Critical: +10% chance of Critical Hit, +10% chance of Spell Critical Hit
 * - Damage: +5 Prismatic Damage
 * - Substats: +3 Stats Per Fight
 */
var Buffs = {
  Items: $effect`items.enh`,
  Meat: $effect`meat.enh`,
  Init: $effect`init.enh`,
  Critical: $effect`critical.enh`,
  Damage: $effect`damage.enh`,
  Substats: $effect`substats.enh`
};
/**
 * Acquire a buff from the Source Terminal
 *
 * @param buff The buff to acquire
 * @see Buffs
 * @returns Whether we successfully acquired the buff
 */
function enhance(buff) {
  if (!Object.values(Buffs).includes(buff)) {
    return false;
  }
  return kolmafia.cliExecute(`terminal enhance ${buff.name}`);
}
/**
 * Rollover buffs that can be acquired from Enquiry
 */
var RolloverBuffs = {
  /** +5 Familiar Weight */
  Familiar: $effect`familiar.enq`,
  /** +25 ML */
  Monsters: $effect`monsters.enq`,
  /** +5 Prismatic Resistance */
  Protect: $effect`protect.enq`,
  /** +100% Muscle, +100% Mysticality, +100% Moxie */
  Stats: $effect`stats.enq`
};
/**
 * Acquire a buff from the Source Terminal
 *
 * @param rolloverBuff The buff to acquire
 * @see RolloverBuffs
 * @returns Whether we successfully `enquire`d the terminal for our rollover buff
 */
function enquiry(rolloverBuff) {
  if (!Object.values(RolloverBuffs).includes(rolloverBuff)) {
    return false;
  }
  return kolmafia.cliExecute(`terminal enquiry ${rolloverBuff.name}`);
}
/**
 * Skills that can be acquired from Enhance
 */
var Skills = {
  /** Collect Source essence from enemies once per combat */
  Extract: $skill`Extract`,
  /** Stagger and create a wandering monster 1-3 times per day */
  Digitize: $skill`Digitize`,
  /** Stagger and deal 25% of enemy HP in damage once per combat */
  Compress: $skill`Compress`,
  /** Double monster's HP, attack, defence, attacks per round and item drops once per fight and once per day (five in The Source) */
  Duplicate: $skill`Duplicate`,
  /** Causes government agent/Source Agent wanderer next turn once per combat and three times per day */
  Portscan: $skill`Portscan`,
  /** Increase Max MP by 100% and recover 1000 MP once per combat with a 30 turn cooldown */
  Turbo: $skill`Turbo`
};
/**
 * Make a skill available.
 * The Source Terminal can give the player access to two skills at any time
 *
 * @param skills Skill or 2-tuple of Skills to learn
 * @see Skills
 * @returns Whether our current skills match the ones we asked for
 */
function educate(skills) {
  var skillsArray = Array.isArray(skills) ? skills.slice(0, 2) : [skills];
  if (arrayEquals$1(skillsArray, getSkills())) return true;
  var _iterator = _createForOfIteratorHelper(skillsArray),
    _step;
  try {
    for (_iterator.s(); !(_step = _iterator.n()).done;) {
      var skill = _step.value;
      if (!Object.values(Skills).includes(skill)) return false;
      kolmafia.cliExecute(`terminal educate ${skill.name.toLowerCase()}.edu`);
    }
  } catch (err) {
    _iterator.e(err);
  } finally {
    _iterator.f();
  }
  return true;
}
/**
 * @returns The Skills currently available from Source Terminal
 */
function getSkills() {
  return ["sourceTerminalEducate1", "sourceTerminalEducate2"].map(p => get$2(p)).filter(Boolean).map(s => kolmafia.Skill.get(s.slice(0, -4)));
}
/**
 * @param skills A Skill or 2-tuple of Skills to check if we currently have active
 * @returns Whether the input agrees with our current skills
 */
function isCurrentSkill(skills) {
  var currentSkills = getSkills();
  var skillsArray = Array.isArray(skills) ? skills.slice(0, 2) : [skills];
  return skillsArray.every(skill => currentSkills.includes(skill));
}
/**
 * Items that can be generated by the Source Terminal
 */
var Items = new Map([[$item`browser cookie`, "food.ext"], [$item`hacked gibson`, "booze.ext"], [$item`Source shades`, "goggles.ext"], [$item`Source terminal GRAM chip`, "gram.ext"], [$item`Source terminal PRAM chip`, "pram.ext"], [$item`Source terminal SPAM chip`, "spam.ext"], [$item`Source terminal CRAM chip`, "cram.ext"], [$item`Source terminal DRAM chip`, "dram.ext"], [$item`Source terminal TRAM chip`, "tram.ext"], [$item`software bug`, "familiar.ext"]]);
/**
 * Collect an item from the Source Terminal (up to three times a day)
 *
 * @param item Item to collect
 * @see Items
 * @returns Whether the `cliExecute` succeeded
 */
function extrude(item) {
  var fileName = Items.get(item);
  if (!fileName) return false;
  return kolmafia.cliExecute(`terminal extrude ${fileName}`);
}
/**
 * @returns chips currently installed to player's Source Terminal
 */
function getChips() {
  return get$2("sourceTerminalChips").split(",");
}
/**
 * @returns number of times digitize was cast today
 */
function getDigitizeUses() {
  return get$2("_sourceTerminalDigitizeUses");
}
/**
 * @returns Monster that is currently digitized, else `null`
 */
function getDigitizeMonster() {
  return get$2("_sourceTerminalDigitizeMonster");
}
/**
 * @returns number of digitized monsters encountered since it was last cast
 */
function getDigitizeMonsterCount() {
  return get$2("_sourceTerminalDigitizeMonsterCount");
}
/**
 * @returns maximum number of digitizes player can cast
 */
function getMaximumDigitizeUses() {
  var chips = getChips();
  return 1 + (chips.includes("TRAM") ? 1 : 0) + (chips.includes("TRIGRAM") ? 1 : 0);
}
/**
 * @returns the current day's number of remaining digitize uses
 */
function getDigitizeUsesRemaining() {
  return getMaximumDigitizeUses() - getDigitizeUses();
}
/**
 * @returns whether the player could theoretically cast Digitize
 */
function couldDigitize() {
  return getDigitizeUses() < getMaximumDigitizeUses();
}
/**
 * Sets Digitize to be one of our skills if it currently isn't
 *
 * @returns Whether we expect that Digitize is one of our active skills now
 */
function prepareDigitize() {
  if (!isCurrentSkill(Skills.Digitize)) {
    return educate(Skills.Digitize);
  }
  return true;
}
/**
 * Determines whether the player can cast Digitize immediately
 * This only considers whether the player has learned the skill
 * and has sufficient daily casts remaining, not whether they have sufficient MP
 *
 * @returns Whether the player can currently cast digitize, ignoring the MP cost but accounting for other factors
 */
function canDigitize() {
  return couldDigitize() && getSkills().includes(Skills.Digitize);
}
new Copier(() => couldDigitize(), () => prepareDigitize(), () => canDigitize(), () => getDigitizeMonster());
/**
 * @returns number of times duplicate was cast today
 */
function getDuplicateUses() {
  return get$2("_sourceTerminalDuplicateUses");
}
/**
 * @returns number of times enhance was cast today
 */
function getEnhanceUses() {
  return get$2("_sourceTerminalEnhanceUses");
}
/**
 * @returns maximum number of times duplicate can be used
 */
function maximumDuplicateUses() {
  return kolmafia.myPath() === kolmafia.Path.get("The Source") ? 5 : 1;
}
/**
 * @returns number of remaining times duplicate can be used today
 */
function duplicateUsesRemaining() {
  return maximumDuplicateUses() - getDuplicateUses();
}
/**
 * @returns number of times enhance can be used per day
 */
function maximumEnhanceUses() {
  return 1 + getChips().filter(chip => ["CRAM", "SCRAM"].includes(chip)).length;
}
/**
 * @returns number of remaining times enahce can be used today
 */
function enhanceUsesRemaining() {
  return maximumEnhanceUses() - getEnhanceUses();
}

var item$a = $item`Witchess Set`;
/**
 * @returns Is the Witchess installed and available in our campground?
 */
function have$H() {
  return haveInCampground(item$a);
}
/**
 * @returns How many Witchess fights have we done so far today?
 */
function fightsDone() {
  return get$2("_witchessFights");
}
var pieces$1 = kolmafia.Monster.get(["Witchess Pawn", "Witchess Knight", "Witchess Bishop", "Witchess Rook", "Witchess Queen", "Witchess King", "Witchess Witch", "Witchess Ox"]);
/**
 * Fight a Witchess piece of your choice
 *
 * @param piece The piece to fight
 * @param combatParams Any parameters you'd like to pass to `runCombat`
 * @returns The value of `runCombat()`, which is the page html of the final round
 */
function fightPiece(piece) {
  if (!pieces$1.includes(piece)) throw new Error("That is not a valid piece.");
  if (!kolmafia.visitUrl("campground.php?action=witchess").includes("whichchoice value=1181")) {
    throw new Error("Failed to open Witchess.");
  }
  if (!kolmafia.runChoice(1).includes("whichchoice=1182")) {
    throw new Error("Failed to visit shrink ray.");
  }
  if (!kolmafia.visitUrl(`choice.php?option=1&pwd=${kolmafia.myHash()}&whichchoice=1182&piece=${piece.id}`, false).includes(piece.name)) {
    throw new Error("Failed to start fight.");
  }
  for (var _len = arguments.length, combatParams = new Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
    combatParams[_key - 1] = arguments[_key];
  }
  return kolmafia.runCombat.apply(void 0, combatParams);
}

var pantogram$1 = $item`portable pantogram`;
var pants = $item`pantogram pants`;
/**
 * @returns Do we `have` the Pantogram itself?
 */
function have$G() {
  return have$P(pantogram$1);
}
/**
 * @returns Do we `have` pantogram pants?
 */
function havePants() {
  return have$P(pants);
}
var Alignment = {
  ["Muscle"]: 1,
  ["Mysticality"]: 2,
  ["Moxie"]: 3
};
var Element = {
  ["Hot Resistance: 2"]: 1,
  ["Cold Resistance: 2"]: 2,
  ["Spooky Resistance: 2"]: 3,
  ["Sleaze Resistance: 2"]: 4,
  ["Stench Resistance: 2"]: 5
};
var LeftSacrifice = {
  ["Maximum HP: 40"]: [-1, 0],
  ["Maximum MP: 20"]: [-2, 0],
  ["HP Regen Max: 10"]: [$item`red pixel potion`, 1],
  ["HP Regen Max: 15"]: [$item`royal jelly`, 1],
  ["HP Regen Max: 20"]: [$item`scented massage oil`, 1],
  ["MP Regen Max: 10"]: [$item`Cherry Cloaca Cola`, 1],
  ["MP Regen Max: 15"]: [$item`bubblin' crude`, 1],
  ["MP Regen Max: 20"]: [$item`glowing New Age crystal`, 1],
  ["Mana Cost: -3"]: [$item`baconstone`, 1]
};
/**
 * Internal function used for `makePants`
 *
 * @param mod Modifier to get from our Left sacrifice
 * @returns Item-tuple to use in our URL string
 */
function getLeftSacPair(mod) {
  return LeftSacrifice[mod];
}
var MiddleSacrifice = {
  ["Combat Rate: -5"]: [-1, 0],
  ["Combat Rate: 5"]: [-2, 0],
  ["Critical Hit Percent: 10"]: [$item`hamethyst`, 1],
  ["Initiative: 50"]: [$item`bar skin`, 1],
  ["Familiar Weight: 10"]: [$item`lead necklace`, 11],
  ["Candy Drop: 100"]: [$item`huge bowl of candy`, 1],
  ["Item Drop Penalty: -10"]: [$item`sea salt crystal`, 11],
  ["Fishing Skill: 5"]: [$item`wriggling worm`, 1],
  ["Pool Skill: 5"]: [$item`8-ball`, 15],
  ["Avatar: Purple"]: [$item`moxie weed`, 99],
  ["Drops Items: true"]: [$item`ten-leaf clover`, 1]
};
/**
 * Internal function used for `makePants`
 *
 * @param mod Modifier to get from our Middle sacrifice
 * @returns Item-tuple to use in our URL string
 */
function getMiddleSacPair(mod) {
  return MiddleSacrifice[mod];
}
var RightSacrifice = {
  ["Weapon Damage: 20"]: [-1, 0],
  ["Spell Damage Percent: 20"]: [-2, 0],
  ["Meat Drop: 30"]: [$item`taco shell`, 1],
  ["Meat Drop: 60"]: [$item`porquoise`, 1],
  ["Item Drop: 15"]: [$item`fairy gravy boat`, 1],
  ["Item Drop: 30"]: [$item`tiny dancer`, 1],
  ["Muscle Experience: 3"]: [$item`Knob Goblin firecracker`, 3],
  ["Mysticality Experience: 3"]: [$item`razor-sharp can lid`, 3],
  ["Moxie Experience: 3"]: [$item`spider web`, 3],
  ["Muscle Experience Percent: 25"]: [$item`synthetic marrow`, 5],
  ["Mysticality Experience Percent: 25"]: [$item`haunted battery`, 5],
  ["Moxie Experience Percent: 25"]: [$item`the funk`, 5]
};
/**
 * Internal function used for `makePants`
 *
 * @param mod Modifier to get from our Right sacrifice
 * @returns Item-tuple to use in our URL string
 */
function getRightSacPair(mod) {
  return RightSacrifice[mod];
}
/**
 * Finds the item requirements for a particular pair of pants.
 *
 * @param modifiers An object consisting of the modifiers you want on your pants. For modifiers repeated across a particular sacrifice, use a tuple of that modifier and its value.
 * @returns A map of the items you need to make these pants and the quantities needed.
 */
function findRequirements(modifiers) {
  var leftSac = modifiers.leftSac,
    rightSac = modifiers.rightSac,
    middleSac = modifiers.middleSac;
  var returnValue = new Map();
  {
    var _getLeftSacPair = getLeftSacPair(leftSac),
      _getLeftSacPair2 = _slicedToArray(_getLeftSacPair, 2),
      sacrifice = _getLeftSacPair2[0],
      quantity = _getLeftSacPair2[1];
    if (sacrifice instanceof kolmafia.Item) {
      returnValue.set(sacrifice, quantity);
    }
  }
  {
    var _getRightSacPair = getRightSacPair(rightSac),
      _getRightSacPair2 = _slicedToArray(_getRightSacPair, 2),
      _sacrifice = _getRightSacPair2[0],
      _quantity = _getRightSacPair2[1];
    if (_sacrifice instanceof kolmafia.Item) {
      returnValue.set(_sacrifice, _quantity);
    }
  }
  {
    var _getMiddleSacPair = getMiddleSacPair(middleSac),
      _getMiddleSacPair2 = _slicedToArray(_getMiddleSacPair, 2),
      _sacrifice2 = _getMiddleSacPair2[0],
      _quantity2 = _getMiddleSacPair2[1];
    if (_sacrifice2 instanceof kolmafia.Item) {
      returnValue.set(_sacrifice2, _quantity2);
    }
  }
  return returnValue;
}
/**
 * Internal function used in `makePants`
 *
 * @param pair Tuple consisting of an item or number and another number
 * @returns URL parameter associated with the tuple
 */
function sacrificePairToURL(pair) {
  var _pair = _slicedToArray(pair, 2),
    rawSacrifice = _pair[0],
    quantity = _pair[1];
  var sacrifice = rawSacrifice instanceof kolmafia.Item ? rawSacrifice.id : rawSacrifice;
  return `${sacrifice},${quantity}`;
}
/**
 * Makes a pair of pants with the given modifiers
 *
 * @param alignment The stat you'd like your pants to improve. Moxie, Mysticality, or Muscle
 * @param element The element you'd like your pants to provide resistance for
 * @param leftSac The modifier you'd like to get from your leftmost sacrifice in Pantagramming.
 * @param middleSac The modifier you'd like to get from your middle sacrifice in Pantagramming.
 * @param rightSac The modifier you'd like to get from your rightmost sacrifice in Pantagramming.
 * @returns Whether or not you successfully created a pair of pants. False if you don't own the pantogram or if you already have pantogram pants.
 */
function makePants(alignment, element, leftSac, middleSac, rightSac) {
  if (have$P(pants) || !have$P(pantogram$1)) return false;
  var requirements = findRequirements({
    leftSac: leftSac,
    rightSac: rightSac,
    middleSac: middleSac
  });
  if (Array.from(requirements.entries()).some(_ref => {
    var _ref2 = _slicedToArray(_ref, 2),
      item = _ref2[0],
      quantity = _ref2[1];
    return !have$P(item, quantity);
  })) {
    return false;
  }
  var s1 = sacrificePairToURL(getLeftSacPair(leftSac));
  var s2 = sacrificePairToURL(getRightSacPair(rightSac));
  var s3 = sacrificePairToURL(getMiddleSacPair(middleSac));
  var url = `choice.php?whichchoice=1270&pwd&option=1&m=${Alignment[alignment]}&e=${Element[element]}&s1=${s1}&s2=${s2}&s3=${s3}`;
  directlyUse(pantogram$1);
  kolmafia.visitUrl(url);
  return have$P(pants);
}

/**
 * The Robortender itself
 */
var familiar$3 = $familiar`Robortender`;
/**
 * @returns Whether you have the Robortender in your terrarium/available
 */
function have$F() {
  return kolmafia.haveFamiliar(familiar$3);
}
var phylumDrops = new Map([[$phylum`Bug`, $item`pickled grasshopper`],
// bottle of anís
[$phylum`Constellation`, kolmafia.Item.get(9348)], [$phylum`Demon`, $item`bottle of novelty hot sauce`], [$phylum`Elemental`, $item`elemental sugarcube`], [$phylum`Elf`, $item`peppermint sprig`], [$phylum`Fish`, $item`bottle of clam juice`], [$phylum`Goblin`, $item`cocktail mushroom`], [$phylum`Hippy`, $item`shot of granola liqueur`], [$phylum`Hobo`, $item`can of cherry-flavored sterno`], [$phylum`Horror`, $item`lump of black ichor`], [$phylum`Humanoid`, $item`bottle of gregnadigne`],
// bottle of Crème de Fugu
[$phylum`Mer-kin`, kolmafia.Item.get(9358)], [$phylum`Orc`, $item`baby oil shooter`], [$phylum`Penguin`, $item`fish head`], [$phylum`Pirate`, $item`limepatch`], [$phylum`Plant`, $item`pile of dirt`], [$phylum`Slime`, $item`slime shooter`], [$phylum`Weird`, $item`imaginary lemon`]]);
/**
 *
 * @param target The phylum or monster you want to know the robortender drop of
 * @returns The robortender drop associated with that phylum or monster
 */
function dropFrom(target) {
  var phylum = target instanceof kolmafia.Monster ? target.phylum : target;
  return phylumDrops.get(phylum) ?? $item.none;
}
/**
 * Determines the probability of getting a robortender drop based on number of drops received
 *
 * @param dropNumber The number of drops to assume you've already received; defaults to mafia's tracked amount
 * @returns The probability of getting a robort drop
 */
function dropChance() {
  var dropNumber = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : get$2("_roboDrops");
  return [1, 0.5, 0.4, 0.4, 0.4, 0.3, 0.3, 0.3][dropNumber] ?? 0.2;
}
var minorDrinks = $items`literal grasshopper, double entendre, Phlegethon, Siberian sunrise, mentholated wine, low tide martini, shroomtini, morning dew, whiskey squeeze, great old fashioned, Gnomish sagngria, vodka stinger, extremely slippery nipple, piscatini, Churchill, soilzerac, London frog, nothingtini`;
var majorDrinks = $items`eighth plague, single entendre, reverse Tantalus, elemental caipiroska, Feliz Navidad, Bloody Nora, moreltini, hell in a bucket, Newark, R'lyeh, Gnollish sangria, vodka barracuda, Mysterious Island iced tea, drive-by shooting, gunner's daughter, dirt julep, Simepore slime, Phil Collins`;
var drinks = [].concat(_toConsumableArray(minorDrinks), _toConsumableArray(majorDrinks));
/**
 * @returns An array consisting of the drinks you've fed your robortender today.
 */
function currentDrinks() {
  var pref = get$2("_roboDrinks");
  if (!pref) return [];
  return pref.split(",").filter(x => x.trim()).map(name => kolmafia.toItem(name)).filter(drink => drinks.includes(drink));
}
/**
 * @param beverage A robortender-consumable drink of choice (i.e. Drive-By Shooting, Single Entendre, etc)
 * @returns A boolean; if true, the user's robortender has drunk that drink after execution. If false, it has not. This ALSO returns false if the user has not passed the function a robortender-consumable drink. If the user does not already have the beverage in their inventory, this function will not purchase the requested for you.
 */
function feed(beverage) {
  if (currentDrinks().includes(beverage)) return true;
  if (currentDrinks().length >= 5) return false;
  if (!drinks.includes(beverage)) return false;
  if (!kolmafia.itemAmount(beverage)) return false;
  if (!have$F()) return false;
  var priorFamiliar = kolmafia.myFamiliar();
  kolmafia.useFamiliar(familiar$3); // must equip Robortender to feed it
  kolmafia.visitUrl(`inventory.php?action=robooze&which=1&whichitem=${beverage.id}`);
  kolmafia.useFamiliar(priorFamiliar);
  return currentDrinks().includes(beverage);
}

/**
 * @returns Is the love tunnel available?
 */
function have$E() {
  return get$2("loveTunnelAvailable");
}
/**
 * @returns Have we visited the love tunnel yet today?
 */
function isUsed$1() {
  return get$2("_loveTunnelUsed");
}
/**
 * @returns Do we `have` an Enamorang?
 */
function haveLovEnamorang() {
  return have$P($item`LOV Enamorang`);
}
/**
 * @returns How many enamorangs have we used today?
 */
function getLovEnamorangUses() {
  return get$2("_enamorangs");
}
/**
 * @returns Can we currently use an enamorang?
 */
function couldUseLoveEnamorang() {
  return !haveWandererCounter(Wanderer.Enamorang) && getLovEnamorangUses() < 3 && haveLovEnamorang();
}
/**
 * @returns The Monster currently in our enamorang; `null` for none
 */
function getLovEnamorangMonster() {
  return get$2("enamorangMonster");
}
new Copier(() => couldUseLoveEnamorang(), null, () => couldUseLoveEnamorang(), () => getLovEnamorangMonster());
/**
 * Internal function used for `fightAll`
 *
 * @param equipment The equipment to select from the tunnel
 * @returns The relevant choice option
 */
function equipmentChoice(equipment) {
  switch (equipment) {
    case "LOV Eardigan":
      return 1;
    case "LOV Epaulettes":
      return 2;
    case "LOV Earring":
      return 3;
  }
}
/**
 *Internal function used for `fightAll`
 *
 * @param effect The effect to select from the tunnel
 * @returns The relevant choice option
 */
function effectChoice(effect) {
  switch (effect) {
    case "Lovebotamy":
      return 1;
    case "Open Heart Surgery":
      return 2;
    case "Wandering Eye Surgery":
      return 3;
  }
}
/**
 * Internal function used for `fightAll`
 *
 * @param extra The extra item to select from the tunnel
 * @returns The relevant choice option
 */
function extraChoice(extra) {
  switch (extra) {
    case "LOV Enamorang":
      return 1;
    case "LOV Emotionizer":
      return 2;
    case "LOV Extraterrestrial Chocolate":
      return 3;
    case "LOV Echinacea Bouquet":
      return 4;
    case "LOV Elephant":
      return 5;
    case "toast":
      return 6;
    case null:
      return 7;
  }
}
/**
 * Fight all LOV monsters and get buffs/equipment.
 *
 * @param equipment Equipment to take from LOV.
 * @param effect Effect to take from LOV.
 * @param extra Extra item to take from LOV.
 */
function fightAll(equipment, effect, extra) {
  withChoices({
    1222: 1,
    1223: 1,
    1224: equipmentChoice(equipment),
    1225: 1,
    1226: effectChoice(effect),
    1227: 1,
    1228: extraChoice(extra)
  }, () => {
    kolmafia.adv1($location`The Tunnel of L.O.V.E.`, 0, "");
  });
}

/**
 * Returns Infinity for counters that do not exist, and otherwise returns the duration of the counter
 *
 * @param counter The name of the counter in question
 * @returns Infinity if the counter does not exist; otherwise returns the duration of the counter
 */
function get(counter) {
  var value = kolmafia.getCounter(counter);
  // getCounter returns -1 for counters that don't exist, but it also returns -1 for counters whose value is -1
  if (value === -1) {
    // if we have a counter with value -1, we check to see if that counter exists via getCounters()
    // We return null if it doesn't exist
    return kolmafia.getCounters(counter, -1, -1).trim() === "" ? Infinity : -1;
  }
  return value;
}
/**
 * The world is everything that is the case. This determines which counters are the case.
 *
 * @param counter The name of the counter in question
 * @returns True for counters which currently exist; false for those which do not
 */
function exists(counter) {
  return kolmafia.getCounter(counter) !== -1 || kolmafia.getCounters(counter, -1, -1).trim() !== "";
}

/**
 * @returns Whether we `have` the latte lovers member's mug
 */
function have$D() {
  return have$P($item`latte lovers member's mug`);
}
/**
 * @returns The current target of `Offer Latte`, assuming the effect is active; otherwise, `null`
 */
function sniffedMonster() {
  return exists("Latte Monster") ? get$2("_latteMonster") : null;
}
/**
 * @returns The number of latte refills remaining for the day
 */
function refillsRemaining() {
  return clamp(3 - get$2("_latteRefillsUsed"), 0, 3);
}
var INGREDIENTS = {
  ancient: {
    modifier: {
      "Spooky Damage": 50
    },
    location: $location`The Mouldering Mansion`
  },
  asp: {
    modifier: {
      "Weapon Damage": 25
    },
    location: $location`The Middle Chamber`
  },
  basil: {
    modifier: {
      "HP Regen Min": 5,
      "HP Regen Max": 5
    },
    location: $location`The Overgrown Lot`
  },
  belgian: {
    modifier: {
      "Moxie Percent": 20,
      "Mysticality Percent": 20,
      "Muscle Percent": 20
    },
    location: $location`Whitey's Grove`
  },
  chalk: {
    modifier: {
      "Cold Damage": 25
    },
    location: $location`The Haunted Billiards Room`
  },
  "bug-thistle": {
    modifier: {
      Mysticality: 20
    },
    location: $location`The Bugbear Pen`
  },
  butternut: {
    modifier: {
      "Spell Damage": 10
    },
    location: $location`Madness Bakery`
  },
  cajun: {
    modifier: {
      "Meat Drop": 40
    },
    location: $location`The Black Forest`
  },
  carrot: {
    modifier: {
      "Item Drop": 20
    },
    location: $location`The Dire Warren`
  },
  carrrdamom: {
    modifier: {
      "MP Regen Min": 4,
      "MP Regen Max": 6
    },
    location: $location`Barrrney's Barrr`
  },
  chili: {
    modifier: {
      "Hot Resistance": 3
    },
    location: $location`The Haunted Kitchen`
  },
  clove: {
    modifier: {
      "Stench Resistance": 3
    },
    location: $location`The Sleazy Back Alley`
  },
  coal: {
    modifier: {
      "Hot Damage": 25
    },
    location: $location`The Haunted Boiler Room`
  },
  cocoa: {
    modifier: {
      "Cold Resistance": 3
    },
    location: $location`The Icy Peak`
  },
  diet: {
    modifier: {
      Initiative: 50
    },
    location: $location`The Cola Wars Battlefield`
  },
  dwarf: {
    modifier: {
      Muscle: 30
    },
    location: $location`Itznotyerzitz Mine`
  },
  dyspepsi: {
    modifier: {
      Initiative: 25
    },
    location: $location`Cola Wars Battlefield (Dyspepsi Uniform)`
  },
  filth: {
    modifier: {
      "Damage Reduction": 20
    },
    location: $location`The Feeding Chamber`
  },
  grass: {
    modifier: {
      Experience: 3
    },
    location: $location`The Hidden Park`
  },
  fungus: {
    modifier: {
      "Maximum MP": 30
    },
    location: $location`The Fungal Nethers`
  },
  mold: {
    modifier: {
      "Spooky Damage": 20
    },
    location: $location`The Unquiet Garves`
  },
  greek: {
    modifier: {
      "Sleaze Damage": 25
    },
    location: $location`Wartime Frat House`
  },
  grobold: {
    modifier: {
      "Sleaze Damage": 25
    },
    location: $location`The Old Rubee Mine`
  },
  guarna: {
    modifier: {
      Adventures: 4
    },
    location: $location`The Bat Hole Entrance`
  },
  gunpowder: {
    modifier: {
      "Weapon Damage": 50
    },
    location: $location`1st Floor, Shiawase-Mitsuhama Building`
  },
  healing: {
    modifier: {
      "HP Regen Min": 10,
      "HP Regen Max": 20
    },
    location: $location`The Daily Dungeon`
  },
  hellion: {
    modifier: {
      "PvP Fights": 6
    },
    location: $location`The Dark Neck of the Woods`
  },
  hobo: {
    modifier: {
      "Damage Absorption": 50
    },
    location: $location`Hobopolis Town Square`
  },
  greasy: {
    modifier: {
      "Muscle Percent": 50
    },
    location: $location`Cobb's Knob Barracks`
  },
  wing: {
    modifier: {
      "Combat Rate": 10
    },
    location: $location`The Dark Heart of the Woods`
  },
  ink: {
    modifier: {
      "Combat Rate": -10
    },
    location: $location`The Haunted Library`
  },
  kombucha: {
    modifier: {
      "Stench Damage": 25
    },
    location: $location`Wartime Hippy Camp`
  },
  lihc: {
    modifier: {
      "Spooky Damage": 25
    },
    location: $location`The Defiled Niche`
  },
  lizard: {
    modifier: {
      "MP Regen Min": 5,
      "MP Regen Max": 15
    },
    location: $location`The Arid, Extra-Dry Desert`
  },
  macaroni: {
    modifier: {
      "Maximum HP": 20
    },
    location: $location`The Haunted Pantry`
  },
  mega: {
    modifier: {
      "Moxie Percent": 50
    },
    location: $location`Cobb's Knob Laboratory`
  },
  oil: {
    modifier: {
      "Sleaze Damage": 20
    },
    location: $location`The Old Landfill`
  },
  msg: {
    modifier: {
      "Critical Hit Percent": 15
    },
    location: $location`The Briniest Deepests`
  },
  norwhal: {
    modifier: {
      "Maximum HP Percent": 200
    },
    location: $location`The Ice Hole`
  },
  paint: {
    modifier: {
      "Prismatic Damage": 5
    },
    location: $location`The Haunted Gallery`
  },
  paradise: {
    modifier: {
      Moxie: 20,
      Muscle: 20,
      Mysticality: 20
    },
    location: $location`The Stately Pleasure Dome`
  },
  rawhide: {
    modifier: {
      "Familiar Weight": 5
    },
    location: $location`The Spooky Forest`
  },
  rock: {
    modifier: {
      "Critical Hit Percent": 10
    },
    location: $location`The Brinier Deepers`
  },
  salt: {
    modifier: {
      "Critical Hit Percent": 5
    },
    location: $location`The Briny Deeps`
  },
  sandalwood: {
    modifier: {
      Moxie: 5,
      Muscle: 5,
      Mysticality: 5
    },
    location: $location`Noob Cave`
  },
  sausage: {
    modifier: {
      "Mysticality Percent": 50
    },
    location: $location`Cobb's Knob Kitchens`
  },
  space: {
    modifier: {
      Moxie: 10,
      Muscle: 10,
      Mysticality: 10
    },
    location: $location`The Hole in the Sky`
  },
  squash: {
    modifier: {
      "Spell Damage": 10
    },
    location: $location`The Copperhead Club`
  },
  teeth: {
    modifier: {
      "Spooky Damage": 25,
      "Weapon Damage": 25
    },
    location: $location`The VERY Unquiet Garves`
  },
  vitamin: {
    modifier: {
      "Familiar Experience": 3
    },
    location: $location`The Dark Elbow of the Woods`
  },
  flour: {
    modifier: {
      "Sleaze Resistance": 3
    },
    location: $location`The Road to the White Citadel`
  },
  squamous: {
    modifier: {
      "Spooky Resistance": 3
    },
    location: $location`The Caliginous Abyss`
  },
  pumpkin: {
    modifier: {
      "Mysticality Experience": 1,
      "Spell Damage": 5,
      "Mysticality Percent": 5
    },
    location: null
  },
  cinnamon: {
    modifier: {
      "Moxie Experience": 1,
      "Pickpocket Rate": 5,
      "Moxie Percent": 5
    },
    location: null
  },
  vanilla: {
    modifier: {
      "Muscle Experience": 1,
      "Weapon Damage Percent": 5,
      "Muscle Percent": 5
    },
    location: null
  }
};
/**
 * @returns An array consisting of the Ingredients you've unlocked so far this ascension
 */
function ingredientsUnlocked() {
  return get$2("latteUnlocks").split(",");
}
/**
 * Fill the latte with ingredients of your choosing
 *
 * @param ingredients Ingredients to fill the latte with
 * @returns Whether we succeeded in this endeavor
 */
function fill() {
  if (refillsRemaining() <= 0) return false;
  for (var _len = arguments.length, ingredients = new Array(_len), _key = 0; _key < _len; _key++) {
    ingredients[_key] = arguments[_key];
  }
  if (new Set(ingredients).size < 3) return false;
  if (ingredients.some(i => !ingredientsUnlocked().includes(i))) return false;
  return kolmafia.cliExecute(`latte refill ${ingredients.join(" ")}`);
}
/**
 * @param ingredient A latte ingredient
 * @returns An object containing the modifiers associated with that latte ingredient
 */
function modifierOf(ingredient) {
  return INGREDIENTS[ingredient].modifier;
}
/**
 * @param ingredient A latte ingredient
 * @returns The location that can be used to unlock said ingredient; null if the ingredient is free
 */
function locationOf(ingredient) {
  return INGREDIENTS[ingredient].location;
}
/**
 * @returns An array consisting of all Ingredients currently in your latte
 */
function currentIngredients() {
  return kolmafia.getProperty("latteIngredients").split(",");
}

var Latte = /*#__PURE__*/Object.freeze({
  __proto__: null,
  currentIngredients: currentIngredients,
  fill: fill,
  have: have$D,
  ingredientsUnlocked: ingredientsUnlocked,
  locationOf: locationOf,
  modifierOf: modifierOf,
  refillsRemaining: refillsRemaining,
  sniffedMonster: sniffedMonster
});

var item$9 = $item`SongBoom™ BoomBox`;
/**
 * @returns Whether we `have` the SongBoom™ BoomBox
 */
function have$C() {
  return have$P(item$9);
}
var keywords = {
  "Eye of the Giger": "spooky",
  "Food Vibrations": "food",
  "Remainin' Alive": "dr",
  "These Fists Were Made for Punchin'": "damage",
  "Total Eclipse of Your Meat": "meat"
};
var songBoomSongs = new Set(Object.keys(keywords));
/**
 * @returns The `SongBoomSong` you currently have active; `null` if none is active at this time
 */
function song() {
  var stored = get$2("boomBoxSong");
  return songBoomSongs.has(stored) ? stored : null;
}
/**
 * @returns Song changes left today.
 */
function songChangesLeft() {
  return get$2("_boomBoxSongsLeft");
}
/**
 * Change the song. Throws an error if unable.
 *
 * @param newSong Song to change to.
 * @returns Whether we successfully changed the song; `false` thus means that this was already our current song.
 */
function setSong(newSong) {
  if (song() !== newSong) {
    if (songChangesLeft() === 0) throw new Error("Out of song changes!");
    kolmafia.cliExecute(`boombox ${newSong ? keywords[newSong] : "none"}`);
    return true;
  } else {
    return false;
  }
}

/**
 * Determines whether we `have` the beach comb
 *
 * @returns Whether we `have` the beach comb
 */
function have$B() {
  return have$P(kolmafia.Item.get("Beach Comb"));
}
/**
 * Determines whether we `have` the beach comb or the driftwood beach comb
 *
 * @returns Whether we `have` either the beach comb or the driftwood beach comb
 */
function available$2() {
  return have$B() || have$P(kolmafia.Item.get("driftwood beach comb"));
}
var headBuffs = [$effect`Hot-Headed`, $effect`Cold as Nice`, $effect`A Brush with Grossness`, $effect`Does It Have a Skull In There??`, $effect`Oiled, Slick`, $effect`Lack of Body-Building`, $effect`We're All Made of Starfish`, $effect`Pomp & Circumsands`, $effect`Resting Beach Face`, $effect`Do I Know You From Somewhere?`, $effect`You Learned Something Maybe!`];
var head = {
  HOT: $effect`Hot-Headed`,
  COLD: $effect`Cold as Nice`,
  STENCH: $effect`A Brush with Grossness`,
  SPOOKY: $effect`Does It Have a Skull In There??`,
  SLEAZE: $effect`Oiled, Slick`,
  MUSCLE: $effect`Lack of Body-Building`,
  MYSTICALITY: $effect`We're All Made of Starfish`,
  INITIATIVE: $effect`Resting Beach Face`,
  FAMILIAR: $effect`Do I Know You From Somewhere?`,
  EXPERIENCE: $effect`You Learned Something Maybe!`
};
/**
 * @returns The number of free combs we have available for today
 */
function freeCombs() {
  return available$2() ? clamp(11 - get$2("_freeBeachWalksUsed"), 0, 11) : 0;
}
/**
 * Determines whether a given Beach Head can be combed today
 *
 * @param target The head in question, either as the Effect it grants or as its name
 * @returns Whether the given head is combable
 */
function headAvailable(target) {
  var effect = target instanceof kolmafia.Effect ? target : head[target];
  var headNumber = 1 + headBuffs.indexOf(effect);
  return kolmafia.getProperty("beachHeadsUnlocked").split(",").includes(headNumber.toString()) && !kolmafia.getProperty("_beachHeadsUsed").split(",").includes(headNumber.toString());
}
/**
 * Tries to comb a given Beach Head
 *
 * @param target The Beach Head to comb, given either as its effect or as its name
 * @returns Whether we have the head effect at the end of the whole rigamarole; this means that if you `tryHead` when you already have the effect, it will (presumably) fail to comb but will return `true`
 */
function tryHead(target) {
  var effect = target instanceof kolmafia.Effect ? target : head[target];
  if (!headBuffs.includes(effect)) return false;
  if (!headAvailable(target)) return false;
  kolmafia.cliExecute(effect.default);
  return have$P(effect);
}

var familiar$2 = $familiar`Pocket Professor`;
/**
 * Calculate the total number of lectures available to you given a particular familiar weight and chip-equipped status
 * @param weight The weight to calculate at--defaults to your current total familiar weight
 * @param includeChip Whether or not to include the memory chip--defaults to whether or not it's currently equipped
 * @returns The total number of lectures you're able to deliver, including ones you've already delivered today
 */
function totalAvailableLectures() {
  var weight = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : totalFamiliarWeight(familiar$2);
  var includeChip = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : kolmafia.haveEquipped($item`Pocket Professor memory chip`);
  return (includeChip ? 2 : 0) + Math.floor(Math.sqrt(weight - 1));
}

var familiar$1 = kolmafia.Familiar.get("Red-Nosed Snapper");
/**
 * Map of phylum to item that phylum drops.
 */
var phylumItem = new Map([[kolmafia.Phylum.get("beast"), kolmafia.Item.get("patch of extra-warm fur")], [kolmafia.Phylum.get("bug"), kolmafia.Item.get("a bug's lymph")], [kolmafia.Phylum.get("constellation"), kolmafia.Item.get("micronova")], [kolmafia.Phylum.get("construct"), kolmafia.Item.get("industrial lubricant")], [kolmafia.Phylum.get("demon"), kolmafia.Item.get("infernal snowball")], [kolmafia.Phylum.get("dude"), kolmafia.Item.get("human musk")], [kolmafia.Phylum.get("elemental"), kolmafia.Item.get("livid energy")], [kolmafia.Phylum.get("elf"), kolmafia.Item.get("peppermint syrup")], [kolmafia.Phylum.get("fish"), kolmafia.Item.get("fish sauce")], [kolmafia.Phylum.get("goblin"), kolmafia.Item.get("guffin")], [kolmafia.Phylum.get("hippy"), kolmafia.Item.get("organic potpourri")], [kolmafia.Phylum.get("hobo"), kolmafia.Item.get("beggin' cologne")], [kolmafia.Phylum.get("horror"), kolmafia.Item.get("powdered madness")], [kolmafia.Phylum.get("humanoid"), kolmafia.Item.get("vial of humanoid growth hormone")], [kolmafia.Phylum.get("mer-kin"), kolmafia.Item.get("Mer-kin eyedrops")], [kolmafia.Phylum.get("orc"), kolmafia.Item.get("boot flask")], [kolmafia.Phylum.get("penguin"), kolmafia.Item.get("envelope full of Meat")], [kolmafia.Phylum.get("pirate"), kolmafia.Item.get("Shantix™")], [kolmafia.Phylum.get("plant"), kolmafia.Item.get("goodberry")], [kolmafia.Phylum.get("slime"), kolmafia.Item.get("extra-strength goo")], [kolmafia.Phylum.get("undead"), kolmafia.Item.get("unfinished pleasure")], [kolmafia.Phylum.get("weird"), kolmafia.Item.get("non-Euclidean angle")]]);
/**
 * Map of drop item to phylum it drops from.
 */
new Map(_toConsumableArray(phylumItem).map(_ref => {
  var _ref2 = _slicedToArray(_ref, 2),
    phylum = _ref2[0],
    item = _ref2[1];
  return [item, phylum];
}));
/**
 * Return whether you have a Red-Nosed Snapper.
 *
 * @returns True if you have a Red-Nosed Snapper, false otherwise.
 */
function have$A() {
  return kolmafia.haveFamiliar(familiar$1);
}
/**
 * Get the phylum currently being tracked by the snapper.
 *
 * @returns Tracked phylum, or null if no phylum tracked.
 */
function getTrackedPhylum() {
  return get$2("redSnapperPhylum");
}
/**
 * Set snapper tracking to a certain phylum.
 *
 * @param phylum Phylum to track.
 */
function trackPhylum(phylum) {
  var currentFamiliar = kolmafia.myFamiliar();
  try {
    kolmafia.useFamiliar(familiar$1);
    kolmafia.cliExecute(`snapper ${phylum}`);
  } finally {
    kolmafia.useFamiliar(currentFamiliar);
  }
}
/**
 * Get progress to next snapper drop.
 *
 * @returns Number of fights completed (out of 11) to reach next drop.
 */
function getProgress() {
  return get$2("redSnapperProgress");
}

var passive = $skill`Comprehensive Cartography`;
/**
 * Determines whether you `have` the skill Comprehensive Cartography
 *
 * @returns Whether you currently `have` the skill
 */
function have$z() {
  return have$P(passive);
}
/**
 * @returns The number of monsters you can map today
 */
function availableMaps() {
  return have$z() ? $skill`Map the Monsters`.dailylimit : 0;
}
/**
 * @returns Whether or not `Map the Monsters` is currently active
 */
function currentlyMapping() {
  return get$2("mappingMonsters");
}

var item$8 = $item`Guzzlr tablet`;
/**
 * Determines whether you `have` Guzzlr
 *
 * @returns Whether you `have` Guzzlr
 */
function have$y() {
  return have$P(item$8);
}
/**
 * Internal function used to navigate the Guzzlr interface
 *
 * @param option Choice option to select
 */
function useTabletWithChoice(option) {
  withChoice(1412, option, () => kolmafia.use(1, item$8));
}
/**
 * Determines whether you currently have an active Guzzlr quest
 *
 * @returns Whether you currently have an active Guzzlr quest
 */
function isQuestActive() {
  return get$2("questGuzzlr") !== "unstarted";
}
/**
 * Determines total number of Platinum deliveries completed
 *
 * @returns Platinum deliveries completed overall
 */
function getPlatinum() {
  return get$2("guzzlrPlatinumDeliveries");
}
/**
 * Determines the number of Platinum deliveries completed today
 *
 * @returns Platinum deliveries completed today
 */
function getPlatinumToday() {
  return get$2("_guzzlrPlatinumDeliveries");
}
/**
 * Determines whether you are currently eligible to do a Platinum delivery
 *
 * @returns Whether you are currently eligible to do a Platinum delivery
 */
function canPlatinum() {
  return !isQuestActive() && getGold() >= 5 && getPlatinumToday() < 1;
}
/**
 * Determines whether you have fully unlocked the Platinum delivery bonuses (done >= 30)
 *
 * @returns Whether you have fully unlocked the Platinum delivery enchantment bonuses
 */
function haveFullPlatinumBonus() {
  return getPlatinum() >= 30;
}
/**
 * Accepts a Platinum delivery
 *
 * @returns Whether we succeeded in this endeavor
 */
function acceptPlatinum() {
  if (!canPlatinum()) return false;
  useTabletWithChoice(4);
  return true;
}
/**
 * Determines total number of Gold deliveries completed
 *
 * @returns Gold deliveries completed overall
 */
function getGold() {
  return get$2("guzzlrGoldDeliveries");
}
/**
 * Determines the number of Gold deliveries completed today
 *
 * @returns Gold deliveries completed today
 */
function getGoldToday() {
  return get$2("_guzzlrGoldDeliveries");
}
/**
 * Determines whether you are currently eligible to do a Gold delivery
 *
 * @returns Whether you are currently eligible to do a Gold delivery
 */
function canGold() {
  return !isQuestActive() && getBronze() >= 5 && getGoldToday() < 3;
}
/**
 * Determines whether you have fully unlocked the Gold delivery bonuses (done >= 150)
 *
 * @returns Whether you have fully unlocked the Gold delivery enchantment bonuses
 */
function haveFullGoldBonus() {
  return getGold() >= 150;
}
/**
 * Accepts a Gold delivery
 *
 * @returns Whether we succeeded in this endeavor
 */
function acceptGold() {
  if (!canGold()) return false;
  useTabletWithChoice(3);
  return true;
}
/**
 * Determines total number of Bronze deliveries completed
 *
 * @returns Bronze deliveries completed overall
 */
function getBronze() {
  return get$2("guzzlrBronzeDeliveries");
}
/**
 * Determines whether you have fully unlocked the Bronze delivery bonuses (done >= 196)
 *
 * @returns Whether you have fully unlocked the Bronze delivery enchantment bonuses
 */
function haveFullBronzeBonus() {
  return getBronze() >= 196;
}
/**
 * Accepts a Bronze delivery
 *
 * @returns Whether we succeeded in this endeavor
 */
function acceptBronze() {
  if (isQuestActive()) return false;
  useTabletWithChoice(2);
  return true;
}
/**
 * Determines whether we can abandon the current Guzzlr quest
 *
 * @returns Whether we are able to abandon our current Guzzlr quest
 */
function canAbandon() {
  return isQuestActive() && !get$2("_guzzlrQuestAbandoned");
}
/**
 * Abandons Guzzlr quest
 *
 * @returns `false` if we were unable to abandon a quest; `true` otherwise
 */
function abandon() {
  if (!canAbandon()) return false;
  kolmafia.visitUrl("inventory.php?tap=guzzlr", false);
  kolmafia.runChoice(1);
  kolmafia.runChoice(5);
  return true;
}
/**
 * Determines the target location for your current Guzzlr quest
 *
 * @returns The current target location for your Guzzlr quest, if it exists
 */
function getLocation() {
  return get$2("guzzlrQuestLocation");
}
/**
 * Determines the tier of your current Guzzlr quest
 *
 * @returns The tier of your current Guzzlr quest; `null` if there is no active quest
 */
function getTier() {
  var tier = get$2("guzzlrQuestTier");
  return tier === "" ? null : tier;
}
/**
 * Determines the current booze item you need to deliver for your current Guzzlr quest
 *
 * @returns The booze item associated with your Guzzlr quest if it exists; `null` otherwise
 */
function getBooze() {
  return get$2("guzzlrQuestBooze");
}
/**
 * List of the platinum cocktails
 */
var Cocktails = $items`Buttery Boy, Steamboat, Ghiaccio Colada, Nog-on-the-Cob, Sourfinger`;
/**
 * Determines whether you currently have a Platinum cocktail available
 *
 * @returns `true` if you have at least one Platinum cocktail in your inventory; `false` otherwise
 */
function havePlatinumBooze() {
  return Cocktails.some(cock => kolmafia.itemAmount(cock) > 0);
}
/**
 * Determines if you currently have in your inventory the booze necessary to progress your Guzzlr quest
 *
 * @returns `true` if you have the booze necessary to progress your Guzzlr quest
 */
function haveBooze() {
  var booze = getBooze();
  switch (booze) {
    case null:
      return false;
    case $item`Guzzlr cocktail set`:
      return havePlatinumBooze();
    default:
      return kolmafia.itemAmount(booze) > 0;
  }
}
var ingredientToPlatinumCocktail = new Map([[$item`miniature boiler`, $item`Steamboat`], [$item`cold wad`, $item`Ghiaccio Colada`], [$item`robin's egg`, $item`Nog-on-the-Cob`], [$item`mangled finger`, $item`Sourfinger`], [$item`Dish of Clarified Butter`, $item`Buttery Boy`]]);
invertMap(ingredientToPlatinumCocktail);
/**
 * Determines the cheapest Platinum cocktail to obtain or produce
 *
 * @param freeCraft Defaults to `true`; if set to `false`, will count the price of any turns spent cocktailcrafting
 * @returns The expected price of the cheapest Platinum cocktail to obtain or produce
 */
function getCheapestPlatinumCocktail() {
  var freeCraft = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : true;
  if (freeCraft) {
    return maxBy(Array.from(ingredientToPlatinumCocktail), ingredientAndCocktail => Math.min.apply(Math, _toConsumableArray(ingredientAndCocktail.map(item => kolmafia.mallPrice(item)))), true)[1];
  } else {
    return maxBy(Array.from(ingredientToPlatinumCocktail), _ref => {
      var _ref2 = _slicedToArray(_ref, 2),
        cocktail = _ref2[1];
      return kolmafia.mallPrice(cocktail);
    })[1];
  }
}
/**
 * Calculates the number of turns remaining for your Guzzlr quest
 *
 * @param useShoes Whether or not the calculation should assume you are using Guzzlr shoes
 * @returns The expected number of turns needed to finish your current delivery
 */
function turnsLeftOnQuest() {
  var useShoes = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : false;
  var progressPerTurn = useShoes ? Math.floor((10 - get$2("_guzzlrDeliveries")) * 1.5) : 10 - get$2("_guzzlrDeliveries");
  return Math.ceil((100 - get$2("guzzlrDeliveryProgress")) / progressPerTurn);
}
/**
 * Calculates the expected Guzzlrbuck reward for completing your current Guzzlr quest
 *
 * @param usePants Whether or not we should account for Guzzlr pants in our calculation
 * @returns The expected Guzzlrbuck reward of our current Guzzlr quest
 */
function expectedReward() {
  var usePants = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : false;
  switch (getTier()) {
    case "platinum":
      // 20-25
      return 22.5 + (usePants ? 5 : 0);
    case "gold":
      // 5-7
      return 6 + (usePants ? 3 : 0);
    case "bronze":
      // 2-4
      return 3 + (usePants ? 3 : 0);
    default:
      return 0;
  }
}

var item$7 = $item`unwrapped knock-off retro superhero cape`;
/**
 * Determines whether you have() the Retro Cape.
 *
 * @returns Whether you have the Retro Cape available.
 */
function have$x() {
  return have$P(item$7);
}
var currentHero = () => get$2("retroCapeSuperhero");
var currentMode = () => get$2("retroCapeWashingInstructions");
/**
 * Tunes retro cape to a given setting
 *
 * @param hero The Superhero to set it to
 * @param mode The washing instructions to set it to
 * @returns Whether we successfully tuned the Retro Cape.
 */
function set(hero, mode) {
  if (!have$x()) return false;
  if (currentHero() === hero && currentMode() === mode) return true;
  kolmafia.cliExecute(`retrocape ${hero} ${mode}`);
  return currentHero() === hero && currentMode() === mode;
}
new Map([[$skill`Smooch of the Daywalker`, ["vampire", "kiss"]], [$skill`Slay the Dead`, ["vampire", "kill"]], [$skill`Unleash the Devil's Kiss`, ["heck", "kiss"]], [$skill`Deploy Robo-Handcuffs`, ["robot", "hold"]], [$skill`Blow a Robo-Kiss`, ["robot", "kiss"]], [$skill`Precision Shot`, ["robot", "kill"]]]);

var orb = kolmafia.Item.get("miniature crystal ball");
/**
 * Determines whether you `have` the Miniature Crystal Ball
 *
 * @returns Whether you `have` the Miniature Crystal Ball
 */
function have$w() {
  return kolmafia.availableAmount(orb) > 0;
}
/**
 * @returns A map keyed by locations and whose values are monsters, representing all active orb predictions
 */
function getPrediction() {
  return new Map(multiSplit("crystalBallPredictions", "|", ":", [Number, kolmafia.toLocation, kolmafia.toMonster]).map(_ref => {
    var _ref2 = _slicedToArray(_ref, 3),
      location = _ref2[1],
      monster = _ref2[2];
    return [location, monster];
  }));
}
var getLastPondered = () => `${kolmafia.myTotalTurnsSpent()};${kolmafia.totalTurnsPlayed()};${get$2("lastAdventure")}`;
var lastPondered = "";
/**
 * Ponders your orb (if it is able to do so safely) and then returns a Map keyed by location consisting of extant predictions
 *
 * @returns A map of all predictions currently active in an adventurer's miniature crystal ball, after visiting the "ponder" URL to refresh them.
 */
function ponder() {
  if (!have$w()) return new Map();
  if (lastPondered !== getLastPondered()) {
    if (canVisitUrl()) {
      logger.debug("Now pondering Crystal Ball.");
      kolmafia.visitUrl("inventory.php?ponder=1", false);
      lastPondered = getLastPondered();
    } else {
      logger.debug("Failed to ponder Crystall Ball.");
    }
  }
  return getPrediction();
}

var helmet$1 = $item`Daylight Shavings Helmet`;
/**
 * Returns whether the player owns an unpackaged Daylight Shavings Helmet, and it's available in either the inventory or other zones as determined by autoSatisfy settings.
 *
 * @returns whether we have the Daylight Shavings Helmet.
 */
function have$v() {
  return have$P(helmet$1);
}
var buffs = $effects`Spectacle Moustache, Toiletbrush Moustache, Barbell Moustache, Grizzly Beard, Surrealist's Moustache, Musician's Musician's Moustache, Gull-Wing Moustache, Space Warlord's Beard, Pointy Wizard Beard, Cowboy Stache, Friendly Chops`;
/**
 * Calculates and returns the cycle of buffs that the hat should cycle through.
 *
 * @param playerclass The class to generate a cycle for
 * @returns An ordered 11-tuple consisting of the cycle for this class. The first element of the array will be the first buff a player should expect to get in a given ascension.
 */
function buffCycle() {
  var playerclass = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : kolmafia.myClass();
  var cycle = tuple(kolmafia.Effect.none, kolmafia.Effect.none, kolmafia.Effect.none, kolmafia.Effect.none, kolmafia.Effect.none, kolmafia.Effect.none, kolmafia.Effect.none, kolmafia.Effect.none, kolmafia.Effect.none, kolmafia.Effect.none, kolmafia.Effect.none);
  if (playerclass.id <= 0) return cycle;
  var id = playerclass.id;
  var seed = id > 6 ? id % 6 + 1 : id;
  for (var i = 1; i < 12; i++) {
    var index = i * seed % 11;
    cycle[i - 1] = buffs[index];
  }
  return cycle;
}
/**
 * Returns the next buff we expect to get from the shaving hat.
 *
 * @returns The next buff we expect to get from the shaving hat.
 */
function nextBuff() {
  var currentBuff = kolmafia.toEffect(get$2("lastBeardBuff").toFixed(0));
  var cycle = buffCycle();
  var index = cycle.indexOf(currentBuff);
  var newIndex = (1 + index) % 11;
  return cycle[newIndex];
}
/**
 * Returns the number of buffs we expect it'll take to get to a given buff. Returns 1 for the next buff, 2 for the one after that, and so on. Returns 11 for the most recent buff.
 *
 * @param buff The shaving buff in question
 * @returns The number of buffs we expect it'll take to get to the inputted buff; null if said buff is not granted by the shaving hat.
 */
function buffsUntil(buff) {
  if (!buffs.includes(buff)) return null;
  var currentIndex = buffs.indexOf(nextBuff()) - 1;
  var newIndex = buffs.indexOf(buff);
  var diff = (newIndex - currentIndex) % 11;
  return diff === 0 ? 11 : diff;
}

var item$6 = kolmafia.Item.get("autumn-aton");
/**
 * Is the autumn-aton currently in your inventory, available to deploy?
 *
 * @returns The whether the autumn-aton is currently available for deployment
 */
function available$1() {
  return kolmafia.availableAmount(item$6) > 0;
}
/**
 * Do you own the autumn-aton?
 *
 * @returns Whether you are an autumn-aton `have`r
 */
function have$u() {
  return get$2("hasAutumnaton") || available$1();
}
/**
 * Internal function used to parse the fallbot's choice adventure to determine which zones are currently available
 *
 * @param html The pagetext of the fallbot's choice adventure
 * @returns The locations currently available to send the fallbot to
 */
function checkLocations(html) {
  return kolmafia.xpath(html, '//select[@name="heythereprogrammer"]//option[position()>1]/@value').map(id => kolmafia.toLocation(Number(id)));
}
function validateLocation(list, location) {
  return list.some(loc => loc.id === location.id);
}
/**
 * Deploy the autumn-aton to a location of your choosing.
 *
 * @param target A location to send the autumn-aton to, or a prioritized list of locations to send it to, or a function to pick which location to send it to.
 * @param upgrade Should we apply any upgrades we see available?
 * @returns Where we ended up sending the autumn-aton; null if we didn't send it off.
 */
function sendTo(target) {
  var upgrade = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : true;
  if (!available$1()) return null;
  var pageHtml = directlyUse(item$6);
  if (upgrade && kolmafia.availableChoiceOptions()[1]) kolmafia.runChoice(1);
  var locationsAvailable = checkLocations(pageHtml);
  var location = target instanceof kolmafia.Location ? target : Array.isArray(target) ? target.find(l => validateLocation(locationsAvailable, l)) : target(locationsAvailable);
  if (!location) return null;
  if (!validateLocation(locationsAvailable, location)) return null;
  if (!kolmafia.handlingChoice()) directlyUse(item$6);
  kolmafia.runChoice(2, `heythereprogrammer=${location.id}`);
  if (kolmafia.handlingChoice()) kolmafia.visitUrl("main.php");
  return location;
}
/**
 * The mafia names for the autumn-aton upgrades
 */
var possibleUpgrades = ["leftarm1", "leftleg1", "rightarm1", "rightleg1", "base_blackhat", "cowcatcher", "periscope", "radardish", "dualexhaust"];
/**
 * @returns An array containing the upgrades that you currently have on your autumn-aton.
 */
function currentUpgrades() {
  return get$2("autumnatonUpgrades").split(",");
}
/**
 * @returns The number of turns remaining in your current autumn-aton quest. This number may be negative for any number of reasons.
 */
function turnsLeft() {
  return get$2("autumnatonQuestTurn") - kolmafia.totalTurnsPlayed();
}
/**
 * @returns The number of leg-upgrades your autumn-aton has installed
 */
function legs() {
  return currentUpgrades().filter(u => u.includes("leg")).length;
}
/**
 * @returns The number of turns we expect your next autumn-aton quest to take.
 */
function turnsForQuest() {
  return 11 * Math.max(1, get$2("_autumnatonQuests") - legs());
}
/**
 * @returns The current visual acuity level of your autumn-aton as determined by the current upgrade-state.
 */
function visualAcuity() {
  var visualUpgrades = ["periscope", "radardish"];
  return 1 + currentUpgrades().filter(u => visualUpgrades.includes(u)).length;
}
/**
 * @returns The number of items from a zone we expect the autumn-aton to steal based on the current upgrade-state. It may not succeed in stealing every item it can.
 */
function zoneItems() {
  return 3 + currentUpgrades().filter(u => u.includes("arm")).length;
}
/**
 * @returns The number of seasonal items we expect the autumn-aton to return with given its current upgrade-state.
 */
function seasonalItems() {
  return currentUpgrades().includes("cowcatcher") ? 2 : 1;
}
var difficulties = ["low", "mid", "high"];
var UNIQUES = {
  outdoor: {
    low: {
      index: 4,
      item: $item`autumn leaf`
    },
    mid: {
      index: 2,
      item: $item`autumn debris shield`
    },
    high: {
      index: 6,
      item: $item`autumn leaf pendant`
    }
  },
  indoor: {
    low: {
      index: 0,
      item: $item`AutumnFest ale`
    },
    mid: {
      index: 3,
      item: $item`autumn-spice donut`
    },
    high: {
      index: 7,
      item: $item`autumn breeze`
    }
  },
  underground: {
    low: {
      index: 1,
      item: $item`autumn sweater-weather sweater`
    },
    mid: {
      index: 5,
      item: $item`autumn dollar`
    },
    high: {
      index: 8,
      item: $item`autumn years wisdom`
    }
  }
};
/**
 * Determines and returns the upgrade and item drop associated with the given location
 *
 * @param location The location to check the expected autumn-aton-unique drops of
 * @returns `null` if the location has no upgrade or drop; otherwise, the upgrade and the autumn-aton item associated with that item
 */
function getUniques(location) {
  var env = location.environment;
  var difficulty = location.difficultyLevel;
  if (arrayContains(env, ["outdoor", "indoor", "underground"]) && arrayContains(difficulty, difficulties)) {
    var _UNIQUES$env$difficul = UNIQUES[env][difficulty],
      index = _UNIQUES$env$difficul.index,
      _item = _UNIQUES$env$difficul.item;
    return {
      upgrade: possibleUpgrades[index],
      item: _item
    };
  }
  return null;
}

var locket = $item`combat lover's locket`;
/**
 * @returns Whether you `have` the Combat Lover's Locket
 */
function have$t() {
  return have$P(locket);
}
/**
 * Filters the set of all unlocked locket monsters to only the ones available to be locketed right now.
 *
 * @returns An array consisting of all Monsters you can fight with your locket right now.
 */
function availableLocketMonsters() {
  if (reminiscesLeft() === 0) return [];
  return Object.entries(kolmafia.getLocketMonsters()).filter(_ref => {
    var _ref2 = _slicedToArray(_ref, 2),
      unused = _ref2[1];
    return unused;
  }).map(_ref3 => {
    var _ref4 = _slicedToArray(_ref3, 1),
      name = _ref4[0];
    return kolmafia.toMonster(name);
  });
}
function parseLocketProperty() {
  return get$2("_locketMonstersFought").split(",").filter(id => id.trim().length > 0);
}
/**
 * Determines how many reminisces remain by parsing the _locketMonstersFought property.
 *
 * @returns The number of reminisces a player has available; 0 if they lack the Locket.
 */
function reminiscesLeft() {
  return have$t() ? clamp(3 - parseLocketProperty().length, 0, 3) : 0;
}
/**
 * Determines which monsters were reminisced today by parsing the _locketMonstersFought property.
 *
 * @returns An array consisting of the Monsters reminisced today.
 */
function monstersReminisced() {
  return parseLocketProperty().map(id => kolmafia.toMonster(id));
}
/**
 * Fight a Monster using the Combat Lover's Locket
 *
 * @param monster The Monster to fight
 * @param combatParams Any parameters you'd like to pass to `runCombat`
 * @returns false if we are unable to reminisce about this monster. Else, returns whether, at the end of all things, we have reminisced about this monster.
 */
function reminisce(monster) {
  if (!have$t() || reminiscesLeft() === 0 || !kolmafia.getLocketMonsters()[monster.name]) {
    return false;
  }
  kolmafia.cliExecute(`reminisce ${monster}`);
  for (var _len = arguments.length, combatParams = new Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
    combatParams[_key - 1] = arguments[_key];
  }
  kolmafia.runCombat.apply(void 0, combatParams);
  return monstersReminisced().includes(monster);
}
/**
 * This function efficiently evaluates all of an adventurer's possibly reminiscable monsters, placing them through a filtering criteria and evaluating them based on a passed function.
 *
 * @param criteria A filtering function for delineating which monsters are "fair game" for the search, such as "is this monster free".
 * @param value A function for deciding which monsters are "better" than others.
 * @returns A singular monster that fulfills the criteria function and maximizes the value function.
 */
function findMonster(criteria) {
  var value = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : () => 1;
  if (!have$t() || reminiscesLeft() === 0) return null;
  var options = availableLocketMonsters().filter(criteria);
  if (!options.length) return null;
  return maxBy(options, value);
}
/**
 * Determine if a monster is currently reminiscible/reminiscable.
 * @param monster The monster in question
 * @returns Whether we can currently reminisce that monster
 */
function canReminisce(monster) {
  return have$t() && reminiscesLeft() > 0 && Boolean(kolmafia.getLocketMonsters()[`${monster}`]);
}

/** designer sweatpants */
var item$5 = $item`designer sweatpants`;
/** designer sweatpants sweat skills */
var skills = {
  SweatFlick: $skill`Sweat Flick`,
  SweatSpray: $skill`Sweat Spray`,
  SweatFlood: $skill`Sweat Flood`,
  SipSomeSweat: $skill`Sip Some Sweat`,
  SweatSip: $skill`Sweat Sip`,
  DrenchYourselfInSweat: $skill`Drench Yourself in Sweat`,
  SweatOutSomeBooze: $skill`Sweat Out Some Booze`,
  MakeSweatAde: $skill`Make Sweat-Ade`
};
/** Map of skill to sweat cost */
var sweatSkillCost = new Map([[skills.SweatFlick, 1], [skills.SweatSpray, 3], [skills.SweatFlood, 5], [skills.SipSomeSweat, 5], [skills.SweatSip, 5], [skills.DrenchYourselfInSweat, 15], [skills.SweatOutSomeBooze, 25], [skills.MakeSweatAde, 50]]);
/**
 * Do you own designer sweatpants
 * @returns Whether you have the designer sweatpants
 */
function have$s() {
  return kolmafia.availableAmount(item$5) > 0;
}
/**
 * Get the current sweat amount
 * @returns The amount of sweat
 */
function sweat() {
  return get$2("sweat");
}
/**
 * Refresh the current sweat amount by visiting the item description
 */
function refresh() {
  examine(item$5);
}
/**
 * Get the sweat cost of a skill
 * @param skill The skill to check
 * @returns The sweat cost, or Infinty for an invalid skill
 */
function sweatCost(skill) {
  return sweatSkillCost.get(skill) ?? Infinity;
}
/**
 * Check if a sweat skill can be cast
 * @param skill The skill to check
 * @returns If this skill can be cast
 */
function canUseSkill(skill) {
  return have$s() && skill.dailylimit !== 0 && sweatCost(skill) <= sweat();
}
/**
 * Get the available number of times a sweat skill can be cast with current sweat
 * @param skill The skill to check
 * @returns The available number of casts
 */
function availableCasts$1(skill) {
  if (!canUseSkill(skill)) return 0;
  return clamp(Math.floor(sweat() / sweatCost(skill)), 0, skill.dailylimit > -1 ? skill.dailylimit : Infinity);
}
/**
 * Get the potential number of times a sweat skill can be cast, with maximum sweat
 * @param skill The skill to check
 * @returns The potential number of casts
 */
function potentialCasts(skill) {
  if (!have$s() || sweatCost(skill) === 0) return 0;
  return clamp(Math.floor(100 / sweatCost(skill)), 0, skill.dailylimit > -1 ? skill.dailylimit : Infinity);
}
/**
 * Cast a sweat skill, on failure refresh sweat amount
 * @param skill The skill to cast
 * @returns If this skill was cast successfully
 */
function useSkill(skill) {
  if (!canUseSkill(skill)) return false;
  var sweatAmt = sweat();
  var result = kolmafia.useSkill(1, skill) && sweatAmt !== sweat();
  if (!result) {
    refresh();
  }
  return result;
}

var cleaver = kolmafia.toItem("June cleaver");
/**
 * Determines whether you currently `have` the June cleaver
 *
 * @returns Whether you currently `have` the June cleaver
 */
function have$r() {
  return kolmafia.availableAmount(cleaver) > 0;
}
/**
 * Determines the number of cleaver-combats it takes to get a particular encounter number.
 *
 * @param encounters The ordinal value of the June cleaver encounter you're asking about
 * @returns The number of cleaver-combats it takes to get a particular encounter number--this is agnostic of your current fights.
 */
function getInterval() {
  var encounters = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : get$2("_juneCleaverEncounters");
  return [1, 6, 10, 12, 15, 20][encounters] ?? 30;
}
/**
 * Determines the number of times today you can skip a june cleaver choice
 *
 * @returns The number of additional times you can select option 4 in a cleaver choice today.
 */
function skipsRemaining() {
  return 5 - get$2("_juneCleaverSkips");
}
var choices = [1467, 1468, 1469, 1470, 1471, 1472, 1473, 1474, 1475];
/**
 * Returns the current June cleaver queue; you are not currently able to encounter any June cleaver choice adventure in this list
 *
 * @returns An array consisting of the cleaver choice adventures currently in the queue.
 */
function queue() {
  return get$2("juneCleaverQueue").split(",").filter(x => x.trim().length > 0).map(x => parseInt(x));
}
/**
 * Determines which choices are currently eligible to be encountered with your June cleaver
 *
 * @returns An array consisting of the cleaver choice adventures not currently in the queue.
 */
function choicesAvailable() {
  var currentQueue = queue();
  return choices.filter(choice => !currentQueue.includes(choice));
}

var item$4 = $item`model train set`;
/**
 * Determines whether the Model Train Set is your current workshed
 *
 * @returns Whether the Model Train Set is your current workshed
 */
function installed$1() {
  return kolmafia.getWorkshed() === item$4;
}
var Station;
(function (Station) {
  /**
   * Unknown station
   */
  Station["UNKNOWN"] = "";
  /**
   * Empty station
   */
  Station["EMPTY"] = "empty";
  /**
   * Gain 800 meat
   */
  Station["GAIN_MEAT"] = "meat_mine";
  /**
   * Effect: Regenerate MP
   */
  Station["TOWER_FIZZY"] = "tower_fizzy";
  /**
   * Gain mus, mys, mox stats
   */
  Station["VIEWING_PLATFORM"] = "viewing_platform";
  /**
   * Effect: Hot resist, cold damage
   */
  Station["TOWER_FROZEN"] = "tower_frozen";
  /**
   * Effect: Stench resist, spooky damage
   */
  Station["SPOOKY_GRAVEYARD"] = "spooky_graveyard";
  /**
   * Get smut bridge part, or stats
   */
  Station["LOGGING_MILL"] = "logging_mill";
  /**
   * Get some candy
   */
  Station["CANDY_FACTORY"] = "candy_factory";
  /**
   * Double strength of next station
   */
  Station["COAL_HOPPER"] = "coal_hopper";
  /**
   * Effect: Cold resist, stench damage
   */
  Station["TOWER_SEWAGE"] = "tower_sewage";
  /**
   * Effect: Spooky resist, sleaze damage
   */
  Station["OIL_REFINERY"] = "oil_refinery";
  /**
   * Effect: Sleaze resist, hot damage
   */
  Station["OIL_BRIDGE"] = "oil_bridge";
  /**
   * Effect: Increased Monster Level
   */
  Station["WATER_BRIDGE"] = "water_bridge";
  /**
   * Get moxie stats
   */
  Station["GROIN_SILO"] = "groin_silo";
  /**
   * Get random booze
   */
  Station["GRAIN_SILO"] = "grain_silo";
  /**
   * Get mys stats
   */
  Station["BRAIN_SILO"] = "brain_silo";
  /**
   * Get muscle stats
   */
  Station["BRAWN_SILO"] = "brawn_silo";
  /**
   * Effect: 50% food drop
   */
  Station["PRAWN_SILO"] = "prawn_silo";
  /**
   * Dupe last food dropped, or gain random food
   */
  Station["TRACKSIDE_DINER"] = "trackside_diner";
  /**
   * Drop random ore, or trapper ore if known
   */
  Station["ORE_HOPPER"] = "ore_hopper";
})(Station || (Station = {}));
new Map([[Station.TOWER_FIZZY, kolmafia.Effect.get("Carbonated")], [Station.TOWER_FROZEN, kolmafia.Effect.get("Frozen")], [Station.SPOOKY_GRAVEYARD, kolmafia.Effect.get("Shivering Spine")], [Station.TOWER_SEWAGE, kolmafia.Effect.get("Hot Soupy Garbage")], [Station.OIL_BRIDGE, kolmafia.Effect.get("Burningly Oiled")], [Station.OIL_REFINERY, kolmafia.Effect.get("Spookily Greasy")], [Station.WATER_BRIDGE, kolmafia.Effect.get("Troubled Waters")], [Station.PRAWN_SILO, kolmafia.Effect.get("Craving Prawns")]]);
new Map([[Station.TOWER_FIZZY, kolmafia.Effect.get("Double Carbonated")], [Station.TOWER_FROZEN, kolmafia.Effect.get("Double Frozen")], [Station.SPOOKY_GRAVEYARD, kolmafia.Effect.get("Doubly Shivering Spine")], [Station.TOWER_SEWAGE, kolmafia.Effect.get("Double Hot Soupy Garbage")], [Station.OIL_BRIDGE, kolmafia.Effect.get("Doubly Burningly Oiled")], [Station.OIL_REFINERY, kolmafia.Effect.get("Doubly Spookily Greasy")], [Station.WATER_BRIDGE, kolmafia.Effect.get("Doubly Troubled Waters")], [Station.PRAWN_SILO, kolmafia.Effect.get("Doubly Craving Prawns")]]);
/**
 * Determines the current configuration of train stations
 *
 * @returns An 8-tuple consisting of the stations currently installed in your Model Train Set; this functions even if the Model Train Set isn't your active workshed
 */
function cycle() {
  return get$2("trainsetConfiguration").split(",");
}
/**
 * Determines how many turns until you can next configure the Model Train Set
 *
 * @returns How many turns until you can next configure the Model Train Set
 */
function nextConfigurable() {
  return clamp(get$2("lastTrainsetConfiguration") + 40 - get$2("trainsetPosition"), 0, 40);
}
/**
 * Determines whether you can currently configure your Model Train Set
 *
 * @returns Whether you can currently configure your Model Train Set
 */
function canConfigure() {
  return installed$1() && !nextConfigurable();
}
var pieces = [Station.EMPTY, Station.GAIN_MEAT, Station.TOWER_FIZZY, Station.VIEWING_PLATFORM, Station.TOWER_FROZEN, Station.SPOOKY_GRAVEYARD, Station.LOGGING_MILL, Station.CANDY_FACTORY, Station.COAL_HOPPER, Station.TOWER_SEWAGE, Station.UNKNOWN, Station.OIL_REFINERY, Station.OIL_BRIDGE, Station.WATER_BRIDGE, Station.GROIN_SILO, Station.GRAIN_SILO, Station.BRAIN_SILO, Station.BRAWN_SILO, Station.PRAWN_SILO, Station.TRACKSIDE_DINER, Station.ORE_HOPPER];
/**
 * Converts a given station to the integer value KoL associates with them
 *
 * @param station The station in question
 * @returns The integer value KoL assigns the train station in question
 */
function stationToInt(station) {
  return pieces.indexOf(station);
}
/**
 * Sets your model train station to the given configuration, if able
 *
 * @param configuration The cycle to try to set your model train station to
 * @returns Whether your model train station matches the given configuration
 */
function setConfiguration(configuration) {
  if (!canConfigure()) return false;
  kolmafia.visitUrl("campground.php?action=workshed");
  kolmafia.runChoice(1, `forceoption=0${configuration.map((station, index) => `&slot[${index}]=${stationToInt(station)}`).join("")}`);
  kolmafia.visitUrl("main.php");
  var currentConfiguration = cycle();
  return configuration.every((station, index) => station === currentConfiguration[index]);
}
/**
 * Determines the next station you expect to encounter when the Model Train Station is active
 *
 * @returns The next station you expect to encounter when the Model Train Station is active
 */
function next() {
  return cycle()[get$2("trainsetPosition") % 8];
}

/**
 * Do you own a still-suit?
 * @returns Whether you have the tiny stillsuit
 */
function have$q() {
  return have$P($item`tiny stillsuit`);
}
/**
 * @returns the expected adventures from drinking stillsuit distillate
 */
function distillateAdventures() {
  if (!have$q()) return 0;
  var sweat = get$2("familiarSweat");
  return Math.round(sweat ** 0.4);
}

/**
 * @returns Whether you `have` the august scepter
 */
function have$p() {
  return have$P($item`august scepter`);
}
Object.freeze($skills`Aug. 1st: Mountain Climbing Day!, Aug. 2nd: Find an Eleven-Leaf Clover Day, Aug. 3rd: Watermelon Day!, Aug. 4th: Water Balloon Day!, Aug. 5th: Oyster Day!, Aug. 6th: Fresh Breath Day!, Aug. 7th: Lighthouse Day!, Aug. 8th: Cat Day!, Aug. 9th: Hand Holding Day!, Aug. 10th: World Lion Day!, Aug. 11th: Presidential Joke Day!, Aug. 12th: Elephant Day!, Aug. 13th: Left/Off Hander's Day!, Aug. 14th: Financial Awareness  Day!, Aug. 15th: Relaxation Day!, Aug. 16th: Roller Coaster Day!, Aug. 17th: Thriftshop Day!, Aug. 18th: Serendipity Day!, Aug. 19th: Honey Bee Awareness Day!, Aug. 20th: Mosquito Day!, Aug. 21st: Spumoni Day!, Aug. 22nd: Tooth Fairy Day!, Aug. 23rd: Ride the Wind Day!, Aug. 24th: Waffle Day!, Aug. 25th: Banana Split Day!, Aug. 26th: Toilet Paper Day!, Aug. 27th: Just Because Day!, Aug. 28th: Race Your Mouse Day!, Aug. 29th: More Herbs\, Less Salt  Day!, Aug. 30th: Beach Day!, Aug. 31st: Cabernet Sauvignon  Day!`);
/**
 * @returns Today's august scepter skill
 */
function todaysSkill() {
  return kolmafia.toSkill((gameDay().getDate() + 7451).toFixed(0));
}
/**
 * @returns whether you have cast Today's august scepter skill
 */
function getTodayCast() {
  return get$2("_augTodayCast");
}

var item$3 = $item`A Guide to Burning Leaves`;
var burnFor = new Map([[$monster`flaming leaflet`, 11], [$item`autumnic bomb`, 37], [$item`impromptu torch`, 42], [$item`flaming fig leaf`, 43], [$item`smoldering drape`, 44], [$item`distilled resin`, 50], [$item`autumnal aegis`, 66], [$item`lit leaf lasso`, 69], [$item`forest canopy bed`, 74], [$item`autumnic balm`, 99], [$monster`flaming monstera`, 111], [$item`day shortener`, 222], [$monster`leaviathan`, 666], [$item`coping juice`, 1111], [$item`smoldering leafcutter ant egg`, 6666], [$item`super-heated leaf`, 11111]]);
/**
 * @returns Whether or not we currently `have` the GuidetoBurningLeaves
 */
function have$o() {
  return haveInCampground(item$3);
}
/**
 * @returns The number of leaves we have remaining
 */
function numberOfLeaves() {
  return kolmafia.itemAmount($item`inflammable leaf`);
}
/**
 * @returns Whether or not we can do the requested burn
 * @param leaves determines the number of leaves to burn
 */
function burnSpecialLeaves(leaves) {
  var lea = burnFor.get(leaves);
  if (lea === undefined || lea > numberOfLeaves()) {
    return false;
  }
  return kolmafia.cliExecute(`leaves ${leaves}`);
}

var cincho = $item`Cincho de Mayo`;
/**
 * @returns Whether you `have` the Cincho de Mayo
 */
function have$n() {
  return have$P(cincho);
}
/**
 * @returns Your current cinch
 */
function currentCinch() {
  return have$n() ? clamp(100 - get$2("_cinchUsed"), 0, 100) : 0;
}
/**
 * @param currentRests The rest number to evaluate
 * @returns The amount of cinch restored by the given rest
 */
function cinchRestoredBy() {
  var currentRests = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : get$2("_cinchoRests");
  return have$n() ? clamp(50 - currentRests * 5, 5, 30) : 0;
}
/**
 * @returns Your current cinch, plus the total amount if cinch that can be generated through free rests
 */
function totalAvailableCinch() {
  var remainingRests = Math.max(0, kolmafia.totalFreeRests() - get$2("timesRested"));
  return have$n() ? currentCinch() + sum(new Array(remainingRests).fill(null).map((_, i) => i + get$2("_cinchoRests")), restNumber => cinchRestoredBy(restNumber)) : 0;
}
({
  SaltAndLime: $skill`Cincho: Dispense Salt and Lime`,
  PartySoundtrack: $skill`Cincho: Party Soundtrack`,
  FiestaExit: $skill`Cincho: Fiesta Exit`,
  ProjectilePiñata: $skill`Cincho: Projectile Piñata`,
  PartyFoul: $skill`Cincho: Party Foul`,
  ConfettiExtrava: $skill`Cincho: Confetti Extravaganza`
});

var item$2 = $item`closed-circuit pay phone`;
/**
 * @returns Whether we currently have the closed-circuit pay phone
 */
function have$m() {
  return have$P(item$2);
}
/**
 * @returns Rufus's currently expected quest target, if he has one
 */
function rufusTarget() {
  var target = get$2("rufusQuestTarget");
  switch (get$2("rufusQuestType")) {
    case "entity":
      return kolmafia.Monster.get(target);
    case "artifact":
    case "items":
      return kolmafia.Item.get(target);
    default:
      return null;
  }
}
var INGRESS_RIFTS = {
  desertbeach: $location`Shadow Rift (Desert Beach)`,
  forestvillage: $location`Shadow Rift (Forest Village)`,
  mclargehuge: $location`Shadow Rift (Mt. McLargeHuge)`,
  beanstalk: $location`Shadow Rift (Somewhere Over the Beanstalk)`,
  manor3: $location`Shadow Rift (Spookyraven Manor Third Floor)`,
  "8bit": $location`Shadow Rift (The 8-Bit Realm)`,
  pyramid: $location`Shadow Rift (The Ancient Buried Pyramid)`,
  giantcastle: $location`Shadow Rift (The Castle in the Clouds in the Sky)`,
  woods: $location`Shadow Rift (The Distant Woods)`,
  hiddencity: $location`Shadow Rift (The Hidden City)`,
  cemetery: $location`Shadow Rift (The Misspelled Cemetary)`,
  plains: $location`Shadow Rift (The Nearby Plains)`,
  town_right: $location`Shadow Rift (The Right Side of the Tracks)`
};
var RIFTS = Array.from(Object.values(INGRESS_RIFTS));
/**
 * Choose a shadow rift to suit your needs
 *
 * @param options An object of various requirements you might have for the rift you want
 * @param options.canAdventure Set to `true` if you want to only select a rift that you can currently access
 * @param options.monsters Set to an array of Shadow Monsters you want to be available in the rift
 * @param options.drops Set to an array of item drops you want to come from monsters in the rift
 * @param options.otherFilter Set an optional additional filtering function not covered by the above logic
 * @param options.sortBy Set a function used to compare valid rifts between eachother; bigger numbers are better
 * @returns A rift that meets the criteria you specify, if one exists
 */
function chooseRift(options) {
  var filterFunction = l => {
    var _options$monsters, _options$drops, _options$otherFilter;
    var monsters = kolmafia.getMonsters(l);
    if (options.canAdventure && !kolmafia.canAdventure(l)) return false;
    if ((_options$monsters = options.monsters) !== null && _options$monsters !== void 0 && _options$monsters.some(m => !monsters.includes(m))) return false;
    if ((_options$drops = options.drops) !== null && _options$drops !== void 0 && _options$drops.every(i => !monsters.some(m => i.name in kolmafia.itemDrops(m)))) return false;
    return ((_options$otherFilter = options.otherFilter) === null || _options$otherFilter === void 0 ? void 0 : _options$otherFilter.call(options, l)) ?? true;
  };
  var validRifts = [].concat(RIFTS).filter(filterFunction);
  if (!validRifts.length) return null;
  return options.sortBy ? maxBy(validRifts, options.sortBy) : validRifts[0];
}
/**
 * Choose a quest based on the options available to us
 *
 * @param chooser A function that maps quest-options to a choice
 * @returns Whether we successfully accepted a quest
 */
function chooseQuest(chooser) {
  if (get$2("questRufus") !== "unstarted") return false;
  if (!have$m()) return false;
  withChoice(1497, "", () => {
    directlyUse(item$2);
    kolmafia.runChoice(chooser({
      artifact: get$2("rufusDesiredArtifact") ?? $item.none,
      entity: get$2("rufusDesiredEntity") ?? $monster.none,
      items: get$2("rufusDesiredItems") ?? $item.none
    }));
  });
  return get$2("questRufus") !== "unstarted";
}
/**
 * Submit your Rufus quest
 *
 * @returns Whether we successfully submitted your Rufus quest
 */
function submitQuest() {
  if (questStep$1("questRufus") === 1) {
    withChoice(1498, 1, () => kolmafia.use(item$2));
    return questStep$1("questRufus") === -1;
  }
  return false;
}

var item$1 = $item`cursed monkey's paw`;
/**
 * @returns Whether or not we currently `have` the cursed monkey's paw
 */
function have$l() {
  return have$P(item$1);
}
/**
 * @returns The number of monkey paw wishes we have remaining
 */
function wishes() {
  return clamp(5 - get$2("_monkeyPawWishesUsed"), 0, 5);
}
/**
 * @param filters An optional object optionally consisting of filtering functions to shrink down the pool of wishable items
 * @param filters.location A filtering function to remove locations from the pool of wishable targets.
 * @param filters.monster A filtering function to remove monsters from the pool of wishable targets.
 * @param filters.drop A filtering function to remove item drops from the pool of wishable targets.
 * @returns A set of all items we expect to be able to wish; this doesn't actually constitute all items
 */
function wishableItems() {
  var filters = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : {};
  return new Set(kolmafia.Location.all().filter(l => {
    var _filters$location;
    return kolmafia.canAdventure(l) && (((_filters$location = filters.location) === null || _filters$location === void 0 ? void 0 : _filters$location.call(filters, l)) ?? true);
  }).flatMap(l => kolmafia.getMonsters(l).filter(m => {
    var _filters$monster;
    return m.copyable && (((_filters$monster = filters.monster) === null || _filters$monster === void 0 ? void 0 : _filters$monster.call(filters, m)) ?? true);
  }).flatMap(m => kolmafia.itemDropsArray(m).filter(_ref => {
    var _filters$drop;
    var type = _ref.type,
      rate = _ref.rate,
      drop = _ref.drop;
    return !drop.quest && (type !== "c" || rate >= 1) && (
    // Remove random roll drops
    ((_filters$drop = filters.drop) === null || _filters$drop === void 0 ? void 0 : _filters$drop.call(filters, {
      type,
      rate,
      drop
    })) ?? true);
  }).map(_ref2 => {
    var drop = _ref2.drop;
    return drop;
  }))));
}
var INVALID_CHARACTERS = /[^a-z\d -]/g;
var _unwishableEffects;
function unwishableEffects() {
  // This is the set of all names of genie-wishable effects, split into the maximal substrings we can actually submit
  var names = kolmafia.Effect.all().filter(e => !e.attributes.includes("nohookah")).map(e => {
    var name = e.name.toLowerCase();
    return {
      name,
      splitName: name.split(INVALID_CHARACTERS)
    };
  });
  return names.filter(_ref3 => {
    var name = _ref3.name,
      splitName = _ref3.splitName;
    return (
      // Any effect that doesn't contain an INVALID_CHARACTER is fine
      splitName.length > 1 &&
      // To be unwishable, there can't be any substrings that uniquely match a genie-wishable effect
      splitName.every(s =>
      // So we check every maximal substring against every one of our genie-wishable effects, excluding the effect we're currently looking at
      // if one of the substrings matches a substring associated with another effect, we're screwed.
      names.some(_ref4 => {
        var n = _ref4.name;
        return n !== name && n.includes(s);
      }))
    );
  }).map(_ref5 => {
    var name = _ref5.name;
    return kolmafia.toEffect(name);
  });
}
/**
 * @returns An Array consisting of all genie-wishable Effects that are not Monkey-wishable
 */
function getUnwishableEffects() {
  return _unwishableEffects ?? (_unwishableEffects = unwishableEffects());
}
/**
 * Decides if we expect that a given Item or Effect can be wished for.
 * May be slow for Effects;
 *
 * @param wish The Item or Effect in question
 * @returns Whether we expect it can be wished for
 */
function isWishable(wish) {
  if (wish instanceof kolmafia.Item) {
    return wishableItems().has(wish);
  } else {
    if (wish.attributes.includes("nohookah")) return false;
    if (!wish.name.match(/[.,']/)) return true;
    return !getUnwishableEffects().includes(wish);
  }
}

var helmet = $item`Apriling band helmet`;
/**
 * @returns whether you `have` the Apriling band helmet
 */
function have$k() {
  return have$P(helmet);
}
var MARCHING_SONGS = Object.freeze(["Apriling Band Patrol Beat", "Apriling Band Battle Cadence", "Apriling Band Celebration Bop"]);
Object.freeze(MARCHING_SONGS.map(song => kolmafia.toEffect(song)));
var INSTRUMENTS = Object.freeze(["Apriling band saxophone", "Apriling band quad tom", "Apriling band tuba", "Apriling band staff", "Apriling band piccolo"]);
Object.freeze(INSTRUMENTS.map(instrument => kolmafia.toItem(instrument)));
var visitConduct = () => kolmafia.visitUrl("inventory.php?pwd&action=apriling");
/**
 * @returns Whether we can currently join a new section of our Apriling band
 */
function canJoinSection() {
  return have$k() && get$2("_aprilBandInstruments") < 2;
}
function makeConductFunction(mafiaClass, canDo, set, offset) {
  return input => {
    if (!canDo()) return false;
    var _ref = typeof input === "string" ? [input, mafiaClass.get(input)] : [input.name, input],
      _ref2 = _slicedToArray(_ref, 2),
      name = _ref2[0],
      instance = _ref2[1];
    if (have$P(instance)) return true;
    var key = set.indexOf(name);
    if (key === -1) return false;
    visitConduct();
    kolmafia.runChoice(key + offset);
    kolmafia.runChoice(9);
    return have$P(instance);
  };
}
/**
 * Joins the given section of your Apriling band, returning whether you successfully obtained its instrument
 *
 * @param section The section of your band to join--either the instrument's name as a string, or the item itself.
 *
 * @returns Whether we have the item, at the end of all things
 */
var joinSection = makeConductFunction(kolmafia.Item, canJoinSection, INSTRUMENTS, 4);
/**
 * Plays a given Apriling band instrument
 *
 * @param instrument The instrument to play
 * @param acquire Whether or not we should obtain the instrument if we don't currently have it
 * @returns Whether we successfully played our instrument
 */
function play(instrument) {
  var acquire = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : false;
  var item = instrument instanceof kolmafia.Item ? instrument : kolmafia.Item.get(instrument);
  if (!canPlay(instrument, acquire)) return false;
  if (acquire && !have$P(item)) joinSection(item);
  var currentUsesRemaining = item.dailyusesleft;
  kolmafia.visitUrl(`inventory.php?pwd=${kolmafia.myHash()}&iid=${item.id}&action=aprilplay`, false);
  return item.dailyusesleft !== currentUsesRemaining;
}
/**
 * Determine whether you can play an instrument
 * @param instrument The instrument you want to play
 * @param acquire Whether you're willing to obtain an instrument you don't already have
 * @returns Whether you can play that instrument
 */
function canPlay(instrument) {
  var acquire = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : false;
  if (!have$k()) return false;
  var item = instrument instanceof kolmafia.Item ? instrument : kolmafia.Item.get(instrument);
  if (!have$P(item) && (!acquire || !canJoinSection())) return false;
  if (item.dailyusesleft <= 0) return false;
  return true;
}

var familiar = $familiar`Chest Mimic`;
/**
 * @returns Whether you `have` the Chest Mimic familiar.
 */
function have$j() {
  return have$P(familiar);
}
var withBank = action => {
  var initial = kolmafia.myFamiliar();
  kolmafia.useFamiliar(familiar);
  try {
    var page = kolmafia.visitUrl("place.php?whichplace=town_right&action=townright_dna", false);
    return action(page);
  } finally {
    kolmafia.visitUrl("main.php");
    kolmafia.useFamiliar(initial);
  }
};
var canDonate = () => have$P($item`mimic egg`) && get$2("_mimicEggsDonated") < 3;
var canReceive = () => familiar.experience >= 100 && get$2("_mimicEggsObtained") < 11;
var makeXpath = (selectNumber, disabled) => `(//select[@name="mid"])[${selectNumber}]/option${disabled ? "[@disabled]" : ""}/@value`;
function getMonsters(selectNumber, page) {
  var total = kolmafia.xpath(page, makeXpath(selectNumber, false));
  var disabled = new Set(kolmafia.xpath(page, makeXpath(selectNumber, true)));
  return total.filter(m => m != "" && !disabled.has(m)).map(id => kolmafia.toMonster(id));
}
/**
 * @returns List of monsters available to receive as an egg at this time
 */
function getReceivableMonsters() {
  if (!canReceive()) return [];
  return withBank(page => getMonsters(1, page));
}
/**
 * Donate an egg to the DNA bank
 *
 * @param monster The monster whose egg you want to donate
 * @returns Whether we succeeded in our endeavor
 */
function donate(monster) {
  if (!canDonate()) return false;
  var selectNumber = canReceive() ? 2 : 1;
  return withBank(page => {
    var available = getMonsters(selectNumber, page);
    if (!available.includes(monster)) return false;
    return kolmafia.runChoice(1, `mid=${monster.id}`).includes("You donate your egg to science.");
  });
}
/**
 * Receive an egg from the DNA bank
 *
 * @param monster The monster whose egg you want to receive
 * @returns Whether we succeeded in our endeavor
 */
function receive(monster) {
  if (!canReceive()) return false;
  return withBank(page => {
    var available = getMonsters(1, page);
    if (!available.includes(monster)) return false;
    return kolmafia.runChoice(2, `mid=${monster.id}`).includes("Your mimic pops into a backroom and returns a few moments later with a fresh mimic egg!");
  });
}
/**
 * Differentiate a Mimic egg into a monster, and fight it!
 *
 * @param monster The monster to differentiate your egg into
 * @param combatParams Any parameters you'd like to pass to `runCombat`
 * @returns Whether we successfully differentiated our egg
 */
function differentiate(monster) {
  var page = directlyUse($item`mimic egg`);
  var monsters = getMonsters(1, page);
  if (!monsters.includes(monster)) {
    kolmafia.visitUrl("main.php");
    return false;
  }
  kolmafia.runChoice(1, `mid=${monster.id}`);
  for (var _len = arguments.length, combatParams = new Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
    combatParams[_key - 1] = arguments[_key];
  }
  kolmafia.runCombat.apply(void 0, combatParams);
  return true;
}
/**
 * @returns A Map containing all monsters available in your mimic eggs and their associated quantities
 */
function eggMonsters() {
  return new Map(multiSplit("mimicEggMonsters", ",", ":", [kolmafia.toMonster, Number]));
}
/**
 * Check how many of a monster is available to differentiate into
 *
 * @param monster The monster to differentiate your egg into
 * @returns How many of a Monster we can differentiate
 */
function differentiableQuantity(monster) {
  if (!have$P($item`mimic egg`)) return 0;
  return eggMonsters().get(monster) ?? 0;
}

var RINGS = Object.freeze([["yam1", "sword", "eye", "chair", "fur", "vessel"], ["lightning", "bottle", "meat", "wood", "yam2"], ["yam3", "wall", "cheese", "eyepatch"], ["explosion", "clock", "yam4"]]);
/**
 * @returns Whether you `have` the Mayam calendar
 */
function have$i() {
  return have$P($item`Mayam Calendar`);
}
/**
 * @returns The Mayam calendar symbols you've used so far today.
 */
function symbolsUsed() {
  return get$2("_mayamSymbolsUsed").split(",");
}
/**
 * Determine whether certain Mayam symbols are available for use today
 *
 * @param {...Glyph[]} symbols The symbols to check
 * @returns Whether or not the given symbols are all available for use
 */
function available() {
  for (var _len = arguments.length, symbols = new Array(_len), _key = 0; _key < _len; _key++) {
    symbols[_key] = arguments[_key];
  }
  return symbols.every(symbol => !symbolsUsed().includes(symbol));
}
/**
 * @returns The remaining number of uses you have of your Mayam calendar today
 */
function remainingUses() {
  return RINGS[3].filter(symbol => available(symbol)).length;
}
/**
 * Utility function to ensure you're dealing with a `Combination` array.
 * @param combination An array containing a `CombinationString` or a `Combination` array.
 * @returns A `Combination` corresponding to the information passed in.
 */
function toCombination(combination) {
  return combination.length === 1 ? combination[0].split(" ") : combination;
}
/**
 * Utility function to ensure you're dealing with a `CombinationString` string.
 * @param combination An array containing a `CombinationString` or a `Combination` array.
 * @returns A `CombinationString` corresponding to the information passed in.
 */
function toCombinationString(combination) {
  return combination.length === 1 ? combination[0] : combination.join(" ");
}
/**
 * Enter a combination in the Mayam calendar
 * @param combination The combination to submit, either as a single string or as a series of symbols
 * @returns Whether we succeeded in our endeavor
 */
function submit() {
  for (var _len2 = arguments.length, combination = new Array(_len2), _key2 = 0; _key2 < _len2; _key2++) {
    combination[_key2] = arguments[_key2];
  }
  if (!available.apply(void 0, _toConsumableArray(toCombination(combination)))) {
    return false;
  }
  return kolmafia.cliExecute(`mayam rings ${combination.join(" ").replace(/yam\d/g, "yam")}`);
}
var RESONANCES = Object.freeze({
  "eye yam2 eyepatch yam4": $item`Mayam spinach`,
  "vessel yam2 cheese explosion": $item`stuffed yam stinkbomb`,
  "yam1 meat cheese yam4": $item`yam and swiss`,
  "sword yam2 eyepatch explosion": $item`yam cannon`,
  "fur lightning eyepatch yam4": $item`tiny yam cannon`,
  "yam1 lightning yam3 clock": $item`yam battery`,
  "fur yam2 wall yam4": $item`furry yam buckler`,
  "yam1 yam2 yam3 explosion": $item`thanksgiving bomb`,
  "yam1 meat eyepatch yam4": $item`yamtility belt`,
  "chair yam2 yam3 clock": $effect`Caught Yam-Handed`,
  "yam1 yam2 cheese clock": $effect`Memories of Cheesier Age`
});
var RESONANCE_KEYS = Object.keys(RESONANCES);
/**
 * Determine what Item or Effect results from a particular resonance
 *
 * @param combination The combination to check, either as a single string or a series of symbols
 * @returns The Item or Effect of the resonance, if it is indeed a resonance; `null` otherwise
 */
function getResonanceResult() {
  for (var _len3 = arguments.length, combination = new Array(_len3), _key3 = 0; _key3 < _len3; _key3++) {
    combination[_key3] = arguments[_key3];
  }
  var combinationString = combination.length === 1 ? combination[0] : combination.join(" ");
  return combinationString in RESONANCES ? RESONANCES[combinationString] : null;
}

$item`tearaway pants`;
/**
 * Calculate the chance of getting adventures from a fight against plants
 * @param advs The number of adventures to calculate the probability at; defaults to the current value
 * @returns The likelihood of getting an adventure from ripping off your pants against plants
 */
function plantsAdventureChance() {
  var advs = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : get$2("_tearawayPantsAdvs");
  return 1 / (2 + advs);
}

/**
 * @returns Whether you `have` the Bat Wings
 */
function have$h() {
  return have$P($item`bat wings`);
}
/**
 * @returns The number of times you can swoop like a bat today
 */
function swoopsRemaining() {
  return have$h() ? $skill`Swoop like a Bat`.dailylimit : 0;
}
/**
 * Calculates the chance of getting a free fight with the Bat Wings equipped
 * @param flaps The number of times your wings have _already_ granted a free fight for the purposes of this calculation; defaults to its current value
 * @returns The chance of getting a free fight
 */
function flapChance() {
  var flaps = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : get$2("_batWingsFreeFights");
  return flaps < 5 ? 1 / (2 + flaps) : 0;
}

var item = $item`TakerSpace letter of Marque`;
/**
 * Determines whether the TakerSpace letter of Marque is your current workshed
 *
 * @returns Whether the TakerSpace letter of Marque is your current workshed
 */
function installed() {
  return kolmafia.getWorkshed() === item;
}
var RESOURCES = ["Spice", "Rum", "Anchor", "Mast", "Silk", "Gold"];
var RECIPES = new Map([[$item`deft pirate hook`, [0, 0, 1, 1, 0, 1]], [$item`iron tricorn hat`, [0, 0, 2, 1, 0, 0]], [$item`jolly roger flag`, [0, 1, 0, 1, 1, 0]], [$item`sleeping profane parrot`, [15, 3, 0, 0, 2, 1]], [$item`pirrrate's currrse`, [2, 2, 0, 0, 0, 0]], [$item`tankard of spiced rum`, [1, 2, 0, 0, 0, 0]], [$item`tankard of spiced Goldschlepper`, [0, 2, 0, 0, 0, 1]], [$item`packaged luxury garment`, [0, 0, 0, 0, 3, 2]], [$item`harpoon`, [0, 0, 0, 2, 0, 0]], [$item`chili powder cutlass`, [5, 0, 1, 0, 0, 0]], [$item`cursed Aztec tamale`, [2, 0, 0, 0, 0, 0]], [$item`jolly roger tattoo kit`, [0, 6, 1, 1, 0, 6]], [$item`golden pet rock`, [0, 0, 0, 0, 0, 7]], [$item`groggles`, [0, 6, 0, 0, 0, 0]], [$item`pirate dinghy`, [0, 0, 1, 1, 1, 0]], [$item`anchor bomb`, [0, 1, 3, 1, 0, 1]], [$item`silky pirate drawers`, [0, 0, 0, 0, 2, 0]], [$item`spices`, [1, 0, 0, 0, 0, 0]]]);
/**
 * @returns A copy of our map of all recipes
 */
function allRecipes() {
  return new Map(_toConsumableArray(RECIPES.entries()).map(_ref => {
    var _ref2 = _slicedToArray(_ref, 2),
      item = _ref2[0],
      recipe = _ref2[1];
    return [item, _toConsumableArray(recipe)];
  }));
}
var defaultAmount = resource => ["Silk", "Gold"].includes(resource) ? 1 : 3;
/**
 * Collect daily resources from takerspace if you haven't yet today and are able to
 * @returns Whether we successfully collected supplies
 */
function collect() {
  if (!installed() || get$2("_takerSpaceSuppliesDelivered")) return false;
  kolmafia.visitUrl("campground.php?action=workshed", false);
  return get$2("_takerSpaceSuppliesDelivered");
}
/**
 * Determine how much of a resource you will have when the TakerSpace is installed
 * @param resource The resource in question
 * @returns The amount of that resource that you will have when the TakerSpace is installed
 */
function amount(resource) {
  collect();
  return get$2(`takerSpace${resource}`) + (!installed() && !get$2("_workshedItemUsed") ? defaultAmount(resource) : 0);
}
/**
 * Determine the Recipe for a given item
 * @param item The item in question
 * @returns The Recipe for that item, as a length-6 array in the order Spice, Rum, Anchor, Mast, Silk, Gold
 */
function recipeFor(item) {
  var result = RECIPES.get(item);
  return result ? _toConsumableArray(result) : null;
}
/**
 * @returns Your current available resources, as a length-6 array in the order Spice, Rum, Anchor, Mast, Silk, Gold
 */
function currentResources() {
  collect();
  return RESOURCES.map(amount);
}
/**
 * Determine if you have enough resources to make a particular item
 * @param item The item in question
 * @param amount The number of the item to make; defaults to one
 * @returns Whether we have enough resources available to make the amount of the item
 */
function haveEnoughFor(item) {
  var amount = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : 1;
  var recipe = recipeFor(item);
  if (!recipe) return false;
  return currentResources().every((resource, index) => resource >= amount * recipe[index]);
}
/**
 * Determines if you're currently able to make a particular item
 * @param item The item in question
 * @param amount The number of the item to make; defaults to one
 * @returns Whether we currently can make the item--that is, whether the TakerSpace is in your workshed and you have enough resources
 */
function canMake(item) {
  var amount = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : 1;
  return installed() && haveEnoughFor(item, amount);
}
/**
 * Attempts to make a particular item
 * @param item The item in question
 * @param amount The number of the item to make; defaults to one
 * @returns Whether we succeeded in our endeavor
 */
function make(item) {
  var amount = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : 1;
  return canMake(item, amount) && kolmafia.create(item, amount);
}

/**
 * @returns Whether or not you have the crepe paper parachute cape
 */
function have$g() {
  return have$P($item`crepe paper parachute cape`);
}
var visitParachute = () => kolmafia.visitUrl("inventory.php?action=parachute&pwd");
function checkMonsters(html) {
  return kolmafia.xpath(html, "//select[@name='monid']//option[position()>1]/@value").map(id => kolmafia.toMonster(Number(id)));
}
/**
 * @param target Either the monster we want to fight or a function for choosing said monster
 * @param combatParams Any parameters you'd like to pass to `runCombat`
 * @returns whether we successfully parachuted into the target monster
 */
function fight(target) {
  if (!have$g()) return false;
  if (have$P($effect`Everything looks Beige`)) return false;
  var monsters = checkMonsters(visitParachute());
  var monster = undelay(target, monsters);
  if (!monsters.includes(monster)) return false;
  if (!kolmafia.handlingChoice() || kolmafia.lastChoice() !== 1543) visitParachute();
  kolmafia.runChoice(1, `monid=${monster.id}`);
  for (var _len = arguments.length, combatParams = new Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
    combatParams[_key - 1] = arguments[_key];
  }
  kolmafia.runCombat.apply(void 0, combatParams);
  return true;
}

/**
 * @returns Whether you `have` the Toy Cupid's bow
 */
function have$f() {
  return have$P($item`toy Cupid bow`);
}
/**
 * @returns The current familiar your cupid bow is getting the equip of (null if there isn't one)
 */
function currentFamiliar() {
  return get$2("cupidBowLastFamiliar");
}
/**
 * @returns An array of familiars who have received their drops from the cupid bow today
 */
function familiarsToday() {
  return get$2("_cupidBowFamiliars").split(";").map(id => kolmafia.toFamiliar(Number(id)));
}

/**
 * @returns Whether you `have` the Leprecondo
 */
function have$e() {
  return have$P($item`Leprecondo`);
}
var NEEDS = Object.freeze(["exercise", "mental stimulation", "dumb entertainment", "food", "booze", "sleep"]);
var FURNITURE_PIECES = Object.freeze(["empty", "buckets of concrete", "thrift store oil painting", "boxes of old comic books", "second-hand hot plate", "beer cooler", "free mattress", "gigantic chess set", "UltraDance karaoke machine", "cupcake treadmill", "beer pong table", "padded weight bench", "internet-connected laptop", "sous vide laboratory", "programmable blender", "sensory deprivation tank", "fruit-smashing robot", "ManCave™ sports bar set", "couch and flatscreen", "kegerator", "fine upholstered dining set", "whiskeybed", "high-end home workout system", "complete classics library", "ultimate retro game console", "Omnipot", "fully-stocked wet bar", "four-poster bed"]);
var Furniture = Object.freeze({
  "beer cooler": {
    booze: $items`beer bomb, bloody beer, ice-cold fotie, ice-cold Sir Schlitz, ice-cold Willer, overpriced "imported" beer, plain old beer`
  },
  "beer pong table": {
    booze: $items`beer bomb, bloody beer, ice-cold fotie, ice-cold Sir Schlitz, ice-cold Willer, overpriced "imported" beer, plain old beer`,
    exercise: $item`table tennis ball`
  },
  "boxes of old comic books": {
    "dumb entertainment": {
      effect: $effect`Wasting Time`,
      duration: 10
    }
  },
  "buckets of concrete": {
    exercise: {
      effect: $effect`Gym Bros`,
      duration: 10
    }
  },
  "complete classics library": {
    "mental stimulation": $item`leprechaun antidepressant pill`
  },
  "couch and flatscreen": {
    "dumb entertainment": {
      effect: $effect`Your Days Are Numbed`,
      duration: 10
    },
    sleep: {
      effect: $effect`Good Night's Sleep`,
      duration: 10
    }
  },
  "cupcake treadmill": {
    exercise: {
      effect: $effect`Your Days Are Numbed`,
      duration: 10
    },
    food: $item`standard-issue cupcake`
  },
  "fine upholstered dining set": {
    food: $items`boring spaghetti, delicious noodles, goat cheese pizza, sausage pizza, spicy bean burrito, spicy noodles`,
    sleep: {
      effect: $effect`Sur La Table`,
      duration: 10
    }
  },
  empty: {},
  "four-poster bed": {
    sleep: {
      effect: $effect`Spacious Night's Sleep`,
      duration: 10
    }
  },
  "free mattress": {
    sleep: {
      effect: $effect`Good Night's Sleep`,
      duration: 10
    }
  },
  "fruit-smashing robot": {
    "dumb entertainment": {
      effect: $effect`You Might Have Gotten Wet`,
      duration: 10
    },
    food: $items`cherry, cranberries, grapefruit, grapes, kiwi, kumquat, lemon, lime, orange, papaya, raspberry, strawberry, tangerine`
  },
  "fully-stocked wet bar": {
    booze: $items`Divine sidecar, prussian cathouse sidecar, tangarita sidecar`
  },
  "gigantic chess set": {
    exercise: {
      effect: $effect`Work Out Smarter, Not Harder`,
      duration: 10
    },
    "mental stimulation": {
      effect: $effect`Well Stimulated`,
      duration: 10
    }
  },
  "high-end home workout system": {
    exercise: $item`scoop of pre-workout powder`
  },
  "internet-connected laptop": {
    "dumb entertainment": {
      effect: $effect`Wasting Time`,
      duration: 10
    },
    "mental stimulation": $item`crafting plans`
  },
  kegerator: {
    booze: $item`pint of Leprechaun Stout`,
    food: $items`bean burrito, plain pizza, spicy mushroom quesadilla, tofu casserole, Trollhouse cookies`
  },
  "ManCave™ sports bar set": {
    booze: $items`melted Jell-o shot, shot of blackberry schnapps, shot of nepenthe schnapps, shot of peach schnapps, shot of pear schnapps, shot of rotgut`,
    "dumb entertainment": $item`bar dart`
  },
  Omnipot: {
    food: $items`Heck ramen, incredible mini-pizza, small beer brat, tiny burrito, tiny peach pie, savoy truffle, white chocolate chip brownies`
  },
  "padded weight bench": {
    exercise: {
      effect: $effect`Gym Bros`,
      duration: 10
    },
    sleep: {
      effect: $effect`Tired Muscles`,
      duration: 10
    }
  },
  "programmable blender": {
    booze: $items`eggnog, extra-spicy bloody mary, grog, white lightning`,
    "mental stimulation": {
      effect: $effect`Counter Intelligence`,
      duration: 10
    }
  },
  "second-hand hot plate": {
    food: $items`bean burrito, chorizo taco, plain pizza, spicy mushroom quesadilla, tofu casserole, Trollhouse cookies`
  },
  "sensory deprivation tank": {
    "mental stimulation": {
      effect: $effect`Alone with Your Thoughts`,
      duration: 10
    },
    sleep: {
      effect: $effect`Quiet Night's Sleep`,
      duration: 10
    }
  },
  "sous vide laboratory": {
    food: $items`tiny burrito, black forest ham, fishy fish, ham steak, Knoll stir-fry, spooky stir-fry`,
    "mental stimulation": {
      effect: $effect`Counter Intelligence`,
      duration: 10
    }
  },
  "thrift store oil painting": {
    "mental stimulation": {
      effect: $effect`Well Stimulated`,
      duration: 10
    }
  },
  "ultimate retro game console": {
    "dumb entertainment": $item`phosphor traces`
  },
  "UltraDance karaoke machine": {
    "dumb entertainment": {
      effect: $effect`Your Days Are Numbed`,
      duration: 10
    },
    exercise: {
      effect: $effect`Vicarious Sweat`,
      duration: 10
    }
  },
  whiskeybed: {
    booze: $items`Divine sidecar, lumbering jack, old-fashioned, whiskey and cola, whiskey and soda, whiskey bittersweet, whiskey sour`,
    sleep: {
      effect: $effect`Moist Night's Sleep`,
      duration: 10
    }
  }
});
/**
 * @returns How many more times you can rearrange today
 */
function rearrangesRemaining() {
  return clamp(3 - get$2("_leprecondoRearrangements"), 0, 3);
}
/**
 * @returns Your current Leprecondo need
 */
function currentNeed() {
  return get$2("leprecondoCurrentNeed");
}
/**
 * @returns Your leprecondo need order, as an array
 */
function needOrder() {
  return get$2("leprecondoNeedOrder").split(",");
}
/**
 * @returns The number of leprecondo furniture pieces you've discovered today
 */
function furnitureDiscoveredToday() {
  return get$2("_leprecondoFurniture");
}
/**
 * @returns How many turns ago your Leprecondo need last changed; an imperfect system for tracking because the Leprecondo relies on turn-taking combats.
 */
function lastNeedChange() {
  return kolmafia.turnsPlayed() - get$2("leprecondoLastNeedChange");
}
/**
 * Find the need-result mapping for a given furniture
 * @param furniture The furniture in question
 * @returns An object, keyed by need, whose values correspond to what the furniture gives you
 */
function getStats(furniture) {
  return Furniture[furniture];
}
/**
 * @returns the Furniture elements you've discovered
 */
function discoveredFurniture() {
  return get$2("leprecondoDiscovered").split(",").map(id => FURNITURE_PIECES[Number(id)]);
}
/**
 * @returns the Furniture elements you've installed, in descending order of importance (starting from bottom right and ending in top left)
 */
function installedFurniture() {
  return get$2("leprecondoInstalled").split(",").map(id => FURNITURE_PIECES[Number(id)]).reverse();
}
/**
 * @param furniture A spread array of furniture pieces to install, starting from bottom right and ending in top left
 * @returns Whether or not you successfully installed the desired furniture
 */
function setFurniture() {
  if (rearrangesRemaining() < 0) return false;
  for (var _len = arguments.length, furniture = new Array(_len), _key = 0; _key < _len; _key++) {
    furniture[_key] = arguments[_key];
  }
  var nonemptyFurniture = furniture.filter(f => f !== "empty");
  if (nonemptyFurniture.some(piece => !discoveredFurniture().includes(piece))) return false;
  if (new Set(nonemptyFurniture).size !== nonemptyFurniture.length) return false;
  directlyUse($item`Leprecondo`);
  kolmafia.runChoice(1, furniture.map((piece, index) => `r${3 - index}=${FURNITURE_PIECES.indexOf(piece)}`).join("&"));
  return arrayEquals$1(installedFurniture(), furniture);
}
/**
 * @param furniture The set of furniture to examine; defaults to currently installed furniture. Reads furniture from most important (bottom right) to least important (top left)
 * @returns The cumulative bonuses of all currently-installed furniture.
 */
function furnitureBonuses() {
  var furniture = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : installedFurniture();
  return furniture.reduce((acc, piece) => _objectSpread2(_objectSpread2({}, Furniture[piece]), acc), {});
}

var Leprecondo = /*#__PURE__*/Object.freeze({
  __proto__: null,
  FURNITURE_PIECES: FURNITURE_PIECES,
  Furniture: Furniture,
  NEEDS: NEEDS,
  currentNeed: currentNeed,
  discoveredFurniture: discoveredFurniture,
  furnitureBonuses: furnitureBonuses,
  furnitureDiscoveredToday: furnitureDiscoveredToday,
  getStats: getStats,
  have: have$e,
  installedFurniture: installedFurniture,
  lastNeedChange: lastNeedChange,
  needOrder: needOrder,
  rearrangesRemaining: rearrangesRemaining,
  setFurniture: setFurniture
});

var peridot = $item`Peridot of Peril`;
/**
 * @returns Whether you `have` the Peridot of Peril.
 */
function have$d() {
  return have$P(peridot);
}
/**
 * Determine if you've already used your peridot NC in a particular location today.
 * @param location The location in question.
 *
 * @returns Whether or not you've used the peridot in that location yet today.
 */
function periledToday(location) {
  return RegExp(`(?:^|,)${location.id}(?:$|,)`).test(get$2("_perilLocations"));
}
/**
 * Determine if you can get the peridot NC in a particular location.
 * @param location The location in question.
 *
 * @returns Whether or not you can currently get the peridot NC in that location.
 */
function canImperil(location) {
  return have$d() && location.wanderers && location.combatPercent >= 0 && kolmafia.getMonsters(location).length > 0 && !periledToday(location);
}
var toChoiceOption = m => `1&bandersnatch=${m.id}`;
/**
 * Create a choices object keyed by choice id, instead of property name.
 * @param monster The monster in question.
 * @returns An object keyed by choice id, with values of choice options.
 */
function getChoiceObject(monster) {
  return {
    1557: toChoiceOption(monster)
  };
}

var beret = $item`prismatic beret`;
var valueEffect = (effect, duration, valuer) => typeof valuer === "function" ? valuer(effect, duration) : Array.isArray(valuer) ? Number(valuer.includes(effect)) * duration : sum(Object.entries(valuer), _ref => {
  var _ref2 = _slicedToArray(_ref, 2),
    modifier = _ref2[0],
    weight = _ref2[1];
  return weight * kolmafia.numericModifier(effect, modifier);
});
/**
 * @returns Whether or not you have the prismatic beret
 */
function have$c() {
  return have$P(beret);
}
function getEffectivePower(item) {
  var hammerTime = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : have$P($effect`Hammertime`);
  switch (kolmafia.toSlot(item)) {
    case $slot`hat`:
      return kolmafia.getPower(item) * (1 + (have$P($skill`Tao of the Terrapin`) ? 1 : 0));
    case $slot`shirt`:
      return kolmafia.getPower(item);
    case $slot`pants`:
      return kolmafia.getPower(item) * (1 + (have$P($skill`Tao of the Terrapin`) ? 1 : 0) + (hammerTime ? 3 : 0));
    default:
      return 0;
  }
}
function sumEquipmentPower() {
  var overrideEquipment = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : {
    hat: kolmafia.equippedItem($slot`hat`),
    pants: kolmafia.equippedItem($slot`pants`),
    shirt: kolmafia.equippedItem($slot`shirt`),
    hammerTime: have$P($effect`Hammertime`)
  };
  return getEffectivePower(overrideEquipment.hat, overrideEquipment.hammerTime) + getEffectivePower(overrideEquipment.pants, overrideEquipment.hammerTime) + getEffectivePower(overrideEquipment.shirt, overrideEquipment.hammerTime);
}
function getUseableClothes() {
  var buyItems = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : true;
  var availableItems = kolmafia.Item.all().filter(i => kolmafia.canEquip(i) && (have$P(i) || buyItems && kolmafia.npcPrice(i) > 0));
  var useableHats = have$P($familiar`Mad Hatrack`) ? [].concat(_toConsumableArray(availableItems.filter(i => kolmafia.toSlot(i) === $slot`hat`)), [$item.none]) : [beret];
  var useablePants = [].concat(_toConsumableArray(availableItems.filter(i => kolmafia.toSlot(i) === $slot`pants`)), [$item.none]);
  var useableShirts = [].concat(_toConsumableArray(availableItems.filter(i => kolmafia.toSlot(i) === $slot`shirt`)), [$item.none]);
  return {
    useableHats,
    useablePants,
    useableShirts
  };
}
function availablePowersums(_ref3) {
  var _ref3$buyItems = _ref3.buyItems,
    buyItems = _ref3$buyItems === void 0 ? true : _ref3$buyItems,
    _ref3$hammerTime = _ref3.hammerTime,
    hammerTime = _ref3$hammerTime === void 0 ? have$P($effect`Hammertime`) : _ref3$hammerTime;
  var _getUseableClothes = getUseableClothes(buyItems),
    useableHats = _getUseableClothes.useableHats,
    useablePants = _getUseableClothes.useablePants,
    useableShirts = _getUseableClothes.useableShirts;
  var hatPowers = _toConsumableArray(new Set(useableHats.map(i => getEffectivePower(i, hammerTime))));
  var pantPowers = _toConsumableArray(new Set(useablePants.map(i => getEffectivePower(i, hammerTime))));
  var shirtPowers = _toConsumableArray(new Set(useableShirts.map(i => getEffectivePower(i, hammerTime))));
  return _toConsumableArray(new Set(hatPowers.flatMap(hat => pantPowers.flatMap(pant => shirtPowers.flatMap(shirt => hat + pant + shirt)))));
}
function scoreBusk(effects, effectValuer, uselessEffects) {
  var usefulEffects = effects.filter(_ref4 => {
    var _ref5 = _slicedToArray(_ref4, 1),
      effect = _ref5[0];
    return !uselessEffects.has(effect);
  });
  return sum(usefulEffects, _ref6 => {
    var _ref7 = _slicedToArray(_ref6, 2),
      effect = _ref7[0],
      duration = _ref7[1];
    return valueEffect(effect, duration, effectValuer);
  });
}
/**
 * Calculate the optimal power-sum at which to busk, given a weighted set of modifiers.
 * @param effectValuer Either a function that maps effect-duration pairs to values, or an object keyed by numeric modifiers with weights as values, or an array of desired effects
 * @param options An object containing the following optional keys:
 * @param options.uselessEffects An array (defaults to empty) of effects not to consider for the purposes of busk valuation
 * @param options.buyItems Whether or not we should consider purchasing items from NPC stores; defaults to true
 * @param options.hammerTime Whether or not to assume you have Hammertime--defaults to whether you currently have it
 * @param buskUses How many busks should we assume we've cast? Defaults to the current number.
 * @returns The power-sum at which you'll find the optimal busk for this situation.
 */
function findOptimalOutfitPower(effectValuer) {
  var _ref8 = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {},
    _ref8$buyItems = _ref8.buyItems,
    buyItems = _ref8$buyItems === void 0 ? true : _ref8$buyItems,
    _ref8$uselessEffects = _ref8.uselessEffects,
    uselessEffects = _ref8$uselessEffects === void 0 ? [] : _ref8$uselessEffects,
    _ref8$hammerTime = _ref8.hammerTime,
    hammerTime = _ref8$hammerTime === void 0 ? have$P($effect`Hammertime`) : _ref8$hammerTime;
  var buskUses = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : get$2("_beretBuskingUses");
  var uselessEffectSet = new Set(uselessEffects);
  var powersums = availablePowersums({
    buyItems,
    hammerTime
  });
  if (!powersums.length) return 0;
  return maxBy(powersums, power => scoreBusk(Object.entries(kolmafia.beretBuskingEffects(power, buskUses)).map(_ref9 => {
    var _ref0 = _slicedToArray(_ref9, 2),
      effect = _ref0[0],
      duration = _ref0[1];
    return [kolmafia.toEffect(effect), duration];
  }).filter(_ref1 => {
    var _ref10 = _slicedToArray(_ref1, 1),
      e = _ref10[0];
    return e !== $effect.none;
  }), effectValuer, uselessEffectSet));
}
var validateItems = (arr, maxPower) => {
  var map = new Map();
  var _iterator = _createForOfIteratorHelper(arr),
    _step;
  try {
    for (_iterator.s(); !(_step = _iterator.n()).done;) {
      var it = _step.value;
      var power = getEffectivePower(it);
      if (power > maxPower) continue;
      var existing = map.get(power);
      if (!existing || !have$P(existing) && (have$P(it) || kolmafia.npcPrice(it) < kolmafia.npcPrice(existing))) {
        map.set(power, it);
      }
    }
  } catch (err) {
    _iterator.e(err);
  } finally {
    _iterator.f();
  }
  return _toConsumableArray(map.values());
};
var relevantSlots = ["hat", "pants", "shirt"];
var functionalPrice = item => have$P(item) || item === kolmafia.Item.none ? 0 : kolmafia.npcPrice(item);
var outfitPrice = outfit => sum(relevantSlots, slot => functionalPrice(outfit[slot]));
function findOutfit(power, buyItems) {
  var _getUseableClothes2 = getUseableClothes(buyItems),
    useableHats = _getUseableClothes2.useableHats,
    useablePants = _getUseableClothes2.useablePants,
    useableShirts = _getUseableClothes2.useableShirts;
  var hats = validateItems(useableHats, power);
  var pantses = validateItems(useablePants, power);
  var shirts = validateItems(useableShirts, power);
  var outfits = hats.flatMap(hat => shirts.flatMap(shirt => pantses.flatMap(pants => sumEquipmentPower({
    hat,
    pants,
    shirt,
    hammerTime: have$P($effect`Hammertime`)
  }) === power ? {
    hat,
    pants,
    shirt
  } : [])));
  if (!outfits.length) return null;
  var outfit = maxBy(outfits, outfitPrice, true);
  logger.debug(`Chose outfit ${outfit.hat} ${outfit.shirt} ${outfit.pants}`);
  if (outfitPrice(outfit) > kolmafia.myMeat()) return null;
  var _iterator2 = _createForOfIteratorHelper(relevantSlots),
    _step2;
  try {
    for (_iterator2.s(); !(_step2 = _iterator2.n()).done;) {
      var slot = _step2.value;
      var item = outfit[slot];
      if (item === kolmafia.Item.none) continue;
      if (have$P(item)) continue;
      if (!kolmafia.buy(item)) {
        logger.debug(`Failed to purchase ${item}`);
        return null;
      }
    }
  } catch (err) {
    _iterator2.e(err);
  } finally {
    _iterator2.f();
  }
  return outfit;
}
/**
 * Attempt to busk at a particular power
 * @param power The power in question
 * @param buyItems Whether or not we should consider purchasing items from NPC stores; defaults to true
 * @returns If we successfully busked at that power
 */
function buskAt(power) {
  var buyItems = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : true;
  if (!have$c()) return false;
  var initialUses = get$2("_beretBuskingUses");
  if (initialUses >= 5) return false;
  var outfit = findOutfit(power, buyItems);
  if (!outfit) return false;
  var initialEquips = $slots`hat, shirt, pants`.map(slot => kolmafia.equippedItem(slot));
  var initialFamiliar = kolmafia.myFamiliar();
  var initialFamequip = kolmafia.equippedItem($slot`familiar`);
  var hat = outfit.hat,
    pants = outfit.pants,
    shirt = outfit.shirt;
  kolmafia.equip($slot`hat`, hat);
  if (hat !== beret) {
    kolmafia.useFamiliar($familiar`Mad Hatrack`);
    kolmafia.equip($slot`familiar`, beret);
  }
  kolmafia.equip($slot`shirt`, shirt);
  kolmafia.equip($slot`pants`, pants);
  try {
    if (sumEquipmentPower() !== power) {
      return false;
    }
    kolmafia.useSkill($skill`Beret Busking`);
    return initialUses !== get$2("_beretBuskingUses");
  } finally {
    $slots`hat, shirt, pants`.forEach((slot, index) => kolmafia.equip(slot, initialEquips[index]));
    if (initialFamiliar !== $familiar`Mad Hatrack` && kolmafia.myFamiliar() === $familiar`Mad Hatrack`) {
      unequip($slot`familiar`);
    }
    kolmafia.useFamiliar(initialFamiliar);
    kolmafia.equip($slot`familiar`, initialFamequip);
  }
}
/**
 * Calculate the best outfit-power you can achieve for a given busk valuation, and then busks.
 * @param effectValuer Either a function that maps effect-duration pairs to values, or an object keyed by numeric modifiers with weights as values, or an array of desired effects
 * @param options An object containing the following optional keys:
 * @param options.uselessEffects An array (defaults to empty) of effects not to consider for the purposes of busk valuation
 * @param options.buyItems Whether or not we should consider purchasing items from NPC stores; defaults to true
 * @returns Whether we were successful in our endeavor
 */
function buskFor(effectValuer) {
  var _ref11 = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {},
    _ref11$buyItems = _ref11.buyItems,
    buyItems = _ref11$buyItems === void 0 ? true : _ref11$buyItems,
    _ref11$uselessEffects = _ref11.uselessEffects,
    uselessEffects = _ref11$uselessEffects === void 0 ? [] : _ref11$uselessEffects;
  var outfitPower = findOptimalOutfitPower(effectValuer, {
    buyItems,
    uselessEffects
  }, get$2("_beretBuskingUses"));
  return buskAt(outfitPower, buyItems);
}

/**
 * @returns Whether or not you have the blood cubic zirconia.
 */
function have$b() {
  return have$P($item`blood cubic zirconia`);
}
/**
 * @param skill The BCZ skill to check.
 * @param castNumber The number of times you've cast the skill
 * @returns The subtat cost to cast the skill.
 */
function skillCost(skill) {
  var castNumber = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : timesCast(skill);
  if (castNumber <= 11) {
    var cycle = Math.floor(castNumber / 3);
    var position = castNumber % 3;
    return [11, 23, 37][position] * 10 ** cycle;
  } else if (castNumber === 12) {
    return 420_000;
  } else {
    var _cycle = Math.floor((castNumber - 13) / 3);
    var _position = (castNumber + 2) % 3;
    return [11, 23, 37][_position] * 10 ** (_cycle + 5);
  }
}
var COSTS = new Map([[$skill`BCZ: Blood Geyser`, $stat`SubMuscle`], [$skill`BCZ: Refracted Gaze`, $stat`SubMysticality`], [$skill`BCZ: Sweat Bullets`, $stat`SubMoxie`], [$skill`BCZ: Blood Bath`, $stat`SubMuscle`], [$skill`BCZ: Craft a Pheromone Cocktail`, $stat`SubMoxie`], [$skill`BCZ: Create Blood Thinner`, $stat`SubMuscle`], [$skill`BCZ: Dial it up to 11`, $stat`SubMysticality`], [$skill`BCZ: Prepare Spinal Tapas`, $stat`SubMysticality`], [$skill`BCZ: Sweat Equity`, $stat`SubMoxie`]]);
var PREFS = new Map([[$skill`BCZ: Blood Geyser`, "_bczBloodGeyserCasts"], [$skill`BCZ: Refracted Gaze`, "_bczRefractedGazeCasts"], [$skill`BCZ: Sweat Bullets`, "_bczSweatBulletsCasts"], [$skill`BCZ: Blood Bath`, "_bczBloodBathCasts"], [$skill`BCZ: Dial it up to 11`, "_bczDialitupCasts"], [$skill`BCZ: Sweat Equity`, "_bczSweatEquityCasts"], [$skill`BCZ: Create Blood Thinner`, "_bczBloodThinnerCasts"], [$skill`BCZ: Prepare Spinal Tapas`, "_bczSpinalTapasCasts"], [$skill`BCZ: Craft a Pheromone Cocktail`, "_bczPheromoneCocktailCasts"]]);
/**
 * @param skill The BCZ skill to check.
 * @returns The number of casts of the skill already used, parsing the pref.
 */
function timesCast(skill) {
  var pref = PREFS.get(skill);
  if (!pref) return 0;
  return get$2(pref, 0);
}
/**
 * @param skill The BCZ skill to check.
 * @returns The substat used to cast the skill.
 */
function substatUsed(skill) {
  var cost = COSTS.get(skill);
  if (!cost) return null;
  return cost;
}
/**
 * @param skill The BCZ skill to check.
 * @param statFloor Minimum base stat you want to keep.
 * @returns The number of casts you can achieve of the selected skill before hitting the given stat floor.
 */
function availableCasts(skill, statFloor) {
  if (!have$b()) return 0;
  var subStat = substatUsed(skill);
  if (!subStat) return 0;
  // const currentStat = myBasestat(stat);
  var currentSubStat = kolmafia.myBasestat(subStat);
  var subStatFloor = statFloor ** 2;
  var casts = 0;
  var remainingSubStat = currentSubStat;
  for (var i = timesCast(skill); i < 25; i++) {
    // 25 is abritrary
    var nextCost = skillCost(skill, i);
    if (remainingSubStat - nextCost < subStatFloor) break;
    remainingSubStat -= nextCost;
    casts++;
  }
  return casts;
}
/**
 * @param skill The BCZ skill to cast.
 * @param count How many times you want to cast the spell
 * @returns Whether you successfully cast the spell.
 */
function cast(skill, count) {
  if (!have$b() || !COSTS.get(skill) || count <= 0) return false;
  kolmafia.cliExecute("checkpoint");
  try {
    kolmafia.equip($item`blood cubic zirconia`);
    for (var i = 0; i < count; i++) {
      if (!kolmafia.useSkill(skill)) {
        return false;
      }
    }
    return true;
  } finally {
    kolmafia.cliExecute("outfit checkpoint");
  }
}

var BONE_PHYLA = new Map([[$phylum`beast`, 0.3], [$phylum`bug`, 0.05], [$phylum`construct`, 0.1], [$phylum`demon`, 0.4], [$phylum`elf`, 0.5], [$phylum`fish`, 0.15], [$phylum`goblin`, 0.4], [$phylum`hobo`, 0.5], [$phylum`humanoid`, 0.4], [$phylum`orc`, 0.7], [$phylum`penguin`, 0.2], [$phylum`pirate`, 0.7], [$phylum`dude`, 0.5], [$phylum`undead`, 0.3], [$phylum`weird`, 0.2]]);
/**
 * @returns Whether or not you have the Skeleton of Crimbo Past.
 */
function have$a() {
  return have$P($familiar`Skeleton of Crimbo Past`);
}
/**
 * @param target The target you expect to fight; accepts Monster, Location, or map of <Monster, number>
 * @returns The odds of getting a knucklebone drop
 */
function expectedBones(target) {
  if (!have$a()) return 0;
  if (get$2("_knuckleboneDrops") >= 100) return 0;
  if (target instanceof kolmafia.Location) {
    return expectedBones(adventureTargetToWeightedMap(target));
  }
  if (target instanceof Map) {
    return sum(_toConsumableArray(target.entries()), _ref => {
      var _ref2 = _slicedToArray(_ref, 2),
        monster = _ref2[0],
        rate = _ref2[1];
      return rate * expectedBones(monster);
    });
  }
  if (target.attributes.includes("SKELETON")) return 0.9;
  return BONE_PHYLA.get(target.phylum) ?? 0;
}

/**
 * @returns Whether you `have` the Legendary Seal-Clubbing Club
 */
function have$9() {
  return have$P($item`legendary seal-clubbing club`);
}
/**
 * @returns The number of casts of Club 'Em Into Next Week available to you right now.
 */
function clubIntoNextWeekAvailable() {
  return have$9() ? $skill`Club 'Em Into Next Week`.dailylimit : 0;
}
/**
 * @returns The monster you currently have copied with Club 'Em Into Next Week.
 */
function clubIntoNextWeekMonster() {
  return get$2("clubEmNextWeekMonster");
}
/**
 * @returns The number of turns remaining until your current Club 'Em Into Next Week fight; infinity if the counter isn't currently set.
 */
function turnsUntilNextWeekFight() {
  return get("Club 'Em Into Next Week Monster");
}

/**
 * @returns Whether you `have` the Cup of 13s.
 */
function have$8() {
  return have$P($item`Cup of 13s`);
}
Object.freeze([].concat(_toConsumableArray($stats`Muscle, Mysticality, Moxie`), _toConsumableArray($effects`Runneth Over, Runneth On Empty, Runneth Wild, Runneth With The Pack, Runneth a Tight Ship, Runneth a Fever, Runneth Cold, Runneth On Fumes, Runneth For Thy Life, Runneth Into Thine Ex`)));

var BASIC_FRUIT = $items`orange, grapefruit, grapes, lemon, lime, papaya, cranberries, strawberry, cherry, kumquat, tangerine, raspberry, kiwi, blackberry, banana, cactus fruit, plum, pear, peach`;
var ADVANCED_FRUIT = $items`classic banana, antique watermelon, quince`;
function getSeed(classId, pathId, daycount) {
  return classId ** 3 + 84 * pathId + 123 * (daycount - 1) + 381;
}
var fixedDropTurns = Array(11).fill(null).map((_, i) => 1 + (i + 1) * i / 2);
var _pityCount = /*#__PURE__*/new WeakMap();
var _pityThreshold = /*#__PURE__*/new WeakMap();
var _fight = /*#__PURE__*/new WeakMap();
var _currentRng = /*#__PURE__*/new WeakMap();
var _currentSeed = /*#__PURE__*/new WeakMap();
var FruitTracker = /*#__PURE__*/function () {
  function FruitTracker(seed) {
    _classCallCheck(this, FruitTracker);
    _classPrivateFieldInitSpec(this, _pityCount, 0);
    _classPrivateFieldInitSpec(this, _pityThreshold, 10);
    _classPrivateFieldInitSpec(this, _fight, 0);
    _defineProperty(this, "seed", void 0);
    _classPrivateFieldInitSpec(this, _currentRng, kolmafia.phpSeed(this.phpSeed));
    _classPrivateFieldInitSpec(this, _currentSeed, this.phpSeed);
    this.seed = seed;
  }
  return _createClass(FruitTracker, [{
    key: "phpSeed",
    get: function get() {
      return this.seed + 381 * (_classPrivateFieldGet2(_fight, this) - 1);
    }
  }, {
    key: "rng",
    get: function get() {
      if (_classPrivateFieldGet2(_currentSeed, this) !== this.phpSeed) {
        _classPrivateFieldSet2(_currentSeed, this, this.phpSeed);
        _classPrivateFieldSet2(_currentRng, this, kolmafia.phpSeed(_classPrivateFieldGet2(_currentSeed, this)));
      }
      return _classPrivateFieldGet2(_currentRng, this);
    }
  }, {
    key: "computeFruit",
    value: function computeFruit() {
      var threshold = _classPrivateFieldGet2(_pityCount, this) < 3 ? _classPrivateFieldGet2(_pityThreshold, this) : 3;
      var isAdvanced = kolmafia.phpMtRand(this.rng, 1, 30) <= threshold;
      if (isAdvanced) {
        var _this$pityCount;
        _classPrivateFieldSet2(_pityCount, this, (_this$pityCount = _classPrivateFieldGet2(_pityCount, this), _this$pityCount++, _this$pityCount));
        _classPrivateFieldSet2(_pityThreshold, this, 10);
        return ADVANCED_FRUIT[kolmafia.phpMtRand(this.rng, 0, 2)];
      } else {
        _classPrivateFieldSet2(_pityThreshold, this, _classPrivateFieldGet2(_pityThreshold, this) + 10);
        return BASIC_FRUIT[kolmafia.phpMtRand(this.rng, 0, 18)];
      }
    }
  }, {
    key: "getFruit",
    value: function getFruit(fight) {
      var _iterator = _createForOfIteratorHelper(fixedDropTurns),
        _step;
      try {
        for (_iterator.s(); !(_step = _iterator.n()).done;) {
          var drop = _step.value;
          if (drop > _classPrivateFieldGet2(_fight, this)) {
            if (fight < drop) {
              _classPrivateFieldSet2(_fight, this, fight);
              return null;
            }
            _classPrivateFieldSet2(_fight, this, drop);
            var fruit = this.computeFruit();
            if (fight === drop) return fruit;
          }
        }
      } catch (err) {
        _iterator.e(err);
      } finally {
        _iterator.f();
      }
      for (_classPrivateFieldGet2(_fight, this); _classPrivateFieldGet2(_fight, this) < fight; _classPrivateFieldSet2(_fight, this, (_this$fight = _classPrivateFieldGet2(_fight, this), _this$fight2 = _this$fight++, _this$fight)), _this$fight2) {
        var _this$fight, _this$fight2;
        if (kolmafia.phpMtRand(this.rng, 1, 50) !== 1) continue;
        this.computeFruit();
      }
      return kolmafia.phpMtRand(this.rng, 1, 50) === 1 ? this.computeFruit() : null;
    }
  }]);
}();
/**
 * @returns Whether or not you `have` The Portable Laughingstock
 */
function have$7() {
  return have$P($item`Portable Laughing Stock`);
}
/**
 * @returns Whether or not you can predict the next Portable Laughingstock drop
 */
function canPredict() {
  return have$7() && kolmafia.daycount() <= 4;
}
/**
 * @returns The next predictable Portable Laughing Stock drop and how many
 * combats until it occurs, or null if the next drop is not predictable.
 */
function nextDrop() {
  var charges = get$2("_laughingStockCharges", 0);
  if (!canPredict()) return null;
  var seed = getSeed(kolmafia.myClass().id, kolmafia.myPath().id, kolmafia.myDaycount());
  var tracker = new FruitTracker(seed);
  for (var fight = 1;; fight++) {
    var fruit = tracker.getFruit(fight);
    if (fight <= charges) continue;
    if (fruit) {
      return [fruit, fight - charges];
    }
  }
}

var summonSkill$6 = $skill`Summon Taffy`;
/**
 * @returns true if the player can Summon Taffy
 */
function have$6() {
  return have$P(summonSkill$6);
}
$items`Alewife™ Ale, bazookafish bubble gum, beefy fish meat, dull fish scale, eel battery, eel sauce, glistening fish meat, high-pressure seltzer bottle, imitation crab crate, ink bladder, live nautical mine, Mer-kin healscroll, Mer-kin lunchbox, Mer-kin thingpouch, pufferfish spine, rough fish scale, salinated mint julep, sand dollar, sea lace, seaweed, shark cartilage, slick fish meat, slug of rum, slug of shochu, slug of vodka, soggy seed packet`;
var RED_TAFFY_DROP_WEIGHTS = new Map([[$item`Alewife™ Ale`, 0.03], [$item`bazookafish bubble gum`, 0.03], [$item`beefy fish meat`, 0.03], [$item`dull fish scale`, 0.0925], [$item`eel battery`, 0.03], [$item`eel sauce`, 0.03], [$item`glistening fish meat`, 0.03], [$item`high-pressure seltzer bottle`, 0.03], [$item`imitation crab crate`, 0.03], [$item`ink bladder`, 0.03], [$item`live nautical mine`, 0.03], [$item`Mer-kin healscroll`, 0.03], [$item`Mer-kin lunchbox`, 0.0925], [$item`Mer-kin thingpouch`, 0.03], [$item`pufferfish spine`, 0.03], [$item`rough fish scale`, 0.03], [$item`salinated mint julep`, 0.03], [$item`sand dollar`, 0.125], [$item`sea lace`, 0.03], [$item`seaweed`, 0.03], [$item`shark cartilage`, 0.03], [$item`slick fish meat`, 0.03], [$item`slug of rum`, 0.03], [$item`slug of shochu`, 0.03], [$item`slug of vodka`, 0.03], [$item`soggy seed packet`, 0.03]]);
/**
 * @returns map containing the chance of an item to be summoned
 */
function expected$6() {
  var rareSummons = get$2("_taffyRareSummons");
  var yellowSummons = get$2("_taffyYellowSummons");
  var onlyYellow = yellowSummons === 0 && rareSummons === 3;
  var totalRareChance = rareSummons < 4 ? 1.0 / 2 ** (rareSummons + 1) : 0.0;
  var commonChance = (1.0 - totalRareChance) / 4;
  var rareChance = onlyYellow ? 0.0 : totalRareChance / (3 - get$2("_taffyYellowSummons"));
  var yellowChance = yellowSummons === 1 ? 0.0 : onlyYellow ? totalRareChance : rareChance;
  return new Map([[$item`pulled blue taffy`, commonChance], [$item`pulled orange taffy`, commonChance], [$item`pulled violet taffy`, commonChance], [$item`pulled red taffy`, commonChance], [$item`pulled indigo taffy`, rareChance], [$item`pulled green taffy`, rareChance], [$item`pulled yellow taffy`, yellowChance]]);
}

var summonSkill$5 = $skill`Summon Candy Heart`;
var libramChance$2 = 1.0 / 6;
var libramExpected$3 = new Map([[$item`green candy heart`, libramChance$2], [$item`lavender candy heart`, libramChance$2], [$item`orange candy heart`, libramChance$2], [$item`pink candy heart`, libramChance$2], [$item`white candy heart`, libramChance$2], [$item`yellow candy heart`, libramChance$2]]);
/**
 * @returns true if the player can Summon Candy Heart
 */
function have$5() {
  return have$P(summonSkill$5);
}
/**
 * @returns map containing the chance of an item to be summoned
 */
function expected$5() {
  return libramExpected$3;
}

var summonSkill$4 = $skill`Summon Party Favor`;
/**
 * @returns true if the player can Summon Party Favors
 */
function have$4() {
  return have$P(summonSkill$4);
}
/**
 * @returns map containing the chance of an item to be summoned
 */
function expected$4() {
  var rareSummons = get$2("_favorRareSummons");
  var totalRareChance = 1.0 / 2 ** (rareSummons + 1);
  var commonChance = (1.0 - totalRareChance) / 3;
  var rareChance = totalRareChance / 3;
  return new Map([[$item`divine blowout`, commonChance], [$item`divine can of silly string`, commonChance], [$item`divine noisemaker`, commonChance], [$item`divine champagne flute`, rareChance], [$item`divine champagne popper`, rareChance], [$item`divine cracker`, rareChance]]);
}

var summonSkill$3 = $skill`Summon Love Song`;
var libramChance$1 = 1.0 / 6;
var libramExpected$2 = new Map([[$item`love song of disturbing obsession`, libramChance$1], [$item`love song of icy revenge`, libramChance$1], [$item`love song of naughty innuendo`, libramChance$1], [$item`love song of smoldering passion`, libramChance$1], [$item`love song of sugary cuteness`, libramChance$1], [$item`love song of vague ambiguity`, libramChance$1]]);
/**
 * @returns true if the player can Summon Love Song
 */
function have$3() {
  return have$P(summonSkill$3);
}
/**
 * @returns map containing the chance of an item to be summoned
 */
function expected$3() {
  return libramExpected$2;
}

var summonSkill$2 = $skill`Summon BRICKOs`;
/**
 * @returns true if the player can Summon BRICKOs
 */
function have$2() {
  return have$P(summonSkill$2);
}
/**
 * @returns map containing the chance of an item to be summoned
 */
function expected$2() {
  var eyeSummons = get$2("_brickoEyeSummons");
  var eyeChance = eyeSummons === 3 ? 0.0 : eyeSummons === 0 ? 0.5 : 1.0 / 3.0;
  return new Map([[$item`BRICKO eye brick`, eyeChance], [$item`BRICKO brick`, 3.0 - eyeChance]]);
}

var summonSkill$1 = $skill`Summon Dice`;
var libramChance = 1.0 / 6;
var libramExpected$1 = new Map([[$item`d4`, libramChance], [$item`d6`, libramChance], [$item`d8`, libramChance], [$item`d10`, libramChance], [$item`d12`, libramChance], [$item`d20`, libramChance]]);
/**
 * @returns true if the player can Summon Dice
 */
function have$1() {
  return have$P(summonSkill$1);
}
/**
 * @returns map containing the chance of an item to be summoned
 */
function expected$1() {
  return libramExpected$1;
}

var summonSkill = $skill`Summon Resolutions`;
var commonChance = 0.98 / 6;
var rareChance = 0.02 / 3;
var libramExpected = new Map([[$item`resolution: be feistier`, commonChance], [$item`resolution: be happier`, commonChance], [$item`resolution: be sexier`, commonChance], [$item`resolution: be smarter`, commonChance], [$item`resolution: be stronger`, commonChance], [$item`resolution: be wealthier`, commonChance], [$item`resolution: be kinder`, rareChance], [$item`resolution: be luckier`, rareChance], [$item`resolution: be more adventurous`, rareChance]]);
/**
 * @returns Whether the player can Summon Resolutions
 */
function have() {
  return have$P(summonSkill);
}
/**
 * @returns Map containing the chance of an item to be summoned
 */
function expected() {
  return libramExpected;
}

/**
 *
 * @returns map containing the chance of items to be summoned for each libram summoning skill available
 */
function possibleLibramSummons() {
  var results = new Map();
  if (have$5()) {
    results.set($skill`Summon Candy Heart`, expected$5());
  }
  if (have$4()) {
    results.set($skill`Summon Party Favor`, expected$4());
  }
  if (have$3()) {
    results.set($skill`Summon Love Song`, expected$3());
  }
  if (have$2()) {
    results.set($skill`Summon BRICKOs`, expected$2());
  }
  if (have$1()) {
    results.set($skill`Summon Dice`, expected$1());
  }
  if (have()) {
    results.set($skill`Summon Resolutions`, expected());
  }
  if (have$6()) {
    results.set($skill`Summon Taffy`, expected$6());
  }
  return results;
}
/**
 * Determines the best libram to cast, based on expected meat value in mall
 *
 * @returns The best libram to cast, based on expected meat value in mall
 */
function bestLibramToCast() {
  var arr = Array.from(possibleLibramSummons().entries());
  if (!arr.length) return null;
  return maxBy(arr, _ref => {
    var _ref2 = _slicedToArray(_ref, 2),
      itemMap = _ref2[1];
    return sum(Array.from(itemMap.entries()), _ref3 => {
      var _ref4 = _slicedToArray(_ref3, 2),
        item = _ref4[0],
        weight = _ref4[1];
      return weight * getSaleValue(item);
    });
  })[0];
}

var commonjsGlobal = typeof globalThis !== 'undefined' ? globalThis : typeof window !== 'undefined' ? window : typeof global !== 'undefined' ? global : typeof self !== 'undefined' ? self : {};

var es_error_toString = {};

var isCallable;
var hasRequiredIsCallable;
function requireIsCallable() {
  if (hasRequiredIsCallable) return isCallable;
  hasRequiredIsCallable = 1;
  // https://tc39.es/ecma262/#sec-IsHTMLDDA-internal-slot
  var documentAll = typeof document == 'object' && document.all;

  // `IsCallable` abstract operation
  // https://tc39.es/ecma262/#sec-iscallable
  // eslint-disable-next-line unicorn/no-typeof-undefined -- required for testing
  isCallable = typeof documentAll == 'undefined' && documentAll !== undefined ? function (argument) {
    return typeof argument == 'function' || argument === documentAll;
  } : function (argument) {
    return typeof argument == 'function';
  };
  return isCallable;
}

var objectDefineProperty = {};

var fails;
var hasRequiredFails;
function requireFails() {
  if (hasRequiredFails) return fails;
  hasRequiredFails = 1;
  fails = function fails(exec) {
    try {
      return !!exec();
    } catch (error) {
      return true;
    }
  };
  return fails;
}

var descriptors;
var hasRequiredDescriptors;
function requireDescriptors() {
  if (hasRequiredDescriptors) return descriptors;
  hasRequiredDescriptors = 1;
  var fails = requireFails();

  // Detect IE8's incomplete defineProperty implementation
  descriptors = !fails(function () {
    // eslint-disable-next-line es/no-object-defineproperty -- required for testing
    return Object.defineProperty({}, 1, {
      get: function get() {
        return 7;
      }
    })[1] !== 7;
  });
  return descriptors;
}

var globalThis_1;
var hasRequiredGlobalThis;
function requireGlobalThis() {
  if (hasRequiredGlobalThis) return globalThis_1;
  hasRequiredGlobalThis = 1;
  var check = function check(it) {
    return it && it.Math === Math && it;
  };

  // https://github.com/zloirock/core-js/issues/86#issuecomment-115759028
  globalThis_1 =
  // eslint-disable-next-line es/no-global-this -- safe
  check(typeof globalThis == 'object' && globalThis) || check(typeof window == 'object' && window) ||
  // eslint-disable-next-line no-restricted-globals -- safe
  check(typeof self == 'object' && self) || check(typeof commonjsGlobal == 'object' && commonjsGlobal) || check(typeof globalThis_1 == 'object' && globalThis_1) ||
  // eslint-disable-next-line no-new-func -- fallback
  function () {
    return this;
  }() || Function('return this')();
  return globalThis_1;
}

var isObject;
var hasRequiredIsObject;
function requireIsObject() {
  if (hasRequiredIsObject) return isObject;
  hasRequiredIsObject = 1;
  var isCallable = requireIsCallable();
  isObject = function isObject(it) {
    return typeof it == 'object' ? it !== null : isCallable(it);
  };
  return isObject;
}

var documentCreateElement;
var hasRequiredDocumentCreateElement;
function requireDocumentCreateElement() {
  if (hasRequiredDocumentCreateElement) return documentCreateElement;
  hasRequiredDocumentCreateElement = 1;
  var globalThis = requireGlobalThis();
  var isObject = requireIsObject();
  var document = globalThis.document;
  // typeof document.createElement is 'object' in old IE
  var EXISTS = isObject(document) && isObject(document.createElement);
  documentCreateElement = function documentCreateElement(it) {
    return EXISTS ? document.createElement(it) : {};
  };
  return documentCreateElement;
}

var ie8DomDefine;
var hasRequiredIe8DomDefine;
function requireIe8DomDefine() {
  if (hasRequiredIe8DomDefine) return ie8DomDefine;
  hasRequiredIe8DomDefine = 1;
  var DESCRIPTORS = requireDescriptors();
  var fails = requireFails();
  var createElement = requireDocumentCreateElement();

  // Thanks to IE8 for its funny defineProperty
  ie8DomDefine = !DESCRIPTORS && !fails(function () {
    // eslint-disable-next-line es/no-object-defineproperty -- required for testing
    return Object.defineProperty(createElement('div'), 'a', {
      get: function get() {
        return 7;
      }
    }).a !== 7;
  });
  return ie8DomDefine;
}

var v8PrototypeDefineBug;
var hasRequiredV8PrototypeDefineBug;
function requireV8PrototypeDefineBug() {
  if (hasRequiredV8PrototypeDefineBug) return v8PrototypeDefineBug;
  hasRequiredV8PrototypeDefineBug = 1;
  var DESCRIPTORS = requireDescriptors();
  var fails = requireFails();

  // V8 ~ Chrome 36-
  // https://bugs.chromium.org/p/v8/issues/detail?id=3334
  v8PrototypeDefineBug = DESCRIPTORS && fails(function () {
    // eslint-disable-next-line es/no-object-defineproperty -- required for testing
    return Object.defineProperty(function () {/* empty */}, 'prototype', {
      value: 42,
      writable: false
    }).prototype !== 42;
  });
  return v8PrototypeDefineBug;
}

var anObject;
var hasRequiredAnObject;
function requireAnObject() {
  if (hasRequiredAnObject) return anObject;
  hasRequiredAnObject = 1;
  var isObject = requireIsObject();
  var $String = String;
  var $TypeError = TypeError;

  // `Assert: Type(argument) is Object`
  anObject = function anObject(argument) {
    if (isObject(argument)) return argument;
    throw new $TypeError($String(argument) + ' is not an object');
  };
  return anObject;
}

var functionBindNative;
var hasRequiredFunctionBindNative;
function requireFunctionBindNative() {
  if (hasRequiredFunctionBindNative) return functionBindNative;
  hasRequiredFunctionBindNative = 1;
  var fails = requireFails();
  functionBindNative = !fails(function () {
    // eslint-disable-next-line es/no-function-prototype-bind -- safe
    var test = function () {/* empty */}.bind();
    // eslint-disable-next-line no-prototype-builtins -- safe
    return typeof test != 'function' || test.hasOwnProperty('prototype');
  });
  return functionBindNative;
}

var functionCall;
var hasRequiredFunctionCall;
function requireFunctionCall() {
  if (hasRequiredFunctionCall) return functionCall;
  hasRequiredFunctionCall = 1;
  var NATIVE_BIND = requireFunctionBindNative();
  var call = Function.prototype.call;
  // eslint-disable-next-line es/no-function-prototype-bind -- safe
  functionCall = NATIVE_BIND ? call.bind(call) : function () {
    return call.apply(call, arguments);
  };
  return functionCall;
}

var getBuiltIn;
var hasRequiredGetBuiltIn;
function requireGetBuiltIn() {
  if (hasRequiredGetBuiltIn) return getBuiltIn;
  hasRequiredGetBuiltIn = 1;
  var globalThis = requireGlobalThis();
  var isCallable = requireIsCallable();
  var aFunction = function aFunction(argument) {
    return isCallable(argument) ? argument : undefined;
  };
  getBuiltIn = function getBuiltIn(namespace, method) {
    return arguments.length < 2 ? aFunction(globalThis[namespace]) : globalThis[namespace] && globalThis[namespace][method];
  };
  return getBuiltIn;
}

var functionUncurryThis;
var hasRequiredFunctionUncurryThis;
function requireFunctionUncurryThis() {
  if (hasRequiredFunctionUncurryThis) return functionUncurryThis;
  hasRequiredFunctionUncurryThis = 1;
  var NATIVE_BIND = requireFunctionBindNative();
  var FunctionPrototype = Function.prototype;
  var call = FunctionPrototype.call;
  // eslint-disable-next-line es/no-function-prototype-bind -- safe
  var uncurryThisWithBind = NATIVE_BIND && FunctionPrototype.bind.bind(call, call);
  functionUncurryThis = NATIVE_BIND ? uncurryThisWithBind : function (fn) {
    return function () {
      return call.apply(fn, arguments);
    };
  };
  return functionUncurryThis;
}

var objectIsPrototypeOf;
var hasRequiredObjectIsPrototypeOf;
function requireObjectIsPrototypeOf() {
  if (hasRequiredObjectIsPrototypeOf) return objectIsPrototypeOf;
  hasRequiredObjectIsPrototypeOf = 1;
  var uncurryThis = requireFunctionUncurryThis();
  objectIsPrototypeOf = uncurryThis({}.isPrototypeOf);
  return objectIsPrototypeOf;
}

var environmentUserAgent;
var hasRequiredEnvironmentUserAgent;
function requireEnvironmentUserAgent() {
  if (hasRequiredEnvironmentUserAgent) return environmentUserAgent;
  hasRequiredEnvironmentUserAgent = 1;
  var globalThis = requireGlobalThis();
  var navigator = globalThis.navigator;
  var userAgent = navigator && navigator.userAgent;
  environmentUserAgent = userAgent ? String(userAgent) : '';
  return environmentUserAgent;
}

var environmentV8Version;
var hasRequiredEnvironmentV8Version;
function requireEnvironmentV8Version() {
  if (hasRequiredEnvironmentV8Version) return environmentV8Version;
  hasRequiredEnvironmentV8Version = 1;
  var globalThis = requireGlobalThis();
  var userAgent = requireEnvironmentUserAgent();
  var process = globalThis.process;
  var Deno = globalThis.Deno;
  var versions = process && process.versions || Deno && Deno.version;
  var v8 = versions && versions.v8;
  var match, version;
  if (v8) {
    match = v8.split('.');
    // in old Chrome, versions of V8 isn't V8 = Chrome / 10
    // but their correct versions are not interesting for us
    version = match[0] > 0 && match[0] < 4 ? 1 : +(match[0] + match[1]);
  }

  // BrowserFS NodeJS `process` polyfill incorrectly set `.v8` to `0.0`
  // so check `userAgent` even if `.v8` exists, but 0
  if (!version && userAgent) {
    match = userAgent.match(/Edge\/(\d+)/);
    if (!match || match[1] >= 74) {
      match = userAgent.match(/Chrome\/(\d+)/);
      if (match) version = +match[1];
    }
  }
  environmentV8Version = version;
  return environmentV8Version;
}

var symbolConstructorDetection;
var hasRequiredSymbolConstructorDetection;
function requireSymbolConstructorDetection() {
  if (hasRequiredSymbolConstructorDetection) return symbolConstructorDetection;
  hasRequiredSymbolConstructorDetection = 1;
  /* eslint-disable es/no-symbol -- required for testing */
  var V8_VERSION = requireEnvironmentV8Version();
  var fails = requireFails();
  var globalThis = requireGlobalThis();
  var $String = globalThis.String;

  // eslint-disable-next-line es/no-object-getownpropertysymbols -- required for testing
  symbolConstructorDetection = !!Object.getOwnPropertySymbols && !fails(function () {
    var symbol = Symbol('symbol detection');
    // Chrome 38 Symbol has incorrect toString conversion
    // `get-own-property-symbols` polyfill symbols converted to object are not Symbol instances
    // nb: Do not call `String` directly to avoid this being optimized out to `symbol+''` which will,
    // of course, fail.
    return !$String(symbol) || !(Object(symbol) instanceof Symbol) ||
    // Chrome 38-40 symbols are not inherited from DOM collections prototypes to instances
    !Symbol.sham && V8_VERSION && V8_VERSION < 41;
  });
  return symbolConstructorDetection;
}

var useSymbolAsUid;
var hasRequiredUseSymbolAsUid;
function requireUseSymbolAsUid() {
  if (hasRequiredUseSymbolAsUid) return useSymbolAsUid;
  hasRequiredUseSymbolAsUid = 1;
  /* eslint-disable es/no-symbol -- required for testing */
  var NATIVE_SYMBOL = requireSymbolConstructorDetection();
  useSymbolAsUid = NATIVE_SYMBOL && !Symbol.sham && typeof Symbol.iterator == 'symbol';
  return useSymbolAsUid;
}

var isSymbol;
var hasRequiredIsSymbol;
function requireIsSymbol() {
  if (hasRequiredIsSymbol) return isSymbol;
  hasRequiredIsSymbol = 1;
  var getBuiltIn = requireGetBuiltIn();
  var isCallable = requireIsCallable();
  var isPrototypeOf = requireObjectIsPrototypeOf();
  var USE_SYMBOL_AS_UID = requireUseSymbolAsUid();
  var $Object = Object;
  isSymbol = USE_SYMBOL_AS_UID ? function (it) {
    return typeof it == 'symbol';
  } : function (it) {
    var $Symbol = getBuiltIn('Symbol');
    return isCallable($Symbol) && isPrototypeOf($Symbol.prototype, $Object(it));
  };
  return isSymbol;
}

var tryToString;
var hasRequiredTryToString;
function requireTryToString() {
  if (hasRequiredTryToString) return tryToString;
  hasRequiredTryToString = 1;
  var $String = String;
  tryToString = function tryToString(argument) {
    try {
      return $String(argument);
    } catch (error) {
      return 'Object';
    }
  };
  return tryToString;
}

var aCallable;
var hasRequiredACallable;
function requireACallable() {
  if (hasRequiredACallable) return aCallable;
  hasRequiredACallable = 1;
  var isCallable = requireIsCallable();
  var tryToString = requireTryToString();
  var $TypeError = TypeError;

  // `Assert: IsCallable(argument) is true`
  aCallable = function aCallable(argument) {
    if (isCallable(argument)) return argument;
    throw new $TypeError(tryToString(argument) + ' is not a function');
  };
  return aCallable;
}

var isNullOrUndefined;
var hasRequiredIsNullOrUndefined;
function requireIsNullOrUndefined() {
  if (hasRequiredIsNullOrUndefined) return isNullOrUndefined;
  hasRequiredIsNullOrUndefined = 1;
  // we can't use just `it == null` since of `document.all` special case
  // https://tc39.es/ecma262/#sec-IsHTMLDDA-internal-slot-aec
  isNullOrUndefined = function isNullOrUndefined(it) {
    return it === null || it === undefined;
  };
  return isNullOrUndefined;
}

var getMethod;
var hasRequiredGetMethod;
function requireGetMethod() {
  if (hasRequiredGetMethod) return getMethod;
  hasRequiredGetMethod = 1;
  var aCallable = requireACallable();
  var isNullOrUndefined = requireIsNullOrUndefined();

  // `GetMethod` abstract operation
  // https://tc39.es/ecma262/#sec-getmethod
  getMethod = function getMethod(V, P) {
    var func = V[P];
    return isNullOrUndefined(func) ? undefined : aCallable(func);
  };
  return getMethod;
}

var ordinaryToPrimitive;
var hasRequiredOrdinaryToPrimitive;
function requireOrdinaryToPrimitive() {
  if (hasRequiredOrdinaryToPrimitive) return ordinaryToPrimitive;
  hasRequiredOrdinaryToPrimitive = 1;
  var call = requireFunctionCall();
  var isCallable = requireIsCallable();
  var isObject = requireIsObject();
  var $TypeError = TypeError;

  // `OrdinaryToPrimitive` abstract operation
  // https://tc39.es/ecma262/#sec-ordinarytoprimitive
  ordinaryToPrimitive = function ordinaryToPrimitive(input, pref) {
    var fn, val;
    if (pref === 'string' && isCallable(fn = input.toString) && !isObject(val = call(fn, input))) return val;
    if (isCallable(fn = input.valueOf) && !isObject(val = call(fn, input))) return val;
    if (pref !== 'string' && isCallable(fn = input.toString) && !isObject(val = call(fn, input))) return val;
    throw new $TypeError("Can't convert object to primitive value");
  };
  return ordinaryToPrimitive;
}

var sharedStore = {exports: {}};

var isPure;
var hasRequiredIsPure;
function requireIsPure() {
  if (hasRequiredIsPure) return isPure;
  hasRequiredIsPure = 1;
  isPure = false;
  return isPure;
}

var defineGlobalProperty;
var hasRequiredDefineGlobalProperty;
function requireDefineGlobalProperty() {
  if (hasRequiredDefineGlobalProperty) return defineGlobalProperty;
  hasRequiredDefineGlobalProperty = 1;
  var globalThis = requireGlobalThis();

  // eslint-disable-next-line es/no-object-defineproperty -- safe
  var defineProperty = Object.defineProperty;
  defineGlobalProperty = function defineGlobalProperty(key, value) {
    try {
      defineProperty(globalThis, key, {
        value: value,
        configurable: true,
        writable: true
      });
    } catch (error) {
      globalThis[key] = value;
    }
    return value;
  };
  return defineGlobalProperty;
}

var hasRequiredSharedStore;
function requireSharedStore() {
  if (hasRequiredSharedStore) return sharedStore.exports;
  hasRequiredSharedStore = 1;
  var IS_PURE = requireIsPure();
  var globalThis = requireGlobalThis();
  var defineGlobalProperty = requireDefineGlobalProperty();
  var SHARED = '__core-js_shared__';
  var store = sharedStore.exports = globalThis[SHARED] || defineGlobalProperty(SHARED, {});
  (store.versions || (store.versions = [])).push({
    version: '3.47.0',
    mode: IS_PURE ? 'pure' : 'global',
    copyright: '© 2014-2025 Denis Pushkarev (zloirock.ru), 2025 CoreJS Company (core-js.io)',
    license: 'https://github.com/zloirock/core-js/blob/v3.47.0/LICENSE',
    source: 'https://github.com/zloirock/core-js'
  });
  return sharedStore.exports;
}

var shared;
var hasRequiredShared;
function requireShared() {
  if (hasRequiredShared) return shared;
  hasRequiredShared = 1;
  var store = requireSharedStore();
  shared = function shared(key, value) {
    return store[key] || (store[key] = value || {});
  };
  return shared;
}

var requireObjectCoercible;
var hasRequiredRequireObjectCoercible;
function requireRequireObjectCoercible() {
  if (hasRequiredRequireObjectCoercible) return requireObjectCoercible;
  hasRequiredRequireObjectCoercible = 1;
  var isNullOrUndefined = requireIsNullOrUndefined();
  var $TypeError = TypeError;

  // `RequireObjectCoercible` abstract operation
  // https://tc39.es/ecma262/#sec-requireobjectcoercible
  requireObjectCoercible = function requireObjectCoercible(it) {
    if (isNullOrUndefined(it)) throw new $TypeError("Can't call method on " + it);
    return it;
  };
  return requireObjectCoercible;
}

var toObject;
var hasRequiredToObject;
function requireToObject() {
  if (hasRequiredToObject) return toObject;
  hasRequiredToObject = 1;
  var requireObjectCoercible = requireRequireObjectCoercible();
  var $Object = Object;

  // `ToObject` abstract operation
  // https://tc39.es/ecma262/#sec-toobject
  toObject = function toObject(argument) {
    return $Object(requireObjectCoercible(argument));
  };
  return toObject;
}

var hasOwnProperty_1;
var hasRequiredHasOwnProperty;
function requireHasOwnProperty() {
  if (hasRequiredHasOwnProperty) return hasOwnProperty_1;
  hasRequiredHasOwnProperty = 1;
  var uncurryThis = requireFunctionUncurryThis();
  var toObject = requireToObject();
  var hasOwnProperty = uncurryThis({}.hasOwnProperty);

  // `HasOwnProperty` abstract operation
  // https://tc39.es/ecma262/#sec-hasownproperty
  // eslint-disable-next-line es/no-object-hasown -- safe
  hasOwnProperty_1 = Object.hasOwn || function hasOwn(it, key) {
    return hasOwnProperty(toObject(it), key);
  };
  return hasOwnProperty_1;
}

var uid;
var hasRequiredUid;
function requireUid() {
  if (hasRequiredUid) return uid;
  hasRequiredUid = 1;
  var uncurryThis = requireFunctionUncurryThis();
  var id = 0;
  var postfix = Math.random();
  var toString = uncurryThis(1.1.toString);
  uid = function uid(key) {
    return 'Symbol(' + (key === undefined ? '' : key) + ')_' + toString(++id + postfix, 36);
  };
  return uid;
}

var wellKnownSymbol;
var hasRequiredWellKnownSymbol;
function requireWellKnownSymbol() {
  if (hasRequiredWellKnownSymbol) return wellKnownSymbol;
  hasRequiredWellKnownSymbol = 1;
  var globalThis = requireGlobalThis();
  var shared = requireShared();
  var hasOwn = requireHasOwnProperty();
  var uid = requireUid();
  var NATIVE_SYMBOL = requireSymbolConstructorDetection();
  var USE_SYMBOL_AS_UID = requireUseSymbolAsUid();
  var Symbol = globalThis.Symbol;
  var WellKnownSymbolsStore = shared('wks');
  var createWellKnownSymbol = USE_SYMBOL_AS_UID ? Symbol['for'] || Symbol : Symbol && Symbol.withoutSetter || uid;
  wellKnownSymbol = function wellKnownSymbol(name) {
    if (!hasOwn(WellKnownSymbolsStore, name)) {
      WellKnownSymbolsStore[name] = NATIVE_SYMBOL && hasOwn(Symbol, name) ? Symbol[name] : createWellKnownSymbol('Symbol.' + name);
    }
    return WellKnownSymbolsStore[name];
  };
  return wellKnownSymbol;
}

var toPrimitive;
var hasRequiredToPrimitive;
function requireToPrimitive() {
  if (hasRequiredToPrimitive) return toPrimitive;
  hasRequiredToPrimitive = 1;
  var call = requireFunctionCall();
  var isObject = requireIsObject();
  var isSymbol = requireIsSymbol();
  var getMethod = requireGetMethod();
  var ordinaryToPrimitive = requireOrdinaryToPrimitive();
  var wellKnownSymbol = requireWellKnownSymbol();
  var $TypeError = TypeError;
  var TO_PRIMITIVE = wellKnownSymbol('toPrimitive');

  // `ToPrimitive` abstract operation
  // https://tc39.es/ecma262/#sec-toprimitive
  toPrimitive = function toPrimitive(input, pref) {
    if (!isObject(input) || isSymbol(input)) return input;
    var exoticToPrim = getMethod(input, TO_PRIMITIVE);
    var result;
    if (exoticToPrim) {
      if (pref === undefined) pref = 'default';
      result = call(exoticToPrim, input, pref);
      if (!isObject(result) || isSymbol(result)) return result;
      throw new $TypeError("Can't convert object to primitive value");
    }
    if (pref === undefined) pref = 'number';
    return ordinaryToPrimitive(input, pref);
  };
  return toPrimitive;
}

var toPropertyKey;
var hasRequiredToPropertyKey;
function requireToPropertyKey() {
  if (hasRequiredToPropertyKey) return toPropertyKey;
  hasRequiredToPropertyKey = 1;
  var toPrimitive = requireToPrimitive();
  var isSymbol = requireIsSymbol();

  // `ToPropertyKey` abstract operation
  // https://tc39.es/ecma262/#sec-topropertykey
  toPropertyKey = function toPropertyKey(argument) {
    var key = toPrimitive(argument, 'string');
    return isSymbol(key) ? key : key + '';
  };
  return toPropertyKey;
}

var hasRequiredObjectDefineProperty;
function requireObjectDefineProperty() {
  if (hasRequiredObjectDefineProperty) return objectDefineProperty;
  hasRequiredObjectDefineProperty = 1;
  var DESCRIPTORS = requireDescriptors();
  var IE8_DOM_DEFINE = requireIe8DomDefine();
  var V8_PROTOTYPE_DEFINE_BUG = requireV8PrototypeDefineBug();
  var anObject = requireAnObject();
  var toPropertyKey = requireToPropertyKey();
  var $TypeError = TypeError;
  // eslint-disable-next-line es/no-object-defineproperty -- safe
  var $defineProperty = Object.defineProperty;
  // eslint-disable-next-line es/no-object-getownpropertydescriptor -- safe
  var $getOwnPropertyDescriptor = Object.getOwnPropertyDescriptor;
  var ENUMERABLE = 'enumerable';
  var CONFIGURABLE = 'configurable';
  var WRITABLE = 'writable';

  // `Object.defineProperty` method
  // https://tc39.es/ecma262/#sec-object.defineproperty
  objectDefineProperty.f = DESCRIPTORS ? V8_PROTOTYPE_DEFINE_BUG ? function defineProperty(O, P, Attributes) {
    anObject(O);
    P = toPropertyKey(P);
    anObject(Attributes);
    if (typeof O === 'function' && P === 'prototype' && 'value' in Attributes && WRITABLE in Attributes && !Attributes[WRITABLE]) {
      var current = $getOwnPropertyDescriptor(O, P);
      if (current && current[WRITABLE]) {
        O[P] = Attributes.value;
        Attributes = {
          configurable: CONFIGURABLE in Attributes ? Attributes[CONFIGURABLE] : current[CONFIGURABLE],
          enumerable: ENUMERABLE in Attributes ? Attributes[ENUMERABLE] : current[ENUMERABLE],
          writable: false
        };
      }
    }
    return $defineProperty(O, P, Attributes);
  } : $defineProperty : function defineProperty(O, P, Attributes) {
    anObject(O);
    P = toPropertyKey(P);
    anObject(Attributes);
    if (IE8_DOM_DEFINE) try {
      return $defineProperty(O, P, Attributes);
    } catch (error) {/* empty */}
    if ('get' in Attributes || 'set' in Attributes) throw new $TypeError('Accessors not supported');
    if ('value' in Attributes) O[P] = Attributes.value;
    return O;
  };
  return objectDefineProperty;
}

var makeBuiltIn = {exports: {}};

var functionName;
var hasRequiredFunctionName;
function requireFunctionName() {
  if (hasRequiredFunctionName) return functionName;
  hasRequiredFunctionName = 1;
  var DESCRIPTORS = requireDescriptors();
  var hasOwn = requireHasOwnProperty();
  var FunctionPrototype = Function.prototype;
  // eslint-disable-next-line es/no-object-getownpropertydescriptor -- safe
  var getDescriptor = DESCRIPTORS && Object.getOwnPropertyDescriptor;
  var EXISTS = hasOwn(FunctionPrototype, 'name');
  // additional protection from minified / mangled / dropped function names
  var PROPER = EXISTS && function something() {/* empty */}.name === 'something';
  var CONFIGURABLE = EXISTS && (!DESCRIPTORS || DESCRIPTORS && getDescriptor(FunctionPrototype, 'name').configurable);
  functionName = {
    EXISTS: EXISTS,
    PROPER: PROPER,
    CONFIGURABLE: CONFIGURABLE
  };
  return functionName;
}

var inspectSource;
var hasRequiredInspectSource;
function requireInspectSource() {
  if (hasRequiredInspectSource) return inspectSource;
  hasRequiredInspectSource = 1;
  var uncurryThis = requireFunctionUncurryThis();
  var isCallable = requireIsCallable();
  var store = requireSharedStore();
  var functionToString = uncurryThis(Function.toString);

  // this helper broken in `core-js@3.4.1-3.4.4`, so we can't use `shared` helper
  if (!isCallable(store.inspectSource)) {
    store.inspectSource = function (it) {
      return functionToString(it);
    };
  }
  inspectSource = store.inspectSource;
  return inspectSource;
}

var weakMapBasicDetection;
var hasRequiredWeakMapBasicDetection;
function requireWeakMapBasicDetection() {
  if (hasRequiredWeakMapBasicDetection) return weakMapBasicDetection;
  hasRequiredWeakMapBasicDetection = 1;
  var globalThis = requireGlobalThis();
  var isCallable = requireIsCallable();
  var WeakMap = globalThis.WeakMap;
  weakMapBasicDetection = isCallable(WeakMap) && /native code/.test(String(WeakMap));
  return weakMapBasicDetection;
}

var createPropertyDescriptor;
var hasRequiredCreatePropertyDescriptor;
function requireCreatePropertyDescriptor() {
  if (hasRequiredCreatePropertyDescriptor) return createPropertyDescriptor;
  hasRequiredCreatePropertyDescriptor = 1;
  createPropertyDescriptor = function createPropertyDescriptor(bitmap, value) {
    return {
      enumerable: !(bitmap & 1),
      configurable: !(bitmap & 2),
      writable: !(bitmap & 4),
      value: value
    };
  };
  return createPropertyDescriptor;
}

var createNonEnumerableProperty;
var hasRequiredCreateNonEnumerableProperty;
function requireCreateNonEnumerableProperty() {
  if (hasRequiredCreateNonEnumerableProperty) return createNonEnumerableProperty;
  hasRequiredCreateNonEnumerableProperty = 1;
  var DESCRIPTORS = requireDescriptors();
  var definePropertyModule = requireObjectDefineProperty();
  var createPropertyDescriptor = requireCreatePropertyDescriptor();
  createNonEnumerableProperty = DESCRIPTORS ? function (object, key, value) {
    return definePropertyModule.f(object, key, createPropertyDescriptor(1, value));
  } : function (object, key, value) {
    object[key] = value;
    return object;
  };
  return createNonEnumerableProperty;
}

var sharedKey;
var hasRequiredSharedKey;
function requireSharedKey() {
  if (hasRequiredSharedKey) return sharedKey;
  hasRequiredSharedKey = 1;
  var shared = requireShared();
  var uid = requireUid();
  var keys = shared('keys');
  sharedKey = function sharedKey(key) {
    return keys[key] || (keys[key] = uid(key));
  };
  return sharedKey;
}

var hiddenKeys;
var hasRequiredHiddenKeys;
function requireHiddenKeys() {
  if (hasRequiredHiddenKeys) return hiddenKeys;
  hasRequiredHiddenKeys = 1;
  hiddenKeys = {};
  return hiddenKeys;
}

var internalState;
var hasRequiredInternalState;
function requireInternalState() {
  if (hasRequiredInternalState) return internalState;
  hasRequiredInternalState = 1;
  var NATIVE_WEAK_MAP = requireWeakMapBasicDetection();
  var globalThis = requireGlobalThis();
  var isObject = requireIsObject();
  var createNonEnumerableProperty = requireCreateNonEnumerableProperty();
  var hasOwn = requireHasOwnProperty();
  var shared = requireSharedStore();
  var sharedKey = requireSharedKey();
  var hiddenKeys = requireHiddenKeys();
  var OBJECT_ALREADY_INITIALIZED = 'Object already initialized';
  var TypeError = globalThis.TypeError;
  var WeakMap = globalThis.WeakMap;
  var set, get, has;
  var enforce = function enforce(it) {
    return has(it) ? get(it) : set(it, {});
  };
  var getterFor = function getterFor(TYPE) {
    return function (it) {
      var state;
      if (!isObject(it) || (state = get(it)).type !== TYPE) {
        throw new TypeError('Incompatible receiver, ' + TYPE + ' required');
      }
      return state;
    };
  };
  if (NATIVE_WEAK_MAP || shared.state) {
    var store = shared.state || (shared.state = new WeakMap());
    /* eslint-disable no-self-assign -- prototype methods protection */
    store.get = store.get;
    store.has = store.has;
    store.set = store.set;
    /* eslint-enable no-self-assign -- prototype methods protection */
    set = function set(it, metadata) {
      if (store.has(it)) throw new TypeError(OBJECT_ALREADY_INITIALIZED);
      metadata.facade = it;
      store.set(it, metadata);
      return metadata;
    };
    get = function get(it) {
      return store.get(it) || {};
    };
    has = function has(it) {
      return store.has(it);
    };
  } else {
    var STATE = sharedKey('state');
    hiddenKeys[STATE] = true;
    set = function set(it, metadata) {
      if (hasOwn(it, STATE)) throw new TypeError(OBJECT_ALREADY_INITIALIZED);
      metadata.facade = it;
      createNonEnumerableProperty(it, STATE, metadata);
      return metadata;
    };
    get = function get(it) {
      return hasOwn(it, STATE) ? it[STATE] : {};
    };
    has = function has(it) {
      return hasOwn(it, STATE);
    };
  }
  internalState = {
    set: set,
    get: get,
    has: has,
    enforce: enforce,
    getterFor: getterFor
  };
  return internalState;
}

var hasRequiredMakeBuiltIn;
function requireMakeBuiltIn() {
  if (hasRequiredMakeBuiltIn) return makeBuiltIn.exports;
  hasRequiredMakeBuiltIn = 1;
  var uncurryThis = requireFunctionUncurryThis();
  var fails = requireFails();
  var isCallable = requireIsCallable();
  var hasOwn = requireHasOwnProperty();
  var DESCRIPTORS = requireDescriptors();
  var CONFIGURABLE_FUNCTION_NAME = requireFunctionName().CONFIGURABLE;
  var inspectSource = requireInspectSource();
  var InternalStateModule = requireInternalState();
  var enforceInternalState = InternalStateModule.enforce;
  var getInternalState = InternalStateModule.get;
  var $String = String;
  // eslint-disable-next-line es/no-object-defineproperty -- safe
  var defineProperty = Object.defineProperty;
  var stringSlice = uncurryThis(''.slice);
  var replace = uncurryThis(''.replace);
  var join = uncurryThis([].join);
  var CONFIGURABLE_LENGTH = DESCRIPTORS && !fails(function () {
    return defineProperty(function () {/* empty */}, 'length', {
      value: 8
    }).length !== 8;
  });
  var TEMPLATE = String(String).split('String');
  var makeBuiltIn$1 = makeBuiltIn.exports = function (value, name, options) {
    if (stringSlice($String(name), 0, 7) === 'Symbol(') {
      name = '[' + replace($String(name), /^Symbol\(([^)]*)\).*$/, '$1') + ']';
    }
    if (options && options.getter) name = 'get ' + name;
    if (options && options.setter) name = 'set ' + name;
    if (!hasOwn(value, 'name') || CONFIGURABLE_FUNCTION_NAME && value.name !== name) {
      if (DESCRIPTORS) defineProperty(value, 'name', {
        value: name,
        configurable: true
      });else value.name = name;
    }
    if (CONFIGURABLE_LENGTH && options && hasOwn(options, 'arity') && value.length !== options.arity) {
      defineProperty(value, 'length', {
        value: options.arity
      });
    }
    try {
      if (options && hasOwn(options, 'constructor') && options.constructor) {
        if (DESCRIPTORS) defineProperty(value, 'prototype', {
          writable: false
        });
        // in V8 ~ Chrome 53, prototypes of some methods, like `Array.prototype.values`, are non-writable
      } else if (value.prototype) value.prototype = undefined;
    } catch (error) {/* empty */}
    var state = enforceInternalState(value);
    if (!hasOwn(state, 'source')) {
      state.source = join(TEMPLATE, typeof name == 'string' ? name : '');
    }
    return value;
  };

  // add fake Function#toString for correct work wrapped methods / constructors with methods like LoDash isNative
  // eslint-disable-next-line no-extend-native -- required
  Function.prototype.toString = makeBuiltIn$1(function toString() {
    return isCallable(this) && getInternalState(this).source || inspectSource(this);
  }, 'toString');
  return makeBuiltIn.exports;
}

var defineBuiltIn;
var hasRequiredDefineBuiltIn;
function requireDefineBuiltIn() {
  if (hasRequiredDefineBuiltIn) return defineBuiltIn;
  hasRequiredDefineBuiltIn = 1;
  var isCallable = requireIsCallable();
  var definePropertyModule = requireObjectDefineProperty();
  var makeBuiltIn = requireMakeBuiltIn();
  var defineGlobalProperty = requireDefineGlobalProperty();
  defineBuiltIn = function defineBuiltIn(O, key, value, options) {
    if (!options) options = {};
    var simple = options.enumerable;
    var name = options.name !== undefined ? options.name : key;
    if (isCallable(value)) makeBuiltIn(value, name, options);
    if (options.global) {
      if (simple) O[key] = value;else defineGlobalProperty(key, value);
    } else {
      try {
        if (!options.unsafe) delete O[key];else if (O[key]) simple = true;
      } catch (error) {/* empty */}
      if (simple) O[key] = value;else definePropertyModule.f(O, key, {
        value: value,
        enumerable: false,
        configurable: !options.nonConfigurable,
        writable: !options.nonWritable
      });
    }
    return O;
  };
  return defineBuiltIn;
}

var toStringTagSupport;
var hasRequiredToStringTagSupport;
function requireToStringTagSupport() {
  if (hasRequiredToStringTagSupport) return toStringTagSupport;
  hasRequiredToStringTagSupport = 1;
  var wellKnownSymbol = requireWellKnownSymbol();
  var TO_STRING_TAG = wellKnownSymbol('toStringTag');
  var test = {};
  // eslint-disable-next-line unicorn/no-immediate-mutation -- ES3 syntax limitation
  test[TO_STRING_TAG] = 'z';
  toStringTagSupport = String(test) === '[object z]';
  return toStringTagSupport;
}

var classofRaw;
var hasRequiredClassofRaw;
function requireClassofRaw() {
  if (hasRequiredClassofRaw) return classofRaw;
  hasRequiredClassofRaw = 1;
  var uncurryThis = requireFunctionUncurryThis();
  var toString = uncurryThis({}.toString);
  var stringSlice = uncurryThis(''.slice);
  classofRaw = function classofRaw(it) {
    return stringSlice(toString(it), 8, -1);
  };
  return classofRaw;
}

var classof;
var hasRequiredClassof;
function requireClassof() {
  if (hasRequiredClassof) return classof;
  hasRequiredClassof = 1;
  var TO_STRING_TAG_SUPPORT = requireToStringTagSupport();
  var isCallable = requireIsCallable();
  var classofRaw = requireClassofRaw();
  var wellKnownSymbol = requireWellKnownSymbol();
  var TO_STRING_TAG = wellKnownSymbol('toStringTag');
  var $Object = Object;

  // ES3 wrong here
  var CORRECT_ARGUMENTS = classofRaw(function () {
    return arguments;
  }()) === 'Arguments';

  // fallback for IE11 Script Access Denied error
  var tryGet = function tryGet(it, key) {
    try {
      return it[key];
    } catch (error) {/* empty */}
  };

  // getting tag from ES6+ `Object.prototype.toString`
  classof = TO_STRING_TAG_SUPPORT ? classofRaw : function (it) {
    var O, tag, result;
    return it === undefined ? 'Undefined' : it === null ? 'Null'
    // @@toStringTag case
    : typeof (tag = tryGet(O = $Object(it), TO_STRING_TAG)) == 'string' ? tag
    // builtinTag case
    : CORRECT_ARGUMENTS ? classofRaw(O)
    // ES3 arguments fallback
    : (result = classofRaw(O)) === 'Object' && isCallable(O.callee) ? 'Arguments' : result;
  };
  return classof;
}

var toString;
var hasRequiredToString;
function requireToString() {
  if (hasRequiredToString) return toString;
  hasRequiredToString = 1;
  var classof = requireClassof();
  var $String = String;
  toString = function toString(argument) {
    if (classof(argument) === 'Symbol') throw new TypeError('Cannot convert a Symbol value to a string');
    return $String(argument);
  };
  return toString;
}

var normalizeStringArgument;
var hasRequiredNormalizeStringArgument;
function requireNormalizeStringArgument() {
  if (hasRequiredNormalizeStringArgument) return normalizeStringArgument;
  hasRequiredNormalizeStringArgument = 1;
  var toString = requireToString();
  normalizeStringArgument = function normalizeStringArgument(argument, $default) {
    return argument === undefined ? arguments.length < 2 ? '' : $default : toString(argument);
  };
  return normalizeStringArgument;
}

var errorToString;
var hasRequiredErrorToString;
function requireErrorToString() {
  if (hasRequiredErrorToString) return errorToString;
  hasRequiredErrorToString = 1;
  var DESCRIPTORS = requireDescriptors();
  var fails = requireFails();
  var anObject = requireAnObject();
  var normalizeStringArgument = requireNormalizeStringArgument();
  var nativeErrorToString = Error.prototype.toString;
  var INCORRECT_TO_STRING = fails(function () {
    if (DESCRIPTORS) {
      // Chrome 32- incorrectly call accessor
      // eslint-disable-next-line es/no-object-create, es/no-object-defineproperty -- safe
      var object = Object.create(Object.defineProperty({}, 'name', {
        get: function get() {
          return this === object;
        }
      }));
      if (nativeErrorToString.call(object) !== 'true') return true;
    }
    // FF10- does not properly handle non-strings
    return nativeErrorToString.call({
      message: 1,
      name: 2
    }) !== '2: 1'
    // IE8 does not properly handle defaults
    || nativeErrorToString.call({}) !== 'Error';
  });
  errorToString = INCORRECT_TO_STRING ? function toString() {
    var O = anObject(this);
    var name = normalizeStringArgument(O.name, 'Error');
    var message = normalizeStringArgument(O.message);
    return !name ? message : !message ? name : name + ': ' + message;
  } : nativeErrorToString;
  return errorToString;
}

var hasRequiredEs_error_toString;
function requireEs_error_toString() {
  if (hasRequiredEs_error_toString) return es_error_toString;
  hasRequiredEs_error_toString = 1;
  var defineBuiltIn = requireDefineBuiltIn();
  var errorToString = requireErrorToString();
  var ErrorPrototype = Error.prototype;

  // `Error.prototype.toString` method fix
  // https://tc39.es/ecma262/#sec-error.prototype.tostring
  if (ErrorPrototype.toString !== errorToString) {
    defineBuiltIn(ErrorPrototype, 'toString', errorToString);
  }
  return es_error_toString;
}

var es_object_toString = {};

var objectToString;
var hasRequiredObjectToString;
function requireObjectToString() {
  if (hasRequiredObjectToString) return objectToString;
  hasRequiredObjectToString = 1;
  var TO_STRING_TAG_SUPPORT = requireToStringTagSupport();
  var classof = requireClassof();

  // `Object.prototype.toString` method implementation
  // https://tc39.es/ecma262/#sec-object.prototype.tostring
  objectToString = TO_STRING_TAG_SUPPORT ? {}.toString : function toString() {
    return '[object ' + classof(this) + ']';
  };
  return objectToString;
}

var hasRequiredEs_object_toString;
function requireEs_object_toString() {
  if (hasRequiredEs_object_toString) return es_object_toString;
  hasRequiredEs_object_toString = 1;
  var TO_STRING_TAG_SUPPORT = requireToStringTagSupport();
  var defineBuiltIn = requireDefineBuiltIn();
  var toString = requireObjectToString();

  // `Object.prototype.toString` method
  // https://tc39.es/ecma262/#sec-object.prototype.tostring
  if (!TO_STRING_TAG_SUPPORT) {
    defineBuiltIn(Object.prototype, 'toString', toString, {
      unsafe: true
    });
  }
  return es_object_toString;
}

var web_atob = {};

var objectGetOwnPropertyDescriptor = {};

var objectPropertyIsEnumerable = {};

var hasRequiredObjectPropertyIsEnumerable;
function requireObjectPropertyIsEnumerable() {
  if (hasRequiredObjectPropertyIsEnumerable) return objectPropertyIsEnumerable;
  hasRequiredObjectPropertyIsEnumerable = 1;
  var $propertyIsEnumerable = {}.propertyIsEnumerable;
  // eslint-disable-next-line es/no-object-getownpropertydescriptor -- safe
  var getOwnPropertyDescriptor = Object.getOwnPropertyDescriptor;

  // Nashorn ~ JDK8 bug
  var NASHORN_BUG = getOwnPropertyDescriptor && !$propertyIsEnumerable.call({
    1: 2
  }, 1);

  // `Object.prototype.propertyIsEnumerable` method implementation
  // https://tc39.es/ecma262/#sec-object.prototype.propertyisenumerable
  objectPropertyIsEnumerable.f = NASHORN_BUG ? function propertyIsEnumerable(V) {
    var descriptor = getOwnPropertyDescriptor(this, V);
    return !!descriptor && descriptor.enumerable;
  } : $propertyIsEnumerable;
  return objectPropertyIsEnumerable;
}

var indexedObject;
var hasRequiredIndexedObject;
function requireIndexedObject() {
  if (hasRequiredIndexedObject) return indexedObject;
  hasRequiredIndexedObject = 1;
  var uncurryThis = requireFunctionUncurryThis();
  var fails = requireFails();
  var classof = requireClassofRaw();
  var $Object = Object;
  var split = uncurryThis(''.split);

  // fallback for non-array-like ES3 and non-enumerable old V8 strings
  indexedObject = fails(function () {
    // throws an error in rhino, see https://github.com/mozilla/rhino/issues/346
    // eslint-disable-next-line no-prototype-builtins -- safe
    return !$Object('z').propertyIsEnumerable(0);
  }) ? function (it) {
    return classof(it) === 'String' ? split(it, '') : $Object(it);
  } : $Object;
  return indexedObject;
}

var toIndexedObject;
var hasRequiredToIndexedObject;
function requireToIndexedObject() {
  if (hasRequiredToIndexedObject) return toIndexedObject;
  hasRequiredToIndexedObject = 1;
  // toObject with fallback for non-array-like ES3 strings
  var IndexedObject = requireIndexedObject();
  var requireObjectCoercible = requireRequireObjectCoercible();
  toIndexedObject = function toIndexedObject(it) {
    return IndexedObject(requireObjectCoercible(it));
  };
  return toIndexedObject;
}

var hasRequiredObjectGetOwnPropertyDescriptor;
function requireObjectGetOwnPropertyDescriptor() {
  if (hasRequiredObjectGetOwnPropertyDescriptor) return objectGetOwnPropertyDescriptor;
  hasRequiredObjectGetOwnPropertyDescriptor = 1;
  var DESCRIPTORS = requireDescriptors();
  var call = requireFunctionCall();
  var propertyIsEnumerableModule = requireObjectPropertyIsEnumerable();
  var createPropertyDescriptor = requireCreatePropertyDescriptor();
  var toIndexedObject = requireToIndexedObject();
  var toPropertyKey = requireToPropertyKey();
  var hasOwn = requireHasOwnProperty();
  var IE8_DOM_DEFINE = requireIe8DomDefine();

  // eslint-disable-next-line es/no-object-getownpropertydescriptor -- safe
  var $getOwnPropertyDescriptor = Object.getOwnPropertyDescriptor;

  // `Object.getOwnPropertyDescriptor` method
  // https://tc39.es/ecma262/#sec-object.getownpropertydescriptor
  objectGetOwnPropertyDescriptor.f = DESCRIPTORS ? $getOwnPropertyDescriptor : function getOwnPropertyDescriptor(O, P) {
    O = toIndexedObject(O);
    P = toPropertyKey(P);
    if (IE8_DOM_DEFINE) try {
      return $getOwnPropertyDescriptor(O, P);
    } catch (error) {/* empty */}
    if (hasOwn(O, P)) return createPropertyDescriptor(!call(propertyIsEnumerableModule.f, O, P), O[P]);
  };
  return objectGetOwnPropertyDescriptor;
}

var objectGetOwnPropertyNames = {};

var mathTrunc;
var hasRequiredMathTrunc;
function requireMathTrunc() {
  if (hasRequiredMathTrunc) return mathTrunc;
  hasRequiredMathTrunc = 1;
  var ceil = Math.ceil;
  var floor = Math.floor;

  // `Math.trunc` method
  // https://tc39.es/ecma262/#sec-math.trunc
  // eslint-disable-next-line es/no-math-trunc -- safe
  mathTrunc = Math.trunc || function trunc(x) {
    var n = +x;
    return (n > 0 ? floor : ceil)(n);
  };
  return mathTrunc;
}

var toIntegerOrInfinity;
var hasRequiredToIntegerOrInfinity;
function requireToIntegerOrInfinity() {
  if (hasRequiredToIntegerOrInfinity) return toIntegerOrInfinity;
  hasRequiredToIntegerOrInfinity = 1;
  var trunc = requireMathTrunc();

  // `ToIntegerOrInfinity` abstract operation
  // https://tc39.es/ecma262/#sec-tointegerorinfinity
  toIntegerOrInfinity = function toIntegerOrInfinity(argument) {
    var number = +argument;
    // eslint-disable-next-line no-self-compare -- NaN check
    return number !== number || number === 0 ? 0 : trunc(number);
  };
  return toIntegerOrInfinity;
}

var toAbsoluteIndex;
var hasRequiredToAbsoluteIndex;
function requireToAbsoluteIndex() {
  if (hasRequiredToAbsoluteIndex) return toAbsoluteIndex;
  hasRequiredToAbsoluteIndex = 1;
  var toIntegerOrInfinity = requireToIntegerOrInfinity();
  var max = Math.max;
  var min = Math.min;

  // Helper for a popular repeating case of the spec:
  // Let integer be ? ToInteger(index).
  // If integer < 0, let result be max((length + integer), 0); else let result be min(integer, length).
  toAbsoluteIndex = function toAbsoluteIndex(index, length) {
    var integer = toIntegerOrInfinity(index);
    return integer < 0 ? max(integer + length, 0) : min(integer, length);
  };
  return toAbsoluteIndex;
}

var toLength;
var hasRequiredToLength;
function requireToLength() {
  if (hasRequiredToLength) return toLength;
  hasRequiredToLength = 1;
  var toIntegerOrInfinity = requireToIntegerOrInfinity();
  var min = Math.min;

  // `ToLength` abstract operation
  // https://tc39.es/ecma262/#sec-tolength
  toLength = function toLength(argument) {
    var len = toIntegerOrInfinity(argument);
    return len > 0 ? min(len, 0x1FFFFFFFFFFFFF) : 0; // 2 ** 53 - 1 == 9007199254740991
  };
  return toLength;
}

var lengthOfArrayLike;
var hasRequiredLengthOfArrayLike;
function requireLengthOfArrayLike() {
  if (hasRequiredLengthOfArrayLike) return lengthOfArrayLike;
  hasRequiredLengthOfArrayLike = 1;
  var toLength = requireToLength();

  // `LengthOfArrayLike` abstract operation
  // https://tc39.es/ecma262/#sec-lengthofarraylike
  lengthOfArrayLike = function lengthOfArrayLike(obj) {
    return toLength(obj.length);
  };
  return lengthOfArrayLike;
}

var arrayIncludes;
var hasRequiredArrayIncludes;
function requireArrayIncludes() {
  if (hasRequiredArrayIncludes) return arrayIncludes;
  hasRequiredArrayIncludes = 1;
  var toIndexedObject = requireToIndexedObject();
  var toAbsoluteIndex = requireToAbsoluteIndex();
  var lengthOfArrayLike = requireLengthOfArrayLike();

  // `Array.prototype.{ indexOf, includes }` methods implementation
  var createMethod = function createMethod(IS_INCLUDES) {
    return function ($this, el, fromIndex) {
      var O = toIndexedObject($this);
      var length = lengthOfArrayLike(O);
      if (length === 0) return !IS_INCLUDES && -1;
      var index = toAbsoluteIndex(fromIndex, length);
      var value;
      // Array#includes uses SameValueZero equality algorithm
      // eslint-disable-next-line no-self-compare -- NaN check
      if (IS_INCLUDES && el !== el) while (length > index) {
        value = O[index++];
        // eslint-disable-next-line no-self-compare -- NaN check
        if (value !== value) return true;
        // Array#indexOf ignores holes, Array#includes - not
      } else for (; length > index; index++) {
        if ((IS_INCLUDES || index in O) && O[index] === el) return IS_INCLUDES || index || 0;
      }
      return !IS_INCLUDES && -1;
    };
  };
  arrayIncludes = {
    // `Array.prototype.includes` method
    // https://tc39.es/ecma262/#sec-array.prototype.includes
    includes: createMethod(true),
    // `Array.prototype.indexOf` method
    // https://tc39.es/ecma262/#sec-array.prototype.indexof
    indexOf: createMethod(false)
  };
  return arrayIncludes;
}

var objectKeysInternal;
var hasRequiredObjectKeysInternal;
function requireObjectKeysInternal() {
  if (hasRequiredObjectKeysInternal) return objectKeysInternal;
  hasRequiredObjectKeysInternal = 1;
  var uncurryThis = requireFunctionUncurryThis();
  var hasOwn = requireHasOwnProperty();
  var toIndexedObject = requireToIndexedObject();
  var indexOf = requireArrayIncludes().indexOf;
  var hiddenKeys = requireHiddenKeys();
  var push = uncurryThis([].push);
  objectKeysInternal = function objectKeysInternal(object, names) {
    var O = toIndexedObject(object);
    var i = 0;
    var result = [];
    var key;
    for (key in O) !hasOwn(hiddenKeys, key) && hasOwn(O, key) && push(result, key);
    // Don't enum bug & hidden keys
    while (names.length > i) if (hasOwn(O, key = names[i++])) {
      ~indexOf(result, key) || push(result, key);
    }
    return result;
  };
  return objectKeysInternal;
}

var enumBugKeys;
var hasRequiredEnumBugKeys;
function requireEnumBugKeys() {
  if (hasRequiredEnumBugKeys) return enumBugKeys;
  hasRequiredEnumBugKeys = 1;
  // IE8- don't enum bug keys
  enumBugKeys = ['constructor', 'hasOwnProperty', 'isPrototypeOf', 'propertyIsEnumerable', 'toLocaleString', 'toString', 'valueOf'];
  return enumBugKeys;
}

var hasRequiredObjectGetOwnPropertyNames;
function requireObjectGetOwnPropertyNames() {
  if (hasRequiredObjectGetOwnPropertyNames) return objectGetOwnPropertyNames;
  hasRequiredObjectGetOwnPropertyNames = 1;
  var internalObjectKeys = requireObjectKeysInternal();
  var enumBugKeys = requireEnumBugKeys();
  var hiddenKeys = enumBugKeys.concat('length', 'prototype');

  // `Object.getOwnPropertyNames` method
  // https://tc39.es/ecma262/#sec-object.getownpropertynames
  // eslint-disable-next-line es/no-object-getownpropertynames -- safe
  objectGetOwnPropertyNames.f = Object.getOwnPropertyNames || function getOwnPropertyNames(O) {
    return internalObjectKeys(O, hiddenKeys);
  };
  return objectGetOwnPropertyNames;
}

var objectGetOwnPropertySymbols = {};

var hasRequiredObjectGetOwnPropertySymbols;
function requireObjectGetOwnPropertySymbols() {
  if (hasRequiredObjectGetOwnPropertySymbols) return objectGetOwnPropertySymbols;
  hasRequiredObjectGetOwnPropertySymbols = 1;
  // eslint-disable-next-line es/no-object-getownpropertysymbols -- safe
  objectGetOwnPropertySymbols.f = Object.getOwnPropertySymbols;
  return objectGetOwnPropertySymbols;
}

var ownKeys;
var hasRequiredOwnKeys;
function requireOwnKeys() {
  if (hasRequiredOwnKeys) return ownKeys;
  hasRequiredOwnKeys = 1;
  var getBuiltIn = requireGetBuiltIn();
  var uncurryThis = requireFunctionUncurryThis();
  var getOwnPropertyNamesModule = requireObjectGetOwnPropertyNames();
  var getOwnPropertySymbolsModule = requireObjectGetOwnPropertySymbols();
  var anObject = requireAnObject();
  var concat = uncurryThis([].concat);

  // all object keys, includes non-enumerable and symbols
  ownKeys = getBuiltIn('Reflect', 'ownKeys') || function ownKeys(it) {
    var keys = getOwnPropertyNamesModule.f(anObject(it));
    var getOwnPropertySymbols = getOwnPropertySymbolsModule.f;
    return getOwnPropertySymbols ? concat(keys, getOwnPropertySymbols(it)) : keys;
  };
  return ownKeys;
}

var copyConstructorProperties;
var hasRequiredCopyConstructorProperties;
function requireCopyConstructorProperties() {
  if (hasRequiredCopyConstructorProperties) return copyConstructorProperties;
  hasRequiredCopyConstructorProperties = 1;
  var hasOwn = requireHasOwnProperty();
  var ownKeys = requireOwnKeys();
  var getOwnPropertyDescriptorModule = requireObjectGetOwnPropertyDescriptor();
  var definePropertyModule = requireObjectDefineProperty();
  copyConstructorProperties = function copyConstructorProperties(target, source, exceptions) {
    var keys = ownKeys(source);
    var defineProperty = definePropertyModule.f;
    var getOwnPropertyDescriptor = getOwnPropertyDescriptorModule.f;
    for (var i = 0; i < keys.length; i++) {
      var key = keys[i];
      if (!hasOwn(target, key) && !(exceptions && hasOwn(exceptions, key))) {
        defineProperty(target, key, getOwnPropertyDescriptor(source, key));
      }
    }
  };
  return copyConstructorProperties;
}

var isForced_1;
var hasRequiredIsForced;
function requireIsForced() {
  if (hasRequiredIsForced) return isForced_1;
  hasRequiredIsForced = 1;
  var fails = requireFails();
  var isCallable = requireIsCallable();
  var replacement = /#|\.prototype\./;
  var isForced = function isForced(feature, detection) {
    var value = data[normalize(feature)];
    return value === POLYFILL ? true : value === NATIVE ? false : isCallable(detection) ? fails(detection) : !!detection;
  };
  var normalize = isForced.normalize = function (string) {
    return String(string).replace(replacement, '.').toLowerCase();
  };
  var data = isForced.data = {};
  var NATIVE = isForced.NATIVE = 'N';
  var POLYFILL = isForced.POLYFILL = 'P';
  isForced_1 = isForced;
  return isForced_1;
}

var _export;
var hasRequired_export;
function require_export() {
  if (hasRequired_export) return _export;
  hasRequired_export = 1;
  var globalThis = requireGlobalThis();
  var getOwnPropertyDescriptor = requireObjectGetOwnPropertyDescriptor().f;
  var createNonEnumerableProperty = requireCreateNonEnumerableProperty();
  var defineBuiltIn = requireDefineBuiltIn();
  var defineGlobalProperty = requireDefineGlobalProperty();
  var copyConstructorProperties = requireCopyConstructorProperties();
  var isForced = requireIsForced();

  /*
    options.target         - name of the target object
    options.global         - target is the global object
    options.stat           - export as static methods of target
    options.proto          - export as prototype methods of target
    options.real           - real prototype method for the `pure` version
    options.forced         - export even if the native feature is available
    options.bind           - bind methods to the target, required for the `pure` version
    options.wrap           - wrap constructors to preventing global pollution, required for the `pure` version
    options.unsafe         - use the simple assignment of property instead of delete + defineProperty
    options.sham           - add a flag to not completely full polyfills
    options.enumerable     - export as enumerable property
    options.dontCallGetSet - prevent calling a getter on target
    options.name           - the .name of the function if it does not match the key
  */
  _export = function _export(options, source) {
    var TARGET = options.target;
    var GLOBAL = options.global;
    var STATIC = options.stat;
    var FORCED, target, key, targetProperty, sourceProperty, descriptor;
    if (GLOBAL) {
      target = globalThis;
    } else if (STATIC) {
      target = globalThis[TARGET] || defineGlobalProperty(TARGET, {});
    } else {
      target = globalThis[TARGET] && globalThis[TARGET].prototype;
    }
    if (target) for (key in source) {
      sourceProperty = source[key];
      if (options.dontCallGetSet) {
        descriptor = getOwnPropertyDescriptor(target, key);
        targetProperty = descriptor && descriptor.value;
      } else targetProperty = target[key];
      FORCED = isForced(GLOBAL ? key : TARGET + (STATIC ? '.' : '#') + key, options.forced);
      // contained in target
      if (!FORCED && targetProperty !== undefined) {
        if (typeof sourceProperty == typeof targetProperty) continue;
        copyConstructorProperties(sourceProperty, targetProperty);
      }
      // add a flag to not completely full polyfills
      if (options.sham || targetProperty && targetProperty.sham) {
        createNonEnumerableProperty(sourceProperty, 'sham', true);
      }
      defineBuiltIn(target, key, sourceProperty, options);
    }
  };
  return _export;
}

var validateArgumentsLength;
var hasRequiredValidateArgumentsLength;
function requireValidateArgumentsLength() {
  if (hasRequiredValidateArgumentsLength) return validateArgumentsLength;
  hasRequiredValidateArgumentsLength = 1;
  var $TypeError = TypeError;
  validateArgumentsLength = function validateArgumentsLength(passed, required) {
    if (passed < required) throw new $TypeError('Not enough arguments');
    return passed;
  };
  return validateArgumentsLength;
}

var base64Map;
var hasRequiredBase64Map;
function requireBase64Map() {
  if (hasRequiredBase64Map) return base64Map;
  hasRequiredBase64Map = 1;
  var commonAlphabet = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789';
  var base64Alphabet = commonAlphabet + '+/';
  var base64UrlAlphabet = commonAlphabet + '-_';
  var inverse = function inverse(characters) {
    // TODO: use `Object.create(null)` in `core-js@4`
    var result = {};
    var index = 0;
    for (; index < 64; index++) result[characters.charAt(index)] = index;
    return result;
  };
  base64Map = {
    i2c: base64Alphabet,
    c2i: inverse(base64Alphabet),
    i2cUrl: base64UrlAlphabet,
    c2iUrl: inverse(base64UrlAlphabet)
  };
  return base64Map;
}

var hasRequiredWeb_atob;
function requireWeb_atob() {
  if (hasRequiredWeb_atob) return web_atob;
  hasRequiredWeb_atob = 1;
  var $ = require_export();
  var globalThis = requireGlobalThis();
  var getBuiltIn = requireGetBuiltIn();
  var uncurryThis = requireFunctionUncurryThis();
  var call = requireFunctionCall();
  var fails = requireFails();
  var toString = requireToString();
  var validateArgumentsLength = requireValidateArgumentsLength();
  var c2i = requireBase64Map().c2i;
  var disallowed = /[^\d+/a-z]/i;
  var whitespaces = /[\t\n\f\r ]+/g;
  var finalEq = /[=]{1,2}$/;
  var $atob = getBuiltIn('atob');
  var fromCharCode = String.fromCharCode;
  var charAt = uncurryThis(''.charAt);
  var replace = uncurryThis(''.replace);
  var exec = uncurryThis(disallowed.exec);
  var BASIC = !!$atob && !fails(function () {
    return $atob('aGk=') !== 'hi';
  });
  var NO_SPACES_IGNORE = BASIC && fails(function () {
    return $atob(' ') !== '';
  });
  var NO_ENCODING_CHECK = BASIC && !fails(function () {
    $atob('a');
  });
  var NO_ARG_RECEIVING_CHECK = BASIC && !fails(function () {
    $atob();
  });
  var WRONG_ARITY = BASIC && $atob.length !== 1;
  var FORCED = !BASIC || NO_SPACES_IGNORE || NO_ENCODING_CHECK || NO_ARG_RECEIVING_CHECK || WRONG_ARITY;

  // `atob` method
  // https://html.spec.whatwg.org/multipage/webappapis.html#dom-atob
  $({
    global: true,
    bind: true,
    enumerable: true,
    forced: FORCED
  }, {
    atob: function atob(data) {
      validateArgumentsLength(arguments.length, 1);
      // `webpack` dev server bug on IE global methods - use call(fn, global, ...)
      if (BASIC && !NO_SPACES_IGNORE && !NO_ENCODING_CHECK) return call($atob, globalThis, data);
      var string = replace(toString(data), whitespaces, '');
      var output = '';
      var position = 0;
      var bc = 0;
      var length, chr, bs;
      if (string.length % 4 === 0) {
        string = replace(string, finalEq, '');
      }
      length = string.length;
      if (length % 4 === 1 || exec(disallowed, string)) {
        throw new (getBuiltIn('DOMException'))('The string is not correctly encoded', 'InvalidCharacterError');
      }
      while (position < length) {
        chr = charAt(string, position++);
        bs = bc % 4 ? bs * 64 + c2i[chr] : c2i[chr];
        if (bc++ % 4) output += fromCharCode(255 & bs >> (-2 * bc & 6));
      }
      return output;
    }
  });
  return web_atob;
}

var web_domException_constructor = {};

var environment;
var hasRequiredEnvironment;
function requireEnvironment() {
  if (hasRequiredEnvironment) return environment;
  hasRequiredEnvironment = 1;
  /* global Bun, Deno -- detection */
  var globalThis = requireGlobalThis();
  var userAgent = requireEnvironmentUserAgent();
  var classof = requireClassofRaw();
  var userAgentStartsWith = function userAgentStartsWith(string) {
    return userAgent.slice(0, string.length) === string;
  };
  environment = function () {
    if (userAgentStartsWith('Bun/')) return 'BUN';
    if (userAgentStartsWith('Cloudflare-Workers')) return 'CLOUDFLARE';
    if (userAgentStartsWith('Deno/')) return 'DENO';
    if (userAgentStartsWith('Node.js/')) return 'NODE';
    if (globalThis.Bun && typeof Bun.version == 'string') return 'BUN';
    if (globalThis.Deno && typeof Deno.version == 'object') return 'DENO';
    if (classof(globalThis.process) === 'process') return 'NODE';
    if (globalThis.window && globalThis.document) return 'BROWSER';
    return 'REST';
  }();
  return environment;
}

var environmentIsNode;
var hasRequiredEnvironmentIsNode;
function requireEnvironmentIsNode() {
  if (hasRequiredEnvironmentIsNode) return environmentIsNode;
  hasRequiredEnvironmentIsNode = 1;
  var ENVIRONMENT = requireEnvironment();
  environmentIsNode = ENVIRONMENT === 'NODE';
  return environmentIsNode;
}

var getBuiltInNodeModule;
var hasRequiredGetBuiltInNodeModule;
function requireGetBuiltInNodeModule() {
  if (hasRequiredGetBuiltInNodeModule) return getBuiltInNodeModule;
  hasRequiredGetBuiltInNodeModule = 1;
  var globalThis = requireGlobalThis();
  var IS_NODE = requireEnvironmentIsNode();
  getBuiltInNodeModule = function getBuiltInNodeModule(name) {
    if (IS_NODE) {
      try {
        return globalThis.process.getBuiltinModule(name);
      } catch (error) {/* empty */}
      try {
        // eslint-disable-next-line no-new-func -- safe
        return Function('return require("' + name + '")')();
      } catch (error) {/* empty */}
    }
  };
  return getBuiltInNodeModule;
}

var objectDefineProperties = {};

var objectKeys;
var hasRequiredObjectKeys;
function requireObjectKeys() {
  if (hasRequiredObjectKeys) return objectKeys;
  hasRequiredObjectKeys = 1;
  var internalObjectKeys = requireObjectKeysInternal();
  var enumBugKeys = requireEnumBugKeys();

  // `Object.keys` method
  // https://tc39.es/ecma262/#sec-object.keys
  // eslint-disable-next-line es/no-object-keys -- safe
  objectKeys = Object.keys || function keys(O) {
    return internalObjectKeys(O, enumBugKeys);
  };
  return objectKeys;
}

var hasRequiredObjectDefineProperties;
function requireObjectDefineProperties() {
  if (hasRequiredObjectDefineProperties) return objectDefineProperties;
  hasRequiredObjectDefineProperties = 1;
  var DESCRIPTORS = requireDescriptors();
  var V8_PROTOTYPE_DEFINE_BUG = requireV8PrototypeDefineBug();
  var definePropertyModule = requireObjectDefineProperty();
  var anObject = requireAnObject();
  var toIndexedObject = requireToIndexedObject();
  var objectKeys = requireObjectKeys();

  // `Object.defineProperties` method
  // https://tc39.es/ecma262/#sec-object.defineproperties
  // eslint-disable-next-line es/no-object-defineproperties -- safe
  objectDefineProperties.f = DESCRIPTORS && !V8_PROTOTYPE_DEFINE_BUG ? Object.defineProperties : function defineProperties(O, Properties) {
    anObject(O);
    var props = toIndexedObject(Properties);
    var keys = objectKeys(Properties);
    var length = keys.length;
    var index = 0;
    var key;
    while (length > index) definePropertyModule.f(O, key = keys[index++], props[key]);
    return O;
  };
  return objectDefineProperties;
}

var html;
var hasRequiredHtml;
function requireHtml() {
  if (hasRequiredHtml) return html;
  hasRequiredHtml = 1;
  var getBuiltIn = requireGetBuiltIn();
  html = getBuiltIn('document', 'documentElement');
  return html;
}

var objectCreate;
var hasRequiredObjectCreate;
function requireObjectCreate() {
  if (hasRequiredObjectCreate) return objectCreate;
  hasRequiredObjectCreate = 1;
  /* global ActiveXObject -- old IE, WSH */
  var anObject = requireAnObject();
  var definePropertiesModule = requireObjectDefineProperties();
  var enumBugKeys = requireEnumBugKeys();
  var hiddenKeys = requireHiddenKeys();
  var html = requireHtml();
  var documentCreateElement = requireDocumentCreateElement();
  var sharedKey = requireSharedKey();
  var GT = '>';
  var LT = '<';
  var PROTOTYPE = 'prototype';
  var SCRIPT = 'script';
  var IE_PROTO = sharedKey('IE_PROTO');
  var EmptyConstructor = function EmptyConstructor() {/* empty */};
  var scriptTag = function scriptTag(content) {
    return LT + SCRIPT + GT + content + LT + '/' + SCRIPT + GT;
  };

  // Create object with fake `null` prototype: use ActiveX Object with cleared prototype
  var NullProtoObjectViaActiveX = function NullProtoObjectViaActiveX(activeXDocument) {
    activeXDocument.write(scriptTag(''));
    activeXDocument.close();
    var temp = activeXDocument.parentWindow.Object;
    // eslint-disable-next-line no-useless-assignment -- avoid memory leak
    activeXDocument = null;
    return temp;
  };

  // Create object with fake `null` prototype: use iframe Object with cleared prototype
  var NullProtoObjectViaIFrame = function NullProtoObjectViaIFrame() {
    // Thrash, waste and sodomy: IE GC bug
    var iframe = documentCreateElement('iframe');
    var JS = 'java' + SCRIPT + ':';
    var iframeDocument;
    iframe.style.display = 'none';
    html.appendChild(iframe);
    // https://github.com/zloirock/core-js/issues/475
    iframe.src = String(JS);
    iframeDocument = iframe.contentWindow.document;
    iframeDocument.open();
    iframeDocument.write(scriptTag('document.F=Object'));
    iframeDocument.close();
    return iframeDocument.F;
  };

  // Check for document.domain and active x support
  // No need to use active x approach when document.domain is not set
  // see https://github.com/es-shims/es5-shim/issues/150
  // variation of https://github.com/kitcambridge/es5-shim/commit/4f738ac066346
  // avoid IE GC bug
  var activeXDocument;
  var _NullProtoObject = function NullProtoObject() {
    try {
      activeXDocument = new ActiveXObject('htmlfile');
    } catch (error) {/* ignore */}
    _NullProtoObject = typeof document != 'undefined' ? document.domain && activeXDocument ? NullProtoObjectViaActiveX(activeXDocument) // old IE
    : NullProtoObjectViaIFrame() : NullProtoObjectViaActiveX(activeXDocument); // WSH
    var length = enumBugKeys.length;
    while (length--) delete _NullProtoObject[PROTOTYPE][enumBugKeys[length]];
    return _NullProtoObject();
  };
  hiddenKeys[IE_PROTO] = true;

  // `Object.create` method
  // https://tc39.es/ecma262/#sec-object.create
  // eslint-disable-next-line es/no-object-create -- safe
  objectCreate = Object.create || function create(O, Properties) {
    var result;
    if (O !== null) {
      EmptyConstructor[PROTOTYPE] = anObject(O);
      result = new EmptyConstructor();
      EmptyConstructor[PROTOTYPE] = null;
      // add "__proto__" for Object.getPrototypeOf polyfill
      result[IE_PROTO] = O;
    } else result = _NullProtoObject();
    return Properties === undefined ? result : definePropertiesModule.f(result, Properties);
  };
  return objectCreate;
}

var defineBuiltInAccessor;
var hasRequiredDefineBuiltInAccessor;
function requireDefineBuiltInAccessor() {
  if (hasRequiredDefineBuiltInAccessor) return defineBuiltInAccessor;
  hasRequiredDefineBuiltInAccessor = 1;
  var makeBuiltIn = requireMakeBuiltIn();
  var defineProperty = requireObjectDefineProperty();
  defineBuiltInAccessor = function defineBuiltInAccessor(target, name, descriptor) {
    if (descriptor.get) makeBuiltIn(descriptor.get, name, {
      getter: true
    });
    if (descriptor.set) makeBuiltIn(descriptor.set, name, {
      setter: true
    });
    return defineProperty.f(target, name, descriptor);
  };
  return defineBuiltInAccessor;
}

var anInstance;
var hasRequiredAnInstance;
function requireAnInstance() {
  if (hasRequiredAnInstance) return anInstance;
  hasRequiredAnInstance = 1;
  var isPrototypeOf = requireObjectIsPrototypeOf();
  var $TypeError = TypeError;
  anInstance = function anInstance(it, Prototype) {
    if (isPrototypeOf(Prototype, it)) return it;
    throw new $TypeError('Incorrect invocation');
  };
  return anInstance;
}

var domExceptionConstants;
var hasRequiredDomExceptionConstants;
function requireDomExceptionConstants() {
  if (hasRequiredDomExceptionConstants) return domExceptionConstants;
  hasRequiredDomExceptionConstants = 1;
  domExceptionConstants = {
    IndexSizeError: {
      s: 'INDEX_SIZE_ERR',
      c: 1,
      m: 1
    },
    DOMStringSizeError: {
      s: 'DOMSTRING_SIZE_ERR',
      c: 2,
      m: 0
    },
    HierarchyRequestError: {
      s: 'HIERARCHY_REQUEST_ERR',
      c: 3,
      m: 1
    },
    WrongDocumentError: {
      s: 'WRONG_DOCUMENT_ERR',
      c: 4,
      m: 1
    },
    InvalidCharacterError: {
      s: 'INVALID_CHARACTER_ERR',
      c: 5,
      m: 1
    },
    NoDataAllowedError: {
      s: 'NO_DATA_ALLOWED_ERR',
      c: 6,
      m: 0
    },
    NoModificationAllowedError: {
      s: 'NO_MODIFICATION_ALLOWED_ERR',
      c: 7,
      m: 1
    },
    NotFoundError: {
      s: 'NOT_FOUND_ERR',
      c: 8,
      m: 1
    },
    NotSupportedError: {
      s: 'NOT_SUPPORTED_ERR',
      c: 9,
      m: 1
    },
    InUseAttributeError: {
      s: 'INUSE_ATTRIBUTE_ERR',
      c: 10,
      m: 1
    },
    InvalidStateError: {
      s: 'INVALID_STATE_ERR',
      c: 11,
      m: 1
    },
    SyntaxError: {
      s: 'SYNTAX_ERR',
      c: 12,
      m: 1
    },
    InvalidModificationError: {
      s: 'INVALID_MODIFICATION_ERR',
      c: 13,
      m: 1
    },
    NamespaceError: {
      s: 'NAMESPACE_ERR',
      c: 14,
      m: 1
    },
    InvalidAccessError: {
      s: 'INVALID_ACCESS_ERR',
      c: 15,
      m: 1
    },
    ValidationError: {
      s: 'VALIDATION_ERR',
      c: 16,
      m: 0
    },
    TypeMismatchError: {
      s: 'TYPE_MISMATCH_ERR',
      c: 17,
      m: 1
    },
    SecurityError: {
      s: 'SECURITY_ERR',
      c: 18,
      m: 1
    },
    NetworkError: {
      s: 'NETWORK_ERR',
      c: 19,
      m: 1
    },
    AbortError: {
      s: 'ABORT_ERR',
      c: 20,
      m: 1
    },
    URLMismatchError: {
      s: 'URL_MISMATCH_ERR',
      c: 21,
      m: 1
    },
    QuotaExceededError: {
      s: 'QUOTA_EXCEEDED_ERR',
      c: 22,
      m: 1
    },
    TimeoutError: {
      s: 'TIMEOUT_ERR',
      c: 23,
      m: 1
    },
    InvalidNodeTypeError: {
      s: 'INVALID_NODE_TYPE_ERR',
      c: 24,
      m: 1
    },
    DataCloneError: {
      s: 'DATA_CLONE_ERR',
      c: 25,
      m: 1
    }
  };
  return domExceptionConstants;
}

var errorStackClear;
var hasRequiredErrorStackClear;
function requireErrorStackClear() {
  if (hasRequiredErrorStackClear) return errorStackClear;
  hasRequiredErrorStackClear = 1;
  var uncurryThis = requireFunctionUncurryThis();
  var $Error = Error;
  var replace = uncurryThis(''.replace);
  var TEST = function (arg) {
    return String(new $Error(arg).stack);
  }('zxcasd');
  // eslint-disable-next-line redos/no-vulnerable, sonarjs/slow-regex -- safe
  var V8_OR_CHAKRA_STACK_ENTRY = /\n\s*at [^:]*:[^\n]*/;
  var IS_V8_OR_CHAKRA_STACK = V8_OR_CHAKRA_STACK_ENTRY.test(TEST);
  errorStackClear = function errorStackClear(stack, dropEntries) {
    if (IS_V8_OR_CHAKRA_STACK && typeof stack == 'string' && !$Error.prepareStackTrace) {
      while (dropEntries--) stack = replace(stack, V8_OR_CHAKRA_STACK_ENTRY, '');
    }
    return stack;
  };
  return errorStackClear;
}

var hasRequiredWeb_domException_constructor;
function requireWeb_domException_constructor() {
  if (hasRequiredWeb_domException_constructor) return web_domException_constructor;
  hasRequiredWeb_domException_constructor = 1;
  var $ = require_export();
  var getBuiltIn = requireGetBuiltIn();
  var getBuiltInNodeModule = requireGetBuiltInNodeModule();
  var fails = requireFails();
  var create = requireObjectCreate();
  var createPropertyDescriptor = requireCreatePropertyDescriptor();
  var defineProperty = requireObjectDefineProperty().f;
  var defineBuiltIn = requireDefineBuiltIn();
  var defineBuiltInAccessor = requireDefineBuiltInAccessor();
  var hasOwn = requireHasOwnProperty();
  var anInstance = requireAnInstance();
  var anObject = requireAnObject();
  var errorToString = requireErrorToString();
  var normalizeStringArgument = requireNormalizeStringArgument();
  var DOMExceptionConstants = requireDomExceptionConstants();
  var clearErrorStack = requireErrorStackClear();
  var InternalStateModule = requireInternalState();
  var DESCRIPTORS = requireDescriptors();
  var IS_PURE = requireIsPure();
  var DOM_EXCEPTION = 'DOMException';
  var DATA_CLONE_ERR = 'DATA_CLONE_ERR';
  var Error = getBuiltIn('Error');
  // NodeJS < 17.0 does not expose `DOMException` to global
  var NativeDOMException = getBuiltIn(DOM_EXCEPTION) || function () {
    try {
      // NodeJS < 15.0 does not expose `MessageChannel` to global
      var MessageChannel = getBuiltIn('MessageChannel') || getBuiltInNodeModule('worker_threads').MessageChannel;
      // eslint-disable-next-line es/no-weak-map, unicorn/require-post-message-target-origin -- safe
      new MessageChannel().port1.postMessage(new WeakMap());
    } catch (error) {
      if (error.name === DATA_CLONE_ERR && error.code === 25) return error.constructor;
    }
  }();
  var NativeDOMExceptionPrototype = NativeDOMException && NativeDOMException.prototype;
  var ErrorPrototype = Error.prototype;
  var setInternalState = InternalStateModule.set;
  var getInternalState = InternalStateModule.getterFor(DOM_EXCEPTION);
  var HAS_STACK = 'stack' in new Error(DOM_EXCEPTION);
  var codeFor = function codeFor(name) {
    return hasOwn(DOMExceptionConstants, name) && DOMExceptionConstants[name].m ? DOMExceptionConstants[name].c : 0;
  };
  var $DOMException = function DOMException() {
    anInstance(this, DOMExceptionPrototype);
    var argumentsLength = arguments.length;
    var message = normalizeStringArgument(argumentsLength < 1 ? undefined : arguments[0]);
    var name = normalizeStringArgument(argumentsLength < 2 ? undefined : arguments[1], 'Error');
    var code = codeFor(name);
    setInternalState(this, {
      type: DOM_EXCEPTION,
      name: name,
      message: message,
      code: code
    });
    if (!DESCRIPTORS) {
      this.name = name;
      this.message = message;
      this.code = code;
    }
    if (HAS_STACK) {
      var error = new Error(message);
      error.name = DOM_EXCEPTION;
      defineProperty(this, 'stack', createPropertyDescriptor(1, clearErrorStack(error.stack, 1)));
    }
  };
  var DOMExceptionPrototype = $DOMException.prototype = create(ErrorPrototype);
  var createGetterDescriptor = function createGetterDescriptor(get) {
    return {
      enumerable: true,
      configurable: true,
      get: get
    };
  };
  var getterFor = function getterFor(key) {
    return createGetterDescriptor(function () {
      return getInternalState(this)[key];
    });
  };
  if (DESCRIPTORS) {
    // `DOMException.prototype.code` getter
    defineBuiltInAccessor(DOMExceptionPrototype, 'code', getterFor('code'));
    // `DOMException.prototype.message` getter
    defineBuiltInAccessor(DOMExceptionPrototype, 'message', getterFor('message'));
    // `DOMException.prototype.name` getter
    defineBuiltInAccessor(DOMExceptionPrototype, 'name', getterFor('name'));
  }
  defineProperty(DOMExceptionPrototype, 'constructor', createPropertyDescriptor(1, $DOMException));

  // FF36- DOMException is a function, but can't be constructed
  var INCORRECT_CONSTRUCTOR = fails(function () {
    return !(new NativeDOMException() instanceof Error);
  });

  // Safari 10.1 / Chrome 32- / IE8- DOMException.prototype.toString bugs
  var INCORRECT_TO_STRING = INCORRECT_CONSTRUCTOR || fails(function () {
    return ErrorPrototype.toString !== errorToString || String(new NativeDOMException(1, 2)) !== '2: 1';
  });

  // Deno 1.6.3- DOMException.prototype.code just missed
  var INCORRECT_CODE = INCORRECT_CONSTRUCTOR || fails(function () {
    return new NativeDOMException(1, 'DataCloneError').code !== 25;
  });

  // Deno 1.6.3- DOMException constants just missed
  var MISSED_CONSTANTS = INCORRECT_CONSTRUCTOR || NativeDOMException[DATA_CLONE_ERR] !== 25 || NativeDOMExceptionPrototype[DATA_CLONE_ERR] !== 25;
  var FORCED_CONSTRUCTOR = IS_PURE ? INCORRECT_TO_STRING || INCORRECT_CODE || MISSED_CONSTANTS : INCORRECT_CONSTRUCTOR;

  // `DOMException` constructor
  // https://webidl.spec.whatwg.org/#idl-DOMException
  $({
    global: true,
    constructor: true,
    forced: FORCED_CONSTRUCTOR
  }, {
    DOMException: FORCED_CONSTRUCTOR ? $DOMException : NativeDOMException
  });
  var PolyfilledDOMException = getBuiltIn(DOM_EXCEPTION);
  var PolyfilledDOMExceptionPrototype = PolyfilledDOMException.prototype;
  if (INCORRECT_TO_STRING && (IS_PURE || NativeDOMException === PolyfilledDOMException)) {
    defineBuiltIn(PolyfilledDOMExceptionPrototype, 'toString', errorToString);
  }
  if (INCORRECT_CODE && DESCRIPTORS && NativeDOMException === PolyfilledDOMException) {
    defineBuiltInAccessor(PolyfilledDOMExceptionPrototype, 'code', createGetterDescriptor(function () {
      return codeFor(anObject(this).name);
    }));
  }

  // `DOMException` constants
  for (var key in DOMExceptionConstants) if (hasOwn(DOMExceptionConstants, key)) {
    var constant = DOMExceptionConstants[key];
    var constantName = constant.s;
    var descriptor = createPropertyDescriptor(6, constant.c);
    if (!hasOwn(PolyfilledDOMException, constantName)) {
      defineProperty(PolyfilledDOMException, constantName, descriptor);
    }
    if (!hasOwn(PolyfilledDOMExceptionPrototype, constantName)) {
      defineProperty(PolyfilledDOMExceptionPrototype, constantName, descriptor);
    }
  }
  return web_domException_constructor;
}

var web_domException_stack = {};

var functionUncurryThisAccessor;
var hasRequiredFunctionUncurryThisAccessor;
function requireFunctionUncurryThisAccessor() {
  if (hasRequiredFunctionUncurryThisAccessor) return functionUncurryThisAccessor;
  hasRequiredFunctionUncurryThisAccessor = 1;
  var uncurryThis = requireFunctionUncurryThis();
  var aCallable = requireACallable();
  functionUncurryThisAccessor = function functionUncurryThisAccessor(object, key, method) {
    try {
      // eslint-disable-next-line es/no-object-getownpropertydescriptor -- safe
      return uncurryThis(aCallable(Object.getOwnPropertyDescriptor(object, key)[method]));
    } catch (error) {/* empty */}
  };
  return functionUncurryThisAccessor;
}

var isPossiblePrototype;
var hasRequiredIsPossiblePrototype;
function requireIsPossiblePrototype() {
  if (hasRequiredIsPossiblePrototype) return isPossiblePrototype;
  hasRequiredIsPossiblePrototype = 1;
  var isObject = requireIsObject();
  isPossiblePrototype = function isPossiblePrototype(argument) {
    return isObject(argument) || argument === null;
  };
  return isPossiblePrototype;
}

var aPossiblePrototype;
var hasRequiredAPossiblePrototype;
function requireAPossiblePrototype() {
  if (hasRequiredAPossiblePrototype) return aPossiblePrototype;
  hasRequiredAPossiblePrototype = 1;
  var isPossiblePrototype = requireIsPossiblePrototype();
  var $String = String;
  var $TypeError = TypeError;
  aPossiblePrototype = function aPossiblePrototype(argument) {
    if (isPossiblePrototype(argument)) return argument;
    throw new $TypeError("Can't set " + $String(argument) + ' as a prototype');
  };
  return aPossiblePrototype;
}

var objectSetPrototypeOf;
var hasRequiredObjectSetPrototypeOf;
function requireObjectSetPrototypeOf() {
  if (hasRequiredObjectSetPrototypeOf) return objectSetPrototypeOf;
  hasRequiredObjectSetPrototypeOf = 1;
  /* eslint-disable no-proto -- safe */
  var uncurryThisAccessor = requireFunctionUncurryThisAccessor();
  var isObject = requireIsObject();
  var requireObjectCoercible = requireRequireObjectCoercible();
  var aPossiblePrototype = requireAPossiblePrototype();

  // `Object.setPrototypeOf` method
  // https://tc39.es/ecma262/#sec-object.setprototypeof
  // Works with __proto__ only. Old v8 can't work with null proto objects.
  // eslint-disable-next-line es/no-object-setprototypeof -- safe
  objectSetPrototypeOf = Object.setPrototypeOf || ('__proto__' in {} ? function () {
    var CORRECT_SETTER = false;
    var test = {};
    var setter;
    try {
      setter = uncurryThisAccessor(Object.prototype, '__proto__', 'set');
      setter(test, []);
      CORRECT_SETTER = test instanceof Array;
    } catch (error) {/* empty */}
    return function setPrototypeOf(O, proto) {
      requireObjectCoercible(O);
      aPossiblePrototype(proto);
      if (!isObject(O)) return O;
      if (CORRECT_SETTER) setter(O, proto);else O.__proto__ = proto;
      return O;
    };
  }() : undefined);
  return objectSetPrototypeOf;
}

var inheritIfRequired;
var hasRequiredInheritIfRequired;
function requireInheritIfRequired() {
  if (hasRequiredInheritIfRequired) return inheritIfRequired;
  hasRequiredInheritIfRequired = 1;
  var isCallable = requireIsCallable();
  var isObject = requireIsObject();
  var setPrototypeOf = requireObjectSetPrototypeOf();

  // makes subclassing work correct for wrapped built-ins
  inheritIfRequired = function inheritIfRequired($this, dummy, Wrapper) {
    var NewTarget, NewTargetPrototype;
    if (
    // it can work only with native `setPrototypeOf`
    setPrototypeOf &&
    // we haven't completely correct pre-ES6 way for getting `new.target`, so use this
    isCallable(NewTarget = dummy.constructor) && NewTarget !== Wrapper && isObject(NewTargetPrototype = NewTarget.prototype) && NewTargetPrototype !== Wrapper.prototype) setPrototypeOf($this, NewTargetPrototype);
    return $this;
  };
  return inheritIfRequired;
}

var hasRequiredWeb_domException_stack;
function requireWeb_domException_stack() {
  if (hasRequiredWeb_domException_stack) return web_domException_stack;
  hasRequiredWeb_domException_stack = 1;
  var $ = require_export();
  var globalThis = requireGlobalThis();
  var getBuiltIn = requireGetBuiltIn();
  var createPropertyDescriptor = requireCreatePropertyDescriptor();
  var defineProperty = requireObjectDefineProperty().f;
  var hasOwn = requireHasOwnProperty();
  var anInstance = requireAnInstance();
  var inheritIfRequired = requireInheritIfRequired();
  var normalizeStringArgument = requireNormalizeStringArgument();
  var DOMExceptionConstants = requireDomExceptionConstants();
  var clearErrorStack = requireErrorStackClear();
  var DESCRIPTORS = requireDescriptors();
  var IS_PURE = requireIsPure();
  var DOM_EXCEPTION = 'DOMException';
  var Error = getBuiltIn('Error');
  var NativeDOMException = getBuiltIn(DOM_EXCEPTION);
  var $DOMException = function DOMException() {
    anInstance(this, DOMExceptionPrototype);
    var argumentsLength = arguments.length;
    var message = normalizeStringArgument(argumentsLength < 1 ? undefined : arguments[0]);
    var name = normalizeStringArgument(argumentsLength < 2 ? undefined : arguments[1], 'Error');
    var that = new NativeDOMException(message, name);
    var error = new Error(message);
    error.name = DOM_EXCEPTION;
    defineProperty(that, 'stack', createPropertyDescriptor(1, clearErrorStack(error.stack, 1)));
    inheritIfRequired(that, this, $DOMException);
    return that;
  };
  var DOMExceptionPrototype = $DOMException.prototype = NativeDOMException.prototype;
  var ERROR_HAS_STACK = 'stack' in new Error(DOM_EXCEPTION);
  var DOM_EXCEPTION_HAS_STACK = 'stack' in new NativeDOMException(1, 2);

  // eslint-disable-next-line es/no-object-getownpropertydescriptor -- safe
  var descriptor = NativeDOMException && DESCRIPTORS && Object.getOwnPropertyDescriptor(globalThis, DOM_EXCEPTION);

  // Bun ~ 0.1.1 DOMException have incorrect descriptor and we can't redefine it
  // https://github.com/Jarred-Sumner/bun/issues/399
  var BUGGY_DESCRIPTOR = !!descriptor && !(descriptor.writable && descriptor.configurable);
  var FORCED_CONSTRUCTOR = ERROR_HAS_STACK && !BUGGY_DESCRIPTOR && !DOM_EXCEPTION_HAS_STACK;

  // `DOMException` constructor patch for `.stack` where it's required
  // https://webidl.spec.whatwg.org/#es-DOMException-specialness
  $({
    global: true,
    constructor: true,
    forced: IS_PURE || FORCED_CONSTRUCTOR
  }, {
    // TODO: fix export logic
    DOMException: FORCED_CONSTRUCTOR ? $DOMException : NativeDOMException
  });
  var PolyfilledDOMException = getBuiltIn(DOM_EXCEPTION);
  var PolyfilledDOMExceptionPrototype = PolyfilledDOMException.prototype;
  if (PolyfilledDOMExceptionPrototype.constructor !== PolyfilledDOMException) {
    if (!IS_PURE) {
      defineProperty(PolyfilledDOMExceptionPrototype, 'constructor', createPropertyDescriptor(1, PolyfilledDOMException));
    }
    for (var key in DOMExceptionConstants) if (hasOwn(DOMExceptionConstants, key)) {
      var constant = DOMExceptionConstants[key];
      var constantName = constant.s;
      if (!hasOwn(PolyfilledDOMException, constantName)) {
        defineProperty(PolyfilledDOMException, constantName, createPropertyDescriptor(6, constant.c));
      }
    }
  }
  return web_domException_stack;
}

var web_domException_toStringTag = {};

var setToStringTag;
var hasRequiredSetToStringTag;
function requireSetToStringTag() {
  if (hasRequiredSetToStringTag) return setToStringTag;
  hasRequiredSetToStringTag = 1;
  var defineProperty = requireObjectDefineProperty().f;
  var hasOwn = requireHasOwnProperty();
  var wellKnownSymbol = requireWellKnownSymbol();
  var TO_STRING_TAG = wellKnownSymbol('toStringTag');
  setToStringTag = function setToStringTag(target, TAG, STATIC) {
    if (target && !STATIC) target = target.prototype;
    if (target && !hasOwn(target, TO_STRING_TAG)) {
      defineProperty(target, TO_STRING_TAG, {
        configurable: true,
        value: TAG
      });
    }
  };
  return setToStringTag;
}

var hasRequiredWeb_domException_toStringTag;
function requireWeb_domException_toStringTag() {
  if (hasRequiredWeb_domException_toStringTag) return web_domException_toStringTag;
  hasRequiredWeb_domException_toStringTag = 1;
  var getBuiltIn = requireGetBuiltIn();
  var setToStringTag = requireSetToStringTag();
  var DOM_EXCEPTION = 'DOMException';

  // `DOMException.prototype[@@toStringTag]` property
  setToStringTag(getBuiltIn(DOM_EXCEPTION), DOM_EXCEPTION);
  return web_domException_toStringTag;
}

var path$1;
var hasRequiredPath;
function requirePath() {
  if (hasRequiredPath) return path$1;
  hasRequiredPath = 1;
  var globalThis = requireGlobalThis();
  path$1 = globalThis;
  return path$1;
}

var atob$1;
var hasRequiredAtob;
function requireAtob() {
  if (hasRequiredAtob) return atob$1;
  hasRequiredAtob = 1;
  requireEs_error_toString();
  requireEs_object_toString();
  requireWeb_atob();
  requireWeb_domException_constructor();
  requireWeb_domException_stack();
  requireWeb_domException_toStringTag();
  var path = requirePath();
  atob$1 = path.atob;
  return atob$1;
}

requireAtob();

// Adapted from https://github.com/mathiasbynens/he/blob/36afe179392226cf1b6ccdb16ebbb7a5a844d93a/src/he.js#L106-L134
var _a;
var decodeMap = new Map([[0, 65533],
// C1 Unicode control character reference replacements
[128, 8364], [130, 8218], [131, 402], [132, 8222], [133, 8230], [134, 8224], [135, 8225], [136, 710], [137, 8240], [138, 352], [139, 8249], [140, 338], [142, 381], [145, 8216], [146, 8217], [147, 8220], [148, 8221], [149, 8226], [150, 8211], [151, 8212], [152, 732], [153, 8482], [154, 353], [155, 8250], [156, 339], [158, 382], [159, 376]]);
/**
 * Polyfill for `String.fromCodePoint`. It is used to create a string from a Unicode code point.
 */
var fromCodePoint =
// eslint-disable-next-line @typescript-eslint/no-unnecessary-condition, n/no-unsupported-features/es-builtins
(_a = String.fromCodePoint) !== null && _a !== void 0 ? _a : codePoint => {
  var output = "";
  if (codePoint > 65535) {
    codePoint -= 65536;
    output += String.fromCharCode(codePoint >>> 10 & 1023 | 55296);
    codePoint = 56320 | codePoint & 1023;
  }
  output += String.fromCharCode(codePoint);
  return output;
};
/**
 * Replace the given code point with a replacement character if it is a
 * surrogate or is outside the valid range. Otherwise return the code
 * point unchanged.
 */
function replaceCodePoint(codePoint) {
  var _a;
  if (codePoint >= 55296 && codePoint <= 57343 || codePoint > 1114111) {
    return 65533;
  }
  return (_a = decodeMap.get(codePoint)) !== null && _a !== void 0 ? _a : codePoint;
}

/*
 * Shared base64 decode helper for generated decode data.
 * Assumes global atob is available.
 */
function decodeBase64(input) {
  var binary =
  // eslint-disable-next-line n/no-unsupported-features/node-builtins
  typeof atob === "function" ?
  // Browser (and Node >=16)
  // eslint-disable-next-line n/no-unsupported-features/node-builtins
  atob(input) :
  // Older Node versions (<16)
  // eslint-disable-next-line n/no-unsupported-features/node-builtins
  typeof Buffer.from === "function" ?
  // eslint-disable-next-line n/no-unsupported-features/node-builtins
  Buffer.from(input, "base64").toString("binary") :
  // eslint-disable-next-line unicorn/no-new-buffer, n/no-deprecated-api
  new Buffer(input, "base64").toString("binary");
  var evenLength = binary.length & -2; // Round down to even length
  var out = new Uint16Array(evenLength / 2);
  for (var index = 0, outIndex = 0; index < evenLength; index += 2) {
    var lo = binary.charCodeAt(index);
    var hi = binary.charCodeAt(index + 1);
    out[outIndex++] = lo | hi << 8;
  }
  return out;
}

// Generated using scripts/write-decode-map.ts
var htmlDecodeTree = /* #__PURE__ */decodeBase64("");

/**
 * Bit flags & masks for the binary trie encoding used for entity decoding.
 *
 * Bit layout (16 bits total):
 * 15..14 VALUE_LENGTH   (+1 encoding; 0 => no value)
 * 13     FLAG13.        If valueLength>0: semicolon required flag (implicit ';').
 *                       If valueLength==0: compact run flag.
 * 12..7  BRANCH_LENGTH  Branch length (0 => single branch in 6..0 if jumpOffset==char) OR run length (when compact run)
 * 6..0   JUMP_TABLE     Jump offset (jump table) OR single-branch char code OR first run char
 */
var BinTrieFlags;
(function (BinTrieFlags) {
  BinTrieFlags[BinTrieFlags["VALUE_LENGTH"] = 49152] = "VALUE_LENGTH";
  BinTrieFlags[BinTrieFlags["FLAG13"] = 8192] = "FLAG13";
  BinTrieFlags[BinTrieFlags["BRANCH_LENGTH"] = 8064] = "BRANCH_LENGTH";
  BinTrieFlags[BinTrieFlags["JUMP_TABLE"] = 127] = "JUMP_TABLE";
})(BinTrieFlags || (BinTrieFlags = {}));

var CharCodes;
(function (CharCodes) {
  CharCodes[CharCodes["NUM"] = 35] = "NUM";
  CharCodes[CharCodes["SEMI"] = 59] = "SEMI";
  CharCodes[CharCodes["EQUALS"] = 61] = "EQUALS";
  CharCodes[CharCodes["ZERO"] = 48] = "ZERO";
  CharCodes[CharCodes["NINE"] = 57] = "NINE";
  CharCodes[CharCodes["LOWER_A"] = 97] = "LOWER_A";
  CharCodes[CharCodes["LOWER_F"] = 102] = "LOWER_F";
  CharCodes[CharCodes["LOWER_X"] = 120] = "LOWER_X";
  CharCodes[CharCodes["LOWER_Z"] = 122] = "LOWER_Z";
  CharCodes[CharCodes["UPPER_A"] = 65] = "UPPER_A";
  CharCodes[CharCodes["UPPER_F"] = 70] = "UPPER_F";
  CharCodes[CharCodes["UPPER_Z"] = 90] = "UPPER_Z";
})(CharCodes || (CharCodes = {}));
/** Bit that needs to be set to convert an upper case ASCII character to lower case */
var TO_LOWER_BIT = 32;
function isNumber(code) {
  return code >= CharCodes.ZERO && code <= CharCodes.NINE;
}
function isHexadecimalCharacter(code) {
  return code >= CharCodes.UPPER_A && code <= CharCodes.UPPER_F || code >= CharCodes.LOWER_A && code <= CharCodes.LOWER_F;
}
function isAsciiAlphaNumeric(code) {
  return code >= CharCodes.UPPER_A && code <= CharCodes.UPPER_Z || code >= CharCodes.LOWER_A && code <= CharCodes.LOWER_Z || isNumber(code);
}
/**
 * Checks if the given character is a valid end character for an entity in an attribute.
 *
 * Attribute values that aren't terminated properly aren't parsed, and shouldn't lead to a parser error.
 * See the example in https://html.spec.whatwg.org/multipage/parsing.html#named-character-reference-state
 */
function isEntityInAttributeInvalidEnd(code) {
  return code === CharCodes.EQUALS || isAsciiAlphaNumeric(code);
}
var EntityDecoderState;
(function (EntityDecoderState) {
  EntityDecoderState[EntityDecoderState["EntityStart"] = 0] = "EntityStart";
  EntityDecoderState[EntityDecoderState["NumericStart"] = 1] = "NumericStart";
  EntityDecoderState[EntityDecoderState["NumericDecimal"] = 2] = "NumericDecimal";
  EntityDecoderState[EntityDecoderState["NumericHex"] = 3] = "NumericHex";
  EntityDecoderState[EntityDecoderState["NamedEntity"] = 4] = "NamedEntity";
})(EntityDecoderState || (EntityDecoderState = {}));
var DecodingMode;
(function (DecodingMode) {
  /** Entities in text nodes that can end with any character. */
  DecodingMode[DecodingMode["Legacy"] = 0] = "Legacy";
  /** Only allow entities terminated with a semicolon. */
  DecodingMode[DecodingMode["Strict"] = 1] = "Strict";
  /** Entities in attributes have limitations on ending characters. */
  DecodingMode[DecodingMode["Attribute"] = 2] = "Attribute";
})(DecodingMode || (DecodingMode = {}));
/**
 * Token decoder with support of writing partial entities.
 */
var EntityDecoder = /*#__PURE__*/function () {
  function EntityDecoder(/** The tree used to decode entities. */
  // biome-ignore lint/correctness/noUnusedPrivateClassMembers: False positive
  decodeTree,
  /**
   * The function that is called when a codepoint is decoded.
   *
   * For multi-byte named entities, this will be called multiple times,
   * with the second codepoint, and the same `consumed` value.
   *
   * @param codepoint The decoded codepoint.
   * @param consumed The number of bytes consumed by the decoder.
   */
  emitCodePoint, /** An object that is used to produce errors. */
  errors) {
    _classCallCheck(this, EntityDecoder);
    this.decodeTree = decodeTree;
    this.emitCodePoint = emitCodePoint;
    this.errors = errors;
    /** The current state of the decoder. */
    this.state = EntityDecoderState.EntityStart;
    /** Characters that were consumed while parsing an entity. */
    this.consumed = 1;
    /**
     * The result of the entity.
     *
     * Either the result index of a numeric entity, or the codepoint of a
     * numeric entity.
     */
    this.result = 0;
    /** The current index in the decode tree. */
    this.treeIndex = 0;
    /** The number of characters that were consumed in excess. */
    this.excess = 1;
    /** The mode in which the decoder is operating. */
    this.decodeMode = DecodingMode.Strict;
  }
  /** Resets the instance to make it reusable. */
  return _createClass(EntityDecoder, [{
    key: "startEntity",
    value: function startEntity(decodeMode) {
      this.decodeMode = decodeMode;
      this.state = EntityDecoderState.EntityStart;
      this.result = 0;
      this.treeIndex = 0;
      this.excess = 1;
      this.consumed = 1;
    }
    /**
     * Write an entity to the decoder. This can be called multiple times with partial entities.
     * If the entity is incomplete, the decoder will return -1.
     *
     * Mirrors the implementation of `getDecoder`, but with the ability to stop decoding if the
     * entity is incomplete, and resume when the next string is written.
     *
     * @param input The string containing the entity (or a continuation of the entity).
     * @param offset The offset at which the entity begins. Should be 0 if this is not the first call.
     * @returns The number of characters that were consumed, or -1 if the entity is incomplete.
     */
  }, {
    key: "write",
    value: function write(input, offset) {
      switch (this.state) {
        case EntityDecoderState.EntityStart:
          {
            if (input.charCodeAt(offset) === CharCodes.NUM) {
              this.state = EntityDecoderState.NumericStart;
              this.consumed += 1;
              return this.stateNumericStart(input, offset + 1);
            }
            this.state = EntityDecoderState.NamedEntity;
            return this.stateNamedEntity(input, offset);
          }
        case EntityDecoderState.NumericStart:
          {
            return this.stateNumericStart(input, offset);
          }
        case EntityDecoderState.NumericDecimal:
          {
            return this.stateNumericDecimal(input, offset);
          }
        case EntityDecoderState.NumericHex:
          {
            return this.stateNumericHex(input, offset);
          }
        case EntityDecoderState.NamedEntity:
          {
            return this.stateNamedEntity(input, offset);
          }
      }
    }
    /**
     * Switches between the numeric decimal and hexadecimal states.
     *
     * Equivalent to the `Numeric character reference state` in the HTML spec.
     *
     * @param input The string containing the entity (or a continuation of the entity).
     * @param offset The current offset.
     * @returns The number of characters that were consumed, or -1 if the entity is incomplete.
     */
  }, {
    key: "stateNumericStart",
    value: function stateNumericStart(input, offset) {
      if (offset >= input.length) {
        return -1;
      }
      if ((input.charCodeAt(offset) | TO_LOWER_BIT) === CharCodes.LOWER_X) {
        this.state = EntityDecoderState.NumericHex;
        this.consumed += 1;
        return this.stateNumericHex(input, offset + 1);
      }
      this.state = EntityDecoderState.NumericDecimal;
      return this.stateNumericDecimal(input, offset);
    }
    /**
     * Parses a hexadecimal numeric entity.
     *
     * Equivalent to the `Hexademical character reference state` in the HTML spec.
     *
     * @param input The string containing the entity (or a continuation of the entity).
     * @param offset The current offset.
     * @returns The number of characters that were consumed, or -1 if the entity is incomplete.
     */
  }, {
    key: "stateNumericHex",
    value: function stateNumericHex(input, offset) {
      while (offset < input.length) {
        var char = input.charCodeAt(offset);
        if (isNumber(char) || isHexadecimalCharacter(char)) {
          // Convert hex digit to value (0-15); 'a'/'A' -> 10.
          var digit = char <= CharCodes.NINE ? char - CharCodes.ZERO : (char | TO_LOWER_BIT) - CharCodes.LOWER_A + 10;
          this.result = this.result * 16 + digit;
          this.consumed++;
          offset++;
        } else {
          return this.emitNumericEntity(char, 3);
        }
      }
      return -1; // Incomplete entity
    }
    /**
     * Parses a decimal numeric entity.
     *
     * Equivalent to the `Decimal character reference state` in the HTML spec.
     *
     * @param input The string containing the entity (or a continuation of the entity).
     * @param offset The current offset.
     * @returns The number of characters that were consumed, or -1 if the entity is incomplete.
     */
  }, {
    key: "stateNumericDecimal",
    value: function stateNumericDecimal(input, offset) {
      while (offset < input.length) {
        var char = input.charCodeAt(offset);
        if (isNumber(char)) {
          this.result = this.result * 10 + (char - CharCodes.ZERO);
          this.consumed++;
          offset++;
        } else {
          return this.emitNumericEntity(char, 2);
        }
      }
      return -1; // Incomplete entity
    }
    /**
     * Validate and emit a numeric entity.
     *
     * Implements the logic from the `Hexademical character reference start
     * state` and `Numeric character reference end state` in the HTML spec.
     *
     * @param lastCp The last code point of the entity. Used to see if the
     *               entity was terminated with a semicolon.
     * @param expectedLength The minimum number of characters that should be
     *                       consumed. Used to validate that at least one digit
     *                       was consumed.
     * @returns The number of characters that were consumed.
     */
  }, {
    key: "emitNumericEntity",
    value: function emitNumericEntity(lastCp, expectedLength) {
      var _a;
      // Ensure we consumed at least one digit.
      if (this.consumed <= expectedLength) {
        (_a = this.errors) === null || _a === void 0 ? void 0 : _a.absenceOfDigitsInNumericCharacterReference(this.consumed);
        return 0;
      }
      // Figure out if this is a legit end of the entity
      if (lastCp === CharCodes.SEMI) {
        this.consumed += 1;
      } else if (this.decodeMode === DecodingMode.Strict) {
        return 0;
      }
      this.emitCodePoint(replaceCodePoint(this.result), this.consumed);
      if (this.errors) {
        if (lastCp !== CharCodes.SEMI) {
          this.errors.missingSemicolonAfterCharacterReference();
        }
        this.errors.validateNumericCharacterReference(this.result);
      }
      return this.consumed;
    }
    /**
     * Parses a named entity.
     *
     * Equivalent to the `Named character reference state` in the HTML spec.
     *
     * @param input The string containing the entity (or a continuation of the entity).
     * @param offset The current offset.
     * @returns The number of characters that were consumed, or -1 if the entity is incomplete.
     */
  }, {
    key: "stateNamedEntity",
    value: function stateNamedEntity(input, offset) {
      var decodeTree = this.decodeTree;
      var current = decodeTree[this.treeIndex];
      // The length is the number of bytes of the value, including the current byte.
      var valueLength = (current & BinTrieFlags.VALUE_LENGTH) >> 14;
      while (offset < input.length) {
        // Handle compact runs (possibly inline): valueLength == 0 and SEMI_REQUIRED bit set.
        if (valueLength === 0 && (current & BinTrieFlags.FLAG13) !== 0) {
          var runLength = (current & BinTrieFlags.BRANCH_LENGTH) >> 7; /* 2..63 */
          var firstChar = current & BinTrieFlags.JUMP_TABLE;
          // Fast-fail if we don't have enough remaining input for the full run (incomplete entity)
          if (offset + runLength > input.length) return -1;
          // Verify first char
          if (input.charCodeAt(offset) !== firstChar) {
            return this.result === 0 ? 0 : this.emitNotTerminatedNamedEntity();
          }
          offset++;
          this.excess++;
          // Remaining characters after the first
          var remaining = runLength - 1;
          // Iterate over packed 2-char words
          for (var runPos = 1; runPos < runLength; runPos += 2) {
            var packedWord = decodeTree[this.treeIndex + 1 + (runPos - 1 >> 1)];
            var low = packedWord & 0xff;
            if (input.charCodeAt(offset) !== low) {
              return this.result === 0 ? 0 : this.emitNotTerminatedNamedEntity();
            }
            offset++;
            this.excess++;
            var high = packedWord >> 8 & 0xff;
            if (runPos + 1 < runLength) {
              if (input.charCodeAt(offset) !== high) {
                return this.result === 0 ? 0 : this.emitNotTerminatedNamedEntity();
              }
              offset++;
              this.excess++;
            }
          }
          this.treeIndex += 1 + (remaining + 1 >> 1);
          current = decodeTree[this.treeIndex];
          valueLength = (current & BinTrieFlags.VALUE_LENGTH) >> 14;
        }
        if (offset >= input.length) break;
        var char = input.charCodeAt(offset);
        /*
         * Implicit semicolon handling for nodes that require a semicolon but
         * don't have an explicit ';' branch stored in the trie. If we have
         * a value on the current node, it requires a semicolon, and the
         * current input character is a semicolon, emit the entity using the
         * current node (without descending further).
         */
        if (char === CharCodes.SEMI && valueLength !== 0 && (current & BinTrieFlags.FLAG13) !== 0) {
          return this.emitNamedEntityData(this.treeIndex, valueLength, this.consumed + this.excess);
        }
        this.treeIndex = determineBranch(decodeTree, current, this.treeIndex + Math.max(1, valueLength), char);
        if (this.treeIndex < 0) {
          return this.result === 0 ||
          // If we are parsing an attribute
          this.decodeMode === DecodingMode.Attribute && (
          // We shouldn't have consumed any characters after the entity,
          valueLength === 0 ||
          // And there should be no invalid characters.
          isEntityInAttributeInvalidEnd(char)) ? 0 : this.emitNotTerminatedNamedEntity();
        }
        current = decodeTree[this.treeIndex];
        valueLength = (current & BinTrieFlags.VALUE_LENGTH) >> 14;
        // If the branch is a value, store it and continue
        if (valueLength !== 0) {
          // If the entity is terminated by a semicolon, we are done.
          if (char === CharCodes.SEMI) {
            return this.emitNamedEntityData(this.treeIndex, valueLength, this.consumed + this.excess);
          }
          // If we encounter a non-terminated (legacy) entity while parsing strictly, then ignore it.
          if (this.decodeMode !== DecodingMode.Strict && (current & BinTrieFlags.FLAG13) === 0) {
            this.result = this.treeIndex;
            this.consumed += this.excess;
            this.excess = 0;
          }
        }
        // Increment offset & excess for next iteration
        offset++;
        this.excess++;
      }
      return -1;
    }
    /**
     * Emit a named entity that was not terminated with a semicolon.
     *
     * @returns The number of characters consumed.
     */
  }, {
    key: "emitNotTerminatedNamedEntity",
    value: function emitNotTerminatedNamedEntity() {
      var _a;
      var result = this.result,
        decodeTree = this.decodeTree;
      var valueLength = (decodeTree[result] & BinTrieFlags.VALUE_LENGTH) >> 14;
      this.emitNamedEntityData(result, valueLength, this.consumed);
      (_a = this.errors) === null || _a === void 0 ? void 0 : _a.missingSemicolonAfterCharacterReference();
      return this.consumed;
    }
    /**
     * Emit a named entity.
     *
     * @param result The index of the entity in the decode tree.
     * @param valueLength The number of bytes in the entity.
     * @param consumed The number of characters consumed.
     *
     * @returns The number of characters consumed.
     */
  }, {
    key: "emitNamedEntityData",
    value: function emitNamedEntityData(result, valueLength, consumed) {
      var decodeTree = this.decodeTree;
      this.emitCodePoint(valueLength === 1 ? decodeTree[result] & ~(BinTrieFlags.VALUE_LENGTH | BinTrieFlags.FLAG13) : decodeTree[result + 1], consumed);
      if (valueLength === 3) {
        // For multi-byte values, we need to emit the second byte.
        this.emitCodePoint(decodeTree[result + 2], consumed);
      }
      return consumed;
    }
    /**
     * Signal to the parser that the end of the input was reached.
     *
     * Remaining data will be emitted and relevant errors will be produced.
     *
     * @returns The number of characters consumed.
     */
  }, {
    key: "end",
    value: function end() {
      var _a;
      switch (this.state) {
        case EntityDecoderState.NamedEntity:
          {
            // Emit a named entity if we have one.
            return this.result !== 0 && (this.decodeMode !== DecodingMode.Attribute || this.result === this.treeIndex) ? this.emitNotTerminatedNamedEntity() : 0;
          }
        // Otherwise, emit a numeric entity if we have one.
        case EntityDecoderState.NumericDecimal:
          {
            return this.emitNumericEntity(0, 2);
          }
        case EntityDecoderState.NumericHex:
          {
            return this.emitNumericEntity(0, 3);
          }
        case EntityDecoderState.NumericStart:
          {
            (_a = this.errors) === null || _a === void 0 ? void 0 : _a.absenceOfDigitsInNumericCharacterReference(this.consumed);
            return 0;
          }
        case EntityDecoderState.EntityStart:
          {
            // Return 0 if we have no entity.
            return 0;
          }
      }
    }
  }]);
}();
/**
 * Creates a function that decodes entities in a string.
 *
 * @param decodeTree The decode tree.
 * @returns A function that decodes entities in a string.
 */
function getDecoder(decodeTree) {
  var returnValue = "";
  var decoder = new EntityDecoder(decodeTree, data => returnValue += fromCodePoint(data));
  return function decodeWithTrie(input, decodeMode) {
    var lastIndex = 0;
    var offset = 0;
    while ((offset = input.indexOf("&", offset)) >= 0) {
      returnValue += input.slice(lastIndex, offset);
      decoder.startEntity(decodeMode);
      var length = decoder.write(input,
      // Skip the "&"
      offset + 1);
      if (length < 0) {
        lastIndex = offset + decoder.end();
        break;
      }
      lastIndex = offset + length;
      // If `length` is 0, skip the current `&` and continue.
      offset = length === 0 ? lastIndex + 1 : lastIndex;
    }
    var result = returnValue + input.slice(lastIndex);
    // Make sure we don't keep a reference to the final string.
    returnValue = "";
    return result;
  };
}
/**
 * Determines the branch of the current node that is taken given the current
 * character. This function is used to traverse the trie.
 *
 * @param decodeTree The trie.
 * @param current The current node.
 * @param nodeIdx The index right after the current node and its value.
 * @param char The current character.
 * @returns The index of the next node, or -1 if no branch is taken.
 */
function determineBranch(decodeTree, current, nodeIndex, char) {
  var branchCount = (current & BinTrieFlags.BRANCH_LENGTH) >> 7;
  var jumpOffset = current & BinTrieFlags.JUMP_TABLE;
  // Case 1: Single branch encoded in jump offset
  if (branchCount === 0) {
    return jumpOffset !== 0 && char === jumpOffset ? nodeIndex : -1;
  }
  // Case 2: Multiple branches encoded in jump table
  if (jumpOffset) {
    var value = char - jumpOffset;
    return value < 0 || value >= branchCount ? -1 : decodeTree[nodeIndex + value] - 1;
  }
  // Case 3: Multiple branches encoded in packed dictionary (two keys per uint16)
  var packedKeySlots = branchCount + 1 >> 1;
  /*
   * Treat packed keys as a virtual sorted array of length `branchCount`.
   * Key(i) = low byte for even i, high byte for odd i in slot i>>1.
   */
  var lo = 0;
  var hi = branchCount - 1;
  while (lo <= hi) {
    var mid = lo + hi >>> 1;
    var slot = mid >> 1;
    var packed = decodeTree[nodeIndex + slot];
    var midKey = packed >> (mid & 1) * 8 & 0xff;
    if (midKey < char) {
      lo = mid + 1;
    } else if (midKey > char) {
      hi = mid - 1;
    } else {
      return decodeTree[nodeIndex + packedKeySlots + mid];
    }
  }
  return -1;
}
var htmlDecoder = /* #__PURE__ */getDecoder(htmlDecodeTree);
/**
 * Decodes an HTML string.
 *
 * @param htmlString The string to decode.
 * @param mode The decoding mode.
 * @returns The decoded string.
 */
function decodeHTML(htmlString) {
  var mode = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : DecodingMode.Legacy;
  return htmlDecoder(htmlString, mode);
}

var clanIdCache = {};
var toPlayerId = player => typeof player === "string" ? kolmafia.getPlayerId(player) : player;
var LOG_FAX_PATTERN = /(\d{2}\/\d{2}\/\d{2}, \d{2}:\d{2}(?:AM|PM): )<a [^>]+>([^<]+)<\/a>(?: faxed in a (?<monster>.*?))<br>/;
var WHITELIST_DEGREE_PATTERN = /(?<name>.*?) \(°(?<degree>\d+)\)/;
var Clan = /*#__PURE__*/function () {
  function Clan(id, name) {
    _classCallCheck(this, Clan);
    _defineProperty(this, "id", void 0);
    _defineProperty(this, "name", void 0);
    this.id = id;
    this.name = name;
  }
  return _createClass(Clan, [{
    key: "_check",
    value: function _check() {
      if (this.id !== kolmafia.getClanId()) {
        throw new Error("You are no longer a member of this clan");
      }
    }
    /**
     * Join clan
     *
     * @returns Joined clan
     */
  }, {
    key: "join",
    value: function join() {
      return Clan.join(this.id);
    }
    /**
     * Check that this clan is the player's current clan
     *
     * @returns Whether this is the current clan
     */
  }, {
    key: "check",
    value: function check() {
      return kolmafia.visitUrl("clan_hall.php").includes(`<b>${this.name}</b>`);
    }
    /**
     * Determine the monster that is currently in the current clan's fax machine if any
     *
     * @returns The current fax monster
     */
  }, {
    key: "getCurrentFax",
    value: function getCurrentFax() {
      this._check();
      var logs = kolmafia.visitUrl("clan_log.php");
      var lastFax = logs.match(LOG_FAX_PATTERN);
      if (!lastFax) return null;
      var _lastFax = _slicedToArray(lastFax, 4),
        monsterName = _lastFax[3];
      if (!monsterName) return null;
      return kolmafia.Monster.get(monsterName);
    }
    /**
     * List available ranks (name, degree and id) from the current clan
     *
     * @returns List of ranks
     */
  }, {
    key: "getRanks",
    value: function getRanks() {
      this._check();
      var page = kolmafia.visitUrl("clan_whitelist.php");
      return kolmafia.xpath(page, '//select[@name="level"]//option').map(option => {
        var validHtml = `<select>${option}</select>`;
        var match = kolmafia.xpath(validHtml, "//text()")[0].match(WHITELIST_DEGREE_PATTERN);
        var id = kolmafia.xpath(validHtml, "//@value")[0];
        if (!match || !id) return null;
        var _match = _slicedToArray(match, 3),
          encodedName = _match[1],
          degree = _match[2];
        return {
          name: decodeHTML(encodedName),
          degree: Number.parseInt(degree),
          id: Number.parseInt(id)
        };
      }).filter(notNull);
    }
    /**
     * Add a player to the current clan's whitelist.
     * If the player is already in the whitelist this will change their rank or title.
     *
     * @param player Player id or name
     * @param rankName Rank to give the player. If not provided they will be given the lowest rank
     * @param title Title to give the player. If not provided, will be blank
     * @returns Success
     */
  }, {
    key: "addPlayerToWhitelist",
    value: function addPlayerToWhitelist(player, rankName) {
      var title = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : "";
      this._check();
      var playerId = toPlayerId(player);
      var ranks = this.getRanks();
      var rank = rankName ? ranks.find(r => r.name === rankName) : ranks.sort((a, b) => a.degree - b.degree)[0];
      if (!rank) return false;
      var result = kolmafia.visitUrl(`clan_whitelist.php?action=add&pwd&addwho=${playerId}&level=${rank.id}&title=${title}`);
      return result.includes("added to whitelist.") || result.includes("That player is already on the whitelist");
    }
    /**
     * Remove a player from the current clan's whitelist
     *
     * @param player Player id or name
     * @returns Success
     */
  }, {
    key: "removePlayerFromWhitelist",
    value: function removePlayerFromWhitelist(player) {
      this._check();
      var playerId = toPlayerId(player);
      var result = kolmafia.visitUrl(`clan_whitelist.php?action=updatewl&pwd&who=${playerId}&remove=Remove`);
      return result.includes("Whitelist updated.");
    }
    /**
     * Return the amount of meat in the current clan's coffer
     *
     * @returns Amount of meat
     */
  }, {
    key: "getMeatInCoffer",
    value: function getMeatInCoffer() {
      this._check();
      var page = kolmafia.visitUrl("clan_stash.php");
      var _ref = page.match(/Your <b>Clan Coffer<\/b> contains ([\d,]+) Meat./) || ["0", "0"],
        _ref2 = _slicedToArray(_ref, 2),
        meat = _ref2[1];
      return parseNumber(meat);
    }
    /**
     * Add the given amount of meat to the current clan's coffer.
     *
     * @param amount Amount of meat to put in coffer
     * @returns Success
     */
  }, {
    key: "putMeatInCoffer",
    value: function putMeatInCoffer(amount) {
      this._check();
      var result = kolmafia.visitUrl(`clan_stash.php?pwd&action=contribute&howmuch=${amount}`);
      return result.includes("You contributed");
    }
  }, {
    key: "take",
    value: function take(items) {
      this._check();
      var map = arrayToCountedMap(items);
      map.forEach((quantity, item) => {
        var needed = Math.max(0, quantity - kolmafia.availableAmount(item));
        if (needed === 0) {
          return map.set(item, 0);
        }
        var foldGroup = getFoldGroup(item);
        var _iterator = _createForOfIteratorHelper(foldGroup),
          _step;
        try {
          for (_iterator.s(); !(_step = _iterator.n()).done;) {
            var foldable = _step.value;
            var quantityToFold = Math.min(needed, kolmafia.availableAmount(foldable));
            for (var _i2 = 0; _i2 < quantityToFold; _i2++) {
              kolmafia.cliExecute(`fold ${item.name}`);
              needed--;
            }
            return map.set(item, needed);
          }
        } catch (err) {
          _iterator.e(err);
        } finally {
          _iterator.f();
        }
        kolmafia.refreshStash();
        for (var _i = 0, _arr = [item].concat(_toConsumableArray(foldGroup)); _i < _arr.length; _i++) {
          var matchingItem = _arr[_i];
          var quantityToTake = Math.min(needed, kolmafia.stashAmount(matchingItem));
          if (quantityToTake === 0) continue;
          // If we can't take from the stash, there's no sense in iterating through the whole fold group
          if (!kolmafia.takeStash(quantityToTake, matchingItem)) return;
          if (matchingItem === item) {
            needed -= quantityToTake;
          } else {
            for (var i = 0; i < quantityToTake; i++) {
              kolmafia.cliExecute(`fold ${matchingItem.name}`);
              needed--;
            }
          }
        }
      });
      return Array.isArray(items) ? countedMapToArray(map) : map;
    }
  }, {
    key: "put",
    value: function put(items) {
      this._check();
      var map = arrayToCountedMap(items);
      if (!this.check()) throw new Error(`Wanted to return ${countedMapToString(map)} to ${this.name} but KoLmafia's clan data is out of sync`);
      map.forEach((quantity, item) => {
        kolmafia.retrieveItem(quantity, item);
        var returned = Math.min(quantity, kolmafia.availableAmount(item));
        kolmafia.putStash(returned, item);
        map.set(item, quantity - returned);
      });
      return Array.isArray(items) ? countedMapToArray(map) : map;
    }
  }, {
    key: "withStash",
    value: function withStash(items, callback) {
      this._check();
      var map = arrayToCountedMap(items);
      return Clan._withStash(() => this.take(map), borrowed => this.put(borrowed), callback);
    }
  }], [{
    key: "_join",
    value: function _join(id) {
      var result = kolmafia.visitUrl(`showclan.php?recruiter=1&whichclan=${id}&pwd&whichclan=${id}&action=joinclan&apply=Apply+to+this+Clan&confirm=on`);
      if (!result.includes("clanhalltop.gif")) {
        throw new Error("Could not join clan");
      }
      return Clan.get();
    }
  }, {
    key: "_withStash",
    value: function _withStash(borrowFn,
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    returnFn,
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    callback) {
      var borrowed = borrowFn();
      var map = arrayToCountedMap(borrowed);
      try {
        return callback(borrowed);
      } finally {
        if (map.size > 0) {
          var returned = arrayToCountedMap(returnFn(borrowed));
          map.forEach((quantity, item) => {
            var remaining = quantity - (returned.get(item) || 0);
            if (remaining > 0) {
              map.set(item, remaining);
            } else {
              map.delete(item);
            }
          });
          if (map.size > 0) {
            logger.error(`Failed to return <b>${countedMapToString(map)}</b> to <b>${this.name}</b> stash`);
          }
        }
      }
    }
    /**
     * Join a clan
     *
     * @param clanIdOrName Clan id or name
     * @returns Instance of joined clan
     */
  }, {
    key: "join",
    value: function join(clanIdOrName) {
      var clanId;
      if (typeof clanIdOrName === "string") {
        var clanName = clanIdOrName.toLowerCase();
        if (clanName === kolmafia.getClanName().toLowerCase()) {
          return Clan.get();
        }
        if (!(clanName in clanIdCache)) {
          var clan = Clan.getWhitelisted().find(c => c.name.toLowerCase() === clanName);
          if (!clan) {
            throw new Error(`Player is not whitelisted to clan '${clanIdOrName}'`);
          }
          clanIdCache[clanName] = clan.id;
        }
        clanId = clanIdCache[clanName];
      } else {
        clanId = clanIdOrName;
        if (clanId === kolmafia.getClanId()) {
          return Clan.get();
        }
      }
      return Clan._join(clanId);
    }
    /**
     * Execute callback as a member of a clan and then restore prior membership
     *
     * @param clanIdOrName Clan id or name
     * @param callback Actions to carry out while member of specified can
     * @returns Return value from callback
     */
  }, {
    key: "with",
    value: function _with(clanIdOrName, callback) {
      var startingClan = Clan.get();
      var clan = Clan.join(clanIdOrName);
      try {
        return callback(clan);
      } finally {
        startingClan.join();
      }
    }
  }, {
    key: "withStash",
    value: function withStash(clanIdOrName, items,
    // eslint-disable-line @typescript-eslint/no-explicit-any
    callback) {
      return Clan._withStash(() => Clan.with(clanIdOrName, clan => clan.take(items)), borrowed => Clan.with(clanIdOrName, clan => clan.put(borrowed)), callback);
    }
    /**
     * Get the player's current clan
     *
     * @returns Player's clan
     */
  }, {
    key: "get",
    value: function get() {
      return new Clan(kolmafia.getClanId(), kolmafia.getClanName());
    }
    /**
     * Get list of clans to which the player is whitelisted
     *
     * @returns List of clans
     */
  }, {
    key: "getWhitelisted",
    value: function getWhitelisted() {
      var page = kolmafia.visitUrl("clan_signup.php");
      return kolmafia.xpath(page, '//select[@name="whichclan"]//option').map(option => {
        var validHtml = `<select>${option}</select>`;
        var id = Number.parseInt(kolmafia.xpath(validHtml, "//@value")[0]);
        var name = decodeHTML(kolmafia.xpath(validHtml, "//text()")[0]);
        return new Clan(id, name);
      });
    }
  }]);
}();

/**
 * Cast Rain Man and fight the target monster
 * @param target the monster to fight
 */
function rainMan(target) {
  if (canRainMan(target)) {
    withChoice(970, `1&whichmonster=${target.id}`, () => kolmafia.useSkill($skill`Rain Man`));
  }
}
/**
 * Check if you can summon this monster using Rain Man
 * @param target the monster to attempt to summon
 * @returns true if you can summon the target monster, false otherwise
 */
function canRainMan(target) {
  if (!have$P($skill`Rain Man`) ||
  // having the skill implies you are in heavy rains path
  kolmafia.myRain() < 50 || !target.copyable || target.id < 0) {
    return false;
  }
  if (kolmafia.monsterFactoidsAvailable(target, false) > 0) {
    return true;
  }
  var page = withChoice(970, 2, () => kolmafia.visitUrl(`runskillz.php?pwd&action=Skillz&whichskill=${$skill`Rain Man`.id}&quantity=1`));
  return page.indexOf(`<option value=${target.id}>`) > 0;
}
var wanderers = Object.freeze(kolmafia.Monster.get(["giant isopod", "gourmet gourami", "freshwater bonefish", "alley catfish", "piranhadon", "giant tardigrade", "aquaconda", "storm cow"]));
var path = $path`Heavy Rains`;

var Not = /*#__PURE__*/_createClass(function Not(thing) {
  _classCallCheck(this, Not);
  _defineProperty(this, "thing", void 0);
  this.thing = thing;
});
/**
 * Assuming list is already sorted, count adjacent items.
 * Effectively run-length encoding.
 *
 * @param list List to count
 * @param isEqual Function that determines item equality
 * @returns List of 2-tuples relating item to count
 */
function aggregate(list, isEqual) {
  var aggregatedList = [];
  var _iterator = _createForOfIteratorHelper(list),
    _step;
  try {
    for (_iterator.s(); !(_step = _iterator.n()).done;) {
      var item = _step.value;
      if (aggregatedList.length === 0) {
        aggregatedList.push([item, 1]);
      } else {
        var last = aggregatedList[aggregatedList.length - 1];
        var _last = _slicedToArray(last, 1),
          lastItem = _last[0];
        if (isEqual ? isEqual(item, lastItem) : item === lastItem) {
          last[1]++;
        } else {
          aggregatedList.push([item, 1]);
        }
      }
    }
  } catch (err) {
    _iterator.e(err);
  } finally {
    _iterator.f();
  }
  return aggregatedList;
}
/**
 * Solve the knapsack problem.
 *
 * @param values Array of {[item, value, weight, maximum]} tuples for knapsack parameter.
 * @param capacity Capacity of knapsack.
 * @returns Tuple {[totalValue, items]} of selected items and total value of those items.
 */
function knapsack(values, capacity) {
  var _ref5;
  if (!Number.isFinite(capacity)) {
    throw new Error("Invalid capacity.");
  }
  // Invert negative values into a fake value for not using it.
  var valuesInverted = values.map(_ref => {
    var _ref2 = _slicedToArray(_ref, 4),
      thing = _ref2[0],
      value = _ref2[1],
      weight = _ref2[2],
      maximum = _ref2[3];
    return weight < 0 && maximum !== undefined ? [new Not(thing), -value, -weight, maximum] : [thing, value, weight, maximum];
  });
  var capacityAdjustment = sum(values, _ref3 => {
    var _ref4 = _slicedToArray(_ref3, 4),
      weight = _ref4[2],
      maximum = _ref4[3];
    return weight < 0 && maximum !== undefined ? -weight * maximum : 0;
  });
  var adjustedCapacity = capacity + capacityAdjustment;
  if (adjustedCapacity < 0) {
    // We don't have enough cleaners to create any space, so can't fit anything.
    return [-Infinity, []];
  }
  // Sort values by weight.
  var valuesSorted = _toConsumableArray(valuesInverted).sort((x, y) => x[2] - y[2]);
  // Convert the problem into 0/1 knapsack - just include as many copies as possible of each item.
  var values01 = (_ref5 = []).concat.apply(_ref5, _toConsumableArray(valuesSorted.map(_ref6 => {
    var _ref7 = _slicedToArray(_ref6, 4),
      thing = _ref7[0],
      value = _ref7[1],
      weight = _ref7[2],
      maximum = _ref7[3];
    if (!Number.isFinite(weight) || weight < 0) {
      throw new Error(`Invalid weight ${weight} for ${thing instanceof Not ? `not ${thing.thing}` : thing}`);
    }
    var maxQuantity = Math.floor(maximum ?? adjustedCapacity / weight);
    if (maxQuantity < 0) {
      throw new Error(`Invalid max quantity ${maxQuantity} for ${thing instanceof Not ? `not ${thing.thing}` : thing}`);
    }
    return new Array(maxQuantity).fill([thing, value, weight]);
  })));
  var memoizationTable = new Array(values01.length);
  for (var i = 0; i < values01.length; i++) {
    memoizationTable[i] = new Array(adjustedCapacity).fill(null);
  }
  var _bestSolution = bestSolution(memoizationTable, values01, values01.length - 1, adjustedCapacity),
    _bestSolution2 = _slicedToArray(_bestSolution, 2),
    value = _bestSolution2[0],
    invertedSolution = _bestSolution2[1];
  // Still need to replace Not<T>s with right quantity of T's.
  var aggregatedSolution = aggregate(invertedSolution);
  var countMap = new Map(aggregatedSolution);
  var valueAdjustment = 0;
  var solution = aggregatedSolution.filter(_ref8 => {
    var _ref9 = _slicedToArray(_ref8, 1),
      thingOrNot = _ref9[0];
    return !(thingOrNot instanceof Not);
  });
  var _iterator2 = _createForOfIteratorHelper(valuesSorted),
    _step2;
  try {
    for (_iterator2.s(); !(_step2 = _iterator2.n()).done;) {
      var _step2$value = _slicedToArray(_step2.value, 4),
        thingOrNot = _step2$value[0],
        _value = _step2$value[1],
        maximum = _step2$value[3];
      if (thingOrNot instanceof Not) {
        var notCount = countMap.get(thingOrNot) ?? 0;
        if (maximum === undefined) {
          throw new Error(`Cannot find maximum for item ${thingOrNot.thing}.`);
        }
        if (notCount > maximum) {
          throw new Error(`Somehow picked ${notCount} more than the maximum ${notCount} for item ${thingOrNot.thing}.`);
        }
        if (notCount < maximum) {
          solution.push([thingOrNot.thing, maximum - notCount]);
        }
        valueAdjustment -= maximum * _value;
      }
    }
  } catch (err) {
    _iterator2.e(err);
  } finally {
    _iterator2.f();
  }
  return [value + valueAdjustment, solution];
}
/**
 * Find the best solution to a knapsack subproblem.
 *
 * @param memoizationTable Memoization table for dynamic programming approach.
 * @param values Array of {[item, value, weight, maximum]} tuples for knapsack parameter.
 * @param currentIndex Current index into values array - only add items before this index.
 * @param remainingCapacity Remaining capacity of knapsack.
 * @returns Tuple of value to item
 */
function bestSolution(memoizationTable, values, currentIndex, remainingCapacity) {
  // If we've used all our capacity, this solution is no good.
  if (remainingCapacity < 0) return [-Infinity, []];
  if (remainingCapacity === 0 || currentIndex < 0) return [0, []];
  var memoized = memoizationTable[currentIndex][remainingCapacity - 1];
  if (memoized !== null) return memoized;
  var _values$currentIndex = _slicedToArray(values[currentIndex], 3),
    item = _values$currentIndex[0],
    value = _values$currentIndex[1],
    weight = _values$currentIndex[2];
  var _bestSolution3 = bestSolution(memoizationTable, values, currentIndex - 1, remainingCapacity - weight),
    _bestSolution4 = _slicedToArray(_bestSolution3, 2),
    valueIncludeRest = _bestSolution4[0],
    itemsInclude = _bestSolution4[1];
  var valueInclude = valueIncludeRest + value;
  var _bestSolution5 = bestSolution(memoizationTable, values, currentIndex - 1, remainingCapacity),
    _bestSolution6 = _slicedToArray(_bestSolution5, 2),
    valueExclude = _bestSolution6[0],
    itemsExclude = _bestSolution6[1];
  // Pick the better of the two options between including/excluding.
  var result = valueInclude > valueExclude ? [valueInclude, [].concat(_toConsumableArray(itemsInclude), [item])] : [valueExclude, itemsExclude];
  memoizationTable[currentIndex][remainingCapacity - 1] = result;
  return result;
}

function isMonday() {
  // Checking Tuesday's ruby is a hack to see if it's Monday in Arizona.
  return get$1("Muscle Percent", $item`Tuesday's ruby`) > 0;
}
function seasoningAdventures(item) {
  var _getRange = getRange(item.adventures),
    _getRange2 = _slicedToArray(_getRange, 2),
    min = _getRange2[0],
    max = _getRange2[1];
  return max - min <= 1 ? 1 : 0.5;
}
/**
/**
 * Expected adventures from an item given a specified state
 *
 * @todo Include Salty Mouth and potentially other modifiers.
 * @param menuItem Menu item to consider
 * @param modifiers Consumption modifiers to consider
 * @returns Adventures expected
 */
function _expectedAdventures(menuItem, modifiers) {
  var _item$notes, _item$notes2, _item$notes3, _item$notes4, _item$notes5;
  var item = menuItem.item;
  if (item.adventures === "") return 0;
  var _getRange3 = getRange(item.adventures),
    _getRange4 = _slicedToArray(_getRange3, 2),
    min = _getRange4[0],
    max = _getRange4[1];
  var interpolated = _toConsumableArray(new Array(max - min + 1).keys()).map(n => n + min);
  var forkMugMultiplier = menuItem.itemType() === "food" && (_item$notes = item.notes) !== null && _item$notes !== void 0 && _item$notes.includes("SALAD") || menuItem.itemType() === "booze" && (_item$notes2 = item.notes) !== null && _item$notes2 !== void 0 && _item$notes2.includes("BEER") ? 1.5 : 1.3;
  var aioliAdventures = item.fullness;
  var garish = modifiers.garish && ((_item$notes3 = item.notes) === null || _item$notes3 === void 0 ? void 0 : _item$notes3.includes("LASAGNA")) && !isMonday();
  var refinedPalate = modifiers.refinedPalate && ((_item$notes4 = item.notes) === null || _item$notes4 === void 0 ? void 0 : _item$notes4.includes("WINE"));
  var pinkyRing = modifiers.pinkyRing && ((_item$notes5 = item.notes) === null || _item$notes5 === void 0 ? void 0 : _item$notes5.includes("WINE"));
  return sum(interpolated, baseAdventures => {
    var _item$notes6, _item$notes7;
    var adventures = baseAdventures;
    if (modifiers.forkMug) {
      adventures = Math.floor(adventures * forkMugMultiplier);
    }
    if ((_item$notes6 = item.notes) !== null && _item$notes6 !== void 0 && _item$notes6.includes("SAUCY") && modifiers.saucemaven) {
      adventures += kolmafia.myPrimestat() === $stat`Mysticality` ? 5 : 3;
    }
    if (garish) adventures += 5;
    if (refinedPalate) adventures = Math.floor(adventures * 1.25);
    if (pinkyRing) adventures = Math.round(adventures * 1.125);
    if ((_item$notes7 = item.notes) !== null && _item$notes7 !== void 0 && _item$notes7.includes("MARTINI") && modifiers.tuxedoShirt) {
      adventures += 2;
    }
    if (menuItem.itemType() === "food" && modifiers.mayoflex) adventures++;
    if (menuItem.itemType() === "food" && modifiers.seasoning) {
      adventures += seasoningAdventures(item);
    }
    if (menuItem.itemType() === "food" && modifiers.aioli) {
      adventures += aioliAdventures;
    }
    if (menuItem.itemType() === "food" && modifiers.whetStone) adventures++;
    return adventures;
  }) / interpolated.length;
}
var MenuItem = /*#__PURE__*/function () {
  /**
   * Construct a new menu item, possibly with extra properties. Items in MenuItem.defaultOptions have intelligent defaults.
   *
   * @param item Item to add to menu.
   * @param options Options for this menu item
   * @param options.organ Designate item as belonging to a specific organ.
   * @param options.size Override item organ size. Necessary for any non-food/booze/spleen item.
   * @param options.maximum Maximum uses remaining today, or "auto" to check dailyusesleft Mafia property.
   * @param options.additionalValue Additional value (positive) or cost (negative) to consider with item, e.g. from buffs.
   * @param options.effect Effect associated with this menu item (pocket wish effect, sweet synthesis effect, pill keeper potion extension)
   * @param options.mayo Which mayo to use before item (ignored if mayo clinic is not installed or item is not a food)
   * @param options.note Any note to track information about item, to be used later
   */
  function MenuItem(item) {
    var options = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {};
    _classCallCheck(this, MenuItem);
    _defineProperty(this, "item", void 0);
    _defineProperty(this, "organ", void 0);
    _defineProperty(this, "size", void 0);
    _defineProperty(this, "maximum", void 0);
    _defineProperty(this, "additionalValue", void 0);
    _defineProperty(this, "effect", void 0);
    _defineProperty(this, "priceOverride", void 0);
    _defineProperty(this, "mayo", void 0);
    _defineProperty(this, "data", void 0);
    _defineProperty(this, "priceCached", void 0);
    _defineProperty(this, "itemTypeCached", void 0);
    var _options = _objectSpread2(_objectSpread2({}, options), MenuItem.defaultOptions().get(item) ?? {}),
      size = _options.size,
      organ = _options.organ,
      maximum = _options.maximum,
      additionalValue = _options.additionalValue,
      effect = _options.effect,
      priceOverride = _options.priceOverride,
      mayo = _options.mayo,
      data = _options.data;
    this.item = item;
    if (notNullish(maximum)) this.maximum = maximum === "auto" ? item.dailyusesleft : maximum;
    if (notNullish(additionalValue)) this.additionalValue = additionalValue;
    if (notNullish(effect)) this.effect = effect;
    if (notNullish(priceOverride)) this.priceOverride = priceOverride;
    if (notNullish(mayo)) this.mayo = mayo;
    if (notNullish(data)) this.data = data;
    if (notNullish(organ)) {
      this.organ = organ;
    } else {
      var typ = kolmafia.itemType(this.item);
      if (isOrgan(typ)) this.organ = typ;
    }
    this.size = size ?? (this.organ === "food" ? this.item.fullness : this.organ === "booze" ? this.item.inebriety : this.organ === "spleen item" ? this.item.spleen : 0);
  }
  return _createClass(MenuItem, [{
    key: "equals",
    value: function equals(other) {
      return this.item === other.item && this.effect === other.effect;
    }
  }, {
    key: "toString",
    value: function toString() {
      if (this.effect) {
        return `${this.item}:${this.effect}`;
      }
      return this.item.toString();
    }
  }, {
    key: "price",
    value: function price() {
      if (!this.priceCached) {
        this.priceCached = this.priceOverride ?? MenuItem.defaultPriceFunction(this.item);
      }
      return this.priceCached;
    }
  }, {
    key: "itemType",
    value: function itemType() {
      if (!this.itemTypeCached) {
        this.itemTypeCached = kolmafia.itemType(this.item);
      }
      return this.itemTypeCached;
    }
  }], [{
    key: "defaultOptions",
    value: function defaultOptions() {
      return new Map([[$item`distention pill`, {
        organ: "food",
        maximum: !have$P($item`distention pill`) || get$2("_distentionPillUsed") ? 0 : 1,
        size: -1
      }], [$item`synthetic dog hair pill`, {
        organ: "booze",
        maximum: !have$P($item`synthetic dog hair pill`) || get$2("_syntheticDogHairPillUsed") ? 0 : 1,
        size: -1
      }], [$item`cuppa Voraci tea`, {
        organ: "food",
        maximum: get$2("_voraciTeaUsed") ? 0 : 1,
        size: -1
      }], [$item`cuppa Sobrie tea`, {
        organ: "booze",
        maximum: get$2("_sobrieTeaUsed") ? 0 : 1,
        size: -1
      }], [$item`mojo filter`, {
        organ: "spleen item",
        maximum: 3 - get$2("currentMojoFilters"),
        size: -1
      }], [$item`spice melange`, {
        maximum: get$2("spiceMelangeUsed") ? 0 : 1
      }], [$item`Ultra Mega Sour Ball`, {
        maximum: get$2("_ultraMegaSourBallUsed") ? 0 : 1
      }], [$item`The Plumber's mushroom stew`, {
        maximum: get$2("_plumbersMushroomStewEaten") ? 0 : 1
      }], [$item`The Mad Liquor`, {
        maximum: get$2("_madLiquorDrunk") ? 0 : 1
      }], [$item`Doc Clock's thyme cocktail`, {
        maximum: get$2("_docClocksThymeCocktailDrunk") ? 0 : 1
      }], [$item`Mr. Burnsger`, {
        maximum: get$2("_mrBurnsgerEaten") ? 0 : 1
      }], [$item`Calzone of Legend`, {
        maximum: get$2("calzoneOfLegendEaten") ? 0 : 1
      }], [$item`Deep Dish of Legend`, {
        maximum: get$2("deepDishOfLegendEaten") ? 0 : 1
      }], [$item`Pizza of Legend`, {
        maximum: get$2("pizzaOfLegendEaten") ? 0 : 1
      }], [$item`jar of fermented pickle juice`, {
        maximum: get$2("_pickleJuiceDrunk") ? 0 : 1
      }], [$item`extra-greasy slider`, {
        maximum: get$2("_extraGreasySliderEaten") ? 0 : 1
      }], [$item`voodoo snuff`, {
        maximum: get$2("_voodooSnuffUsed") ? 0 : 1
      }], [$item`Ol' Scratch's salad fork`, {
        maximum: get$2("_saladForkUsed") ? 0 : 1
      }], [$item`Frosty's frosty mug`, {
        maximum: get$2("_frostyMugUsed") ? 0 : 1
      }], [$item`tin cup of mulligan stew`, {
        maximum: get$2("_mulliganStewEaten") ? 0 : 1
      }], [$item`Hodgman's blanket`, {
        maximum: get$2("_hodgmansBlanketDrunk") ? 0 : 1
      }]]);
    }
  }]);
}();
_defineProperty(MenuItem, "defaultPriceFunction", item => kolmafia.npcPrice(item) > 0 ? kolmafia.npcPrice(item) : kolmafia.mallPrice(item));
var organs = ["food", "booze", "spleen item"];
/**
 * @param x Name of thing that might be an organ
 * @returns Whether the string supplied is the name of an organ
 */
function isOrgan(x) {
  return organs.includes(x);
}
var DietPlanner = /*#__PURE__*/function () {
  function DietPlanner(mpa, menu) {
    var _this = this;
    _classCallCheck(this, DietPlanner);
    _defineProperty(this, "mpa", void 0);
    _defineProperty(this, "menu", void 0);
    _defineProperty(this, "mayoLookup", void 0);
    _defineProperty(this, "fork", void 0);
    _defineProperty(this, "mug", void 0);
    _defineProperty(this, "seasoning", void 0);
    _defineProperty(this, "whetStone", void 0);
    _defineProperty(this, "aioli", void 0);
    _defineProperty(this, "spleenValue", 0);
    _defineProperty(this, "baseDefaultModifiers", {
      forkMug: false,
      seasoning: false,
      whetStone: false,
      aioli: false,
      mayoflex: false,
      refinedPalate: have$P($effect`Refined Palate`),
      garish: have$P($effect`Gar-ish`),
      saucemaven: have$P($skill`Saucemaven`),
      pinkyRing: have$P($item`mafia pinky ring`) && kolmafia.canEquip($item`mafia pinky ring`),
      tuxedoShirt: have$P($item`tuxedo shirt`) && kolmafia.canEquip($item`tuxedo shirt`)
    });
    this.mpa = mpa;
    var fork = menu.find(item => item.item === $item`Ol' Scratch's salad fork`);
    if (fork) this.fork = fork;
    var mug = menu.find(item => item.item === $item`Frosty's frosty mug`);
    if (mug) this.mug = mug;
    var seasoning = menu.find(item => item.item === $item`Special Seasoning`);
    if (seasoning) this.seasoning = seasoning;
    var whetStone = menu.find(item => item.item === $item`whet stone`);
    if (whetStone) this.whetStone = whetStone;
    var aioli = menu.find(item => item.item === $item`mini kiwi aioli`);
    if (aioli) this.aioli = aioli;
    this.mayoLookup = new Map();
    if (installed$2()) {
      var _loop = function _loop() {
        var mayo = _arr[_i];
        var menuItem = menu.find(item => item.item === mayo);
        if (menuItem) _this.mayoLookup.set(mayo, menuItem);
      };
      for (var _i = 0, _arr = [Mayo$1.flex, Mayo$1.zapine]; _i < _arr.length; _i++) {
        _loop();
      }
    }
    this.menu = menu.filter(item => item.organ);
    if (menu.filter(item => kolmafia.historicalPrice(item.item) === 0 || kolmafia.historicalAge(item.item) >= 1).length > 100) {
      kolmafia.mallPrices("food");
      kolmafia.mallPrices("booze");
    }
    var spleenItems = menu.filter(item => kolmafia.itemType(item.item) === "spleen item");
    spleenItems.sort((x, y) => -(this.consumptionValue(x) / x.item.spleen - this.consumptionValue(y) / y.item.spleen));
    if (spleenItems.length > 0) {
      // Marginal value for sliders and jars depends on our best unlimited spleen item.
      // TODO: spleenLimit() - mySpleenUse() is a poor estimate.
      var bestMarginalSpleenItem = spleenItems.find(spleenItem => spleenItem.maximum === undefined || spleenItem.maximum * spleenItem.size >= kolmafia.spleenLimit() - kolmafia.mySpleenUse());
      if (bestMarginalSpleenItem) {
        this.spleenValue = Math.max(0, this.consumptionValue(bestMarginalSpleenItem) / bestMarginalSpleenItem.size);
      }
    }
  }
  /**
   * Determine the value of consuming a menu item with any profitable helpers.
   *
   * @param menuItem Menu item to check.
   * @returns Value for consuming that menu item.
   */
  return _createClass(DietPlanner, [{
    key: "consumptionValue",
    value: function consumptionValue(menuItem) {
      return this.consumptionHelpersAndValue(menuItem, {})[1];
    }
    /**
     * Determine which helpers will be used with a menu item and its resulting value.
     *
     * @param menuItem Menu item to check.
     * @param overrideModifiers Overrides for consumption modifiers, if any.
     * @returns Pair [array of helpers and base menu item, value].
     */
  }, {
    key: "consumptionHelpersAndValue",
    value: function consumptionHelpersAndValue(menuItem, overrideModifiers) {
      var helpers = [];
      if (menuItem.itemType() === "food" && this.mayoLookup.size) {
        var mayo = menuItem.mayo ? this.mayoLookup.get(menuItem.mayo) : this.mayoLookup.get(Mayo$1.flex);
        if (mayo) helpers.push(mayo);
      }
      var defaultModifiers = _objectSpread2(_objectSpread2({}, this.baseDefaultModifiers), {}, {
        mayoflex: this.mayoLookup.size ? helpers.some(item => item.item === Mayo$1.flex) : false
      }, overrideModifiers);
      if (this.seasoning && menuItem.itemType() === "food" && this.mpa * seasoningAdventures(menuItem.item) > this.seasoning.price()) {
        helpers.push(this.seasoning);
      }
      if (this.whetStone && menuItem.itemType() === "food" && this.mpa > this.whetStone.price()) {
        helpers.push(this.whetStone);
      }
      if (this.aioli && menuItem.itemType() === "food" && this.mpa * menuItem.item.fullness > this.aioli.price()) {
        helpers.push(this.aioli);
      }
      var forkMug = menuItem.itemType() === "food" ? this.fork : menuItem.itemType() === "booze" ? this.mug : null;
      var forkMugPrice = forkMug ? forkMug.price() : Infinity;
      var baseCost = menuItem.price() + sum(helpers, item => item.price());
      var valueRaw = _expectedAdventures(menuItem, defaultModifiers) * this.mpa - baseCost + (menuItem.additionalValue ?? 0);
      var valueForkMug = _expectedAdventures(menuItem, _objectSpread2(_objectSpread2({}, defaultModifiers), {}, {
        forkMug: true
      })) * this.mpa - baseCost - forkMugPrice + (menuItem.additionalValue ?? 0);
      var valueSpleen = $items`jar of fermented pickle juice, extra-greasy slider`.includes(menuItem.item) ? 5 * this.spleenValue : 0;
      return forkMug && valueForkMug > valueRaw ? [[].concat(helpers, [forkMug, menuItem]), valueForkMug + valueSpleen] : [[].concat(helpers, [menuItem]), valueRaw + valueSpleen];
    }
    /**
     * Plan an individual organ.
     *
     * @param organ Organ to plan
     * @param capacity Organ capacity.
     * @param overrideModifiers Overrides for consumption modifiers, if any.
     * @returns Pair of [value, menu items and quantities].
     */
  }, {
    key: "planOrgan",
    value: function planOrgan(organ, capacity) {
      var overrideModifiers = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : {};
      var submenu = this.menu.filter(menuItem => menuItem.organ === organ && kolmafia.myLevel() >= menuItem.item.levelreq);
      var knapsackValues = submenu.map(menuItem => [].concat(_toConsumableArray(this.consumptionHelpersAndValue(menuItem, overrideModifiers)), [menuItem.size, menuItem.maximum]));
      return knapsack(knapsackValues, capacity);
    }
    /**
     * Plan organs.
     *
     * @param organCapacities Organ capacities.
     * @param overrideModifiers Overrides for consumption modifiers, if any.
     * @returns Pair of [value, menu items and quantities].
     */
  }, {
    key: "planOrgans",
    value: function planOrgans(organCapacities) {
      var _ref5;
      var overrideModifiers = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {};
      var valuePlans = organCapacities.map(_ref => {
        var _ref2 = _slicedToArray(_ref, 2),
          organ = _ref2[0],
          capacity = _ref2[1];
        return this.planOrgan(organ, capacity, overrideModifiers);
      });
      return [sum(valuePlans, _ref3 => {
        var _ref4 = _slicedToArray(_ref3, 1),
          value = _ref4[0];
        return value;
      }), (_ref5 = []).concat.apply(_ref5, _toConsumableArray(valuePlans.map(_ref6 => {
        var _ref7 = _slicedToArray(_ref6, 2),
          plan = _ref7[1];
        return plan;
      })))];
    }
    /**
     * Plan organs, retrying with and without each trial item. Runtime is
     * proportional to 2 ^ trialItems.length.
     *
     * @param organCapacities Organ capacities.
     * @param trialItems Items to rerun solver with and without.
     * @param overrideModifiers Overrides for consumption modifiers, if any.
     * @returns Pair of [value, menu items and quantities].
     */
  }, {
    key: "planOrgansWithTrials",
    value: function planOrgansWithTrials(organCapacities, trialItems, overrideModifiers) {
      if (trialItems.length === 0) {
        return this.planOrgans(organCapacities, overrideModifiers);
      }
      var _trialItems$ = _slicedToArray(trialItems[0], 2),
        trialItem = _trialItems$[0],
        organSizes = _trialItems$[1];
      if (trialItem.maximum !== undefined && trialItem.maximum <= 0) {
        return this.planOrgansWithTrials(organCapacities, trialItems.slice(1), overrideModifiers);
      }
      var organCapacitiesWithMap = new Map(organCapacities);
      var _iterator = _createForOfIteratorHelper(organSizes),
        _step;
      try {
        for (_iterator.s(); !(_step = _iterator.n()).done;) {
          var _step$value = _slicedToArray(_step.value, 2),
            organ = _step$value[0],
            size = _step$value[1];
          var current = organCapacitiesWithMap.get(organ);
          if (current === undefined) {
            // Organs with no capacity are excluded from the organCapacities map, so this item excluded from the trial.
            // Solves the problem with the diet offering to eat toasted brie after exiting Shrunken Adventurer with 20/15 fullness.
            return this.planOrgansWithTrials(organCapacities, trialItems.slice(1), overrideModifiers);
          }
          organCapacitiesWithMap.set(organ, current - size);
        }
      } catch (err) {
        _iterator.e(err);
      } finally {
        _iterator.f();
      }
      var organCapacitiesWith = _toConsumableArray(organCapacitiesWithMap);
      var isRefinedPalate = trialItem.item === $item`pocket wish` && trialItem.effect === $effect`Refined Palate` || trialItem.item === $item`toasted brie`;
      var isGarish = trialItem.item === $item`pocket wish` && trialItem.effect === $effect`Gar-ish` || trialItem.item === $item`potion of the field gar`;
      var _this$planOrgansWithT = this.planOrgansWithTrials(organCapacities, trialItems.slice(1), overrideModifiers),
        _this$planOrgansWithT2 = _slicedToArray(_this$planOrgansWithT, 2),
        valueWithout = _this$planOrgansWithT2[0],
        planWithout = _this$planOrgansWithT2[1];
      var _this$planOrgansWithT3 = this.planOrgansWithTrials(organCapacitiesWith, trialItems.slice(1), _objectSpread2(_objectSpread2(_objectSpread2({}, overrideModifiers), isRefinedPalate ? {
          refinedPalate: true
        } : {}), isGarish ? {
          garish: true
        } : {})),
        _this$planOrgansWithT4 = _slicedToArray(_this$planOrgansWithT3, 2),
        valueWith = _this$planOrgansWithT4[0],
        planWith = _this$planOrgansWithT4[1];
      var _this$consumptionHelp = this.consumptionHelpersAndValue(trialItem, {}),
        _this$consumptionHelp2 = _slicedToArray(_this$consumptionHelp, 2),
        helpersAndItem = _this$consumptionHelp2[0],
        value = _this$consumptionHelp2[1];
      return valueWithout > valueWith + value ? [valueWithout, planWithout] : [valueWith + value, [].concat(_toConsumableArray(planWith), [[helpersAndItem, 1]])];
    }
  }]);
}();
/**
 * Because the knapsack solver is one-dimensional only, any items that touch
 * multiple organs have to be treated specially. What we do is run the knapsack
 * solver multiple times, trying with + without each interacting item.
 */
var interactingItems = [[$item`spice melange`, [["food", -3], ["booze", -3]]], [$item`Ultra Mega Sour Ball`, [["food", -3], ["booze", -3]]], [$item`The Plumber's mushroom stew`, [["food", 3], ["booze", -1]]], [$item`The Mad Liquor`, [["food", -1], ["booze", 3]]], [$item`Doc Clock's thyme cocktail`, [["food", -2], ["booze", 4]]], [$item`Mr. Burnsger`, [["food", 4], ["booze", -2]]], [$effect`Refined Palate`, []], [$item`toasted brie`, [["food", 2]]], [$effect`Gar-ish`, []], [$item`potion of the field gar`, []]];
/**
 * Plan out an optimal diet using a knapsack algorithm.
 *
 * @param mpa Meat per adventure value.
 * @param menu Array of MenuItems to consider for diet purposes.
 * @param organCapacities Optional override of each organ's capacity.
 * @returns Array of [menu item and helpers, count].
 */
function planDiet(mpa, menu) {
  var organCapacities = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : [["food", null], ["booze", null], ["spleen item", null]];
  // FIXME: Figure out a better way to handle overfull organs (e.g. coming out of Ed).
  var resolvedOrganCapacities = organCapacities.map(_ref8 => {
    var _ref9 = _slicedToArray(_ref8, 2),
      organ = _ref9[0],
      size = _ref9[1];
    return [organ, size ?? (organ === "food" ? kolmafia.fullnessLimit() - kolmafia.myFullness() : organ === "booze" ? kolmafia.inebrietyLimit() - kolmafia.myInebriety() : organ === "spleen item" ? kolmafia.spleenLimit() - kolmafia.mySpleenUse() : 0)];
  });
  /**
   * Per above description, separate out items with cross-organ interaction
   * ("interacting items") for special treatment. These will be checked by
   * running the solver several times.
   */
  var includedInteractingItems = menu.map(menuItem => {
    var interacting = interactingItems.find(_ref0 => {
      var _ref1 = _slicedToArray(_ref0, 1),
        itemOrEffect = _ref1[0];
      return menuItem.item === itemOrEffect || menuItem.item === $item`pocket wish` && menuItem.effect === itemOrEffect;
    });
    if (interacting) {
      var _interacting = _slicedToArray(interacting, 2),
        organSizes = _interacting[1];
      return [menuItem, organSizes];
    } else {
      return null;
    }
  }).filter(value => value !== null);
  // Filter out interacting items from natural consideration.
  var dietPlanner = new DietPlanner(mpa, menu.filter(menuItem => !includedInteractingItems.some(_ref10 => {
    var _ref11 = _slicedToArray(_ref10, 1),
      interacting = _ref11[0];
    return interacting === menuItem;
  })));
  /**
   * Because our knapsack solver is one-dimensional, we have to consider
   * each organ separately. Since there are no spleen items that affect
   * stomach/liver, we consider those two first, with an approximation of the
   * value of spleen-cleaning. Afterwards, we see how much spleen we have and
   * plan that.
   */
  var _dietPlanner$planOrga = dietPlanner.planOrgansWithTrials(resolvedOrganCapacities.filter(_ref12 => {
      var _ref13 = _slicedToArray(_ref12, 2),
        organ = _ref13[0],
        capacity = _ref13[1];
      return ["food", "booze"].includes(organ) && capacity >= 0;
    }), includedInteractingItems, {}),
    _dietPlanner$planOrga2 = _slicedToArray(_dietPlanner$planOrga, 2),
    planFoodBooze = _dietPlanner$planOrga2[1];
  var spleenCapacity = resolvedOrganCapacities.find(_ref14 => {
    var _ref15 = _slicedToArray(_ref14, 1),
      organ = _ref15[0];
    return organ === "spleen item";
  });
  if (spleenCapacity) {
    // Count sliders and pickle juice, figure out how much extra spleen we got.
    var additionalSpleen = sum(planFoodBooze, _ref16 => {
      var _ref17 = _slicedToArray(_ref16, 2),
        items = _ref17[0],
        number = _ref17[1];
      return items.some(menuItem => $items`jar of fermented pickle juice, extra-greasy slider`.includes(menuItem.item)) ? 5 * number : 0;
    });
    var _spleenCapacity = _slicedToArray(spleenCapacity, 2),
      availableSpleen = _spleenCapacity[1];
    var _dietPlanner$planOrga3 = dietPlanner.planOrgan("spleen item", availableSpleen + additionalSpleen),
      _dietPlanner$planOrga4 = _slicedToArray(_dietPlanner$planOrga3, 2),
      planSpleen = _dietPlanner$planOrga4[1];
    return [].concat(_toConsumableArray(planFoodBooze), _toConsumableArray(planSpleen));
  } else {
    return planFoodBooze;
  }
}
var DietEntry = /*#__PURE__*/function () {
  function DietEntry(menuItems, quantity) {
    _classCallCheck(this, DietEntry);
    _defineProperty(this, "quantity", void 0);
    _defineProperty(this, "menuItems", void 0);
    this.menuItems = Object.freeze(menuItems);
    this.quantity = quantity;
  }
  return _createClass(DietEntry, [{
    key: "target",
    value: function target() {
      return this.menuItems[this.menuItems.length - 1];
    }
  }, {
    key: "helpers",
    value: function helpers() {
      if (this.menuItems.length > 1) {
        return this.menuItems.slice(0, -1);
      }
      return [];
    }
  }, {
    key: "expectedAdventures",
    value: function expectedAdventures(diet) {
      {
        if (this.menuItems.length === 0 || this.quantity === 0) {
          return 0;
        } else {
          var items = this.menuItems.map(m => m.item);
          var targetItem = this.menuItems[this.menuItems.length - 1].item;
          var fork = kolmafia.itemType(targetItem) === "food" && items.includes($item`Ol' Scratch's salad fork`);
          var mug = kolmafia.itemType(targetItem) === "booze" && items.includes($item`Frosty's frosty mug`);
          return this.quantity * _expectedAdventures(this.menuItems[this.menuItems.length - 1], {
            forkMug: fork || mug,
            seasoning: items.includes($item`Special Seasoning`),
            whetStone: items.includes($item`whet stone`),
            aioli: items.includes($item`mini kiwi aioli`),
            mayoflex: items.includes(Mayo$1.flex),
            refinedPalate: diet.refinedPalate,
            garish: diet.garish,
            saucemaven: diet.saucemaven,
            pinkyRing: diet.pinkyRing,
            tuxedoShirt: diet.tuxedoShirt
          });
        }
      }
    }
  }, {
    key: "expectedValue",
    value: function expectedValue(mpa, diet) {
      var method = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : "gross";
      var gross = mpa * this.expectedAdventures(diet) + this.quantity * sum(this.menuItems, menuItem => menuItem.additionalValue ?? 0);
      if (method === "gross") {
        return gross;
      } else {
        return gross - this.expectedPrice();
      }
    }
  }, {
    key: "expectedPrice",
    value: function expectedPrice() {
      return this.quantity * sum(this.menuItems, menuItem => menuItem.price());
    }
  }]);
}();
/**
 * A representation of a potential diet
 */
var Diet = /*#__PURE__*/function () {
  function Diet() {
    var entries = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : [];
    _classCallCheck(this, Diet);
    _defineProperty(this, "entries", void 0);
    this.entries = entries;
  }
  return _createClass(Diet, [{
    key: "refinedPalate",
    get: function get() {
      return this.entries.some(dietEntry => dietEntry.menuItems.some(trialItem => trialItem.item === $item`pocket wish` && trialItem.effect === $effect`Refined Palate` || trialItem.item === $item`toasted brie`));
    }
  }, {
    key: "garish",
    get: function get() {
      return this.entries.some(dietEntry => dietEntry.menuItems.some(trialItem => trialItem.item === $item`pocket wish` && trialItem.effect === $effect`Gar-ish` || trialItem.item === $item`potion of the field gar`));
    }
  }, {
    key: "saucemaven",
    get: function get() {
      return have$P($skill`Saucemaven`);
    }
  }, {
    key: "tuxedoShirt",
    get: function get() {
      return have$P($item`tuxedo shirt`) && kolmafia.canEquip($item`tuxedo shirt`);
    }
  }, {
    key: "pinkyRing",
    get: function get() {
      return have$P($item`mafia pinky ring`) && kolmafia.canEquip($item`mafia pinky ring`);
    }
  }, {
    key: "expectedAdventures",
    value: function expectedAdventures() {
      return sum(this.entries, dietEntry => dietEntry.expectedAdventures(this));
    }
  }, {
    key: "expectedValue",
    value: function expectedValue(mpa) {
      var method = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : "gross";
      return sum(this.entries, dietEntry => dietEntry.expectedValue(mpa, this, method));
    }
  }, {
    key: "expectedPrice",
    value: function expectedPrice() {
      return sum(this.entries, dietEntry => dietEntry.expectedPrice());
    }
  }, {
    key: "copy",
    value: function copy() {
      return new Diet(_toConsumableArray(this.entries));
    }
  }], [{
    key: "from",
    value: function from(rawDiet) {
      var diet = rawDiet.map(item => {
        var _item = _slicedToArray(item, 2),
          menuItems = _item[0],
          quantity = _item[1];
        return new DietEntry(menuItems, quantity);
      });
      return new Diet(diet);
    }
  }, {
    key: "plan",
    value: function plan(mpa, menu) {
      var organCapacities = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : {
        food: "auto",
        booze: "auto",
        spleen: "auto"
      };
      var food = organCapacities.food,
        booze = organCapacities.booze,
        spleen = organCapacities.spleen;
      var plannerCapacity = [];
      if (food) {
        plannerCapacity.push(["food", food === "auto" ? null : food]);
      }
      if (booze) {
        plannerCapacity.push(["booze", booze === "auto" ? null : booze]);
      }
      if (spleen) {
        plannerCapacity.push(["spleen item", spleen === "auto" ? null : spleen]);
      }
      return Diet.from(planDiet(mpa, menu, plannerCapacity));
    }
  }]);
}();

/**
 * Mines in the Kingdom of Loathing
 */
var Mine;
(function (Mine) {
  /** Inside of Itznotyerzitz Mine */
  Mine[Mine["ITZNOTYERZITZ"] = 1] = "ITZNOTYERZITZ";
  /** Deep Inside the Knob Shaft */
  Mine[Mine["KNOB"] = 2] = "KNOB";
  /** Anemone Mine */
  Mine[Mine["ANEMONE"] = 3] = "ANEMONE";
  /** The Gummi Mine (Retired, Crimbo 2011) */
  Mine[Mine["GUMMI"] = 4] = "GUMMI";
  /** Crimbonium Mine (Retired, Crimbo 2014) */
  Mine[Mine["CRIMBONIUM"] = 5] = "CRIMBONIUM";
  /** The Velvet / Gold Mine */
  Mine[Mine["VOLCANO"] = 6] = "VOLCANO";
})(Mine || (Mine = {}));
/**
 * @returns Number of unconditionally free mines (minin' dynamite is not counted as it only works with non-sparkly spots)
 */
function countFreeMines() {
  return (have$P($skill`Unaccompanied Miner`) ? 5 - get$2("_unaccompaniedMinerUsed") : 0) + kolmafia.haveEffect($effect`Loded`);
}

var _OscusSoda, _MagicalSausages;
var aprilShieldEffects = new Map([[$skill`Empathy of the Newt`, $effect`Thoughtful Empathy`], [$skill`Sauce Contemplation`, $effect`Lubricating Sauce`], [$skill`Manicotti Meditation`, $effect`Tubes of Universal Meat`], [$skill`Seal Clubbing Frenzy`, $effect`Slippery as a Seal`], [$skill`Patience of the Tortoise`, $effect`Strength of the Tortoise`], [$skill`Disco Aerobics`, $effect`Disco over Matter`], [$skill`Moxie of the Mariachi`, $effect`Mariachi Moisture`]]);
var pastaWandEffects = new Map([[$skill`Bind Vampieroghi`, $effect`Legendary Bloody Potato Bits`], [$skill`Bind Vermincelli`, $effect`Legendary Slinking Noodle Glob`], [$skill`Bind Angel Hair Wisp`, $effect`Legendary Whispering Strands`], [$skill`Bind Undead Elbow Macaroni`, $effect`Legendary Macaroni Coating`], [$skill`Bind Penne Dreadful`, $effect`Legendary Penne Fedora`], [$skill`Bind Lasagmbie`, $effect`Legendary Pasta Eyeball`], [$skill`Bind Spice Ghost`, $effect`Legendary Spice Haze`]]);
var MpSource = /*#__PURE__*/function () {
  function MpSource() {
    _classCallCheck(this, MpSource);
  }
  return _createClass(MpSource, [{
    key: "usesRemaining",
    value: function usesRemaining() {
      return 0;
    }
  }, {
    key: "availableMpMax",
    value: function availableMpMax() {
      return this.availableMpMin();
    }
  }]);
}();
var OscusSoda = /*#__PURE__*/function (_MpSource) {
  function OscusSoda() {
    _classCallCheck(this, OscusSoda);
    return _callSuper(this, OscusSoda, arguments);
  }
  _inherits(OscusSoda, _MpSource);
  return _createClass(OscusSoda, [{
    key: "available",
    value: function available() {
      return have$P($item`Oscus's neverending soda`);
    }
  }, {
    key: "usesRemaining",
    value: function usesRemaining() {
      return get$2("oscusSodaUsed") ? 0 : 1;
    }
  }, {
    key: "availableMpMin",
    value: function availableMpMin() {
      return this.available() && this.usesRemaining() > 0 ? 200 : 0;
    }
  }, {
    key: "availableMpMax",
    value: function availableMpMax() {
      return this.available() && this.usesRemaining() > 0 ? 300 : 0;
    }
  }, {
    key: "execute",
    value: function execute() {
      kolmafia.use($item`Oscus's neverending soda`);
    }
  }]);
}(MpSource);
_OscusSoda = OscusSoda;
_defineProperty(OscusSoda, "instance", new _OscusSoda());
var MagicalSausages = /*#__PURE__*/function (_MpSource2) {
  function MagicalSausages() {
    _classCallCheck(this, MagicalSausages);
    return _callSuper(this, MagicalSausages, arguments);
  }
  _inherits(MagicalSausages, _MpSource2);
  return _createClass(MagicalSausages, [{
    key: "available",
    value: function available() {
      return have$P($item`Kramco Sausage-o-Matic™`);
    }
  }, {
    key: "usesRemaining",
    value: function usesRemaining() {
      var maxSausages = kolmafia.availableAmount($item`magical sausage`) + kolmafia.availableAmount($item`magical sausage casing`);
      return this.available() ? clamp(23 - get$2("_sausagesEaten"), 0, maxSausages) : 0;
    }
  }, {
    key: "availableMpMin",
    value: function availableMpMin() {
      return this.available() ? Math.min(kolmafia.myMaxmp(), 999) * this.usesRemaining() : 0;
    }
  }, {
    key: "execute",
    value: function execute() {
      var mpSpaceAvailable = kolmafia.myMaxmp() - kolmafia.myMp();
      if (mpSpaceAvailable < 700) return;
      var maxSausages = Math.min(this.usesRemaining(), Math.floor((kolmafia.myMaxmp() - kolmafia.myMp()) / Math.min(kolmafia.myMaxmp() - kolmafia.myMp(), 999)));
      kolmafia.retrieveItem(maxSausages, $item`magical sausage`);
      kolmafia.eat(maxSausages, $item`magical sausage`);
    }
  }]);
}(MpSource);
_MagicalSausages = MagicalSausages;
_defineProperty(MagicalSausages, "instance", new _MagicalSausages());
var MoodElement = /*#__PURE__*/function () {
  function MoodElement() {
    _classCallCheck(this, MoodElement);
  }
  return _createClass(MoodElement, [{
    key: "mpCostPerTurn",
    value: function mpCostPerTurn() {
      return 0;
    }
  }, {
    key: "turnIncrement",
    value: function turnIncrement() {
      return 1;
    }
  }]);
}();
var SkillMoodElement = /*#__PURE__*/function (_MoodElement) {
  function SkillMoodElement(skill, options) {
    var _this;
    _classCallCheck(this, SkillMoodElement);
    _this = _callSuper(this, SkillMoodElement);
    _defineProperty(_this, "skill", void 0);
    _defineProperty(_this, "effect", void 0);
    _defineProperty(_this, "options", void 0);
    _this.skill = skill;
    if (options.requireAprilShield) {
      _this.effect = aprilShieldEffects.get(skill) ?? $effect.none;
    } else if (have$P($item`legendary pasta wand`)) {
      var legendaryPastaEffect = pastaWandEffects.get(skill);
      _this.effect = legendaryPastaEffect ?? kolmafia.toEffect(skill);
    } else {
      _this.effect = kolmafia.toEffect(skill);
    }
    _this.options = options;
    return _this;
  }
  _inherits(SkillMoodElement, _MoodElement);
  return _createClass(SkillMoodElement, [{
    key: "aprilShieldRestricted",
    get: function get() {
      return !this.options.requireAprilShield && this.skill === $skill`Empathy of the Newt`;
    }
  }, {
    key: "mpCostPerTurn",
    value: function mpCostPerTurn() {
      var turns = kolmafia.turnsPerCast(this.skill);
      return turns > 0 ? kolmafia.mpCost(this.skill) / turns : 0;
    }
  }, {
    key: "turnIncrement",
    value: function turnIncrement() {
      return kolmafia.turnsPerCast(this.skill);
    }
  }, {
    key: "execute",
    value: function execute(mood, ensureTurns) {
      var _this2 = this;
      if (this.effect === $effect.none) return false;
      var initialTurns = kolmafia.haveEffect(this.effect);
      // Track these separately because of LHM
      var shieldSlot = kolmafia.Slot.all().find(slot => kolmafia.equippedItem(slot) === $item`April Shower Thoughts shield`);
      var initialOffhand = kolmafia.equippedItem($slot`off-hand`);
      var initialWeapon = kolmafia.equippedItem($slot`weapon`);
      if (initialTurns >= ensureTurns) return true;
      if (!kolmafia.haveSkill(this.skill)) return false;
      if (this.aprilShieldRestricted && shieldSlot) {
        unequip($item`April Shower Thoughts shield`);
      }
      if (mood.options.songSlots && isSong(this.skill) && !have$P(this.effect)) {
        var activeSongs = getActiveSongs();
        var _iterator = _createForOfIteratorHelper(activeSongs),
          _step;
        try {
          var _loop = function _loop() {
            var song = _step.value;
            var slot = mood.options.songSlots.find(slot => slot.includes(song));
            if (!slot || slot.includes(_this2.effect)) {
              kolmafia.cliExecute(`shrug ${song}`);
              return 1; // break
            }
          };
          for (_iterator.s(); !(_step = _iterator.n()).done;) {
            if (_loop()) break;
          }
        } catch (err) {
          _iterator.e(err);
        } finally {
          _iterator.f();
        }
      }
      var oldRemainingCasts = -1;
      var remainingCasts = Math.ceil((ensureTurns - kolmafia.haveEffect(this.effect)) / kolmafia.turnsPerCast(this.skill));
      try {
        while (remainingCasts > 0 && oldRemainingCasts !== remainingCasts) {
          if (this.options.requireAprilShield && !shieldSlot) {
            if (kolmafia.weaponHands(initialWeapon) > 1) unequip(initialWeapon);
            if (!kolmafia.equip($item`April Shower Thoughts shield`)) return false;
          }
          var maxCasts = 0;
          if (kolmafia.hpCost(this.skill) > 0) {
            maxCasts = Math.max(0, Math.floor((kolmafia.myHp() - 1) / kolmafia.hpCost(this.skill)));
          } else {
            var cost = kolmafia.mpCost(this.skill);
            maxCasts = Math.floor(Math.min(mood.availableMp(), kolmafia.myMp()) / cost);
            if (maxCasts < remainingCasts) {
              var bestMp = Math.min(remainingCasts * cost, kolmafia.myMaxmp());
              mood.moreMp(bestMp);
              maxCasts = Math.floor(Math.min(mood.availableMp(), kolmafia.myMp()) / cost);
            }
          }
          var casts = clamp(remainingCasts, 0, Math.min(100, maxCasts));
          kolmafia.useSkill(casts, this.skill);
          oldRemainingCasts = remainingCasts;
          remainingCasts = Math.ceil((ensureTurns - kolmafia.haveEffect(this.effect)) / kolmafia.turnsPerCast(this.skill));
        }
        return kolmafia.haveEffect(this.effect) >= ensureTurns;
      } finally {
        if (shieldSlot) kolmafia.equip($item`April Shower Thoughts shield`, shieldSlot);
        if (initialOffhand !== kolmafia.equippedItem($slot`off-hand`)) kolmafia.equip(initialOffhand, $slot`off-hand`);
        if (initialWeapon !== kolmafia.equippedItem($slot`weapon`)) kolmafia.equip(initialWeapon, $slot`weapon`);
      }
    }
  }]);
}(MoodElement);
var PotionMoodElement = /*#__PURE__*/function (_MoodElement2) {
  function PotionMoodElement(potion, maxPricePerTurn) {
    var _this3;
    _classCallCheck(this, PotionMoodElement);
    _this3 = _callSuper(this, PotionMoodElement);
    _defineProperty(_this3, "potion", void 0);
    _defineProperty(_this3, "maxPricePerTurn", void 0);
    _this3.potion = potion;
    _this3.maxPricePerTurn = maxPricePerTurn;
    return _this3;
  }
  _inherits(PotionMoodElement, _MoodElement2);
  return _createClass(PotionMoodElement, [{
    key: "execute",
    value: function execute(mood, ensureTurns) {
      // FIXME: Smarter buying logic.
      // FIXME: Allow constructing stuff (e.g. snow cleats)
      var effect = kolmafia.effectModifier(this.potion, "Effect");
      var effectTurns = kolmafia.haveEffect(effect);
      var turnsPerUse = kolmafia.numericModifier(this.potion, "Effect Duration");
      if (kolmafia.mallPrice(this.potion) > this.maxPricePerTurn * turnsPerUse) {
        return false;
      }
      // integer part
      if (effectTurns < ensureTurns) {
        var uses = Math.floor((ensureTurns - effectTurns) / turnsPerUse);
        var quantityToBuy = clamp(uses - kolmafia.availableAmount(this.potion), 0, 100);
        kolmafia.buy(quantityToBuy, this.potion, Math.floor(this.maxPricePerTurn * turnsPerUse));
        var quantityToUse = clamp(uses, 0, kolmafia.availableAmount(this.potion));
        kolmafia.use(quantityToUse, this.potion);
      }
      // fractional part
      var remainingDifference = ensureTurns - kolmafia.haveEffect(effect);
      if (remainingDifference > 0) {
        var maxPrice = Math.floor(this.maxPricePerTurn * remainingDifference);
        if (kolmafia.mallPrice(this.potion) <= maxPrice) {
          if (kolmafia.availableAmount(this.potion) || kolmafia.buy(1, this.potion, maxPrice)) {
            kolmafia.use(1, this.potion);
          }
        }
      }
      return kolmafia.haveEffect(effect) >= ensureTurns;
    }
  }]);
}(MoodElement);
var GenieMoodElement = /*#__PURE__*/function (_MoodElement3) {
  function GenieMoodElement(effect) {
    var _this4;
    _classCallCheck(this, GenieMoodElement);
    _this4 = _callSuper(this, GenieMoodElement);
    _defineProperty(_this4, "effect", void 0);
    _this4.effect = effect;
    return _this4;
  }
  _inherits(GenieMoodElement, _MoodElement3);
  return _createClass(GenieMoodElement, [{
    key: "execute",
    value: function execute(mood, ensureTurns) {
      if (kolmafia.haveEffect(this.effect) >= ensureTurns) return true;
      var neededWishes = Math.ceil((kolmafia.haveEffect(this.effect) - ensureTurns) / 20);
      var wishesToBuy = clamp(neededWishes - kolmafia.availableAmount($item`pocket wish`), 0, 20);
      kolmafia.buy(wishesToBuy, $item`pocket wish`, 50000);
      var wishesToUse = clamp(neededWishes, 0, kolmafia.availableAmount($item`pocket wish`));
      for (; wishesToUse > 0; wishesToUse--) {
        kolmafia.cliExecute(`genie effect ${this.effect.name}`);
      }
      return kolmafia.haveEffect(this.effect) >= ensureTurns;
    }
  }]);
}(MoodElement);
var CustomMoodElement = /*#__PURE__*/function (_MoodElement4) {
  function CustomMoodElement(effect, gainEffect) {
    var _this5;
    _classCallCheck(this, CustomMoodElement);
    _this5 = _callSuper(this, CustomMoodElement);
    _defineProperty(_this5, "effect", void 0);
    _defineProperty(_this5, "gainEffect", void 0);
    _this5.effect = effect;
    _this5.gainEffect = gainEffect ?? (() => kolmafia.cliExecute(effect.default));
    return _this5;
  }
  _inherits(CustomMoodElement, _MoodElement4);
  return _createClass(CustomMoodElement, [{
    key: "execute",
    value: function execute(mood, ensureTurns) {
      var currentTurns = kolmafia.haveEffect(this.effect);
      var lastCurrentTurns = -1;
      while (currentTurns < ensureTurns && currentTurns !== lastCurrentTurns) {
        this.gainEffect();
        lastCurrentTurns = currentTurns;
        currentTurns = kolmafia.haveEffect(this.effect);
      }
      return kolmafia.haveEffect(this.effect) > ensureTurns;
    }
  }]);
}(MoodElement);
var AsdonMoodElement = /*#__PURE__*/function (_MoodElement5) {
  function AsdonMoodElement(effect) {
    var _this6;
    var preferInventory = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : false;
    _classCallCheck(this, AsdonMoodElement);
    _this6 = _callSuper(this, AsdonMoodElement);
    _defineProperty(_this6, "effect", void 0);
    _defineProperty(_this6, "preferInventory", void 0);
    _this6.effect = effect;
    _this6.preferInventory = preferInventory;
    return _this6;
  }
  _inherits(AsdonMoodElement, _MoodElement5);
  return _createClass(AsdonMoodElement, [{
    key: "execute",
    value: function execute(mood, ensureTurns) {
      return drive(this.effect, ensureTurns, this.preferInventory);
    }
  }]);
}(MoodElement);
/**
 * Class representing a mood object. Add mood elements using the instance methods, which can be chained.
 */
var Mood = /*#__PURE__*/function () {
  /**
   * Construct a new Mood instance.
   *
   * @param options Options for mood.
   */
  function Mood() {
    var options = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : {};
    _classCallCheck(this, Mood);
    _defineProperty(this, "options", void 0);
    _defineProperty(this, "elements", []);
    this.options = _objectSpread2(_objectSpread2({}, Mood.defaultOptions), options);
  }
  /**
   * Get the MP available for casting skills.
   *
   * @returns Available MP
   */
  return _createClass(Mood, [{
    key: "availableMp",
    value: function availableMp() {
      return this.options.useNativeRestores ? Infinity : sum(this.options.mpSources, mpSource => mpSource.availableMpMin()) + Math.max(kolmafia.myMp() - this.options.reserveMp, 0);
    }
  }, {
    key: "moreMp",
    value: function moreMp(minimumTarget) {
      if (kolmafia.myMp() >= minimumTarget) return;
      var _iterator2 = _createForOfIteratorHelper(this.options.mpSources),
        _step2;
      try {
        for (_iterator2.s(); !(_step2 = _iterator2.n()).done;) {
          var mpSource = _step2.value;
          if (mpSource.usesRemaining() > 0) {
            mpSource.execute();
            if (kolmafia.myMp() >= minimumTarget) break;
          }
        }
      } catch (err) {
        _iterator2.e(err);
      } finally {
        _iterator2.f();
      }
      if (this.options.useNativeRestores) {
        kolmafia.restoreMp(minimumTarget);
      }
    }
    /**
     * Add a skill to the mood.
     *
     * @param skill Skill to add.
     * @param options Additional `SkillEffectOptions` to pass to new `SkillMoodElement`
     * @returns This mood to enable chaining
     */
  }, {
    key: "skill",
    value: function skill(_skill) {
      var options = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {};
      this.elements.push(new SkillMoodElement(_skill, options));
      return this;
    }
    /**
     * Add an effect to the mood, with casting based on {effect.default}.
     *
     * @param effect Effect to add.
     * @param gainEffect How to gain the effect. Only runs if we don't have the effect.
     * @returns This mood to enable chaining
     */
  }, {
    key: "effect",
    value: function effect(_effect, gainEffect) {
      var skill = kolmafia.toSkill(_effect);
      if (_toConsumableArray(aprilShieldEffects.values()).includes(_effect)) {
        return this.skill(skill, {
          requireAprilShield: true
        });
      }
      if (!gainEffect && skill !== $skill.none) return this.skill(skill);
      this.elements.push(new CustomMoodElement(_effect, gainEffect));
      return this;
    }
    /**
     * Add a potion to the mood.
     *
     * @param potion Potion to add.
     * @param maxPricePerTurn Maximum price to pay per turn of the effect.
     * @returns This mood to enable chaining
     */
  }, {
    key: "potion",
    value: function potion(_potion, maxPricePerTurn) {
      this.elements.push(new PotionMoodElement(_potion, maxPricePerTurn));
      return this;
    }
    /**
     * Add an effect to acquire via pocket wishes to the mood.
     *
     * @param effect Effect to wish for in the mood.
     * @returns This mood to enable chaining
     */
  }, {
    key: "genie",
    value: function genie(effect) {
      this.elements.push(new GenieMoodElement(effect));
      return this;
    }
    /**
     * Add an Asdon Martin driving style to the mood.
     *
     * @param effect Driving style to add to the mood.
     * @returns This mood to enable chaining
     */
  }, {
    key: "drive",
    value: function drive(effect) {
      if (Object.values(Driving).includes(effect) && installed$4()) {
        this.elements.push(new AsdonMoodElement(effect));
      }
      return this;
    }
    /**
     * Execute the mood, trying to ensure {ensureTurns} of each effect.
     *
     * @param ensureTurns Turns of each effect to try and achieve.
     * @returns Whether or not we successfully got this many turns of every effect in the mood.
     */
  }, {
    key: "execute",
    value: function execute() {
      var ensureTurns = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : 1;
      var availableMp = this.availableMp();
      var totalMpPerTurn = sum(this.elements, element => element.mpCostPerTurn());
      var potentialTurns = Math.floor(availableMp / totalMpPerTurn);
      var completeSuccess = true;
      var _iterator3 = _createForOfIteratorHelper(this.elements),
        _step3;
      try {
        for (_iterator3.s(); !(_step3 = _iterator3.n()).done;) {
          var element = _step3.value;
          var elementTurns = ensureTurns;
          if (element.mpCostPerTurn() > 0) {
            var elementPotentialTurns = Math.floor(potentialTurns / element.turnIncrement()) * element.turnIncrement();
            elementTurns = Math.min(ensureTurns, elementPotentialTurns);
          }
          completeSuccess = element.execute(this, elementTurns) && completeSuccess;
        }
      } catch (err) {
        _iterator3.e(err);
      } finally {
        _iterator3.f();
      }
      this.moreMp(this.options.reserveMp);
      return completeSuccess;
    }
  }], [{
    key: "setDefaultOptions",
    value:
    /**
     * Set default options for new Mood instances.
     *
     * @param options Default options for new Mood instances.
     */
    function setDefaultOptions(options) {
      Mood.defaultOptions = _objectSpread2(_objectSpread2({}, Mood.defaultOptions), options);
    }
  }]);
}();
_defineProperty(Mood, "defaultOptions", {
  songSlots: [],
  mpSources: [MagicalSausages.instance, OscusSoda.instance],
  reserveMp: 0,
  useNativeRestores: false
});

/**
 * Represents an exception thrown when the current KoLmafia version does not
 * match an expected condition.
 */
var KolmafiaVersionError = /*#__PURE__*/function (_Error) {
  function KolmafiaVersionError(message) {
    var _this;
    _classCallCheck(this, KolmafiaVersionError);
    _this = _callSuper(this, KolmafiaVersionError, [message]);
    // Explicitly set the prototype, so that 'instanceof' still works in Node.js
    // even when the class is transpiled down to ES5
    // See: https://github.com/Microsoft/TypeScript-wiki/blob/master/Breaking-Changes.md#extending-built-ins-like-error-array-and-map-may-no-longer-work
    // Note that this code isn't needed for Rhino.
    Object.setPrototypeOf(_this, KolmafiaVersionError.prototype);
    return _this;
  }
  _inherits(KolmafiaVersionError, _Error);
  return _createClass(KolmafiaVersionError);
}(/*#__PURE__*/_wrapNativeSuper(Error));
// Manually set class name, so that the stack trace shows proper name in Rhino
KolmafiaVersionError.prototype.name = "KolmafiaVersionError";
/**
 * Returns the currently executing script name, suitable for embedding in an
 * error message.
 *
 * @returns Path of the main script wrapped in single-quotes, or `"This script"`
 *    if the path cannot be determined
 */
function getScriptName() {
  var _require$main;
  // In Rhino, the current script name is available in require.main.id
  var scriptName = (_require$main = require.main) === null || _require$main === void 0 ? void 0 : _require$main.id;
  return scriptName ? `'${scriptName}'` : "This script";
}
/**
 * If KoLmafia's revision number is less than `revision`, throws an exception.
 * Otherwise, does nothing.
 *
 * This behaves like the `since rXXX;` statement in ASH.
 *
 * @param revision Revision number
 * @throws {KolmafiaVersionError}
 *    If KoLmafia's revision number is less than `revision`.
 * @throws {TypeError} If `revision` is not an integer
 * @example
 * ```ts
 * // Throws if KoLmafia revision is less than r20500
 * sinceKolmafiaRevision(20500);
 * ```
 */
function sinceKolmafiaRevision(revision) {
  if (!Number.isInteger(revision)) {
    throw new TypeError(`Invalid revision number ${revision} (must be an integer)`);
  }
  // Based on net.sourceforge.kolmafia.textui.Parser.sinceException()
  var currentRevision = kolmafia.getRevision();
  if (currentRevision > 0 && currentRevision < revision) {
    throw new KolmafiaVersionError(`${getScriptName()} requires revision r${revision} of kolmafia or higher (current: ${kolmafia.getRevision()}). Up-to-date builds can be found at https://ci.kolmafia.us/.`);
  }
}

var EMPTY_VALUE = Symbol("empty");
/**
 * Fetches a URL and returns the response
 * @param path Path to resource, e.g. "clan_basement.php"
 * @param query Query parameters,
 *  either as an object, e.g. { action: "cleansewer" },
 *  or as a list of [key, value] pairs, e.g. [["action", "cleansewer"]]
 * @param options Additional options
 * @param options.method HTTP method to use, either "GET" or "POST", defaults to "POST"
 * @returns the response from visiting the URL
 */
function fetchUrl(path) {
  var query = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : [];
  var options = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : {};
  var _options$method = options.method,
    method = _options$method === void 0 ? "POST" : _options$method;
  var url = buildUrl(path, query);
  return kolmafia.visitUrl(url, method === "POST", true);
}
/**
 * Builds a URL from a path and query
 * @param path Path to resource, e.g. "clan_basement.php"
 * @param query Query parameters,
 *  either as an object, e.g. { action: "cleansewer" },
 *  or as a list of [key, value] pairs, e.g. [["action", "cleansewer"]]
 * @returns the constructed URL, e.g. "clan_basement.php?action=cleansewer"
 */
function buildUrl(path) {
  var query = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : [];
  var urlParams = Array.isArray(query) ? query : Object.entries(query);
  if (urlParams.length === 0) {
    return path;
  }
  var chunks = [path];
  var sep = path.includes("?") ? "&" : "?";
  var _iterator = _createForOfIteratorHelper(urlParams),
    _step;
  try {
    for (_iterator.s(); !(_step = _iterator.n()).done;) {
      var param = _step.value;
      if (param.length !== 2) {
        throw new Error(`Query parameter array may only contain pair elements`);
      }
      var _param = _slicedToArray(param, 2),
        key = _param[0],
        value = _param[1];
      chunks.push(sep);
      sep = "&";
      chunks.push(encodeURIComponent(key));
      if (value !== EMPTY_VALUE) {
        chunks.push("=");
        chunks.push(encodeURIComponent(value));
      }
    }
  } catch (err) {
    _iterator.e(err);
  } finally {
    _iterator.f();
  }
  return chunks.join("");
}
/**
 * Combines a list of queries into a single query
 * @param queries a list of query objects and/or arrays, can be mixed
 * @returns a single query
 */
function combineQuery() {
  for (var _len = arguments.length, queries = new Array(_len), _key = 0; _key < _len; _key++) {
    queries[_key] = arguments[_key];
  }
  if (queries.length === 1) {
    return queries[0];
  }
  var result = [];
  for (var _i = 0, _queries = queries; _i < _queries.length; _i++) {
    var query = _queries[_i];
    if (Array.isArray(query)) {
      result.push.apply(result, _toConsumableArray(query));
    } else {
      result.push.apply(result, _toConsumableArray(Object.entries(query)));
    }
  }
  return result;
}

var Kmail = /*#__PURE__*/function () {
  function Kmail(rawKmail) {
    _classCallCheck(this, Kmail);
    _defineProperty(this, "id", void 0);
    _defineProperty(this, "date", void 0);
    _defineProperty(this, "type", void 0);
    _defineProperty(this, "senderId", void 0);
    _defineProperty(this, "senderName", void 0);
    _defineProperty(this, "rawMessage", void 0);
    _defineProperty(this, "_parsedMessageParts", void 0);
    this.id = Number(rawKmail.id);
    this.date = new Date(Number(rawKmail.azunixtime) * 1000);
    this.type = rawKmail.type;
    this.senderId = Number(rawKmail.fromid);
    this.senderName = rawKmail.fromname;
    this.rawMessage = rawKmail.message;
  }
  /**
   * Delete the kmail
   *
   * @returns Whether the kmail was deleted
   */
  return _createClass(Kmail, [{
    key: "delete",
    value: function _delete() {
      return Kmail.delete([this]) === 1;
    }
  }, {
    key: "_messageParts",
    get: function get() {
      return this._parsedMessageParts ?? (this._parsedMessageParts = this._parseMessageParts());
    }
  }, {
    key: "_parseMessageParts",
    value: function _parseMessageParts() {
      var text = this.rawMessage;
      var insideText;
      if (this.type === "normal") {
        // strip potential valentine
        if (text.startsWith("<center><table>")) {
          var endIdx = text.indexOf("</center>");
          text = text.slice(endIdx + 9);
        }
      } else if (this.type === "giftshop") {
        var _text$split = text.split("<p>Inside Note:<p>");
        var _text$split2 = _slicedToArray(_text$split, 2);
        text = _text$split2[0];
        insideText = _text$split2[1];
      }
      var split = s => {
        var idx = s.indexOf("<");
        if (idx === -1) return [s];
        return [s.slice(0, idx), s.slice(idx)];
      };
      var _split = split(text),
        _split2 = _slicedToArray(_split, 2),
        outsideNote = _split2[0],
        _split2$ = _split2[1],
        outsideAttachments = _split2$ === void 0 ? null : _split2$;
      var _ref = insideText !== undefined ? split(insideText) : [],
        _ref2 = _slicedToArray(_ref, 2),
        _ref2$ = _ref2[0],
        insideNote = _ref2$ === void 0 ? null : _ref2$,
        _ref2$2 = _ref2[1],
        insideAttachments = _ref2$2 === void 0 ? null : _ref2$2;
      return {
        outsideNote: decodeHTML(outsideNote),
        outsideAttachments,
        insideNote: insideNote && decodeHTML(insideNote),
        insideAttachments
      };
    }
    /**
     * Get message contents without any HTML from items or meat
     *
     * @returns Cleaned message contents
     */
  }, {
    key: "message",
    get: function get() {
      var _this$_messageParts = this._messageParts,
        outsideNote = _this$_messageParts.outsideNote,
        insideNote = _this$_messageParts.insideNote;
      if (insideNote !== null) {
        return `${outsideNote}\n\nInside Note:\n${insideNote}`;
      }
      return outsideNote;
    }
    /**
     * Get the note on the outside of the gift. If the kmail is not a gift,
     * this will be the entire message.
     *
     * @returns Note on the outside of the gift, or the entire message for non-gifts
     */
  }, {
    key: "outsideNote",
    get: function get() {
      return this._messageParts.outsideNote;
    }
    /**
     * Get the note on the inside of the gift
     *
     * @returns Note on the inside of the gift
     */
  }, {
    key: "insideNote",
    get: function get() {
      return this._messageParts.insideNote;
    }
    /**
     * Get items attached to the kmail
     *
     * @returns Map of items attached to the kmail and their quantities
     */
  }, {
    key: "items",
    value: function items() {
      var _this$_messageParts2 = this._messageParts,
        outsideAttachments = _this$_messageParts2.outsideAttachments,
        insideAttachments = _this$_messageParts2.insideAttachments;
      return extractItems(`${outsideAttachments}${insideAttachments}`);
    }
    /**
     * Get items attached to the outside of the gift, which should be
     * just the gift wrapper for giftshop items, and all items for normal kmails
     *
     * @returns Map of items attached to the kmail and their quantities
     */
  }, {
    key: "outsideItems",
    value: function outsideItems() {
      var outsideAttachments = this._messageParts.outsideAttachments;
      if (!outsideAttachments) return new Map();
      return extractItems(outsideAttachments);
    }
    /**
     * Get items attached to the inside of the gift
     *
     * @returns Map of items attached to the kmail and their quantities
     */
  }, {
    key: "insideItems",
    value: function insideItems() {
      var insideAttachments = this._messageParts.insideAttachments;
      if (!insideAttachments) return new Map();
      return extractItems(insideAttachments);
    }
    /**
     * Get meat attached to the kmail
     *
     * @returns Meat attached to the kmail
     */
  }, {
    key: "meat",
    value: function meat() {
      var _this$_messageParts3 = this._messageParts,
        outsideAttachments = _this$_messageParts3.outsideAttachments,
        insideAttachments = _this$_messageParts3.insideAttachments;
      if (!outsideAttachments && !insideAttachments) return 0;
      return kolmafia.extractMeat(`${outsideAttachments}${insideAttachments}`);
    }
    /**
     * Reply to kmail
     *
     * @param message Message with which to reply
     * @param items Items to send
     * @param meat Meat to send
     * @see Kmail.send
     * @returns True if the kmail was successfully sent
     */
  }, {
    key: "reply",
    value: function reply() {
      var message = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : "";
      var items = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : [];
      var meat = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : 0;
      return Kmail.send(this.senderId, message, items, meat);
    }
  }], [{
    key: "parse",
    value:
    /**
     * Parses a kmail from KoL's native format
     *
     * @param rawKmail Kmail in the format supplies by api.php
     * @returns Parsed kmail
     */
    function parse(rawKmail) {
      return new Kmail(rawKmail);
    }
    /**
     * Returns all of the player's kmails
     *
     * @param count Number of kmails to fetch
     * @returns Parsed kmails
     */
  }, {
    key: "inbox",
    value: function inbox() {
      var count = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : 100;
      return JSON.parse(kolmafia.visitUrl(`api.php?what=kmail&for=libram&count=${count}`)).map(Kmail.parse);
    }
    /**
     * Bulk delete kmails
     *
     * @param kmails Kmails to delete
     * @returns Number of kmails deleted
     */
  }, {
    key: "delete",
    value: function _delete(kmails) {
      var _results$match;
      var results = fetchUrl("messages.php", [["the_action", "delete"], ["box", "Inbox"], ["pwd", EMPTY_VALUE]].concat(_toConsumableArray(kmails.map(k => [`sel${k.id}`, "on"]))));
      return Number(((_results$match = results.match(/<td>(\d) messages? deleted.<\/td>/)) === null || _results$match === void 0 ? void 0 : _results$match[1]) ?? 0);
    }
  }, {
    key: "_genericSend",
    value: function _genericSend(to, message, items, meat, chunkSize, constructUrl, successString) {
      var m = meat;
      var sendableItems = _toConsumableArray(arrayToCountedMap(items).entries()).filter(_ref3 => {
        var _ref4 = _slicedToArray(_ref3, 1),
          item = _ref4[0];
        return kolmafia.isGiftable(item);
      });
      var result = true;
      var chunks = chunk(sendableItems, chunkSize);
      // Split the items to be sent into chunks of max 11 item types
      var _iterator = _createForOfIteratorHelper(chunks.length > 0 ? chunks : [null]),
        _step;
      try {
        var _loop = function _loop() {
            var c = _step.value;
            var itemsQuery = {};
            if (c !== null) {
              c.forEach((_ref5, i) => {
                var _ref6 = _slicedToArray(_ref5, 2),
                  item = _ref6[0],
                  quantity = _ref6[1];
                itemsQuery[`whichitem${i + 1}`] = item.id;
                itemsQuery[`howmany${i + 1}`] = quantity;
              });
            }
            var _constructUrl = constructUrl({
                meat: m,
                chunkSize: (c === null || c === void 0 ? void 0 : c.length) ?? 0
              }),
              path = _constructUrl.path,
              query = _constructUrl.query;
            var r = fetchUrl(path, combineQuery(query, itemsQuery));
            if (r.includes("That player cannot receive Meat or items")) {
              return {
                v: Kmail.gift(to, message, items, meat)
              };
            }
            // Make sure we don't send the same batch of meat with every chunk
            m = 0;
            result && (result = r.includes(successString));
          },
          _ret;
        for (_iterator.s(); !(_step = _iterator.n()).done;) {
          _ret = _loop();
          if (_ret) return _ret.v;
        }
      } catch (err) {
        _iterator.e(err);
      } finally {
        _iterator.f();
      }
      return result;
    }
    /**
     * Sends a kmail to a player
     *
     * Sends multiple kmails if more than 11 unique item types are attached.
     * Ignores any ungiftable items.
     * Sends a gift package to players in run
     *
     * @param to The player name or id to receive the kmail
     * @param message The text contents of the message
     * @param items The items to be attached
     * @param meat The quantity of meat to be attached
     * @returns True if the kmail was successfully sent
     */
  }, {
    key: "send",
    value: function send(to) {
      var message = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : "";
      var items = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : [];
      var meat = arguments.length > 3 && arguments[3] !== undefined ? arguments[3] : 0;
      return Kmail._genericSend(to, message, items, meat, 11, _ref7 => {
        var meat = _ref7.meat;
        return {
          path: "sendmessage.php",
          query: {
            action: "send",
            pwd: EMPTY_VALUE,
            towho: to,
            message,
            sendmeat: meat
          }
        };
      }, ">Message sent.</");
    }
    /**
     * Sends a gift to a player
     *
     * Sends multiple kmails if more than 3 unique item types are attached.
     * Ignores any ungiftable items.
     *
     * @param to The player name or id to receive the gift
     * @param message Message to send
     * @param items The items to be attached
     * @param meat The quantity of meat to be attached
     * @param insideNote The note on the inside of the gift
     * @returns True if the gift was successfully sent
     */
  }, {
    key: "gift",
    value: function gift(to) {
      var message = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : "";
      var items = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : [];
      var meat = arguments.length > 3 && arguments[3] !== undefined ? arguments[3] : 0;
      var insideNote = arguments.length > 4 && arguments[4] !== undefined ? arguments[4] : "";
      return Kmail._genericSend(to, message, items, meat, 3, _ref8 => {
        var meat = _ref8.meat,
          chunkSize = _ref8.chunkSize;
        return {
          path: `town_sendgift.php`,
          query: {
            action: "Yep.",
            pwd: EMPTY_VALUE,
            fromwhere: 0,
            note: message,
            insidenote: insideNote,
            towho: to,
            whichpackage: chunkSize,
            sendmeat: meat
          }
        };
      }, ">Package sent.</");
    }
  }]);
}();

/**
 * Return a mapping of the session items, mapping foldable items to a single of their forms
 *
 * @param sessionOnly should closet, DC, and storage be ignored for the session calculation
 * @returns the item session results, with foldables mapped to a single of their folding forms
 */
function mySessionItemsWrapper() {
  var sessionOnly = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : false;
  var manyToOne = (primary, mapped) => mapped.map(target => [target, primary]);
  var foldable = item => manyToOne(item, getFoldGroup(item));
  var itemMappings = new Map([].concat(_toConsumableArray(foldable($item`liar's pants`)), _toConsumableArray(foldable($item`ice pick`)), _toConsumableArray(manyToOne($item`Spooky Putty sheet`, [$item`Spooky Putty monster`].concat(_toConsumableArray(getFoldGroup($item`Spooky Putty sheet`))))), _toConsumableArray(foldable($item`stinky cheese sword`)), _toConsumableArray(foldable($item`naughty paper shuriken`)), _toConsumableArray(foldable($item`Loathing Legion knife`)), _toConsumableArray(foldable($item`deceased crimbo tree`)), _toConsumableArray(foldable($item`makeshift turban`)), _toConsumableArray(foldable($item`turtle wax shield`)), _toConsumableArray(foldable($item`metallic foil bow`)), _toConsumableArray(foldable($item`ironic moustache`)), _toConsumableArray(foldable($item`bugged balaclava`)), _toConsumableArray(foldable($item`toggle switch (Bartend)`)), _toConsumableArray(foldable($item`mushroom cap`)), _toConsumableArray(manyToOne($item`can of Rain-Doh`, $items`empty Rain-Doh can`)), _toConsumableArray(manyToOne($item`meteorite fragment`, $items`meteorite earring, meteorite necklace, meteorite ring`)), _toConsumableArray(manyToOne($item`Sneaky Pete's leather jacket`, $items`Sneaky Pete's leather jacket (collar popped)`)), _toConsumableArray(manyToOne($item`Boris's Helm`, $items`Boris's Helm (askew)`)), _toConsumableArray(manyToOne($item`Jarlsberg's pan`, $items`Jarlsberg's pan (Cosmic portal mode)`)), _toConsumableArray(manyToOne($item`tiny plastic sword`, $items`grogtini, bodyslam, dirty martini, vesper, cherry bomb, sangria del diablo`)), _toConsumableArray(manyToOne($item`earthenware muffin tin`, $items`blueberry muffin, bran muffin, chocolate chip muffin`)), _toConsumableArray(manyToOne($item`ChibiBuddy™ (on)`, $items`ChibiBuddy™ (off)`))));
  var inventory = new Map();
  var invLocations = sessionOnly ? [kolmafia.mySessionItems] : [kolmafia.mySessionItems, kolmafia.getCloset, kolmafia.getDisplay, kolmafia.getStorage];
  if (!sessionOnly) {
    for (var _i = 0, _Object$entries = Object.entries(kolmafia.getCampground()); _i < _Object$entries.length; _i++) {
      var _Object$entries$_i = _slicedToArray(_Object$entries[_i], 2),
        itemStr = _Object$entries$_i[0],
        quantity = _Object$entries$_i[1];
      if (!quantity) continue;
      var item = kolmafia.toItem(itemStr);
      if (item === $item`big rock`) continue; // Used to represent an empty house slot
      var mappedItem = itemMappings.get(item) ?? item;
      inventory.set(mappedItem, quantity + (inventory.get(mappedItem) ?? 0));
    }
  }
  for (var _i2 = 0, _invLocations = invLocations; _i2 < _invLocations.length; _i2++) {
    var inventoryFunc = _invLocations[_i2];
    for (var _i3 = 0, _Object$entries2 = Object.entries(inventoryFunc()); _i3 < _Object$entries2.length; _i3++) {
      var _Object$entries2$_i = _slicedToArray(_Object$entries2[_i3], 2),
        _itemStr = _Object$entries2$_i[0],
        _quantity = _Object$entries2$_i[1];
      if (!_quantity) continue;
      var _item = kolmafia.toItem(_itemStr);
      var _mappedItem = itemMappings.get(_item) ?? _item;
      inventory.set(_mappedItem, _quantity + (inventory.get(_mappedItem) ?? 0));
      if (inventory.get(_mappedItem) === 0) inventory.delete(_mappedItem);
    }
  }
  return inventory;
}
/**
 * Perform a binary element-wise operation on two inventories
 *
 * @param a The LHS inventory to perform the operation on
 * @param b The RHS inventory to perform the operation on
 * @param op an operator to compute between the sets
 * @returns a new map representing the combined inventories
 */
function inventoryOperation(a, b, op) {
  // return every entry that is in a and not in b
  var difference = new Map();
  var _iterator = _createForOfIteratorHelper(new Set([].concat(_toConsumableArray(a.keys()), _toConsumableArray(b.keys())))),
    _step;
  try {
    for (_iterator.s(); !(_step = _iterator.n()).done;) {
      var item = _step.value;
      difference.set(item, op(a.get(item) ?? 0, b.get(item) ?? 0));
    }
  } catch (err) {
    _iterator.e(err);
  } finally {
    _iterator.f();
  }
  var diffEntries = _toConsumableArray(difference.entries());
  return new Map(diffEntries.filter(_ref => {
    var _ref2 = _slicedToArray(_ref, 2),
      value = _ref2[1];
    return value !== 0;
  }));
}
/**
 * A wrapper around tracking items and meat gained from this session
 * Smartly handles foldables being added/removed based on their state
 * Provides operations to add sessions and subtract Sessions so you can isolate the value of each Session using a baseline
 *
 * @member meat the raw meat associated with this Session
 * @member items a map representing the items gained/lost during this Session
 */
var Session = /*#__PURE__*/function () {
  /**
   * Construct a new session
   *
   * @param meat the amount of meat associated with this session
   * @param items the items associated with this session
   * @param totalTurns the number of turns associated with this session
   */
  function Session(meat, items, totalTurns) {
    _classCallCheck(this, Session);
    _defineProperty(this, "meat", void 0);
    _defineProperty(this, "items", void 0);
    _defineProperty(this, "totalTurns", void 0);
    this.meat = meat;
    this.items = items;
    this.totalTurns = totalTurns;
  }
  /**
   * Register session results that do not get tracked natively
   *
   * @param target either the Item or a string saying "meat" of what quantity to modify
   * @param quantity How much to modify the tracked amount by
   */
  return _createClass(Session, [{
    key: "register",
    value: function register(target, quantity) {
      if (target === "meat") {
        this.meat += quantity;
      } else {
        this.items.set(target, (this.items.get(target) ?? 0) + quantity);
      }
    }
    /**
     * Value this session
     *
     * @param itemValue a function that, when given an item, will give a meat value of the item
     * @returns ItemResult with the full value of this session given the input function
     */
  }, {
    key: "value",
    value: function value(itemValue) {
      // TODO: add garbo specific pricing (sugar equipment for synth, etc.)
      var turns = this.totalTurns;
      var meat = Math.floor(this.meat);
      var itemDetails = _toConsumableArray(this.items.entries()).map(_ref3 => {
        var _ref4 = _slicedToArray(_ref3, 2),
          item = _ref4[0],
          quantity = _ref4[1];
        return {
          item,
          quantity,
          // only run itemValue if quantity is nonzero
          value: quantity ? itemValue(item) * quantity : 0
        };
      });
      var items = Math.floor(sum(itemDetails, "value"));
      return {
        meat,
        items,
        total: meat + items,
        itemDetails,
        turns
      };
    }
    /**
     * Subtract the contents of another session from this one, removing any items that have a resulting quantity of 0
     *  (this will ignore elements in b but not in a)
     *
     * @param other the session from which to pull values to remove from this session
     * @returns a new session representing the difference between this session and the other session
     */
  }, {
    key: "diff",
    value: function diff(other) {
      return new Session(this.meat - other.meat, inventoryOperation(this.items, other.items, (a, b) => a - b), this.totalTurns - other.totalTurns);
    }
    /**
     * Subtract the contents of snasphot b from session a, removing any items that have a resulting quantity of 0
     *  (this will ignore elements in b but not in a)
     *
     * @param a the session from which to subtract elements
     * @param b the session from which to add elements
     * @returns a new session representing the difference between a and b
     */
  }, {
    key: "add",
    value:
    /**
     * Generate a new session combining multiple sessions together
     *
     * @param other the session from which to add elements to this set
     * @returns a new session representing the addition of other to this
     */
    function add(other) {
      return new Session(this.meat + other.meat, inventoryOperation(this.items, other.items, (a, b) => a + b), this.totalTurns + other.totalTurns);
    }
    /**
     * Combine the contents of sessions
     *
     * @param sessions the set of sessions to combine together
     * @returns a new session representing the difference between a and b
     */
  }, {
    key: "toFile",
    value:
    /**
     * Export this session to a file in the data/ directory. Conventionally this file should end in ".json"
     *
     * @param filename The file into which to export
     */
    function toFile(filename) {
      var val = {
        meat: this.meat,
        items: Object.fromEntries(_toConsumableArray(this.items.entries()).sort((_ref5, _ref6) => {
          var _ref7 = _slicedToArray(_ref5, 1),
            a = _ref7[0];
          var _ref8 = _slicedToArray(_ref6, 1),
            b = _ref8[0];
          return a.id - b.id;
        })),
        totalTurns: this.totalTurns
      };
      kolmafia.bufferToFile(JSON.stringify(val, null, 2), Session.getFilepath(filename));
    }
    /**
     * Import a session from a file in the data/ directory. Conventionally the file should end in ".json"
     *
     * @param filename The file from which to import
     * @returns the session represented by the file
     */
  }, {
    key: "computeMPA",
    value:
    /**
     * @param other the session to diff against this session when computing MPA
     * @param options options for computing MPA
     * @param options.value a function to compute the meat value of a given item
     * @param options.isOutlier a function to compute if an item is considered an outlier. By default, no items are outliers
     * @param options.excludeValue meat values to exclude when calculating specific portions of the MPA
     * @param options.excludeValue.meat how much meat to exclude when calculating the meat portion of MPA
     * @param options.excludeValue.item how much meat to exclude when calculating hte item portion of MPA
     * @returns an analysis of the effective MPA for the given session
     */
    function computeMPA(other, options) {
      return Session.computeMPA(this, other, options);
    }
  }], [{
    key: "diff",
    value: function diff(a, b) {
      return a.diff(b);
    }
  }, {
    key: "add",
    value: function add() {
      for (var _len = arguments.length, sessions = new Array(_len), _key = 0; _key < _len; _key++) {
        sessions[_key] = arguments[_key];
      }
      return sessions.reduce((previousSession, currentSession) => previousSession.add(currentSession));
    }
  }, {
    key: "getFilepath",
    value: function getFilepath(filename) {
      return filename.endsWith(".json") ? filename : `snapshots/${kolmafia.myName()}/${kolmafia.todayToString()}_${filename}.json`;
    }
  }, {
    key: "fromFile",
    value: function fromFile(filename) {
      var fileValue = kolmafia.fileToBuffer(Session.getFilepath(filename));
      // fileToBuffer returns empty string for files that don't exist
      if (fileValue.length > 0) {
        var val = JSON.parse(fileValue);
        var parsedItems = Object.entries(val.items).map(_ref9 => {
          var _ref0 = _slicedToArray(_ref9, 2),
            itemStr = _ref0[0],
            quantity = _ref0[1];
          return [kolmafia.toItem(itemStr), quantity];
        });
        return new Session(val.meat, new Map(parsedItems), val.totalTurns ?? 0);
      } else {
        // if the file does not exist, return an empty session
        return new Session(0, new Map(), 0);
      }
    }
    /**
     * Return the meat and items for the current session
     *
     * @param sessionOnly should closet, DC, and storage be ignored for the session calculation
     * @returns current session
     */
  }, {
    key: "current",
    value: function current() {
      var sessionOnly = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : false;
      var meat = sessionOnly ? [kolmafia.mySessionMeat] : [kolmafia.mySessionMeat, kolmafia.myClosetMeat, kolmafia.myStorageMeat];
      return new Session(sum(meat, f => f()), mySessionItemsWrapper(sessionOnly), kolmafia.totalTurnsPlayed());
    }
    /**
     * @param baseline the base session to use when computing MPA
     * @param full the full session to use when computing MPA
     * @param options options for computing MPA
     * @param options.value a function to compute the meat value of a given item
     * @param options.isOutlier a function to compute if an item is considered an outlier. By default, no items are outliers
     * @param options.excludeValue meat values to exclude when calculating specific portions of the MPA
     * @param options.excludeValue.meat how much meat to exclude when calculating the meat portion of MPA
     * @param options.excludeValue.item how much meat to exclude when calculating hte item portion of MPA
     * @returns an analysis of the effective MPA for the given session
     */
  }, {
    key: "computeMPA",
    value: function computeMPA(baseline, full, options) {
      var value = options.value;
      var excludeValue = options.excludeValue ?? {
        meat: 0,
        item: 0
      };
      var isOutlier = options.isOutlier;
      var result = full.diff(baseline).value(value);
      var meatValue = result.meat - (excludeValue.meat ?? 0);
      var outlierItems = isOutlier ? result.itemDetails.filter(isOutlier) : [];
      var outliersValue = sum(outlierItems, detail => detail.value);
      var itemValue = result.items - outliersValue - (excludeValue.item ?? 0);
      var turns = result.turns;
      return {
        mpa: {
          effective: (meatValue + itemValue) / turns,
          total: (meatValue + itemValue + outliersValue) / turns,
          meat: meatValue / turns,
          items: itemValue / turns
        },
        values: {
          effective: meatValue + itemValue,
          total: meatValue + itemValue + outliersValue,
          meat: meatValue,
          items: itemValue
        },
        outlierItems: outlierItems,
        turns: turns
      };
    }
  }]);
}();

/**
 * The strategy to use for combat for a task, which indicates what to do
 * for each monster.
 *
 * There are two ways to specify in a task what to do for a given monster:
 *   1. Provide a macro directly through .macro(macro, ...monsters)
 *   2. Provide an action through .action(action, ...monsters)
 *
 * An action is a strategy for dealing with a monster that is not fully
 * defined in the task. The possible actions are set with the type parameter A.
 * Actions should typically end the fight.
 *
 * For example, a task may want to banish a monster but not necessarily know or
 * care which banisher is used. Instead, it is best for the engine to determine
 * which banisher to use on the monster. To facilitate this, "banish" can be
 * defined as an action, e.g. with CombatStrategy<"banish">;
 *
 * Each action can be resolved by the engine by:
 *   1. Providing a default macro for the action through ActionDefaults<A>,
 *      which can be done through combat_defaults in Engine options, or
 *   2. Providing a CombatResource for the action through CombatResources<A>.
 *      This is typically done in Engine.customize() by checking if a given
 *      action is requested by the task with combat.can(.), and then providing
 *      an appropriate resource with resources.provide(.).
 *
 * A monster may have both a macro and an action defined, and a macro or action
 * can be specified to be done on all monsters. The order of combat is then:
 * 1. The macro(s) given in .startingMacro().
 * 2. The monster-specific macro(s) from .macro().
 * 3. The general macro(s) from .macro().
 * 4. The monster-specific action from .action().
 * 5. The general action from .action().
 *
 * If an autoattack is set with .autoattack(), the order of the autoattack is:
 * 1. The monster-specific macro(s) from .autoattack().
 * 2. The general macro(s) from .autoattack().
 */
var CombatStrategy = /*#__PURE__*/function () {
  function CombatStrategy() {
    _classCallCheck(this, CombatStrategy);
    this.macros = new Map();
    this.autoattacks = new Map();
    this.actions = new Map();
    this.ccs_entries = new Map();
  }
  /**
   * Add a macro to perform for this monster. If multiple macros are given
   * for the same monster, they are concatinated.
   *
   * @param macro The macro to perform.
   * @param monsters Which monsters to use the macro on. If not given, add the
   *  macro as a general macro.
   * @param prepend If true, add the macro before all previous macros for
   *    the same monster. If false, add after all previous macros.
   * @returns this
   */
  return _createClass(CombatStrategy, [{
    key: "macro",
    value: function macro(_macro, monsters, prepend) {
      var _a, _b;
      if (monsters === undefined) {
        if (this.default_macro === undefined) this.default_macro = [];
        if (prepend) this.default_macro.unshift(_macro);else this.default_macro.push(_macro);
      } else {
        if (monsters instanceof kolmafia.Monster) monsters = [monsters];
        var _iterator = _createForOfIteratorHelper(monsters),
          _step;
        try {
          for (_iterator.s(); !(_step = _iterator.n()).done;) {
            var monster = _step.value;
            if (!this.macros.has(monster)) this.macros.set(monster, []);
            if (prepend) (_a = this.macros.get(monster)) === null || _a === void 0 ? void 0 : _a.unshift(_macro);else (_b = this.macros.get(monster)) === null || _b === void 0 ? void 0 : _b.push(_macro);
          }
        } catch (err) {
          _iterator.e(err);
        } finally {
          _iterator.f();
        }
      }
      return this;
    }
    /**
     * Add a macro to perform as an autoattack for this monster. If multiple
     * macros are given for the same monster, they are concatinated.
     *
     * @param macro The macro to perform as autoattack.
     * @param monsters Which monsters to use the macro on. If not given, add the
     *  macro as a general macro.
     * @param prepend If true, add the macro before all previous autoattack
     *    macros for the same monster. If false, add after all previous macros.
     * @returns this
     */
  }, {
    key: "autoattack",
    value: function autoattack(macro, monsters, prepend) {
      var _a, _b;
      if (monsters === undefined) {
        if (this.default_autoattack === undefined) this.default_autoattack = [];
        if (prepend) this.default_autoattack.unshift(macro);else this.default_autoattack.push(macro);
      } else {
        if (monsters instanceof kolmafia.Monster) monsters = [monsters];
        var _iterator2 = _createForOfIteratorHelper(monsters),
          _step2;
        try {
          for (_iterator2.s(); !(_step2 = _iterator2.n()).done;) {
            var monster = _step2.value;
            if (!this.autoattacks.has(monster)) this.autoattacks.set(monster, []);
            if (prepend) (_a = this.autoattacks.get(monster)) === null || _a === void 0 ? void 0 : _a.unshift(macro);else (_b = this.autoattacks.get(monster)) === null || _b === void 0 ? void 0 : _b.push(macro);
          }
        } catch (err) {
          _iterator2.e(err);
        } finally {
          _iterator2.f();
        }
      }
      return this;
    }
    /**
     * Add a macro to perform at the start of combat.
     * @param macro The macro to perform.
     * @param prepend If true, add the macro before all previous starting
     *    macros. If false, add after all previous starting macros.
     * @returns this
     */
  }, {
    key: "startingMacro",
    value: function startingMacro(macro, prepend) {
      if (this.starting_macro === undefined) this.starting_macro = [];
      if (prepend) this.starting_macro.unshift(macro);else this.starting_macro.push(macro);
      return this;
    }
    /**
     * Add an action to perform for this monster. Only one action can be set for
     * each monster; any previous actions are overwritten.
     *
     * @param action The action to perform.
     * @param monsters Which monsters to use the action on. If not given, set the
     *  action as the general action for all monsters.
     * @returns this
     */
  }, {
    key: "action",
    value: function action(_action, monsters) {
      if (monsters === undefined) {
        this.default_action = _action;
      } else if (monsters instanceof kolmafia.Monster) {
        this.actions.set(monsters, _action);
      } else {
        var _iterator3 = _createForOfIteratorHelper(monsters),
          _step3;
        try {
          for (_iterator3.s(); !(_step3 = _iterator3.n()).done;) {
            var monster = _step3.value;
            this.actions.set(monster, _action);
          }
        } catch (err) {
          _iterator3.e(err);
        } finally {
          _iterator3.f();
        }
      }
      return this;
    }
    /**
     * Add a separate entry in the grimoire-generated CCS file for the specified
     * monster. If multiple entries are given for the same monster, they are
     * concatinated.
     *
     * This should typically be only used rarely, on monsters for which KoL does
     * not support macros in combat (e.g. rampaging adding machine).
     *
     * @param entry The entry to add for the given monster.
     * @param monsters Which monsters to add the entry to.
     * @param prepend If true, add the entry before all previous entries. If
     *   false, add after all previous entries.
     */
  }, {
    key: "ccs",
    value: function ccs(entry, monsters, prepend) {
      var _a, _b;
      if (monsters instanceof kolmafia.Monster) monsters = [monsters];
      var _iterator4 = _createForOfIteratorHelper(monsters),
        _step4;
      try {
        for (_iterator4.s(); !(_step4 = _iterator4.n()).done;) {
          var monster = _step4.value;
          if (!this.ccs_entries.has(monster)) this.ccs_entries.set(monster, []);
          if (prepend) (_a = this.ccs_entries.get(monster)) === null || _a === void 0 ? void 0 : _a.unshift(entry);else (_b = this.ccs_entries.get(monster)) === null || _b === void 0 ? void 0 : _b.push(entry);
        }
      } catch (err) {
        _iterator4.e(err);
      } finally {
        _iterator4.f();
      }
      return this;
    }
    /**
     * Check if the provided action was requested for any monsters, or for the
     * general action.
     */
  }, {
    key: "can",
    value: function can(action) {
      if (action === this.default_action) return true;
      return Array.from(this.actions.values()).includes(action);
    }
    /**
     * Return the general action (if it exists).
     */
  }, {
    key: "getDefaultAction",
    value: function getDefaultAction() {
      return this.default_action;
    }
    /**
     * Return all monsters where the provided action was requested.
     */
  }, {
    key: "where",
    value: function where(action) {
      return Array.from(this.actions.keys()).filter(key => this.actions.get(key) === action);
    }
    /**
     * Return the requested action (if it exists) for the provided monster.
     */
  }, {
    key: "currentStrategy",
    value: function currentStrategy(monster) {
      var _a;
      return (_a = this.actions.get(monster)) !== null && _a !== void 0 ? _a : this.default_action;
    }
    /**
     * Perform a deep copy of this combat strategy.
     */
  }, {
    key: "clone",
    value: function clone() {
      var result = new CombatStrategy();
      if (this.starting_macro) result.starting_macro = _toConsumableArray(this.starting_macro);
      if (this.default_macro) result.default_macro = _toConsumableArray(this.default_macro);
      var _iterator5 = _createForOfIteratorHelper(this.macros),
        _step5;
      try {
        for (_iterator5.s(); !(_step5 = _iterator5.n()).done;) {
          var pair = _step5.value;
          result.macros.set(pair[0], _toConsumableArray(pair[1]));
        }
      } catch (err) {
        _iterator5.e(err);
      } finally {
        _iterator5.f();
      }
      if (this.default_autoattack) result.default_autoattack = _toConsumableArray(this.default_autoattack);
      var _iterator6 = _createForOfIteratorHelper(this.autoattacks),
        _step6;
      try {
        for (_iterator6.s(); !(_step6 = _iterator6.n()).done;) {
          var _pair = _step6.value;
          result.autoattacks.set(_pair[0], _toConsumableArray(_pair[1]));
        }
      } catch (err) {
        _iterator6.e(err);
      } finally {
        _iterator6.f();
      }
      result.default_action = this.default_action;
      var _iterator7 = _createForOfIteratorHelper(this.actions),
        _step7;
      try {
        for (_iterator7.s(); !(_step7 = _iterator7.n()).done;) {
          var _pair2 = _step7.value;
          result.actions.set(_pair2[0], _pair2[1]);
        }
      } catch (err) {
        _iterator7.e(err);
      } finally {
        _iterator7.f();
      }
      var _iterator8 = _createForOfIteratorHelper(this.ccs_entries),
        _step8;
      try {
        for (_iterator8.s(); !(_step8 = _iterator8.n()).done;) {
          var _pair3 = _step8.value;
          result.ccs_entries.set(_pair3[0], _toConsumableArray(_pair3[1]));
        }
      } catch (err) {
        _iterator8.e(err);
      } finally {
        _iterator8.f();
      }
      return result;
    }
    /**
     * Compile this combat strategy into a complete macro.
     *
     * @param resources The resources to use to fulfil actions.
     * @param defaults Macros to perform for each action without a resource.
     * @param location The adventuring location, if known.
     * @param ctx: The current engine state to be passed to task functions.
     * @returns The compiled macro.
     */
  }, {
    key: "compile",
    value: function compile(resources, defaults, location, ctx) {
      var _a, _b;
      var result = new Macro$1();
      // If there is macro precursor, do it now
      if (this.starting_macro) {
        result.step.apply(result, _toConsumableArray(this.starting_macro.map(macro => undelay(macro, ctx))));
      }
      // Perform any monster-specific macros (these may or may not end the fight)
      var monster_macros = new CompressedMacro();
      this.macros.forEach((value, key) => {
        var _Macro;
        monster_macros.add(key, (_Macro = new Macro$1()).step.apply(_Macro, _toConsumableArray(value.map(macro => undelay(macro, ctx)))));
      });
      result.step(monster_macros.compile());
      // Perform the non-monster specific macro
      if (this.default_macro) result.step.apply(result, _toConsumableArray(this.default_macro.map(macro => undelay(macro, ctx))));
      // Perform any monster-specific actions (these should end the fight)
      var monster_actions = new CompressedMacro();
      this.actions.forEach((action, key) => {
        var _a, _b;
        var macro = (_a = resources.getMacro(action, ctx)) !== null && _a !== void 0 ? _a : (_b = defaults === null || defaults === void 0 ? void 0 : defaults[action]) === null || _b === void 0 ? void 0 : _b.call(defaults, key);
        if (macro) monster_actions.add(key, new Macro$1().step(macro));
      });
      result.step(monster_actions.compile());
      // Perform the non-monster specific action (these should end the fight)
      if (this.default_action) {
        var macro = (_a = resources.getMacro(this.default_action, ctx)) !== null && _a !== void 0 ? _a : (_b = defaults === null || defaults === void 0 ? void 0 : defaults[this.default_action]) === null || _b === void 0 ? void 0 : _b.call(defaults, location);
        if (macro) result.step(macro);
      }
      return result;
    }
    /**
     * Compile the autoattack of this combat strategy into a complete macro.
     *
     * @param ctx: The current engine state to be passed to task functions.
     * @returns The compiled autoattack macro.
     */
  }, {
    key: "compileAutoattack",
    value: function compileAutoattack(ctx) {
      var result = new Macro$1();
      // Perform any monster-specific autoattacks (these may or may not end the fight)
      var monster_macros = new CompressedMacro();
      this.autoattacks.forEach((value, key) => {
        var _Macro2;
        monster_macros.add(key, (_Macro2 = new Macro$1()).step.apply(_Macro2, _toConsumableArray(value.map(macro => undelay(macro, ctx)))));
      });
      result.step(monster_macros.compile());
      // Perform the non-monster specific macro
      if (this.default_autoattack) result.step.apply(result, _toConsumableArray(this.default_autoattack.map(macro => undelay(macro, ctx))));
      return result;
    }
    /**
     * Compile the CCS entries of this combat strategy into a single array.
     *
     * @returns The lines of a CCS file, not including the [default] macro.
     */
  }, {
    key: "compileCcs",
    value: function compileCcs() {
      var result = [];
      var _iterator9 = _createForOfIteratorHelper(this.ccs_entries),
        _step9;
      try {
        for (_iterator9.s(); !(_step9 = _iterator9.n()).done;) {
          var ccs_entry = _step9.value;
          result.push.apply(result, [`[${ccs_entry[0].name}]`].concat(_toConsumableArray(ccs_entry[1])));
        }
      } catch (err) {
        _iterator9.e(err);
      } finally {
        _iterator9.f();
      }
      return result;
    }
    /**
     * For advanced users, this method will generate a fluent API for requesting
     * actions. That is, it allows you to do
     *   combat.banish(monster1).kill(monster2)
     * instead of
     *   combat.action("banish", monster1).action("kill", monster2)
     *
     * Example usage:
     *   const myActions = ["kill", "banish"] as const;
     *   class MyCombatStrategy extends CombatStrategy.withActions(myActions) {}
     *
     *   const foo: MyCombatStrategy = new MyCombatStrategy();
     *   const bar: MyCombatStrategy = foo.banish($monster`crate`).kill($monster`tumbleweed`);
     */
  }], [{
    key: "withActions",
    value: function withActions(actions) {
      var CombatStrategyWithActions = /*#__PURE__*/function (_this) {
        function CombatStrategyWithActions() {
          _classCallCheck(this, CombatStrategyWithActions);
          return _callSuper(this, CombatStrategyWithActions, arguments);
        }
        _inherits(CombatStrategyWithActions, _this);
        return _createClass(CombatStrategyWithActions);
      }(this); // eslint-disable-next-line @typescript-eslint/no-explicit-any
      var proto = CombatStrategyWithActions.prototype;
      var _iterator0 = _createForOfIteratorHelper(actions),
        _step0;
      try {
        var _loop = function _loop() {
          var action = _step0.value;
          proto[action] = function (monsters) {
            return this.action(action, monsters);
          };
        };
        for (_iterator0.s(); !(_step0 = _iterator0.n()).done;) {
          _loop();
        }
        // eslint-disable-next-line @typescript-eslint/no-explicit-any
      } catch (err) {
        _iterator0.e(err);
      } finally {
        _iterator0.f();
      }
      return CombatStrategyWithActions;
    }
  }]);
}();
/**
 * A class to build a macro that combines if statements (keyed on monster) with
 * identical body into a single if statement, to avoid the 37-action limit.
 * Ex: [if x; A; if y; B; if z; A;] will turn into [if x || z; A; if y; B]
 */
var CompressedMacro = /*#__PURE__*/function () {
  function CompressedMacro() {
    _classCallCheck(this, CompressedMacro);
    this.components = new Map();
  }
  /**
   * Set the macro for a given monster (replacing any previous macros).
   */
  return _createClass(CompressedMacro, [{
    key: "add",
    value: function add(monster, macro) {
      var _a;
      var macro_text = macro.toString();
      if (macro_text.length === 0) return;
      if (!this.components.has(macro_text)) this.components.set(macro_text, [monster]);else (_a = this.components.get(macro_text)) === null || _a === void 0 ? void 0 : _a.push(monster);
    }
    /**
     * Compile the compressed form of the macro.
     */
  }, {
    key: "compile",
    value: function compile() {
      var result = new Macro$1();
      this.components.forEach((monsters, macro) => {
        var condition = monsters.map(mon => `monsterid ${mon.id}`).join(" || ");
        result.if_(condition, macro);
      });
      return result;
    }
  }]);
}();
/**
 * A class for providing resources to fulfil combat actions.
 */
var CombatResources = /*#__PURE__*/function () {
  function CombatResources() {
    _classCallCheck(this, CombatResources);
    this.resources = new Map();
  }
  /**
   * Use the provided resource to fulfil the provided action.
   * (If the resource is undefined, this does nothing).
   */
  return _createClass(CombatResources, [{
    key: "provide",
    value: function provide(action, resource) {
      if (resource === undefined) return;
      this.resources.set(action, resource);
    }
    /**
     * Return true if the provided action has a resource provided.
     */
  }, {
    key: "has",
    value: function has(action) {
      return this.resources.has(action);
    }
    /**
     * Returns the resource for the provided action, if set.
     */
  }, {
    key: "get",
    value: function get(action) {
      return this.resources.get(action);
    }
    /**
     * Return all provided combat resources.
     */
  }, {
    key: "all",
    value: function all() {
      return Array.from(this.resources.values());
    }
    /**
     * Get the macro provided by the resource for this action, or undefined if
     * no resource was provided.
     */
  }, {
    key: "getMacro",
    value: function getMacro(action, ctx) {
      var resource = this.resources.get(action);
      if (resource === undefined) return undefined;
      if (resource.do instanceof kolmafia.Item) return new Macro$1().item(resource.do);
      if (resource.do instanceof kolmafia.Skill) return new Macro$1().skill(resource.do);
      return undelay(resource.do, ctx);
    }
  }]);
}();

var FORCE_REFRESH_REQUIREMENT = new Requirement([], {
  forceUpdate: true
});
var outfitSlots$1 = ["hat", "back", "weapon", "offhand", "shirt", "pants", "acc1", "acc2", "acc3", "famequip"];
var weaponHands = i => i ? kolmafia.weaponHands(i) : 0;
var modeableCommands = ["backupcamera", "umbrella", "snowsuit", "edpiece", "retrocape", "parka", "jillcandle"];
var Outfit = /*#__PURE__*/function () {
  function Outfit() {
    _classCallCheck(this, Outfit);
    this.equips = new Map();
    this.riders = new Map();
    this.modes = {};
    this.skipDefaults = false;
    this.modifier = [];
    this.avoid = [];
    this.bonuses = new Map();
    this.postActions = [];
    this.preActions = [];
  }
  /**
   * Create an outfit from your current player state.
   */
  return _createClass(Outfit, [{
    key: "equippedAmount",
    value:
    /**
     * Check how many of an item is equipped on the outfit.
     */
    function equippedAmount(item) {
      return _toConsumableArray(this.equips.values()).filter(i => i === item).length;
    }
  }, {
    key: "isAvailable",
    value: function isAvailable(item) {
      var _a;
      if ((_a = this.avoid) === null || _a === void 0 ? void 0 : _a.includes(item)) return false;
      if (!have$P(item, this.equippedAmount(item) + 1)) return false;
      if (kolmafia.booleanModifier(item, "Single Equip") && this.equippedAmount(item) > 0) return false;
      return true;
    }
    /**
     * Check whether an item is equipped on the outfit, optionally in a specific slot.
     */
  }, {
    key: "haveEquipped",
    value: function haveEquipped(item, slot) {
      if (slot === undefined) return this.equippedAmount(item) > 0;
      return this.equips.get(slot) === item;
    }
  }, {
    key: "equipItemNone",
    value: function equipItemNone(item, slot) {
      if (item !== $item.none) return false;
      if (slot === undefined) return true;
      if (this.equips.has(slot)) return false;
      this.equips.set(slot, item);
      return true;
    }
  }, {
    key: "equipNonAccessory",
    value: function equipNonAccessory(item, slot) {
      if ($slots`acc1, acc2, acc3`.includes(kolmafia.toSlot(item))) return false;
      if (slot !== undefined && slot !== kolmafia.toSlot(item)) return false;
      if (this.equips.has(kolmafia.toSlot(item))) return false;
      switch (kolmafia.toSlot(item)) {
        case $slot`off-hand`:
          if (this.equips.has($slot`weapon`) && weaponHands(this.equips.get($slot`weapon`)) !== 1) {
            return false;
          }
          break;
        case $slot`familiar`:
          if (this.familiar !== undefined && !kolmafia.canEquip(this.familiar, item)) return false;
          break;
        case $slot`weapon`:
          if (!weaponsCompatible(item, this.equips.get($slot`off-hand`))) return false;
          break;
      }
      if (kolmafia.toSlot(item) !== $slot`familiar` && !kolmafia.canEquip(item)) return false;
      this.equips.set(kolmafia.toSlot(item), item);
      return true;
    }
  }, {
    key: "equipAccessory",
    value: function equipAccessory(item, slot) {
      if (![undefined].concat(_toConsumableArray($slots`acc1, acc2, acc3`)).includes(slot)) return false;
      if (kolmafia.toSlot(item) !== $slot`acc1`) return false;
      if (!kolmafia.canEquip(item)) return false;
      if (slot === undefined) {
        // We don't care which of the accessory slots we equip in
        var empty = $slots`acc1, acc2, acc3`.find(s => !this.equips.has(s));
        if (empty === undefined) return false;
        this.equips.set(empty, item);
      } else {
        if (this.equips.has(slot)) return false;
        this.equips.set(slot, item);
      }
      return true;
    }
  }, {
    key: "equipUsingDualWield",
    value: function equipUsingDualWield(item, slot) {
      if (![undefined, $slot`off-hand`].includes(slot)) return false;
      if (kolmafia.toSlot(item) !== $slot`weapon`) return false;
      if (this.equips.has($slot`weapon`) && weaponHands(this.equips.get($slot`weapon`)) !== 1) {
        return false;
      }
      if (this.equips.has($slot`off-hand`)) return false;
      if (!have$P($skill`Double-Fisted Skull Smashing`)) return false;
      if (weaponHands(item) !== 1) return false;
      if (!kolmafia.canEquip(item)) return false;
      if (!weaponsCompatible(this.equips.get($slot`weapon`), item)) return false;
      this.equips.set($slot`off-hand`, item);
      return true;
    }
  }, {
    key: "getHoldingFamiliar",
    value: function getHoldingFamiliar(item) {
      switch (kolmafia.toSlot(item)) {
        case $slot`weapon`:
          return $familiar`Disembodied Hand`;
        case $slot`off-hand`:
          return $familiar`Left-Hand Man`;
        case $slot`pants`:
          return $familiar`Fancypants Scarecrow`;
        case $slot`hat`:
          return $familiar`Mad Hatrack`;
        default:
          return undefined;
      }
    }
    /**
     * Returns the bonus value associated with a given item.
     *
     * @param item The item to check the bonus of.
     * @returns The bonus assigned to that item.
     */
  }, {
    key: "getBonus",
    value: function getBonus(item) {
      var _a;
      return (_a = this.bonuses.get(item)) !== null && _a !== void 0 ? _a : 0;
    }
    /**
     * Applies a value to any existing bonus this item has, using a rule assigned by the `reducer` parameter
     *
     * @param item The item to try to apply a bonus to.
     * @param value The value to try to apply.
     * @param reducer Function that combines new and current bonus
     * @returns The total assigned bonus to that item.
     */
  }, {
    key: "applyBonus",
    value: function applyBonus(item, value, reducer) {
      var previous = this.getBonus(item);
      return this.setBonus(item, reducer(value, previous));
    }
    /**
     * Sets the bonus value of an item equal to a given value, overriding any current bonus assigned.
     *
     * @param item The item to try to apply a bonus to.
     * @param value The value to try to apply.
     * @returns The total assigned bonus to that item.
     */
  }, {
    key: "setBonus",
    value: function setBonus(item, value) {
      this.bonuses.set(item, value);
      return value;
    }
    /**
     * Adds a value to any existing bonus this item has
     *
     * @param item The item to try to add a bonus to.
     * @param value The value to try to add.
     * @returns The total assigned bonus to that item.
     */
  }, {
    key: "addBonus",
    value: function addBonus(item, value) {
      return this.applyBonus(item, value, (a, b) => a + b);
    }
    /**
     * Apply the given items' bonuses to the outfit, using a rule given by the reducer
     *
     * @param items A map containing items and their bonuses
     * @param reducer A way of combining new bonuses with existing bonuses
     */
  }, {
    key: "applyBonuses",
    value: function applyBonuses(items, reducer) {
      var _iterator = _createForOfIteratorHelper(items),
        _step;
      try {
        for (_iterator.s(); !(_step = _iterator.n()).done;) {
          var _step$value = _slicedToArray(_step.value, 2),
            item = _step$value[0],
            value = _step$value[1];
          this.applyBonus(item, value, reducer);
        }
      } catch (err) {
        _iterator.e(err);
      } finally {
        _iterator.f();
      }
    }
    /**
     * Sets the bonuses of the given items, overriding existing bonuses
     *
     * @param items Map containing items and bonuses
     */
  }, {
    key: "setBonuses",
    value: function setBonuses(items) {
      this.applyBonuses(items, a => a);
    }
    /**
     * Adds the bonuses of the given items to any existing bonuses they ahave
     *
     * @param items Map containing items and bonuses
     */
  }, {
    key: "addBonuses",
    value: function addBonuses(items) {
      this.applyBonuses(items, (a, b) => a + b);
    }
  }, {
    key: "equipUsingFamiliar",
    value: function equipUsingFamiliar(item, slot) {
      if (![undefined, $slot`familiar`].includes(slot)) return false;
      if (this.equips.has($slot`familiar`)) return false;
      if (kolmafia.booleanModifier(item, "Single Equip")) return false;
      var familiar = this.getHoldingFamiliar(item);
      // Hats/pants don't get the full effect on the familiar, unlike weapons/off-hands which are basically all fully functional
      if (familiar === undefined || $familiars`Fancypants Scarecrow, Mad Hatrack`.includes(familiar) && slot === undefined) {
        return false;
      }
      if (!this.equip(familiar)) return false;
      this.equips.set($slot`familiar`, item);
      return true;
    }
  }, {
    key: "equipItem",
    value: function equipItem(item, slot) {
      return this.haveEquipped(item, slot) || this.equipItemNone(item, slot) || this.isAvailable(item) && (this.equipNonAccessory(item, slot) || this.equipAccessory(item, slot) || this.equipUsingDualWield(item, slot) || this.equipUsingFamiliar(item, slot));
    }
  }, {
    key: "equipFamiliar",
    value: function equipFamiliar(familiar) {
      if (familiar === this.familiar) return true;
      if (this.familiar !== undefined) return false;
      if (familiar !== $familiar.none) {
        if (!have$P(familiar)) return false;
        if (Array.from(this.riders.values()).includes(familiar)) return false;
      }
      var item = this.equips.get($slot`familiar`);
      if (item !== undefined && item !== $item.none && !kolmafia.canEquip(familiar, item)) return false;
      this.familiar = familiar;
      return true;
    }
  }, {
    key: "equipSpec",
    value: function equipSpec(spec) {
      var _this$avoid;
      var _a, _b, _c, _d, _e, _f;
      var succeeded = true;
      for (var _i = 0, _outfitSlots = outfitSlots$1; _i < _outfitSlots.length; _i++) {
        var slotName = _outfitSlots[_i];
        var slot = (_a = new Map([["famequip", $slot`familiar`], ["offhand", $slot`off-hand`]]).get(slotName)) !== null && _a !== void 0 ? _a : kolmafia.toSlot(slotName);
        var itemOrItems = spec[slotName];
        if (itemOrItems !== undefined && !this.equip(itemOrItems, slot)) succeeded = false;
      }
      var _iterator2 = _createForOfIteratorHelper((_b = spec === null || spec === void 0 ? void 0 : spec.equip) !== null && _b !== void 0 ? _b : []),
        _step2;
      try {
        for (_iterator2.s(); !(_step2 = _iterator2.n()).done;) {
          var item = _step2.value;
          if (!this.equip(item)) succeeded = false;
        }
      } catch (err) {
        _iterator2.e(err);
      } finally {
        _iterator2.f();
      }
      if ((spec === null || spec === void 0 ? void 0 : spec.familiar) !== undefined) {
        if (!this.equip(spec.familiar)) succeeded = false;
      }
      (_this$avoid = this.avoid).push.apply(_this$avoid, _toConsumableArray((_c = spec === null || spec === void 0 ? void 0 : spec.avoid) !== null && _c !== void 0 ? _c : []));
      this.skipDefaults = this.skipDefaults || ((_d = spec.skipDefaults) !== null && _d !== void 0 ? _d : false);
      if (spec.modifier) {
        var _this$modifier;
        if (Array.isArray(spec.modifier)) (_this$modifier = this.modifier).push.apply(_this$modifier, _toConsumableArray(spec.modifier));else this.modifier.push(spec.modifier);
      }
      if (spec.modes) {
        if (!this.setModes(spec.modes)) {
          succeeded = false;
        }
      }
      if (spec.riders) {
        if (spec.riders["buddy-bjorn"] && !this.bjornify(spec.riders["buddy-bjorn"])) succeeded = false;
        if (spec.riders["crown-of-thrones"] && !this.enthrone(spec.riders["crown-of-thrones"])) succeeded = false;
      }
      if (spec.bonuses) {
        this.addBonuses(spec.bonuses);
      }
      this.beforeDress.apply(this, _toConsumableArray((_e = spec.beforeDress) !== null && _e !== void 0 ? _e : []));
      this.afterDress.apply(this, _toConsumableArray((_f = spec.afterDress) !== null && _f !== void 0 ? _f : []));
      return succeeded;
    }
    /**
     * Equip the first thing that can be equipped to the outfit.
     *
     * @param things The things to equip.
     * @param slot The slot to equip them.
     * @returns True if one of the things is equipped, and false otherwise.
     */
  }, {
    key: "equipFirst",
    value: function equipFirst(things, slot) {
      // some() returns false on an empty array, yet every() returns true.
      // This keeps behavior consistent between slotful and slotless equipping.
      if (things.length === 0) return true;
      return things.some(val => this.equip(val, slot));
    }
    /**
     * Equip a thing to the outfit.
     *
     * If no slot is given, then the thing will be equipped wherever possible
     * (possibly using dual-wielding, any of the accessory slots, or as
     * familiar equipment). If it is impossible to add this thing anywhere to
     * the outfit, this function will return false.
     *
     * If a slot is given, the item will be equipped only in that slot. If the
     * slot is filled with a different item, this function will return false.
     *
     * If the thing is already equipped in the provided slot, or if no slot is
     * given and the thing is already equipped in any slot, this function will
     * return true and not change the outfit.
     *
     * @param thing The thing or things to equip.
     * @param slot The slot to equip them.
     * @returns True if the thing was sucessfully equipped, and false otherwise.
     */
  }, {
    key: "equip",
    value: function equip(thing, slot) {
      if (Array.isArray(thing)) {
        if (slot !== undefined) return this.equipFirst(thing, slot);
        return thing.every(val => this.equip(val));
      }
      if (thing instanceof kolmafia.Item) return this.equipItem(thing, slot);
      if (thing instanceof kolmafia.Familiar) return this.equipFamiliar(thing);
      if (thing instanceof Outfit) return this.equipSpec(thing.spec());
      return this.equipSpec(thing);
    }
  }, {
    key: "equipRider",
    value:
    /**
     * Add a rider to the outfit.
     *
     * This function does *not* equip the corresponding item; it must be equipped separately.
     *
     * If a familiar is already specified as the rider that is different from the provided target, this function will return false and not change the rider.
     * @param target The familiar to use as the rider, or a ranked list of familiars to try to use as the rider.
     * @returns True if we successfully set the slot to a valid rider.
     */
    function equipRider(target, slot) {
      var current = this.riders.get(slot);
      var targets = Array.isArray(target) ? target : [target];
      if (current) {
        return targets.includes(current);
      }
      // Gather the set of riders that are equipped in other rider slots.
      var otherRiders = _toConsumableArray(this.riders.entries()).filter(_ref => {
        var _ref2 = _slicedToArray(_ref, 1),
          key = _ref2[0];
        return slot !== key;
      }).map(_ref3 => {
        var _ref4 = _slicedToArray(_ref3, 2),
          value = _ref4[1];
        return value;
      });
      var fam = targets.find(f => have$P(f) && this.familiar !== f && !otherRiders.includes(f));
      if (fam) {
        this.riders.set(slot, fam);
        return true;
      }
      return false;
    }
    /**
     * Add a bjornified familiar to the outfit.
     *
     * This function does *not* equip the buddy bjorn itself; it must be equipped separately.
     *
     * If a familiar is already specified for the buddy bjorn that is different from the provided target, this function will return false and not change the buddy bjorn.
     * @param target The familiar to bjornify, or a ranked list of familiars to try to bjornify.
     * @returns True if we successfully set the bjorn to a valid target.
     */
  }, {
    key: "bjornify",
    value: function bjornify(target) {
      return this.equipRider(target, $slot`buddy-bjorn`);
    }
    /**
     * Add anenthroned familiar to the outfit.
     *
     * This function does *not* equip the crown of thrones itself; it must be equipped separately.
     *
     * If a familiar is already specified for the crown of thrones that is different from the provided target, this function will return false and not change the crown of thrones.
     * @param target The familiar to enthrone, or a ranked list of familiars to try to enthrone.
     * @returns True if we successfully set the enthrone to a valid target.
     */
  }, {
    key: "enthrone",
    value: function enthrone(target) {
      return this.equipRider(target, $slot`crown-of-thrones`);
    }
    /**
     * Set the provided modes for items that may be equipped in the outfit.
     *
     * This function does *not* equip items for the set modes; they must be
     * equipped separately.
     *
     * If a mode is already set for an item that is different from the provided
     * mode, this function will return false and not change the mode for that
     * item. (But other modes might still be changed if they are compatible.)
     *
     * Note that the superhero and instuctions of a retrocape can be set
     * independently (`undefined` is treated as "don't care").
     *
     * @param modes Modes to set in this outfit.
     * @returns True if all modes were sucessfully set, and false otherwise.
     */
  }, {
    key: "setModes",
    value: function setModes(modes) {
      var _a, _b;
      var compatible = true;
      // Check if the new modes are compatible with existing modes
      for (var _i2 = 0, _modeableCommands = modeableCommands; _i2 < _modeableCommands.length; _i2++) {
        var mode = _modeableCommands[_i2];
        if (mode === "retrocape") continue; // checked below
        if (this.modes[mode] && modes[mode] && this.modes[mode] !== modes[mode]) {
          compatible = false;
        }
      }
      // Check if retrocape modes are compatible
      // (Parts that are undefined are compatible with everything)
      if (this.modes["retrocape"] && modes["retrocape"]) {
        if (this.modes["retrocape"][0] && modes["retrocape"][0] && this.modes["retrocape"][0] !== modes["retrocape"][0]) {
          compatible = false;
        }
        if (this.modes["retrocape"][1] && modes["retrocape"][1] && this.modes["retrocape"][1] !== modes["retrocape"][1]) {
          compatible = false;
        }
        this.modes["retrocape"][0] = (_a = this.modes["retrocape"][0]) !== null && _a !== void 0 ? _a : modes["retrocape"][0];
        this.modes["retrocape"][1] = (_b = this.modes["retrocape"][1]) !== null && _b !== void 0 ? _b : modes["retrocape"][1];
      }
      this.modes = _objectSpread2(_objectSpread2({}, modes), this.modes);
      return compatible;
    }
    /**
     * Check if it is possible to equip a thing to this outfit using .equip().
     *
     * This does not change the current outfit.
     *
     * @param thing The thing to equip.
     * @param slot The slot to equip them.
     * @returns True if this thing can be equipped.
     */
  }, {
    key: "canEquip",
    value: function canEquip(thing, slot) {
      var outfit = this.clone();
      return outfit.equip(thing, slot);
    }
    /**
     * Check if it is possible to equip a thing to this outfit using .equip(); if it is, do so.
     *
     * This does change the current outfit.
     * @param thing The thing to equip.
     * @param slot The slot to equip them.
     * @returns True if this thing was successfully equipped.
     */
  }, {
    key: "tryEquip",
    value: function tryEquip(thing, slot) {
      return this.canEquip(thing, slot) && this.equip(thing, slot);
    }
  }, {
    key: "afterDress",
    value: function afterDress() {
      var _this$postActions;
      (_this$postActions = this.postActions).push.apply(_this$postActions, arguments);
    }
  }, {
    key: "beforeDress",
    value: function beforeDress() {
      var _this$preActions;
      (_this$preActions = this.preActions).push.apply(_this$preActions, arguments);
    }
    /**
     * Equip this outfit.
     */
  }, {
    key: "_dress",
    value: function _dress(refreshed) {
      if (this.familiar) kolmafia.useFamiliar(this.familiar);
      var targetEquipment = Array.from(this.equips.values());
      var usedSlots = new Set();
      // First, we equip non-accessory equipment.
      var nonaccessorySlots = $slots`weapon, off-hand, hat, back, shirt, pants, familiar`;
      var bjorn = this.riders.get($slot`buddy-bjorn`);
      if (bjorn && (this.equips.get($slot`back`) === $item`Buddy Bjorn` || this.getBonus($item`Buddy Bjorn`))) {
        usedSlots.add($slot`buddy-bjorn`);
        usedSlots.add($slot`crown-of-thrones`);
      }
      var crown = this.riders.get($slot`crown-of-thrones`);
      if (crown && (this.equips.get($slot`hat`) === $item`Crown of Thrones` || this.getBonus($item`Crown of Thrones`))) {
        usedSlots.add($slot`buddy-bjorn`);
        usedSlots.add($slot`crown-of-thrones`);
      }
      // Then, we remove existing equipment only when it would block the new outfit:
      // 1. An existing 2-handed weapon would block offhands
      if (weaponHands(kolmafia.equippedItem($slot`weapon`)) !== 1 && this.equips.has($slot`offhand`) && !this.equips.has($slot`weapon`)) kolmafia.equip($slot`weapon`, $item.none);
      // 2. An existing dual-fisted ranged weapon would block melee weapons.
      if (!weaponsCompatible(this.equips.get($slot`weapon`), kolmafia.equippedItem($slot`off-hand`))) kolmafia.equip($slot`off-hand`, $item.none);
      // 3. Equipment that will be used in a different slot than
      // where it is currently equipped, to avoid a mafia issue.
      // Order is anchored here to prevent DFSS shenanigans
      var _iterator3 = _createForOfIteratorHelper(nonaccessorySlots),
        _step3;
      try {
        for (_iterator3.s(); !(_step3 = _iterator3.n()).done;) {
          var slot = _step3.value;
          if (targetEquipment.includes(kolmafia.equippedItem(slot)) && this.equips.get(slot) !== kolmafia.equippedItem(slot) || this.avoid.includes(kolmafia.equippedItem(slot))) kolmafia.equip(slot, $item.none);
        }
        // Then we equip all the non-accessory equipment.
      } catch (err) {
        _iterator3.e(err);
      } finally {
        _iterator3.f();
      }
      var _iterator4 = _createForOfIteratorHelper(nonaccessorySlots),
        _step4;
      try {
        for (_iterator4.s(); !(_step4 = _iterator4.n()).done;) {
          var _slot = _step4.value;
          var equipment = this.equips.get(_slot);
          if (equipment) {
            kolmafia.equip(_slot, equipment);
            usedSlots.add(_slot);
          }
        }
        // Next, we equip accessories
      } catch (err) {
        _iterator4.e(err);
      } finally {
        _iterator4.f();
      }
      var accessorySlots = $slots`acc1, acc2, acc3`;
      var accessoryEquips = accessorySlots.map(slot => this.equips.get(slot)).filter(item => item !== undefined);
      // To plan how to equip accessories, first check which accessories are
      // already equipped in some accessory slot. There is no need to move them,
      // since KoL doesn't care what order accessories are equipped in.
      var missingAccessories = []; // accessories that are not already equipped
      var _iterator5 = _createForOfIteratorHelper(accessoryEquips),
        _step5;
      try {
        var _loop = function _loop() {
          var accessory = _step5.value;
          var alreadyEquipped = accessorySlots.find(slot => !usedSlots.has(slot) && kolmafia.equippedItem(slot) === accessory);
          if (alreadyEquipped) {
            usedSlots.add(alreadyEquipped);
          } else {
            missingAccessories.push(accessory);
          }
        };
        for (_iterator5.s(); !(_step5 = _iterator5.n()).done;) {
          _loop();
        }
        // Then, for all accessories that are not currently equipped, use the first
        // open slot to place them.
      } catch (err) {
        _iterator5.e(err);
      } finally {
        _iterator5.f();
      }
      for (var _i3 = 0, _missingAccessories = missingAccessories; _i3 < _missingAccessories.length; _i3++) {
        var accessory = _missingAccessories[_i3];
        var unusedSlot = accessorySlots.find(slot => !usedSlots.has(slot));
        if (unusedSlot === undefined) {
          // This should only occur if there is a bug in .dress()
          throw `No accessory slots remaining`;
        }
        kolmafia.equip(unusedSlot, accessory);
        usedSlots.add(unusedSlot);
      }
      // Remaining slots are filled by the maximizer
      var modes = convertToLibramModes(this.modes);
      if (this.modifier.length > 0 || _toConsumableArray(this.bonuses).filter(_ref5 => {
        var _ref6 = _slicedToArray(_ref5, 2),
          value = _ref6[1];
        return value;
      }).length > 0) {
        var allRequirements = [new Requirement(this.modifier, {
          preventSlot: _toConsumableArray(usedSlots),
          preventEquip: this.avoid,
          modes: modes,
          bonusEquip: this.bonuses
        })];
        if (refreshed) allRequirements.push(FORCE_REFRESH_REQUIREMENT);
        if (!Requirement.merge(allRequirements).maximize()) {
          if (!refreshed) {
            kolmafia.cliExecute("refresh inventory");
            this._dress(true);
            return;
          } else throw new Error("Failed to maximize properly!");
        }
        kolmafia.logprint(`Maximize: ${this.modifier}`);
      }
      // Set the modes of any equipped items.
      applyModes(modes);
      // Handle the rider slots next
      if (bjorn && kolmafia.haveEquipped($item`Buddy Bjorn`)) {
        if (kolmafia.myEnthronedFamiliar() === bjorn) kolmafia.enthroneFamiliar($familiar.none);
        if (kolmafia.myBjornedFamiliar() !== bjorn) kolmafia.bjornifyFamiliar(bjorn);
      }
      if (crown && kolmafia.haveEquipped($item`Crown of Thrones`)) {
        if (kolmafia.myBjornedFamiliar() === crown) kolmafia.bjornifyFamiliar($familiar.none);
        if (kolmafia.myEnthronedFamiliar() !== crown) kolmafia.enthroneFamiliar(crown);
      }
      // Verify that all equipment was indeed equipped
      if (this.familiar !== undefined && kolmafia.myFamiliar() !== this.familiar) throw `Failed to fully dress (expected: familiar ${this.familiar})`;
      var _iterator6 = _createForOfIteratorHelper(nonaccessorySlots),
        _step6;
      try {
        for (_iterator6.s(); !(_step6 = _iterator6.n()).done;) {
          var _slot2 = _step6.value;
          if (this.equips.has(_slot2) && kolmafia.equippedItem(_slot2) !== this.equips.get(_slot2)) {
            throw `Failed to fully dress (expected: ${_slot2} ${this.equips.get(_slot2)})`;
          }
        }
      } catch (err) {
        _iterator6.e(err);
      } finally {
        _iterator6.f();
      }
      var _iterator7 = _createForOfIteratorHelper(accessoryEquips),
        _step7;
      try {
        var _loop2 = function _loop2() {
          var accessory = _step7.value;
          if (kolmafia.equippedAmount(accessory) < accessoryEquips.filter(acc => acc === accessory).length) {
            throw `Failed to fully dress (expected: acc ${accessory})`;
          }
        };
        for (_iterator7.s(); !(_step7 = _iterator7.n()).done;) {
          _loop2();
        }
      } catch (err) {
        _iterator7.e(err);
      } finally {
        _iterator7.f();
      }
      for (var _i4 = 0, _arr = [[$slot`buddy-bjorn`, $item`Buddy Bjorn`, kolmafia.myBjornedFamiliar], [$slot`crown-of-thrones`, $item`Crown of Thrones`, kolmafia.myEnthronedFamiliar]]; _i4 < _arr.length; _i4++) {
        var _arr$_i = _slicedToArray(_arr[_i4], 3),
          rider = _arr$_i[0],
          throne = _arr$_i[1],
          checkingFunction = _arr$_i[2];
        var wanted = this.riders.get(rider);
        if (_toConsumableArray(this.equips.values()).includes(throne) && wanted && checkingFunction() !== wanted) {
          throw `Failed to fully dress: (expected ${rider} ${wanted})`;
        }
      }
    }
  }, {
    key: "dress",
    value: function dress() {
      var _iterator8 = _createForOfIteratorHelper(this.preActions),
        _step8;
      try {
        for (_iterator8.s(); !(_step8 = _iterator8.n()).done;) {
          var action = _step8.value;
          action();
        }
      } catch (err) {
        _iterator8.e(err);
      } finally {
        _iterator8.f();
      }
      this._dress(false);
      var _iterator9 = _createForOfIteratorHelper(this.postActions),
        _step9;
      try {
        for (_iterator9.s(); !(_step9 = _iterator9.n()).done;) {
          var _action = _step9.value;
          _action();
        }
      } catch (err) {
        _iterator9.e(err);
      } finally {
        _iterator9.f();
      }
    }
    /**
     * Build an Outfit identical to this outfit.
     */
  }, {
    key: "clone",
    value: function clone() {
      var result = new Outfit();
      result.equips = new Map(this.equips);
      result.skipDefaults = this.skipDefaults;
      result.familiar = this.familiar;
      result.modifier = _toConsumableArray(this.modifier);
      result.avoid = _toConsumableArray(this.avoid);
      result.modes = _objectSpread2({}, this.modes);
      result.riders = new Map(this.riders);
      result.bonuses = new Map(this.bonuses);
      result.beforeDress.apply(result, _toConsumableArray(this.preActions));
      result.afterDress.apply(result, _toConsumableArray(this.postActions));
      return result;
    }
    /**
     * Build an OutfitSpec identical to this outfit.
     */
  }, {
    key: "spec",
    value: function spec() {
      var _a;
      var result = {
        modifier: _toConsumableArray(this.modifier),
        avoid: _toConsumableArray(this.avoid),
        skipDefaults: this.skipDefaults,
        modes: _objectSpread2({}, this.modes),
        bonuses: new Map(this.bonuses)
      };
      if (this.familiar) result.familiar = this.familiar;
      // Add all equipment forced in a particular slot
      for (var _i5 = 0, _outfitSlots2 = outfitSlots$1; _i5 < _outfitSlots2.length; _i5++) {
        var slotName = _outfitSlots2[_i5];
        var entry = this.equips.get((_a = new Map([["famequip", $slot`familiar`], ["offhand", $slot`off-hand`]]).get(slotName)) !== null && _a !== void 0 ? _a : kolmafia.toSlot(slotName));
        if (entry) result[slotName] = entry;
      }
      // Include the riders
      var riders = {};
      var buddyRider = this.riders.get($slot`buddy-bjorn`);
      if (buddyRider !== undefined) riders["buddy-bjorn"] = buddyRider;
      var throneRider = this.riders.get($slot`crown-of-thrones`);
      if (throneRider !== undefined) riders["crown-of-thrones"] = throneRider;
      if (buddyRider !== undefined || throneRider !== undefined) result.riders = riders;
      if (this.preActions.length) result.beforeDress = this.preActions;
      if (this.postActions.length) result.afterDress = this.postActions;
      return result;
    }
  }], [{
    key: "current",
    value: function current() {
      var _a;
      var outfit = new Outfit();
      var familiar = kolmafia.myFamiliar();
      if (outfit.equip(familiar)) {
        throw `Failed to create outfit from current state (expected: familiar ${familiar})`;
      }
      for (var _i6 = 0, _outfitSlots3 = outfitSlots$1; _i6 < _outfitSlots3.length; _i6++) {
        var slotName = _outfitSlots3[_i6];
        var slot = (_a = new Map([["famequip", $slot`familiar`], ["offhand", $slot`off-hand`]]).get(slotName)) !== null && _a !== void 0 ? _a : kolmafia.toSlot(slotName);
        var item = kolmafia.equippedItem(slot);
        if (!outfit.equip(item, slot)) {
          throw `Failed to create outfit from current state (expected: ${slot} ${item})`;
        }
      }
      if (kolmafia.haveEquipped($item`Crown of Thrones`)) outfit.riders.set($slot`crown-of-thrones`, kolmafia.myEnthronedFamiliar());
      if (kolmafia.haveEquipped($item`Buddy Bjorn`)) outfit.riders.set($slot`buddy-bjorn`, kolmafia.myBjornedFamiliar());
      outfit.setModes(getCurrentModes());
      return outfit;
    }
  }, {
    key: "from",
    value: function from(spec) {
      var error = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : null;
      var _a;
      var outfit = new Outfit();
      if (spec instanceof Requirement) {
        var result = {};
        result.modifier = spec.maximizeParameters;
        if ((_a = spec.maximizeOptions.forceEquip) === null || _a === void 0 ? void 0 : _a.length) {
          result.equip = spec.maximizeOptions.forceEquip;
        }
        result.avoid = spec.maximizeOptions.preventEquip;
        result.bonuses = spec.maximizeOptions.bonusEquip;
        if (spec.maximizeOptions.modes) {
          result.modes = convertFromLibramModes(spec.maximizeOptions.modes);
        }
        // Not sure if this is necessary
        var cleanedResult = Object.fromEntries(_toConsumableArray(Object.entries(result)).filter(_ref7 => {
          var _ref8 = _slicedToArray(_ref7, 2),
            v = _ref8[1];
          return v !== undefined;
        }));
        return Outfit.from(cleanedResult);
      }
      var success = outfit.equip(spec);
      if (!success && error) throw error;
      return success ? outfit : null;
    }
  }]);
}();
/**
 * Get the modes of this outfit in a type compatible with Libram.
 *
 * This conversion is needed since we store the retrocape modes
 * internally as an array, but libram uses a string.
 *
 * @returns The modes equipped to this outfit.
 */
function convertToLibramModes(modes) {
  var _a;
  return {
    backupcamera: modes["backupcamera"],
    umbrella: modes["umbrella"],
    snowsuit: modes["snowsuit"],
    edpiece: modes["edpiece"],
    retrocape: (_a = modes["retrocape"]) === null || _a === void 0 ? void 0 : _a.filter(s => s !== undefined).join(" "),
    parka: modes["parka"],
    jillcandle: modes["jillcandle"]
  };
}
function convertFromLibramModes(modes) {
  return modes.retrocape ? _objectSpread2(_objectSpread2({}, modes), {}, {
    retrocape: modes.retrocape.split(" ")
  }) : modes;
}
/**
 * Get the current modes of all items.
 *
 * @returns The current mode settings for all items, equipped or not.
 */
function getCurrentModes() {
  return {
    backupcamera: getMode("backupCameraMode", ["ml", "meat", "init"]),
    umbrella: getMode("umbrellaState", ["broken", "forward-facing", "bucket style", "pitchfork style", "constantly twirling", "cocoon"]),
    snowsuit: getMode("snowsuit", ["eyebrows", "smirk", "nose", "goatee", "hat"]),
    edpiece: getMode("edPiece", ["bear", "owl", "puma", "hyena", "mouse", "weasel", "fish"]),
    retrocape: [getMode("retroCapeSuperhero", ["vampire", "heck", "robot"]), getMode("retroCapeWashingInstructions", ["hold", "thrill", "kiss", "kill"])],
    parka: getMode("parkaMode", ["kachungasaur", "dilophosaur", "ghostasaurus", "spikolodon", "pterodactyl"]),
    jillcandle: getMode("jillcandle", ["disco", "ultraviolet", "reading", "red"])
  };
}
/**
 * Get the current value for a mode in a type-safe way.
 *
 * @param property The mafia property for the mode.
 * @param options A typed list of options for the mode.
 * @returns The mode if the property value matched a valid option, or undefined.
 */
function getMode(property, options) {
  var val = get$2(property, "");
  return options.find(s => s === val); // .includes has type issues
}
/**
 * Returns true if the provided weapons are compatible for dual-wielding.
 * (Ranged weapons cannot be equipped alongside melee/myst weapons).
 */
function weaponsCompatible(weapon, offhand) {
  if (!weapon || weapon === $item`none`) return true;
  if (!offhand || offhand === $item`none`) return true;
  if (kolmafia.toSlot(offhand) !== $slot`weapon`) return true;
  var weaponStat = kolmafia.weaponType(weapon);
  var offhandStat = kolmafia.weaponType(offhand);
  if (weaponStat === $stat`Moxie` && (offhandStat === $stat`Mysticality` || offhandStat === $stat`Muscle`)) return false;
  if (offhandStat === $stat`Moxie` && (weaponStat === $stat`Mysticality` || weaponStat === $stat`Muscle`)) return false;
  return true;
}

var EngineOptions = /*#__PURE__*/_createClass(function EngineOptions() {
  _classCallCheck(this, EngineOptions);
});
var grimoireCCS = "grimoire_macro";
/**
 * An Engine which allows for custom engine state. Most beginning users should
 * use the Engine class instead.
 */
var ContextualEngine = /*#__PURE__*/function () {
  /**
   * Create the engine.
   * @param tasks A list of tasks for looking up task dependencies.
   * @param options Basic configuration of the engine.
   */
  function ContextualEngine(tasks, options) {
    _classCallCheck(this, ContextualEngine);
    this.attempts = {};
    this.propertyManager = new PropertiesManager();
    this.tasks_by_name = new Map();
    this.cachedCcsContents = "";
    this.options = options !== null && options !== void 0 ? options : {};
    this.tasks = tasks.map(task => _objectSpread2(_objectSpread2({}, this.options.default_task_options), task));
    var _iterator = _createForOfIteratorHelper(this.tasks),
      _step;
    try {
      for (_iterator.s(); !(_step = _iterator.n()).done;) {
        var task = _step.value;
        this.tasks_by_name.set(task.name, task);
      }
    } catch (err) {
      _iterator.e(err);
    } finally {
      _iterator.f();
    }
    this.initPropertiesManager(this.propertyManager);
  }
  /**
   * Determine the next task to perform.
   * By default, this is the first task in the task list that is available.
   * @returns The next task to perform, or undefined if no tasks are available.
   */
  return _createClass(ContextualEngine, [{
    key: "getNextTask",
    value: function getNextTask() {
      return this.tasks.find(task => this.available(task));
    }
    /**
     * Continually get the next task and execute it.
     * @param actions If given, only perform up to this many tasks.
     */
  }, {
    key: "run",
    value: function run(actions) {
      for (var i = 0; i < (actions !== null && actions !== void 0 ? actions : Infinity); i++) {
        var task = this.getNextTask();
        if (!task) return;
        this.execute(task);
      }
    }
    /**
     * Close the engine and reset all properties.
     * After this has been called, this object should not be used.
     */
  }, {
    key: "destruct",
    value: function destruct() {
      this.propertyManager.resetAll();
      kolmafia.setAutoAttack(0);
    }
    /**
     * Check if the given task is available at this moment.
     * @param task: The task to check.
     * @returns true if all dependencies are complete and the task is ready.
     *  Note that dependencies are not checked transitively. That is, if
     *  A depends on B which depends on C, then A is ready if B is complete
     *  (regardless of if C is complete or not).
     */
  }, {
    key: "available",
    value: function available(task) {
      var _a, _b;
      if (((_a = task.limit) === null || _a === void 0 ? void 0 : _a.skip) !== undefined && this.attempts[task.name] >= task.limit.skip) return false;
      var _iterator2 = _createForOfIteratorHelper((_b = task.after) !== null && _b !== void 0 ? _b : []),
        _step2;
      try {
        for (_iterator2.s(); !(_step2 = _iterator2.n()).done;) {
          var after = _step2.value;
          var after_task = this.tasks_by_name.get(after);
          if (after_task === undefined) throw `Unknown task dependency ${after} on ${task.name}`;
          if (!after_task.completed(this.getContext(task))) return false;
        }
      } catch (err) {
        _iterator2.e(err);
      } finally {
        _iterator2.f();
      }
      if (task.ready && !task.ready(this.getContext(task))) return false;
      if (task.completed(this.getContext(task))) return false;
      return true;
    }
    /**
     * Perform all steps to execute the provided task.
     * This is the main entry point for the Engine.
     * @param task The current executing task.
     */
  }, {
    key: "execute",
    value: function execute(task) {
      var _a, _b, _c, _d, _e;
      this.printExecutingMessage(task);
      // Determine the proper postcondition for after the task executes.
      var postcondition = (_b = (_a = task.limit) === null || _a === void 0 ? void 0 : _a.guard) === null || _b === void 0 ? void 0 : _b.call(_a, this.getContext(task));
      // Acquire any items and effects first, possibly for later execution steps.
      this.acquireItems(task);
      this.acquireEffects(task);
      // Prepare the outfit, with resources.
      var task_combat = (_d = (_c = task.combat) === null || _c === void 0 ? void 0 : _c.clone()) !== null && _d !== void 0 ? _d : new CombatStrategy();
      var outfit = this.createOutfit(task);
      var task_resources = new CombatResources();
      this.customize(task, outfit, task_combat, task_resources);
      this.dress(task, outfit);
      // Prepare combat and choices
      this.setCombat(task, task_combat, task_resources);
      this.setChoices(task, this.propertyManager);
      // Actually perform the task
      var _iterator3 = _createForOfIteratorHelper(task_resources.all()),
        _step3;
      try {
        for (_iterator3.s(); !(_step3 = _iterator3.n()).done;) {
          var resource = _step3.value;
          (_e = resource.prepare) === null || _e === void 0 ? void 0 : _e.call(resource, this.getContext(task));
        }
      } catch (err) {
        _iterator3.e(err);
      } finally {
        _iterator3.f();
      }
      this.prepare(task);
      this.do(task);
      while (this.shouldRepeatAdv(task)) {
        _set("lastEncounter", "");
        this.do(task);
      }
      this.post(task);
      // Mark that we tried the task, and apply limits
      this.markAttempt(task);
      this.checkLimits(task, postcondition);
    }
    /**
     * Print a message to indicate the task has begun.
     * @param task The current executing task.
     */
  }, {
    key: "printExecutingMessage",
    value: function printExecutingMessage(task) {
      kolmafia.print(``);
      kolmafia.print(`Executing ${task.name}`, "blue");
    }
    /**
     * Acquire all items for the task.
     * @param task The current executing task.
     */
  }, {
    key: "acquireItems",
    value: function acquireItems(task) {
      var _a;
      var acquire = undelay(task.acquire, this.getContext(task));
      var _iterator4 = _createForOfIteratorHelper(acquire || []),
        _step4;
      try {
        for (_iterator4.s(); !(_step4 = _iterator4.n()).done;) {
          var to_get = _step4.value;
          var num_needed = (_a = to_get.num) !== null && _a !== void 0 ? _a : 1;
          var num_have = kolmafia.itemAmount(to_get.item) + kolmafia.equippedAmount(to_get.item);
          if (num_needed <= num_have) continue;
          if (to_get.useful !== undefined && !to_get.useful()) continue;
          if (to_get.get) {
            to_get.get();
          } else if (to_get.price !== undefined) {
            kolmafia.buy(to_get.item, num_needed - num_have, to_get.price);
          } else if (Object.keys(kolmafia.getRelated(to_get.item, "fold")).length > 0) {
            kolmafia.cliExecute(`fold ${to_get.item}`);
          } else {
            kolmafia.retrieveItem(to_get.item, num_needed);
          }
          if (kolmafia.itemAmount(to_get.item) + kolmafia.equippedAmount(to_get.item) < num_needed && !to_get.optional) {
            throw `Task ${task.name} was unable to acquire ${num_needed} ${to_get.item}`;
          }
        }
      } catch (err) {
        _iterator4.e(err);
      } finally {
        _iterator4.f();
      }
    }
    /**
     * Acquire all effects for the task.
     * @param task The current executing task.
     */
  }, {
    key: "acquireEffects",
    value: function acquireEffects(task) {
      var _a;
      var effects = (_a = undelay(task.effects, this.getContext(task))) !== null && _a !== void 0 ? _a : [];
      var songs = effects.filter(effect => isSong(effect));
      if (songs.length > maxSongs()) throw "Too many AT songs";
      var extraSongs = Object.keys(kolmafia.myEffects()).map(effectName => kolmafia.toEffect(effectName)).filter(effect => isSong(effect) && !songs.includes(effect));
      while (songs.length + extraSongs.length > maxSongs()) {
        var toRemove = extraSongs.pop();
        if (toRemove === undefined) {
          break;
        } else {
          uneffect(toRemove);
        }
      }
      var _iterator5 = _createForOfIteratorHelper(effects),
        _step5;
      try {
        for (_iterator5.s(); !(_step5 = _iterator5.n()).done;) {
          var effect = _step5.value;
          ensureEffect(effect);
        }
      } catch (err) {
        _iterator5.e(err);
      } finally {
        _iterator5.f();
      }
    }
    /**
     * Create an outfit for the task with all required equipment.
     * @param task The current executing task.
     */
  }, {
    key: "createOutfit",
    value: function createOutfit(task) {
      var spec = undelay(task.outfit, this.getContext(task));
      if (spec instanceof Outfit) return spec.clone();
      var outfit = new Outfit();
      if (spec !== undefined) {
        if (!outfit.equip(spec) && !this.options.allow_partial_outfits) {
          throw `Unable to equip all items for ${task.name}`;
        }
      }
      return outfit;
    }
    /**
     * Equip the outfit for the task.
     * @param task The current executing task.
     * @param outfit The outfit for the task, possibly augmented by the engine.
     */
  }, {
    key: "dress",
    value: function dress(task, outfit) {
      if (task.do instanceof kolmafia.Location) kolmafia.setLocation(task.do);
      outfit.dress();
    }
    /* eslint-disable @typescript-eslint/no-unused-vars */
    /**
     * Perform any engine-specific customization for the outfit and combat plan.
     *
     * This is a natural method to override in order to:
     *   * Enable the use of any resources in the outfit or combat (e.g., allocate banishers).
     *   * Equip a default outfit.
     *   * Determine additional monster macros at a global level (e.g., use flyers).
     * @param task The current executing task.
     * @param outfit The outfit for the task.
     * @param combat The combat strategy so far for the task.
     * @param resources The combat resources assigned so far for the task.
     */
  }, {
    key: "customize",
    value: function customize(task, outfit, combat, resources) {
      // do nothing by default
    }
    /* eslint-enable @typescript-eslint/no-unused-vars */
    /**
     * Set the choice settings for the task.
     * @param task The current executing task.
     * @param manager The property manager to use.
     */
  }, {
    key: "setChoices",
    value: function setChoices(task, manager) {
      var _a;
      for (var _i = 0, _Object$entries = Object.entries(undelay((_a = task.choices) !== null && _a !== void 0 ? _a : {}, this.getContext(task))); _i < _Object$entries.length; _i++) {
        var _Object$entries$_i = _slicedToArray(_Object$entries[_i], 2),
          key = _Object$entries$_i[0],
          value = _Object$entries$_i[1];
        if (value === undefined) continue;
        manager.setChoice(parseInt(key), value);
      }
    }
    /**
     * Save the combat macro for this task.
     * @param task The current executing task.
     * @param task_combat The completed combat strategy far for the task.
     * @param task_resources The combat resources assigned for the task.
     */
  }, {
    key: "setCombat",
    value: function setCombat(task, task_combat, task_resources) {
      var _a;
      // Save regular combat macro
      var macro = task_combat.compile(task_resources, (_a = this.options) === null || _a === void 0 ? void 0 : _a.combat_defaults, task.do instanceof kolmafia.Location ? task.do : undefined, this.getContext(task));
      macro.save();
      if (!this.options.ccs) {
        // Use the macro through a CCS file
        var otherCCSEntries = task_combat.compileCcs();
        var ccsContents = ["[default]", `"${macro.toString()}"`].concat(_toConsumableArray(otherCCSEntries)).join("\n");
        // Log Macro + other CCS
        kolmafia.logprint(`CCS: ${ccsContents.replace("\n", "\\n ")}`);
        if (ccsContents !== this.cachedCcsContents) {
          kolmafia.writeCcs(ccsContents, grimoireCCS);
          kolmafia.cliExecute(`ccs ${grimoireCCS}`); // force Mafia to reparse the ccs
          this.cachedCcsContents = ccsContents;
        }
      }
      // Save autoattack combat macro
      var autoattack = task_combat.compileAutoattack(this.getContext(task));
      if (autoattack.toString().length > 1) {
        kolmafia.logprint(`Autoattack macro: ${autoattack.toString()}`);
        autoattack.setAutoAttack();
      } else {
        kolmafia.setAutoAttack(0);
      }
    }
    /**
     * Do any task-specific preparation.
     * @param task The current executing task.
     */
  }, {
    key: "prepare",
    value: function prepare(task) {
      var _a;
      (_a = task.prepare) === null || _a === void 0 ? void 0 : _a.call(task, this.getContext(task));
    }
    /**
     * Actually perform the task.
     * @param task The current executing task.
     */
  }, {
    key: "do",
    value: function _do(task) {
      var result = typeof task.do === "function" ? task.do(this.getContext(task)) : task.do;
      if (result instanceof kolmafia.Location) kolmafia.adv1(result, -1, "");
      kolmafia.runCombat();
      while (kolmafia.inMultiFight()) kolmafia.runCombat();
      if (kolmafia.choiceFollowsFight()) kolmafia.runChoice(-1);
    }
    /**
     * Check if the task.do should be immediately repeated without any prep.
     *
     * By default, this is only used to repeat a task if we hit one of:
     *   1. Halloweener dog noncombats,
     *   2. June cleaver noncombats,
     *   3. Lil' Doctor™ bag noncombat, or
     *   4. Turtle taming noncombats.
     * @param task The current executing task.
     * @returns True if the task should be immediately repeated.
     */
  }, {
    key: "shouldRepeatAdv",
    value: function shouldRepeatAdv(task) {
      return task.do instanceof kolmafia.Location && lastEncounterWasWanderingNC();
    }
    /**
     * Do any task-specific wrapup activities.
     * @param task The current executing task.
     */
  }, {
    key: "post",
    value: function post(task) {
      var _a;
      (_a = task.post) === null || _a === void 0 ? void 0 : _a.call(task, this.getContext(task));
    }
    /**
     * Mark that an attempt was made on the current task.
     * @param task The current executing task.
     */
  }, {
    key: "markAttempt",
    value: function markAttempt(task) {
      if (!(task.name in this.attempts)) this.attempts[task.name] = 0;
      this.attempts[task.name]++;
    }
    /**
     * Check if the task has passed any of its internal limits.
     * @param task The task to check.
     * @param postcondition The postcondition from the task guard.
     * @throws An error if any of the internal limits have been passed.
     */
  }, {
    key: "checkLimits",
    value: function checkLimits(task, postcondition) {
      var _a;
      if (!task.limit) return;
      var failureMessage = task.limit.message ? ` ${task.limit.message}` : "";
      if (!task.completed(this.getContext(task))) {
        if (task.limit.tries && this.attempts[task.name] >= task.limit.tries) throw `Task ${task.name} did not complete within ${task.limit.tries} attempts. Please check what went wrong.${failureMessage}`;
        if (task.limit.soft && this.attempts[task.name] >= task.limit.soft) throw `Task ${task.name} did not complete within ${task.limit.soft} attempts. Please check what went wrong (you may just be unlucky).${failureMessage}`;
        if (task.limit.turns && task.do instanceof kolmafia.Location && task.do.turnsSpent >= task.limit.turns) throw `Task ${task.name} did not complete within ${task.limit.turns} turns. Please check what went wrong.${failureMessage}`;
        if (task.limit.unready && ((_a = task.ready) === null || _a === void 0 ? void 0 : _a.call(task, this.getContext(task)))) throw `Task ${task.name} is still ready, but it should not be. Please check what went wrong.${failureMessage}`;
        if (task.limit.completed) throw `Task ${task.name} is not completed, but it should be. Please check what went wrong.${failureMessage}`;
      }
      if (postcondition && !postcondition()) {
        throw `Task ${task.name} failed its guard. Please check what went wrong.${failureMessage}`;
      }
    }
  }, {
    key: "getDefaultSettings",
    value: function getDefaultSettings() {
      return this.constructor.defaultSettings;
    }
    /**
     * Initialize properties for the script.
     * @param manager The properties manager to use.
     */
  }, {
    key: "initPropertiesManager",
    value: function initPropertiesManager(manager) {
      var _a;
      // Properties adapted from garbo
      manager.set(this.getDefaultSettings());
      if (this.options.ccs !== "") {
        if (this.options.ccs === undefined && kolmafia.readCcs(grimoireCCS) === "") {
          // Write a simple CCS so we can switch to it
          kolmafia.writeCcs("[ default ]\nabort", grimoireCCS);
        }
        manager.set({
          customCombatScript: (_a = this.options.ccs) !== null && _a !== void 0 ? _a : grimoireCCS
        });
      }
    }
  }]);
}();
ContextualEngine.defaultSettings = {
  logPreferenceChange: true,
  logPreferenceChangeFilter: _toConsumableArray(new Set([].concat(_toConsumableArray(get$2("logPreferenceChangeFilter").split(",")), ["libram_savedMacro", "maximizerMRUList", "testudinalTeachings", "_lastCombatStarted"]))).sort().filter(a => a).join(","),
  battleAction: "custom combat script",
  autoSatisfyWithMall: true,
  autoSatisfyWithNPCs: true,
  autoSatisfyWithCoinmasters: true,
  autoSatisfyWithStash: false,
  dontStopForCounters: true,
  maximizerFoldables: true,
  hpAutoRecovery: "-0.05",
  hpAutoRecoveryTarget: "0.0",
  mpAutoRecovery: "-0.05",
  mpAutoRecoveryTarget: "0.0",
  afterAdventureScript: "",
  betweenBattleScript: "",
  choiceAdventureScript: "",
  familiarScript: "",
  currentMood: "apathetic",
  autoTuxedo: true,
  autoPinkyRing: true,
  autoGarish: true,
  allowNonMoodBurning: false,
  allowSummonBurning: true,
  libramSkillsSoftcore: "none"
};
var Engine = /*#__PURE__*/function (_ContextualEngine) {
  function Engine() {
    _classCallCheck(this, Engine);
    return _callSuper(this, Engine, arguments);
  }
  _inherits(Engine, _ContextualEngine);
  return _createClass(Engine, [{
    key: "getContext",
    value:
    // eslint-disable-next-line @typescript-eslint/no-unused-vars
    function getContext(task) {
      return;
    }
  }]);
}(ContextualEngine);
function maxSongs() {
  return have$P($skill`Mariachi Memory`) ? 4 : 3;
}
var wanderingNCs = new Set([
// Halloweener dog noncombats
"Wooof! Wooooooof!", "Playing Fetch*",
// June cleaver noncombats
"Aunts not Ants", "Bath Time", "Beware of Aligator", "Delicious Sprouts", "Hypnotic Master", "Lost and Found", "Poetic Justice", "Summer Days", "Teacher's Pet",
// Lil' Doctor™ bag noncombat
"A Pound of Cure",
// 11 Things I H8 About U
"Eye-Eye-Eye!"]);
var environmentSpecificNCs = new Map([["Even Tamer Than Usual", "indoor"], ["Never Break the Chain", "indoor"], ["Close, but Yes Cigar", "indoor"], ["Armchair Quarterback", "indoor"], ["This Turtle Rocks!", "outdoor"], ["Really Sticking Her Neck Out", "outdoor"], ["It Came from Beneath the Sewer? Great!", "outdoor"], ["Don't Be Alarmed, Now", "outdoor"], ["Puttin' it on Wax", "underground"], ["More Like... Hurtle", "underground"], ["Musk! Musk! Musk!", "underground"], ["Silent Strolling", "underwater"]]);
var zoneSpecificNCs = new Map(Object.entries(kolmafia.fileToBuffer("data/encounters.txt").split("\n").reduce((obj, line) => {
  var _a;
  var _line$split = line.split("\t"),
    _line$split2 = _slicedToArray(_line$split, 3),
    location = _line$split2[0],
    type = _line$split2[1],
    name = _line$split2[2];
  if (type !== "TURTLE" || location === "*") return obj;
  return _objectSpread2(_objectSpread2({}, obj), {}, {
    [name]: [].concat(_toConsumableArray((_a = obj[name]) !== null && _a !== void 0 ? _a : []), [kolmafia.toLocation(location)])
  });
}, {})));
/**
 * Return true if the last adv was one of:
 *   1. Halloweener dog noncombats,
 *   2. June cleaver noncombats,
 *   3. Lil' Doctor™ bag noncombat, or
 *   4. Turtle taming noncombats.
 */
function lastEncounterWasWanderingNC() {
  var _a, _b, _c;
  var last = get$2("lastEncounter");
  if (zoneSpecificNCs.has(last)) {
    // Handle NCs with a duplicated name
    var zones = (_a = zoneSpecificNCs.get(last)) !== null && _a !== void 0 ? _a : [];
    return zones.includes((_b = get$2("lastAdventure")) !== null && _b !== void 0 ? _b : $location.none);
  } else {
    var environment = environmentSpecificNCs.get(last);
    if (environment === ((_c = get$2("lastAdventure")) === null || _c === void 0 ? void 0 : _c.environment)) return true;
    return wanderingNCs.has(last);
  }
}

/**
 * Extract a list of tasks from the provided quests.
 *
 * Each task name is prepended with the quest name ("Quest Name/Task Name").
 * The quest-local names referred to in task.after are updated appropriately.
 * The task completion condition is updated to include the quest completion.
 *
 * Tasks are returned in-order: all tasks from the first quest, then all tasks
 * from the second quest, etc.
 *
 * @param quests The list of quests. This method does not modify the quest
 *    objects or their tasks.
 * @param implicitAfter If true, each task with task.after = undefined will
 *    have a dependency added on the previous task in the list.
 * @returns A list of tasks from the input quests (with updated properties).
 */
function getTasks(quests) {
  var implicitAfter = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : false;
  var verifyTaskDependencies = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : true;
  var _a;
  var result = [];
  var _iterator = _createForOfIteratorHelper(quests),
    _step;
  try {
    var _loop = function _loop() {
      var quest = _step.value;
      var questCompleted = quest.completed;
      var questReady = quest.ready;
      var _iterator2 = _createForOfIteratorHelper(quest.tasks),
        _step2;
      try {
        var _loop2 = function _loop2() {
          var task = _step2.value;
          // Include quest name in task names and dependencies (unless dependency quest is given)
          var renamedTask = _objectSpread2({}, task);
          renamedTask.name = `${quest.name}/${task.name}`;
          renamedTask.after = (_a = task.after) === null || _a === void 0 ? void 0 : _a.map(after => after.includes("/") ? after : `${quest.name}/${after}`);
          // Include previous task as a dependency
          if (implicitAfter && task.after === undefined && result.length > 0) renamedTask.after = [result[result.length - 1].name];
          // Include quest completion in task completion
          if (questCompleted !== undefined) {
            var taskCompleted = task.completed;
            renamedTask.completed = ctx => questCompleted(ctx) || taskCompleted(ctx);
          }
          var taskReady = renamedTask.ready;
          if (questReady !== undefined && taskReady !== undefined) {
            renamedTask.ready = ctx => questReady(ctx) && taskReady(ctx);
          } else if (questReady !== undefined) {
            renamedTask.ready = ctx => questReady(ctx);
          }
          result.push(renamedTask);
        };
        for (_iterator2.s(); !(_step2 = _iterator2.n()).done;) {
          _loop2();
        }
      } catch (err) {
        _iterator2.e(err);
      } finally {
        _iterator2.f();
      }
    };
    for (_iterator.s(); !(_step = _iterator.n()).done;) {
      _loop();
    }
  } catch (err) {
    _iterator.e(err);
  } finally {
    _iterator.f();
  }
  if (verifyTaskDependencies) verifyDependencies(result);
  return result;
}
function verifyDependencies(tasks) {
  var _a;
  // Verify the dependency names of all tasks
  var names = new Set();
  var _iterator3 = _createForOfIteratorHelper(tasks),
    _step3;
  try {
    for (_iterator3.s(); !(_step3 = _iterator3.n()).done;) {
      var task = _step3.value;
      names.add(task.name);
    }
  } catch (err) {
    _iterator3.e(err);
  } finally {
    _iterator3.f();
  }
  var _iterator4 = _createForOfIteratorHelper(tasks),
    _step4;
  try {
    for (_iterator4.s(); !(_step4 = _iterator4.n()).done;) {
      var _task = _step4.value;
      var _iterator5 = _createForOfIteratorHelper((_a = _task.after) !== null && _a !== void 0 ? _a : []),
        _step5;
      try {
        for (_iterator5.s(); !(_step5 = _iterator5.n()).done;) {
          var after = _step5.value;
          if (!names.has(after)) {
            throw `Unknown task dependency ${after} of ${_task.name}`;
          }
        }
      } catch (err) {
        _iterator5.e(err);
      } finally {
        _iterator5.f();
      }
    }
  } catch (err) {
    _iterator4.e(err);
  } finally {
    _iterator4.f();
  }
  return tasks;
}

var FarmingMethod = /*#__PURE__*/function (FarmingMethod) {
  FarmingMethod[FarmingMethod["BARF_MOUNTAIN"] = 0] = "BARF_MOUNTAIN";
  FarmingMethod[FarmingMethod["THE_CORAL_CORRAL"] = 1] = "THE_CORAL_CORRAL";
  return FarmingMethod;
}({});
var workshedAliases = [{
  item: $item`model train set`,
  aliases: ["trainrealm"]
}, {
  item: $item`Asdon Martin keyfob (on ring)`,
  aliases: ["breadcar", "car", "aston"]
}, {
  item: $item`Little Geneticist DNA-Splicing Lab`,
  aliases: ["dnalab"]
}];
var unaliasedSheds = $items`TakerSpace letter of Marque, cold medicine cabinet, diabolic pizza cube, portable Mayo Clinic, spinning wheel, warbear auto-anvil, warbear chemistry lab, warbear high-efficiency still, warbear induction oven, warbear jackhammer drill press, warbear LP-ROM burner`;
var allWorkshedAliases = [].concat(_toConsumableArray(workshedAliases.map(_ref => {
  var item = _ref.item,
    aliases = _ref.aliases;
  return {
    item: item,
    aliases: [].concat(_toConsumableArray(aliases), [item.name.toLowerCase()])
  };
})), _toConsumableArray(unaliasedSheds.map(item => {
  return {
    item: item,
    aliases: [item.name.toLowerCase()]
  };
})));
function toInitials(s) {
  var initials = s.split(" ").map(term => term[0]).join("");
  return initials.length >= 3 ? initials : "";
}
function stripString(s) {
  if (s.includes(" ")) return stripString(s.replace(" ", ""));else if (s.includes("-")) return stripString(s.replace("-", ""));
  return s;
}
function stringToWorkshedItem(s) {
  // An empty string is a subset of every string and will match all the worksheds
  // So we explicitly handle this case here
  if (s === "") return null;
  var lowerCaseWorkshed = s.toLowerCase();
  var strippedWorkshed = stripString(lowerCaseWorkshed);
  var validWorksheds = allWorkshedAliases.filter(_ref2 => {
    var item = _ref2.item,
      aliases = _ref2.aliases;
    return toInitials(item.name.toLowerCase()) === lowerCaseWorkshed || item.name.toLowerCase().includes(lowerCaseWorkshed) || stripString(item.name.toLowerCase()).includes(strippedWorkshed) || aliases.some(alias => alias === lowerCaseWorkshed);
  });

  // grimoire catches the errors and throws its own errors
  // so throw new Error(text) would result in the text not getting printed.
  if (validWorksheds.length > 1) {
    kolmafia.print(`Invalid Workshed: ${s} matches multiple worksheds! Matched:`, "red");
    validWorksheds.forEach(_ref3 => {
      var item = _ref3.item;
      return kolmafia.print(`${item}`, "red");
    });
    throw new Error();
  } else if (validWorksheds.length === 0) {
    kolmafia.print(`Invalid Workshed: ${s} does not match any worksheds!`, "red");
    throw new Error();
  }
  return validWorksheds[0].item;
}
var globalOptions = Args.create("garbo", 'This script is an automated turn-burning script for the Kingdom of Loathing that spends a day\'s resources and adventures on farming\n\
You can use multiple options in conjunction, e.g. "garbo nobarf ascend"', {
  ascend: Args.flag({
    setting: "",
    help: "operate under the assumption that you're ascending after running it, rather than experiencing rollover. It will use borrowed time, it won't charge stinky cheese items, etc.",
    default: false
  }),
  loginvalidwishes: Args.flag({
    setting: "",
    help: "Logs any invalid wishes at the end of the day.",
    hidden: true
  }),
  nobarf: Args.flag({
    setting: "",
    help: "do beginning of the day setup, copy target monster, and various daily flags, but will terminate before normal Barf Mountain turns. May close NEP for the day.",
    default: false
  }),
  nodiet: Args.flag({
    setting: "",
    help: "skip eating and drinking anything as a part of its run (including pantsgiving).",
    default: false
  }),
  quick: Args.flag({
    setting: "",
    help: "garbo will sacrifice some optimal behaviors to run quicker. Estimated and actual profits may be less accurate in this mode. Sets quickcombat and quickgear.",
    default: false
  }),
  quickcombat: Args.flag({
    setting: "",
    help: "garbo will stasis in combat for 5 turns instead of 20, trading some potential profits for a faster run speed.",
    default: false
  }),
  quickgear: Args.flag({
    setting: "",
    help: "garbo will limit maximizer combinations and exclude some synergistic items, trading correctness for speed.",
    default: false
  }),
  returnstash: Args.flag({
    setting: "",
    help: "return all items to your stash clan's stash, then quit",
    default: false
  }),
  simdiet: Args.flag({
    setting: "",
    help: "print out what it computes as an optimal diet and then exit.",
    default: false
  }),
  turns: Args.number({
    setting: "",
    help: 'terminate after the specified number of turns, e.g. "garbo 200" or "garbo turns=200" will terminate after 200 turns are spent. Negative inputs will cause garbo to terminate when the specified number of turns remain.',
    default: 0
  }),
  target: Args.monster({
    setting: "garbo_copyTarget",
    help: "The monster to use all copies on",
    default: $monster.none
  }),
  usekarma: Args.flag({
    setting: "",
    help: "Use instant karma as part of diet",
    default: false
  }),
  usepilsners: Args.flag({
    setting: "garbo_usePilsners",
    help: "Use instant pilsners as part of diet",
    default: true
  }),
  halt: Args.string({
    setting: "",
    help: "Halt after a grimoire task is run that contains this sub-string. If a task is skipped this will not trigger."
  }),
  history: Args.flag({
    setting: "garbo_history",
    help: "Write grimoire task history to garbo_history_<date>.csv",
    default: false
  }),
  version: Args.flag({
    setting: "",
    help: "Print the current version and exit."
  }),
  workshed: Args.custom({
    default: null,
    help: "Intelligently switch into the workshed whose item name you give us. Also accepts substrings of the item name (e.g. dna, trainset), certain shorthand aliases (e.g. car) and initials of length >= 3 (e.g. cmc).",
    options: [].concat(_toConsumableArray(allWorkshedAliases.map(_ref4 => {
      var item = _ref4.item,
        aliases = _ref4.aliases;
      return [item, `${[].concat(_toConsumableArray(aliases), [toInitials(item.name.toLowerCase())]).filter(alias => alias !== "").join(", ")}`];
    })), [[null, "leave this field blank"]])
  }, stringToWorkshedItem, "Item"),
  prefs: Args.group("You can manually set the properties below, but it's recommended that you use the relay interface (dropdown menu at the top left in the browser)", {
    valueOfAdventure: Args.number({
      setting: "valueOfAdventure",
      help: "This is a native mafia property, garbo will make purchasing decisions based on this value. Recommended to be at least 3501."
    }),
    valueOfFreeFight: Args.number({
      setting: "garbo_valueOfFreeFight",
      help: "Set to whatever you estimate the value of a free fight/run to be for you. (Default 2000)",
      default: 2000
    }),
    valueOfPvPFight: Args.number({
      setting: "garbo_valueOfPvPFight",
      help: "Set to whatever you estimate the value of a PvP fight to be for you. (Default 0)",
      default: 0
    }),
    yachtzeechain: Args.flag({
      setting: "garbo_yachtzeechain",
      help: "only diets after free fights, and attempts to estimate if Yachtzee! chaining is profitable for you - if so, it consumes a specific diet which uses ~0-36 spleen;\
      if not it automatically continues with the regular diet. Requires Spring Break Beach access (it will not grab a one-day pass for you, but will make an attempt if one is used).\
      Sweet Synthesis is strongly recommended, as with access to other meat% buffs from Source Terminal, Fortune Teller, KGB and the summoning chamber. Having access to a PYEC (on hand or in the clan stash) is a plus.",
      default: false
    }),
    candydish: Args.flag({
      setting: "garbo_candydish",
      help: "*DANGEROUS* garbo will consider using porcelain candy dishes. This could result in potentially destructive behavior in the instance that the user does not have sufficient meat (1-2 million) to purchase as many dishes as garbo desires or there is a price cliff.",
      default: false,
      hidden: true
    }),
    dmtDupeItem: Args.item({
      setting: "garbo_dmtDupeItem",
      help: "Item to duplicate in the deep machine tunnel instead of calculating by value. Will be ignored if the item is not in inventory or not able to be duped.",
      hidden: true
    }),
    meatTargetMultiplier: Args.number({
      setting: "garbo_meatTargetMultiplier",
      help: "The amount we multiply our valueOfAdventure by when estimating marginal meat target profit. (Default 1.5)",
      default: 1.5
    }),
    stashClan: Args.string({
      setting: "garbo_stashClan",
      help: "If set, garbo will attempt to switch to this clan to take and return useful clan stash item, i.e. a Haiku Katana or Repaid Diaper. Leave blank to disable.",
      default: ""
    }),
    vipClan: Args.string({
      setting: "garbo_vipClan",
      help: "If set, garbo will attempt to switch to this clan to utilize VIP furniture if you have a key. Leave blank to disable",
      default: ""
    }),
    skipAscensionCheck: Args.boolean({
      setting: "garbo_skipAscensionCheck",
      help: "Set to true to skip verifying that your account has broken the prism, otherwise you will be warned upon starting the script."
    }),
    fightGlitch: Args.boolean({
      setting: "garbo_fightGlitch",
      help: "Set to true to fight the glitch season reward. You need certain skills, see relay for info."
    }),
    chargeYeti: Args.boolean({
      setting: "garbo_chargeYeti",
      help: "If set, garbo will value Cooler Yeti familiar experience to help you nightcap more effectively",
      default: false
    }),
    buyPass: Args.boolean({
      setting: "garbo_buyPass",
      help: "Set to true to buy a Dinsey day pass with FunFunds at the end of the day, if possible."
    }),
    beSelfish: Args.boolean({
      setting: "_garbo_beSelfish",
      help: "Set to true to not spend a small amount of daily resources on community endeavors."
    }),
    autoUserConfirm: Args.boolean({
      setting: "garbo_autoUserConfirm",
      help: "**WARNING: Experimental** Don't show user confirm dialogs, instead automatically select yes/no in a way that will allow garbo to continue executing. Useful for scripting/headless. Risky and potentially destructive."
    }),
    autoUserConfirm_targetInvocationsThreshold: Args.number({
      setting: "garbo_autoUserConfirm_targetInvocationsThreshold",
      help: "This is used only when autoUserConfirm is true, will automatically use resources (such as pocket wishes, 11-leaf clovers, etc) up to this threshold to source a target monster for chaining before requesting user interference.",
      default: 1
    }),
    restoreHpTarget: Args.number({
      setting: "garbo_restoreHpTarget",
      help: "If you're a very high level, what HP threshold should garbo aim to maintain?",
      default: 2000
    }),
    rolloverBuffer: Args.number({
      setting: "garbo_rolloverBuffer",
      help: "At how many minutes before Rollover should we terminate to let you get ready for bed?",
      default: 5
    }),
    farmingMethod: Args.custom({
      default: FarmingMethod.BARF_MOUNTAIN,
      help: "Select the farming method to use.",
      options: [[FarmingMethod.BARF_MOUNTAIN, "barf mountain"], [FarmingMethod.THE_CORAL_CORRAL, "sea cows"]]
    }, value => {
      switch (value.toLowerCase()) {
        case "barf":
        case "barf mountain":
          return FarmingMethod.BARF_MOUNTAIN;
        case "cowo":
        case "sea cows":
        case "the coral corral":
          return FarmingMethod.THE_CORAL_CORRAL;
        default:
          return FarmingMethod.BARF_MOUNTAIN;
      }
    }, "Farming Method")
  }),
  /*
    Hidden preferences, CLI input ignored
  */
  stopTurncount: Args.custom({
    hidden: true,
    default: null
  }, () => null, ""),
  saveTurns: Args.custom({
    hidden: true,
    default: 0
  }, () => 0, ""),
  askedAboutWish: Args.custom({
    hidden: true,
    default: false
  }, () => false, ""),
  triedToUnlockHiddenTavern: Args.custom({
    hidden: true,
    default: false
  }, () => false, ""),
  wishAnswer: Args.custom({
    hidden: true,
    default: false
  }, () => false, ""),
  dietCompleted: Args.flag({
    hidden: true,
    default: false
  })
}, {
  positionalArgs: ["turns"]
});
function isQuickGear() {
  return globalOptions.quick || globalOptions.quickgear;
}
function isQuickCombat() {
  return globalOptions.quick || globalOptions.quickcombat;
}

function makeValue() {
  var options = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : {};
  var regularValueCache = new Map();
  var historicalValueCache = new Map();
  var inputValues = options.itemValues ? _toConsumableArray(options.itemValues.entries()).map(_ref => {
    var _ref2 = _slicedToArray(_ref, 2),
      item = _ref2[0],
      val = _ref2[1];
    return [item, () => undelay(val)];
  }) : [];
  var specialValueLookup = new Map([[$item`Freddy Kruegerand`, currency.apply(void 0, _toConsumableArray($items`bottle of Bloodweiser, electric Kool-Aid, Dreadsylvanian skeleton key`))], [$item`Beach Buck`, currency($item`one-day ticket to Spring Break Beach`)], [$item`Coinspiracy`, currency.apply(void 0, _toConsumableArray($items`Merc Core deployment orders, karma shawarma`))], [$item`FunFunds™`, currency($item`one-day ticket to Dinseylandfill`)], [$item`Volcoino`, currency($item`one-day ticket to That 70s Volcano`)], [$item`Wal-Mart gift certificate`, currency($item`one-day ticket to The Glaciest`)], [$item`cop dollar`, currency($item`shoe gum`)], [$item`Rubee™`, currency($item`FantasyRealm guest pass`)], [$item`Guzzlrbuck`, currency($item`Never Don't Stop Not Striving`)]].concat(_toConsumableArray(complexCandy()), [[$item`Merc Core deployment orders`, () => value($item`one-day ticket to Conspiracy Island`)], [$item`free-range mushroom`, () => 3 * Math.max(value($item`mushroom tea`) - value($item`soda water`), value($item`mushroom whiskey`) - value($item`fermenting powder`), value($item`mushroom filet`))], [$item`little firkin`, () => averageValue.apply(void 0, _toConsumableArray($items`martini, screwdriver, strawberry daiquiri, margarita, vodka martini, tequila sunrise, bottle of Amontillado, barrel-aged martini, barrel gun`))], [$item`normal barrel`, () => averageValue.apply(void 0, _toConsumableArray($items`a little sump'm sump'm, pink pony, rockin' wagon, roll in the hay, slip 'n' slide, slap and tickle`))], [$item`big tun`, () => averageValue.apply(void 0, _toConsumableArray($items`gibson, gin and tonic, mimosette, tequila sunset, vodka and tonic, zmobie`))], [$item`weathered barrel`, () => averageValue.apply(void 0, _toConsumableArray($items`bean burrito, enchanted bean burrito, jumping bean burrito`))], [$item`dusty barrel`, () => averageValue.apply(void 0, _toConsumableArray($items`spicy bean burrito, spicy enchanted bean burrito, spicy jumping bean burrito`))], [$item`disintegrating barrel`, () => averageValue.apply(void 0, _toConsumableArray($items`insanely spicy bean burrito, insanely spicy enchanted bean burrito, insanely spicy jumping bean burrito`))], [$item`moist barrel`, () => averageValue.apply(void 0, _toConsumableArray($items`cast, concentrated magicalness pill, enchanted barbell, giant moxie weed, Mountain Stream soda`))], [$item`rotting barrel`, () => averageValue.apply(void 0, _toConsumableArray($items`Doc Galaktik's Ailment Ointment, extra-strength strongness elixir, jug-o-magicalness, Marquis de Poivre soda, suntan lotion of moxiousness`))], [$item`mouldering barrel`, () => averageValue.apply(void 0, _toConsumableArray($items`creepy ginger ale, haunted battery, scroll of drastic healing, synthetic marrow, the funk`))], [$item`barnacled barrel`, () => averageValue.apply(void 0, _toConsumableArray($items`Alewife™ Ale, bazookafish bubble gum, beefy fish meat, eel battery, glistening fish meat, ink bladder, pufferfish spine, shark cartilage, slick fish meat, slug of rum, slug of shochu, slug of vodka, temporary teardrop tattoo`))], [$item`psychoanalytic jar`, () =>
  // Exclude jick because he's rate-limited
  Math.max.apply(Math, _toConsumableArray($items`jar of psychoses (The Meatsmith), jar of psychoses (The Captain of the Gourd), jar of psychoses (The Crackpot Mystic), jar of psychoses (The Pretentious Artist), jar of psychoses (The Old Man), jar of psychoses (The Suspicious-Looking Guy)`.map(jar => value(jar))))], [$item`warbear whosit`, () => (0.35 * averageValue.apply(void 0, _toConsumableArray($items`warbear auto-anvil, warbear chemistry lab, warbear high-efficiency still, warbear induction oven, warbear jackhammer drill press, warbear LP-ROM burner, warbear energy bracers, warbear exhaust manifold, warbear exo-arm, warbear foil hat, warbear laser beacon, warbear oil pan`)) + 0.65 * averageValue.apply(void 0, _toConsumableArray($items`warbear metalworking primer, warbear beeping telegram, warbear gyrocopter, warbear procedural hilarity drone, warbear robo-camouflage unit, warbear sequential gaiety distribution system`))) / 100]], _toConsumableArray($items`worthless gewgaw, worthless knick-knack, worthless trinket`.map(i => [i, currency.apply(void 0, _toConsumableArray($items`seal tooth, chisel, petrified noodles, jabañero pepper, banjo strings, hot buttered roll, wooden figurine, ketchup, catsup, volleyball`).concat(_toConsumableArray(kolmafia.myClass() === $class`Seal Clubber` ? $items`figurine of an ancient seal` : [])))])), [[$item`Boris's key`, () => value($item`Boris's key lime`) - value($item`lime`)], [$item`Jarlsberg's key`, () => value($item`Jarlsberg's key lime`) - value($item`lime`)], [$item`Sneaky Pete's key`, () => value($item`Sneaky Pete's key lime`) - value($item`lime`)], [$item`fat loot token`, currency.apply(void 0, _toConsumableArray($items`Boris's key, Jarlsberg's key, Sneaky Pete's key, Boris's ring, Jarlsberg's earring, Sneaky Pete's breath spray, potato sprout, sewing kit, Spellbook: Singer's Faithful Ocelot, Spellbook: Drescher's Annoying Noise, Spellbook: Walberg's Dim Bulb, dried gelatinous cube`))], [$item`inflammable leaf`, inflammableLeafCurrency()], [$item`envelope full of Meat`, () => 50_000], [$item`crystalline cheer`, currency.apply(void 0, _toConsumableArray($items`stale cheer wine, stale Cheer-E-Os, Cheer-Up soda, cheer-o-gram, cheerful antler hat, cheerful Crimbo sweater, cheerful pajama pants`))], [$item`knucklebone`, () => Math.max.apply(Math, _toConsumableArray($items`tiny plastic sword, A Crimbo Carol\, Ch. 1, Rethinking Candy`.map(jar => value(jar)))) / 2687]], _toConsumableArray(kolmafia.Familiar.all().map(f => kolmafia.familiarEquipment(f)).filter(i => i !== $item.none && i.tradeable && i.discardable).map(i => [i, () => Math.min(saleValue(i, false), value($item`box of Familiar Jacks`))])), _toConsumableArray(inputValues)));
  var exclusions = new Set([
  // For tradeable items which can be "consumed" infinitely
  $item`ChibiBuddy™ (off)`]);
  function saleValue(item, useHistorical) {
    if (useHistorical) {
      if (kolmafia.historicalAge(item) <= 7.0 && kolmafia.historicalPrice(item) > 0) {
        var isMallMin = kolmafia.historicalPrice(item) === Math.max(100, 2 * kolmafia.autosellPrice(item));
        return isMallMin ? kolmafia.autosellPrice(item) : 0.9 * kolmafia.historicalPrice(item);
      }
    }
    return getSaleValue(item);
  }
  function currency() {
    for (var _len = arguments.length, items = new Array(_len), _key = 0; _key < _len; _key++) {
      items[_key] = arguments[_key];
    }
    var unitCost = items.map(i => {
      var coinmaster = kolmafia.Coinmaster.all().find(c => kolmafia.sellsItem(c, i));
      if (!coinmaster) {
        return [i, Infinity];
      } else {
        return [i, kolmafia.sellPrice(coinmaster, i) || Infinity];
      }
    });
    return () => Math.max.apply(Math, _toConsumableArray(unitCost.map(_ref3 => {
      var _ref4 = _slicedToArray(_ref3, 2),
        item = _ref4[0],
        cost = _ref4[1];
      return value(item) / cost;
    })));
  }
  function inflammableLeafCurrency() {
    if (!have$o()) return () => 0;
    var ignored = $items`lit leaf lasso, day shortener`; // Ignore limited purchases
    var unitCost = _toConsumableArray(burnFor).filter(entry => entry[0] instanceof kolmafia.Item && entry[0].tradeable && !ignored.includes(entry[0]));
    return () => Math.max.apply(Math, _toConsumableArray(unitCost.map(_ref5 => {
      var _ref6 = _slicedToArray(_ref5, 2),
        item = _ref6[0],
        cost = _ref6[1];
      return value(item) / cost;
    })));
  }
  function complexCandy() {
    var candies = kolmafia.Item.all().filter(i => i.candyType === "complex");
    var candyLookup = [[], [], [], [], []];
    var _iterator = _createForOfIteratorHelper(candies),
      _step;
    try {
      for (_iterator.s(); !(_step = _iterator.n()).done;) {
        var candy = _step.value;
        var id = kolmafia.toInt(candy) % 5;
        if (candy.tradeable) {
          candyLookup[id].push(candy);
        }
      }
    } catch (err) {
      _iterator.e(err);
    } finally {
      _iterator.f();
    }
    var candyIdPrices = candies.filter(i => !i.tradeable).map(i => [i, () => Math.min.apply(Math, _toConsumableArray(candyLookup[kolmafia.toInt(i) % 5].map(i => value(i))))]);
    return candyIdPrices;
  }
  function value(inputItem) {
    var useHistorical = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : false;
    var _ref7 = inputItem instanceof kolmafia.Item ? {
        item: inputItem,
        quantity: 1
      } : inputItem,
      item = _ref7.item,
      quantity = _ref7.quantity;
    if (exclusions.has(item)) return 0;
    var cachedValue = regularValueCache.get(item) ?? (useHistorical ? historicalValueCache.get(item) : undefined);
    if (cachedValue === undefined) {
      var specialValueCompute = specialValueLookup.get(item);
      var _value = (specialValueCompute === null || specialValueCompute === void 0 ? void 0 : specialValueCompute()) ?? saleValue(item, useHistorical);
      (useHistorical ? historicalValueCache : regularValueCache).set(item, _value);
      return _value;
    }
    return quantity * cachedValue;
  }
  function averageValue() {
    for (var _len2 = arguments.length, items = new Array(_len2), _key2 = 0; _key2 < _len2; _key2++) {
      items[_key2] = arguments[_key2];
    }
    return sum(items, value) / items.length;
  }
  return {
    averageValue,
    value
  };
}

var draggableFights = ["backup", "wanderer", "yellow ray", "freefight", "freerun", "conditional freefight",
// This is a free fight that will not always be successful, like Darts Bullseye
"freefight (no items)" // This is a free fight that causes you to get no item drops, like Assert your authority
];
function isDraggableFight(fight) {
  return draggableFights.includes(fight);
}
var UnlockableZones = [{
  zone: "Spaaace",
  available: () => have$P($effect`Transpondent`),
  unlocker: $item`transporter transponder`,
  noInv: false
}, {
  zone: "Wormwood",
  available: () => have$P($effect`Absinthe-Minded`),
  unlocker: $item`tiny bottle of absinthe`,
  noInv: false
}, {
  zone: "Rabbit Hole",
  available: () => have$P($effect`Down the Rabbit Hole`),
  unlocker: $item`"DRINK ME" potion`,
  noInv: false
}, {
  zone: "Conspiracy Island",
  available: () => realmAvailable("spooky"),
  unlocker: $item`one-day ticket to Conspiracy Island`,
  noInv: true
}, {
  zone: "Dinseylandfill",
  available: () => realmAvailable("stench"),
  unlocker: $item`one-day ticket to Dinseylandfill`,
  noInv: true
}, {
  zone: "The Glaciest",
  available: () => realmAvailable("cold"),
  unlocker: $item`one-day ticket to The Glaciest`,
  noInv: true
}, {
  zone: "Spring Break Beach",
  available: () => realmAvailable("sleaze"),
  unlocker: $item`one-day ticket to Spring Break Beach`,
  noInv: true
}];
function underwater(location) {
  return location.environment === "underwater";
}
var ILLEGAL_PARENTS = ["Clan Basement", "Psychoses", "PirateRealm", "A Monorail Station", "Memories"];
var ILLEGAL_ZONES = ["The Drip", "Suburbs"];
var canAdventureOrUnlockSkipList = [].concat(_toConsumableArray($locations`The Bubblin' Caldera, Barrrney's Barrr, The F'c'le, The Poop Deck, Belowdecks, The Secret Government Laboratory, The Dire Warren, Inside the Palindome, The Haiku Dungeon, An Incredibly Strange Place (Bad Trip), An Incredibly Strange Place (Mediocre Trip), An Incredibly Strange Place (Great Trip), El Vibrato Island, The Daily Dungeon, Trick-or-Treating, Seaside Megalopolis, The Orcish Frat House, Through the Spacegate, Mt. Molehill`), _toConsumableArray(kolmafia.Location.all().filter(_ref => {
  var parent = _ref.parent,
    zone = _ref.zone;
  return ILLEGAL_PARENTS.includes(parent) || ILLEGAL_ZONES.includes(zone);
})));
function canAdventureOrUnlock(loc) {
  var includeUnlockable = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : true;
  var skiplist = _toConsumableArray(canAdventureOrUnlockSkipList);
  if (!have$P($effect`Ultrahydrated`)) {
    skiplist.push($location`The Oasis`);
  }
  if (!have$P($item`repaid diaper`) && have$P($item`Great Wolf's beastly trousers`)) {
    skiplist.push($location`The Icy Peak`);
  }
  if (minutesToNoon() === 0 || minutesToMidnight() === 0) {
    skiplist.push.apply(skiplist, _toConsumableArray(LOCATIONS));
  }
  if (!goingPostalSafe() || !have$P($item`Hey Deze map`) && !have$P($item`Hey Deze nuts`) && !haveInCampground($item`pagoda plans`) // Quest not tracked, but these three checks work
  ) {
    skiplist.push($location`Pandamonium Slums`);
  }
  var canUnlock = includeUnlockable && UnlockableZones.some(z => loc.zone === z.zone && (z.available() || !z.noInv));
  return !underwater(loc) && !skiplist.includes(loc) && (kolmafia.canAdventure(loc) || canUnlock);
}
function unlock(loc, value) {
  var unlockableZone = UnlockableZones.find(z => z.zone === loc.zone);
  if (!unlockableZone) return kolmafia.canAdventure(loc);
  if (unlockableZone.available()) return true;
  if (kolmafia.buy(1, unlockableZone.unlocker, value) === 0) return false;
  return kolmafia.use(unlockableZone.unlocker);
}
var backupSkiplist = $locations`The Mansion of Dr. Weirdeaux, Professor Jacking's Huge-A-Ma-Tron, Your Mushroom Garden, The Island Barracks`;
// These are locations where all non-combats have skips or lead to a combat.
var backupSafelist = $locations`The Haunted Gallery, The Haunted Ballroom, The Haunted Library, The Penultimate Fantasy Airship, Cobb's Knob Barracks, The Castle in the Clouds in the Sky (Basement), The Castle in the Clouds in the Sky (Ground Floor), The Castle in the Clouds in the Sky (Top Floor), The Haiku Dungeon, Twin Peak, A Mob of Zeppelin Protesters, The Upper Chamber, The Orcish Frat House`;
// These are locations where all non-combats are skippable
var yellowRaySafelist = $locations`Madness Bakery, The Overgrown Lot, The Skeleton Store, The Haunted Gallery, The Haunted Ballroom, The Haunted Library, Cobb's Knob Barracks, The Castle in the Clouds in the Sky (Basement), The Castle in the Clouds in the Sky (Ground Floor), The Haiku Dungeon, Twin Peak, A Mob of Zeppelin Protesters, The Upper Chamber`;
function canWanderTypeBackup(location) {
  return !backupSkiplist.includes(location) && (location.combatPercent >= 100 || backupSafelist.includes(location));
}
function canWanderTypeFreeFight(location) {
  if (location === $location`The Fun-Guy Mansion` && get$2("funGuyMansionKills") >= 100) {
    return false;
  }
  return !backupSkiplist.includes(location) && (location.combatPercent >= 100 || yellowRaySafelist.includes(location));
}
var wandererSkiplist = $locations`The Smut Orc Logging Camp, The Batrat and Ratbat Burrow, Guano Junction, The Beanbat Chamber, A-Boo Peak, The Mouldering Mansion, The Rogue Windmill, The Stately Pleasure Dome, Pandamonium Slums, Lair of the Ninja Snowmen`;
function canWanderTypeWander(location) {
  return !wandererSkiplist.includes(location) && location.wanderers;
}
function canWander(location, type) {
  if (underwater(location)) return false;
  switch (type) {
    case "backup":
    case "freerun":
      return canWanderTypeBackup(location);
    case "freefight":
    case "yellow ray":
    case "conditional freefight":
    case "freefight (no items)":
      return canWanderTypeFreeFight(location);
    case "wanderer":
      return canWanderTypeWander(location);
  }
}
var WandererTarget = /*#__PURE__*/_createClass(
/**
 * Process for determining where to put a wanderer to extract additional value from it
 * @param options An object containing the following  keys:
 * @param options.name name of this wanderer - for documentation/logging purposes
 * @param options.location returns the location to adventure to target this; null only if something goes wrong
 * @param options.zoneValue value of an encounter existing within a zone, regardless of which monster you fight
 * @param options.monsterBonusValues A map of monsters and their expected non-itemdrop bonus value from this wanderer for encountering it
 * @param options.monsterItemValues A map of monsters and their expected itemdrop value from this wanderer for encountering it
 * @param options.prepareTurn attempt to set up, spending meat and or items as necessary
 */
function WandererTarget(_ref2) {
  var name = _ref2.name,
    location = _ref2.location,
    zoneValue = _ref2.zoneValue,
    _ref2$monsterBonusVal = _ref2.monsterBonusValues,
    monsterBonusValues = _ref2$monsterBonusVal === void 0 ? new Map() : _ref2$monsterBonusVal,
    _ref2$monsterItemValu = _ref2.monsterItemValues,
    monsterItemValues = _ref2$monsterItemValu === void 0 ? new Map() : _ref2$monsterItemValu,
    _ref2$prepareTurn = _ref2.prepareTurn,
    prepareTurn = _ref2$prepareTurn === void 0 ? () => true : _ref2$prepareTurn;
  _classCallCheck(this, WandererTarget);
  _defineProperty(this, "name", void 0);
  _defineProperty(this, "location", void 0);
  _defineProperty(this, "zoneValue", void 0);
  _defineProperty(this, "monsterBonusValues", void 0);
  _defineProperty(this, "monsterItemValues", void 0);
  _defineProperty(this, "prepareTurn", void 0);
  this.name = name;
  this.location = location;
  this.zoneValue = zoneValue;
  this.monsterBonusValues = monsterBonusValues;
  this.monsterItemValues = monsterItemValues;
  this.prepareTurn = prepareTurn;
});
function defaultFactory() {
  return [new WandererTarget({
    name: "Default",
    location: $location`The Haunted Kitchen`,
    zoneValue: 0
  })];
}
var WanderingSources = [{
  name: "CMG",
  item: $item`cursed magnifying glass`,
  max: 3,
  property: "_voidFreeFights",
  type: "wanderer"
}, {
  name: "Voter",
  item: $item`"I Voted!" sticker`,
  max: 3,
  property: "_voteFreeFights",
  type: "wanderer"
}, {
  name: "Voter",
  item: $item`"I Voted!" sticker`,
  max: 3,
  property: "_voteFreeFights",
  type: "wanderer"
}, {
  name: "Backup",
  item: $item`backup camera`,
  max: 11,
  property: "_backUpUses",
  type: "backup"
}];
function wandererTurnsAvailableToday(options, location, requiresMonsterKill) {
  var canWanderCache = {
    backup: canWander(location, "backup"),
    wanderer: canWander(location, "wanderer"),
    "yellow ray": canWander(location, "yellow ray"),
    freefight: canWander(location, "freefight"),
    "conditional freefight": canWander(location, "conditional freefight"),
    "freefight (no items)": canWander(location, "freefight (no items)"),
    freerun: canWander(location, "freerun")
  };
  var digitize = canWanderCache["backup"] && options.digitzesRemaining ? options.digitzesRemaining(options.estimatedTurns()) : 0;
  var pigSkinnerRay = canWanderCache["backup"] && have$P($skill`Free-For-All`) ? Math.floor(options.estimatedTurns() / 25) : 0;
  var yellowRayCooldown = have$P($skill`Fondeluge`) ? 50 : 100;
  var yellowRay = canWanderCache["yellow ray"] ? Math.floor(options.estimatedTurns() / yellowRayCooldown) : 0;
  var wanderers = sum(WanderingSources, source => canWanderCache[source.type] && have$P(source.item) ? clamp(get$2(source.property), 0, source.max) : 0);
  // This is the ELG cooldown for spring shoes
  var freeRun = !requiresMonsterKill && canWanderCache["freerun"] && have$P($item`spring shoes`) ? Math.floor(options.estimatedTurns() / 30) : 0;
  return digitize + pigSkinnerRay + yellowRay + wanderers + freeRun;
}
var LIMITED_BOFA_DROPS = $items`pocket wish, tattered scrap of paper`;
function bofaValue(_ref3, monster) {
  var plentifulMonsters = _ref3.plentifulMonsters,
    itemValue = _ref3.itemValue,
    effectValue = _ref3.effectValue;
  switch (monster.factType) {
    case "item":
      {
        var item = kolmafia.itemFact(monster);
        var quantity = kolmafia.numericFact(monster);
        if (LIMITED_BOFA_DROPS.includes(item) && plentifulMonsters.some(monster => kolmafia.toItem(monster.fact) === item)) {
          return 0;
        }
        return quantity * itemValue(item);
      }
    case "effect":
      {
        var effect = kolmafia.effectFact(monster);
        var duration = kolmafia.numericFact(monster);
        return effectValue(effect, duration);
      }
    case "meat":
      {
        return kolmafia.numericFact(monster);
      }
    default:
      return 0;
  }
}
function questBetween(quest, lower, upper) {
  var inclusive = arguments.length > 3 && arguments[3] !== undefined ? arguments[3] : true;
  var step = questStep$1(quest);
  return inclusive ? step >= lower && step <= upper : step > lower && step < upper;
}
var goingPostalSafe = () => !questBetween("questM11Postal", -1, 999, false); // Going Postal tracking is not especially granular
var alwaysSafeUltraRares = $locations`The Cola Wars Battlefield, The Icy Peak, Cobb's Knob Treasury, Cobb's Knob Menagerie\, Level 1, The Dungeons of Doom, A Mob of Zeppelin Protesters, Camp Logging Camp`;
function getAvailableUltraRareZones() {
  var zones = _toConsumableArray(alwaysSafeUltraRares);
  if ($location`The Haunted Billiards Room`.turnsSpent > 0) {
    zones.push($location`The Haunted Billiards Room`); // no better check for pool cue adventure
  }
  if (questStep$1("questG03Ego") !== 0) {
    if (!questBetween("questG04Nemesis", 0, 2)) {
      zones.push($location`The Unquiet Garves`);
    }
    if (goingPostalSafe()) zones.push($location`The VERY Unquiet Garves`);
  }
  if (have$P($item`the Slug Lord's map`) &&
  // Quest not tracked, but certainly if you currently own the map you aren't going to get it again
  goingPostalSafe() && questStep$1("questG08Moxie") !== 0 && questStep$1("questM02Artist") !== 0) {
    zones.push($location`The Sleazy Back Alley`);
  }
  if (goingPostalSafe() && (have$P($item`Hey Deze map`) || have$P($item`Hey Deze nuts`) || haveInCampground($item`pagoda plans`)) // Quest not tracked, but these three checks work
  ) {
    zones.push($location`Pandamonium Slums`);
  }
  if (questBetween("questL11Palindome", 1, 5)) {
    zones.push($location`Inside the Palindome`); // Step 1 is having rearranged the photos, which means you got all the superlikelies already
  }
  if (goingPostalSafe() && $location`The Spooky Forest`.turnsSpent - $location`The Spooky Forest`.lastNoncombatTurnsSpent >= 7) {
    zones.push($location`The Spooky Forest`);
  }
  return zones.filter(l => kolmafia.canAdventure(l));
}
var nameCollisionCache = new Map();
function hasNameCollision(monster) {
  var cached = nameCollisionCache.get(monster);
  if (cached !== undefined) return cached;
  var _iterator = _createForOfIteratorHelper(kolmafia.Monster.all()),
    _step;
  try {
    for (_iterator.s(); !(_step = _iterator.n()).done;) {
      var other = _step.value;
      if (other === monster) continue;
      if (other.manuelName === monster.manuelName) {
        nameCollisionCache.set(other, true);
        nameCollisionCache.set(monster, true);
        return true;
      }
    }
  } catch (err) {
    _iterator.e(err);
  } finally {
    _iterator.f();
  }
  nameCollisionCache.set(monster, false);
  return false;
}
// TODO These seem to be bugged peridot zones. Can remove if they get fixed.
var unperidotableZones = $locations`A Mob of Zeppelin Protesters, The Upper Chamber, The Haunted Billiards Room`;
/**
 * Retrieve an element from a map if it exists; setting a value for the given key if it doesn't.
 * @param map The map in question.
 * @param key The key to try to retrieve from the map.
 * @param defaultValue A delayed value to assign to the key in the map if there isn't already an existing object.
 * @returns The retrieved value, which, by the end of this function, will exist in the map.
 */
function ensureMapElement(map, key, defaultValue) {
  var current = map.get(key);
  if (map.has(key)) return current;
  var value = undelay(defaultValue);
  map.set(key, value);
  return value;
}
/**
 * Add the values of a numeric-valued map to another with the same key type, mutating the first map.
 * @param left The "left" addend map. This map will be mutated by this function.
 * @param right The "right" addend map, to be added to the left.
 */
function addMaps(left, right) {
  var _iterator2 = _createForOfIteratorHelper(right),
    _step2;
  try {
    for (_iterator2.s(); !(_step2 = _iterator2.n()).done;) {
      var _step2$value = _slicedToArray(_step2.value, 2),
        key = _step2$value[0],
        value = _step2$value[1];
      var current = left.get(key) ?? 0;
      left.set(key, current + value);
    }
  } catch (err) {
    _iterator2.e(err);
  } finally {
    _iterator2.f();
  }
}
var BAD_ATTRIBUTES = ["LUCKY", "ULTRARARE", "BOSS"];
function availableMonsters(location) {
  kolmafia.appearanceRates(location, true); // Force a recalculation
  var rates = kolmafia.appearanceRates(location);
  return kolmafia.getMonsters(location).filter(m => !BAD_ATTRIBUTES.some(attribute => m.attributes.includes(attribute)) && rates[m.name] > 0);
}
var UNPERIDOTABLE_MONSTERS = new Set([].concat(_toConsumableArray(kolmafia.modifierEval("G") < 4 ? $monsters`alielf, cat-alien, dog-alien` : []), _toConsumableArray($monsters`Arizona bark scorpion, swimming pool monster`)));
function averageMonsterValue(monsterValues, rates) {
  return sum(_toConsumableArray(monsterValues.entries()), _ref4 => {
    var _ref5 = _slicedToArray(_ref4, 2),
      monster = _ref5[0],
      value = _ref5[1];
    return value * (rates[monster.name] / 100);
  });
}

function considerAbandon(options, locationSkiplist) {
  var location = getLocation();
  var remaningTurns = Math.ceil((100 - get$2("guzzlrDeliveryProgress")) / (10 - get$2("_guzzlrDeliveries")));
  kolmafia.print(`Got guzzlr quest ${getTier()} at ${getLocation()} with remaining turns ${remaningTurns}`);
  if (canAbandon() && (
  // consider abandoning
  !location ||
  // if mafia failed to track the location correctly
  locationSkiplist.includes(location) || !canAdventureOrUnlock(location) ||
  // or the zone is marked as "generally cannot adv"
  options.ascend && wandererTurnsAvailableToday(options, location, true) < remaningTurns) // or ascending and not enough turns to finish
  ) {
    kolmafia.print("Abandoning...");
    abandon();
  }
}
function acceptGuzzlrQuest(options, locationSkiplist) {
  if (isQuestActive()) considerAbandon(options, locationSkiplist);
  while (!isQuestActive()) {
    kolmafia.print("Picking a guzzlr quest");
    if (canPlatinum() && !(options.prioritizeCappingGuzzlr && haveFullPlatinumBonus())) {
      acceptPlatinum();
    } else if (canGold() && (haveFullBronzeBonus() || !haveFullGoldBonus())) {
      // if gold is not maxed, do that first since they are limited per day
      acceptGold();
    } else {
      // fall back to bronze when can't plat, can't gold, or bronze is not maxed
      acceptBronze();
    }
    considerAbandon(options, locationSkiplist);
  }
}
function guzzlrValuePerTurn(buckValue, tier, guzzlrBooze) {
  var turnsToCompleteQuest = 100 / Math.max(3, 10 - get$2("_guzzlrDeliveries"));
  var boozePrice = kolmafia.mallPrice(guzzlrBooze);
  switch (tier) {
    case null:
      return 0;
    case "bronze":
      return (3 * buckValue - boozePrice) / turnsToCompleteQuest;
    case "gold":
      return (6 * buckValue - boozePrice) / turnsToCompleteQuest;
    case "platinum":
      return (21.5 * buckValue - boozePrice) / turnsToCompleteQuest;
  }
}
function guzzlrFactory(type, locationSkiplist, options) {
  if (have$y() && type !== "freerun") {
    var buckValue = options.itemValue($item`Guzzlrbuck`);
    acceptGuzzlrQuest(options, locationSkiplist);
    var location = getLocation();
    if (location !== null) {
      var guzzlrBooze = getTier() === "platinum" ? getCheapestPlatinumCocktail() : getBooze();
      return guzzlrBooze ? [new WandererTarget({
        name: "Guzzlr",
        location: location,
        zoneValue: guzzlrValuePerTurn(buckValue, getTier(), guzzlrBooze),
        prepareTurn: () => {
          if (!guzzlrBooze) {
            // this is an error state - accepted a guzzlr quest but mafia doesn't know the booze
            return false;
          }
          if (!have$P(guzzlrBooze)) {
            var fancy = guzzlrBooze && kolmafia.craftType(guzzlrBooze).includes("fancy");
            if (guzzlrBooze && (!fancy || fancy && freeCrafts("booze") > 0)) {
              kolmafia.retrieveItem(guzzlrBooze);
            } else if (guzzlrBooze) {
              kolmafia.buy(1, guzzlrBooze, buckValue * expectedReward());
            }
          }
          return have$P(guzzlrBooze);
        }
      })] : [];
    }
  }
  return [];
}

var LovebugTargets = [
// exclude barf mountain because we spend most of our turns there anyway
{
  element: "cold",
  location: $location`VYKEA`,
  currency: $item`Wal-Mart gift certificate`
}, {
  element: "sleaze",
  location: $location`The Fun-Guy Mansion`,
  currency: $item`Beach Buck`
}, {
  element: "spooky",
  location: $location`The Deep Dark Jungle`,
  currency: $item`Coinspiracy`
}];
function lovebugsFactory(type, _locationSkiplist, options) {
  if (get$2("lovebugsUnlocked") && type !== "freerun") {
    return LovebugTargets.filter(t => realmAvailable(t.element)).map(t => new WandererTarget({
      name: `Lovebugs`,
      location: t.location,
      zoneValue: options.itemValue(t.currency) * 0.05
    }));
  }
  return [];
}

function valueMonster(m, forceItemDrops, options) {
  var canDuplicate = have$I() && duplicateUsesRemaining() > 0;
  var canMctwist = have$P($item`pro skateboard`) && !get$2("_epicMcTwistUsed");
  var possibleDuplicateFactor = 2 ** [canDuplicate, canMctwist].filter(Boolean).length;
  var items = kolmafia.itemDropsArray(m).filter(drop => ["", "n"].includes(drop.type));
  var duplicateFactor = !m.attributes.includes("NOCOPY") ? possibleDuplicateFactor : 1;
  // TODO: this should consider unbuffed meat drop and unbuffed item drop, probably
  var meatDrop = clamp((m.minMeat + m.maxMeat) / 2, 0, 1000);
  var itemDrop = duplicateFactor * sum(items, drop => {
    var yrRate = (drop.type === "" && forceItemDrops ? 100 : drop.rate) / 100;
    return yrRate * options.itemValue(drop.drop);
  });
  return itemDrop + meatDrop;
}
function monsterValues$1(location, forceItemDrops, options) {
  var monsters = availableMonsters(location);
  if (monsters.length === 0) {
    return new Map();
  }
  var monsterValues = new Map(monsters.map(m => {
    return [m, valueMonster(m, forceItemDrops, options)];
  }));
  return monsterValues;
}
// Monster item drop values
function itemDropFactory(type, locationSkiplist, options) {
  if (["yellow ray", "freefight", "conditional freefight"].includes(type)) {
    var validLocations = kolmafia.Location.all().filter(location => canWander(location, "yellow ray") && canAdventureOrUnlock(location) && !locationSkiplist.includes(location));
    return _toConsumableArray(validLocations).map(l => {
      return new WandererTarget({
        name: `Item Drop Values`.concat(type === "yellow ray" ? ` (Guaranteed Drops)` : ""),
        location: l,
        zoneValue: 0,
        monsterItemValues: monsterValues$1(l, type === "yellow ray", options)
      });
    });
  }
  return [];
}

var bonusColor = ["black", "blue", "green", "red"];
var TREASURE_HOUSE_FAT_LOOT_TOKEN_COST = 20000;
// taken from tour guide, who took it from beldur
var locationColor = {
  black: $location`Vanya's Castle`,
  blue: $location`Megalo-City`,
  green: $location`Hero's Field`,
  red: $location`The Fungus Plains`
};
function value(color, options) {
  var denominator = options.ascend ? get$2("8BitScore") - TREASURE_HOUSE_FAT_LOOT_TOKEN_COST : TREASURE_HOUSE_FAT_LOOT_TOKEN_COST;
  return options.itemValue($item`fat loot token`) * kolmafia.eightBitPoints(locationColor[color]) / denominator;
}
function eightbitFactory(type, locationSkiplist, options) {
  if (have$P($item`continuum transfunctioner`) && !["backup", "freerun"].includes(type) && get$2("8BitScore") < TREASURE_HOUSE_FAT_LOOT_TOKEN_COST) {
    return bonusColor.map(color => new WandererTarget({
      name: `8-bit (${color})`,
      location: locationColor[color],
      zoneValue: value(color, options)
    })).filter(t => !locationSkiplist.includes(t.location));
  }
  return [];
}

function gingerbreadFactory(type, _locationSkiplist, options) {
  if (type !== "wanderer" && available$3() && minutesToMidnight() !== 0 && minutesToNoon() !== 0 && (minutesToMidnight() > 0 && (kolmafia.availableAmount($item`sprinkles`) > 5 || kolmafia.haveOutfit("gingerbread best")) || minutesToNoon() > 0)) {
    var turnsUntilNextNC = minutesToNoon() > 0 ? minutesToNoon() : minutesToMidnight();
    return [new WandererTarget({
      name: "Gingerbread Minutes",
      location: $location`Gingerbread Civic Center`,
      // 50 value is arbitrary until we have proper valuation of our options from Midnight and Noons NCs
      zoneValue: wandererTurnsAvailableToday(options, $location`Gingerbread Civic Center`, false) > turnsUntilNextNC ? 50 : 0
    })];
  }
  return [];
}

function ultraRareFactory(type, _locationSkiplist, options) {
  return getAvailableUltraRareZones().filter(l => kolmafia.canAdventure(l)).map(z => {
    var _itemDropsArray$;
    var ultraRareMonster = kolmafia.getMonsters(z).find(m => m.attributes.includes("ULTRARARE"));
    return new WandererTarget({
      name: `UltraRare Monster ${ultraRareMonster}`,
      location: z,
      zoneValue: options.itemValue(((_itemDropsArray$ = kolmafia.itemDropsArray(ultraRareMonster ?? $monster.none)[0]) === null || _itemDropsArray$ === void 0 ? void 0 : _itemDropsArray$.drop) ?? $item.none) / 500_000_000 // Ultra rares are rare, let's say 1 in 500 million to be conservative
    });
  });
}

function cookbookbatQuestFactory(type, locationSkiplist, options) {
  var questLocation = get$2("_cookbookbatQuestLastLocation");
  var questReward = get$2("_cookbookbatQuestIngredient");
  var questMonster = get$2("_cookbookbatQuestMonster");
  // TODO: These are fixed in mafia (we get the correct location), however we cannot use peridot to target them for cookbookbat specifically, because we cannot know which monster cookbookbat wants because of duplicate names
  var blackListedLocations = $locations`The Orcish Frat House, The Orcish Frat House (Bombed Back to the Stone Age)`;
  if (["yellow ray", "freefight", "freerun", "conditional freefight", "freefight (no items)"].includes(type) &&
  // Runs still get you the quest reward
  questLocation && questReward && questMonster && !locationSkiplist.includes(questLocation) && !blackListedLocations.includes(questLocation)) {
    return [new WandererTarget({
      name: `Cookbookbat Quest`,
      location: questLocation,
      zoneValue: 0,
      monsterBonusValues: new Map([[questMonster, 3 * options.itemValue(questReward)]])
    })];
  }
  return [];
}

function monsterValues(location, options) {
  var monsters = availableMonsters(location);
  return new Map(monsters.map(m => {
    return [m, bofaValue(options, m)];
  }));
}
function bofaFactory(type, locationSkiplist, options) {
  if (["yellow ray", "freefight", "conditional freefight", "freefight (no items)"].includes(type) && have$P($skill`Just the Facts`)) {
    var validLocations = kolmafia.Location.all().filter(location => canWander(location, "yellow ray") && canAdventureOrUnlock(location) && !locationSkiplist.includes(location));
    return _toConsumableArray(validLocations).map(l => {
      return new WandererTarget({
        name: `Book of Facts`,
        location: l,
        zoneValue: 0,
        monsterBonusValues: monsterValues(l, options)
      });
    });
  }
  return [];
}

function sober$1() {
  return kolmafia.myInebriety() <= kolmafia.inebrietyLimit() + (kolmafia.myFamiliar() === $familiar`Stooper` ? -1 : 0);
}
var wanderFactories = [defaultFactory, itemDropFactory, lovebugsFactory, guzzlrFactory, eightbitFactory, gingerbreadFactory, ultraRareFactory, cookbookbatQuestFactory, bofaFactory];
function zoneAverageMonsterValue(location, monsterBonusValues, monsterItemValues) {
  var rates = kolmafia.appearanceRates(location, true);
  var averageBonusValue = averageMonsterValue(monsterBonusValues, rates);
  var averageItemValue = averageMonsterValue(monsterItemValues, rates);
  return averageBonusValue + averageItemValue;
}
function targetedMonsterValue(monsterBonusValues, monsterItemValues) {
  var monsterTotalValue = new Map(monsterBonusValues);
  addMaps(monsterTotalValue, monsterItemValues);
  var availableMonsters = _toConsumableArray(monsterTotalValue.entries()).filter(_ref => {
    var _ref2 = _slicedToArray(_ref, 1),
      m = _ref2[0];
    return !UNPERIDOTABLE_MONSTERS.has(m);
  });
  if (availableMonsters.length === 0) return [$monster.none, 0];
  return maxBy(availableMonsters, 1);
}
function zoneRefractedGazeValue(location, monsterBonusValues, monsterItemValues) {
  var totalItemValue = sumNumbers(_toConsumableArray(monsterItemValues.values()));
  var withFeesh = totalItemValue;
  // If we aren't using Feesh, we will get bonuses from one monster, but lose the items from that monster
  var rates = kolmafia.appearanceRates(location, true);
  var averageBonusValue = averageMonsterValue(monsterBonusValues, rates);
  var averageItemValue = averageMonsterValue(monsterItemValues, rates);
  var noFeesh = totalItemValue - averageItemValue + averageBonusValue;
  return {
    withFeesh,
    noFeesh
  };
}
function updateZoneData(zoneData, wanderer) {
  zoneData.targets.push(wanderer);
  addMaps(zoneData.monsterBonusValues, wanderer.monsterBonusValues);
  addMaps(zoneData.monsterItemValues, wanderer.monsterItemValues);
  zoneData.zoneValue += wanderer.zoneValue;
}
function determineWandererLocationInfo(shouldRefract, shouldFeesh, shouldPeridot, bestMonsterCandidate, monsterTargetedValue, monsterAverageValue, refractedGazeValue) {
  if (shouldRefract) {
    return {
      bestMonster: $monster.none,
      monsterValue: refractedGazeValue,
      shouldBcz: true,
      shouldFeesh
    };
  }
  if (shouldPeridot) {
    return {
      bestMonster: bestMonsterCandidate,
      monsterValue: monsterTargetedValue,
      shouldBcz: false,
      shouldFeesh: false
    };
  }
  return {
    bestMonster: $monster.none,
    monsterValue: monsterAverageValue,
    shouldBcz: false,
    shouldFeesh: false
  };
}
function bestWander(type, locationSkiplist, nameSkiplist, options) {
  var locationValues = new Map();
  // Create data for zone/monster values from all factories
  for (var _i = 0, _wanderFactories = wanderFactories; _i < _wanderFactories.length; _i++) {
    var wanderFactory = _wanderFactories[_i];
    var wanderTargets = wanderFactory(type, locationSkiplist, options);
    var _iterator = _createForOfIteratorHelper(wanderTargets),
      _step;
    try {
      for (_iterator.s(); !(_step = _iterator.n()).done;) {
        var wanderTarget = _step.value;
        if (!nameSkiplist.includes(wanderTarget.name) && !locationSkiplist.includes(wanderTarget.location) && canWander(wanderTarget.location, type)) {
          var location = wanderTarget.location;
          // Retrieve existing data for location if extant
          var zoneData = ensureMapElement(locationValues, location, {
            location,
            targets: [],
            zoneValue: 0,
            monsterBonusValues: new Map(),
            monsterItemValues: new Map()
          });
          updateZoneData(zoneData, wanderTarget);
        }
      }
    } catch (err) {
      _iterator.e(err);
    } finally {
      _iterator.f();
    }
  }
  // Determine combined values, and whether best forced target is better than the best average location drops
  var locationMonsterValues = new Map();
  var _iterator2 = _createForOfIteratorHelper(locationValues),
    _step2;
  try {
    for (_iterator2.s(); !(_step2 = _iterator2.n()).done;) {
      var _step2$value = _slicedToArray(_step2.value, 2),
        _location = _step2$value[0],
        _step2$value$ = _step2$value[1],
        targets = _step2$value$.targets,
        zoneValue = _step2$value$.zoneValue,
        monsterBonusValues = _step2$value$.monsterBonusValues,
        monsterItemValues = _step2$value$.monsterItemValues;
      var monsterAverageValue = zoneAverageMonsterValue(_location, monsterBonusValues, monsterItemValues);
      var _zoneRefractedGazeVal = zoneRefractedGazeValue(_location, monsterBonusValues, monsterItemValues),
        withFeesh = _zoneRefractedGazeVal.withFeesh,
        noFeesh = _zoneRefractedGazeVal.noFeesh;
      var shouldFeeshCandidate = have$P($item`Monodent of the Sea`) && withFeesh > noFeesh;
      var refractedGazeValue = options.canRefractedGaze && type !== "conditional freefight" ? shouldFeeshCandidate ? withFeesh : noFeesh : 0;
      var _targetedMonsterValue = targetedMonsterValue(monsterBonusValues, monsterItemValues),
        _targetedMonsterValue2 = _slicedToArray(_targetedMonsterValue, 2),
        bestMonsterCandidate = _targetedMonsterValue2[0],
        monsterTargetedValue = _targetedMonsterValue2[1];
      var shouldRefract = refractedGazeValue > monsterAverageValue && refractedGazeValue > monsterTargetedValue;
      var shouldPeridot = canImperil(_location) && !unperidotableZones.includes(_location) && !shouldRefract && monsterTargetedValue > monsterAverageValue;
      var _determineWandererLoc = determineWandererLocationInfo(shouldRefract, shouldFeeshCandidate, shouldPeridot, bestMonsterCandidate, monsterTargetedValue, monsterAverageValue, refractedGazeValue),
        bestMonster = _determineWandererLoc.bestMonster,
        monsterValue = _determineWandererLoc.monsterValue,
        shouldBcz = _determineWandererLoc.shouldBcz,
        shouldFeesh = _determineWandererLoc.shouldFeesh;
      locationMonsterValues.set(_location, {
        location: _location,
        targets,
        peridotMonster: bestMonster,
        value: zoneValue + monsterValue,
        useRefractedGaze: shouldBcz,
        useFeesh: shouldFeesh
      });
    }
  } catch (err) {
    _iterator2.e(err);
  } finally {
    _iterator2.f();
  }
  if (locationMonsterValues.size === 0) {
    throw "Could not determine a wander target!";
  }
  return maxBy(_toConsumableArray(locationMonsterValues.values()), "value");
}
/**
 * Recursively Check for zones to wander to
 * @param type type of fight we are looking for
 * @param nameSkiplist Any wanderer tasks that should be skipped because they could not be prepared
 * @param locationSkiplist Any locations that should be skipped because they could not be unlocked
 * @returns A location at which to wander
 */
function wanderWhere(options, type) {
  var nameSkiplist = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : [];
  var locationSkiplist = arguments.length > 3 && arguments[3] !== undefined ? arguments[3] : [];
  var candidate = bestWander(type, locationSkiplist, nameSkiplist, options);
  var failed = candidate.targets.filter(target => !target.prepareTurn());
  var badLocation = !canAdventureOrUnlock(candidate.location) || !unlock(candidate.location, candidate.value) || !canWander(candidate.location, type) ? [candidate.location] : [];
  if (failed.length > 0 || badLocation.length > 0) {
    return wanderWhere(options, type, [].concat(_toConsumableArray(nameSkiplist), _toConsumableArray(failed.map(target => target.name))), [].concat(_toConsumableArray(locationSkiplist), badLocation));
  } else {
    var targets = candidate.targets.map(t => t.name).join("; ");
    var value = candidate.value.toFixed(2);
    var peridotPrintText = candidate.peridotMonster !== $monster.none ? `, forcing ${candidate.peridotMonster.name},` : "";
    var bczPrintText = candidate.useRefractedGaze ? `, using Refracted Gaze${candidate.useFeesh ? ` with Monodent` : ""},` : "";
    kolmafia.print(`Wandering at ${candidate.location}${peridotPrintText}${bczPrintText} for expected value ${value} (${targets})`, kolmafia.isDarkMode() ? "yellow" : "blue");
    return {
      location: candidate.location,
      peridotMonster: candidate.peridotMonster,
      familiar: candidate.targets.find(t => t.name.includes(`Cookbookbat`)) ? $familiar`Cookbookbat` : $familiar`none`,
      useRefractedGaze: candidate.useRefractedGaze ?? false,
      useFeesh: candidate.useFeesh ?? false
    };
  }
}
var WandererManager = /*#__PURE__*/function () {
  function WandererManager(_options) {
    _classCallCheck(this, WandererManager);
    _defineProperty(this, "unsupportedChoices", new Map([[$location`The Spooky Forest`, {
      502: 2,
      505: 2
    }], [$location`Guano Junction`, {
      1427: 1
    }], [$location`The Hidden Apartment Building`, {
      780: 6,
      1578: 6
    }], [$location`The Black Forest`, {
      923: 1,
      924: 1
    }], [$location`LavaCo™ Lamp Factory`, {
      1091: 9
    }], [$location`The Haunted Laboratory`, {
      884: 6
    }], [$location`The Haunted Nursery`, {
      885: 6
    }], [$location`The Haunted Storage Room`, {
      886: 6
    }], [$location`The Haunted Ballroom`,
    // Skip, and Choose currently playing song, or skip
    () => ({
      90: 3,
      106: get$2("lastQuartetRequest") || 4
    })], [$location`The Haunted Library`, {
      163: 4,
      888: 5,
      889: 5
    }], [$location`The Haunted Gallery`, {
      89: 6,
      91: 2
    }], [$location`The Hidden Park`, {
      789: 6
    }], [$location`A Mob of Zeppelin Protesters`, {
      1432: 1,
      856: 2,
      857: 3,
      858: 2
    }], [$location`A-Boo Peak`, {
      1430: 2
    }], [$location`Sloppy Seconds Diner`, {
      919: 6
    }], [$location`VYKEA`, {
      1115: 6
    }], [$location`The Ice Hotel`, (options, valueOfTurn) => {
      var valueOfCertificates = get$2("_iceHotelRoomsRaided") ? 0 : options.itemValue($item`Wal-Mart gift certificate`) * 3;
      return {
        1116: valueOfCertificates > valueOfTurn ? 5 : 6
      };
    }], [$location`The Castle in the Clouds in the Sky (Basement)`, {
      669: 1,
      670: 4,
      671: 4
    }], [$location`The Haunted Bedroom`, {
      876: 1,
      // old leather wallet, 500 meat
      877: 1,
      // old coin purse, 500 meat
      878: 1,
      // 400-600 meat
      879: 2,
      // grouchy spirit
      880: 2 // a dumb 75 meat club
    }], [$location`The Copperhead Club`, {
      855: 4
    }], [$location`The Haunted Bathroom`, {
      882: 2
    }],
    // skip; it's the towel adventure but we don't want towels
    [$location`The Castle in the Clouds in the Sky (Top Floor)`, {
      1431: 1,
      675: 4,
      // Go to Steampunk choice
      676: 4,
      // Go to Punk Rock choice
      677: 1,
      // Fight Steam Punk Giant
      678: 3 // Go to Steampunk choice
    }], [$location`The Castle in the Clouds in the Sky (Ground Floor)`, {
      672: 3,
      // Skip
      673: 3,
      // Skip
      674: 3,
      // Skip
      1026: 3 // Skip
    }], [$location`The Hidden Office Building`, {
      786: 6
    }], [$location`The Hidden Temple`, {
      581: 3
    }],
    // Fight clan of cave bars
    [$location`Cobb's Knob Barracks`, {
      522: 2
    }],
    // skip
    [$location`The Penultimate Fantasy Airship`, {
      178: 2,
      182: 1
    }],
    // Skip, and Fight random enemy
    [$location`The Haiku Dungeon`, {
      297: 3
    }],
    // skip
    [$location`The Orcish Frat House`, {
      1425: 4
    }],
    // fight eXtreme Sports Orcs
    [$location`Madness Bakery`, {
      1061: 6
    }], [$location`The Skeleton Store`, {
      1060: 5
    }], [$location`The Overgrown Lot`, {
      1062: 7
    }], [$location`The Haunted Billiards Room`, {
      1436: 2,
      875: 3
    }] // Hustle away from the ghost
    ]));
    _defineProperty(this, "equipment", new Map([].concat(_toConsumableArray(kolmafia.Location.all().filter(l => l.zone === "The 8-Bit Realm").map(l => [l, $items`continuum transfunctioner`])), [[$location`Shadow Rift (The 8-Bit Realm)`, $items`continuum transfunctioner`]])));
    _defineProperty(this, "cacheKey", "");
    _defineProperty(this, "targets", {});
    _defineProperty(this, "options", void 0);
    this.options = _options;
  }
  return _createClass(WandererManager, [{
    key: "getTarget",
    value: function getTarget(wanderer) {
      var _this$targets, _ref4;
      var _ref3 = isDraggableFight(wanderer) ? {
          draggableFight: wanderer,
          options: {}
        } : {
          draggableFight: wanderer.wanderer,
          options: wanderer
        },
        draggableFight = _ref3.draggableFight,
        options = _ref3.options;
      var _options$drunkSafe = options.drunkSafe,
        drunkSafe = _options$drunkSafe === void 0 ? true : _options$drunkSafe,
        _options$allowEquipme = options.allowEquipment,
        allowEquipment = _options$allowEquipme === void 0 ? false : _options$allowEquipme;
      var newKey = `${kolmafia.myTotalTurnsSpent()};${kolmafia.totalTurnsPlayed()};${get$2("familiarSweat")}`;
      if (this.cacheKey !== newKey) this.clear();
      this.cacheKey = newKey;
      var locationSkipList = allowEquipment ? [] : _toConsumableArray(this.equipment.keys());
      return sober$1() || !drunkSafe ? (_this$targets = this.targets)[_ref4 = `${draggableFight}:${allowEquipment}`] ?? (_this$targets[_ref4] = wanderWhere(this.options, draggableFight, [], locationSkipList)) : {
        location: $location`Drunken Stupor`,
        peridotMonster: $monster.none,
        familiar: $familiar`none`,
        useRefractedGaze: false,
        useFeesh: false
      };
    }
    /**
     * Get choice map for the upcoming wander
     * @param target Description of the wander or location in which to wander
     * @param takeTurnForProfit Should the choices include any that would make a profit from your valueOfAdventure
     * @returns Map of choice numbers to decisions
     */
  }, {
    key: "getChoices",
    value: function getChoices(target) {
      var takeTurnForProfit = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : this.options.takeTurnForProfit;
      var location = target instanceof kolmafia.Location ? target : this.getTarget(target).location;
      var valueOfTurn = takeTurnForProfit ? (this.options.valueOfAdventure ?? 0) + sum(getActiveEffects(), e => this.options.effectValue(e, 1)) : Infinity;
      var baseChoices = this.unsupportedChoices.get(location) ?? {};
      if (!(target instanceof kolmafia.Location) && this.getTarget(target).peridotMonster !== $monster.none) {
        var peridotChoice = getChoiceObject(this.getTarget(target).peridotMonster);
        var newChoices = Object.assign(peridotChoice, baseChoices);
        return undelay(newChoices, this.options, valueOfTurn);
      }
      return undelay(baseChoices, this.options, valueOfTurn);
    }
  }, {
    key: "clear",
    value: function clear() {
      this.targets = {};
    }
  }, {
    key: "getEquipment",
    value: function getEquipment(wanderer) {
      return this.equipment.get(wanderer instanceof kolmafia.Location ? wanderer : this.getTarget(wanderer).location) ?? [];
    }
  }, {
    key: "peridotMonster",
    value: function peridotMonster(wanderer) {
      return this.getTarget(wanderer).peridotMonster;
    }
  }, {
    key: "getFamiliar",
    value: function getFamiliar(wanderer) {
      return this.getTarget(wanderer).familiar;
    }
  }]);
}();

var DEFAULT_VALUE_FUNCTIONS = makeValue();

var AutumnAtonManager = /*#__PURE__*/function () {
  function AutumnAtonManager(_ref) {
    var averageItemValue = _ref.averageItemValue,
      estimatedTurns = _ref.estimatedTurns,
      estimatedTurnsTomorrow = _ref.estimatedTurnsTomorrow;
    _classCallCheck(this, AutumnAtonManager);
    _defineProperty(this, "averageItemValue", DEFAULT_VALUE_FUNCTIONS.averageValue);
    _defineProperty(this, "estimatedTurns", kolmafia.myAdventures);
    _defineProperty(this, "estimatedTurnsTomorrow", () => 0);
    _defineProperty(this, "bestLocation", locations => maxBy(this.bestLocationsByUpgrade(locations), it => this.averageValue(it)));
    if (averageItemValue) this.averageItemValue = averageItemValue;
    if (estimatedTurns) this.estimatedTurns = estimatedTurns;
    if (estimatedTurnsTomorrow) {
      this.estimatedTurnsTomorrow = estimatedTurnsTomorrow;
    }
  }
  return _createClass(AutumnAtonManager, [{
    key: "seasonalItemValue",
    value: function seasonalItemValue(location, seasonalOverride) {
      var _AutumnAton$getUnique;
      // Find the value of the drops based on zone difficulty/type
      var autumnItems = $items`autumn leaf, AutumnFest ale, autumn breeze, autumn dollar, autumn years wisdom`;
      var avgValueOfRandomAutumnItem = this.averageItemValue.apply(this, _toConsumableArray(autumnItems));
      var autumnMeltables = $items`autumn debris shield, autumn leaf pendant, autumn sweater-weather sweater`;
      var autumnItem = (_AutumnAton$getUnique = getUniques(location)) === null || _AutumnAton$getUnique === void 0 ? void 0 : _AutumnAton$getUnique.item;
      var seasonalItemDrops = seasonalOverride ?? seasonalItems();
      if (autumnItem) {
        return (seasonalItemDrops > 1 ? avgValueOfRandomAutumnItem : 0) + (autumnMeltables.includes(autumnItem) ?
        // If we already have the meltable, then we get a random item, else value at 0
        kolmafia.availableAmount(autumnItem) > 0 ? avgValueOfRandomAutumnItem : 0 : this.averageItemValue(autumnItem));
      } else {
        // If we're in a location without any uniques, we still get cowcatcher items
        return seasonalItemDrops > 1 ? avgValueOfRandomAutumnItem : 0;
      }
    }
  }, {
    key: "averageValue",
    value: function averageValue(location, acuityOverride, slotOverride) {
      if (location === $location`Shadow Rift`) {
        kolmafia.setLocation($location`Shadow Rift`);
      } // FIXME This bypasses a mafia bug where ingress is not updated
      var monsters = availableMonsters(location);
      if (monsters.length === 0) {
        return this.seasonalItemValue(location); // We still get seasonal items, even if there are no monsters
      } else {
        var maximumDrops = slotOverride ?? zoneItems();
        var acuityCutoff = 20 - (acuityOverride ?? visualAcuity()) * 5;
        var validDrops = monsters.flatMap(m => kolmafia.itemDropsArray(m)).map(_ref2 => {
          var rate = _ref2.rate,
            type = _ref2.type,
            drop = _ref2.drop;
          return {
            value: !["c", "0", "a"].includes(type) ? this.averageItemValue(drop) : 0,
            preAcuityExpectation: ["c", "0", ""].includes(type) ? 2 * rate / 100 : 0,
            postAcuityExpectation: rate >= acuityCutoff && ["c", "0", ""].includes(type) ? 8 * rate / 100 : 0
          };
        });
        var overallExpectedDropQuantity = sum(validDrops, _ref3 => {
          var preAcuityExpectation = _ref3.preAcuityExpectation,
            postAcuityExpectation = _ref3.postAcuityExpectation;
          return preAcuityExpectation + postAcuityExpectation;
        });
        var expectedCollectionValue = sum(validDrops, _ref4 => {
          var value = _ref4.value,
            preAcuityExpectation = _ref4.preAcuityExpectation,
            postAcuityExpectation = _ref4.postAcuityExpectation;
          // This gives us the adjusted amount to fit within our total amount of available drop slots
          var adjustedDropAmount = (preAcuityExpectation + postAcuityExpectation) * Math.min(1, maximumDrops / overallExpectedDropQuantity);
          return adjustedDropAmount * value;
        });
        return this.seasonalItemValue(location) + expectedCollectionValue;
      }
    }
  }, {
    key: "expectedRemainingExpeditions",
    value: function expectedRemainingExpeditions() {
      var legs$1 = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : legs();
      // Better estimate upgrade value if not ascending
      var availableAutumnatonTurns = this.estimatedTurns() - turnsLeft() + this.estimatedTurnsTomorrow();
      var quests = get$2("_autumnatonQuests");
      var legOffsetFactor = 11 * Math.max(quests - legs$1 - 1, 0);
      return Math.floor(Math.sqrt(quests ** 2 + 2 * (availableAutumnatonTurns - legOffsetFactor) / 11));
    }
  }, {
    key: "profitFromExtraAcuity",
    value: function profitFromExtraAcuity(bestLocationContainingUpgrade, bestLocationWithInstalledUpgrade) {
      return this.averageValue(bestLocationContainingUpgrade) + this.averageValue(bestLocationWithInstalledUpgrade) * Math.max(0, this.expectedRemainingExpeditions() - 1);
    }
  }, {
    key: "profitFromExtraLeg",
    value: function profitFromExtraLeg(bestLocationContainingUpgrade, bestLocationWithInstalledUpgrade) {
      return this.averageValue(bestLocationContainingUpgrade) + this.averageValue(bestLocationWithInstalledUpgrade) * Math.max(0, this.expectedRemainingExpeditions(legs() + 1) - 1);
    }
  }, {
    key: "profitFromExtraArm",
    value: function profitFromExtraArm(bestLocationContainingUpgrade, bestLocationWithInstalledUpgrade) {
      return this.averageValue(bestLocationContainingUpgrade) + this.averageValue(bestLocationWithInstalledUpgrade) * Math.max(0, this.expectedRemainingExpeditions() - 1);
    }
  }, {
    key: "profitFromExtraAutumnItem",
    value: function profitFromExtraAutumnItem(bestLocationContainingUpgrade, bestLocationWithInstalledUpgrade) {
      return this.averageValue(bestLocationContainingUpgrade) + (this.seasonalItemValue(bestLocationWithInstalledUpgrade) + this.averageValue(bestLocationWithInstalledUpgrade)) * Math.max(0, this.expectedRemainingExpeditions() - 1);
    }
  }, {
    key: "makeUpgradeValuator",
    value: function makeUpgradeValuator(fullLocations, currentBestLocation) {
      return upgrade => {
        var upgradeLocations = fullLocations.filter(location => {
          var _AutumnAton$getUnique2;
          return ((_AutumnAton$getUnique2 = getUniques(location)) === null || _AutumnAton$getUnique2 === void 0 ? void 0 : _AutumnAton$getUnique2.upgrade) === upgrade;
        });
        if (!upgradeLocations.length) {
          return {
            upgrade,
            profit: 0
          };
        }
        var bestLocationContainingUpgrade = maxBy(upgradeLocations, l => this.averageValue(l));
        switch (upgrade) {
          case "periscope":
          case "radardish":
            {
              var bestLocationWithInstalledUpgrade = maxBy(fullLocations, loc => this.averageValue(loc, visualAcuity() + 1));
              return {
                upgrade,
                profit: this.profitFromExtraAcuity(bestLocationContainingUpgrade, bestLocationWithInstalledUpgrade)
              };
            }
          case "rightleg1":
          case "leftleg1":
            {
              return {
                upgrade,
                profit: this.profitFromExtraLeg(bestLocationContainingUpgrade, currentBestLocation)
              };
            }
          case "rightarm1":
          case "leftarm1":
            {
              var _bestLocationWithInstalledUpgrade = maxBy(fullLocations, loc => this.averageValue(loc, undefined, zoneItems() + 1));
              return {
                upgrade,
                profit: this.profitFromExtraArm(bestLocationContainingUpgrade, _bestLocationWithInstalledUpgrade)
              };
            }
          case "cowcatcher":
            {
              return {
                upgrade,
                profit: this.profitFromExtraAutumnItem(bestLocationContainingUpgrade, currentBestLocation)
              };
            }
          default:
            {
              return {
                upgrade,
                profit: 0
              };
            }
        }
      };
    }
  }, {
    key: "bestLocationsByUpgrade",
    value: function bestLocationsByUpgrade(fullLocations) {
      var validLocations = fullLocations.filter(l => l.parent !== "Clan Basement" && !AutumnAtonManager.locationBanlist.includes(l));
      // This function shouldn't be getting called if we don't have an expedition left
      if (this.expectedRemainingExpeditions() < 1) {
        return validLocations;
      }
      var currentUpgrades$1 = currentUpgrades();
      var acquirableUpgrades = AutumnAtonManager.profitRelevantUpgrades.filter(upgrade => !currentUpgrades$1.includes(upgrade));
      if (acquirableUpgrades.length === 0) {
        return validLocations;
      }
      var currentBestLocation = maxBy(validLocations, l => this.averageValue(l));
      var currentExpectedProfit = this.averageValue(currentBestLocation) * this.expectedRemainingExpeditions();
      var upgradeValuations = acquirableUpgrades.map(this.makeUpgradeValuator(validLocations, currentBestLocation));
      var _maxBy = maxBy(upgradeValuations, "profit"),
        highestValueUpgrade = _maxBy.upgrade,
        profitFromBestUpgrade = _maxBy.profit;
      if (profitFromBestUpgrade > currentExpectedProfit) {
        var upgradeLocations = validLocations.filter(location => {
          var _AutumnAton$getUnique3;
          return ((_AutumnAton$getUnique3 = getUniques(location)) === null || _AutumnAton$getUnique3 === void 0 ? void 0 : _AutumnAton$getUnique3.upgrade) === highestValueUpgrade;
        });
        return upgradeLocations;
      } else {
        return validLocations;
      }
    }
  }]);
}();
_defineProperty(AutumnAtonManager, "locationBanlist", $locations`The Daily Dungeon`);
// The Daily Dungeon has no native monsters
_defineProperty(AutumnAtonManager, "profitRelevantUpgrades", ["leftarm1", "leftleg1", "rightarm1", "rightleg1", "cowcatcher", "periscope", "radardish"]);

var FILE_PATH = "garbo_item_values.json";
function maybePrint(message) {
  var color = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : undefined;
  {
    kolmafia.print(message, color);
  }
}
function readItemValues() {
  var itemValuesStr = kolmafia.fileToBuffer(FILE_PATH);
  if (itemValuesStr.length > 0) {
    var val = JSON.parse(itemValuesStr);
    var parsedItems = Object.entries(val).map(_ref => {
      var _ref2 = _slicedToArray(_ref, 2),
        itemStr = _ref2[0],
        price = _ref2[1];
      return [kolmafia.toItem(itemStr), price];
    });
    return new Map(parsedItems);
  } else {
    return new Map();
  }
}
function printPriceOverrideWarning() {
  maybePrint("WARNING: You are using garbo item price overrides. This can have unexpected side effects on dieting and adventuring!", "red");
}

var log = [];
function logMessage(message) {
  log.push(message);
}
function printLog(color) {
  var _iterator = _createForOfIteratorHelper(log),
    _step;
  try {
    for (_iterator.s(); !(_step = _iterator.n()).done;) {
      var message = _step.value;
      kolmafia.print(message, color);
    }
  } catch (err) {
    _iterator.e(err);
  } finally {
    _iterator.f();
  }
}

var _valueFunctions;
function garboValueFunctions() {
  if (!_valueFunctions) {
    var itemValues = new Map([[$item`fake hand`, 50_000]]);
    var overrideItemValues = readItemValues();
    if (overrideItemValues.size > 0) {
      printPriceOverrideWarning();
      printLog("GARBO PRICE OVERRIDES");
      overrideItemValues.forEach((value, item) => {
        printLog(`${item}: ${value}`);
        itemValues.set(item, value);
      });
    }
    _valueFunctions = makeValue({
      itemValues
    });
  }
  return _valueFunctions;
}
function garboValue(item) {
  return garboValueFunctions().value(item);
}
function garboAverageValue() {
  var _garboValueFunctions;
  return (_garboValueFunctions = garboValueFunctions()).averageValue.apply(_garboValueFunctions, arguments);
}

var priceCaps = {
  "cuppa Voraci tea": 200000,
  "cuppa Sobrie tea": 200000,
  "potion of the field gar": 50000,
  "Special Seasoning": 20000,
  "mini kiwi aioli": 20000,
  "whet stone": 20000,
  "spice melange": 500000,
  "mojo filter": 20000,
  "Ol' Scratch's salad fork": 200000,
  "Frosty's frosty mug": 200000,
  "sweet tooth": 250000
};
function acquire(qty, item, maxPrice) {
  var throwOnFail = arguments.length > 3 && arguments[3] !== undefined ? arguments[3] : true;
  var maxAggregateCost = arguments.length > 4 ? arguments[4] : undefined;
  var tryRetrievingUntradeable = arguments.length > 5 && arguments[5] !== undefined ? arguments[5] : false;
  if (maxPrice === undefined) maxPrice = priceCaps[item.name];
  if (!item.tradeable && !tryRetrievingUntradeable || maxPrice !== undefined && maxPrice <= 0) {
    return 0;
  }
  if (maxPrice === undefined) throw new Error(`No price cap for ${item.name}.`);
  kolmafia.print(`Trying to acquire ${qty} ${item.plural}; max price ${maxPrice.toFixed(0)}.`, "green");
  if (qty * kolmafia.mallPrice(item) > (maxAggregateCost ?? 3000000)) {
    throw new Error("Aggregate cost too high! Probably a bug.");
  }
  var startAmount = kolmafia.itemAmount(item);
  var remaining = qty - startAmount;
  if (remaining <= 0) return qty;
  var logError = (target, source) => {
    throw new Error(`Failed to remove ${target} from ${source}`);
  };
  if (get$2("autoSatisfyWithCloset")) {
    var getCloset = Math.min(remaining, kolmafia.closetAmount(item));
    if (!kolmafia.takeCloset(getCloset, item) && throwOnFail) logError(item, "closet");
    remaining -= getCloset;
    if (remaining <= 0) return qty;
  }
  var getStorage = Math.min(remaining, kolmafia.storageAmount(item));
  if (!kolmafia.takeStorage(getStorage, item) && throwOnFail) logError(item, "storage");
  remaining -= getStorage;
  if (remaining <= 0) return qty;
  var getMall = Math.min(remaining, kolmafia.shopAmount(item));
  if (!kolmafia.takeShop(getMall, item)) {
    kolmafia.cliExecute("refresh shop");
    kolmafia.cliExecute("refresh inventory");
    remaining = qty - kolmafia.itemAmount(item);
    getMall = Math.min(remaining, kolmafia.shopAmount(item));
    if (!kolmafia.takeShop(getMall, item) && throwOnFail) logError(item, "shop");
  }
  remaining -= getMall;
  var coinmaster = kolmafia.Coinmaster.all().find(cm => kolmafia.sellsItem(cm, item));
  var coinmasterPrice = coinmaster ? garboValue(coinmaster.item) * kolmafia.sellPrice(coinmaster, item) : 0;
  if (coinmaster && coinmasterPrice > kolmafia.mallPrice(item)) {
    kolmafia.buy(item, remaining, maxPrice);
  } else {
    withProperty("autoBuyPriceLimit", maxPrice, () => kolmafia.retrieveItem(item, qty));
  }
  if (kolmafia.itemAmount(item) < qty && throwOnFail) {
    throw new Error(`Failed to purchase sufficient quantities of ${item} from the mall.`);
  }
  return kolmafia.itemAmount(item) - startAmount;
}

var GarboStrategy = /*#__PURE__*/function (_CombatStrategy) {
  function GarboStrategy(macro) {
    var _this;
    var postAuto = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : macro;
    var useAutoAttack = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : () => true;
    var options = arguments.length > 3 && arguments[3] !== undefined ? arguments[3] : {};
    _classCallCheck(this, GarboStrategy);
    _this = _callSuper(this, GarboStrategy);
    var macroCustom = context => customizeMacro(macro(context), options);
    if (useAutoAttack()) {
      var postAutoCustom = context => customizeMacro(postAuto(context), options);
      _this.autoattack(macroCustom).macro(postAutoCustom);
    } else {
      _this.macro(macroCustom);
    }
    return _this;
  }
  _inherits(GarboStrategy, _CombatStrategy);
  return _createClass(GarboStrategy);
}(CombatStrategy);

var CLARA_TARGETS = ["volcoino", "yachtzee", "gerald/ine", "shadow waters"];
var fishyTurns = () => Math.max(kolmafia.haveEffect($effect`Fishy`) - kolmafia.myAdventures(), 0) + (have$P($item`fishy pipe`) && !get$2("_fishyPipeUsed") ? 10 : 0) + (get$2("skateParkStatus") === "ice" && !get$2("_skateBuff1") ? 30 : 0);
function canYachtzee() {
  return fishyTurns() > 0 && realmAvailable("sleaze") && get$2("valueOfAdventure") < 20_000; // Can we check for "value of doing the taffy copier"?
}
var _claraIsVolcoino = false;
var claimClaraVolcoino = () => _claraIsVolcoino = true;
var claraTarget = () => _claraIsVolcoino ? "volcoino" : canYachtzee() ? "yachtzee" : ["food", "booze"].includes(get$2("_questPartyFairQuest")) ? "gerald/ine" : "shadow waters";
var shouldClara = target => have$P($item`Clara's bell`) && !get$2("_claraBellUsed") && CLARA_TARGETS.indexOf(claraTarget()) >= CLARA_TARGETS.indexOf(target);
var nonCinchNCs = () => shouldClara("yachtzee") ? 1 : 0 + (have$P($item`Apriling band tuba`) ? $item`Apriling band tuba`.dailyusesleft : 0);
var cinchNCs = () => Math.min(Math.floor(totalAvailableCinch() / 60), Math.max(fishyTurns() - nonCinchNCs(), 0));
var maximumYachtzees = () => clamp(nonCinchNCs() + cinchNCs(), 0, fishyTurns());
var willYachtzee = () => canYachtzee() && maximumYachtzees() > 0;
function cinchYachtzeeProfitable() {
  // A yachtzee costs a turn and gives us 20k meat for 60 cinch, projectile pinata costs 5 cinch and gets us 3 feliz candies
  return 20000 - get$2("valueOfAdventure") > 12 * 3 * felizValue();
}
function maximumPinataCasts() {
  return canYachtzee() && cinchYachtzeeProfitable() // If we're doing Yachtzee at end of day, only use up our excess Cincho on candy
  ? Math.max(0, Math.floor((totalAvailableCinch() - cinchNCs() * 60) / 5)) : 30;
}

function volcanoQuestItemCost(_ref) {
  var quantity = _ref.quantity,
    item = _ref.item;
  if (item === $item`fused fuse`) {
    // Check if clara's bell is available and unused
    if (!have$P($item`Clara's bell`) || get$2("_claraBellUsed")) return Infinity;
    if (willYachtzee()) {
      return garboValue($item`Volcoino`) - (20000 - get$2("valueOfAdventure"));
    }
    return quantity * get$2("valueOfAdventure");
  }
  if (!item.tradeable) return Infinity;
  return quantity * kolmafia.retrievePrice(item);
}
function checkVolcanoQuest() {
  kolmafia.print("Checking volcano quest", HIGHLIGHT);
  kolmafia.visitUrl("place.php?whichplace=airport_hot&action=airport4_questhub");
  var volcoinoValue = garboValue($item`Volcoino`);
  var bestItem = maxBy([{
    item: getItem("_volcanoItem1") ?? $item.none,
    quantity: get$2("_volcanoItemCount1"),
    choice: 1
  }, {
    item: getItem("_volcanoItem2") ?? $item.none,
    quantity: get$2("_volcanoItemCount2"),
    choice: 2
  }, {
    item: getItem("_volcanoItem3") ?? $item.none,
    quantity: get$2("_volcanoItemCount3"),
    choice: 3
  }], volcanoQuestItemCost, true);
  if (bestItem.item === $item`fused fuse`) {
    claimClaraVolcoino();
  } else if (volcanoQuestItemCost(bestItem) < volcoinoValue) {
    withProperty("autoBuyPriceLimit", volcoinoValue, () => kolmafia.retrieveItem(bestItem.item, bestItem.quantity));
    kolmafia.visitUrl("place.php?whichplace=airport_hot&action=airport4_questhub");
    kolmafia.runChoice(bestItem.choice);
  }
}
var _attemptCompletingBarfQuest = true;
function attemptCompletingBarfQuest() {
  return _attemptCompletingBarfQuest;
}
function completeBarfQuest() {
  if (!_attemptCompletingBarfQuest) return;
  if (get$2("questEStGiveMeFuel") === "started") {
    var globuleCosts = kolmafia.retrievePrice($item`toxic globule`, 20);
    if (globuleCosts < 3 * garboValue($item`FunFunds™`)) {
      kolmafia.print(`The cost of 20 toxic globules (${globuleCosts}) is less than the profits expected from 3 FunFunds™ (${3 * garboValue($item`FunFunds™`)}). Proceeding to acquire toxic globules.`, "green");
      _attemptCompletingBarfQuest = acquire(20, $item`toxic globule`, 1.5 * globuleCosts / 20, false) >= 20;
    } else {
      _attemptCompletingBarfQuest = false;
      kolmafia.print(`The cost of 20 toxic globules (${globuleCosts}) exceeds the profits expected from 3 FunFunds™ (${3 * garboValue($item`FunFunds™`)}). Consider farming some globules yourself.`, "red");
    }
  }
  if (get$2("questEStSuperLuber") === "step2" || get$2("questEStGiveMeFuel") === "step1") {
    kolmafia.print("Completing Barf Quest", "blue");
    kolmafia.visitUrl("place.php?whichplace=airport_stench&action=airport3_kiosk");
    kolmafia.visitUrl("choice.php?whichchoice=1066&pwd&option=3");
  }
  return;
}
function checkBarfQuest() {
  var _page$match, _page$match2;
  var page = kolmafia.visitUrl("place.php?whichplace=airport_stench&action=airport3_kiosk");

  // If we are on an assignment, try completing and then return after
  if (page.includes("Current Assignment")) {
    return completeBarfQuest();
  }

  // If there are no available nor current assignments, then we are done for the day
  if (!page.includes("Available Assignments")) {
    // Reset prefs to unstarted just in case (since they do not automatically reset on rollover)
    _set("questEStSuperLuber", "unstarted");
    _set("questEStGiveMeFuel", "unstarted");
    return;
  }
  var targets = globalOptions.nobarf ? ["Electrical Maintenance"] : ["Track Maintenance", "Electrical Maintenance"]; // In decreasing order of priority

  // Page includes Track/Electrical Maintenance and we aren't on an assignment -> choose assignment
  var quests = [((_page$match = page.match("(width=250>)(.*?)(value=1>)")) === null || _page$match === void 0 || (_page$match = _page$match[2]) === null || _page$match === void 0 || (_page$match = _page$match.match("(<b>)(.*?)(</b>)")) === null || _page$match === void 0 ? void 0 : _page$match[2]) ?? "", ((_page$match2 = page.match("(value=1>)(.*?)(value=2>)")) === null || _page$match2 === void 0 || (_page$match2 = _page$match2[2]) === null || _page$match2 === void 0 || (_page$match2 = _page$match2.match("(<b>)(.*?)(</b>)")) === null || _page$match2 === void 0 ? void 0 : _page$match2[2]) ?? ""];
  kolmafia.print("Barf Quests Available:", "blue");
  quests.forEach(quest => kolmafia.print(quest, "blue"));

  // If page does not include Track/Electrical Maintenance quest, return
  if (!targets.some(target => page.includes(target))) {
    kolmafia.print("No suitable Barf Quests available.", "red");
    return;
  }
  for (var _i = 0, _targets = targets; _i < _targets.length; _i++) {
    var target = _targets[_i];
    var _iterator = _createForOfIteratorHelper(quests.entries()),
      _step;
    try {
      for (_iterator.s(); !(_step = _iterator.n()).done;) {
        var _step$value = _slicedToArray(_step.value, 2),
          idx = _step$value[0],
          qst = _step$value[1];
        if (target === qst) {
          kolmafia.print(`Accepting Barf Quest: ${qst}`, "blue");
          kolmafia.visitUrl(`choice.php?whichchoice=1066&pwd&option=${idx + 1}`);
          return completeBarfQuest();
        }
      }
    } catch (err) {
      _iterator.e(err);
    } finally {
      _iterator.f();
    }
  }
  return;
}

function redTaffyWorth() {
  var averageRedTaffyValue = sum(_toConsumableArray(RED_TAFFY_DROP_WEIGHTS.entries()), _ref => {
    var _ref2 = _slicedToArray(_ref, 2),
      item = _ref2[0],
      weight = _ref2[1];
    return garboValue(item) * weight;
  });
  return kolmafia.mallPrice($item`pulled red taffy`) < averageRedTaffyValue;
}
var olfactionCopies = have$P($skill`Transcendent Olfaction`) ? 3 : 0;
var gallapagosCopies = have$P($skill`Gallapagosian Mating Call`) ? 1 : 0;
var garbageTourists = 1 + olfactionCopies + gallapagosCopies,
  touristFamilies = 1,
  angryTourists = 1;
var barfTourists = garbageTourists + touristFamilies + angryTourists;
var garbageTouristRatio = garbageTourists / barfTourists;
var touristFamilyRatio = touristFamilies / barfTourists;
// 30 tourists till NC, with families counting as 3
// Estimate number of turns till the counter hits 27
// then estimate the expected number of turns required to hit a counter of >= 30

var DEFAULT_OPTIONS = {
  bonusEffects: [],
  bonusModifiers: [],
  banishMonsters: [],
  ncTurns: Infinity,
  post: () => {},
  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  outfit: _context => ({})
};

// eslint-disable-next-line @typescript-eslint/no-empty-object-type, @typescript-eslint/no-unsafe-declaration-merging
// eslint-disable-next-line @typescript-eslint/no-unsafe-declaration-merging
var FarmingStrategySkeleton = /*#__PURE__*/function () {
  function FarmingStrategySkeleton() {
    _classCallCheck(this, FarmingStrategySkeleton);
  }
  return _createClass(FarmingStrategySkeleton, [{
    key: "isUnderwater",
    value: function isUnderwater() {
      return this.location.environment === "underwater";
    }
  }, {
    key: "accountForNC",
    value: function accountForNC() {
      return this.ncTurns !== Infinity;
    }
  }, {
    key: "olfactMonster",
    value: function olfactMonster() {
      return this.shouldOlfact ? undelay(this.targetMonster) : null;
    }
  }, {
    key: "monsters",
    value: function monsters() {
      return kolmafia.getMonsters(this.location);
    }
  }, {
    key: "turnsToNC",
    value: function turnsToNC() {
      return undelay(this.ncTurns);
    }
  }, {
    key: "ncAdjustment",
    value: function ncAdjustment() {
      if (!this.accountForNC()) return 1;
      return this.turnsToNC() / (1 + this.turnsToNC());
    }
  }, {
    key: "itemDropValue",
    value: function itemDropValue() {
      return sum(_toConsumableArray(adventureTargetToWeightedMap(this.location).entries()), _ref3 => {
        var _ref4 = _slicedToArray(_ref3, 2),
          monster = _ref4[0],
          monsterWeight = _ref4[1];
        return monsterWeight * sum(kolmafia.itemDropsArray(monster), _ref5 => {
          var drop = _ref5.drop,
            rate = _ref5.rate;
          return (
            // One 100 because % the other because % improvement
            rate / 100 * garboValue(drop)
          );
        });
      }) / 100;
    }
  }, {
    key: "valuableModifiers",
    value: function valuableModifiers() {
      return ["Meat Drop", "Familiar Weight", "Smithsness", "Item Drop"].concat(_toConsumableArray(this.bonusModifiers), _toConsumableArray(this.isUnderwater() ? ["Hidden Familiar Weight", "Meat Drop Penalty"] : []));
    }
  }, {
    key: "monstersToBanish",
    value: function monstersToBanish() {
      return this.banishMonsters.filter(m => !kolmafia.isBanished(m));
    }
  }]);
}();
var FarmingStrategy = new Proxy(new FarmingStrategySkeleton(), {
  get: (target, prop, receiver) => {
    if (Object.prototype.hasOwnProperty.call(FarmingStrategySkeleton.prototype, prop)) {
      // eslint-disable-next-line @typescript-eslint/no-explicit-any
      var method = FarmingStrategySkeleton.prototype[prop];
      method.bind(receiver);
    }
    var strategyOptions = currentStrategy();
    var stringProp = String(prop);
    if (stringProp in strategyOptions) {
      return strategyOptions[stringProp];
    }
    if (stringProp in DEFAULT_OPTIONS) {
      return DEFAULT_OPTIONS[stringProp];
    }
    // Fallback to standard target resolution
    return Reflect.get(target, prop, receiver);
  }
});
var BARF_MOUNTAIN = {
  stasisRounds: 20,
  asdonEffect: $effect`Driving Observantly`,
  ensureBarfAccess: true,
  baseMeat: 250,
  ncTurns: () => 27 * barfTourists / (garbageTourists + angryTourists + 3 * touristFamilies) + 1 * touristFamilyRatio + 2 * (1 - touristFamilyRatio) * touristFamilyRatio + 3 * (1 - touristFamilyRatio) * (1 - touristFamilyRatio),
  location: $location`Barf Mountain`,
  bonusEffects: $effects`How to Scam Tourists`,
  targetMonster: () => have$P($familiar`Skeleton of Crimbo Past`) && get$2("_knuckleboneDrops", 0) < 100 ? $monster`angry tourist` : $monster`garbage tourist`,
  shouldOlfact: true,
  outfit: () => ({
    equip: get$2("dinseyRollercoasterNext") && have$P($item`lube-shoes`) ? $items`lube-shoes` : []
  }),
  combat: new GarboStrategy(() => Macro.meatKill(), () => Macro.if_(`(monsterid ${globalOptions.target.id}) && !gotjump && !(pastround 2)`, Macro.meatKill()).abort()),
  post: completeBarfQuest
};
var THE_CORAL_CORRAL = {
  stasisRounds: 5,
  asdonEffect: $effect`Driving Waterproofly`,
  ensureBarfAccess: false,
  baseMeat: 300,
  location: $location`The Coral Corral`,
  banishMonsters: $monsters`Mer-kin rustler, sea cowboy`,
  targetMonster: $monster`sea cow`,
  shouldOlfact: false,
  outfit: _ref6 => {
    var banish = _ref6.banish;
    var banishItem = banish === null || banish === void 0 ? void 0 : banish.equip;
    if (banishItem) {
      kolmafia.print(`Planning to banish equipping ${banishItem === null || banishItem === void 0 ? void 0 : banishItem.name}`);
    }
    return banishItem ? {
      equip: [banishItem]
    } : {};
  },
  combat: new GarboStrategy(_ref7 => {
    var banish = _ref7.banish;
    var delevel = kolmafia.myBuffedstat($stat`Moxie`) < $monster`sea cow`.baseAttack + 10 || have$P($skill`Hero of the Half-Shell`) && kolmafia.itemType(kolmafia.equippedItem($slot`offhand`)) === "shield" && kolmafia.myBuffedstat($stat`Muscle`) < $monster`sea cow`.baseAttack + 10;
    var macro = new Macro().externalIf(delevel, Macro.delevel()).externalIf(redTaffyWorth(), Macro.tryItem($item`pulled red taffy`)).meatKill(false);
    if (banish) {
      Macro.if_($monsters`Mer-kin rustler, sea cowboy`, banish.macro).step(macro);
    }
    return macro;
  })
};
function currentStrategy() {
  switch (globalOptions.prefs.farmingMethod) {
    case FarmingMethod.THE_CORAL_CORRAL:
      return THE_CORAL_CORRAL;
    case FarmingMethod.BARF_MOUNTAIN:
    default:
      return BARF_MOUNTAIN;
  }
}

var eventLog = {
  initialCopyTargetsFought: 0,
  digitizedCopyTargetsFought: 0,
  copyTargetSources: [],
  yachtzees: 0
};
var BonusEquipMode = /*#__PURE__*/function (BonusEquipMode) {
  BonusEquipMode[BonusEquipMode["FREE"] = 0] = "FREE";
  BonusEquipMode[BonusEquipMode["MEAT_TARGET"] = 1] = "MEAT_TARGET";
  BonusEquipMode[BonusEquipMode["DMT"] = 2] = "DMT";
  BonusEquipMode[BonusEquipMode["BARF"] = 3] = "BARF";
  return BonusEquipMode;
}({});
function modeIsFree(mode) {
  return [BonusEquipMode.FREE, BonusEquipMode.DMT].includes(mode);
}
function modeUseLimitedDrops(mode) {
  return [BonusEquipMode.BARF, BonusEquipMode.FREE].includes(mode);
}
function modeValueOfMeat(mode) {
  return modeIsFree(mode) ? 0 : (baseMeat() + (mode === BonusEquipMode.MEAT_TARGET ? targetMeatDifferential() : 0)) / 100;
}
function modeValueOfItem(mode) {
  return mode === BonusEquipMode.BARF ? FarmingStrategy.itemDropValue() : 0;
}
var WISH_VALUE = 50000;
var HIGHLIGHT = kolmafia.isDarkMode() ? "yellow" : "blue";
var ESTIMATED_OVERDRUNK_TURNS = 50;
var MEAT_TARGET_MULTIPLIER = () => globalOptions.prefs.meatTargetMultiplier;
var propertyManager = new PropertiesManager();
var songboomMeat = () => have$C() && (songChangesLeft() > 0 || song() === "Total Eclipse of Your Meat" && kolmafia.myInebriety() <= kolmafia.inebrietyLimit()) ? 25 : 0;

// all tourists have a basemeat of 250
var baseMeat = () => FarmingStrategy.baseMeat + songboomMeat();
var targetMeat = () => kolmafia.meatDrop(globalOptions.target) + songboomMeat();
var basePointerRingMeat = () => 500;
var targetPointerRingMeat = () => {
  if (globalOptions.target.attributes.includes("FREE")) return 0;
  var meat = targetMeat();
  if (meat >= 500) {
    return 700;
  } else if (meat >= 100) {
    return 500;
  } else if (meat >= 1) {
    return 300;
  }
  return 50;
};
var targetMeatDifferential = () => {
  var baseMeatVal = baseMeat();
  var targetMeatVal = targetMeat();
  return clamp(targetMeatVal - baseMeatVal, 0, targetMeatVal);
};
var targetingMeat = () => !isFree(globalOptions.target) && targetMeat() > baseMeat();
var targetingItems = () => !targetingMeat();
var gooseDroneEligible = () => targetingItems() && kolmafia.itemDropsArray(globalOptions.target).filter(item => !["c", "0", "p", "a"].includes(item.type)).length === 1 && have$P($familiar`Grey Goose`);
function averageTargetNet() {
  return targetingItems() ? valueDrops(globalOptions.target) : targetMeat() * kolmafia.meatDropModifier() / 100;
}
function averageTouristNet() {
  return baseMeat() * kolmafia.meatDropModifier() / 100;
}
function expectedTargetProfit() {
  return isFreeAndCopyable(globalOptions.target) ? averageTargetNet() : averageTargetNet() - averageTouristNet();
}
function safeInterrupt() {
  if (globalOptions.prefs.rolloverBuffer * 60 * 1000 > kolmafia.rollover() * 1000 - Date.now()) {
    throw new Error(`Eep! It's a mere ${Math.round(kolmafia.rollover() - Date.now() / 1000)} seconds until rollover!`);
  }
  if (get$2("garbo_interrupt", false)) {
    _set("garbo_interrupt", false);
    throw new Error("User interrupt requested. Stopping Garbage Collector.");
  }
}
function resetDailyPreference(trackingPreference) {
  var today = kolmafia.todayToString();
  if (getString(trackingPreference) !== today) {
    _set(trackingPreference, today);
    return true;
  } else {
    return false;
  }
}
function setChoice(adventure, value) {
  propertyManager.setChoices({
    [adventure]: value
  });
}

/**
 * Shuffle a copy of {array}.
 * @param array Array to shuffle.
 */
function shuffle(array) {
  var shuffledArray = _toConsumableArray(array);
  for (var i = shuffledArray.length - 1; i > 0; i--) {
    var j = Math.floor(Math.random() * (i + 1));
    var temp = shuffledArray[i];
    shuffledArray[i] = shuffledArray[j];
    shuffledArray[j] = temp;
  }
  return shuffledArray;
}
function mapMonster(location, monster) {
  if (kolmafia.haveSkill($skill`Map the Monsters`) && !get$2("mappingMonsters") && get$2("_monstersMapped") < 3) {
    kolmafia.useSkill($skill`Map the Monsters`);
  }
  if (!get$2("mappingMonsters")) throw "Failed to setup Map the Monsters.";
  var myTurns = kolmafia.myTurncount();
  var mapPage = "";
  // Handle zone intros and holiday wanderers
  for (var tries = 0; tries < 10; tries++) {
    mapPage = kolmafia.visitUrl(kolmafia.toUrl(location), false, true);
    if (mapPage.includes("Leading Yourself Right to Them")) break;
    // Time-pranks can show up here, annoyingly
    if (mapPage.includes("<!-- MONSTERID: 1965 -->") || mapPage.includes("<!-- MONSTERID: 1622  -->")) {
      kolmafia.runCombat(Macro.attack().repeat().toString());
    }
    if (kolmafia.handlingChoice()) kolmafia.runChoice(-1);
    if (kolmafia.myTurncount() > myTurns + 1) throw `Map the monsters unsuccessful?`;
    if (tries === 9) throw `Stuck trying to Map the monsters.`;
  }
  var fightPage = kolmafia.visitUrl(`choice.php?pwd&whichchoice=1435&option=1&heyscriptswhatsupwinkwink=${monster.id}`);
  if (!fightPage.includes(monster.name)) {
    throw "Something went wrong starting the fight.";
  }
  if (kolmafia.choiceFollowsFight()) kolmafia.runChoice(-1);
}

/**
 * Returns true if the arguments have all elements equal.
 * @param array1 First array.
 * @param array2 Second array.
 */
function arrayEquals(array1, array2) {
  return array1.length === array2.length && array1.every((element, index) => element === array2[index]);
}
function questStep(questName) {
  var stringStep = getString(questName);
  if (stringStep === "unstarted" || stringStep === "") return -1;else if (stringStep === "started") return 0;else if (stringStep === "finished") return 999;else {
    if (stringStep.substring(0, 4) !== "step") {
      throw "Quest state parsing error.";
    }
    return parseInt(stringStep.substring(4), 10);
  }
}
function tryFeast(familiar) {
  if (have$P(familiar)) {
    kolmafia.useFamiliar(familiar);
    kolmafia.use($item`moveable feast`);
  }
}
function tryFindFreeRunOrBanish(constraints) {
  return tryFindFreeRun(constraints) ?? tryFindBanish(constraints);
}
var ltbRestraints = {
  requireUnlimited: () => true,
  noFamiliar: () => true,
  noRequirements: () => true,
  maximumCost: () => get$2("autoBuyPriceLimit")
};
function ltbRun() {
  return tryFindFreeRunOrBanish(ltbRestraints) ?? ensureFreeRun(ltbRestraints);
}
function kramcoGuaranteed() {
  return have$P($item`Kramco Sausage-o-Matic™`) && getKramcoWandererChance() >= 1;
}

/**
 * Determines the opportunity cost of not using the Pillkeeper to fight an embezzler
 * @returns The expected value of using a pillkeeper charge to fight an embezzler
 */
function pillkeeperOpportunityCost() {
  var canTreasury = kolmafia.canAdventure($location`Cobb's Knob Treasury`);
  var alternateUses = [{
    can: canTreasury,
    value: MEAT_TARGET_MULTIPLIER() * get$2("valueOfAdventure")
  }, {
    can: realmAvailable("sleaze"),
    value: 40000
  }].filter(x => x.can);
  var alternateUse = alternateUses.length ? maxBy(alternateUses, "value") : undefined;
  var alternateUseValue = alternateUse === null || alternateUse === void 0 ? void 0 : alternateUse.value;
  if (!alternateUseValue) return 0;
  if (!canTreasury) return alternateUseValue;
  var canStartChain = [have$t() && kolmafia.getLocketMonsters()[globalOptions.target.name], have$J() && paintingMonster() === globalOptions.target && !paintingFought(), have$P($item`Clan VIP Lounge key`) && !get$2("_photocopyUsed")].some(x => x);
  return canStartChain ? alternateUseValue : WISH_VALUE;
}

/**
 * Burns existing MP on the mall-optimal libram skill until unable to cast any more.
 */
function burnLibrams() {
  var mpTarget = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : 0;
  var libramToCast = bestLibramToCast();
  while (libramToCast && kolmafia.mpCost(libramToCast) <= kolmafia.myMp() - mpTarget) {
    kolmafia.useSkill(libramToCast);
    libramToCast = bestLibramToCast();
  }
  if (mpTarget > 0) {
    kolmafia.cliExecute(`burn -${mpTarget}`);
  } else {
    kolmafia.cliExecute("burn *");
  }
}
function howManySausagesCouldIEat() {
  if (!have$P($item`Kramco Sausage-o-Matic™`)) return 0;
  // You may be full but you can't be overfull
  if (kolmafia.myFullness() > kolmafia.fullnessLimit()) return 0;
  return clamp(23 - get$2("_sausagesEaten"), 0, kolmafia.itemAmount($item`magical sausage`) + kolmafia.itemAmount($item`magical sausage casing`));
}
function safeRestoreMpTarget() {
  //  If our max MP is close to 200, we could be restoring every turn even if we don't need to, avoid that case.
  if (Math.abs(kolmafia.myMaxmp() - 200) < 40) {
    return Math.min(kolmafia.myMaxmp(), 100);
  }
  return Math.min(kolmafia.myMaxmp(), 200);
}
var _ignoreBeatenUp = false;
var unignoreBeatenUp = () => _ignoreBeatenUp = false;
function safeRestore() {
  if (get$2("_lastCombatLost") && kolmafia.lastMonster() !== $monster`Sssshhsssblllrrggghsssssggggrrgglsssshhssslblgl`) {
    _set("_lastCombatLost", "false");
    throw new Error("You lost your most recent combat! Check to make sure everything is alright before rerunning.");
  }
  if (have$P($effect`Beaten Up`) && !_ignoreBeatenUp) {
    if (kolmafia.lastMonster() === $monster`Sssshhsssblllrrggghsssssggggrrgglsssshhssslblgl`) {
      uneffect($effect`Beaten Up`);
    } else {
      throw new Error("Hey, you're beaten up, and that's a bad thing. Lick your wounds, handle your problems, and run me again when you feel ready.");
    }
  }
  var lowPercentageHealth = FarmingStrategy.isUnderwater() ? kolmafia.myInebriety() > kolmafia.inebrietyLimit() ? 0.9 : 0.6 : 0.5;
  if (kolmafia.myHp() < Math.min(kolmafia.myMaxhp() * lowPercentageHealth, get$2("garbo_restoreHpTarget", 2000))) {
    kolmafia.restoreHp(Math.min(kolmafia.myMaxhp() * 0.9, get$2("garbo_restoreHpTarget", 2000)));
  }
  var mpTarget = safeRestoreMpTarget();
  var shouldRestoreMp = () => kolmafia.myMp() < mpTarget;
  if (shouldRestoreMp() && howManySausagesCouldIEat() > 0) {
    kolmafia.eat($item`magical sausage`);
  }
  var soulFoodCasts = Math.floor(kolmafia.mySoulsauce() / kolmafia.soulsauceCost($skill`Soul Food`));
  if (shouldRestoreMp() && soulFoodCasts > 0) {
    kolmafia.useSkill(soulFoodCasts, $skill`Soul Food`);
  }
  if (shouldRestoreMp()) kolmafia.restoreMp(mpTarget);
  burnLibrams(mpTarget * 2); // Leave a mp buffer when burning
}

/**
 * Compares the local version of Garbo against the most recent release branch, printing results to the CLI
 */
function checkGithubVersion() {
  {
    var localSHA = kolmafia.gitInfo("loathers-garbage-collector-release").commit || kolmafia.gitInfo("Loathing-Associates-Scripting-Society-garbage-collector-release").commit;
    var gitData = kolmafia.visitUrl(`https://api.github.com/repos/${"loathers/garbage-collector"}/branches`);
    if (!gitData) kolmafia.print("Failed to reach github!");else {
      var _gitBranches$find;
      // Query GitHub for latest release commit
      var gitBranches = JSON.parse(gitData);
      var releaseSHA = (_gitBranches$find = gitBranches.find(branchInfo => branchInfo.name === "release")) === null || _gitBranches$find === void 0 || (_gitBranches$find = _gitBranches$find.commit) === null || _gitBranches$find === void 0 ? void 0 : _gitBranches$find.sha;
      kolmafia.print(`Local Version: ${localSHA} (built from ${"main"}@${"655551e56350e94c4c0f72b9da7a9cb27b86b51e"})`);
      if (releaseSHA === localSHA) {
        kolmafia.print("Garbo is up to date!", HIGHLIGHT);
      } else if (releaseSHA === undefined) {
        kolmafia.print("Garbo may be out of date, unable to query GitHub for latest version. Maybe run 'git update'?", HIGHLIGHT);
      } else {
        kolmafia.print(`Release Version: ${releaseSHA}`);
        kolmafia.print("Garbo is out of date. Please run 'git update'!", "red");
      }
    }
  }
}
function formatNumber(num) {
  return num.toString().replace(/(\d)(?=(\d{3})+(?!\d))/g, "$1,");
}

/**
 * Confirmation dialog that supports automatic resolution via garbo_autoUserConfirm preference
 * @param msg string to display in confirmation dialog
 * @param defaultValue default answer if user doesn't provide one
 * @param timeOut time to show dialog before submitting default value
 * @returns answer to confirmation dialog
 */
function userConfirmDialog(msg, defaultValue, timeOut) {
  if (globalOptions.prefs.autoUserConfirm) {
    kolmafia.print(`Automatically selected ${defaultValue} for ${msg}`, "red");
    return defaultValue;
  }
  if (timeOut) return kolmafia.userConfirm(msg, timeOut, defaultValue);
  return kolmafia.userConfirm(msg);
}
function determineFreeBunnyBanish() {
  var extraOrbFights = have$P($item`miniature crystal ball`) ? 1 : 0;
  var possibleGregsFromSpleen = Math.floor((kolmafia.spleenLimit() - kolmafia.mySpleenUse()) / 2) * (3 + extraOrbFights);
  var currentAvailableGregs = Math.max(0, get$2("beGregariousCharges")) * (3 + extraOrbFights);
  var habitatFights = (3 - clamp(get$2("_monsterHabitatsRecalled"), 0, 3)) * (5 + extraOrbFights);
  var expectedPocketProfFights = !have$P($familiar`Pocket Professor`) ? 0 : (!get$2("_garbo_meatChain", false) ? Math.max(10 - get$2("_pocketProfessorLectures"), 0) : 0) + (!get$2("_garbo_weightChain", false) ? Math.min(15 - get$2("_pocketProfessorLectures"), 5) : 0);
  var expectedDigitizesDuringGregs = have$I() && get$2("_sourceTerminalDigitizeUses") < 3 ? 3 : 0; // To encounter 3 digitize monsters it takes 91 adventures. Just estimate we fight all 3 to be safe.
  var expectedReplacerFights = (have$P($skill`Meteor Lore`) ? 10 - get$2("_macrometeoriteUses") : 0) + (have$P($item`Powerful Glove`) ? Math.floor((100 - get$2("_powerfulGloveBatteryPowerUsed")) / 10) : 0);
  var useFreeBanishes = getBanishedMonsters().get($item`ice house`) !== $monster`fluffy bunny` &&
  // 60 turns of banish from mafia middle finger ring, and 30 x 2 from two snokebombs
  // Account for our chain-starting fight as well as other embezzler sources that occur during our greg chain
  1 + possibleGregsFromSpleen + currentAvailableGregs + habitatFights + expectedPocketProfFights + expectedDigitizesDuringGregs + expectedReplacerFights < 120 && habitatFights + currentAvailableGregs + possibleGregsFromSpleen > 0 && have$P($item`mafia middle finger ring`) && !get$2("_mafiaMiddleFingerRingUsed") && have$P($skill`Snokebomb`) && get$2(`_snokebombUsed`) <= 1;
  return useFreeBanishes;
}
var usingFreeBunnyBanish;
function getUsingFreeBunnyBanish() {
  if (usingFreeBunnyBanish === undefined) {
    usingFreeBunnyBanish = determineFreeBunnyBanish();
  }
  return usingFreeBunnyBanish;
}
var reservedBanishes = new Map([[$skill`Snokebomb`, () => get$2(`_snokebombUsed`) > 0],
// We intend to save at least 2 uses for embezzlers, so if we've already used one, disallow usage.
[$item`mafia middle finger ring`, () => true]]);
function freeRunConstraints(spec) {
  return {
    allowedAction: action => {
      var _action$constraints$e, _action$constraints;
      var initialActionOutfit = Outfit.from(((_action$constraints$e = (_action$constraints = action.constraints).equipmentRequirements) === null || _action$constraints$e === void 0 ? void 0 : _action$constraints$e.call(_action$constraints)) ?? {});
      if (!(initialActionOutfit !== null && initialActionOutfit !== void 0 && initialActionOutfit.equip(spec ?? {}))) return false;
      var disallowUsage = reservedBanishes.get(action.source);
      return !(disallowUsage !== null && disallowUsage !== void 0 && disallowUsage() && getUsingFreeBunnyBanish());
    }
  };
}
var GHOST_DOG_ADVENTURES = ["Puttin' it on Wax", "Wooof! Wooooooof!", "Playing Fetch*", "Your Dog Found Something Again"];
var JUNE_CLEAVER_ADVENTURES = ["Aunts not Ants", "Bath Time", "Beware of Aligator", "Delicious Sprouts", "Hypnotic Master", "Lost and Found", "Poetic Justice", "Summer Days", "Teacher's Pet"];
var VIOLET_FOG_ADVENTURES = ["She's So Unusual", "The Big Scary Place", "The Prince of Wishful Thinking", "Violet Fog"];
function lastAdventureWasWeird() {
  var _ref2 = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : {},
    _ref2$extraEncounters = _ref2.extraEncounters,
    extraEncounters = _ref2$extraEncounters === void 0 ? [] : _ref2$extraEncounters,
    _ref2$includeGhostDog = _ref2.includeGhostDog,
    includeGhostDog = _ref2$includeGhostDog === void 0 ? true : _ref2$includeGhostDog,
    _ref2$includeHolidayW = _ref2.includeHolidayWanderers,
    includeHolidayWanderers = _ref2$includeHolidayW === void 0 ? true : _ref2$includeHolidayW,
    _ref2$includeJuneClea = _ref2.includeJuneCleaver,
    includeJuneCleaver = _ref2$includeJuneClea === void 0 ? true : _ref2$includeJuneClea,
    _ref2$includeVioletFo = _ref2.includeVioletFog,
    includeVioletFog = _ref2$includeVioletFo === void 0 ? true : _ref2$includeVioletFo;
  return [].concat(_toConsumableArray(extraEncounters), _toConsumableArray(includeGhostDog ? GHOST_DOG_ADVENTURES : []), _toConsumableArray(includeHolidayWanderers ? getTodaysHolidayWanderers().map(monster => monster.name) : []), _toConsumableArray(includeJuneCleaver ? JUNE_CLEAVER_ADVENTURES : []), _toConsumableArray(includeVioletFog ? VIOLET_FOG_ADVENTURES : [])).includes(get$2("lastEncounter"));
}
var juneCleaverChoiceValues = {
  1467: {
    1: 0,
    2: 0,
    3: 5 * get$2("valueOfAdventure")
  },
  1468: {
    1: 0,
    2: 5,
    3: 0
  },
  1469: {
    1: 0,
    2: $item`Dad's brandy`,
    3: 1500
  },
  1470: {
    1: 0,
    2: $item`teacher's pen`,
    3: 0
  },
  1471: {
    1: $item`savings bond`,
    2: 250,
    3: 0
  },
  1472: {
    1: $item`trampled ticket stub`,
    2: $item`fire-roasted lake trout`,
    3: 0
  },
  1473: {
    1: $item`gob of wet hair`,
    2: 0,
    3: 0
  },
  1474: {
    1: 0,
    2: $item`guilty sprout`,
    3: 0
  },
  1475: {
    1: $item`mother's necklace`,
    2: 0,
    3: 0
  }
};
function valueJuneCleaverOption(result) {
  return result instanceof kolmafia.Item ? garboValue(result) : result;
}
function bestJuneCleaverOption(id) {
  var options = [1, 2, 3];
  return maxBy(options, option => valueJuneCleaverOption(juneCleaverChoiceValues[id][option]));
}
var romanticMonsterImpossible = () => get("Romantic Monster Window end") === Infinity || get("Romantic Monster Window begin") > 0 && get("Romantic Monster window begin") !== Infinity || get$2("_romanticFightsLeft") <= 0;
function sober() {
  return kolmafia.myInebriety() <= kolmafia.inebrietyLimit() + (kolmafia.myFamiliar() === $familiar`Stooper` ? -1 : 0);
}
var asArray = singleOrArray => Array.isArray(singleOrArray) ? singleOrArray : [singleOrArray];
var _bestShadowRift = null;
function bestShadowRift() {
  if (!_bestShadowRift) {
    _bestShadowRift = withLocation($location`Shadow Rift`, () => chooseRift({
      canAdventure: true,
      otherFilter: l => l !== $location`Shadow Rift (The 8-Bit Realm)`,
      sortBy: l => {
        // We probably aren't capping item drops with the penalty
        // so we don't really need to compute the actual outfit (or the dropModifier for that matter actually)
        var dropModifier = 1 + kolmafia.numericModifier("Item Drop") / 100;
        return sum(kolmafia.getMonsters(l), m => {
          return sum(kolmafia.itemDropsArray(m), _ref3 => {
            var drop = _ref3.drop,
              rate = _ref3.rate;
            return garboValue(drop) * clamp(rate * dropModifier / 100, 0, 1);
          });
        });
      }
    }));
    if (!_bestShadowRift) {
      throw new Error("Failed to find a suitable Shadow Rift to adventure in");
    }
  }
  return _bestShadowRift;
}
function withLocation(location, action) {
  var start = kolmafia.myLocation();
  try {
    kolmafia.setLocation(location);
    return action();
  } finally {
    kolmafia.setLocation(start);
  }
}
function freeRest() {
  if (get$2("timesRested") >= kolmafia.totalFreeRests()) return false;
  var start = kolmafia.myFamiliar();

  // Switch familiar first, a familiar may be granting max hp/mp
  if (have$P($familiar`Skeleton of Crimbo Past`) && get$2("_knuckleboneRests", 0) < 5) {
    kolmafia.useFamiliar($familiar`Skeleton of Crimbo Past`);
  }
  if (kolmafia.myHp() >= kolmafia.myMaxhp() && kolmafia.myMp() >= kolmafia.myMaxmp()) {
    if (acquire(1, $item`awful poetry journal`, 10000, false)) {
      kolmafia.use($item`awful poetry journal`);
    } else {
      // burn some mp so that we can rest
      var bestSkill = maxBy(kolmafia.Skill.all().filter(sk => have$P(sk) && kolmafia.mpCost(sk) >= 1), sk => -kolmafia.mpCost(sk)); // are there any other skills that cost mana which we should blacklist?
      // Facial expressions? But this usually won't be an issue since all *NORMAL* classes have access to a level1 1mp skill
      kolmafia.useSkill(bestSkill);
    }
  }
  var timesRested = get$2("timesRested");
  kolmafia.visitUrl("campground.php?action=rest");
  if (start !== kolmafia.myFamiliar()) {
    kolmafia.useFamiliar(start);
  }
  // Only return true when a rest was actually used
  return timesRested < get$2("timesRested");
}
function printEventLog() {
  if (resetDailyPreference("garboTargetDate")) {
    _set("garboTargetCount", 0);
    _set("garboTargetSources", "");
    _set("garboYachtzeeCount", 0);
  }
  var totalTargetCopies = getNumber("garboTargetCount", 0) + eventLog.initialCopyTargetsFought + eventLog.digitizedCopyTargetsFought;
  var allTargetSources = getString("garboTargetSources").split(",").filter(source => source);
  allTargetSources.push.apply(allTargetSources, _toConsumableArray(eventLog.copyTargetSources));
  var yacthzeeCount = get$2("garboYachtzeeCount", 0) + eventLog.yachtzees;
  _set("garboTargetCount", totalTargetCopies);
  _set("garboTargetSources", allTargetSources.join(","));
  _set("garboYachtzeeCount", yacthzeeCount);
  kolmafia.print(`You fought ${eventLog.initialCopyTargetsFought} ${globalOptions.target} at the beginning of the day, and an additional ${eventLog.digitizedCopyTargetsFought} digitized ${globalOptions.target} throughout the day. Good work, probably!`, HIGHLIGHT);
  kolmafia.print(`Including this, you have fought ${totalTargetCopies} across all ascensions today`, HIGHLIGHT);
  if (yacthzeeCount > 0) {
    kolmafia.print(`You explored the undersea yacht ${eventLog.yachtzees} times`, HIGHLIGHT);
    kolmafia.print(`Including this, you explored the undersea yacht ${yacthzeeCount} times across all ascensions today`, HIGHLIGHT);
  }
}
function untangleDigitizes(turnCount, chunks) {
  var turnsPerChunk = turnCount / chunks;
  var monstersPerChunk = Math.sqrt((turnsPerChunk + 3) / 5 + 1 / 4) - 1 / 2;
  return Math.round(chunks * monstersPerChunk);
}
function digitizedMonstersRemainingForTurns(estimatedTurns) {
  if (!have$I()) return 0;
  var digitizesLeft = getDigitizeUsesRemaining();
  if (digitizesLeft === getMaximumDigitizeUses()) {
    return untangleDigitizes(estimatedTurns, getMaximumDigitizeUses());
  }
  var monsterCount = getDigitizeMonsterCount() + 1;
  var turnsLeftAtNextMonster = estimatedTurns - get("Digitize Monster");
  if (turnsLeftAtNextMonster <= 0) return 0;
  var turnsAtLastDigitize = turnsLeftAtNextMonster + ((monsterCount + 1) * monsterCount * 5 - 3);
  return untangleDigitizes(turnsAtLastDigitize, digitizesLeft + 1) - getDigitizeMonsterCount();
}
function maxCarriedFamiliarDamage(familiar) {
  // Only considering familiars we reasonably may carry
  switch (familiar) {
    // +5 to Familiar Weight
    case $familiar`Animated Macaroni Duck`:
      return 50;
    case $familiar`Barrrnacle`:
    case $familiar`Gelatinous Cubeling`:
    case $familiar`Penguin Goodfella`:
      return 30;
    case $familiar`Misshapen Animal Skeleton`:
      return 40 + kolmafia.numericModifier("Spooky Damage");

    // +25% Meat from Monsters
    case $familiar`Hobo Monkey`:
      return 25;

    // +20% Meat from Monsters
    case $familiar`Grouper Groupie`:
      // Double sleaze damage at Barf Mountain
      return 25 + kolmafia.numericModifier("Sleaze Damage") * (kolmafia.myLocation() === $location`Barf Mountain` ? 2 : 1);
    case $familiar`Jitterbug`:
      return 20;
    case $familiar`Mutant Cactus Bud`:
      // 25 poison damage (25+12+6+3+1)
      return 47;
    case $familiar`Robortender`:
      return 20;
  }
  return 0;
}
function maxFamiliarDamage(familiar) {
  var totalFamWeight = kolmafia.familiarWeight(familiar) + kolmafia.weightAdjustment();
  switch (familiar) {
    case $familiar`Cocoabo`:
      return totalFamWeight + 3;
    case $familiar`Feather Boa Constrictor`:
      // Double sleaze damage at Barf Mountain
      return totalFamWeight + 3 + kolmafia.numericModifier("Sleaze Damage") * (kolmafia.myLocation() === $location`Barf Mountain` ? 2 : 1);
    case $familiar`Ninja Pirate Zombie Robot`:
      return Math.floor((totalFamWeight + 3) * 1.5);
    case $familiar`Jill-of-All-Trades`:
      return totalFamWeight + 3;
    // TODO: Unknown rate, assume 2x until properly spaded
    case $familiar`Adventurous Spelunker`:
      return Math.floor((totalFamWeight + 3) * 2);
  }
  return 0;
}
function maxPassiveDamage() {
  // Only considering passive damage sources we reasonably may have
  var vykeaMaxDamage = get$2("_VYKEACompanionLevel") > 0 ? 10 * get$2("_VYKEACompanionLevel") + 10 : 0;

  // Lasagmbie does max 2*level damage while Vermincelli does max level + (1/2 * level) + (1/2 * 1/2 * level) + ...
  var thrallMaxDamage = kolmafia.myThrall().level >= 5 && $thralls`Lasagmbie,Vermincelli`.includes(kolmafia.myThrall()) ? kolmafia.myThrall().level * 2 : 0;
  var crownMaxDamage = kolmafia.haveEquipped($item`Crown of Thrones`) ? maxCarriedFamiliarDamage(kolmafia.myEnthronedFamiliar()) : 0;
  var bjornMaxDamage = kolmafia.haveEquipped($item`Buddy Bjorn`) ? maxCarriedFamiliarDamage(kolmafia.myBjornedFamiliar()) : 0;
  var familiarMaxDamage = maxFamiliarDamage(kolmafia.myFamiliar());
  return vykeaMaxDamage + thrallMaxDamage + crownMaxDamage + bjornMaxDamage + familiarMaxDamage;
}
var monsterManuelCached = undefined;
function monsterManuelAvailable() {
  if (monsterManuelCached !== undefined) return Boolean(monsterManuelCached);
  monsterManuelCached = kolmafia.visitUrl("questlog.php?which=3").includes("Monster Manuel");
  return Boolean(monsterManuelCached);
}
var listItems = {};
function getDropsList(key) {
  return listItems[key] ?? (listItems[key] = JSON.parse(kolmafia.fileToBuffer("garbo_item_lists.json"))[key].map(i => kolmafia.Item.get(i)));
}
function felizValue() {
  return garboAverageValue.apply(void 0, _toConsumableArray(getDropsList("Feliz Navidad")));
}
function newarkValue() {
  return garboAverageValue.apply(void 0, _toConsumableArray(getDropsList("Newark")));
}
function candyFactoryValue() {
  return garboAverageValue.apply(void 0, _toConsumableArray(getDropsList("trainset")));
}
function aprilFoolsRufus() {
  if (kolmafia.holiday().includes("April Fool's Day")) {
    kolmafia.visitUrl("questlog.php?which=7");
  }
}
var luckyAdventureValues = [{
  location: $location`The Limerick Dungeon`,
  value: () => garboValue($item`cyclops eyedrops`)
}, {
  location: $location`Cobb's Knob Menagerie, Level 2`,
  value: () => garboValue($item`irradiated pet snacks`)
}, {
  location: $location`The Sleazy Back Alley`,
  value: () => 3 * garboValue($item`distilled fortified wine`)
}, {
  location: $location`The Haunted Pantry`,
  value: () => 3 * garboValue($item`tasty tart`)
}, {
  location: $location`The Outskirts of Cobb's Knob`,
  value: () => garboValue($item`Knob Goblin lunchbox`)
}, {
  location: $location`Cobb's Knob Harem`,
  value: () => 3 * garboValue($item`scented massage oil`)
}, {
  location: $location`The Castle in the Clouds in the Sky (Top Floor)`,
  value: () => garboValue($item`Mick's IcyVapoHotness Inhaler`)
}];
var luckyAdventures = [{
  location: $location`Cobb's Knob Treasury`,
  phase: "target",
  value: () => kolmafia.canAdventure($location`Cobb's Knob Treasury`) ? 3 * get$2("valueOfAdventure") // Rough estimation, they have 4x the basemeat of barf
  : 0
}].concat(_toConsumableArray(luckyAdventureValues.map(_ref4 => {
  var location = _ref4.location,
    _value = _ref4.value;
  return {
    location,
    phase: "barf",
    value: () => kolmafia.canAdventure(location) ? _value() - get$2("valueOfAdventure") : 0
  };
})));
function determineBestLuckyAdventure() {
  return maxBy(luckyAdventures, _ref5 => {
    var value = _ref5.value;
    return value();
  });
}
var bestLuckyAdventure;
function getBestLuckyAdventure() {
  return bestLuckyAdventure ?? (bestLuckyAdventure = determineBestLuckyAdventure());
}
var SCALE_PATTERN = /Scale: /;
var CAP_PATTERN = /Cap: (\d*)/;
function calculateScalerCap(_ref6) {
  var attributes = _ref6.attributes;
  var scaleMatch = SCALE_PATTERN.test(attributes);
  if (!scaleMatch) return 0;
  var capMatch = CAP_PATTERN.exec(attributes);
  if (!(capMatch !== null && capMatch !== void 0 && capMatch[1])) return Infinity;
  return Number(capMatch[1]);
}
var MONSTER_SCALER_CAPS = new Map();
function scalerCap(monster) {
  var cached = MONSTER_SCALER_CAPS.get(monster);
  if (cached) return cached;
  var cap = calculateScalerCap(monster);
  MONSTER_SCALER_CAPS.set(monster, cap);
  return cap;
}
var STRONG_SCALER_THRESHOLD = 1_000;
var isStrongScaler = m => scalerCap(m) > STRONG_SCALER_THRESHOLD;
var isFreeAndCopyable = monster => monster.copyable && monster.attributes.includes("FREE");
var valueDrops = monster => sum(kolmafia.itemDropsArray(monster), _ref7 => {
  var drop = _ref7.drop,
    rate = _ref7.rate,
    type = _ref7.type;
  return !["c", "0", "p", "a"].includes(type) ? garboValue(drop) * rate / 100 : 0;
});
var isFree = monster => monster.attributes.includes("FREE");
var unlimitedFreeRunList = $items`handful of split pea soup, tennis ball, Louder Than Bomb, divine champagne popper`;
function totalModifier(effect, stat) {
  return get$1(stat.toString(), effect) + 0.2 * get$1(`${stat.toString()} Percent`, effect);
}
function asEffect(thing) {
  return thing instanceof kolmafia.Effect ? thing : kolmafia.effectModifier(thing, "Effect");
}
function improvesStat(thing, stat) {
  return totalModifier(asEffect(thing), stat) > 0;
}
function improvedStats(thing) {
  return kolmafia.Stat.all().filter(stat => improvesStat(thing, stat));
}
function improvesAStat(thing) {
  return improvedStats(thing).length > 0;
}
function willDrunkAdventure() {
  return have$P($item`Drunkula's wineglass`) && globalOptions.ascend;
}
function freeFishyAvailable() {
  return have$P($effect`Fishy`) || have$P($item`fishy pipe`) && !get$2("_fishyPipeUsed") || get$2("skateParkStatus") === "ice" && !get$2("_skateBuff1");
}
var ULTRA_RARE_MONSTERS = kolmafia.Monster.all().filter(m => m.attributes.includes("ULTRARARE"));

// Marginal value of familiar weight in % meat drop.
function marginalFamWeightValue() {
  var familiarMultiplier = have$P($familiar`Robortender`) ? 2 : have$P($familiar`Hobo Monkey`) ? 1.25 : 1;

  // Assume base weight of 100 pounds. This is off but close enough.
  var assumedBaseWeight = 100;
  return 2 * familiarMultiplier + Math.sqrt(220 * familiarMultiplier) / (2 * Math.sqrt(assumedBaseWeight));
}
function mainStatLevel(level) {
  return (level - 1) ** 2 + 4;
}

function mafiaThumbRing(mode) {
  if (!have$P($item`mafia thumb ring`) || modeIsFree(mode)) {
    return new Map([]);
  }
  return new Map([[$item`mafia thumb ring`, (1 / 0.96 - 1) * get$2("valueOfAdventure")]]);
}
function luckyGoldRingDropValues(includeVolcoino, includeFreddy) {
  // Volcoino has a low drop rate which isn't accounted for here
  // Overestimating until it drops is probably fine, don't @ me
  var dropValues = [100].concat(_toConsumableArray([kolmafia.itemAmount($item`hobo nickel`) > 0 ? 100 : 0,
  // This should be closeted
  kolmafia.itemAmount($item`sand dollar`) > 0 ? garboValue($item`sand dollar`) : 0,
  // This should be closeted
  includeFreddy ? garboValue($item`Freddy Kruegerand`) : 0].concat(_toConsumableArray(lgrCurrencies().map(i => i === $item`Volcoino` && !includeVolcoino ? 0 : garboValue(i)))).filter(value => value > 0)));
  return dropValues;
}
function luckyGoldRing(mode) {
  // Ignore for DMT, assuming mafia might get confused about the volcoino drop by the weird combats
  if (!have$P($item`lucky gold ring`) || mode === BonusEquipMode.DMT) {
    return new Map([]);
  }
  var dropValues = luckyGoldRingDropValues(!(mode === BonusEquipMode.MEAT_TARGET && !globalOptions.nobarf),
  // Volcoino drops once per day, only wear during meat targets if nobarf
  kolmafia.itemAmount($item`Freddy Kruegerand`) > 0);

  // Items drop every ~10 turns
  return new Map([[$item`lucky gold ring`, sumNumbers(dropValues) / dropValues.length / 10]]);
}

// Possible drops are any pvpable potion that are not marked as banned by standard in the future,
// which can be checked with the "Last Available" modifier being unset.
// Resulting value from this function should be cached to prevent reprocessing
function calculateMrCheengsSpectaclesBonus() {
  var lastAvailableModifier = kolmafia.Modifier.get("Last Available");
  var possibleDrops = kolmafia.Item.all().filter(i => i.tradeable && i.discardable && i.potion && kolmafia.stringModifier(i, lastAvailableModifier) === "");
  var dropRate = 0.25; // Items drop every 4 turns
  var maxPrice = 100_000; // arbitrary, to help avoid outliers
  return sum(possibleDrops, item => Math.min(garboValue(item), maxPrice)) / possibleDrops.length * dropRate;
}
var mrCheengsBonus;
function mrCheengsSpectacles() {
  if (!have$P($item`Mr. Cheeng's spectacles`)) {
    return new Map([]);
  }
  mrCheengsBonus ?? (mrCheengsBonus = calculateMrCheengsSpectaclesBonus());
  return new Map([[$item`Mr. Cheeng's spectacles`, mrCheengsBonus]]);
}
function mrScreegesSpectacles() {
  if (!have$P($item`Mr. Screege's spectacles`)) {
    return new Map([]);
  }

  // TODO: Calculate actual bonus value (good luck!)
  return new Map([[$item`Mr. Screege's spectacles`, 180]]);
}
function cinchoDeMayo(mode) {
  if (!have$P($item`Cincho de Mayo`) || currentCinch() === 0 ||
  // Ignore for DMT? Requires specific combat stuff, so probably weird there
  mode === BonusEquipMode.DMT || mode === BonusEquipMode.MEAT_TARGET ||
  // Require manuel to make sure we don't kill during stasis
  !monsterManuelAvailable() ||
  // If we're doing Yachtzees, only use up excess cincho.
  maximumPinataCasts() <= 0 ||
  // If we have more than 50 passive damage, we'll never be able to cast projectile pinata without risking the monster dying
  maxPassiveDamage() >= 50) {
    return new Map([]);
  }

  // Account for a single use of Projectile Pinata, which gives 3x Robortender candies
  return new Map([[$item`Cincho de Mayo`, 3 * felizValue()]]);
}
function calculateLaughingStockBonus(alwaysUseHorizon) {
  var basicFruitValue = garboAverageValue.apply(void 0, _toConsumableArray($items`orange, grapefruit, grapes, lemon, lime, papaya, cranberries, strawberry, cherry, kumquat, tangerine, raspberry, kiwi, blackberry, banana, cactus fruit, plum, pear, peach`));
  var classicFruitValue = garboAverageValue.apply(void 0, _toConsumableArray($items`classic banana, antique watermelon, quince`));
  var horizonValue = 0.02 * (0.1 * classicFruitValue + 0.9 * basicFruitValue);
  if (alwaysUseHorizon) return horizonValue;
  var nextDrop$1 = nextDrop();
  if (nextDrop$1) {
    var _nextDrop = _slicedToArray(nextDrop$1, 2),
      fruit = _nextDrop[0],
      fights = _nextDrop[1];
    if (fights > estimatedGarboTurns()) return 0;
    return Math.max(garboValue(fruit) / fights, horizonValue);
  }
  return horizonValue;
}
function portableLaughingStock(mode) {
  if (!have$P($item`Portable Laughing Stock`) || mode === BonusEquipMode.DMT) {
    return new Map([]);
  }
  var laughingStockBonus = calculateLaughingStockBonus(mode !== BonusEquipMode.MEAT_TARGET);
  return new Map([[$item`Portable Laughing Stock`, laughingStockBonus]]);
}

/*
This is separate from bonusGear to prevent circular references
bonusGear() calls pantsgiving(), which calls estimatedGarboTurns(), which calls usingThumbRing()
If this isn't separated from bonusGear(), usingThumbRing() will call bonusGear(), creating a dangerous loop
*/
function bonusAccessories(mode) {
  return new Map([].concat(_toConsumableArray(mafiaThumbRing(mode)), _toConsumableArray(luckyGoldRing(mode)), _toConsumableArray(mrCheengsSpectacles()), _toConsumableArray(portableLaughingStock(mode)), _toConsumableArray(mrScreegesSpectacles()), _toConsumableArray(cinchoDeMayo(mode))));
}
var cachedUsingThumbRing = null;
/**
 * Calculates whether we expect to be wearing the thumb ring for most of the farming day.
 * This is used in functions that leverage projected turns; for instance, calculating the
 * number of turns of sweet synthesis required in our diet calcs or potion costs.
 * @returns boolean of whether we expect to be wearing the thumb ring for much of the day
 */
function usingThumbRing() {
  if (!have$P($item`mafia thumb ring`)) {
    return false;
  }
  if (cachedUsingThumbRing === null) {
    var gear = bonusAccessories(BonusEquipMode.BARF);
    var accessoryBonuses = _toConsumableArray(gear.entries()).filter(_ref => {
      var _ref2 = _slicedToArray(_ref, 1),
        item = _ref2[0];
      return have$P(item);
    });

    // Mafia resolves env()/zone()/loc() modifiers against the last location
    // set, so restore it or unrelated gear is priced against this one.
    var meatAccessories = withLocation(FarmingStrategy.location, () => kolmafia.Item.all().filter(item => have$P(item) && kolmafia.toSlot(item) === $slot`acc1` && get$1("Meat Drop", item) > 0).map(item => [item, get$1("Meat Drop", item) * baseMeat() / 100]));
    var accessoryValues = new Map(accessoryBonuses);
    var _iterator = _createForOfIteratorHelper(meatAccessories),
      _step;
    try {
      for (_iterator.s(); !(_step = _iterator.n()).done;) {
        var _step$value = _slicedToArray(_step.value, 2),
          accessory = _step$value[0],
          value = _step$value[1];
        accessoryValues.set(accessory, value + (accessoryValues.get(accessory) ?? 0));
      }
    } catch (err) {
      _iterator.e(err);
    } finally {
      _iterator.f();
    }
    if (have$P($item`mafia pointer finger ring`) && (kolmafia.myClass() === $class`Seal Clubber` && have$P($skill`Furious Wallop`) || have$P($item`haiku katana`) || have$P($item`Operation Patriot Shield`) || have$P($item`unwrapped knock-off retro superhero cape`) || have$P($item`left bear arm`) || have$P($skill`Head in the Game`))) {
      accessoryValues.set($item`mafia pointer finger ring`, basePointerRingMeat());
    }
    var bestAccessories = _toConsumableArray(accessoryValues.entries()).sort((_ref3, _ref4) => {
      var _ref5 = _slicedToArray(_ref3, 2),
        aBonus = _ref5[1];
      var _ref6 = _slicedToArray(_ref4, 2),
        bBonus = _ref6[1];
      return bBonus - aBonus;
    }).map(_ref7 => {
      var _ref8 = _slicedToArray(_ref7, 1),
        item = _ref8[0];
      return item;
    });
    cachedUsingThumbRing = bestAccessories.slice(0, 2).includes($item`mafia thumb ring`);
  }
  return cachedUsingThumbRing;
}

var SPINNING_YOUR_TIME_SPINNER = 1195;
var TRAVEL_TO_A_RECENT_FIGHT = 1196;
var refusedMonsterIds = new Set();

/**
 * Whether the Time-Spinner has declined to travel to a monster this run.
 * Refusals spend no minutes, so sources must check this or they re-offer.
 * @param monster The monster to check
 * @returns Whether a travel to this monster was refused
 */
function timeSpinnerRefused(monster) {
  return refusedMonsterIds.has(monster.id);
}

/**
 * Whether a monster is on the <select name="monid"> in choice 1196.
 * @param monster The monster to look for
 * @returns Whether the monster is offered, or null if no list was found
 */
function offersMonster(monster) {
  var monids = kolmafia.availableChoiceSelectInputs(1)["monid"];
  if (!monids || Object.keys(monids).length === 0) return null;
  if (`${monster.id}` in monids) return true;
  kolmafia.print(`The Time-Spinner is only offering: ${Object.values(monids).join(", ")}`, HIGHLIGHT);
  return false;
}

/**
 * Record a refused travel and leave the Time-Spinner choice. An open choice
 * blocks every later equipment change.
 * @param monster The monster the Time-Spinner would not travel to
 */
function escapeRefusal(monster) {
  refusedMonsterIds.add(monster.id);
  kolmafia.print(`The Time-Spinner would not travel to a ${monster}; it is no longer in the recent-fight list. Backing out of the choice.`, HIGHLIGHT);
  // "Maybe Later" returns to 1195, which any non-choice request leaves.
  if (kolmafia.lastChoice() === TRAVEL_TO_A_RECENT_FIGHT) kolmafia.runChoice(2);
  if (kolmafia.handlingChoice() && kolmafia.lastChoice() === SPINNING_YOUR_TIME_SPINNER) {
    kolmafia.visitUrl("main.php");
  }
  if (kolmafia.handlingChoice()) {
    kolmafia.abort(`Still stuck in choice ${kolmafia.lastChoice()} after the Time-Spinner refused to fight a ${monster}. Resolve it in the relay browser before continuing.`);
  }
}

/**
 * From the menu that using the Time-Spinner opens, travel to a recent fight.
 * @param monster The monster to fight
 * @returns Whether a fight started, rather than the Time-Spinner refusing
 */
function travelToRecentFight(monster) {
  // runChoice() sends nothing outside a choice, so check each page is open.
  if (!kolmafia.handlingChoice() || kolmafia.lastChoice() !== SPINNING_YOUR_TIME_SPINNER) {
    kolmafia.abort("Using the Time-Spinner did not open its menu.");
  }
  // Opening the menu re-reads the minutes, and a travel costs 3.
  if (get$2("_timeSpinnerMinutesUsed") > 7) {
    kolmafia.visitUrl("main.php");
    return false;
  }
  kolmafia.runChoice(1);
  if (!kolmafia.handlingChoice() || kolmafia.lastChoice() !== TRAVEL_TO_A_RECENT_FIGHT) {
    kolmafia.abort("The Time-Spinner menu did not open the recent-fight list.");
  }
  var offered = offersMonster(monster);
  if (offered === false) {
    escapeRefusal(monster);
    return false;
  }
  if (offered === null) {
    kolmafia.print("Could not find the Time-Spinner's recent-fight list on the page; attempting the travel anyway.", HIGHLIGHT);
  }
  kolmafia.runChoice(1, false, `monid=${monster.id}`);
  if (kolmafia.handlingChoice()) {
    escapeRefusal(monster);
    return false;
  }
  return true;
}

var mimicExperienceNeeded = needKickstarterEgg => 50 * (11 - get$2("_mimicEggsObtained")) + (globalOptions.ascend ? needKickstarterEgg && !have$I() && get$2("_mimicEggsObtained") < 11 ? 50 : 0 : 550);
function shouldChargeMimic(needKickstarterEgg) {
  /* If we can't make any more eggs tomorrow, don't charge the mimic more */
  return $familiar`Chest Mimic`.experience < mimicExperienceNeeded(needKickstarterEgg);
}
var monsterInEggnet;
var monsterIsInEggnet = () => monsterInEggnet ?? (monsterInEggnet = getReceivableMonsters().includes(globalOptions.target));
function shouldMakeEgg(barf) {
  var needKickstarterEgg = differentiableQuantity(globalOptions.target) <= 0;
  if (needKickstarterEgg && !monsterIsInEggnet()) return false;
  var experienceNeeded = 50 * (11 - get$2("_mimicEggsObtained")) + (needKickstarterEgg ? 50 : 0);
  return $familiar`Chest Mimic`.experience >= experienceNeeded && get$2("_mimicEggsObtained") < 11;
}
var minimumMimicExperience = () => 50 + (differentiableQuantity(globalOptions.target) ? 0 : 100);

var fam;
function findBestLeprechauns() {
  var validFamiliars = kolmafia.Familiar.all().filter(f => have$P(f) && f !== $familiar`Ghost of Crimbo Commerce`);
  validFamiliars.sort((a, b) => findLeprechaunMultiplier(b) - findLeprechaunMultiplier(a));
  var bestLepMult = findLeprechaunMultiplier(validFamiliars[0]);
  var firstBadLeprechaun = validFamiliars.findIndex(f => findLeprechaunMultiplier(f) < bestLepMult);
  if (firstBadLeprechaun === -1) return validFamiliars;
  return validFamiliars.slice(0, firstBadLeprechaun);
}
function findBestLeprechaun() {
  return maxBy(findBestLeprechauns(), findFairyMultiplier);
}
function setBestLeprechaunAsMeatFamiliar() {
  fam = findBestLeprechaun();
}
function meatFamiliar() {
  return fam ?? (fam = $familiars`Robortender, Jill-of-All-Trades`.find(have$P) ?? findBestLeprechaun());
}

function bestBjornalike(outfit) {
  var bjornalikes = $items`Buddy Bjorn, Crown of Thrones`.filter(item => outfit.canEquip(item));
  if (bjornalikes.length === 0) return null;
  if (bjornalikes.length === 1) return bjornalikes[0];
  if (outfit.bonuses.has($item`bat wings`)) return $item`Crown of Thrones`;
  var hasStrongLep = findLeprechaunMultiplier(meatFamiliar()) >= 2;
  var goodRobortHats = $items`crumpled felt fedora`;
  if (kolmafia.myClass() === $class`Turtle Tamer`) {
    goodRobortHats.push($item`warbear foil hat`);
  }
  if (kolmafia.numericModifier($item`shining star cap`, "Familiar Weight") === 10) {
    goodRobortHats.push($item`shining star cap`);
  }
  if (have$P($item`carpe`) && (!hasStrongLep || !goodRobortHats.some(hat => have$P(hat) && kolmafia.canEquip(hat)))) {
    return $item`Crown of Thrones`;
  }
  return $item`Buddy Bjorn`;
}
function cleaverCheck() {
  if (kolmafia.availableAmount($item`June cleaver`) > 1) kolmafia.cliExecute("refresh inventory");
}
function useUPCs() {
  var UPC = $item`scratch 'n' sniff UPC sticker`;
  if ($items`scratch 'n' sniff sword, scratch 'n' sniff crossbow`.every(i => !have$P(i))) {
    kolmafia.visitUrl(`bedazzle.php?action=juststick&sticker=${kolmafia.toInt(UPC)}&pwd`);
  }
  for (var slotNumber = 1; slotNumber <= 3; slotNumber++) {
    var slot = kolmafia.toSlot(`sticker${slotNumber}`);
    var sticker = kolmafia.equippedItem(slot);
    if (sticker === UPC) continue;
    kolmafia.visitUrl("bedazzle.php");
    if (sticker !== $item.none) {
      kolmafia.visitUrl(`bedazzle.php?action=peel&pwd&slot=${slotNumber}`);
    }
    kolmafia.visitUrl(`bedazzle.php?action=stick&pwd&slot=${slotNumber}&sticker=${kolmafia.toInt(UPC)}`);
  }
}
var stickerSlots = $slots`sticker1, sticker2, sticker3`;
var UPC = $item`scratch 'n' sniff UPC sticker`;
function useUPCsIfNeeded(_ref) {
  var familiar = _ref.familiar;
  var currentWeapon = 25 * (familiar ? findLeprechaunMultiplier(familiar) : 0);
  var targets = globalOptions.ascend ? Math.min(20, highMeatMonsterCount() || wanderingCopytargetsRemaining()) : 20;
  var addedValueOfFullSword = targets * ((75 - currentWeapon) * targetMeat()) / 100;
  if (addedValueOfFullSword > 3 * kolmafia.mallPrice(UPC)) {
    var needed = 3 - stickerSlots.filter(sticker => kolmafia.equippedItem(sticker) === UPC).length;
    if (needed) acquire(needed, UPC, addedValueOfFullSword / 3, false);
    useUPCs();
  }
}
var waterBreathingEquipment = $items`The Crown of Ed the Undying, aerated diving helmet, crappy Mer-kin mask, Mer-kin gladiator mask, Mer-kin scholar mask, old SCUBA tank`;
var familiarWaterBreathingEquipment = $items`das boot, little bitty bathysphere`;
function toSpec(source) {
  if (!source) return {};
  if (source instanceof Requirement) {
    var _source$maximizeOptio;
    var result = {};
    if (source.maximizeParameters.length) {
      result.modifier = source.maximizeParameters;
    }
    if ((_source$maximizeOptio = source.maximizeOptions.forceEquip) !== null && _source$maximizeOptio !== void 0 && _source$maximizeOptio.length) {
      result.equip = source.maximizeOptions.forceEquip;
    }
    if (source.maximizeOptions.preventEquip) {
      result.avoid = source.maximizeOptions.preventEquip;
    }
    return result;
  } else {
    var _source$constraints$e, _source$constraints, _source$constraints$f, _source$constraints2;
    var req = (_source$constraints$e = (_source$constraints = source.constraints).equipmentRequirements) === null || _source$constraints$e === void 0 ? void 0 : _source$constraints$e.call(_source$constraints);
    var spec = req ? toSpec(req) : {};
    var familiar = (_source$constraints$f = (_source$constraints2 = source.constraints).familiar) === null || _source$constraints$f === void 0 ? void 0 : _source$constraints$f.call(_source$constraints2);
    if (familiar) spec.familiar = familiar;
    return spec;
  }
}
var cachedUsingPurse = null;
function usingPurse() {
  if (cachedUsingPurse === null) {
    cachedUsingPurse = kolmafia.myInebriety() <= kolmafia.inebrietyLimit() && !have$P($item`KoL Con 13 snowglobe`) && !have$P($item`can of mixed everything`) && (!have$P($item`latte lovers member's mug`) || !have$P($familiar`Robortender`) && !have$P($familiar`Hobo Monkey`) || !kolmafia.canAdventure($location`The Black Forest`));
  }
  return cachedUsingPurse;
}
function validateGarbageFoldable(spec) {
  var garbageItems = getFoldGroup($item`January's Garbage Tote`);
  var _iterator = _createForOfIteratorHelper(garbageItems),
    _step;
  try {
    var _loop = function _loop() {
      var garbageItem = _step.value;
      if (Object.values(spec).some(specEntry => specEntry === garbageItem || Array.isArray(specEntry) && specEntry.includes(garbageItem))) {
        if (!have$P(garbageItem)) kolmafia.cliExecute(`fold ${garbageItem}`);
        return 1; // break
      }
    };
    for (_iterator.s(); !(_step = _iterator.n()).done;) {
      if (_loop()) break;
    }
  } catch (err) {
    _iterator.e(err);
  } finally {
    _iterator.f();
  }
}
var bestPantsAdventures;
var getBestPantsAdventures = () => bestPantsAdventures ?? (bestPantsAdventures = Math.max.apply(Math, [0].concat(_toConsumableArray(kolmafia.Item.all().filter(item => kolmafia.toSlot(item) === $slot`pants` && have$P(item) && kolmafia.numericModifier(item, "Adventures") > 0).map(pants => kolmafia.numericModifier(pants, "Adventures"))))));
function cheeseBonus(mode) {
  if (globalOptions.ascend) return 0;
  if (mode === BonusEquipMode.MEAT_TARGET) return 0;
  if (get$2("_stinkyCheeseCount") >= 100) return 0;
  if (!getFoldGroup($item`stinky cheese diaper`).some(item => have$P(item))) {
    return 0;
  }
  if (estimatedGarboTurns() < 100 - get$2("_stinkyCheeseCount")) return 0;
  return get$2("valueOfAdventure") * (10 - getBestPantsAdventures()) * (1 / 100);
}
function applyCheeseBonus(outfit, mode) {
  var bonus = cheeseBonus(mode);
  if (bonus > 0) outfit.modifier.push(`${bonus.toFixed(2)} stinky cheese`);
}

function beretEffectValue(effect, duration) {
  var skill = kolmafia.toSkill(effect);
  if (skill !== kolmafia.Skill.none && have$P(skill)) return 0;
  var meatValue = duration * sum([{
    modifier: "Meat Drop",
    value: baseMeat() / 100
  }, {
    modifier: "Familiar Weight",
    value: marginalFamWeightValue() * baseMeat() / 100
  }], _ref => {
    var modifier = _ref.modifier,
      value = _ref.value;
    return value * get$1(modifier, effect);
  });
  if (meatValue <= 0) return meatValue;
  var potionPrices = kolmafia.Item.all().filter(i => i.potion && i.tradeable && kolmafia.effectsModifier(i, "Effect").includes(effect)).map(i => getAcquirePrice(i) * duration / get$1("Effect Duration", i));
  return Math.min.apply(Math, [meatValue].concat(_toConsumableArray(potionPrices)));
}

function sweatEquityROI() {
  return baseMeat() * 0.4 * 30;
}
function parentStat(sub) {
  switch (sub) {
    case $stat`subMuscle`:
      return $stat`Muscle`;
    case $stat`subMysticality`:
      return $stat`Mysticality`;
    case $stat`subMoxie`:
      return $stat`Moxie`;
  }
  return $stat`Muscle`;
}
var BCT_LEVEL_THRESHOLDS = [26, 20, 13];
function getBCZStatFloor(skill) {
  var userSelectedStatFloor = get$2("garbo_bczStatFloor", 0);
  var stat = parentStat(substatUsed(skill));
  if (stat !== kolmafia.myPrimestat()) {
    if (stat === $stat`Moxie` && have$P($item`crumpled felt fedora`)) {
      return clamp(200, userSelectedStatFloor, Infinity);
    }
    return clamp(100, userSelectedStatFloor, Infinity); // ? is this good?
  }
  var minimumLevel = globalOptions.ascend && kolmafia.myDaycount() >= 2 ? BCT_LEVEL_THRESHOLDS.find(threshold => kolmafia.myLevel() > threshold) : 26;
  if (!minimumLevel) {
    return clamp(kolmafia.myBasestat(stat), userSelectedStatFloor, Infinity); // So low level we can't afford to lose exp at all
  }
  return clamp(mainStatLevel(minimumLevel), userSelectedStatFloor, Infinity);
}
function safeBCZCasts(skill) {
  var availableStatFloorCasts = availableCasts(skill, getBCZStatFloor(skill));
  var availableCheapCasts = 5 - timesCast(skill); // First 5 casts are essentially free, even when we're already low stats
  return availableCheapCasts > availableStatFloorCasts ? availableCheapCasts : availableStatFloorCasts;
}
function safeSweatEquityCasts() {
  return safeBCZCasts($skill`BCZ: Sweat Equity`);
}
function safeRefractedCasts() {
  return safeBCZCasts($skill`BCZ: Refracted Gaze`);
}
function safeSweatBulletCasts(drumMachineROI) {
  if (sweatEquityROI() > drumMachineROI) return 0;
  return safeBCZCasts($skill`BCZ: Sweat Bullets`);
}

var SKILL_OPTIONS = [
// August 1 deliberately omitted; does not trigger on monster replacers
{
  skill: $skill`Aug. 2nd: Find an Eleven-Leaf Clover Day`,
  value: () => getBestLuckyAdventure().value(),
  type: "special"
}, {
  skill: $skill`Aug. 3rd: Watermelon Day!`,
  value: () => garboValue($item`watermelon`),
  type: "summon"
}, {
  skill: $skill`Aug. 4th: Water Balloon Day!`,
  value: () => 3 * garboValue($item`water balloon`),
  type: "summon"
}, {
  skill: $skill`Aug. 5th: Oyster Day!`,
  value: () => 3 * garboAverageValue.apply(void 0, _toConsumableArray($items`brilliant oyster egg, gleaming oyster egg, glistening oyster egg, lustrous oyster egg, magnificent oyster egg, pearlescent oyster egg, scintillating oyster egg`)),
  type: "summon"
}, {
  skill: $skill`Aug. 7th: Lighthouse Day!`,
  value: () => effectValue($effect`Incredibly Well Lit`, 30),
  type: "buff"
}, {
  skill: $skill`Aug. 8th: Cat Day!`,
  value: () => globalOptions.prefs.valueOfFreeFight,
  type: "fight"
}, {
  skill: $skill`Aug. 13th: Left/Off Hander's Day!`,
  value: () => new Potion($item`august scepter`, {
    effect: $effect`Offhand Remarkable`,
    duration: 30,
    effectValues: {
      meatDrop: 80
    } // Half a purse
  }).gross(highMeatMonsterCount("Scepter")) + (globalOptions.ascend ? 0 : (5 + (have$P($familiar`Left-Hand Man`) ? 5 : 0)) * get$2("valueOfAdventure")),
  type: "special" // Don't want to cast right away
}, {
  skill: $skill`Aug. 14th: Financial Awareness  Day!`,
  value: () => Math.min(100 * kolmafia.myLevel(), 1500, kolmafia.myMeat()) / 2,
  type: "summon"
}, {
  skill: $skill`Aug. 16th: Roller Coaster Day!`,
  value: () => 8 * get$2("valueOfAdventure"),
  type: "special"
}, {
  skill: $skill`Aug. 18th: Serendipity Day!`,
  value: () => 3000,
  // Dummy value; we should some day calculate this based on free fight count, careful to avoid circular imports
  type: "buff"
}, {
  skill: $skill`Aug. 22nd: Tooth Fairy Day!`,
  value: () => globalOptions.prefs.valueOfFreeFight,
  type: "fight"
}, {
  skill: $skill`Aug. 24th: Waffle Day!`,
  value: () => 3 * garboValue($item`waffle`),
  type: "summon"
}, {
  skill: $skill`Aug. 25th: Banana Split Day!`,
  value: () => garboValue($item`banana split`),
  type: "summon"
}, {
  skill: $skill`Aug. 26th: Toilet Paper Day!`,
  value: () => garboValue($item`handful of toilet paper`),
  type: "summon"
}, {
  skill: $skill`Aug. 29th: More Herbs, Less Salt  Day!`,
  value: () => 3 * garboValue($item`Mrs. Rush`),
  type: "summon"
}, {
  skill: $skill`Aug. 30th: Beach Day!`,
  value: () => 100 + (globalOptions.ascend ? 0 : clamp(7 - get$1("Adventures", kolmafia.Item.all().filter(i => have$P(i) && kolmafia.toSlot(i) === $slot`acc1` && kolmafia.canEquip(i)).sort((a, b) => get$1("Adventures", b) - get$1("Adventures", a))[2] ?? $item.none), 0, 7) * get$2("valueOfAdventure")),
  type: "summon"
}, {
  skill: $skill`Aug. 31st: Cabernet Sauvignon  Day!`,
  value: () => 2 * garboValue($item`bottle of Cabernet Sauvignon`),
  type: "summon"
}];
var bestScepterSkills = null;
function getBestScepterSkills() {
  return bestScepterSkills ?? (bestScepterSkills = SKILL_OPTIONS.filter(_ref => {
    var skill = _ref.skill;
    return todaysSkill() !== skill && skill.dailylimit > 0;
  }).sort((a, b) => b.value() - a.value()).splice(0, clamp(5 - get$2("_augSkillsCast"), 0, 5)));
}
function shouldAugustCast(skill) {
  return have$p() && (getBestScepterSkills().some(s => skill === s.skill) && skill.dailylimit && get$2("_augSkillsCast") < 5 || todaysSkill() === skill && !getTodayCast() && skill.dailylimit >= 1);
}
function summonTask(_ref2) {
  var skill = _ref2.skill;
  return {
    name: skill.name,
    completed: () => !shouldAugustCast(skill),
    do: () => kolmafia.useSkill(skill),
    spendsTurn: false
  };
}
function augustSummonTasks() {
  return have$p() ? SKILL_OPTIONS.filter(_ref3 => {
    var type = _ref3.type;
    return type === "summon";
  }).map(summonTask) : [];
}
function castAugustScepterBuffs() {
  if (have$p()) {
    var _iterator = _createForOfIteratorHelper(SKILL_OPTIONS.filter(_ref5 => {
        var skill = _ref5.skill,
          type = _ref5.type;
        return shouldAugustCast(skill) && type === "buff";
      })),
      _step;
    try {
      for (_iterator.s(); !(_step = _iterator.n()).done;) {
        var skill = _step.value.skill;
        kolmafia.useSkill(skill);
      }
    } catch (err) {
      _iterator.e(err);
    } finally {
      _iterator.f();
    }
    var today = SKILL_OPTIONS.find(_ref4 => {
      var skill = _ref4.skill,
        type = _ref4.type;
      return type === "buff" && skill === todaysSkill();
    });
    if (today && !getTodayCast()) kolmafia.useSkill(today.skill);
    if (globalOptions.ascend && shouldAugustCast($skill`Aug. 13th: Left/Off Hander's Day!`)) {
      kolmafia.useSkill($skill`Aug. 13th: Left/Off Hander's Day!`);
    }
  }
}

var banned = $items`Uncle Greenspan's Bathroom Finance Guide`;
var failedWishes = [];
var mutuallyExclusiveList = [$effects`Blue Tongue, Green Tongue, Orange Tongue, Purple Tongue, Red Tongue, Black Tongue`, $effects`Cupcake of Choice, The Cupcake of Wrath, Shiny Happy Cupcake, Your Cupcake Senses Are Tingling, Tiny Bubbles in the Cupcake`, $effects`Broken Heart, Fiery Heart, Cold Hearted, Sweet Heart, Withered Heart, Lustful Heart`, $effects`Coldform, Hotform, Sleazeform, Spookyform, Stenchform`];
var mutuallyExclusive = new Map();
for (var _i = 0, _mutuallyExclusiveLis = mutuallyExclusiveList; _i < _mutuallyExclusiveLis.length; _i++) {
  var effectGroup = _mutuallyExclusiveLis[_i];
  var _iterator = _createForOfIteratorHelper(effectGroup),
    _step;
  try {
    var _loop = function _loop() {
      var effect = _step.value;
      mutuallyExclusive.set(effect, [].concat(_toConsumableArray(mutuallyExclusive.get(effect) ?? []), _toConsumableArray(effectGroup.filter(other => other !== effect))));
    };
    for (_iterator.s(); !(_step = _iterator.n()).done;) {
      _loop();
    }
  } catch (err) {
    _iterator.e(err);
  } finally {
    _iterator.f();
  }
}
var INVALID_CHARS_REGEX = /[.',]/g;
var wishableEffects = kolmafia.Effect.all().filter(e => !e.attributes.includes("nohookah"));
var wishableEffectData = wishableEffects.map(e => {
  var name = e.name.toLowerCase();
  var splitName = name.split(INVALID_CHARS_REGEX);
  return {
    e,
    name,
    splitName
  };
});
var invalidWishStrings = wishableEffectData.filter(_ref => {
  var name = _ref.name;
  return name.match(INVALID_CHARS_REGEX);
}).filter(_ref2 => {
  var name = _ref2.name,
    splitName = _ref2.splitName;
  return splitName.every(s => wishableEffectData.some(n => n.name !== name && n.splitName.some(x => x.includes(s))));
}).map(_ref3 => {
  var name = _ref3.name;
  return name;
});
var availableItems = _toConsumableArray(new Set(kolmafia.Location.all().filter(l => kolmafia.canAdventure(l)).flatMap(l => kolmafia.getMonsters(l).filter(m => m.copyable).flatMap(m => kolmafia.itemDropsArray(m).filter(_ref4 => {
  var rate = _ref4.rate;
  return rate > 1;
}))).map(_ref5 => {
  var drop = _ref5.drop;
  return drop;
}))).map(i => i.name);
var validPawWishes = new Map(wishableEffectData.filter(_ref6 => {
  var e = _ref6.e,
    name = _ref6.name;
  return !invalidWishStrings.includes(name) && (globalOptions.prefs.yachtzeechain ? e !== $effect`Eau d' Clochard` : true);
} // hardcoded heuristics
).map(_ref7 => {
  var e = _ref7.e,
    name = _ref7.name,
    splitName = _ref7.splitName;
  if (!name.match(INVALID_CHARS_REGEX)) return [e, name];
  return [e, splitName.filter(s => !availableItems.includes(s) && !wishableEffectData.some(n => n.name !== name && n.splitName.some(x => x.includes(s))))[0]];
}));
function retrieveUntradeablePrice(it) {
  return kolmafia.retrievePrice(it, kolmafia.availableAmount(it) + 1) - kolmafia.autosellPrice(it) * kolmafia.availableAmount(it);
}
var BUFFER_TURNS = 30;
var Potion = /*#__PURE__*/function () {
  function Potion(potion) {
    var options = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {};
    _classCallCheck(this, Potion);
    this.potion = potion;
    this.providesDoubleDuration = options.providesDoubleDuration;
    this.canDouble = options.canDouble ?? true;
    this.overrideDuration = options.duration;
    this.overrideEffect = options.effect;
    this.priceOverride = options.price;
    this.useOverride = options.use;
    this.acquire = options.acquire ?? acquire;
    this.effectValues = options.effectValues;
  }
  return _createClass(Potion, [{
    key: "doubleDuration",
    value: function doubleDuration() {
      if (this.canDouble) {
        return new Potion(this.potion, {
          providesDoubleDuration: true,
          canDouble: this.canDouble,
          duration: this.overrideDuration,
          effect: this.overrideEffect,
          price: this.priceOverride,
          use: this.useOverride,
          acquire: this.acquire
        });
      }
      return this;
    }
  }, {
    key: "effect",
    value: function effect() {
      return this.overrideEffect ?? kolmafia.effectModifier(this.potion, "Effect");
    }
  }, {
    key: "effectDuration",
    value: function effectDuration() {
      return (this.overrideDuration ?? get$1("Effect Duration", this.potion)) * (this.providesDoubleDuration ? 2 : 1);
    }
  }, {
    key: "smithsness",
    value: function smithsness() {
      var _this$effectValues;
      return ((_this$effectValues = this.effectValues) === null || _this$effectValues === void 0 ? void 0 : _this$effectValues.smithsness) ?? get$1("Smithsness", this.effect());
    }
  }, {
    key: "meatDrop",
    value: function meatDrop() {
      var _this$effectValues2;
      return ((_this$effectValues2 = this.effectValues) === null || _this$effectValues2 === void 0 ? void 0 : _this$effectValues2.meatDrop) ?? get$1("Meat Drop", this.effect()) + (FarmingStrategy.isUnderwater() ? get$1("Meat Drop Penalty", this.effect()) : 0) + 2 * (usingPurse() ? this.smithsness() : 0);
    }
  }, {
    key: "familiarWeight",
    value: function familiarWeight() {
      var _this$effectValues3;
      return ((_this$effectValues3 = this.effectValues) === null || _this$effectValues3 === void 0 ? void 0 : _this$effectValues3.famWeight) ?? get$1("Familiar Weight", this.effect()) + (FarmingStrategy.isUnderwater() ? get$1("Hidden Familiar Weight", this.effect()) : 0);
    }
  }, {
    key: "bonusMeat",
    value: function bonusMeat() {
      return this.familiarWeight() * marginalFamWeightValue() + this.meatDrop();
    }
  }, {
    key: "gross",
    value:
    /**
     * @param targets The total number of meat targets we will be encountering
     * @param durationOverride A number lower than the normal duration of the potion to override the potion duration
     * @returns The amount of meat we expect to gain from having the effect a potion grants
     */
    function gross(targets, durationOverride) {
      var bonusMeat = this.bonusMeat();
      var duration = clamp(durationOverride ?? Infinity, this.effectDuration(), 0);
      // Number of meat targets this will actually be in effect for.
      var targetsApplied = Math.max(Math.min(duration, targets - kolmafia.haveEffect(this.effect())), 0);
      return bonusMeat / 100 * (baseMeat() * (duration - targetsApplied) * FarmingStrategy.ncAdjustment() + (baseMeat() + targetMeatDifferential()) * targetsApplied);
    }
  }, {
    key: "price",
    value: function price(historical) {
      if (this.priceOverride) return this.priceOverride(historical);
      // If asked for historical, and age < 14 days, use historical.
      // If potion is not tradeable, use retrievePrice instead
      return this.potion.tradeable ? historical && kolmafia.historicalAge(this.potion) < 14 ? kolmafia.historicalPrice(this.potion) : kolmafia.mallPrice(this.potion) : retrieveUntradeablePrice(this.potion);
    }
  }, {
    key: "net",
    value: function net(targets) {
      var historical = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : false;
      return this.gross(targets) - this.price(historical);
    }
  }, {
    key: "doublingValue",
    value: function doublingValue(targets) {
      var historical = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : false;
      return Math.min(Math.max(this.doubleDuration().net(targets, historical), 0) - Math.max(this.net(targets, historical), 0), this.price(true));
    }
  }, {
    key: "usesToCover",
    value:
    /**
     * Compute how many times to use this potion to cover the range of turns
     * @param turns the number of turns to cover
     * @param allowOverage whether or not to allow the potion to extend past this number of turns
     * @returns the number of uses required by this potion to cover thatrange
     */
    function usesToCover(turns, allowOverage) {
      if (allowOverage) {
        return Math.ceil(turns / this.effectDuration());
      } else {
        return Math.floor(turns / this.effectDuration());
      }
    }
  }, {
    key: "overage",
    value:
    /**
     * Compute how many fewer or more turns we are from the desired turn count based on the input usage
     * @param turns the number of turns to cover
     * @param uses the number of uses of hte potion
     * @returns negative number of the number of turns short, positive number of the number of extra turns
     */
    function overage(turns, uses) {
      return this.effectDuration() * uses - turns;
    }
  }, {
    key: "value",
    value:
    /**
     * Compute up to 4 possible value thresholds for this potion based on the number of meat targets to fight at the start of the day
     * - using it to only cover meat targets
     * - using it to cover both barf and meat targets (this is max 1 use)
     * - using it to only cover barf
     * - using it to cover turns in barf and those that would be lost at the end of the day
     * @param targets The number of meat targets expected to be fought in a block at the start of the day
     * @returns
     */
    function value(targets, turns, limit) {
      var startingTurns = kolmafia.haveEffect(this.effect());
      var ascending = globalOptions.ascend;
      var BUFFER_TURNS = 30;
      var totalTurns = turns ?? estimatedGarboTurns() + BUFFER_TURNS;
      var values = [];
      var limitFunction = limit ? quantity => clamp(limit - sum(values, "quantity"), 0, quantity) : quantity => quantity;

      // compute the value of covering meat targets
      var targetTurns = Math.max(0, targets - startingTurns);
      var targetQuantity = this.usesToCover(targetTurns, false);
      var targetValue = targetQuantity ? this.gross(targets) : 0;
      values.push({
        name: "target",
        quantity: limitFunction(targetQuantity),
        value: targetValue
      });

      // compute the number of meat targets missed before, and their value (along with barf unless nobarf)
      var overlapTargets = -this.overage(targetTurns, targetQuantity);
      if (overlapTargets > 0) {
        values.push({
          name: "overlap",
          quantity: limitFunction(1),
          value: this.gross(overlapTargets, globalOptions.nobarf ? overlapTargets : undefined)
        });
      }
      var targetCoverage = targetQuantity + (overlapTargets > 0 ? 1 : 0) * this.effectDuration();
      if (!globalOptions.nobarf) {
        // unless nobarf, compute the value of barf turns
        // if ascending, break those turns that are not fully covered by a potion into their own value
        var remainingTurns = Math.max(0, totalTurns - targetCoverage - startingTurns);
        var barfQuantity = this.usesToCover(remainingTurns, !ascending);
        values.push({
          name: "barf",
          quantity: limitFunction(barfQuantity),
          value: this.gross(0)
        });
        if (ascending && this.overage(remainingTurns, barfQuantity) < 0) {
          var ascendingTurns = Math.max(0, remainingTurns - barfQuantity * this.effectDuration());
          values.push({
            name: "ascending",
            quantity: limitFunction(1),
            value: this.gross(0, ascendingTurns)
          });
        }
      }
      return values.filter(tier => tier.quantity > 0);
    }
  }, {
    key: "_use",
    value: function _use(quantity) {
      if (this.useOverride) {
        return this.useOverride(quantity);
      } else if (kolmafia.itemType(this.potion) === "potion") {
        return kolmafia.use(quantity, this.potion);
      } else {
        // must provide an override for non potions, otherwise they won't use
        return false;
      }
    }
  }, {
    key: "use",
    value: function use(quantity) {
      var effectTurns = kolmafia.haveEffect(this.effect());
      var result = this._use(quantity);
      // If we tried wishing but failed, no longer try this wish in the future
      if (this.potion === $item`pocket wish` && kolmafia.haveEffect(this.effect()) <= effectTurns) {
        failedWishes.push(this.effect());
      }
      return result;
    }
  }], [{
    key: "bonusMeat",
    value: function bonusMeat(item) {
      return new Potion(item).bonusMeat();
    }
  }, {
    key: "gross",
    value: function gross(item, targets) {
      return new Potion(item).gross(targets);
    }
  }, {
    key: "net",
    value: function net(item, targets) {
      var historical = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : false;
      return new Potion(item).net(targets, historical);
    }
  }, {
    key: "doublingValue",
    value: function doublingValue(item, targets) {
      var historical = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : false;
      return new Potion(item).doublingValue(targets, historical);
    }
  }, {
    key: "usesToCover",
    value: function usesToCover(item, turns, allowOverage) {
      return new Potion(item).usesToCover(turns, allowOverage);
    }
  }, {
    key: "overage",
    value: function overage(item, turns, uses) {
      return new Potion(item).overage(turns, uses);
    }
  }]);
}();
function useAsValuable(potion, targets, targetsOnly) {
  var value = potion.value(targets);
  var price = potion.price(false);
  var amountsAcquired = value.map(value => value.value - price > 0 ? potion.acquire(value.quantity, potion.potion, value.value, false, undefined, true) : 0);
  var total = sumNumbers(amountsAcquired);
  if (total > 0) {
    var effect = potion.effect();
    if (isSong(effect) && !have$P(effect)) {
      var _iterator2 = _createForOfIteratorHelper(getActiveSongs()),
        _step2;
      try {
        var _loop2 = function _loop2() {
          var song = _step2.value;
          var slot = Mood.defaultOptions.songSlots.find(slot => slot.includes(song));
          if (!slot || slot.includes(effect)) {
            kolmafia.cliExecute(`shrug ${song}`);
          }
        };
        for (_iterator2.s(); !(_step2 = _iterator2.n()).done;) {
          _loop2();
        }
      } catch (err) {
        _iterator2.e(err);
      } finally {
        _iterator2.f();
      }
    }
    kolmafia.print(`Using ${total} ${potion.potion.plural}`);
    potion.use(total);
  }
  return total;
}
var rufusPotion = new Potion($item`closed-circuit pay phone`, {
  providesDoubleDuration: false,
  canDouble: false,
  effect: $effect`Shadow Waters`,
  duration: 30,
  price: historical => {
    if (!have$P($item`closed-circuit pay phone`)) return Infinity;
    var target = rufusTarget();
    var haveItemQuest = get$2("rufusQuestType") === "items" && target instanceof kolmafia.Item;
    var haveArtifact = get$2("rufusQuestType") === "artifact" && target instanceof kolmafia.Item && have$P(target);

    // We will only buff up if we can complete the item quest
    if (!(!target || haveItemQuest || haveArtifact || have$P($item`Rufus's shadow lodestone`))) {
      return Infinity;
    }

    // If we are overdrunk, we will need to be able to grab the NC (with a wineglass)
    if (kolmafia.myInebriety() > kolmafia.inebrietyLimit() && (!have$P($item`Drunkula's wineglass`) || !kolmafia.canEquip($item`Drunkula's wineglass`))) {
      return Infinity;
    }

    // We consider the average price of the shadow items to not get gated behind an expensive one
    var shadowItems = $items`shadow brick, shadow ice, shadow sinew, shadow glass, shadow stick, shadow skin, shadow flame, shadow fluid, shadow sausage, shadow bread, shadow venom, shadow nectar`;
    var averagePrice = sum(shadowItems, it => historical && kolmafia.historicalAge(it) < 14 ? kolmafia.historicalPrice(it) : kolmafia.mallPrice(it)) / shadowItems.length;
    return 3 * averagePrice;
  },
  acquire: qty => {
    if (kolmafia.myInebriety() > kolmafia.inebrietyLimit()) {
      kolmafia.equip($slot`weapon`, $item.none);
      kolmafia.equip($slot`off-hand`, $item`Drunkula's wineglass`);
    }
    for (var iteration = 0; iteration < qty; iteration++) {
      // Grab a lodestone if we don't have one
      if (!have$P($item`Rufus's shadow lodestone`)) {
        // If we currently have no quest, acquire one
        chooseQuest(() => 3);
        aprilFoolsRufus();

        // If we need to acquire items, do so; then complete the quest
        var target = rufusTarget();
        if (get$2("rufusQuestType") === "items") {
          if (acquire(3, target, 2 * kolmafia.mallPrice(target), false, 100000)) {
            withChoice(1498, 1, () => kolmafia.use($item`closed-circuit pay phone`));
          } else break;
        } else if (get$2("rufusQuestType") === "artifact") {
          if (have$P(target)) {
            withChoice(1498, 1, () => kolmafia.use($item`closed-circuit pay phone`));
          } else break;
        }
      }

      // Grab the buff from the NC
      var curTurncount = kolmafia.myTurncount();
      if (have$P($item`Rufus's shadow lodestone`)) {
        withChoice(1500, 2, () => kolmafia.adv1(bestShadowRift(), -1, ""));
      }
      if (kolmafia.myTurncount() > curTurncount) {
        throw new Error("Failed to acquire Shadow Waters and spent a turn!");
      }
    }
    kolmafia.setLocation($location.none); // Reset location to not affect mafia's item drop calculations
    return 0;
  },
  use: () => {
    return false;
  }
});
var wishPotions = wishableEffects.map(effect => new Potion($item`pocket wish`, {
  effect,
  canDouble: false,
  duration: 20,
  use: quantity => {
    for (var i = 0; i < quantity; i++) {
      var madeValidWish = kolmafia.cliExecute(`genie effect ${effect}`);
      if (!madeValidWish) return false;
    }
    return true;
  }
}));
var pawPotions = Array.from(validPawWishes.keys()).filter(effect => kolmafia.numericModifier(effect, "Meat Drop") >= 100).map(effect => new Potion($item`cursed monkey's paw`, {
  effect,
  canDouble: false,
  price: () => !have$l() || wishes() === 0 || failedWishes.includes(effect) ? 2 ** 100 // Something large but non-infinite for sorting reasons
  : 0,
  duration: 30,
  acquire: () => wishes() ? 1 : 0,
  use: () => {
    if (!have$l() || wishes() === 0 || failedWishes.includes(effect)) {
      return false;
    }
    if (!isWishable(effect)) return false;
    if (!kolmafia.monkeyPaw(effect)) {
      failedWishes.push(effect);
      return false;
    }
    return true;
  }
}));
var farmingPotions = () => [].concat(_toConsumableArray(kolmafia.Item.all().filter(item => item.tradeable && !banned.includes(item) && kolmafia.itemType(item) === "potion").map(item => new Potion(item)).filter(potion => potion.bonusMeat() > 0)), _toConsumableArray(wishPotions), [new Potion($item`papier-mâché toothpicks`)], _toConsumableArray(have$P($item`closed-circuit pay phone`) ? [rufusPotion] : []));
function getFarmingPotions() {
  var avoidStats = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : false;
  return avoidStats ? farmingPotions().filter(potion => !improvesAStat(potion.effect())) : farmingPotions();
}
function doublingPotions(targets) {
  return farmingPotions().filter(potion => potion.doubleDuration().gross(targets) / potion.price(true) > 0.5).map(potion => {
    return {
      potion: potion,
      value: potion.doublingValue(targets)
    };
  }).sort((a, b) => b.value - a.value).map(pair => pair.potion);
}
function usePawWishes(singleUseValuation) {
  while (wishes() > 0) {
    // Sort the paw potions by the profits of a single wish, then use the best one
    var madeValidWish = pawPotions.sort((a, b) => singleUseValuation(b) - singleUseValuation(a)).some(potion => potion.use(1));
    if (!madeValidWish) return;
  }
}
function useBusks() {
  if (!have$c() || get$2("_beretBuskingUses") >= 5 || kolmafia.myInebriety() < kolmafia.inebrietyLimit() // Do not use busks until after dieting, prioritize Salty Mouth
  ) {
    return;
  }
  for (var i = get$2("_beretBuskingUses"); i < 5; i++) {
    buskFor(beretEffectValue, {});
  }
}
function sweatEquity() {
  if (!have$b() || safeSweatEquityCasts() === 0) {
    return;
  }

  // TODO Use new libram cast function for this when it's added
  cast($skill`BCZ: Sweat Equity`, safeSweatEquityCasts());
}
var completedPotionSetup = false;
function potionSetupCompleted() {
  return completedPotionSetup;
}
/**
 * Determines if potions are worth using by comparing against meat-equilibrium. Considers using pillkeeper to double them. Accounts for non-wanderer targets. Does not account for PYEC/LTC, or running out of turns with the ascend flag.
 * @param targetsOnly Are we valuing the potions only for meat targets (noBarf)?
 */
function potionSetup(targetsOnly) {
  var avoidStats = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : false;
  castAugustScepterBuffs();
  useBusks();
  sweatEquity();
  // TODO: Count PYEC.
  // TODO: Count free fights (25 meat each for most).
  withLocation($location.none, () => {
    var targets = highMeatMonsterCount();
    if (have$P($item`Eight Days a Week Pill Keeper`) && !get$2("_freePillKeeperUsed")) {
      var possibleDoublingPotions = doublingPotions(targets);
      var bestPotion = possibleDoublingPotions.length > 0 ? possibleDoublingPotions[0] : undefined;
      if (bestPotion && bestPotion.doubleDuration().net(targets) > pillkeeperOpportunityCost()) {
        kolmafia.print(`Determined that ${bestPotion.potion} was the best potion to double`, HIGHLIGHT);
        kolmafia.cliExecute("pillkeeper extend");
        bestPotion.acquire(1, bestPotion.potion, bestPotion.doubleDuration().gross(targets));
        bestPotion.use(1);
      }
    }

    // Only test potions which are reasonably close to being profitable using historical price.
    var testPotions = getFarmingPotions(avoidStats).filter(potion => potion.gross(targets) / potion.price(true) > 0.5);
    var nonWishTestPotions = testPotions.filter(potion => potion.potion !== $item`pocket wish`);
    nonWishTestPotions.sort((a, b) => b.net(targets) - a.net(targets));
    var excludedEffects = new Set();
    var _iterator3 = _createForOfIteratorHelper(getActiveEffects()),
      _step3;
    try {
      for (_iterator3.s(); !(_step3 = _iterator3.n()).done;) {
        var effect = _step3.value;
        var _iterator6 = _createForOfIteratorHelper(mutuallyExclusive.get(effect) ?? []),
          _step6;
        try {
          for (_iterator6.s(); !(_step6 = _iterator6.n()).done;) {
            var excluded = _step6.value;
            excludedEffects.add(excluded);
          }
        } catch (err) {
          _iterator6.e(err);
        } finally {
          _iterator6.f();
        }
      }
    } catch (err) {
      _iterator3.e(err);
    } finally {
      _iterator3.f();
    }
    var _iterator4 = _createForOfIteratorHelper(nonWishTestPotions),
      _step4;
    try {
      for (_iterator4.s(); !(_step4 = _iterator4.n()).done;) {
        var potion = _step4.value;
        var _effect = potion.effect();
        if (!excludedEffects.has(_effect) && useAsValuable(potion, targets, targetsOnly) > 0) {
          var _iterator7 = _createForOfIteratorHelper(mutuallyExclusive.get(_effect) ?? []),
            _step7;
          try {
            for (_iterator7.s(); !(_step7 = _iterator7.n()).done;) {
              var _excluded = _step7.value;
              excludedEffects.add(_excluded);
            }
          } catch (err) {
            _iterator7.e(err);
          } finally {
            _iterator7.f();
          }
        }
      }
    } catch (err) {
      _iterator4.e(err);
    } finally {
      _iterator4.f();
    }
    usePawWishes(potion => {
      var value = potion.value(targets);
      return value.length > 0 ? maxBy(value, _ref8 => {
        var quantity = _ref8.quantity,
          value = _ref8.value;
        return quantity > 0 ? value : 0;
      }).value : 0;
    });
    var wishTestPotions = testPotions.filter(potion => potion.potion === $item`pocket wish`);
    wishTestPotions.sort((a, b) => b.net(targets) - a.net(targets));
    var _iterator5 = _createForOfIteratorHelper(wishTestPotions),
      _step5;
    try {
      for (_iterator5.s(); !(_step5 = _iterator5.n()).done;) {
        var _potion = _step5.value;
        var _effect2 = _potion.effect();
        if (!excludedEffects.has(_effect2) && !failedWishes.includes(_effect2) && useAsValuable(_potion, targets, targetsOnly) > 0) {
          var _iterator8 = _createForOfIteratorHelper(mutuallyExclusive.get(_effect2) ?? []),
            _step8;
          try {
            for (_iterator8.s(); !(_step8 = _iterator8.n()).done;) {
              var _excluded2 = _step8.value;
              excludedEffects.add(_excluded2);
            }
          } catch (err) {
            _iterator8.e(err);
          } finally {
            _iterator8.f();
          }
        }
      }
    } catch (err) {
      _iterator5.e(err);
    } finally {
      _iterator5.f();
    }
    variableMeatPotionsSetup(0, targets, avoidStats);
    completedPotionSetup = true;
  });
}

/**
 * Uses a Greenspan iff profitable; does not account for PYEC/LTC, or running out of adventures with the ascend flag.
 * @param targets Do we want to account for targets when calculating the value of bathroom finance?
 */
function bathroomFinance(targets) {
  if (have$P($effect`Buy!  Sell!  Buy!  Sell!`)) return;

  // Average meat % for targets is sum of arithmetic series, 2 * sum(1 -> targets)
  var averageTargetGross = targetMeat() * 2 * (targets + 1) / 2 / 100;
  var targetGross = averageTargetGross * targets;
  var tourists = 100 - targets;

  // Average meat % for tourists is sum of arithmetic series, 2 * sum(targets + 1 -> 100)
  var averageTouristGross = baseMeat() * 2 * (100 + targets + 1) / 2 / 100;
  var touristGross = averageTouristGross * tourists;
  var greenspan = $item`Uncle Greenspan's Bathroom Finance Guide`;
  if (touristGross + targetGross > kolmafia.mallPrice(greenspan)) {
    acquire(1, greenspan, touristGross + targetGross, false);
    if (kolmafia.itemAmount(greenspan) > 0) {
      kolmafia.print(`Using ${greenspan}!`, HIGHLIGHT);
      kolmafia.use(greenspan);
    }
  }
}
function triangleNumber(b) {
  var a = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : 0;
  return 0.5 * (b * (b + 1) - a * (a + 1));
}
var VariableMeatPotion = /*#__PURE__*/function () {
  // Number of turns to cap out variable bonus
  // meat% bonus per turn

  function VariableMeatPotion(potion, softcap, meatBonusPerTurn, duration, effect) {
    _classCallCheck(this, VariableMeatPotion);
    this.potion = potion;
    this.effect = effect ?? kolmafia.effectModifier(potion, "Effect");
    this.duration = duration ?? kolmafia.numericModifier(potion, "Effect Duration");
    this.softcap = softcap;
    this.meatBonusPerTurn = meatBonusPerTurn;
    this.cappedMeatBonus = softcap * meatBonusPerTurn;
  }
  return _createClass(VariableMeatPotion, [{
    key: "use",
    value: function use(quantity) {
      acquire(quantity, this.potion, 1.2 * kolmafia.retrievePrice(this.potion, quantity) / quantity, false, 2000000);
      if (kolmafia.availableAmount(this.potion) < quantity) return false;
      return kolmafia.use(quantity, this.potion);
    }
  }, {
    key: "price",
    value: function price(historical) {
      // If asked for historical, and age < 14 days, use historical.
      // If potion is not tradeable, use retrievePrice instead
      return this.potion.tradeable ? historical && kolmafia.historicalAge(this.potion) < 14 ? kolmafia.historicalPrice(this.potion) : kolmafia.mallPrice(this.potion) : retrieveUntradeablePrice(this.potion);
    }
  }, {
    key: "getOptimalNumberToUse",
    value: function getOptimalNumberToUse(yachtzees, targets) {
      var barfTurns = Math.max(0, estimatedGarboTurns() + BUFFER_TURNS - yachtzees - targets);
      var potionAmountsToConsider = [];
      var considerSoftcap = [0, this.softcap];
      var considerTargets = targets > 0 ? [0, targets] : [0];
      for (var _i2 = 0, _arr = [Math.floor, Math.ceil]; _i2 < _arr.length; _i2++) {
        var fn = _arr[_i2];
        var _iterator9 = _createForOfIteratorHelper(considerSoftcap),
          _step9;
        try {
          for (_iterator9.s(); !(_step9 = _iterator9.n()).done;) {
            var sc = _step9.value;
            var _iterator0 = _createForOfIteratorHelper(considerTargets),
              _step0;
            try {
              for (_iterator0.s(); !(_step0 = _iterator0.n()).done;) {
                var em = _step0.value;
                var considerBarfTurns = em === targets && barfTurns > 0 ? [0, barfTurns] : [0];
                for (var _i3 = 0, _considerBarfTurns = considerBarfTurns; _i3 < _considerBarfTurns.length; _i3++) {
                  var bt = _considerBarfTurns[_i3];
                  var potionAmount = fn((yachtzees + em + bt + sc) / this.duration);
                  if (!potionAmountsToConsider.includes(potionAmount)) {
                    potionAmountsToConsider.push(potionAmount);
                  }
                }
              }
            } catch (err) {
              _iterator0.e(err);
            } finally {
              _iterator0.f();
            }
          }
        } catch (err) {
          _iterator9.e(err);
        } finally {
          _iterator9.f();
        }
      }
      var profitsFromPotions = potionAmountsToConsider.map(quantity => ({
        quantity,
        value: this.valueNPotions(quantity, yachtzees, targets, barfTurns)
      }));
      var bestOption = maxBy(profitsFromPotions, "value");
      if (bestOption.value > 0) {
        kolmafia.print(`Expected to profit ${bestOption.value.toFixed(2)} from ${bestOption.quantity} ${this.potion.plural}`, "blue");
        var ascendingOverlap = globalOptions.ascend || globalOptions.nobarf ? 0 : this.softcap / this.duration;
        var potionsToUse = bestOption.quantity + ascendingOverlap - Math.floor(kolmafia.haveEffect(this.effect) / this.duration);
        return Math.max(potionsToUse, 0);
      }
      return 0;
    }
  }, {
    key: "valueNPotions",
    value: function valueNPotions(n, yachtzees, targets, barfTurns) {
      var yachtzeeValue = 2000;
      var targetValue = targetMeat();
      var barfValue = FarmingStrategy.accountForNC() ? baseMeat() * FarmingStrategy.turnsToNC() / 30 : baseMeat();
      var totalCosts = kolmafia.retrievePrice(this.potion, n);
      var totalDuration = n * this.duration;
      var cappedDuration = Math.max(0, totalDuration - this.softcap + 1);
      var decayDuration = Math.min(totalDuration, this.softcap - 1);
      var totalValue = 0;
      var turnTypes = [[yachtzees, yachtzeeValue], [targets, targetValue], [barfTurns, barfValue]];
      for (var _i4 = 0, _turnTypes = turnTypes; _i4 < _turnTypes.length; _i4++) {
        var _turnTypes$_i = _slicedToArray(_turnTypes[_i4], 2),
          turns = _turnTypes$_i[0],
          value = _turnTypes$_i[1];
        var cappedTurns = Math.min(cappedDuration, turns);
        var decayTurns = Math.min(decayDuration, turns - cappedTurns);
        totalValue += value * (cappedTurns * this.cappedMeatBonus + triangleNumber(decayDuration, decayDuration - decayTurns) * this.meatBonusPerTurn) / 100;
        cappedDuration -= cappedTurns;
        decayDuration -= decayTurns;
        if (decayDuration === 0) break;
      }
      return totalValue - totalCosts;
    }
  }]);
}();
function variableMeatPotionsSetup(yachtzees, targets) {
  var avoidStats = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : false;
  var potions = [].concat(_toConsumableArray(avoidStats ? [] : [new VariableMeatPotion($item`love song of sugary cuteness`, 20, 2)]), [new VariableMeatPotion($item`pulled yellow taffy`, 50, 2)], _toConsumableArray(globalOptions.prefs.candydish ? [new VariableMeatPotion($item`porcelain candy dish`, 500, 1)] : []));
  var excludedEffects = new Set();
  var _iterator1 = _createForOfIteratorHelper(getActiveEffects()),
    _step1;
  try {
    for (_iterator1.s(); !(_step1 = _iterator1.n()).done;) {
      var effect = _step1.value;
      var _iterator11 = _createForOfIteratorHelper(mutuallyExclusive.get(effect) ?? []),
        _step11;
      try {
        for (_iterator11.s(); !(_step11 = _iterator11.n()).done;) {
          var excluded = _step11.value;
          excludedEffects.add(excluded);
        }
      } catch (err) {
        _iterator11.e(err);
      } finally {
        _iterator11.f();
      }
    }
  } catch (err) {
    _iterator1.e(err);
  } finally {
    _iterator1.f();
  }
  var _iterator10 = _createForOfIteratorHelper(potions),
    _step10;
  try {
    for (_iterator10.s(); !(_step10 = _iterator10.n()).done;) {
      var potion = _step10.value;
      var _effect3 = kolmafia.effectModifier(potion.potion, "Effect");
      var n = excludedEffects.has(_effect3) ? 0 : potion.getOptimalNumberToUse(yachtzees, targets);
      if (n > 0) {
        potion.use(n);
        var _iterator12 = _createForOfIteratorHelper(mutuallyExclusive.get(_effect3) ?? []),
          _step12;
        try {
          for (_iterator12.s(); !(_step12 = _iterator12.n()).done;) {
            var _excluded3 = _step12.value;
            excludedEffects.add(_excluded3);
          }
        } catch (err) {
          _iterator12.e(err);
        } finally {
          _iterator12.f();
        }
      }
    }
  } catch (err) {
    _iterator10.e(err);
  } finally {
    _iterator10.f();
  }
}

/**
 * WARNING: Expensive
 */
function effectValue(effect, duration, maxTurnsWanted) {
  var targets = arguments.length > 3 && arguments[3] !== undefined ? arguments[3] : highMeatMonsterCount("Scepter");
  if (effect === $effect`Shadow Affinity`) {
    return globalOptions.prefs.valueOfFreeFight * duration; // Each turn of Shadow Affinity gives us one free fight
  }
  if (effect === $effect`Loded` && realmAvailable("hot")) {
    return 3400 * duration; // 70s Mining is 3400 VoA, which will always be higher than the meat% in current climate
  }
  var durationOverride = maxTurnsWanted ? clamp(maxTurnsWanted - kolmafia.haveEffect(effect), 0, duration) : undefined;
  return new Potion($item.none, {
    duration,
    effect
  }).gross(targets, durationOverride);
}
function effectExtenderValue(duration, maximumNumberOfEffects) {
  var targets = highMeatMonsterCount();
  var turns = estimatedGarboTurns() + BUFFER_TURNS;
  return sum(getActiveEffects(), effect => {
    var skill = kolmafia.toSkill(effect);
    if (skill !== $skill`none` && have$P(skill) && skill.dailylimit === -1) {
      return 0; // If we have an unlimited skill to cast it, there's no value in extending
    }
    return effectValue(effect, duration, turns, targets);
  }) * (maximumNumberOfEffects ? Math.min(1, maximumNumberOfEffects / getActiveEffects().length) : 1);
}

var bestAlternative = get$1("Meat Drop", $item`amulet coin`);
// Constant Value familiars are those that drop items at a constant rate without limit, compare Rotating Value familiars
var standardFamiliars = [{
  familiar: $familiar`Obtuse Angel`,
  value: () => 0.02 * garboValue($item`time's arrow`)
}, {
  familiar: $familiar`Stocking Mimic`,
  value: mode => garboAverageValue.apply(void 0, _toConsumableArray($items`Polka Pop, BitterSweetTarts, Piddles`)) / 6 - (
  // We can't equip an amulet coin if we equip the bag of many confections
  mode === "barf" ? bestAlternative * baseMeat() / 100 : 0) + (1 / 3 + (have$P($effect`Jingle Jangle Jingle`) ? 0.1 : 0)) * totalFamiliarWeight($familiar`Stocking Mimic`)
}, {
  familiar: $familiar`Shorter-Order Cook`,
  value: () => garboAverageValue.apply(void 0, _toConsumableArray($items`short beer, short stack of pancakes, short stick of butter, short glass of water, short white`)) / 11 // 9 with blue plate
}, {
  familiar: $familiar`Robortender`,
  value: mode => {
    var olfactedMonster = get$2("olfactedMonster");
    var olfactedIsFromBarf = olfactedMonster && kolmafia.getMonsters($location`Barf Mountain`).includes(olfactedMonster);
    return dropChance() * garboValue(dropFrom(mode === "barf" && olfactedIsFromBarf ? olfactedMonster : mode === "target" ? globalOptions.target : $monster.none)) + (currentDrinks().includes($item`Feliz Navidad`) ? felizValue() * 0.25 : 0) + (currentDrinks().includes($item`Newark`) ? newarkValue() * 0.25 : 0);
  }
}, {
  familiar: $familiar`Twitching Space Critter`,
  // Item is ludicrously overvalued and incredibly low-volume.
  // We can remove this cap once the price reaches a lower equilibrium
  // we probably won't, but we can.
  value: () => Math.min(garboValue($item`twitching space egg`) * 0.0002, 690)
}, {
  familiar: $familiar`Hobo Monkey`,
  value: () => 75
}, {
  familiar: $familiar`Trick-or-Treating Tot`,
  // This is the value of getting a pirate costume over getting an amulet coin or whatever
  value: mode => have$P($item`li'l pirate costume`) && mode === "barf" ? baseMeat() * (300 - bestAlternative) / 100 : 0
}, {
  familiar: $familiar`Cookbookbat`,
  value: mode => 3 * garboAverageValue.apply(void 0, _toConsumableArray($items`Vegetable of Jarlsberg, Yeast of Boris, St. Sneaky Pete's Whey`)) / 11 + (mode === "barf" ? cookbookbatPerilBonus() : 0) // We cannot run the turn spending task during our start of day freefights, so cannot guarantee this value
}, {
  familiar: $familiar`Unspeakachu`,
  value: () => {
    return effectExtenderValue(5) * 0.5 * 0.05;
  }
}, {
  familiar: $familiar`Patriotic Eagle`,
  value: () => kolmafia.holiday().includes("Dependence Day") ? 0.05 * garboValue($item`souvenir flag`) : 0,
  worksOnFreeRun: true
}, {
  familiar: $familiar`Mini Kiwi`,
  value: mode => mode === "barf" ? 0 // Handled in outfit caching code
  : clamp(totalFamiliarWeight($familiar`Mini Kiwi`) * 0.005, 0, 1) * garboValue($item`mini kiwi`) // faster with aviator goggles
}, {
  familiar: $familiar`Quantum Entangler`,
  value: () => garboValue($item`quantized familiar experience`) / 11
}, {
  familiar: $familiar`Peace Turkey`,
  value: () =>
  // drops are ~1/2 of the activations, whirled peas are twice as likely to drop
  garboAverageValue.apply(void 0, _toConsumableArray($items`whirled peas, whirled peas, piece of cake, peace shooter`)) * peaceTurkeyDropChance() / 2,
  worksOnFreeRun: true
}, {
  familiar: $familiar`XO Skeleton`,
  value: () => garboAverageValue.apply(void 0, _toConsumableArray($items`X, O`)) / 9 // counters for X & O are simultaneous but offset by 5
}];
function peaceTurkeyDropChance() {
  return 0.24 + kolmafia.squareRoot(totalFamiliarWeight($familiar`Peace Turkey`)) / 100;
}
function getConstantValueFamiliars(mode) {
  return standardFamiliars.filter(_ref => {
    var familiar = _ref.familiar;
    return have$P(familiar);
  }).map(_ref2 => {
    var familiar = _ref2.familiar,
      value = _ref2.value,
      _ref2$worksOnFreeRun = _ref2.worksOnFreeRun,
      worksOnFreeRun = _ref2$worksOnFreeRun === void 0 ? false : _ref2$worksOnFreeRun;
    return {
      familiar,
      worksOnFreeRun,
      expectedValue: value(mode),
      leprechaunMultiplier: findLeprechaunMultiplier(familiar),
      limit: "none"
    };
  });
}
var locationsWithMonsters = kolmafia.Location.all().filter(l => kolmafia.getMonsters(l).length > 0);
function cookbookbatPerilBonus() {
  if (!have$P($item`Peridot of Peril`) || get$2("_cookbookbatCombatsUntilNewQuest") + 1 > estimatedGarboTurns()) {
    return 0;
  }
  // canAdventure includes some zones we need to exclude
  var canAdvExclusions = $locations`Fastest Adventurer Contest, Strongest Adventurer Contest, Smartest Adventurer Contest, Smoothest Adventurer Contest, Hottest Adventurer Contest, Coldest Adventurer Contest, Spookiest Adventurer Contest, Stinkiest Adventurer Contest, Sleaziest Adventurer Contest, The Hedge Maze, Tower Level 1, Tower Level 2, Tower Level 3, Tower Level 5, The Naughty Sorceress' Chamber, The Daily Dungeon, An Overgrown Shrine (Northwest), An Overgrown Shrine (Southwest), An Overgrown Shrine (Northeast), An Overgrown Shrine (Southeast), A Crater Full of Space Beasts, Mt. Molehill, The Red Queen's Garden, An Incredibly Strange Place (Bad Trip), An Incredibly Strange Place (Mediocre Trip), An Incredibly Strange Place (Great Trip), The Primordial Soup, The Jungles of Ancient Loathing, Seaside Megalopolis, Domed City of Ronaldus, Domed City of Grimacia, Hamburglaris Shield Generator, The X-32-F Combat Training Snowman, The Haiku Dungeon, The Deep Machine Tunnels, Shadow Rift, The Island Barracks`;
  if (!have$P($effect`Ultrahydrated`)) {
    canAdvExclusions.push($location`The Oasis`);
  }
  var cookbookbatQuestLocations = locationsWithMonsters.filter(l => canAdventureOrUnlock(l, false) && !canAdvExclusions.includes(l));
  var availablePeridotCookbookbatLocations = cookbookbatQuestLocations.filter(l => canImperil(l) && !unperidotableZones.includes(l));
  var doableQuestChance = availablePeridotCookbookbatLocations.length / cookbookbatQuestLocations.length;
  var averageCookbookbatRewardValue = 3 * garboAverageValue.apply(void 0, _toConsumableArray($items`Vegetable of Jarlsberg, Yeast of Boris, St. Sneaky Pete's Whey`));

  // It takes 5 turns to get a quest, times the chance we hit a zone we can do with peridot. Assume worst case of spending a turn to complete the quest
  return Math.max(0, (averageCookbookbatRewardValue * doableQuestChance - get$2("valueOfAdventure")) / 5);
}

function expectedTurnsValue(expected, index) {
  return Array.isArray(expected) ? expected[index] : expected(index);
}
function dropValue(drop) {
  return drop instanceof kolmafia.Item ? garboValue(drop) : garboAverageValue.apply(void 0, _toConsumableArray(drop));
}
function valueStandardDropFamiliar(_ref) {
  var familiar = _ref.familiar,
    expected = _ref.expected,
    drop = _ref.drop,
    additionalValue = _ref.additionalValue,
    _ref$worksOnFreeRun = _ref.worksOnFreeRun,
    worksOnFreeRun = _ref$worksOnFreeRun === void 0 ? false : _ref$worksOnFreeRun;
  var expectedTurns = expectedTurnsValue(expected, familiar.dropsToday) || Infinity;
  var expectedValue = dropValue(drop) / expectedTurns + ((additionalValue === null || additionalValue === void 0 ? void 0 : additionalValue()) ?? 0);
  return {
    familiar,
    expectedValue,
    leprechaunMultiplier: findLeprechaunMultiplier(familiar),
    limit: "drops",
    worksOnFreeRun
  };
}

// Rotating Value familiars are those whose drop rate changes, compare Constant Value familiars
var rotatingFamiliars = [{
  familiar: $familiar`Fist Turkey`,
  expected: [3.91, 4.52, 4.52, 5.29, 5.29],
  drop: $item`Ambitious Turkey`
}, {
  familiar: $familiar`Llama Lama`,
  expected: [3.42, 3.91, 4.52, 5.29, 5.29],
  drop: $item`llama lama gong`
}, {
  familiar: $familiar`Astral Badger`,
  expected: [3.03, 3.42, 3.91, 4.52, 5.29],
  drop: $item`astral mushroom`
}, {
  familiar: $familiar`Li'l Xenomorph`,
  expected: [3.03, 3.42, 3.91, 4.52, 5.29],
  drop: $item`transporter transponder`
}, {
  familiar: $familiar`Rogue Program`,
  expected: [3.03, 3.42, 3.91, 4.52, 5.29],
  drop: $item`Game Grid token`
}, {
  familiar: $familiar`Bloovian Groose`,
  expected: [3.03, 3.42, 3.91, 4.52, 5.29],
  drop: $item`groose grease`
}, {
  familiar: $familiar`Baby Sandworm`,
  expected: [3.03, 3.42, 3.91, 4.52, 5.29],
  drop: $item`agua de vida`
}, {
  familiar: $familiar`Green Pixie`,
  expected: id => have$P($effect`Absinthe-Minded`) ? Infinity : [3.03, 3.42, 3.91, 4.52, 5.29][id] ?? Infinity,
  drop: $item`tiny bottle of absinthe`
}, {
  familiar: $familiar`Blavious Kloop`,
  expected: [3.03, 3.42, 3.91, 4.52, 5.29],
  drop: $item`devilish folio`
}, {
  familiar: $familiar`Galloping Grill`,
  expected: [3.03, 3.42, 3.91, 4.52, 5.29],
  drop: $item`hot ashes`
}, {
  familiar: $familiar`Grim Brother`,
  expected: [3.03, 3.42, 3.91, 4.52, 5.29],
  drop: $item`grim fairy tale`
}, {
  familiar: $familiar`Golden Monkey`,
  expected: [3.03, 3.42, 3.91, 4.52, 5.29],
  drop: $item`powdered gold`
}, {
  familiar: $familiar`Unconscious Collective`,
  expected: [3.03, 3.42, 3.91, 4.52, 5.29],
  drop: $item`Unconscious Collective Dream Jar`
}, {
  familiar: $familiar`Ms. Puck Man`,
  expected: Array($familiar`Ms. Puck Man`.dropsLimit).fill(12.85),
  drop: $item`power pill`,
  additionalValue: () => garboValue($item`yellow pixel`)
}, {
  familiar: $familiar`Puck Man`,
  expected: Array($familiar`Puck Man`.dropsLimit).fill(12.85),
  drop: $item`power pill`,
  additionalValue: () => garboValue($item`yellow pixel`)
}, {
  familiar: $familiar`Adventurous Spelunker`,
  expected: [7.0],
  drop: $item`Tales of Spelunking`
}, {
  familiar: $familiar`Angry Jung Man`,
  expected: [30.0],
  drop: $item`psychoanalytic jar`
}, {
  familiar: $familiar`Grimstone Golem`,
  expected: [45.0],
  drop: $item`grimstone mask`
}, {
  familiar: $familiar`Cookbookbat`,
  expected: [33.0],
  drop: [$item`Recipe of Before Yore: Deep Dish of Legend`, $item`Recipe of Before Yore: Pizza of Legend`, $item`Recipe of Before Yore: Calzone of Legend`, $item`Recipe of Before Yore: plain calzone`, $item`Recipe of Before Yore: roasted vegetable focaccia`, $item`Recipe of Before Yore: baked veggie ricotta`, $item`Recipe of Before Yore: roasted vegetable of J.`, $item`Recipe of Before Yore: Pete's rich ricotta`, $item`Recipe of Before Yore: Boris's bread`, $item`Recipe of Before Yore: Boris's beer`, $item`Recipe of Before Yore: honey bun of Boris`, $item`Recipe of Before Yore: ratatouille de Jarlsberg`, $item`Recipe of Before Yore: Jarlsberg's vegetable soup`, $item`Recipe of Before Yore: Pete's wily whey bar`, $item`Recipe of Before Yore: St. Pete's sneaky smoothie`],
  additionalValue: () => 3 * garboAverageValue.apply(void 0, _toConsumableArray($items`Vegetable of Jarlsberg, Yeast of Boris, St. Sneaky Pete's Whey`)) / 11
}, {
  familiar: $familiar`Hobo in Sheep's Clothing`,
  expected: i => 10 * i + 10,
  // faster with half-height cigar
  drop: $item`grubby wool`
}, {
  familiar: $familiar`Jill-of-All-Trades`,
  expected: i => 3 * Math.pow(20, i),
  drop: $item`map to a candy-rich block`,
  additionalValue: () => (6 + 4 * totalFamiliarWeight($familiar`Jill-of-All-Trades`)) * 0.33
}, {
  familiar: $familiar`Rockin' Robin`,
  expected: i => i === $familiar`Rockin' Robin`.dropsToday ? clamp(30 - get$2("rockinRobinProgress"), 1, 30) : 30,
  drop: $item`robin's egg`
}, {
  familiar: $familiar`Optimistic Candle`,
  expected: i => i === $familiar`Optimistic Candle`.dropsToday ? clamp(30 - get$2("optimisticCandleProgress"), 1, 30) : 30,
  drop: $item`glob of melted wax`
}, {
  familiar: $familiar`Garbage Fire`,
  expected: i => i === $familiar`Garbage Fire`.dropsToday ? clamp(30 - get$2("garbageFireProgress"), 1, 30) : 30,
  drop: $items`burning newspaper, extra-toasted half sandwich, mulled hobo wine`
}];
function getDropFamiliars() {
  return rotatingFamiliars.map(valueStandardDropFamiliar).filter(_ref2 => {
    var familiar = _ref2.familiar,
      expectedValue = _ref2.expectedValue,
      leprechaunMultiplier = _ref2.leprechaunMultiplier;
    return have$P(familiar) && (expectedValue || leprechaunMultiplier);
  });
}
function getAllDrops(fam) {
  var target = rotatingFamiliars.find(_ref3 => {
    var familiar = _ref3.familiar;
    return familiar === fam;
  });
  if (!have$P(fam) || !target) return [];
  var expected = target.expected,
    drop = target.drop,
    additionalValue = target.additionalValue;
  var current = fam.dropsToday;
  var returnValue = [];
  var length = Array.isArray(expected) ? expected.length : 11; // 11 seems a reasonable max
  for (var i = current; i < length; i++) {
    var turns = expectedTurnsValue(target.expected, i);
    returnValue.push({
      expectedValue: dropValue(drop) / turns + ((additionalValue === null || additionalValue === void 0 ? void 0 : additionalValue()) ?? 0),
      expectedTurns: turns
    });
  }
  return returnValue;
}

var isUsed = (used, mode) => typeof used === "string" ? get$2(used) : used(mode);
var experienceFamiliars = [{
  familiar: $familiar`Pocket Professor`,
  used: "_thesisDelivered",
  useValue: 11 * get$2("valueOfAdventure"),
  baseExp: 200
}, {
  familiar: $familiar`Grey Goose`,
  used: "_meatifyMatterUsed",
  useValue: 15 ** 4,
  baseExp: 25
}, {
  familiar: $familiar`Chest Mimic`,
  used: mode => !shouldChargeMimic(mode === "barf"),
  useValue: () => MEAT_TARGET_MULTIPLIER() * get$2("valueOfAdventure"),
  baseExp: 0,
  xpCost: 50,
  xpLimit: mode => mimicExperienceNeeded(mode === "barf")
}, {
  familiar: $familiar`Cooler Yeti`,
  used: () => {
    return $familiar`Cooler Yeti`.experience >= 400 || globalOptions.ascend || !globalOptions.prefs.chargeYeti;
  },
  // Vintage Smart Drink is 40 adventures
  useValue: getAverageAdventures($item`vintage smart drink`) * get$2("valueOfAdventure"),
  baseExp: 0
}];
function valueExperienceFamiliar(_ref, mode) {
  var familiar = _ref.familiar,
    useValue = _ref.useValue,
    xpCost = _ref.xpCost,
    baseExp = _ref.baseExp;
  var currentExp = familiar.experience || (have$P($familiar`Shorter-Order Cook`) ? 100 : 0);
  var experienceNeeded = xpCost ?? 400 - (globalOptions.ascend ? currentExp : baseExp);
  var estimatedExperience = mode === "free" ? 12 : estimatedBarfExperience();
  return {
    familiar,
    expectedValue: undelay(useValue) / (experienceNeeded / estimatedExperience),
    leprechaunMultiplier: findLeprechaunMultiplier(familiar),
    limit: "experience",
    worksOnFreeRun: false
  };
}
function getExperienceFamiliars(mode) {
  return experienceFamiliars.filter(_ref2 => {
    var used = _ref2.used,
      familiar = _ref2.familiar,
      xpLimit = _ref2.xpLimit;
    return have$P(familiar) && !isUsed(used, mode) && familiar.experience < ((xpLimit === null || xpLimit === void 0 ? void 0 : xpLimit(mode)) ?? 400);
  }).map(f => valueExperienceFamiliar(f, mode));
}
function getExperienceFamiliarLimit(fam) {
  var _target$xpLimit;
  var target = experienceFamiliars.find(_ref3 => {
    var familiar = _ref3.familiar;
    return familiar === fam;
  });
  if (!have$P(fam) || !target) return 0;
  return ((((_target$xpLimit = target.xpLimit) === null || _target$xpLimit === void 0 ? void 0 : _target$xpLimit.call(target, "barf")) ?? 400) - fam.experience) / estimatedBarfExperience();
}

// Familiars that should never be introduced as a surprise
var NO_TCB_FAMILIARS = $familiars`Mini-Hipster, Artistic Goth Kid`;
function getToyCupidBowFamiliars() {
  if (!have$f()) return [];
  var skipFamiliars = getUsedTcbFamiliars();
  var _iterator = _createForOfIteratorHelper(NO_TCB_FAMILIARS),
    _step;
  try {
    for (_iterator.s(); !(_step = _iterator.n()).done;) {
      var familiar = _step.value;
      skipFamiliars.add(familiar);
    }

    // If there aren't enough turns to run someone to completion, only check for the current cupid familiar
  } catch (err) {
    _iterator.e(err);
  } finally {
    _iterator.f();
  }
  var current = currentFamiliar();
  if (current && estimatedGarboTurns() < tcbTurnsLeft(current, skipFamiliars)) {
    var _current = currentFamiliar();
    if (!_current) return [];
    if (skipFamiliars.has(_current)) return [];
    return [{
      familiar: _current,
      expectedValue: familiarEquipmentValue(_current) / tcbTurnsLeft(_current, skipFamiliars),
      worksOnFreeRun: true,
      limit: "cupid",
      leprechaunMultiplier: findLeprechaunMultiplier(_current)
    }];
  }

  // Otherwise find the best for each leprechaun multiplier
  var bestFamiliarsByLeprechaunMultiplier = new Map();
  var _iterator2 = _createForOfIteratorHelper(kolmafia.Familiar.all()),
    _step2;
  try {
    for (_iterator2.s(); !(_step2 = _iterator2.n()).done;) {
      var _bestFamiliarsByLepre;
      var _familiar = _step2.value;
      if (!have$P(_familiar)) continue;
      if (skipFamiliars.has(_familiar)) continue;
      if (!kolmafia.familiarEquipment(_familiar).tradeable && _familiar !== $familiar`Cornbeefadon`) {
        continue;
      }
      if (_familiar === $familiar`Mini-Adventurer` && !get$2("miniAdvClass") && !get$2("choiceAdventure768")) {
        if (globalOptions.ascend) {
          propertyManager.setChoice(768, 4);
        } // Littlest identity crisis, sauceror
        else continue;
      }
      if (_familiar === $familiar`Doppelshifter`) continue;
      var leprechaunMultiplier = findLeprechaunMultiplier(_familiar);
      var expectedValue = familiarEquipmentValue(_familiar) / tcbTurnsLeft(_familiar, skipFamiliars);
      var currentBestValue = ((_bestFamiliarsByLepre = bestFamiliarsByLeprechaunMultiplier.get(leprechaunMultiplier)) === null || _bestFamiliarsByLepre === void 0 ? void 0 : _bestFamiliarsByLepre.expectedValue) ?? 0;
      if (expectedValue > currentBestValue) {
        bestFamiliarsByLeprechaunMultiplier.set(leprechaunMultiplier, {
          familiar: _familiar,
          expectedValue,
          worksOnFreeRun: true,
          limit: "cupid",
          leprechaunMultiplier
        });
      }
    }
  } catch (err) {
    _iterator2.e(err);
  } finally {
    _iterator2.f();
  }
  return _toConsumableArray(bestFamiliarsByLeprechaunMultiplier.values());
}

var _wanderer;
function wanderer() {
  if (!_wanderer) {
    _wanderer = new WandererManager({
      ascend: globalOptions.ascend,
      estimatedTurns: estimatedGarboTurns,
      itemValue: garboValue,
      effectValue,
      prioritizeCappingGuzzlr: get$2("garbo_prioritizeCappingGuzzlr", false),
      freeFightExtraValue: location => freeFightFamiliarData(location).expectedValue,
      digitzesRemaining: digitizedMonstersRemainingForTurns,
      plentifulMonsters: [globalOptions.target].concat(_toConsumableArray(globalOptions.nobarf ? [] : FarmingStrategy.monsters()), _toConsumableArray(have$P($item`Kramco Sausage-o-Matic™`) ? $monsters`sausage goblin` : [])),
      valueOfAdventure: get$2("valueOfAdventure"),
      takeTurnForProfit: true,
      canRefractedGaze: have$b() && safeRefractedCasts() > 0
    });
  }
  return _wanderer;
}
var destinationToLocation = destination => destination instanceof kolmafia.Location ? destination : wanderer().getTarget(destination).location;
function toAdventure(arg) {
  if (arg instanceof kolmafia.Monster) return {
    target: arg,
    location: $location.none
  };
  if (arg instanceof kolmafia.Location) return {
    target: arg,
    location: arg
  };
  if (typeof arg === "string" || !("target" in arg)) {
    var location = wanderer().getTarget(arg).location;
    return {
      target: location,
      location
    };
  }
  return {
    target: arg.target,
    location: destinationToLocation(arg.location)
  };
}

function menu$1(adventure) {
  var _ref = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {},
    _ref$canChooseMacro = _ref.canChooseMacro,
    canChooseMacro = _ref$canChooseMacro === void 0 ? true : _ref$canChooseMacro,
    _ref$extraFamiliars = _ref.extraFamiliars,
    extraFamiliars = _ref$extraFamiliars === void 0 ? [] : _ref$extraFamiliars,
    _ref$excludeFamiliar = _ref.excludeFamiliar,
    excludeFamiliar = _ref$excludeFamiliar === void 0 ? [] : _ref$excludeFamiliar,
    _ref$includeExperienc = _ref.includeExperienceFamiliars,
    includeExperienceFamiliars = _ref$includeExperienc === void 0 ? true : _ref$includeExperienc,
    _ref$allowAttackFamil = _ref.allowAttackFamiliars,
    allowAttackFamiliars = _ref$allowAttackFamil === void 0 ? true : _ref$allowAttackFamil,
    _ref$mode = _ref.mode,
    mode = _ref$mode === void 0 ? "free" : _ref$mode;
  var familiarMenu = [].concat(_toConsumableArray(getConstantValueFamiliars(mode)), _toConsumableArray(getDropFamiliars()), _toConsumableArray(getToyCupidBowFamiliars()), _toConsumableArray(includeExperienceFamiliars ? getExperienceFamiliars(mode) : []), _toConsumableArray(extraFamiliars));
  var _toAdventure = toAdventure(adventure),
    target = _toAdventure.target;
  var monsterRates = adventureTargetToWeightedMap(target);
  if (canChooseMacro && kolmafia.myInebriety() <= kolmafia.inebrietyLimit()) {
    if (timeToMeatify()) {
      familiarMenu.push({
        familiar: $familiar`Grey Goose`,
        expectedValue: (Math.max(kolmafia.familiarWeight($familiar`Grey Goose`) - 5), 0) ** 4,
        leprechaunMultiplier: 0,
        limit: "experience",
        worksOnFreeRun: false
      });
    }
    if (mode === "target" && gooseDroneEligible() && get$2("gooseDronesRemaining") < copyTargetCount()) {
      familiarMenu.push({
        familiar: $familiar`Grey Goose`,
        expectedValue:
        // It takes 9 experience to go from level 5 to 6 and emit a drone
        clamp(get$1("Familiar Experience") / 9, 0,
        // The limit to how valuable any emission can be is how many drones are actually gonna hit the copyTarget
        copyTargetCount() - get$2("gooseDronesRemaining")) * valueDrops(globalOptions.target),
        leprechaunMultiplier: 0,
        limit: "experience",
        worksOnFreeRun: false
      });
    }
    if (mode === "target" && have$A()) {
      familiarMenu.push({
        familiar: $familiar`Red-Nosed Snapper`,
        expectedValue: snapperValue(),
        leprechaunMultiplier: 0,
        limit: "special",
        worksOnFreeRun: false
      });
    }
    if (canOpenRedPresent()) {
      familiarMenu.push({
        familiar: $familiar`Crimbo Shrub`,
        expectedValue: 2500,
        leprechaunMultiplier: 0,
        limit: "special",
        worksOnFreeRun: true
      });
    }
    if (have$P($familiar`Space Jellyfish`)) {
      familiarMenu.push({
        familiar: $familiar`Space Jellyfish`,
        expectedValue: sum(_toConsumableArray(monsterRates.entries()), _ref2 => {
          var _ref3 = _slicedToArray(_ref2, 2),
            monster = _ref3[0],
            rate = _ref3[1];
          return monster.defenseElement === $element`Stench` ? rate * garboValue($item`stench jelly`) / (get$2("_spaceJellyfishDrops") < 5 ? get$2("_spaceJellyfishDrops") + 1 : 20) : 0;
        }),
        leprechaunMultiplier: 0,
        limit: "special",
        worksOnFreeRun: true
      });
    }
  }
  if (have$a()) {
    familiarMenu.push({
      familiar: $familiar`Skeleton of Crimbo Past`,
      expectedValue: expectedBones(target) * garboValue($item`knucklebone`),
      leprechaunMultiplier: 0,
      limit: "special",
      worksOnFreeRun: false
    });
  }
  var meatFam = meatFamiliar();
  familiarMenu.push({
    familiar: meatFam,
    expectedValue: 0,
    leprechaunMultiplier: findLeprechaunMultiplier(meatFam),
    limit: "none",
    // Because strictly speaking this is better than using no familiar at all
    worksOnFreeRun: true
  });
  return familiarMenu.filter(_ref4 => {
    var familiar = _ref4.familiar,
      worksOnFreeRun = _ref4.worksOnFreeRun;
    return (mode !== "run" || worksOnFreeRun) && (allowAttackFamiliars || !(familiar.physicalDamage || familiar.elementalDamage)) && !excludeFamiliar.some(excludedFamiliar => excludedFamiliar === familiar);
  });
}
function getAllJellyfishDrops() {
  if (!have$P($familiar`Space Jellyfish`)) {
    return [{
      expectedValue: 0,
      turnsAtValue: 0
    }];
  }
  var current = get$2("_spaceJellyfishDrops");
  var returnValue = [];
  for (var dropNumber = clamp(current + 1, 0, 6); dropNumber <= 6; dropNumber++) {
    returnValue.push({
      expectedValue: garboValue($item`stench jelly`) / (dropNumber > 5 ? 20 : dropNumber),
      turnsAtValue: dropNumber > 5 ? Infinity : dropNumber
    });
  }
  return returnValue;
}
function freeFightFamiliarData(adventure) {
  var options = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {};
  var usedTcbFamiliars = getUsedTcbFamiliars();
  var compareFamiliars = (a, b) => {
    if (a === null) return b;
    var aValue = a.expectedValue + tcbValue(a.familiar, usedTcbFamiliars, options.equipmentForced);
    var bValue = b.expectedValue + tcbValue(b.familiar, usedTcbFamiliars, options.equipmentForced);
    if (aValue === bValue) {
      return a.leprechaunMultiplier > b.leprechaunMultiplier ? a : b;
    }
    return aValue > bValue ? a : b;
  };
  return menu$1(adventure, options).reduce(compareFamiliars, {
    familiar: $familiar.none,
    expectedValue: 0,
    leprechaunMultiplier: 0,
    limit: "none",
    worksOnFreeRun: true
  });
}
function freeFightFamiliar(target) {
  var options = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {};
  return freeFightFamiliarData(target, options).familiar;
}

var crate = $monster`crate`;
function expectedGregs(skillSource) {
  var habitatCharges = have$P($skill`Just the Facts`) ? 3 - get$2("_monsterHabitatsRecalled", 0) : 0;
  var habitatGregs = new Array(habitatCharges).fill({
    copies: 5,
    skillSource: "habitat",
    replaces: 0,
    extra: 0
  });
  var extroGregs = new Array(50).fill({
    copies: 3,
    skillSource: "extro",
    replaces: 0,
    extra: 0
  });
  var baseGregs = [].concat(_toConsumableArray(habitatGregs), _toConsumableArray(extroGregs));
  var replacementsPerGreg = source => have$P($skill`Transcendent Olfaction`) ? Math.floor(source.copies * 7 / 3) : Math.floor(source.copies * 5 / 3);
  var timeSpunGregs = have$P($item`Time-Spinner`) ? Math.floor((10 - get$2("_timeSpinnerMinutesUsed")) / 3) : 0;
  var orbGregs = have$P($item`miniature crystal ball`) ? 1 : 0;
  var macrometeors = have$P($skill`Meteor Lore`) ? 10 - get$2("_macrometeoriteUses") : 0;
  var replaceEnemies = have$P($item`Powerful Glove`) ? Math.floor((100 - get$2("_powerfulGloveBatteryPowerUsed")) / 10) : 0;
  var firstReplaces = clamp(replacementsPerGreg(baseGregs[0]), 0, macrometeors + replaceEnemies);
  var initialCast = {
    replacesLeft: macrometeors + replaceEnemies - firstReplaces,
    sources: [_objectSpread2(_objectSpread2({}, baseGregs[0]), {}, {
      replaces: firstReplaces,
      extra: timeSpunGregs + orbGregs
    })]
  };
  return baseGregs.slice(1).reduce((acc, curr) => {
    var currentReplaces = clamp(replacementsPerGreg(curr), 0, acc.replacesLeft);
    return {
      replacesLeft: acc.replacesLeft - currentReplaces,
      sources: [].concat(_toConsumableArray(acc.sources), [_objectSpread2(_objectSpread2({}, curr), {}, {
        replaces: currentReplaces,
        extra: orbGregs
      })])
    };
  }, initialCast).sources.filter(source => source.skillSource === skillSource).map(source => source.copies + source.replaces + source.extra);
}
function doingGregFight() {
  var extrovermectin = get$2("beGregariousCharges") > 0 || get$2("beGregariousFightsLeft") > 0 && get$2("beGregariousMonster") === globalOptions.target || !globalOptions.dietCompleted && !globalOptions.nodiet && hasMonsterReplacers();
  var habitat = have$P($skill`Just the Facts`) && (get$2("_monsterHabitatsRecalled") < 3 || get$2("_monsterHabitatsFightsLeft") > 0 && get$2("_monsterHabitatsMonster") === globalOptions.target);
  return extrovermectin || habitat;
}
var isOlfacted = monster => get$2("olfactedMonster") === monster && have$P($effect`On the Trail`);
var isConned = monster => get$2("longConMonster") === monster;
var isTurtlesexed = monster => get$2("_gallapagosMonster") === monster;
var isLatted = monster => sniffedMonster() === monster;
function crateStrategy() {
  if (!doingGregFight()) return null;
  if (have$P($skill`Transcendent Olfaction`) && (isOlfacted(crate) || get$2("_olfactionsUsed") < 2) || have$P($skill`Long Con`) && (isConned(crate) || get$2("_longConUsed") < 4)) {
    return "Sniff";
  }
  if (have$P($item`miniature crystal ball`)) return "Orb";
  if (have$P($item`Fourth of May Cosplay Saber`) && get$2("_saberForceUses") < 5) {
    return "Saber";
  }
  return null;
}
function hasMonsterReplacers() {
  return have$P($skill`Meteor Lore`) && get$2("_macrometeoriteUses") < 10 || have$P($item`Powerful Glove`) && get$2("_powerfulGloveBatteryPowerUsed") < 90;
}

/**
 * Saberfriends a crate if we are able to do so.
 */
function saberCrateIfSafe() {
  var canSaber = have$P($item`Fourth of May Cosplay Saber`) && get$2("_saberForceUses") < 5;
  var isSafeToSaber = !gregReady() || get$2("_saberForceMonsterCount") > 0;
  if (!canSaber || !isSafeToSaber) return;
  var run = tryFindFreeRun(freeRunConstraints({
    equip: $items`Fourth of May Cosplay Saber`
  })) ?? ltbRun();
  do {
    var _run$constraints$fami, _run$constraints, _run$constraints$prep, _run$constraints2, _run$constraints$equi, _run$constraints3;
    kolmafia.useFamiliar(((_run$constraints$fami = (_run$constraints = run.constraints).familiar) === null || _run$constraints$fami === void 0 ? void 0 : _run$constraints$fami.call(_run$constraints)) ?? freeFightFamiliar($location`Noob Cave`, {
      canChooseMacro: false
    }));
    (_run$constraints$prep = (_run$constraints2 = run.constraints).preparation) === null || _run$constraints$prep === void 0 || _run$constraints$prep.call(_run$constraints2);
    new Requirement([], {
      forceEquip: $items`Fourth of May Cosplay Saber`,
      preventEquip: $items`Kramco Sausage-o-Matic™`
    }).merge(((_run$constraints$equi = (_run$constraints3 = run.constraints).equipmentRequirements) === null || _run$constraints$equi === void 0 ? void 0 : _run$constraints$equi.call(_run$constraints3)) ?? new Requirement([], {})).maximize();
    setChoice(1387, 2);
    garboAdventure($location`Noob Cave`, Macro.if_(crate, Macro.skill($skill`Use the Force`)).if_($monster`sausage goblin`, Macro.kill()).ifInnateWanderer(Macro.step(run.macro)).abort());
  } while (lastAdventureWasWeird());
}

/**
 * Equip the miniature crystal ball if the current prediction is good for us.
 */
function equipOrbIfDesired() {
  if (have$P($item`miniature crystal ball`) && !(get$2("_saberForceMonster") === crate && get$2("_saberForceMonsterCount") > 0) && crateStrategy() !== "Sniff" && [undefined, crate].includes(ponder().get($location`Noob Cave`))) {
    kolmafia.equip($slot`familiar`, $item`miniature crystal ball`);
  }
}

/**
 * Pre-olfact/saber crates, for extrovermectin/gregarious reasons.
 */
function initializeCrates() {
  do {
    // We use the force while olfacting sometimes, so we'll need to refresh mafia's knowledge of olfaction
    if (getString("olfactedMonster") !== "crate") {
      kolmafia.visitUrl(`desc_effect.php?whicheffect=${$effect`On the Trail`.descid}`);
    }
    // if we have olfaction, that's our primary method for ensuring crates
    if (crateStrategy() === "Sniff" && !isOlfacted(crate) && !isConned(crate) || crateStrategy() === "Orb" && (have$P($skill`Gallapagosian Mating Call`) && !isTurtlesexed(crate) || have$P($item`latte lovers member's mug`) && !isLatted(crate))) {
      var _run$constraints$fami2, _run$constraints4, _run$constraints$prep2, _run$constraints5, _run$constraints$equi2, _run$constraints6;
      var possibleBanish = ltbRun();
      var run = tryFindFreeRun(freeRunConstraints({
        equip: $items`latte lovers member's mug, Fourth of May Cosplay Saber`.filter(item => have$P(item))
      })) ?? possibleBanish;
      setChoice(1387, 2); // use the force, in case we decide to use that

      // Sniff the crate in as many ways as humanly possible
      var sniffrun = Macro.trySkill($skill`Transcendent Olfaction`).trySkill($skill`Long Con`).trySkill($skill`Offer Latte to Opponent`).externalIf(!isTurtlesexed(crate), Macro.trySkill($skill`Gallapagosian Mating Call`)).trySkill($skill`Use the Force`).trySkill($skill`CLEESH`).step(run.macro);

      // equip latte and saber for lattesniff and saberfriends, if we want to
      // Crank up ML to make sure the crate survives several rounds; we may have some passive damage
      kolmafia.useFamiliar(((_run$constraints$fami2 = (_run$constraints4 = run.constraints).familiar) === null || _run$constraints$fami2 === void 0 ? void 0 : _run$constraints$fami2.call(_run$constraints4)) ?? freeFightFamiliar($location`Noob Cave`, {
        canChooseMacro: false
      }));
      (_run$constraints$prep2 = (_run$constraints5 = run.constraints).preparation) === null || _run$constraints$prep2 === void 0 || _run$constraints$prep2.call(_run$constraints5);
      new Requirement(["100 Monster Level"], {
        forceEquip: $items`latte lovers member's mug, Fourth of May Cosplay Saber`.filter(item => have$P(item)),
        preventEquip: $items`carnivorous potted plant`
      }).merge(((_run$constraints$equi2 = (_run$constraints6 = run.constraints).equipmentRequirements) === null || _run$constraints$equi2 === void 0 ? void 0 : _run$constraints$equi2.call(_run$constraints6)) ?? new Requirement([], {})).maximize();
      garboAdventure($location`Noob Cave`, Macro.if_(crate, sniffrun).ifInnateWanderer(Macro.step(run.macro)).abort());
      kolmafia.visitUrl(`desc_effect.php?whicheffect=${$effect`On the Trail`.descid}`);
      if (run === possibleBanish && !have$P($skill`CLEESH`)) {
        var _run$constraints$fami3, _run$constraints7, _run$constraints$prep3, _run$constraints8, _run$constraints$equi3, _run$constraints9;
        kolmafia.useFamiliar(((_run$constraints$fami3 = (_run$constraints7 = run.constraints).familiar) === null || _run$constraints$fami3 === void 0 ? void 0 : _run$constraints$fami3.call(_run$constraints7)) ?? freeFightFamiliar($location`The Haunted Kitchen`, {
          canChooseMacro: false
        }));
        (_run$constraints$prep3 = (_run$constraints8 = run.constraints).preparation) === null || _run$constraints$prep3 === void 0 || _run$constraints$prep3.call(_run$constraints8);
        new Requirement([], {
          preventEquip: $items`Kramco Sausage-o-Matic™`
        }).merge(((_run$constraints$equi3 = (_run$constraints9 = run.constraints).equipmentRequirements) === null || _run$constraints$equi3 === void 0 ? void 0 : _run$constraints$equi3.call(_run$constraints9)) ?? new Requirement([], {})).maximize();
        do {
          garboAdventure($location`The Haunted Kitchen`, run.macro);
        } while (lastAdventureWasWeird({
          extraEncounters: ["Lights Out in the Kitchen"],
          includeHolidayWanderers: false
        }));
      }
    } else if (crateStrategy() === "Saber" && (get$2("_saberForceMonster") !== crate || get$2("_saberForceMonsterCount") === 0) && get$2("_saberForceUses") < 5) {
      saberCrateIfSafe();
    } else break; // we can break the loop if there's nothing to do
  } while (!["crate", "Using the Force"].includes(get$2("lastEncounter")) && !kolmafia.isBanished(crate)); // loop until we actually hit a crate

  if (kolmafia.isBanished($monster`crate`.phylum)) {
    throw new Error("Accidentally banished crate phylum! And failed to unbanish.");
  } else if (kolmafia.isBanished($monster`crate`)) {
    throw new Error("Accidentally banished crate! And failed to unbanish.");
  }
}
function getClub() {
  if (have$P($skill`Iron Palm Technique`) && !have$P($effect`Iron Palms`)) {
    kolmafia.useSkill($skill`Iron Palm Technique`);
  }
  var availableClub = kolmafia.Item.all().find(i => have$P(i) && kolmafia.canEquip(i) && kolmafia.weaponHands(i) === 2 && (kolmafia.itemType(i) === "club" || have$P($effect`Iron Palms`) && kolmafia.itemType(i) === "sword")) ?? $item`amok putter`;
  kolmafia.retrieveItem(availableClub);
  return availableClub;
}
var MAX_BANISH_PRICE = 100000; // price of nanobrawny

var combatItem = (item, maxPrice) => ({
  name: `${item}`,
  available: () => getAcquirePrice(item) < (maxPrice ?? MAX_BANISH_PRICE),
  macro: () => Macro.item(item),
  price: () => getAcquirePrice(item),
  prepare: () => acquire(1, item, maxPrice ?? MAX_BANISH_PRICE) // put a sanity ceiling of 50k on the banish
});
function springKickBanish(adventure) {
  var run = tryFindFreeRunOrBanish(freeRunConstraints({
    equip: $items`spring shoes`
  })) ?? ltbRun();
  return {
    name: "Spring Kick",
    available: () => have$P($item`spring shoes`),
    price: () => run.cost(),
    macro: () => Macro.skill($skill`Spring Kick`).step(run.macro),
    prepare: () => {
      var _run$constraints$fami4, _run$constraints0, _run$constraints$prep4, _run$constraints1, _run$constraints$equi4, _run$constraints10;
      kolmafia.useFamiliar(((_run$constraints$fami4 = (_run$constraints0 = run.constraints).familiar) === null || _run$constraints$fami4 === void 0 ? void 0 : _run$constraints$fami4.call(_run$constraints0)) ?? freeFightFamiliar(adventure, {
        canChooseMacro: false,
        allowAttackFamiliars: false
      }));
      (_run$constraints$prep4 = (_run$constraints1 = run.constraints).preparation) === null || _run$constraints$prep4 === void 0 || _run$constraints$prep4.call(_run$constraints1);
      // To prevent death of both self and monster
      new Requirement(["100 Monster Level, 100 Muscle"], {
        preventEquip: $items`carnivorous potted plant, Kramco Sausage-o-Matic™`,
        forceEquip: [$item`spring shoes`]
      }).merge(((_run$constraints$equi4 = (_run$constraints10 = run.constraints).equipmentRequirements) === null || _run$constraints$equi4 === void 0 ? void 0 : _run$constraints$equi4.call(_run$constraints10)) ?? new Requirement([], {})).maximize();
    }
  };
}
var longBanishes = [combatItem($item`human musk`), combatItem($item`tryptophan dart`), combatItem($item`Daily Affirmation: Be a Mind Master`), {
  name: "Batter Up!",
  available: () => kolmafia.myFury() >= 5 && have$P($skill`Batter Up!`),
  price: () => get$2("valueOfAdventure"),
  // Batter up takes an adventure, cost is slightly higher than this because of effect cost
  macro: () => Macro.skill($skill`Batter Up!`),
  prepare: () => {
    var club = getClub();
    new Requirement(["100 Monster Level"], {
      preventEquip: $items`carnivorous potted plant`,
      forceEquip: [club]
    }).maximize();
  }
}, {
  name: "Nanobrawny",
  available: () => true,
  price: () => kolmafia.mallPrice($item`pocket wish`) * 2,
  // could be 3 if you are unlucky
  macro: () => Macro.skill($skill`Unleash Nanites`),
  prepare: () => {
    while (kolmafia.haveEffect($effect`Nanobrawny`) < 40) {
      acquire(1, $item`pocket wish`, 50000);
      kolmafia.cliExecute(`genie effect Nanobrawny`);
    }
  }
}];
var freeBunnyBanish = {
  name: "Mafia Middle Finger Ring",
  available: () => !get$2("_mafiaMiddleFingerRingUsed"),
  macro: () => Macro.skill($skill`Show them your ring`),
  prepare: () => {
    new Requirement([], {
      preventEquip: $items`carnivorous potted plant`,
      forceEquip: [$item`mafia middle finger ring`]
    }).maximize();
  }
};
var iceHouseBanish = {
  name: "Ice House",
  available: () => true,
  macro: () => Macro.item($item`ice house`),
  prepare: () => acquire(1, $item`ice house`, 1000000)
};
var shortBanishes = [combatItem($item`Louder Than Bomb`, 10000), combatItem($item`tennis ball`, 10000), combatItem($item`handful of split pea soup`, 10000)];
function iceHouseAllowed() {
  if (get$2("garboDisallowIceHouseNotify", false) || globalOptions.prefs.autoUserConfirm) {
    return false;
  }
  if (userConfirmDialog("Would you like to allow garbo to ice house a fluffy bunny? This saves significant costs on banishers in the long run.", false)) {
    return true;
  }
  _set("garboDisallowIceHouseNotify", true);
  return false;
}
function banishBunny() {
  var banishes = [].concat(longBanishes, [springKickBanish($location`The Dire Warren`)], _toConsumableArray(!have$P($item`miniature crystal ball`) ? shortBanishes : [])).filter(b => b.available());
  var usingIceHouseBanish = getBanishedMonsters().get($item`ice house`) !== $monster`fluffy bunny` && kolmafia.retrievePrice($item`ice house`) < 1000000 && iceHouseAllowed();
  var banish = usingIceHouseBanish ? iceHouseBanish : getUsingFreeBunnyBanish() ? freeBunnyBanish : maxBy(banishes, banish => {
    var _banish$price;
    return ((_banish$price = banish.price) === null || _banish$price === void 0 ? void 0 : _banish$price.call(banish)) ?? 0;
  }, true);
  do {
    var _banish$prepare;
    (_banish$prepare = banish.prepare) === null || _banish$prepare === void 0 || _banish$prepare.call(banish);
    garboAdventure($location`The Dire Warren`, Macro.if_($monster`fluffy bunny`, banish.macro()).target("fluffy bunny banish"));
  } while ("fluffy bunny" !== get$2("lastEncounter") && !get$2("banishedMonsters").includes("fluffy bunny"));
}
function getBanishedPhyla() {
  var phylumBanish = new Map();
  var banishPart = get$2("banishedPhyla").split(":").slice(0, 2);
  if (banishPart.length === 2) {
    var _banishPart = _slicedToArray(banishPart, 2),
      skill = _banishPart[0],
      phylum = _banishPart[1];
    // for now, the only phylum banish is Patriotic Screech, a skill
    phylumBanish.set(kolmafia.toSkill(skill), kolmafia.toPhylum(phylum));
  }
  return phylumBanish;
}
function initializeDireWarren() {
  kolmafia.visitUrl("museum.php?action=icehouse");
  var banishedMonsters = getBanishedMonsters();
  var banishedPhyla = getBanishedPhyla();
  if (_toConsumableArray(banishedMonsters.values()).find(m => m === $monster`fluffy bunny`)) {
    return;
  }
  if (_toConsumableArray(banishedPhyla.values()).find(p => p === $monster`fluffy bunny`.phylum)) {
    return;
  }
  banishBunny();
}
function initializeExtrovermectinZones() {
  if (get$2("beGregariousFightsLeft") === 0) {
    if (hasMonsterReplacers()) initializeCrates();
    initializeDireWarren();
  }
}
function gregReady() {
  return get$2("beGregariousMonster") === globalOptions.target && get$2("beGregariousFightsLeft") > 0 || get$2("_monsterHabitatsMonster") === globalOptions.target && get$2("_monsterHabitatsFightsLeft") > 0;
}
function totalGregCharges(countPartial) {
  var extroPartial = get$2("beGregariousFightsLeft") > 0 ? 1 : 0;
  var habitatPartial = get$2("_monsterHabitatsFightsLeft") > 0 ? 1 : 0;
  return get$2("beGregariousCharges") + (have$P($skill`Just the Facts`) ? 3 - get$2("_monsterHabitatsRecalled") : 0) + (extroPartial + habitatPartial );
}
function possibleGregCrystalBall() {
  if (have$P($item`miniature crystal ball`)) {
    var ponderCount = ponder().get($location`The Dire Warren`) === globalOptions.target ? 1 : 0;
    return totalGregCharges() + ponderCount;
  }
  return 0;
}

var nextWeekReady = () => clubIntoNextWeekAvailable() && (turnsUntilNextWeekFight() <= 0 || !clubIntoNextWeekMonster());
var nextWeekFights = () => clubIntoNextWeekAvailable() + (clubIntoNextWeekMonster() === globalOptions.target ? 1 : 0);

/**
 * Configure the behavior of the fights in use in different parts of the fight engine
 * @interface TargetFightConfigOptions
 * @member {OutfitSpec} spec maximizer requirements to use for this fight (defaults to empty)
 * @member {draggableFight?} draggable if this fight can be pulled into another zone and what kind of draggable it is (defaults to undefined)
 * @member {boolean?} canInitializeWandererCounters if this fight can be used to initialize wanderers (defaults to false)
 * @member {boolean?} gregariousReplace if this is a "monster replacement" fight - pulls another monster from the CSV (defautls to false)
 * @member {boolean?} wrongEncounterName if mafia does not update the lastEncounter properly when doing this fight (defaults to value of gregariousReplace)
 */

function checkUnderwater() {
  // first check to see if underwater even makes sense
  if (questStep$1("questS01OldGuy") >= 0 && !(get$2("_envyfishEggUsed") || have$P($item`envyfish egg`)) && (get$2("_garbo_weightChain", false) || !have$P($familiar`Pocket Professor`)) && (kolmafia.booleanModifier("Adventure Underwater") || waterBreathingEquipment.some(item => have$P(item) && kolmafia.canEquip(item))) && freeFishyAvailable() && !willYachtzee()) {
    if (!have$P($effect`Fishy`) && have$P($item`fishy pipe`) && !get$2("_fishyPipeUsed")) {
      kolmafia.use($item`fishy pipe`);
    }
    if (!have$P($effect`Fishy`) && get$2("skateParkStatus") === "ice" && !get$2("_skateBuff1")) {
      kolmafia.cliExecute("skate lutz");
    }
    return have$P($effect`Fishy`);
  }
  return false;
}
function getChangeLastAdvLocationMethod() {
  if (questStep$1("questL11Worship") > 3) {
    return "hiddencity";
  } else {
    return "dailydungeon";
  }
}

// for now, return a psuedo task since target fights are not grimoirized
function changeLastAdvLocationTask() {
  var base = {
    ready: () => ponder().get($location`The Dire Warren`) !== globalOptions.target,
    completed: () => kolmafia.myLocation() !== $location`The Dire Warren`
  };
  switch (getChangeLastAdvLocationMethod()) {
    case "hiddencity":
      return _objectSpread2(_objectSpread2({}, base), {}, {
        do: () => withChoice(785, 6, () => kolmafia.adv1($location`An Overgrown Shrine (Northeast)`, -1, ""))
      });
    case "dailydungeon":
      return _objectSpread2(_objectSpread2({}, base), {}, {
        do: () =>
        // at this point, we're either at an NC we can walk away from or the whole DD is done
        // only track the choices we need to walk away since hitting it when it is done does nothing
        withChoices({
          692: 8,
          693: 3
        }, () => kolmafia.adv1($location`The Daily Dungeon`, -1, ""))
      });
  }
}

var CopyTargetFight = /*#__PURE__*/function () {
  /**
   * This is the class that creates all the different ways to fight copy targets
   * @classdesc Copy Target Fight enc
   * @prop {string} name The name of the source of this fight, primarily used to identify special cases.
   * @prop {() => boolean} available Returns whether or not we can do this fight right now (this may change later in the day).
   * @prop {() => number} potential Returns the number of targets we expect to be able to fight from this source given the current state of hte character
   *  This is used when computing turns for buffs, so it should be as accurate as possible to the number of KGE we will fight
   * @prop {(options: RunOptions) => void} execute This runs the combat, optionally using the provided location and macro. Location is used only by draggable fights.
   *  This is the meat of each fight. How do you initialize the fight? Are there any special considerations?
   * @prop {TargetFightConfigOptions} options configuration options for this fight. see TargetFightConfigOptions for full details of all available options
   * @example
   * // suppose that we wanted to add a fight that will use print screens repeatedly, as long as we have them in our inventory
   * new CopyTargetFight(
   *  "Print Screen Monster",
   *  () => have($item`screencapped monster`) && get('screencappedMonster') === globalOptions.target, // in order to start this fight, a KGE must already be screen capped
   *  () => availableAmount($item`screencapped monster`) + availableAmount($item`print screen button`) // the total of potential of this fight is the number of already copied KGE + the number of potentially copiable KGE
   *  () => (options: RunOptions) => {
   *    const macro = Macro
   *      .externalIf(have($item`print screen button`), Macro.tryItem($item`print screen button`))
   *      .step(options.macro); // you should always include the macro passed in via options, as it may have special considerations for this fight
   *    withMacro(macro, () => useItem($item`screen capped monster`));
   *  },
   *  {
   *    canInitializeWandererCounts: false; // this copy cannot be used to start wanderer counters, since the combats are not adv.php
   *  }
   * )
   */
  function CopyTargetFight(name, available, potential) {
    var execute = arguments.length > 3 && arguments[3] !== undefined ? arguments[3] : options => {
      var adventureFunction = options.useAuto ? garboAdventureAuto : garboAdventure;
      adventureFunction(options.location, options.macro, options.macro);
    };
    var options = arguments.length > 4 && arguments[4] !== undefined ? arguments[4] : {};
    _classCallCheck(this, CopyTargetFight);
    this.name = name;
    this.available = available;
    this.potential = potential;
    this.execute = execute;
    this.spec = options.spec ?? {};
    this.draggable = options.draggable;
    this.canInitializeWandererCounters = options.canInitializeWandererCounters ?? false;
    this.gregariousReplace = options.gregariousReplace ?? false;
    this.wrongEncounterName = options.wrongEncounterName ?? this.gregariousReplace;
    this.location = options.location;
  }
  return _createClass(CopyTargetFight, [{
    key: "run",
    value: function run(options) {
      if (!this.available() || !kolmafia.myAdventures()) return;
      kolmafia.print(`Now running ${globalOptions.target} fight: ${this.name}. Stay tuned for details.`);
      this.execute(options);
    }
  }]);
}();
var chainStarters = [new CopyTargetFight("Witchess", () => have$H() && pieces$1.includes(globalOptions.target) && fightsDone() < 5, () => have$H() && pieces$1.includes(globalOptions.target) ? Math.max(5 - fightsDone(), 0) : 0, options => {
  withMacro(options.macro, () => fightPiece(globalOptions.target), options.useAuto);
}), new CopyTargetFight("Chateau Painting", () => have$J() && !paintingFought() && paintingMonster() === globalOptions.target, () => have$J() && !paintingFought() && paintingMonster() === globalOptions.target ? 1 : 0, options => {
  withMacro(options.macro, () => fightPainting(), options.useAuto);
}), new CopyTargetFight("Combat Lover's Locket", () => canReminisce(globalOptions.target), () => canReminisce(globalOptions.target) ? 1 : 0, options => {
  withMacro(options.macro, () => reminisce(globalOptions.target), options.useAuto);
}), new CopyTargetFight("Fax", () => have$P($item`Clan VIP Lounge key`) && !get$2("_photocopyUsed") && have$P($item`photocopied monster`) && get$2("photocopyMonster") === globalOptions.target && kolmafia.getClanLounge()["deluxe fax machine"] !== undefined, () => have$P($item`Clan VIP Lounge key`) && !get$2("_photocopyUsed") && have$P($item`photocopied monster`) && get$2("photocopyMonster") === globalOptions.target && kolmafia.getClanLounge()["deluxe fax machine"] !== undefined ? 1 : 0, options => {
  withMacro(options.macro, () => kolmafia.use($item`photocopied monster`), options.useAuto);
}), new CopyTargetFight("Mimic Eggs", () => differentiableQuantity(globalOptions.target) >= 1, () => differentiableQuantity(globalOptions.target) + clamp(Math.floor($familiar`Chest Mimic`.experience / 50), 0, 11 - get$2("_mimicEggsObtained")), options => {
  withMacro(options.macro, () => differentiate(globalOptions.target), options.useAuto);
}), new CopyTargetFight("Rain Main", () => have$P($skill`Rain Man`) && kolmafia.myRain() >= 50, () => Math.floor(kolmafia.myRain() / 50), options => {
  withMacro(options.macro, () => rainMan(globalOptions.target), options.useAuto);
})];
var TIME_SPINNER_LOCATIONS = $locations`Noob Cave, The Dire Warren, The Haunted Kitchen`;

/**
 * Whether the target is in a combat queue the Time-Spinner draws from.
 *
 * Matches entries exactly: a substring test claims a sea cow when only a sea
 * cowboy was fought.
 * @returns Whether the target is in one of those queues
 */
function targetInCombatQueue() {
  return TIME_SPINNER_LOCATIONS.some(location => location.combatQueue.split("; ").includes(globalOptions.target.name));
}
var copySources = [new CopyTargetFight("Time-Spinner", () => !timeSpinnerRefused(globalOptions.target) && have$P($item`Time-Spinner`) && targetInCombatQueue() && get$2("_timeSpinnerMinutesUsed") <= 7, () => !timeSpinnerRefused(globalOptions.target) && have$P($item`Time-Spinner`) && (targetInCombatQueue() || totalGregCharges() > 0) ? Math.floor((10 - get$2("_timeSpinnerMinutesUsed")) / 3) : 0, options => {
  withMacro(options.macro, () => {
    directlyUse($item`Time-Spinner`);
    if (travelToRecentFight(globalOptions.target)) kolmafia.runCombat();
  }, options.useAuto);
}), new CopyTargetFight("Spooky Putty & Rain-Doh", () => have$P($item`Spooky Putty monster`) && get$2("spookyPuttyMonster") === globalOptions.target || have$P($item`Rain-Doh box full of monster`) && get$2("rainDohMonster") === globalOptions.target, () => {
  var havePutty = have$P($item`Spooky Putty sheet`);
  var havePuttyMonster = have$P($item`Spooky Putty monster`);
  var haveRainDoh = have$P($item`Rain-Doh black box`);
  var haveRainDohMonster = have$P($item`Rain-Doh box full of monster`);
  var puttyUsed = get$2("spookyPuttyCopiesMade");
  var rainDohUsed = get$2("_raindohCopiesMade");
  var hardLimit = 6 - puttyUsed - rainDohUsed;
  var monsterCount = 0;
  var puttyLeft = 5 - puttyUsed;
  var rainDohLeft = 5 - rainDohUsed;
  if (!havePutty && !havePuttyMonster) {
    puttyLeft = 0;
  }
  if (!haveRainDoh && !haveRainDohMonster) {
    rainDohLeft = 0;
  }
  if (havePuttyMonster) {
    if (get$2("spookyPuttyMonster") === globalOptions.target) {
      monsterCount++;
    } else {
      puttyLeft = 0;
    }
  }
  if (haveRainDohMonster) {
    if (get$2("rainDohMonster") === globalOptions.target) {
      monsterCount++;
    } else {
      rainDohLeft = 0;
    }
  }
  var naiveLimit = Math.min(puttyLeft + rainDohLeft, hardLimit);
  return naiveLimit + monsterCount;
}, options => {
  var macro = options.macro;
  withMacro(macro, () => {
    if (have$P($item`Spooky Putty monster`)) {
      return kolmafia.use($item`Spooky Putty monster`);
    }
    return kolmafia.use($item`Rain-Doh box full of monster`);
  }, options.useAuto);
}), new CopyTargetFight("4-d Camera", () => have$P($item`shaking 4-d camera`) && get$2("cameraMonster") === globalOptions.target && !get$2("_cameraUsed"), () => have$P($item`shaking 4-d camera`) && get$2("cameraMonster") === globalOptions.target && !get$2("_cameraUsed") ? 1 : 0, options => {
  withMacro(options.macro, () => kolmafia.use($item`shaking 4-d camera`), options.useAuto);
}), new CopyTargetFight("Ice Sculpture", () => have$P($item`ice sculpture`) && get$2("iceSculptureMonster") === globalOptions.target && !get$2("_iceSculptureUsed"), () => have$P($item`ice sculpture`) && get$2("iceSculptureMonster") === globalOptions.target && !get$2("_iceSculptureUsed") ? 1 : 0, options => {
  withMacro(options.macro, () => kolmafia.use($item`ice sculpture`), options.useAuto);
}), new CopyTargetFight("Green Taffy", () => have$P($item`envyfish egg`) && get$2("envyfishMonster") === globalOptions.target && !get$2("_envyfishEggUsed"), () => have$P($item`envyfish egg`) && get$2("envyfishMonster") === globalOptions.target && !get$2("_envyfishEggUsed") ? 1 : 0, options => {
  withMacro(options.macro, () => kolmafia.use($item`envyfish egg`), options.useAuto);
}), new CopyTargetFight("Screencapped Monster", () => have$P($item`screencapped monster`) && get$2("screencappedMonster") === globalOptions.target, () => get$2("screencappedMonster") === globalOptions.target ? kolmafia.itemAmount($item`screencapped monster`) : 0, options => {
  withMacro(options.macro, () => kolmafia.use($item`screencapped monster`), options.useAuto);
}), new CopyTargetFight("Sticky Clay Homunculus", () => have$P($item`sticky clay homunculus`) && get$2("crudeMonster") === globalOptions.target, () => get$2("crudeMonster") === globalOptions.target ? kolmafia.itemAmount($item`sticky clay homunculus`) : 0, options => withMacro(options.macro, () => kolmafia.use($item`sticky clay homunculus`), options.useAuto))];
var wanderSources = [new CopyTargetFight("Lucky!", () => kolmafia.canAdventure($location`Cobb's Knob Treasury`) && have$P($effect`Lucky!`) && globalOptions.target === $monster`Knob Goblin Embezzler`, () => kolmafia.canAdventure($location`Cobb's Knob Treasury`) && have$P($effect`Lucky!`) && globalOptions.target === $monster`Knob Goblin Embezzler` ? 1 : 0, undefined, {
  location: $location`Cobb's Knob Treasury`
}), new CopyTargetFight("Digitize", () => get$2("_sourceTerminalDigitizeMonster") === globalOptions.target && get("Digitize Monster") <= 0, () => have$I() && getDigitizeUses() === 0 ? 1 : 0, undefined, {
  draggable: "wanderer"
}), new CopyTargetFight("Guaranteed Romantic Monster", () => get$2("_romanticFightsLeft") > 0 && get("Romantic Monster window begin") <= 0 && get("Romantic Monster window end") <= 0, () => 0, undefined, {
  draggable: "wanderer"
}), new CopyTargetFight("Enamorang", () => get("Enamorang") <= 0 && get$2("enamorangMonster") === globalOptions.target, () => get("Enamorang") <= 0 && get$2("enamorangMonster") === globalOptions.target || have$P($item`LOV Enamorang`) && !get$2("_enamorangs") ? 1 : 0, undefined, {
  draggable: "wanderer"
}), new CopyTargetFight("Legendary Seal Clubbing Club", () => get("Club 'Em Into Next Week Monster") <= 0 && clubIntoNextWeekMonster() === globalOptions.target, nextWeekFights, undefined, {
  draggable: "wanderer"
})];
function changeLastAdvLocation() {
  var task = changeLastAdvLocationTask();
  if (task.ready() && !task.completed()) {
    task.do();
  }
  kolmafia.visitUrl("main.php");
}
var gregFights = (name, haveCheck, monsterProp, fightsProp, totalCharges) => {
  function runGregFight(options) {
    var _run$constraints$prep, _run$constraints;
    var run = ltbRun();
    var runMacro = getUsingFreeBunnyBanish() ? Macro.skill($skill`Snokebomb`) : ltbRun().macro;
    (_run$constraints$prep = (_run$constraints = run.constraints).preparation) === null || _run$constraints$prep === void 0 || _run$constraints$prep.call(_run$constraints);
    var bunnyIsBanished = kolmafia.isBanished($monster`fluffy bunny`);
    var adventureFunction = options.useAuto ? garboAdventureAuto : garboAdventure;
    adventureFunction($location`The Dire Warren`, Macro.if_($monster`fluffy bunny`, runMacro).step(options.macro), Macro.if_($monster`fluffy bunny`, runMacro).step(options.macro));
    if (get$2("lastEncounter") === $monster`fluffy bunny`.name && bunnyIsBanished) {
      var _find;
      var bunnyBanish = (_find = _toConsumableArray(getBanishedMonsters().entries()).find(_ref => {
        var _ref2 = _slicedToArray(_ref, 2),
          monster = _ref2[1];
        return monster === $monster`fluffy bunny`;
      })) === null || _find === void 0 ? void 0 : _find[0];
      kolmafia.abort(`Fluffy bunny is supposedly banished by ${bunnyBanish}, but this appears not to be the case; the most likely issue is that your ${fightsProp} preference is nonzero and should probably be zero.`);
    }
  }
  var resourceIsOccupied = () => get$2(fightsProp) > 0 && ![null, globalOptions.target].includes(get$2(monsterProp));
  return [new CopyTargetFight(name, () => haveCheck() && !resourceIsOccupied() && get$2(fightsProp) > (have$P($item`miniature crystal ball`) ? 1 : 0), () => !resourceIsOccupied() ? totalCharges() : 0, options => {
    runGregFight(options);
    // reset the crystal ball prediction by staring longingly at toast
    if (get$2(fightsProp) === 1 && have$P($item`miniature crystal ball`)) {
      var warrenPrediction = ponder().get($location`The Dire Warren`);
      if (warrenPrediction !== globalOptions.target) {
        changeLastAdvLocation();
      }
    }
  }, {
    canInitializeWandererCounters: true
  }), new CopyTargetFight(`${name} (Set Up Crystal Ball)`, () => get$2(monsterProp) === globalOptions.target && get$2(fightsProp) === 1 && have$P($item`miniature crystal ball`) && !ponder().get($location`The Dire Warren`), () => get$2(monsterProp) === globalOptions.target && get$2(fightsProp) > 0 || totalCharges() > 0 ? 1 : 0, runGregFight, {
    spec: {
      equip: $items`miniature crystal ball`.filter(item => have$P(item))
    },
    canInitializeWandererCounters: true
  })];
};
var gregLikeFights = [].concat(_toConsumableArray(gregFights("Be Gregarious", () => true,
// we can always use extrovermectin
"beGregariousMonster", "beGregariousFightsLeft", () => get$2("beGregariousCharges") * 3 + get$2("beGregariousFightsLeft"))), _toConsumableArray(gregFights("Habitats Monster", () => have$P($skill`Just the Facts`), "_monsterHabitatsMonster", "_monsterHabitatsFightsLeft", () => have$P($skill`Just the Facts`) ? (3 - get$2("_monsterHabitatsRecalled")) * 5 + get$2("_monsterHabitatsFightsLeft") : 0)));

/**
 * Determines whether we want to do this particular Target fight; if we aren't using orb, should always return true. If we're using orb and it's a crate, we'll have to see!
 * @returns
 */
function proceedWithOrb() {
  var strat = crateStrategy();
  // If we can't possibly use orb, return true
  if (!have$P($item`miniature crystal ball`) || strat !== "Orb") return true;

  // If we're using orb, we have a KGE prediction, and we can reset it, return false
  var gregFightNames = ["Macrometeorite", "Powerful Glove", "Habitats Monster", "Be Gregarious", "Orb Prediction"];
  if (ponder().get($location`Noob Cave`) === globalOptions.target && copyTargetSources.filter(source => !gregFightNames.some(name => source.name.includes(name))).find(source => source.available())) {
    return false;
  }
  return true;
}
var conditionalSources = [new CopyTargetFight("Orb Prediction", () => have$P($item`miniature crystal ball`) && !get$2("_garbo_doneGregging", false) && ponder().get($location`The Dire Warren`) === globalOptions.target, () => possibleGregCrystalBall(), options => {
  kolmafia.visitUrl("inventory.php?ponder=1");
  if (ponder().get($location`The Dire Warren`) !== globalOptions.target) {
    return;
  }
  var adventureFunction = options.useAuto ? garboAdventureAuto : garboAdventure;
  adventureFunction($location`The Dire Warren`, options.macro, options.macro);
  changeLastAdvLocation();
  if (!doingGregFight()) _set("_garbo_doneGregging", true);
}, {
  spec: {
    equip: $items`miniature crystal ball`
  },
  canInitializeWandererCounters: true
}), new CopyTargetFight("Macrometeorite", () => gregReady() && have$P($skill`Meteor Lore`) && get$2("_macrometeoriteUses") < 10 && proceedWithOrb(), () => doingGregFight() && have$P($skill`Meteor Lore`) ? 10 - get$2("_macrometeoriteUses") : 0, options => {
  equipOrbIfDesired();
  var crateIsSabered = get$2("_saberForceMonster") === $monster`crate`;
  var notEnoughCratesSabered = get$2("_saberForceMonsterCount") < 2;
  var weWantToSaberCrates = !crateIsSabered || notEnoughCratesSabered;
  setChoice(1387, 2);
  var macro = Macro.if_($monster`crate`, Macro.externalIf(crateStrategy() !== "Saber" && !have$P($effect`On the Trail`) && get$2("_olfactionsUsed") < 2, Macro.tryHaveSkill($skill`Transcendent Olfaction`)).externalIf(kolmafia.haveEquipped($item`Fourth of May Cosplay Saber`) && weWantToSaberCrates && get$2("_saberForceUses") < 5, Macro.trySkill($skill`Use the Force`)).skill($skill`Macrometeorite`)).step(options.macro);
  var adventureFunction = options.useAuto ? garboAdventureAuto : garboAdventure;
  adventureFunction($location`Noob Cave`, macro, macro);
  if (ponder().get($location`Noob Cave`) === globalOptions.target) {
    changeLastAdvLocation();
  }
}, {
  gregariousReplace: true
}), new CopyTargetFight("Powerful Glove", () => gregReady() && have$P($item`Powerful Glove`) && get$2("_powerfulGloveBatteryPowerUsed") <= 90 && proceedWithOrb(), () => doingGregFight() && have$P($item`Powerful Glove`) ? Math.min((100 - get$2("_powerfulGloveBatteryPowerUsed")) / 10) : 0, options => {
  equipOrbIfDesired();
  var crateIsSabered = get$2("_saberForceMonster") === $monster`crate`;
  var notEnoughCratesSabered = get$2("_saberForceMonsterCount") < 2;
  var weWantToSaberCrates = !crateIsSabered || notEnoughCratesSabered;
  setChoice(1387, 2);
  var macro = Macro.if_($monster`crate`, Macro.externalIf(crateStrategy() !== "Saber" && !have$P($effect`On the Trail`) && get$2("_olfactionsUsed") < 2, Macro.tryHaveSkill($skill`Transcendent Olfaction`)).externalIf(kolmafia.haveEquipped($item`Fourth of May Cosplay Saber`) && weWantToSaberCrates && get$2("_saberForceUses") < 5, Macro.trySkill($skill`Use the Force`)).skill($skill`CHEAT CODE: Replace Enemy`)).step(options.macro);
  var adventureFunction = options.useAuto ? garboAdventureAuto : garboAdventure;
  adventureFunction($location`Noob Cave`, macro, macro);
  if (ponder().get($location`Noob Cave`) === globalOptions.target) {
    changeLastAdvLocation();
  }
}, {
  spec: {
    equip: $items`Powerful Glove`
  },
  gregariousReplace: true
})].concat(_toConsumableArray(gregLikeFights), [new CopyTargetFight("Backup", () => get$2("lastCopyableMonster") === globalOptions.target && have$P($item`backup camera`) && get$2("_backUpUses") < 11, () => have$P($item`backup camera`) ? 11 - get$2("_backUpUses") : 0, options => {
  var adventureFunction = options.useAuto ? garboAdventureAuto : garboAdventure;
  adventureFunction(options.location, Macro.if_(`!monsterid ${globalOptions.target.id}`, Macro.skill($skill`Back-Up to your Last Enemy`)).step(options.macro), Macro.if_(`!monsterid ${globalOptions.target.id}`, Macro.skill($skill`Back-Up to your Last Enemy`)).step(options.macro));
}, {
  spec: {
    equip: $items`backup camera`,
    modes: {
      backupcamera: "meat"
    }
  },
  draggable: "backup",
  wrongEncounterName: true,
  canInitializeWandererCounters: true
})]);
var fakeSources = [new CopyTargetFight("Professor MeatChain", () => false, () => have$P($familiar`Pocket Professor`) && !get$2("_garbo_meatChain", false) ? Math.max(10 - get$2("_pocketProfessorLectures"), 0) : 0, () => {
  return;
}), new CopyTargetFight("Professor WeightChain", () => false, () => have$P($familiar`Pocket Professor`) && !get$2("_garbo_weightChain", false) ? Math.min(15 - get$2("_pocketProfessorLectures"), 5) : 0, () => {
  return;
})];
function copyTargetConfirmInvocation(msg) {
  // If user does not have autoUserConfirm set to true
  // If the incocatedCount has already reached or exceeded the default limit
  if (!globalOptions.prefs.autoUserConfirm) {
    // userConfirmDialog is not called as
    // 1. If autoUserConfirm is true, it'd make the counter useless as it'll always return the default
    // 2. If autoUserConfirm is false, then it'll call userConfirm regardless
    // The user should be consulted about this so that they can either raise the count or decline the option
    return kolmafia.userConfirm(msg);
  }
  var invocatedCount = get$2("_garbo_autoUserConfirm_targetInvocatedCount", 0);
  if (invocatedCount >= globalOptions.prefs.autoUserConfirm_targetInvocationsThreshold) {
    return false;
  }
  _set("_garbo_autoUserConfirm_targetInvocatedCount", invocatedCount + 1);
  return true;
}
var emergencyChainStarters = [new CopyTargetFight("Mimic Egg (from clinic)", () => have$j() && $familiar`Chest Mimic`.experience >= 100 && monsterIsInEggnet() && get$2("_mimicEggsObtained") < 11, () => 0, options => {
  receive(globalOptions.target);
  withMacro(options.macro, () => differentiate(globalOptions.target), options.useAuto);
}), new CopyTargetFight("Pocket Wish (untapped potential)", () => {
  if (!globalOptions.target.wishable) return false;
  var potential = Math.floor(copyTargetCount());
  if (potential < 1) return false;
  if (get$2("_genieFightsUsed") >= 3) return false;
  if (globalOptions.askedAboutWish) return globalOptions.wishAnswer;
  var profit = (potential + 1) * averageTargetNet() - WISH_VALUE;
  if (profit < 0) return false;
  kolmafia.print(`You have the following copy target sources untapped right now:`, HIGHLIGHT);
  copyTargetSources.filter(source => source.potential() > 0).map(source => `${source.potential()} from ${source.name}`).forEach(text => kolmafia.print(text, HIGHLIGHT));
  globalOptions.askedAboutWish = true;
  globalOptions.wishAnswer = copyTargetConfirmInvocation(`Garbo has detected you have ${potential} potential ways to copy a ${globalOptions.target}, but no way to start a fight with one. Current ${globalOptions.target} net (before potions) is ${averageTargetNet()}, so we expect to earn ${profit} meat, after the cost of a wish. Should we wish for ${globalOptions.target}?`);
  return globalOptions.wishAnswer;
}, () => 0, options => {
  globalOptions.askedAboutWish = false;
  withMacro(options.macro, () => {
    acquire(1, $item`pocket wish`, WISH_VALUE);
    kolmafia.visitUrl(`inv_use.php?pwd=${kolmafia.myHash()}&which=3&whichitem=9537`, false, true);
    kolmafia.visitUrl(`choice.php?pwd&whichchoice=1267&option=1&wish=to fight a ${globalOptions.target} `, true, true);
    kolmafia.visitUrl("main.php", false);
    kolmafia.runCombat();
    globalOptions.askedAboutWish = false;
  }, options.useAuto);
})];
var copyTargetSources = [].concat(wanderSources, _toConsumableArray(conditionalSources), copySources, chainStarters, emergencyChainStarters, fakeSources);
function copyTargetCount() {
  return sum(copyTargetSources, source => source.potential());
}

/**
 * Gets next available copy target fight. If there is no way to generate a fight, but copies are available,
 * the user is prompted to purchase a pocket wish to start the copy target chain.
 * @returns the next available copy target fight
 */
function getNextCopyTargetFight() {
  var wanderer = wanderSources.find(fight => fight.available());
  if (wanderer) return wanderer;
  var conditional = conditionalSources.find(fight => fight.available());
  if (conditional) {
    var leftoverReplacers = (have$P($skill`Meteor Lore`) ? 10 - get$2("_macrometeoriteUses") : 0) + (have$P($item`Powerful Glove`) ? Math.floor((100 - get$2("_powerfulGloveBatteryPowerUsed")) / 10) : 0);
    // we don't want to reset our orb with a gregarious fight; that defeats the purpose
    var skip = conditional.name === "Be Gregarious" && crateStrategy() === "Orb" && leftoverReplacers;
    if (!skip) return conditional;
  }
  var copy = copySources.find(fight => fight.available());
  if (copy) return copy;
  var chainStart = chainStarters.find(fight => fight.available());
  if (chainStart) return chainStart;
  return conditional ?? emergencyChainStarters.find(fight => fight.available()) ?? null;
}

var luckySourceTasks = [{
  name: "Scepter",
  acquire: [{
    item: $item`august scepter`
  }],
  ready: () => !have$P($effect`Lucky!`),
  completed: () => !shouldAugustCast($skill`Aug. 2nd: Find an Eleven-Leaf Clover Day`),
  do: () => {
    kolmafia.useSkill($skill`Aug. 2nd: Find an Eleven-Leaf Clover Day`);
    if (!have$P($effect`Lucky!`)) {
      _set("_aug2Cast", true);
    }
  },
  spendsTurn: false,
  turns: () => shouldAugustCast($skill`Aug. 2nd: Find an Eleven-Leaf Clover Day`) ? 1 : 0
}, {
  name: "Saxophone",
  ready: () => !have$P($effect`Lucky!`),
  completed: () => !canPlay($item`Apriling band saxophone`),
  do: () => {
    play($item`Apriling band saxophone`);
    if (!have$P($effect`Lucky!`)) return;
  },
  spendsTurn: false,
  turns: () => $item`Apriling band saxophone`.dailyusesleft
}, {
  name: "Pillkeeper",
  acquire: [{
    item: $item`Eight Days a Week Pill Keeper`
  }],
  ready: () => !have$P($effect`Lucky!`) && have$P($item`Eight Days a Week Pill Keeper`),
  completed: () => get$2("_freePillKeeperUsed"),
  do: () => {
    kolmafia.cliExecute("pillkeeper semirare");
    if (!have$P($effect`Lucky!`)) {
      _set("_freePillKeeperUsed", true);
      return;
    }
  },
  spendsTurn: false,
  turns: () => have$P($item`Eight Days a Week Pill Keeper`) && !get$2("_freePillKeeperUsed") ? 1 : 0
}, {
  name: "Heartstone",
  ready: () => !have$P($effect`Lucky!`),
  completed: () => !have$P($item`Heartstone`) || !get$2("heartstoneLuckUnlocked") || get$2("_heartstoneLuckUsed"),
  do: () => {
    kolmafia.useSkill($skill`Heartstone: %luck`);
    if (!have$P($effect`Lucky!`)) return;
  },
  spendsTurn: false,
  turns: () => have$P($item`Heartstone`) && get$2("heartstoneLuckUnlocked") && !get$2("_heartstoneLuckUsed") ? 1 : 0
}];

function valueBjornModifiers(mode, familiar) {
  var meatValue = modeValueOfMeat(mode);
  var leprechaunMultiplier = findLeprechaunMultiplier(familiar);
  var leprechaunCoefficient = meatValue * (2 * leprechaunMultiplier + Math.sqrt(leprechaunMultiplier));
  var itemValue = modeValueOfItem(mode);
  var fairyMultiplier = findFairyMultiplier(familiar);
  var fairyCoefficient = itemValue * (fairyMultiplier + Math.sqrt(fairyMultiplier) / 2);
  return createModifierValueFunction(["Familiar Weight", "Meat Drop", "Item Drop"], {
    "Familiar Weight": mod => mod * (fairyCoefficient + leprechaunCoefficient),
    "Item Drop": mod => mod * itemValue,
    "Meat Drop": mod => mod * meatValue
  });
}
function dropsValueFunction(drops) {
  return Array.isArray(drops) ? garboAverageValue.apply(void 0, _toConsumableArray(drops)) : sum(_toConsumableArray(drops.entries()), _ref => {
    var _ref2 = _slicedToArray(_ref, 2),
      item = _ref2[0],
      quantity = _ref2[1];
    return quantity * garboValue(item);
  }) / sumNumbers(_toConsumableArray(drops.values()));
}
function chooseBjorn(mode, familiar) {
  var sim = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : false;
  var leprechaunMultiplier = findLeprechaunMultiplier(familiar);
  var fairyMultiplier = findFairyMultiplier(familiar);
  var ignoreLimitedDrops = sim || !modeUseLimitedDrops(mode);
  var key = `Leprechaun:${leprechaunMultiplier.toFixed(2)};Fairy:${fairyMultiplier.toFixed(2)};ignoreLimitedDrops:${ignoreLimitedDrops}`;
  if (!hasRiderMode(key)) {
    createRiderMode(key, {
      ignoreLimitedDrops,
      modifierValueFunction: valueBjornModifiers(mode, familiar),
      dropsValueFunction
    });
  }
  var result = pickRider(key);
  if (!result) throw new Error(`Unable to choose rider for key ${key}`);
  return {
    familiar: result.familiar,
    value: valueRider(result, valueBjornModifiers(mode, familiar), dropsValueFunction)
  };
}

var allowList = $items`Fudgie Roll, peanut brittle shield, sugar shotgun, sugar shillelagh, sugar shank, sugar chapeau, sugar shorts, sugar shield, sugar shirt`;

// For safety, explicitly skip candies that are no longer obtainable or extremely rare
var blockList = new Set([$item`candied nuts`, $item`candy kneecapping stick`, $item`chocolate cigar`, $item`fancy but probably evil chocolate`, $item`fancy chocolate`, $item`fancy chocolate car`, $item`gummi ammonite`, $item`gummi belemnite`, $item`gummi trilobite`, $item`powdered candy sushi set`, $item`radio button candy`, $item`spiritual candy cane`, $item`Ultra Mega Sour Ball`, $item`vitachoconutriment capsule`]);
function synthesize(casts, effect) {
  var saveLimit = 1;
  var buyableCandies = $items.all().filter(i => i.tradeable && i.candyType === "complex" && !blockList.has(i)).sort((a, b) => kolmafia.mallPrice(a) - kolmafia.mallPrice(b)).slice(0, 50);
  var shuffledAllowlist = shuffle(allowList);
  var _iterator = _createForOfIteratorHelper(shuffledAllowlist),
    _step;
  try {
    for (_iterator.s(); !(_step = _iterator.n()).done;) {
      var untradeable = _step.value;
      if (kolmafia.availableAmount(untradeable) <= saveLimit) continue;
      var _iterator2 = _createForOfIteratorHelper(buyableCandies),
        _step2;
      try {
        for (_iterator2.s(); !(_step2 = _iterator2.n()).done;) {
          var buyable = _step2.value;
          if (kolmafia.sweetSynthesisResult(untradeable, buyable) !== effect) continue;
          var possibleCasts = kolmafia.availableAmount(untradeable) - saveLimit;
          var spleen = Math.max(kolmafia.spleenLimit() - kolmafia.mySpleenUse(), 0);
          var castsToDo = Math.min(possibleCasts, casts, spleen);
          if (castsToDo === 0) continue;
          kolmafia.retrieveItem(untradeable, castsToDo);
          kolmafia.retrieveItem(buyable, castsToDo);
          if (kolmafia.sweetSynthesis(castsToDo, untradeable, buyable)) casts -= castsToDo;
          if (casts <= 0) return;
        }
      } catch (err) {
        _iterator2.e(err);
      } finally {
        _iterator2.f();
      }
    }
  } catch (err) {
    _iterator.e(err);
  } finally {
    _iterator.f();
  }
  kolmafia.sweetSynthesis(clamp(casts, 0, kolmafia.spleenLimit() - kolmafia.mySpleenUse()), effect);
}

Mood.setDefaultOptions({
  songSlots: [$effects`Polka of Plenty`, $effects`Fat Leon's Phat Loot Lyric, Ur-Kel's Aria of Annoyance`, $effects`Chorale of Companionship`, $effects`The Ballad of Richie Thingfinder`],
  useNativeRestores: true
});
function meatMood() {
  var urKels = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : false;
  var meat = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : undefined;
  var baseMeat$1 = baseMeat();
  meat || (meat = baseMeat$1);
  // Reserve the amount of MP we try to restore before each fight.
  var mood = new Mood({
    reserveMp: safeRestoreMpTarget()
  });
  mood.potion($item`resolution: be wealthier`, 0.3 * baseMeat$1);
  mood.potion($item`resolution: be happier`, 0.15 * 0.45 * 0.8 * 200);
  var flaskValue = usingPurse() ? 0.3 * baseMeat$1 : 5;
  mood.potion($item`Flaskfull of Hollow`, flaskValue);
  mood.skill($skill`Blood Bond`);
  mood.skill($skill`Leash of Linguini`);
  mood.skill($skill`Empathy of the Newt`);
  mood.skill($skill`Only Dogs Love a Drunken Sailor`);
  if (have$P($item`April Shower Thoughts shield`)) {
    mood.effect($effect`Thoughtful Empathy`);
    mood.effect($effect`Lubricating Sauce`);
    mood.effect($effect`Tubes of Universal Meat`);
    mood.effect($effect`Strength of the Tortoise`);
  }
  mood.skill($skill`The Polka of Plenty`);
  mood.skill($skill`Disco Leer`);
  mood.skill($skill`Singer's Faithful Ocelot`);
  mood.skill($skill`The Spirit of Taking`);
  if (FarmingStrategy.location === $location`Barf Mountain`) {
    mood.potion($item`How to Avoid Scams`, 3 * baseMeat$1);
  }
  if (FarmingStrategy.location.recommendedStat <= 300) {
    mood.skill($skill`Drescher's Annoying Noise`);
    mood.skill($skill`Pride of the Puffin`);
    mood.skill(urKels ? $skill`Ur-Kel's Aria of Annoyance` : $skill`Fat Leon's Phat Loot Lyric`);
  }
  if (FarmingStrategy.location.recommendedStat >= 400) {
    mood.skill($skill`Ruthless Efficiency`);
    mood.skill($skill`Ghostly Shell`);
    mood.skill($skill`Shield of the Pastalord`);
    if (kolmafia.myInebriety() > kolmafia.inebrietyLimit()) {
      mood.skill($skill`Get Big`);
      mood.skill($skill`Song of Bravado`);
      mood.skill($skill`Rage of the Reindeer`);
      mood.skill($skill`Disco Fever`);
      mood.skill($skill`Carol of the Bulls`);
      mood.skill($skill`Blood Bubble`);
      mood.skill($skill`Tenacity of the Snapper`);
      mood.skill($skill`Grease Up`);
      mood.effect($effect`Disco over Matter`);
    }
  }
  if (FarmingStrategy.isUnderwater()) mood.skill($skill`Donho's Bubbly Ballad`);
  mood.skill($skill`Walk: Leisurely Amble`);
  mood.skill($skill`Call For Backup`);
  mood.skill($skill`Soothing Flute`);
  var mmjCost = (100 - (have$P($skill`Five Finger Discount`) ? 5 : 0) - (have$P($item`Travoltan trousers`) ? 5 : 0)) * (200 / (1.5 * kolmafia.myLevel() + 5));
  var genericManaPotionCost = kolmafia.mallPrice($item`generic mana potion`) * (200 / (2.5 * kolmafia.myLevel()));
  var mpRestorerCost = Math.min(mmjCost, genericManaPotionCost);
  if (kolmafia.myClass() !== $class`Pastamancer` && 0.1 * meat * 10 > mpRestorerCost) {
    mood.skill($skill`Bind Lasagmbie`);
  }
  if (kolmafia.getWorkshed() === $item`Asdon Martin keyfob (on ring)`) {
    mood.drive(FarmingStrategy.asdonEffect);
  }
  if (have$P($item`Kremlin's Greatest Briefcase`)) {
    mood.effect($effect`A View to Some Meat`, () => {
      if (get$2("_kgbClicksUsed") < 22) {
        var buffTries = Math.ceil((22 - get$2("_kgbClicksUsed")) / 3);
        kolmafia.cliExecute(`Briefcase buff ${new Array(buffTries).fill("meat").join(" ")}`);
      }
    });
  }
  if (!get$2("concertVisited") && get$2("sidequestArenaCompleted") === "fratboy") {
    kolmafia.cliExecute("concert winklered");
  } else if (!get$2("concertVisited") && get$2("sidequestArenaCompleted") === "hippy") {
    kolmafia.cliExecute("concert optimist primal");
  }
  if (kolmafia.itemAmount($item`Bird-a-Day calendar`) > 0) {
    if (!have$P($skill`Seek out a Bird`) || !get$2("_canSeekBirds")) {
      kolmafia.use(1, $item`Bird-a-Day calendar`);
    }
    if (have$P($skill`Visit your Favorite Bird`) && !get$2("_favoriteBirdVisited") && (kolmafia.numericModifier($effect`Blessing of your favorite Bird`, "Meat Drop") > 0 || kolmafia.numericModifier($effect`Blessing of your favorite Bird`, "Item Drop") > 0)) {
      kolmafia.useSkill($skill`Visit your Favorite Bird`);
    }
    if (have$P($skill`Seek out a Bird`) && get$2("_birdsSoughtToday") < 6 && (kolmafia.numericModifier($effect`Blessing of the Bird`, "Meat Drop") > 0 || kolmafia.numericModifier($effect`Blessing of the Bird`, "Item Drop") > 0)) {
      // Ensure we don't get stuck in the choice if the count is wrong
      setChoice(1399, 2);
      kolmafia.useSkill($skill`Seek out a Bird`, 6 - get$2("_birdsSoughtToday"));
    }
  }
  if (have$P($skill`Incredible Self-Esteem`) && $effects`Always be Collecting, Work For Hours a Week`.some(effect => have$P(effect)) && !get$2("_incredibleSelfEsteemCast")) {
    kolmafia.useSkill($skill`Incredible Self-Esteem`);
  }
  if (!get$2("_alliedRadioWildsunBoon") && wildsunBoonWorthIt()) {
    var acquired = acquire(1, $item`handheld Allied radio`, effectValue($effect`Wildsun Boon`, 100), false);
    if (!acquired) _wildsunBoonWorthIt = false;
    kolmafia.alliedRadio("wildsun boon");
  }
  var canRecord = kolmafia.getWorkshed() === $item`warbear LP-ROM burner` || have$P($item`warbear LP-ROM burner`) && !get$2("_workshedItemUsed") || get$2("questG04Nemesis") === "finished";
  if (kolmafia.myClass() === $class`Accordion Thief` && kolmafia.myLevel() >= 15 && !canRecord) {
    if (have$P($skill`The Ballad of Richie Thingfinder`)) {
      kolmafia.useSkill($skill`The Ballad of Richie Thingfinder`, 10 - get$2("_thingfinderCasts"));
    }
    if (have$P($skill`Chorale of Companionship`)) {
      kolmafia.useSkill($skill`Chorale of Companionship`, 10 - get$2("_companionshipCasts"));
    }
  }
  if (have$P($skill`Heartstone: %pals`)) {
    kolmafia.useSkill($skill`Heartstone: %pals`, 5 - get$2("_heartstonePalsUsed"));
  }
  shrugBadEffects();
  return mood;
}
function freeFightMood() {
  var mood = new Mood();
  for (var _len = arguments.length, additionalEffects = new Array(_len), _key = 0; _key < _len; _key++) {
    additionalEffects[_key] = arguments[_key];
  }
  for (var _i = 0, _additionalEffects = additionalEffects; _i < _additionalEffects.length; _i++) {
    var effect = _additionalEffects[_i];
    mood.effect(effect);
  }
  if (kolmafia.haveEffect($effect`Blue Swayed`) < 50) {
    kolmafia.use(Math.ceil((50 - kolmafia.haveEffect($effect`Blue Swayed`)) / 10), $item`pulled blue taffy`);
  }
  mood.potion($item`white candy heart`, 30);
  mood.skill($skill`Curiosity of Br'er Tarrypin`);
  shrugBadEffects.apply(void 0, additionalEffects);
  if (kolmafia.getWorkshed() === $item`Asdon Martin keyfob (on ring)`) {
    mood.drive(FarmingStrategy.asdonEffect);
  }
  return mood;
}
var damageEffects = kolmafia.Effect.all().filter(x => ["Thorns", "Sporadic Thorns", "Damage Aura", "Sporadic Damage Aura"].some(modifier => kolmafia.numericModifier(x, modifier) > 0));
var textAlteringEffects = kolmafia.Effect.all().filter(x => kolmafia.booleanModifier(x, "Alters Page Text"));
var teleportEffects = kolmafia.Effect.all().filter(x => kolmafia.booleanModifier(x, "Adventure Randomly"));
var otherBadEffects = kolmafia.Effect.all().filter(x => kolmafia.booleanModifier(x, "Blind") || kolmafia.booleanModifier(x, "Always Fumble"));
function shrugBadEffects() {
  for (var _len2 = arguments.length, exclude = new Array(_len2), _key2 = 0; _key2 < _len2; _key2++) {
    exclude[_key2] = arguments[_key2];
  }
  [].concat(_toConsumableArray(damageEffects), _toConsumableArray(textAlteringEffects), _toConsumableArray(teleportEffects), _toConsumableArray(otherBadEffects)).filter(effect => have$P(effect) && !exclude.includes(effect)).forEach(effect => uneffect(effect));
}
var _wildsunBoonWorthIt;
function wildsunBoonWorthIt() {
  return _wildsunBoonWorthIt ?? (_wildsunBoonWorthIt = effectValue($effect`Wildsun Boon`, 100) > kolmafia.mallPrice($item`handheld Allied radio`));
}

var GOOD_TRAIN_STATIONS = [{
  piece: Station.GAIN_MEAT,
  value: () => 900
}, {
  // Some day this'll be better
  piece: Station.TRACKSIDE_DINER,
  value: () => $monsters`Witchess Knight`.includes(globalOptions.target) && copyTargetCount() > 0 ? garboValue($item`jumping horseradish`) : garboAverageValue.apply(void 0, _toConsumableArray($items`bowl of cottage cheese, hot buttered roll, toast`))
}, {
  piece: Station.CANDY_FACTORY,
  value: candyFactoryValue
}, {
  piece: Station.GRAIN_SILO,
  value: () => 2 * garboAverageValue.apply(void 0, _toConsumableArray($items`bottle of gin, bottle of vodka, bottle of whiskey, bottle of rum, bottle of tequila, boxed wine`))
}, {
  piece: Station.ORE_HOPPER,
  value: () => garboAverageValue.apply(void 0, _toConsumableArray($items`linoleum ore, asbestos ore, chrome ore, teflon ore, vinyl ore, velcro ore, bubblewrap ore, cardboard ore, styrofoam ore`))
}];
var trainCycle;
function getBestCycle() {
  if (!trainCycle) {
    var cycle = [Station.COAL_HOPPER].concat(_toConsumableArray(GOOD_TRAIN_STATIONS.sort((_ref, _ref2) => {
      var a = _ref.value;
      var b = _ref2.value;
      return b() - a();
    }).map(_ref3 => {
      var piece = _ref3.piece;
      return piece;
    })), [Station.TOWER_FIZZY, Station.VIEWING_PLATFORM]);
    trainCycle = cycle;
  }
  return _toConsumableArray(trainCycle);
}
function valueStation(station) {
  var _GOOD_TRAIN_STATIONS$;
  if (station === Station.COAL_HOPPER) {
    return valueStation(getBestCycle()[1]);
  }
  return ((_GOOD_TRAIN_STATIONS$ = GOOD_TRAIN_STATIONS.find(_ref4 => {
    var piece = _ref4.piece;
    return piece === station;
  })) === null || _GOOD_TRAIN_STATIONS$ === void 0 ? void 0 : _GOOD_TRAIN_STATIONS$.value()) ?? 0;
}
function valueOffset(offset) {
  var firstFortyTurns = 5 * sum(getBestCycle(), valueStation);
  var extraTurns = sum(getBestCycle().slice(0, offset - 1), valueStation);
  return (firstFortyTurns + extraTurns) / (40 + offset);
}
var bestOffset = null;
function getBestOffset() {
  return bestOffset ?? (bestOffset = maxBy([2, 3, 4, 5, 6, 7, 8], valueOffset));
}
function getPrioritizedStations() {
  return getBestCycle().slice(0, getBestOffset() - 1);
}
function getRotatedCycle() {
  var offset = get$2("trainsetPosition") % 8;
  var newPieces = [];
  var defaultPieces = getBestCycle();
  for (var i = 0; i < 8; i++) {
    var newPos = (i + offset) % 8;
    newPieces[newPos] = defaultPieces[i];
  }
  return newPieces;
}
function trainNeedsRotating() {
  if (!canConfigure()) return false;
  if (!get$2("trainsetConfiguration")) {
    // Visit the workshed to make sure it's actually empty, instead of us having not yet seen it this run
    kolmafia.visitUrl("campground.php?action=workshed");
    kolmafia.visitUrl("main.php");
  }
  if (!get$2("trainsetConfiguration")) return true;
  if (arrayEquals$1(getRotatedCycle(), cycle())) return false;
  if (globalOptions.ascend && estimatedGarboTurns() <= 40) return false;
  var bestStations = getPrioritizedStations();
  if (bestStations.includes(next())) return false;
  return true;
}
function rotateToOptimalCycle() {
  var hasRotated = setConfiguration(getRotatedCycle());

  // If the trainset was not configured but still claims to be configurable
  if (!hasRotated && canConfigure()) {
    // Set the trainset configuration to believe it'll be configurable in one turn
    _set("lastTrainsetConfiguration", get$2("trainsetPosition") - 39);
  }
  return hasRotated;
}
function grabMedicine() {
  var options = kolmafia.visitUrl("campground.php?action=workshed");
  var i = 0;
  var match;
  var regexp = /descitem\((\d+)\)/g;
  var itemChoices = new Map();
  if (!globalOptions.nobarf) {
    switch (FarmingStrategy.location.environment) {
      case "underground":
        itemChoices.set($item`Breathitin™`, -1);
        break;
      case "indoor":
        itemChoices.set($item`Extrovermectin™`, -1);
        break;
      case "outdoor":
        itemChoices.set($item`Homebodyl™`, -1);
        break;
      default:
        itemChoices.set($item`Fleshazole™`, -1);
        break;
    }
    // if spending turns at barf, we probably will be able to get an extro so always consider it
  }
  while ((match = regexp.exec(options)) !== null) {
    i++;
    var item = kolmafia.descToItem(match[1]);
    itemChoices.set(item, i);
  }
  var bestItem = maxBy(_toConsumableArray(itemChoices.keys()), garboValue);
  var bestChoice = itemChoices.get(bestItem);
  if (bestChoice && bestChoice > 0) {
    kolmafia.visitUrl("campground.php?action=workshed");
    kolmafia.runChoice(bestChoice);
  }
  if (kolmafia.handlingChoice()) kolmafia.visitUrl("main.php");
}

// Silk and Gold are thrice as rare as other ingredients, so we value them thrice as much
// Yes, it's pretty dumb
function naiveTakerspaceCost(recipe) {
  return sum(_toConsumableArray(recipe.entries()), _ref5 => {
    var _ref6 = _slicedToArray(_ref5, 2),
      amount = _ref6[0],
      index = _ref6[1];
    return amount * ([4, 5].includes(index) ? 3 : 1);
  });
}
function bestTakerspaceItem() {
  var makeables = _toConsumableArray(allRecipes().entries()).filter(_ref7 => {
    var _ref8 = _slicedToArray(_ref7, 1),
      i = _ref8[0];
    return canMake(i);
  });
  return makeables.length ? maxBy(makeables, _ref9 => {
    var _ref0 = _slicedToArray(_ref9, 2),
      item = _ref0[0],
      recipe = _ref0[1];
    return garboValue(item) / naiveTakerspaceCost(recipe);
  })[0] : null;
}

var GarboWorkshed = /*#__PURE__*/function () {
  function GarboWorkshed(options) {
    _classCallCheck(this, GarboWorkshed);
    _defineProperty(this, "available", () => true);
    this.workshed = options.workshed;
    if (options.done) this.done = options.done;
    if (options.action) this.action = options.action;
    if (options.available) this.available = options.available;
    this.minTurns = options.minTurns ?? 0;
  }
  return _createClass(GarboWorkshed, [{
    key: "canRemove",
    value: function canRemove() {
      var _this$done, _GarboWorkshed$next;
      return (((_this$done = this.done) === null || _this$done === void 0 ? void 0 : _this$done.call(this)) ?? true) || estimatedGarboTurns() <= (((_GarboWorkshed$next = GarboWorkshed.next) === null || _GarboWorkshed$next === void 0 ? void 0 : _GarboWorkshed$next.minTurns) ?? 0);
    }
  }, {
    key: "use",
    value: function use() {
      var _this$done2, _this$action;
      if (!((_this$done2 = this.done) !== null && _this$done2 !== void 0 && _this$done2.call(this))) (_this$action = this.action) === null || _this$action === void 0 || _this$action.call(this);
    }
  }, {
    key: "task",
    get: function get() {
      return {
        name: `Workshed: ${this.workshed}`,
        completed: () => {
          var _this$done3;
          return ((_this$done3 = this.done) === null || _this$done3 === void 0 ? void 0 : _this$done3.call(this)) ?? true;
        },
        ready: () => kolmafia.getWorkshed() === this.workshed && this.available() && !!this.action,
        do: () => this.use(),
        available: () => {
          var _GarboWorkshed$curren, _GarboWorkshed$next2;
          return [(_GarboWorkshed$curren = GarboWorkshed.current) === null || _GarboWorkshed$curren === void 0 ? void 0 : _GarboWorkshed$curren.workshed, (_GarboWorkshed$next2 = GarboWorkshed.next) === null || _GarboWorkshed$next2 === void 0 ? void 0 : _GarboWorkshed$next2.workshed].includes(this.workshed);
        }
      };
    }
  }], [{
    key: "get",
    value: function get(item) {
      return worksheds.find(_ref => {
        var workshed = _ref.workshed;
        return workshed === item;
      }) ?? null;
    }
  }, {
    key: "current",
    get: function get() {
      return GarboWorkshed.get(kolmafia.getWorkshed());
    }
  }, {
    key: "next",
    get: function get() {
      if (get$2("_workshedItemUsed") || kolmafia.getWorkshed() === globalOptions.workshed) {
        return null;
      }
      return GarboWorkshed.get(globalOptions.workshed);
    }
  }, {
    key: "useNext",
    value: function useNext() {
      if (get$2("_workshedItemUsed")) return null;
      var next = GarboWorkshed.next;
      if (next && have$P(next.workshed)) {
        kolmafia.use(next.workshed);
      }
      return GarboWorkshed.current;
    }
  }]);
}();
var _attemptedMakingTonics = false;
var _lastCMCTurn = kolmafia.myTotalTurnsSpent();
var worksheds = [new GarboWorkshed({
  workshed: $item`model train set`,
  // We should always get value from the trainset, so we would never switch from it
  done: () => false,
  available: trainNeedsRotating,
  action: rotateToOptimalCycle
}), new GarboWorkshed({
  workshed: $item`cold medicine cabinet`,
  done: () => get$2("_coldMedicineConsults") >= 5,
  available: () => get$2("_nextColdMedicineConsult") <= kolmafia.totalTurnsPlayed() && kolmafia.myTotalTurnsSpent() !== _lastCMCTurn,
  // TODO: Ensure that we have a good expected cmc result
  action: () => {
    grabMedicine();
    _lastCMCTurn = kolmafia.myTotalTurnsSpent();
  },
  minTurns: 80
}), new GarboWorkshed({
  workshed: $item`Asdon Martin keyfob (on ring)`,
  done: () => {
    return kolmafia.haveEffect(FarmingStrategy.asdonEffect) >= estimatedGarboTurns() + (globalOptions.ascend ? 0 : estimatedTurnsTomorrow);
  },
  action: () => {
    drive(FarmingStrategy.asdonEffect, estimatedGarboTurns() + (globalOptions.ascend ? 0 : estimatedTurnsTomorrow));
  }
}), new GarboWorkshed({
  workshed: $item`Little Geneticist DNA-Splicing Lab`,
  done: () => {
    // This will likely always return true or false for now, depending on the start state of garbo
    // Since we don't actually support using the syringe in combat at this time, the counter will never change
    return _attemptedMakingTonics || get$2("_dnaPotionsMade") >= 3;
  },
  action: () => {
    // Just grab whatever tonics for now, since we don't actually have support for DNA
    if (get$2("dnaSyringe")) makeTonic(3);
    _attemptedMakingTonics = true;
  }
}), new GarboWorkshed({
  workshed: $item`spinning wheel`,
  done: () => get$2("_spinningWheel"),
  action: () => {
    // We simply assume you will not gain a level while garboing, since we do not do powerlevellings
    // So we will just use the spinning wheel immediately
    kolmafia.visitUrl("campground.php?action=spinningwheel");
  }
}), new GarboWorkshed({
  workshed: $item`TakerSpace letter of Marque`,
  done: () => _toConsumableArray(allRecipes().keys()).every(item => !canMake(item)),
  action: () => {
    var best = bestTakerspaceItem();
    while (best) {
      make(best);
      best = bestTakerspaceItem();
    }
  },
  available: () => GarboWorkshed.next && !get$2("_workshedItemUsed") || globalOptions.ascend
})].concat(_toConsumableArray($items`diabolic pizza cube, portable Mayo Clinic, warbear high-efficiency still, warbear induction oven`.map(item => new GarboWorkshed({
  workshed: item,
  done: () => globalOptions.dietCompleted
}))), _toConsumableArray($items`warbear chemistry lab, warbear LP-ROM burner`.map(item => new GarboWorkshed({
  workshed: item,
  done: potionSetupCompleted
}))), _toConsumableArray($items`snow machine, warbear jackhammer drill press, warbear auto-anvil`.map(item => new GarboWorkshed({
  workshed: item
}))));
var SAFETY_TURNS_THRESHOLD = 25;
function workshedTasks() {
  return [].concat(_toConsumableArray(worksheds.map(workshed => workshed.task)), [{
    name: "Swap Workshed",
    completed: () => get$2("_workshedItemUsed"),
    ready: () => {
      var _GarboWorkshed$curren2, _GarboWorkshed$next3;
      var canRemove = ((_GarboWorkshed$curren2 = GarboWorkshed.current) === null || _GarboWorkshed$curren2 === void 0 ? void 0 : _GarboWorkshed$curren2.canRemove()) ?? true;
      var haveNext = GarboWorkshed.next !== null && have$P(GarboWorkshed.next.workshed);
      var enoughTurns = !((_GarboWorkshed$next3 = GarboWorkshed.next) !== null && _GarboWorkshed$next3 !== void 0 && _GarboWorkshed$next3.minTurns) || GarboWorkshed.next.minTurns + SAFETY_TURNS_THRESHOLD > estimatedGarboTurns();
      return canRemove && haveNext && enoughTurns;
    },
    do: () => GarboWorkshed.useNext(),
    available: () => !get$2("_workshedItemUsed") && !!GarboWorkshed.next
  }]);
}

var MPA = get$2("valueOfAdventure");
kolmafia.print(`Using adventure value ${MPA}.`, HIGHLIGHT);
var Mayo = Mayo$1;
function hasMoonZoneRestaurant() {
  return $locations`Camp Logging Camp, Thugnderdome`.some(loc => kolmafia.canAdventure(loc));
}
function availableFromMoonZoneRestaurant(item) {
  return hasMoonZoneRestaurant() && kolmafia.dailySpecial() === item;
}
function consumeWhileRespectingMoonRestaurant(command, item) {
  var usingMoonZoneRestaurant = availableFromMoonZoneRestaurant(item);
  withProperties({
    autoSatisfyWithCloset: !usingMoonZoneRestaurant && get$2("autoSatisfyWithCloset"),
    autoSatisfyWithMall: !usingMoonZoneRestaurant
  }, command);
}
function eatSafe(qty, item) {
  if (have$P($item`Universal Seasoning`) && $item`Universal Seasoning`.dailyusesleft > 0 && !get$2("universalSeasoningActive")) {
    kolmafia.use($item`Universal Seasoning`);
  }
  if (kolmafia.myLevel() >= 15 && !get$2("_hungerSauceUsed") && kolmafia.mallPrice($item`Hunger™ Sauce`) < 3 * MPA) {
    acquire(1, $item`Hunger™ Sauce`, 3 * MPA);
    kolmafia.use($item`Hunger™ Sauce`);
  }
  if (kolmafia.mallPrice($item`fudge spork`) < 3 * MPA && !get$2("_fudgeSporkUsed")) {
    kolmafia.eat($item`fudge spork`);
  }
  if (kolmafia.myClass() === $class`Pastamancer` && kolmafia.myThrall() !== $thrall`Spice Ghost` && !get$2("_legendarySpiceGhostFood") && $thrall`Spice Ghost`.level + (get$2("pumpkinSpiceWhorlUsed") ? 3 : 0) >= 11) {
    kolmafia.useSkill($skill`Bind Spice Ghost`);
  }
  useIfUnused($item`milk of magnesium`, "_milkOfMagnesiumUsed", 5 * MPA);
  consumeWhileRespectingMoonRestaurant(() => {
    if (!kolmafia.eat(qty, item)) throw "Failed to eat safely";
  }, item);
}
var EXPENSIVE_SONGS = $effects`The Ballad of Richie Thingfinder, Chorale of Companionship`;
var USEFUL_SONGS = $effects`Polka of Plenty, Ur-Kel's Aria of Annoyance, Fat Leon's Phat Loot Lyric`;
function shrugForOde() {
  var inexpensiveSongs = getActiveSongs().filter(e => !EXPENSIVE_SONGS.includes(e));
  var uselessSongs = inexpensiveSongs.filter(e => !USEFUL_SONGS.includes(e));
  if (uselessSongs.length >= 1) return uneffect(uselessSongs[0]);
  if (inexpensiveSongs.length === 1) return uneffect(inexpensiveSongs[0]);
  return uneffect(maxBy(inexpensiveSongs, e => kolmafia.haveEffect(e) * kolmafia.mpCost(kolmafia.toSkill(e)), true));
}
function buskEffectValuer(effect, duration) {
  if (effect === $effect`Salty Mouth`) return 5 * get$2("valueOfAdventure");
  if (effect === $effect`Hammertime` && !have$P($effect`Hammertime`) && get$2("_beretBuskingUses") === 0) {
    return 1_000; // Arbitrary value, assume it will give upcoming busks more value if it's our first busk
  }
  return beretEffectValue(effect, duration);
}
function canBusk() {
  return have$c() && get$2("_beretBuskingUses") < 5;
}
function buskForSaltyMouth() {
  if (!canBusk()) return;
  for (var i = get$2("_beretBuskingUses"); i < 5; i++) {
    if (have$P($effect`Salty Mouth`)) break;
    buskFor(buskEffectValuer, {});
  }
}
function drinkSafe(qty, item) {
  var prevDrunk = kolmafia.myInebriety();
  if (have$P($skill`The Ode to Booze`)) {
    if (!have$P($effect`Ode to Booze`) && getSongCount() >= getSongLimit()) {
      shrugForOde();
    }
    var odeTurns = qty * item.inebriety;
    var castTurns = odeTurns - kolmafia.haveEffect($effect`Ode to Booze`);
    if (castTurns > 0) {
      kolmafia.useSkill($skill`The Ode to Booze`, Math.ceil(castTurns / kolmafia.turnsPerCast($skill`The Ode to Booze`)));
    }
  }
  consumeWhileRespectingMoonRestaurant(() => {
    var _item$notes;
    if ((_item$notes = item.notes) !== null && _item$notes !== void 0 && _item$notes.includes("BEER") && canBusk()) {
      for (var i = 0; i < qty; i++) {
        buskForSaltyMouth();
        if (!kolmafia.drink(1, item)) throw "Failed to drink safely";
      }
    } else if (!kolmafia.drink(qty, item)) throw "Failed to drink safely";
  }, item);
  if (item.inebriety === 1 && prevDrunk === qty + kolmafia.myInebriety() - 1) {
    // sometimes mafia does not track the mime army shotglass property
    kolmafia.setProperty("_mimeArmyShotglassUsed", "true");
  }
}
function chewSafe(qty, item) {
  if (!kolmafia.chew(qty, item)) throw "Failed to chew safely";
}
function consumeSafe(qty, item, additionalValue, skipAcquire) {
  var spleenCleaned = spleenCleaners.get(item);
  if (spleenCleaned && kolmafia.mySpleenUse() < spleenCleaned) {
    throw "No spleen to clear with this.";
  }
  var averageAdventures = getAverageAdventures(item);
  var usingMoonZoneRestaurant = availableFromMoonZoneRestaurant(item);
  if (!usingMoonZoneRestaurant) {
    if (averageAdventures > 0 || additionalValue) {
      var cap = Math.max(0, averageAdventures * MPA) + (additionalValue ?? 0);
      acquire(qty, item, cap, true);
    } else {
      acquire(qty, item);
    }
  }
  // When eating the daily special, we need to closet any excess food, since it's much cheaper to eat the special
  var excessAmount = usingMoonZoneRestaurant ? kolmafia.itemAmount(item) : 0;
  if (usingMoonZoneRestaurant && kolmafia.itemAmount(item) > 0) {
    kolmafia.putCloset(item, excessAmount);
  }
  if (kolmafia.itemType(item) === "food" || item === saladFork) {
    eatSafe(qty, item);
    if (excessAmount > 0) kolmafia.takeCloset(item, excessAmount);
    return;
  }
  if (kolmafia.itemType(item) === "booze" || item === frostyMug) {
    drinkSafe(qty, item);
    if (excessAmount > 0) kolmafia.takeCloset(item, excessAmount);
    return;
  }
  if (kolmafia.itemType(item) === "spleen item") {
    chewSafe(qty, item);
    return;
  }
  kolmafia.use(qty, item);
}
function propTrue(prop) {
  if (typeof prop === "boolean") {
    return prop;
  } else {
    return get$2(prop);
  }
}
function useIfUnused(item, prop, maxPrice) {
  if (!propTrue(prop)) {
    if (kolmafia.mallPrice(item) <= maxPrice) {
      acquire(1, item, maxPrice, false);
      if (!have$P(item)) return;
      kolmafia.use(1, item);
    } else {
      kolmafia.print(`Skipping ${item.name}; too expensive (${kolmafia.mallPrice(item)} > ${maxPrice}).`);
    }
  }
}
function nonOrganAdventures() {
  useIfUnused($item`fancy chocolate car`, get$2("_chocolatesUsed") !== 0, 2 * MPA);
  while (get$2("_loveChocolatesUsed") < 3) {
    var price = have$P($item`LOV Extraterrestrial Chocolate`) ? 15000 : 20000;
    var value = clamp(3 - get$2("_loveChocolatesUsed"), 0, 3) * get$2("valueOfAdventure");
    if (value < price) break;
    if (!have$P($item`LOV Extraterrestrial Chocolate`)) {
      Kmail.send("sellbot", `${$item`LOV Extraterrestrial Chocolate`.name} (1)`, undefined, 20000);
      kolmafia.wait(11);
      kolmafia.cliExecute("refresh inventory");
      if (!have$P($item`LOV Extraterrestrial Chocolate`)) {
        kolmafia.print("I'm tired of waiting for sellbot to send me some chocolate", "red");
        break;
      }
    }
    kolmafia.use($item`LOV Extraterrestrial Chocolate`);
  }
  var chocos = new Map([[$class`Seal Clubber`, $item`chocolate seal-clubbing club`], [$class`Turtle Tamer`, $item`chocolate turtle totem`], [$class`Pastamancer`, $item`chocolate pasta spoon`], [$class`Sauceror`, $item`chocolate saucepan`], [$class`Accordion Thief`, $item`chocolate stolen accordion`], [$class`Disco Bandit`, $item`chocolate disco ball`]]);
  var classChoco = chocos.get(kolmafia.myClass());
  var chocExpVal = (remaining, item) => {
    var advs = [0, 0, 1, 2, 3][remaining + (item === classChoco ? 1 : 0)];
    return advs * MPA - kolmafia.mallPrice(item);
  };
  var chocosRemaining = clamp(3 - get$2("_chocolatesUsed"), 0, 3);
  var _loop = function _loop(i) {
    var chocoVals = _toConsumableArray(chocos.values()).map(choc => {
      return {
        choco: choc,
        value: chocExpVal(i, choc)
      };
    });
    var best = maxBy(chocoVals, "value");
    if (best.value > 0) {
      acquire(1, best.choco, best.value + kolmafia.mallPrice(best.choco), false);
      kolmafia.use(1, best.choco);
    } else return 1; // break
  };
  for (var i = chocosRemaining; i > 0; i--) {
    if (_loop(i)) break;
  }
  useIfUnused($item`fancy chocolate sculpture`, get$2("_chocolateSculpturesUsed") > 0, 5 * MPA + 5000);
  useIfUnused($item`essential tofu`, "_essentialTofuUsed", 5 * MPA);
  if (!get$2("_etchedHourglassUsed") && have$P($item`etched hourglass`)) {
    kolmafia.use(1, $item`etched hourglass`);
  }
  if (kolmafia.getProperty("_timesArrowUsed") !== "true" && kolmafia.mallPrice($item`time's arrow`) < 5 * MPA) {
    acquire(1, $item`time's arrow`, 5 * MPA);
    kolmafia.cliExecute("csend 1 time's arrow to botticelli");
    kolmafia.setProperty("_timesArrowUsed", "true");
  }
  if (have$P($skill`Ancestral Recall`) && kolmafia.mallPrice($item`blue mana`) < 3 * MPA) {
    var casts = Math.max(10 - get$2("_ancestralRecallCasts"), 0);
    acquire(casts, $item`blue mana`, 3 * MPA);
    kolmafia.useSkill(casts, $skill`Ancestral Recall`);
  }
  if (globalOptions.ascend) {
    useIfUnused($item`borrowed time`, "_borrowedTimeUsed", 20 * MPA);
  }
  if (get$2("_extraTimeUsed", 3) < 3) {
    var extraTimeValue = timesUsed => {
      var advs = [5, 3, 1][timesUsed];
      return advs * MPA;
    };
    var extraTimeUsed = get$2("_extraTimeUsed", 3);
    for (var _i = extraTimeUsed; _i < 3; _i++) {
      if (extraTimeValue(_i) > kolmafia.mallPrice($item`extra time`)) {
        if (acquire(1, $item`extra time`, extraTimeValue(_i), false)) {
          kolmafia.use($item`extra time`);
        }
      } else break;
    }
  }
  if (get$2("_clocksUsed", 2) < 2) {
    var clockValue = timesUsed => {
      var advs = [3, 2][timesUsed];
      return advs * MPA;
    };
    var clocksUsed = get$2("_clocksUsed", 2);
    for (var _i2 = clocksUsed; _i2 < 2; _i2++) {
      if (clockValue(_i2) > kolmafia.mallPrice($item`clock`)) {
        if (acquire(1, $item`clock`, clockValue(_i2), false)) {
          kolmafia.use($item`clock`);
        }
      } else break;
    }
  }
}
function pillCheck() {
  if (!get$2("_distentionPillUsed")) {
    if (!get$2("garbo_skipPillCheck", false) && !have$P($item`distention pill`, 1)) {
      _set("garbo_skipPillCheck", userConfirmDialog("You do not have any distention pills. Continue anyway? (Defaulting to no in 15 seconds)", false, 15000));
    }
  }
  if (!get$2("_syntheticDogHairPillUsed")) {
    if (!get$2("garbo_skipPillCheck", false) && !have$P($item`synthetic dog hair pill`, 1)) {
      _set("garbo_skipPillCheck", userConfirmDialog("You do not have any synthetic dog hair pills. Continue anyway? (Defaulting to no in 15 seconds)", false, 15000));
    }
  }
}
var saladFork = $item`Ol' Scratch's salad fork`;
var frostyMug = $item`Frosty's frosty mug`;
var spleenCleaners = new Map([[$item`extra-greasy slider`, 5], [$item`jar of fermented pickle juice`, 5], [$item`mojo filter`, 1]]);
var stomachLiverCleaners = new Map([[$item`spice melange`, [-3, -3]], [$item`synthetic dog hair pill`, [0, -1]], [$item`cuppa Sobrie tea`, [0, -1]], [$item`designer sweatpants`, [0, -1]], [$item`august scepter`, [-1, 0]], [$item`Mr. Burnsger`, [4, -2]], [$item`Doc Clock's thyme cocktail`, [-2, 4]], [$item`The Plumber's mushroom stew`, [3, -1]], [$item`The Mad Liquor`, [-1, 3]]]);
function legendaryPizzaToMenu(pizzas, maker) {
  if (!globalOptions.ascend) return [];
  var canCookLegendaryPizza = pizza => {
    var recipes = [pizza].concat(_toConsumableArray($items`roasted vegetable of Jarlsberg, Pete's rich ricotta, Boris's bread`)).map(i => kolmafia.toInt(i));
    return !recipes.some(id => get$2(`unknownRecipe${id}`, true));
  };
  return pizzas.filter(_ref => {
    var item = _ref.item,
      pref = _ref.pref;
    return !get$2(pref, true) && canCookLegendaryPizza(item);
  }).map(_ref2 => {
    var item = _ref2.item;
    return maker({
      item,
      price: 2 * sum($items`Vegetable of Jarlsberg, St. Sneaky Pete's Whey, Yeast of Boris`, ingredientCost)
    });
  });
}
var cheapestItem = items => maxBy(items, MenuItem.defaultPriceFunction, true);

/**
 * Generate a basic menu of high-yield items to consider
 * @returns basic menu
 */
function menu() {
  var spaghettiBreakfast = have$P($item`spaghetti breakfast`) && kolmafia.myFullness() === 0 && get$2("_timeSpinnerFoodAvailable") === "" && !get$2("_spaghettiBreakfastEaten") ? 1 : 0;

  /*
   * generated in mafia with an account that has super human cocktail crafting
   *  > js Item.all().filter((item) => item.inebriety > 0 && item.quality === "EPIC" && getIngredients(item)["mushroom fermenting powder]).join(", ")
   */
  var complexMushroomWines = $items`overpowering mushroom wine, complex mushroom wine, smooth mushroom wine, blood-red mushroom wine, buzzing mushroom wine, swirling mushroom wine`;
  /*
   * generated in mafia with:
   *  > js Item.all().filter((item) => item.inebriety > 0 && getIngredients(item)["perfect ice cube"]).join(", ")
   */
  var perfectDrinks = $items`perfect cosmopolitan, perfect negroni, perfect dark and stormy, perfect mimosa, perfect old-fashioned, perfect paloma`;
  /*
   * generated in mafia with an account that has Transcendental Noodlecraft
   *  > js Item.all().filter((item) => item.fullness > 0 && item.name.indexOf("lasagna") > 0 && getIngredients(item)["savory dry noodles"]).join(", ")
   */
  var lasagnas = $items`fishy fish lasagna, gnat lasagna, long pork lasagna`;

  /*
   * standardSpleenItem indicates a spleen item of size 4 with an adventure yield of 5-10. Taken from the wiki. They are all functionally equivalent.
   */
  var standardSpleenItems = $items`agua de vida, gooey paste, oily paste, ectoplasmic paste, greasy paste, bug paste, hippy paste, orc paste, demonic paste, indescribably horrible paste, fishy paste, goblin paste, pirate paste, chlorophyll paste, strange paste, Mer-kin paste, slimy paste, penguin paste, elemental paste, cosmic paste, hobo paste, Crimbo paste, groose grease, Unconscious Collective Dream Jar, grim fairy tale, powdered gold`;
  var smallEpics = [].concat(_toConsumableArray($items`meteoreo, ice rice`), [$item`Tea, Earl Grey, Hot`]);
  var crimboKeyValue = garboValue(kolmafia.toItem(kolmafia.toInt(kolmafia.myId()) % 4 + $item`pirate encryption key alpha`.id));
  var boxingDayCareItems = $items`glass of raw eggs, punch-drunk punch`.filter(item => have$P(item));
  var pilsners = globalOptions.usepilsners || globalOptions.ascend ? $items`astral pilsner`.filter(item => have$P(item)) : [];
  var instantKarma = globalOptions.usekarma ? $items`Instant Karma`.filter(item => have$P(item)) : [];
  var crimboKeyItem = cheapestItem($items`corned beet, pickled bread, salted mutton`);
  var limitedItems = [].concat(_toConsumableArray(boxingDayCareItems), _toConsumableArray(pilsners), _toConsumableArray(instantKarma)).map(item => new MenuItem(item, {
    maximum: kolmafia.availableAmount(item)
  }));
  var legendaryPizzas = legendaryPizzaToMenu([{
    item: $item`Calzone of Legend`,
    pref: "calzoneOfLegendEaten"
  }, {
    item: $item`Pizza of Legend`,
    pref: "pizzaOfLegendEaten"
  }], out => new MenuItem(out.item, {
    maximum: 1,
    priceOverride: out.price
  }));
  var dailySpecialItem = hasMoonZoneRestaurant() && get$2("_dailySpecialPrice") < kolmafia.mallPrice(kolmafia.dailySpecial()) ? [new MenuItem(kolmafia.dailySpecial(), {
    priceOverride: get$2("_dailySpecialPrice")
  })] : [];
  return [
  // FOOD
  new MenuItem($item`Dreadsylvanian cold pocket`), new MenuItem($item`Dreadsylvanian hot pocket`), new MenuItem($item`Dreadsylvanian sleaze pocket`), new MenuItem($item`Dreadsylvanian stink pocket`), new MenuItem($item`Dreadsylvanian spooky pocket`), new MenuItem($item`tin cup of mulligan stew`), new MenuItem($item`frozen banquet`), new MenuItem($item`deviled egg`), new MenuItem($item`spaghetti breakfast`, {
    maximum: spaghettiBreakfast
  }), new MenuItem($item`extra-greasy slider`), new MenuItem(cheapestItem(lasagnas)), new MenuItem(cheapestItem(smallEpics)), new MenuItem($item`green hamhock`)].concat(_toConsumableArray(legendaryPizzas.flat()), [
  // BOOZE
  new MenuItem($item`elemental caipiroska`), new MenuItem($item`moreltini`), new MenuItem($item`Dreadsylvanian cold-fashioned`), new MenuItem($item`Dreadsylvanian dank and stormy`), new MenuItem($item`Dreadsylvanian grimlet`), new MenuItem($item`Dreadsylvanian hot toddy`), new MenuItem($item`Dreadsylvanian slithery nipple`), new MenuItem($item`Hodgman's blanket`), new MenuItem($item`Sacramento wine`), new MenuItem($item`iced plum wine`), new MenuItem($item`splendid martini`), new MenuItem($item`low tide martini`), new MenuItem($item`yam martini`), new MenuItem($item`Eye and a Twist`), new MenuItem($item`jar of fermented pickle juice`), new MenuItem(cheapestItem(complexMushroomWines)), new MenuItem(cheapestItem(perfectDrinks)), new MenuItem($item`green eggnog`), new MenuItem($item`can of Brütalbräu`, {
    additionalValue: garboValue($item`fancy tin beer can`)
  }), new MenuItem($item`can of Drooling Monk`, {
    additionalValue: garboValue($item`fancy tin beer can`)
  }), new MenuItem($item`can of Impetuous Scofflaw`, {
    additionalValue: garboValue($item`fancy tin beer can`)
  }),
  // SPLEEN
  new MenuItem($item`octolus oculus`), new MenuItem($item`prismatic wad`), new MenuItem($item`transdermal smoke patch`), new MenuItem($item`antimatter wad`), new MenuItem($item`voodoo snuff`), new MenuItem($item`blood-drive sticker`), new MenuItem(cheapestItem(standardSpleenItems)), new MenuItem(cheapestItem($items`not-a-pipe, glimmering roc feather`))], _toConsumableArray(limitedItems), _toConsumableArray(crimboKeyValue >= kolmafia.mallPrice(crimboKeyItem) ? [new MenuItem(crimboKeyItem, {
    additionalValue: crimboKeyValue,
    maximum: clamp(
    // Restrict to a 3rd of our open stomach, capped at 5 to avoid using stomach cleansers
    Math.floor((kolmafia.fullnessLimit() - kolmafia.myFullness()) / 3), 0, 5)
  })] : []), dailySpecialItem, [
  // HELPERS
  new MenuItem($item`distention pill`), new MenuItem($item`cuppa Voraci tea`), new MenuItem(Mayo.flex), new MenuItem(Mayo.zapine), new MenuItem($item`Special Seasoning`), new MenuItem($item`mini kiwi aioli`), new MenuItem($item`whet stone`), new MenuItem(saladFork), new MenuItem(frostyMug), new MenuItem($item`mojo filter`), new MenuItem($item`pocket wish`, {
    maximum: 1,
    effect: $effect`Refined Palate`
  }), new MenuItem($item`toasted brie`, {
    maximum: 1
  }), new MenuItem($item`potion of the field gar`, {
    maximum: 1
  })], _toConsumableArray(_toConsumableArray(stomachLiverCleaners.keys()).map(item => new MenuItem(item))), [new MenuItem($item`sweet tooth`, {
    size: -1,
    organ: "food",
    maximum: get$2("_sweetToothUsed") ? 0 : 1
  }), new MenuItem($item`designer sweatpants`, {
    size: -1,
    organ: "booze",
    maximum: availableCasts$1($skill`Sweat Out Some Booze`)
  }), new MenuItem($item`august scepter`, {
    size: -1,
    organ: "food",
    maximum: shouldAugustCast($skill`Aug. 16th: Roller Coaster Day!`) ? 1 : 0
  })]).filter(item => item.price() < Infinity);
}
function bestConsumable(organType) {
  var levelRestrict = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : true;
  var restrictList = arguments.length > 2 ? arguments[2] : undefined;
  var maxSize = arguments.length > 3 ? arguments[3] : undefined;
  var fullMenu = potionMenu(menu(), 0, 0);
  var organMenu = fullMenu.filter(menuItem => kolmafia.itemType(menuItem.item) === organType);
  if (restrictList) {
    if (restrictList instanceof kolmafia.Item) {
      organMenu = organMenu.filter(menuItem => restrictList !== menuItem.item);
    } else {
      organMenu = organMenu.filter(menuItem => !restrictList.includes(menuItem.item));
    }
  }
  if (maxSize) {
    organMenu = organMenu.filter(menuItem => menuItem.size <= maxSize);
  }
  if (levelRestrict) {
    organMenu = organMenu.filter(menuItem => menuItem.item.levelreq <= kolmafia.myLevel());
  }
  var organList = organMenu.map(consumable => {
    var edible = consumable.item;
    var buffs = get$1("Effect", edible);
    var turnsPerUse = get$1("Effect Duration", edible);
    var meatDrop = sum(buffs, buff => get$1("Meat Drop", kolmafia.toEffect(buff)));
    var famWeight = sum(buffs, buff => get$1("Familiar Weight", kolmafia.toEffect(buff)));
    var buffValue = (meatDrop + famWeight * 25 / 10) * turnsPerUse * targetMeat() / 100;
    var advValue = getAverageAdventures(edible) * get$2("valueOfAdventure");
    var organSpace = consumable.size;
    return {
      edible: edible,
      value: (buffValue + advValue - kolmafia.mallPrice(edible)) / organSpace
    };
  });
  var best = maxBy(organList, "value");
  return best;
}
function gregariousCount() {
  var gregariousCharges = get$2("beGregariousCharges") + (get$2("beGregariousFightsLeft") > 0 && get$2("beGregariousMonster") === globalOptions.target ? 1 : 0);
  var gregariousFightsPerCharge = expectedGregs("extro");
  // remove and preserve the last index - that is the marginal count of gregarious fights
  var marginalGregariousFights = gregariousFightsPerCharge.splice(gregariousFightsPerCharge.length - 1, 1)[0];
  var expectedGregariousFights = gregariousFightsPerCharge.slice(gregariousCharges);
  return {
    expectedGregariousFights,
    marginalGregariousFights
  };
}
function copiers() {
  var targetDifferential = targetingMeat() ? MEAT_TARGET_MULTIPLIER() * MPA : 0;
  var _gregariousCount = gregariousCount(),
    expectedGregariousFights = _gregariousCount.expectedGregariousFights,
    marginalGregariousFights = _gregariousCount.marginalGregariousFights;
  var extros = kolmafia.myInebriety() > kolmafia.inebrietyLimit() ? [] : [].concat(_toConsumableArray(expectedGregariousFights.map(targets => new MenuItem($item`Extrovermectin™`, {
    additionalValue: targets * targetDifferential,
    maximum: 1
  }))), [new MenuItem($item`Extrovermectin™`, {
    additionalValue: marginalGregariousFights * targetDifferential
  })]);
  return _toConsumableArray(extros);
}
function countCopies(diet) {
  // this only counts the copies not yet realized
  // any copies already realized will be properly counted by copyTargetCount

  // returns an array of expected counts for number of greg copies to fight per pill use
  // the last value is how much you expect to fight per pill
  var extros = sum(diet.entries, _ref3 => {
    var menuItems = _ref3.menuItems,
      quantity = _ref3.quantity;
    return menuItems.some(menuItem => menuItem.item === $item`Extrovermectin™`) ? quantity : 0;
  });
  var _gregariousCount2 = gregariousCount(),
    expectedGregariousFights = _gregariousCount2.expectedGregariousFights,
    marginalGregariousFights = _gregariousCount2.marginalGregariousFights;

  // slice will never return an array that is bigger than the original array
  var replaceExtros = sumNumbers(expectedGregariousFights.slice(0, extros));
  var bonusExtros = clamp(extros - expectedGregariousFights.length, 0, extros) * marginalGregariousFights;
  return replaceExtros + bonusExtros;
}
function ingredientCost(item) {
  var ingredientMallPrice = kolmafia.mallPrice(item);
  var ingredientAutosellPrice = kolmafia.autosellPrice(item);
  if (!have$P(item) || item.tradeable && ingredientMallPrice > Math.max(100, 2 * ingredientAutosellPrice)) {
    return ingredientMallPrice;
  }
  return ingredientAutosellPrice;
}

/**
 * Generate a potion diet that has entries
 * @param targets number of target monsters expected to be encountered on this day
 * @param turns number of turns total expecte
 */
function potionMenu(baseMenu, targets, turns) {
  function limitedPotion(input, limit) {
    var _GarboWorkshed$curren;
    var options = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : {};
    if (limit === 0) {
      return [];
    }
    var potion = input instanceof kolmafia.Item ? new Potion(input) : input;
    var mayo = undefined;
    if (kolmafia.itemType(potion.potion) === "food" && (((_GarboWorkshed$curren = GarboWorkshed.current) === null || _GarboWorkshed$curren === void 0 ? void 0 : _GarboWorkshed$curren.workshed) === $item`portable Mayo Clinic` || switchingToMayo())) {
      potion = potion.doubleDuration();
      mayo = Mayo.zapine;
    }
    return potion.value(targets, turns, limit).map(tier => new MenuItem(potion.potion, {
      maximum: tier.quantity,
      additionalValue: tier.value,
      priceOverride: options.price,
      organ: options.organ,
      size: options.size,
      data: tier.name,
      mayo
    }));
  }
  function potion(potion) {
    var options = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {};
    return limitedPotion(potion, undefined, options);
  }
  var speakeasy = $item`Clan speakeasy`;
  var hasSpeakeasy = kolmafia.getClanLounge()[`${speakeasy}`];
  var twiceHauntedPrice = Math.min(ingredientCost($item`haunted orange`), ingredientCost($item`orange`) + ingredientCost($item`ghostly ectoplasm`)) + Math.min(ingredientCost($item`haunted bottle of vodka`), ingredientCost($item`bottle of vodka`) + ingredientCost($item`ghostly ectoplasm`));
  var campfireHotdog = get$2("getawayCampsiteUnlocked") ? potion($item`campfire hot dog`, {
    price: ingredientCost($item`stick of firewood`)
  }) : [];
  var foodCone = realmAvailable("stench") || globalOptions.simdiet && !globalOptions.nobarf ? limitedPotion($item`Dinsey food-cone`, Math.floor(kolmafia.availableAmount($item`FunFunds™`) / 2), {
    price: 2 * garboValue($item`FunFunds™`)
  }) : [];
  var borisBread = !get$2("unknownRecipe10978") // this property is true if you don't know the recipe, false if you do
  ? potion($item`Boris's bread`, {
    price: 2 * ingredientCost($item`Yeast of Boris`)
  }) : [];
  var deepDish = legendaryPizzaToMenu([{
    item: $item`Deep Dish of Legend`,
    pref: "deepDishOfLegendEaten"
  }], out => limitedPotion(out.item, 1, {
    price: out.price
  }));
  return [].concat(_toConsumableArray(baseMenu), _toConsumableArray(copiers()), _toConsumableArray(potion($item`jumping horseradish`)), _toConsumableArray(potion($item`tempura cauliflower`)), _toConsumableArray(potion($item`sea truffle`)), _toConsumableArray(potion($item`tempura broccoli`)), _toConsumableArray(potion($item`Miserable Pie`)), _toConsumableArray(potion($item`Every Day is Like This Sundae`)), _toConsumableArray(potion($item`bowl of mummy guts`)), _toConsumableArray(potion($item`haunted Hell ramen`)), _toConsumableArray(campfireHotdog), _toConsumableArray(foodCone), _toConsumableArray(borisBread), _toConsumableArray(deepDish.flat()), _toConsumableArray(potion($item`dirt julep`)), _toConsumableArray(potion($item`Ambitious Turkey`)), _toConsumableArray(potion($item`Friendly Turkey`)), _toConsumableArray(potion($item`vintage smart drink`)), _toConsumableArray(potion($item`Strikes Again Bigmouth`)), _toConsumableArray(potion($item`Irish Coffee, English Heart`)), _toConsumableArray(potion($item`Jack-O-Lantern beer`)), _toConsumableArray(potion($item`Amnesiac Ale`)), _toConsumableArray(potion($item`mentholated wine`)), _toConsumableArray(potion($item`Feliz Navidad`)), _toConsumableArray(potion($item`broberry brogurt`)), _toConsumableArray(potion($item`haunted martini`)), _toConsumableArray(potion($item`bottle of Greedy Dog`)), _toConsumableArray(potion($item`twice-haunted screwdriver`, {
    price: twiceHauntedPrice
  })), _toConsumableArray(limitedPotion($item`high-end ginger wine`, kolmafia.availableAmount($item`high-end ginger wine`))), _toConsumableArray(limitedPotion($item`Hot Socks`, hasSpeakeasy ? 3 : 0, {
    price: 5000
  })), _toConsumableArray(realmAvailable("sleaze") && kolmafia.sellsItem($coinmaster`The Frozen Brogurt Stand`, $item`broberry brogurt`) ? limitedPotion($item`broberry brogurt`, Math.floor(kolmafia.itemAmount($item`Beach Buck`) / 10), {
    price: 10 * garboValue($item`Beach Buck`)
  }) : []), _toConsumableArray(potion($item`cute mushroom`)), _toConsumableArray(potion($item`beggin' cologne`)), _toConsumableArray(potion($item`Knob Goblin nasal spray`)), _toConsumableArray(potion($item`handful of Smithereens`)), _toConsumableArray(potion($item`black striped oyster egg`)), _toConsumableArray(potion($item`black paisley oyster egg`)), _toConsumableArray(potion($item`black polka-dot oyster egg`)), _toConsumableArray(potion($item`lustrous oyster egg`)), _toConsumableArray(potion($item`glimmering buzzard feather`)), _toConsumableArray(potion($item`Knob Goblin pet-buffing spray`)), _toConsumableArray(potion($item`abstraction: joy`)), _toConsumableArray(potion($item`beastly paste`)), _toConsumableArray(potion($item`gleaming oyster egg`)), _toConsumableArray(potion($item`Party-in-a-Can™`)), _toConsumableArray(limitedPotion($item`body spradium`, clamp(kolmafia.availableAmount($item`body spradium`), 0, 1))), _toConsumableArray(have$P($skill`Sweet Synthesis`) ? potion(new Potion($item`Rethinking Candy`, {
    effect: $effect`Synthesis: Greed`,
    duration: 30
  }), {
    size: 1,
    organ: "spleen item",
    price: 0
  }) : []));
}
function balanceMenu(baseMenu, dietPlanner) {
  var baseTargets = highMeatMonsterCount();
  function rebalance(menu, iterations, targets, adventures) {
    var fullMenu = potionMenu(menu, baseTargets + targets, estimatedGarboTurns(false) + adventures);
    if (iterations <= 0) {
      return fullMenu;
    } else {
      var balancingDiet = dietPlanner(fullMenu);
      return rebalance(menu, iterations - 1, countCopies(balancingDiet), balancingDiet.expectedAdventures());
    }
  }
  var baseDiet = dietPlanner(baseMenu);
  return rebalance(baseMenu, 5, 0, baseDiet.expectedAdventures());
}
function computeDiet() {
  kolmafia.print("Calculating diet, please wait...", HIGHLIGHT);
  // Handle spleen manually, as the diet planner doesn't support synth. Only fill food and booze.

  var orEmpty = diet => diet.expectedValue(MPA, "net") < 0 ? new Diet() : diet;
  var fullDietPlanner = menu => orEmpty(Diet.plan(MPA, menu));
  var shotglassDietPlanner = menu => orEmpty(Diet.plan(MPA, menu, {
    booze: 1
  }));
  var pantsgivingDietPlanner = menu => orEmpty(Diet.plan(MPA, menu, {
    food: 1
  }));
  var sweatpantsDietPlanner = menu => orEmpty(Diet.plan(MPA, menu, {
    booze: getRemainingLiver()
  }));
  // const shotglassFilter = (menuItem: MenuItem)

  return {
    diet: () => fullDietPlanner(balanceMenu(menu().filter(menuItem => !priceCaps[menuItem.item.name] || priceCaps[menuItem.item.name] >= kolmafia.mallPrice(menuItem.item)), fullDietPlanner)),
    shotglass: () => shotglassDietPlanner(balanceMenu(menu().filter(menuItem => kolmafia.itemType(menuItem.item) === "booze" && menuItem.size === 1), shotglassDietPlanner)),
    pantsgiving: () => pantsgivingDietPlanner(balanceMenu(menu().filter(menuItem => kolmafia.itemType(menuItem.item) === "food" && menuItem.size === 1 || [Mayo.flex, Mayo.zapine, $item`Special Seasoning`, $item`mini kiwi aioli`, $item`whet stone`].includes(menuItem.item)), pantsgivingDietPlanner)),
    sweatpants: () => sweatpantsDietPlanner(balanceMenu(menu().filter(menuItem => kolmafia.itemType(menuItem.item) === "booze" && menuItem.size <= 3), sweatpantsDietPlanner))
  };
}
function printDiet(diet, name) {
  kolmafia.print(`===== ${name} DIET =====`);
  if (diet.entries.length === 0) return;
  diet = diet.copy();
  diet.entries.sort((a, b) => itemPriority(b.menuItems) - itemPriority(a.menuItems));
  var targets = Math.floor(highMeatMonsterCount() + countCopies(diet));
  var adventures = Math.floor(estimatedGarboTurns(false) + diet.expectedAdventures());
  kolmafia.print(`Planning to fight ${targets} ${globalOptions.target} and run ${adventures} adventures`);
  var _iterator = _createForOfIteratorHelper(diet.entries),
    _step;
  try {
    for (_iterator.s(); !(_step = _iterator.n()).done;) {
      var dietEntry = _step.value;
      if (dietEntry.quantity === 0) continue;
      var target = dietEntry.target();
      var datastr = target.data ? `(${target.data})` : "";
      var maxstr = target.maximum ? ` (max ${target.maximum})` : "";
      var helpersstr = dietEntry.helpers().length > 0 ? ` helpers: ${dietEntry.helpers().join(", ")}` : "";
      var addvalstr = target.additionalValue ? ` (additional value: ${target.additionalValue})` : "";
      var valuestr = `value: ${Math.floor(dietEntry.expectedValue(MPA, diet))}${addvalstr} price: ${Math.floor(dietEntry.expectedPrice())}`;
      kolmafia.print(`${dietEntry.quantity}${maxstr} ${target}${datastr}${helpersstr} ${valuestr}`);
    }
  } catch (err) {
    _iterator.e(err);
  } finally {
    _iterator.f();
  }
  var totalValue = diet.expectedValue(MPA);
  var totalCost = diet.expectedPrice();
  var netValue = totalValue - totalCost;
  kolmafia.print(`Assuming MPA of ${MPA}, Total Cost ${totalCost}, Total Value ${totalValue}, Net Value ${netValue}`);
}

// Item priority - higher means we eat it first.
// Anything that gives a consumption buff should go first (e.g. Refined Palate).
function itemPriority(menuItems) {
  // Last menu item is the food itself.
  var menuItem = menuItems[menuItems.length - 1];
  if (menuItem === undefined) {
    throw "Shouldn't have an empty menu item.";
  }
  if (menuItem.item === $item`spaghetti breakfast`) return 200;
  if ($items`pocket wish, toasted brie`.includes(menuItem.item) || spleenCleaners.get(menuItem.item) || stomachLiverCleaners.get(menuItem.item)) {
    return 100;
  } else {
    return 0;
  }
}
function consumeDiet(diet, name) {
  if (diet.entries.length === 0) return;
  diet = diet.copy();
  diet.entries.sort((a, b) => itemPriority(b.menuItems) - itemPriority(a.menuItems));
  kolmafia.print();
  printDiet(diet, name);
  kolmafia.print();
  var seasoningCount = sum(diet.entries, _ref4 => {
    var menuItems = _ref4.menuItems,
      quantity = _ref4.quantity;
    return menuItems.some(menuItem => menuItem.item === $item`Special Seasoning`) ? quantity : 0;
  });
  acquire(seasoningCount, $item`Special Seasoning`, MPA);

  // Fill organs in rounds, making sure we're making progress in each round.
  var organs = () => [kolmafia.myFullness(), kolmafia.myInebriety(), kolmafia.mySpleenUse()];
  var lastOrgans = [-1, -1, -1];
  var capacities = () => [kolmafia.fullnessLimit(), kolmafia.inebrietyLimit(), kolmafia.spleenLimit()];
  var lastCapacities = [-1, -1, -1];
  var currentQuantity = sum(diet.entries, "quantity");
  var lastQuantity = -1;
  while (currentQuantity > 0) {
    if (arrayEquals(lastOrgans, organs()) && arrayEquals(lastCapacities, capacities()) && lastQuantity === currentQuantity) {
      kolmafia.print();
      printDiet(diet, "REMAINING");
      kolmafia.print();
      throw "Failed to consume some diet item.";
    }
    lastOrgans = organs();
    lastCapacities = capacities();
    lastQuantity = currentQuantity;
    var _iterator2 = _createForOfIteratorHelper(diet.entries),
      _step2;
    try {
      for (_iterator2.s(); !(_step2 = _iterator2.n()).done;) {
        var dietEntry = _step2.value;
        var menuItems = dietEntry.menuItems,
          quantity = dietEntry.quantity;
        if (quantity === 0) continue;
        var countToConsume = quantity;
        var capacity = {
          food: kolmafia.fullnessLimit() - kolmafia.myFullness(),
          booze: kolmafia.inebrietyLimit() - kolmafia.myInebriety(),
          "spleen item": kolmafia.spleenLimit() - kolmafia.mySpleenUse()
        };
        var _iterator3 = _createForOfIteratorHelper(menuItems),
          _step3;
        try {
          for (_iterator3.s(); !(_step3 = _iterator3.n()).done;) {
            var _menuItem = _step3.value;
            kolmafia.logprint(`Considering item ${_menuItem.item}.`);
            if (_menuItem.organ === "booze" && _menuItem.size === 1 && !get$2("_mimeArmyShotglassUsed")) {
              countToConsume = 1;
            } else if (_menuItem.organ && _menuItem.size > 0) {
              countToConsume = Math.min(countToConsume, Math.floor(capacity[_menuItem.organ] / _menuItem.size));
            }
            kolmafia.logprint(`Based on organ size, planning to consume ${countToConsume}.`);
            var cleaning = stomachLiverCleaners.get(_menuItem.item);
            if (cleaning) {
              var _cleaning = _slicedToArray(cleaning, 2),
                fullness = _cleaning[0],
                inebriety = _cleaning[1];
              countToConsume = Math.min(fullness < 0 ? Math.floor(-kolmafia.myFullness() / fullness) : quantity, inebriety < 0 ? Math.floor(-kolmafia.myInebriety() / inebriety) : quantity, countToConsume);
              kolmafia.logprint(`Based on organ-cleaning, planning to consume ${countToConsume}.`);
            }
            var spleenCleaned = spleenCleaners.get(_menuItem.item);
            if (spleenCleaned) {
              countToConsume = Math.min(countToConsume, Math.floor(kolmafia.mySpleenUse() / spleenCleaned));
              kolmafia.logprint(`Based on organ-cleaning, planning to consume ${countToConsume}.`);
            }
          }
        } catch (err) {
          _iterator3.e(err);
        } finally {
          _iterator3.f();
        }
        if (countToConsume === 0) continue;
        var elementalResistAction = element => {
          return (countToConsume, menuItem) => {
            if (kolmafia.myMaxhp() < 1000 * (1 - kolmafia.elementalResistance(element) / 100)) {
              maximizeCached(["0.05 HP", `${element} Resistance`]);
              if (kolmafia.myMaxhp() < 1000 * (1 - kolmafia.elementalResistance(element) / 100)) {
                throw `Could not achieve enough ${element} resistance for ${menuItem.item}.`;
              }
            }
            consumeSafe(countToConsume, menuItem.item);
          };
        };
        var speakeasyDrinks = Object.keys(kolmafia.getClanLounge()).map(s => kolmafia.toItem(s)).filter(i => i.inebriety > 0).map(drink => [drink, (countToConsume, menuItem) => {
          kolmafia.cliExecute(`drink ${countToConsume} ${menuItem.item}`);
        }]);
        var mayoActions = Object.values(Mayo).map(i => [i, (countToConsume, menuItem) => {
          setMayoMinder(menuItem.item, countToConsume);
        }]);
        var itemActions = new Map([[saladFork, elementalResistAction($element`hot`)], [frostyMug, elementalResistAction($element`cold`)], [$item`pocket wish`, (countToConsume, menuItem) => acquire(countToConsume, $item`pocket wish`, 60000) && kolmafia.cliExecute(`genie effect ${menuItem.effect}`)], [$item`campfire hot dog`, (countToConsume, menuItem) => {
          // mafia does not support retrieveItem on campfire hot dog because it does not work on stick of firewood
          if (!have$P($item`stick of firewood`)) {
            kolmafia.buy(1, $item`stick of firewood`, ingredientCost($item`stick of firewood`));
          }
          consumeSafe(countToConsume, menuItem.item);
        }], [$item`Special Seasoning`, "skip"], [$item`mini kiwi aioli`, (countToConsume, menuItem) => {
          kolmafia.retrieveItem(menuItem.item, countToConsume);
          kolmafia.use(menuItem.item);
        }], [$item`Rethinking Candy`, (countToConsume, menuItem) => synthesize(countToConsume, menuItem.effect ?? $effect`Synthesis: Greed`)]].concat(_toConsumableArray(mayoActions), _toConsumableArray(speakeasyDrinks), [[$item`broberry brogurt`, (countToConsume, menuItem) => {
          var amountNeeded = countToConsume - kolmafia.availableAmount($item`broberry brogurt`);
          if (amountNeeded > 0) {
            var coinmasterPrice = realmAvailable("sleaze") && kolmafia.sellsItem($coinmaster`The Frozen Brogurt Stand`, $item`broberry brogurt`) ? 10 * garboValue($item`Beach Buck`) : Infinity;
            var regularPrice = kolmafia.mallPrice($item`broberry brogurt`);
            if (coinmasterPrice < regularPrice) {
              var amountToBuy = Math.min(amountNeeded, Math.floor(kolmafia.itemAmount($item`Beach Buck`)));
              kolmafia.buy($coinmaster`The Frozen Brogurt Stand`, amountToBuy, $item`broberry brogurt`);
            }
            kolmafia.buy(countToConsume - kolmafia.availableAmount($item`broberry brogurt`), $item`broberry brogurt`);
          }
          consumeSafe(countToConsume, menuItem.item, menuItem.additionalValue);
        }], [$item`designer sweatpants`, countToConsume => {
          for (var n = 1; n <= countToConsume; n++) {
            kolmafia.useSkill($skill`Sweat Out Some Booze`);
          }
        }], [$item`august scepter`, () => kolmafia.useSkill($skill`Aug. 16th: Roller Coaster Day!`)]]));
        var _iterator4 = _createForOfIteratorHelper(menuItems),
          _step4;
        try {
          for (_iterator4.s(); !(_step4 = _iterator4.n()).done;) {
            var _menuItem2 = _step4.value;
            var itemAction = itemActions.get(_menuItem2.item);
            if (itemAction === "skip") {
              continue;
            } else if (itemAction) {
              itemAction(countToConsume, _menuItem2);
            } else {
              consumeSafe(countToConsume, _menuItem2.item, _menuItem2.additionalValue);
            }
          }
        } catch (err) {
          _iterator4.e(err);
        } finally {
          _iterator4.f();
        }
        dietEntry.quantity -= countToConsume;
      }
    } catch (err) {
      _iterator2.e(err);
    } finally {
      _iterator2.f();
    }
    currentQuantity = sum(diet.entries, "quantity");
  }
}
function dailySpecialPrice(item) {
  if (!hasMoonZoneRestaurant() || item !== get$2("_dailySpecial")) return 0;
  return get$2("_dailySpecialPrice");
}
MenuItem.defaultPriceFunction = item => {
  var prices = [kolmafia.retrievePrice(item), kolmafia.mallPrice(item), kolmafia.npcPrice(item), dailySpecialPrice(item)].filter(p => p > 0 && p < Number.MAX_SAFE_INTEGER);
  if (prices.length > 0) {
    return Math.min.apply(Math, _toConsumableArray(prices));
  }
  return !item.tradeable && have$P(item) ? 0 : Infinity;
};
function runDiet() {
  withVIPClan(() => {
    // Strip organ capacity enhancers to avoid accidental overfilling.
    if (kolmafia.myFamiliar() === $familiar`Stooper`) {
      kolmafia.useFamiliar($familiar.none);
    }
    var _iterator5 = _createForOfIteratorHelper(kolmafia.Slot.all()),
      _step5;
    try {
      for (_iterator5.s(); !(_step5 = _iterator5.n()).done;) {
        var slot = _step5.value;
        var item = kolmafia.equippedItem(slot);
        if (kolmafia.numericModifier(item, $modifier`stomach capacity`) || kolmafia.numericModifier(item, $modifier`liver capacity`) || kolmafia.numericModifier(item, $modifier`spleen capacity`)) {
          unequip(slot);
        }
      }

      // Compute diet
    } catch (err) {
      _iterator5.e(err);
    } finally {
      _iterator5.f();
    }
    var dietBuilder = computeDiet();
    if (globalOptions.simdiet) {
      kolmafia.print("===== SIMULATED DIET =====");
      if (!get$2("_mimeArmyShotglassUsed") && have$P($item`mime army shotglass`)) {
        printDiet(dietBuilder.shotglass(), "SHOTGLASS");
      }
      printDiet(dietBuilder.diet(), "FULL");
    } else {
      if (switchingToMayo()) {
        var _GarboWorkshed$curren2, _GarboWorkshed$useNex;
        if (((_GarboWorkshed$curren2 = GarboWorkshed.current) === null || _GarboWorkshed$curren2 === void 0 ? void 0 : _GarboWorkshed$curren2.workshed) === $item`Asdon Martin keyfob (on ring)`) {
          drive(FarmingStrategy.asdonEffect, dietAdventures(dietBuilder.diet()) + (globalOptions.ascend ? 0 : estimatedTurnsTomorrow));
        } else {
          var _GarboWorkshed$curren3, _GarboWorkshed$curren4;
          (_GarboWorkshed$curren3 = GarboWorkshed.current) === null || _GarboWorkshed$curren3 === void 0 || (_GarboWorkshed$curren4 = _GarboWorkshed$curren3.action) === null || _GarboWorkshed$curren4 === void 0 || _GarboWorkshed$curren4.call(_GarboWorkshed$curren3);
        }
        if (((_GarboWorkshed$useNex = GarboWorkshed.useNext()) === null || _GarboWorkshed$useNex === void 0 ? void 0 : _GarboWorkshed$useNex.workshed) !== $item`portable Mayo Clinic`) {
          throw new Error("Failed to switch to portable Mayo clinic");
        }
      }
      pillCheck();
      nonOrganAdventures();
      if (have$P($item`astral six-pack`)) {
        kolmafia.use($item`astral six-pack`);
      }
      if (!get$2("_mimeArmyShotglassUsed") && have$P($item`mime army shotglass`)) {
        consumeDiet(dietBuilder.shotglass(), "SHOTGLASS");
      }
      if (get$2("barrelShrineUnlocked") && !get$2("_barrelPrayer") && $classes`Turtle Tamer, Accordion Thief`.includes(kolmafia.myClass())) {
        kolmafia.cliExecute("barrelprayer buff");
      }
      consumeDiet(dietBuilder.diet(), "FULL");
      shrugBadEffects();
    }
  });
  globalOptions.dietCompleted = true;
}
var PRE_DIET_WORKSHEDS = [undefined].concat(_toConsumableArray($items`Asdon Martin keyfob (on ring), TakerSpace letter of Marque, spinning wheel`));
function switchingToMayo() {
  var _GarboWorkshed$next, _GarboWorkshed$curren5, _GarboWorkshed$curren6;
  return ((_GarboWorkshed$next = GarboWorkshed.next) === null || _GarboWorkshed$next === void 0 ? void 0 : _GarboWorkshed$next.workshed) === $item`portable Mayo Clinic` && (PRE_DIET_WORKSHEDS.includes((_GarboWorkshed$curren5 = GarboWorkshed.current) === null || _GarboWorkshed$curren5 === void 0 ? void 0 : _GarboWorkshed$curren5.workshed) || !!((_GarboWorkshed$curren6 = GarboWorkshed.current) !== null && _GarboWorkshed$curren6 !== void 0 && _GarboWorkshed$curren6.canRemove()));
}
function dietAdventures(diet) {
  return Math.floor(estimatedGarboTurns(false) + diet.expectedAdventures());
}

var pantsgivingBonuses = new Map();
function pantsgiving(mode) {
  if (!have$P($item`Pantsgiving`) || !modeUseLimitedDrops(mode)) {
    return new Map();
  }
  var count = get$2("_pantsgivingCount");
  var turnArray = [5, 50, 500, 5000];
  var index = kolmafia.myFullness() === kolmafia.fullnessLimit() ? get$2("_pantsgivingFullness") : turnArray.findIndex(x => count < x);
  var turns = turnArray[index] || 50000;
  if (turns - count > estimatedGarboTurns()) return new Map();
  var cachedBonus = pantsgivingBonuses.get(turns);
  if (cachedBonus) return new Map([[$item`Pantsgiving`, cachedBonus]]);
  var expectedSinusTurns = kolmafia.getWorkshed() === $item`portable Mayo Clinic` ? 100 : 50;
  var expectedUseableSinusTurns = globalOptions.ascend ? clamp(estimatedGarboTurns() - (turns - count) - kolmafia.haveEffect($effect`Kicked in the Sinuses`), 0, expectedSinusTurns) : expectedSinusTurns;
  var sinusVal = expectedUseableSinusTurns * 1.0 * baseMeat();
  var fullnessValue = sinusVal + get$2("valueOfAdventure") * 6.5 - (kolmafia.mallPrice($item`jumping horseradish`) + kolmafia.mallPrice($item`Special Seasoning`));
  var pantsgivingBonus = fullnessValue / (turns * 0.9);
  pantsgivingBonuses.set(turns, pantsgivingBonus);
  return new Map([[$item`Pantsgiving`, pantsgivingBonus]]);
}
function sweatpants(mode) {
  if (!have$P($item`designer sweatpants`) || mode === BonusEquipMode.MEAT_TARGET) {
    return new Map();
  }
  var needSweat = !globalOptions.ascend && sweat() < sweatCost($skill`Sweat Out Some Booze`) * 3 || sweat() < sweatCost($skill`Sweat Out Some Booze`) * potentialCasts($skill`Sweat Out Some Booze`);
  if (!needSweat) return new Map();
  var VOA = get$2("valueOfAdventure");
  var bestPerfectDrink = cheapestItem($items`perfect cosmopolitan, perfect negroni, perfect dark and stormy, perfect mimosa, perfect old-fashioned, perfect paloma`);
  var perfectDrinkValuePerDrunk = ((getAverageAdventures(bestPerfectDrink) + 3) * VOA - kolmafia.mallPrice(bestPerfectDrink)) / 3;
  var splendidMartiniValuePerDrunk = (getAverageAdventures($item`splendid martini`) + 2) * VOA;
  var bonus = Math.max(perfectDrinkValuePerDrunk, splendidMartiniValuePerDrunk) * 2 / 25;
  return new Map([[$item`designer sweatpants`, bonus]]);
}
function pantogramPants() {
  if (!have$P($item`pantogram pants`) || !get$2("_pantogramModifier").includes("Drops Items")) {
    return new Map([]);
  }

  // TODO: Calculate actual bonus value (good luck!)
  return new Map([[$item`pantogram pants`, 100]]);
}
function bagOfManyConfections() {
  if (!have$P($item`bag of many confections`) || !have$P($familiar`Stocking Mimic`)) {
    return new Map([]);
  }
  return new Map([[$item`bag of many confections`, garboAverageValue.apply(void 0, _toConsumableArray($items`Polka Pop, BitterSweetTarts, Piddles`)) / 6]]);
}
function snowSuit(mode) {
  // Ignore for MEAT_TARGET
  // Ignore for DMT, assuming mafia might get confused about the drop by the weird combats
  if (!have$P($item`Snow Suit`) || get$2("_carrotNoseDrops") >= 3 || !modeUseLimitedDrops(mode)) {
    return new Map([]);
  }
  return new Map([[$item`Snow Suit`, garboValue($item`carrot nose`) / 10]]);
}
function mayflowerBouquet(mode) {
  // +40% meat drop 12.5% of the time (effectively 5%)
  // Drops flowers 50% of the time, wiki says 5-10 a day.
  // Theorized that flower drop rate drops off but no info on wiki.
  // During testing I got 4 drops then the 5th took like 40 more adventures
  // so let's just assume rate drops by 11% with a min of 1% ¯\_(ツ)_/¯

  // Ignore for MEAT_TARGET
  // Ignore for DMT, assuming mafia might get confused about the drop by the weird combats
  if (!have$P($item`Mayflower bouquet`) || !modeUseLimitedDrops(mode)) {
    return new Map([]);
  }
  var sporadicMeatBonus = 40 * 0.125 * modeValueOfMeat(mode) / 100;
  var averageFlowerValue = garboAverageValue.apply(void 0, _toConsumableArray($items`tin magnolia, upsy daisy, lesser grodulated violet, half-orchid, begpwnia`)) * Math.max(0.01, 0.5 - get$2("_mayflowerDrops") * 0.11);
  return new Map([[$item`Mayflower bouquet`, (get$2("_mayflowerDrops") < 10 ? averageFlowerValue : 0) + sporadicMeatBonus]]);
}
function magnifyingGlass() {
  if (!have$P($item`cursed magnifying glass`) || get$2("_voidFreeFights") >= 5 || get$2("cursedMagnifyingGlassCount") >= 13) {
    return new Map();
  }
  return new Map([[$item`cursed magnifying glass`, globalOptions.prefs.valueOfFreeFight / 13]]);
}
function bindlestocking(mode) {
  // Requires a guaranteed critical hit that does not need the weapon or off-hand slots
  // Only BARF is supported as it is difficult to always crit elsewhere
  var canCrit = have$P($skill`Furious Wallop`) && kolmafia.myFury() > 0 || have$P($skill`Head in the Game`);
  if (!have$P($item`bindlestocking`) || mode !== BonusEquipMode.BARF || !canCrit) {
    return new Map();
  }

  // TODO: Confirm drop rates with excavator https://excavator.loathers.net/projects/bindlestocking
  // The only valuable items are fancy chocolate or jawbruiser which probably appear ~1% of the time
  var value = 0.49 * garboAverageValue.apply(void 0, _toConsumableArray($items`Angry Farmer candy, Cold Hots candy, Daffy Taffy, Mr. Mediocrebar, Senior Mints, Wint-O-Fresh mint, orange`)) + 0.15 * garboAverageValue.apply(void 0, _toConsumableArray($items`eggnog, fruitcake, yo-yo`)) + 0.2 * garboAverageValue.apply(void 0, _toConsumableArray($items`candy cane, ball, fancy dress ball, gingerbread bugbear, razor-tipped yo-yo`)) + 0.15 * garboAverageValue.apply(void 0, _toConsumableArray($items`buckyball, gyroscope, monomolecular yo-yo, possessed top, top`)) + 0.01 * garboAverageValue.apply(void 0, _toConsumableArray($items`fancy chocolate, jawbruiser`));
  return new Map([[$item`bindlestocking`, value]]);
}
function simpleTargetCrits(mode) {
  var canCrit = have$P($skill`Furious Wallop`) && kolmafia.myFury() > 0 || have$P($skill`Head in the Game`);
  if (!have$P($item`mafia pointer finger ring`) || mode !== BonusEquipMode.MEAT_TARGET || !canCrit || globalOptions.target.attributes.includes("FREE")) {
    return new Map();
  }
  return new Map([[$item`mafia pointer finger ring`, targetPointerRingMeat()]]);
}
function batWings(mode) {
  var batWings = $item`bat wings`;
  if (!have$h() || mode !== BonusEquipMode.BARF || flapChance() === 0) {
    return new Map();
  }
  var value = flapChance() * get$2("valueOfAdventure");
  return new Map([[batWings, value]]);
}
function bonusGear(mode) {
  var valueCircumstantialBonus = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : true;
  return new Map([].concat(_toConsumableArray(bonusAccessories(mode)), _toConsumableArray(pantogramPants()), _toConsumableArray(bagOfManyConfections()), _toConsumableArray(stickers(mode)), _toConsumableArray(powerGlove()), _toConsumableArray(sneegleebs()), _toConsumableArray(bindlestocking(mode)), _toConsumableArray(simpleTargetCrits(mode)), _toConsumableArray(batWings(mode)), _toConsumableArray(cupOfThirteens(mode)), _toConsumableArray(mobius(mode)), _toConsumableArray(valueCircumstantialBonus ? new Map([].concat(_toConsumableArray(pantsgiving(mode)), _toConsumableArray(sweatpants(mode)), _toConsumableArray(shavingBonus()), _toConsumableArray(snowSuit(mode)), _toConsumableArray(mayflowerBouquet(mode)), _toConsumableArray(mode === BonusEquipMode.BARF ? magnifyingGlass() : []), _toConsumableArray(juneCleaver$1(mode)), _toConsumableArray(rakeLeaves(mode)), _toConsumableArray(aviatorGoggles(mode)), _toConsumableArray(skeletonCane(mode)))) : [])));
}
var encounterMap = [4,
// 0
7,
// 1
14,
// 2
14,
// 3
25,
// 4
25,
// 5
41,
// 6
41,
// 7
41,
// 8
41,
// 9
41,
// 10
51,
// 11
51,
// 12
51,
// 13
51,
// 14
51,
// 15
51,
// 16
51,
// 17
51 // 18
];
function mobius(mode) {
  if (mode === BonusEquipMode.BARF) {
    var value = kolmafia.totalTurnsPlayed() - get$2("_lastMobiusStripTurn", 0) > encounterMap[get$2("_mobiusStripEncounters", 0)] - 3 ? Math.max(kolmafia.mallPrice($item`clock`), get$2("valueOfAdventure") * 3) / 2 : 0;
    return new Map([[$item`Möbius ring`, value]]);
  }
  return new Map();
}
function shavingBonus() {
  if (!have$v() || buffs.some(buff => have$P(buff, 2))) {
    return new Map();
  }
  var timeToMeatBuff = 11 * (buffsUntil($effect`Friendly Chops`) ?? Infinity);
  if (globalOptions.ascend && timeToMeatBuff > estimatedGarboTurns()) {
    return new Map();
  }
  if (!globalOptions.ascend && nextBuff() === $effect`Friendly Chops` && estimatedGarboTurns() < 11 * 11) {
    return new Map();
  }
  var bonusValue = (baseMeat() * 100 + 72 * 50) / 100;
  return new Map([[$item`Daylight Shavings Helmet`, bonusValue]]);
}
var juneCleaverEV = null;
function juneCleaver$1(mode) {
  var estimatedJuneCleaverTurns = remainingUserTurns() + estimatedGarboTurns();
  if (!have$P($item`June cleaver`) || get$2("_juneCleaverFightsLeft") > estimatedJuneCleaverTurns || !get$2("_juneCleaverFightsLeft")) {
    return new Map();
  }
  if (!juneCleaverEV) {
    juneCleaverEV = sum(_toConsumableArray(choices), choice => valueJuneCleaverOption(juneCleaverChoiceValues[choice][bestJuneCleaverOption(choice)])) / choices.length;
  }
  // If we're ascending then the chances of hitting choices in the queue is reduced
  if (globalOptions.ascend && estimatedJuneCleaverTurns <= 180 && getInterval() === 30) {
    var availEV = sum(_toConsumableArray(choicesAvailable()), choice => valueJuneCleaverOption(juneCleaverChoiceValues[choice][bestJuneCleaverOption(choice)])) / choicesAvailable().length;
    var queueEV = sum(_toConsumableArray(queue()), choice => {
      var choiceValue = valueJuneCleaverOption(juneCleaverChoiceValues[choice][bestJuneCleaverOption(choice)]);
      var cleaverEncountersLeft = Math.floor(estimatedJuneCleaverTurns / 30);
      var encountersToQueueExit = 1 + queue().indexOf(choice);
      var chancesLeft = Math.max(0, cleaverEncountersLeft - encountersToQueueExit);
      var encounterProbability = 1 - Math.pow(2 / 3, chancesLeft);
      return choiceValue * encounterProbability;
    }) / queue().length;
    juneCleaverEV = queueEV + availEV;
  }
  var interval = mode === BonusEquipMode.MEAT_TARGET ? 30 : getInterval();
  return new Map([[$item`June cleaver`, juneCleaverEV / interval]]);
}
function rakeLeaves(mode) {
  if (mode === BonusEquipMode.MEAT_TARGET || !have$o()) {
    return new Map();
  }
  var rakeValue = garboValue($item`inflammable leaf`) * 1.5;
  return new Map([[$item`rake`, rakeValue], [$item`tiny rake`, rakeValue]]);
}
function aviatorGoggles(mode) {
  if (mode === BonusEquipMode.MEAT_TARGET || !have$P($familiar`Mini Kiwi`)) {
    return new Map();
  }
  var goggleValue = garboValue($item`mini kiwi`) * 0.25;
  return new Map([[$item`aviator goggles`, goggleValue]]);
}
function skeletonCane(mode) {
  if (mode === BonusEquipMode.MEAT_TARGET || !have$P($familiar`Skeleton of Crimbo Past`) || get$2("_knuckleboneDrops") >= 100) {
    return new Map();
  }
  // Cane improves drop rate by 10%
  var caneValue = garboValue($item`knucklebone`) * 0.1;
  return new Map([[$item`small peppermint-flavored sugar walking crook`, caneValue]]);
}
function stickers(mode) {
  // This function represents the _cost_ of using stickers
  // Embezzlers are the best monster to use them on, so there's functionally no cost
  if (mode === BonusEquipMode.MEAT_TARGET) return new Map();
  var cost = sumNumbers($slots`sticker1, sticker2, sticker3`.map(s => kolmafia.mallPrice(kolmafia.equippedItem(s)) / 20));
  return new Map([[$item`scratch 'n' sniff sword`, -1 * cost], [$item`scratch 'n' sniff crossbow`, -1 * cost]]);
}
function powerGlove() {
  if (!have$P($item`Powerful Glove`)) return new Map();
  // 23% proc rate, according to the wiki
  // https://kol.coldfront.net/thekolwiki/index.php/Powerful_Glove
  return new Map([[$item`Powerful Glove`, 0.25 * garboAverageValue.apply(void 0, _toConsumableArray($items`blue pixel, green pixel, red pixel, white pixel`))]]);
}
var speakeasyBanList = $items`glass of "milk", cup of "tea", thermos of "whiskey", Lucky Lindy, Bee's Knees, Sockdollager, Ish Kabibble, Hot Socks, Phonus Balonus, Flivver, Sloppy Jalopy`;
var POSSIBLE_SNEEGLEEB_DROPS = kolmafia.Item.all().filter(i => i.tradeable && i.discardable && (i.inebriety || i.fullness || i.potion && kolmafia.stringModifier(i, kolmafia.Modifier.get("Last Available")) === "") && !speakeasyBanList.includes(i));
var sneegleebBonus;
var SNEEGLEEB_DROP_RATE = 0.13;
var MAX_SNEEGLEEB_PRICE = 100_000; // arbitrary, to help avoid outliers
function sneegleebs() {
  sneegleebBonus ?? (sneegleebBonus = sum(POSSIBLE_SNEEGLEEB_DROPS, item => Math.min(garboValue(item), MAX_SNEEGLEEB_PRICE)) / POSSIBLE_SNEEGLEEB_DROPS.length * SNEEGLEEB_DROP_RATE);
  return new Map([[$item`KoL Con 13 snowglobe`, sneegleebBonus], [$item`can of mixed everything`, sneegleebBonus / 2]].filter(_ref => {
    var _ref2 = _slicedToArray(_ref, 1),
      item = _ref2[0];
    return have$P(item);
  }));
}
function toyCupidBow(familiar) {
  if (!have$f()) return new Map();
  var turns = tcbTurnsLeft(familiar, getUsedTcbFamiliars());
  if (estimatedGarboTurns() <= turns) {
    return new Map();
  }
  return new Map([[$item`toy Cupid bow`, familiarEquipmentValue(familiar) / turns]]);
}
var CUP_OF_THIRTEENS_DROPS = POSSIBLE_SNEEGLEEB_DROPS.filter(item => item.inebriety);
var cupOfThirteensBonus;
function cupOfThirteens(mode) {
  var _cupOfThirteensBonus;
  if (!have$8() || mode !== BonusEquipMode.BARF && mode !== BonusEquipMode.FREE) {
    return new Map();
  }
  var dropsToday = get$2("_cupOf13sDrops");

  // A drop occurs at 6 charges if we haven't gotten a drop today.
  // Otherwise, the next drop occurs at 10 charges.
  var chargeRequired = dropsToday === 0 ? 6 : 10;
  var qualities = dropsToday <= 1 ? ["EPIC", "awesome"] : dropsToday <= 3 ? ["awesome", "good"] : dropsToday <= 5 ? ["good", "decent"] : ["decent", "crappy"];
  if (((_cupOfThirteensBonus = cupOfThirteensBonus) === null || _cupOfThirteensBonus === void 0 ? void 0 : _cupOfThirteensBonus[0]) !== qualities[0]) {
    var possibleDrops = CUP_OF_THIRTEENS_DROPS.filter(item => qualities.includes(item.quality));
    cupOfThirteensBonus = [qualities[0], garboAverageValue.apply(void 0, _toConsumableArray(possibleDrops))];
  }
  var cupBonus = cupOfThirteensBonus[1] / chargeRequired;
  return new Map([[$item`Cup of 13s`, cupBonus]]);
}

function meatTargetOutfit() {
  var spec = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : {};
  var adventureArgument = arguments.length > 1 ? arguments[1] : undefined;
  cleaverCheck();
  validateGarbageFoldable(spec);
  var outfit = Outfit.from(spec, new Error(`Failed to construct outfit from spec ${JSON.stringify(spec)}`));
  var _toAdventure = toAdventure(adventureArgument ?? $location.none),
    location = _toAdventure.location,
    target = _toAdventure.target;
  if (location === $location`Crab Island`) {
    var meat = kolmafia.meatDrop($monster`giant giant crab`) + songboomMeat();
    outfit.modifier.push(`${meat / 100} Meat Drop`, "-tie");
  } else if (target === $monster`Knob Goblin Embezzler` && have$P($effect`Lucky!`)) {
    var _meat = kolmafia.meatDrop($monster`Knob Goblin Embezzler`) + songboomMeat();
    outfit.modifier.push(`${_meat / 100} Meat Drop`, "-tie");
  } else {
    if (targetingMeat()) {
      outfit.modifier.push(`${modeValueOfMeat(BonusEquipMode.MEAT_TARGET)} Meat Drop`, "-tie");
    } else if (globalOptions.target.attributes.includes("FREE")) {
      outfit.modifier.push("-tie");
    }
    if (nextWeekReady()) {
      outfit.equip($item`legendary seal-clubbing club`);
    }
    if (!have$P($effect`Everything Looks Purple`) && (location === null || location === void 0 ? void 0 : location.environment) !== Environment.Underwater && !shouldRedigitize()) {
      outfit.equip($item`Roman Candelabra`);
    }
  }
  applyCheeseBonus(outfit, targetingMeat() ? BonusEquipMode.MEAT_TARGET : BonusEquipMode.FREE);
  outfit.avoid.push($item`cheap sunglasses`); // Even if we're adventuring in Barf Mountain itself, these are bad
  outfit.familiar ?? (outfit.familiar = targetingMeat() ? meatFamiliar() : freeFightFamiliar(location ?? globalOptions.target, {
    equipmentForced: !outfit.canEquip($item`toy Cupid bow`)
  }));
  var bjornChoice = chooseBjorn(targetingMeat() ? BonusEquipMode.MEAT_TARGET : BonusEquipMode.FREE, outfit.familiar);
  var underwater = (location === null || location === void 0 ? void 0 : location.environment) === "underwater";
  if (underwater) {
    if (!outfit.familiar.underwater) {
      outfit.equipFirst(familiarWaterBreathingEquipment);
    }
    if (!outfit.equipFirst(waterBreathingEquipment)) {
      outfit.modifier.push("sea");
    }
  }
  if (outfit.familiar === $familiar`Jill-of-All-Trades`) {
    outfit.equip($item`LED candle`);
    outfit.setModes({
      jillcandle: "ultraviolet"
    });
  }
  useUPCsIfNeeded(outfit);
  outfit.addBonuses(bonusGear(targetingMeat() ? BonusEquipMode.MEAT_TARGET : BonusEquipMode.FREE));
  if (!targetingMeat()) outfit.addBonuses(toyCupidBow(outfit.familiar));
  var bjornalike = bestBjornalike(outfit);
  if (location === getLocation() && turnsLeftOnQuest(false) === 1 && haveBooze()) {
    outfit.addBonus($item`Guzzlr pants`, expectedReward(true) - expectedReward(false));
  }
  if (bjornalike) {
    outfit.setBonus(bjornalike, bjornChoice.value);
    outfit.equip(bjornalike);
    var other = $items`Buddy Bjorn, Crown of Thrones`.filter(i => i !== bjornalike)[0];
    outfit.avoid.push(other);
    switch (bjornalike) {
      case $item`Buddy Bjorn`:
        outfit.bjornify(bjornChoice.familiar);
        break;
      case $item`Crown of Thrones`:
        outfit.enthrone(bjornChoice.familiar);
        break;
    }
  }
  outfit.setModes({
    snowsuit: "nose",
    parka: "kachungasaur",
    edpiece: "fish"
  });
  return outfit;
}

function embezzlerFights() {
  for (var _len = arguments.length, exludedLuckySources = new Array(_len), _key = 0; _key < _len; _key++) {
    exludedLuckySources[_key] = arguments[_key];
  }
  return sum(luckySourceTasks.filter(t => {
    var _t$ready;
    return !exludedLuckySources.includes(t.name) && (((_t$ready = t.ready) === null || _t$ready === void 0 ? void 0 : _t$ready.call(t)) ?? true) && !t.completed();
  }), t => undelay(t.turns));
}
var embezzlerFightTask = {
  name: "Fight Lucky Embezzler",
  outfit: () => meatTargetOutfit(sober() ? {} : {
    equip: $items`Drunkula's wineglass`
  }, {
    location: $location`Cobb's Knob Treasury`,
    target: $monster`Knob Goblin Embezzler`
  }),
  ready: () => kolmafia.canAdventure($location`Cobb's Knob Treasury`),
  completed: () => !have$P($effect`Lucky!`),
  do: $location`Cobb's Knob Treasury`,
  spendsTurn: true,
  combat: new GarboStrategy(() => Macro.meatKill()),
  turns: 0 // Turns spent are tracked by the lucky sources
};
var EmbezzlerFightsQuest = {
  name: "Lucky Embezzlers",
  ready: () => getBestLuckyAdventure().phase === "target" && getBestLuckyAdventure().value() > 0 && (sober() || have$P($item`Drunkula's wineglass`) && kolmafia.canEquip($item`Drunkula's wineglass`)),
  tasks: [].concat(_toConsumableArray(luckySourceTasks), [embezzlerFightTask])
};

/**
 * Computes the estimated number of turns during which garbo will run
 * @returns A guess of how many runs garbo will run in total
 */
function estimatedGarboTurns() {
  var estimateEmptyOrgans = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : true;
  // Assume roughly 2 fullness from pantsgiving and 8 adventures/fullness.
  var pantsgivingAdventures = have$P($item`Pantsgiving`) ? Math.max(0, 2 - get$2("_pantsgivingFullness")) * 8 : 0;
  var sausageAdventures = howManySausagesCouldIEat();
  var thesisAdventures = have$P($familiar`Pocket Professor`) && !get$2("_thesisDelivered") ? 11 : 0;
  var nightcapAdventures = globalOptions.ascend && kolmafia.myInebriety() <= kolmafia.inebrietyLimit() && have$P($item`Drunkula's wineglass`) ? ESTIMATED_OVERDRUNK_TURNS : 0;
  var thumbRingMultiplier = usingThumbRing() ? 1 / 0.96 : 1;

  // Estimate potential adventures from empty organs
  var unrealizedOrganAdventures = estimateEmptyOrgans ? Math.max(potentialFullnessAdventures() + potentialInebrietyAdventures() + potentialNonOrganAdventures(), 0) : 0;
  var turns;
  if (globalOptions.stopTurncount) {
    turns = globalOptions.stopTurncount - kolmafia.myTurncount();
  } else if (globalOptions.nobarf) {
    turns = targetingMeat() ? highMeatMonsterCount() : 0;
  } else if (globalOptions.saveTurns > 0 || !globalOptions.ascend) {
    turns = (kolmafia.myAdventures() + sausageAdventures + pantsgivingAdventures + thesisAdventures + unrealizedOrganAdventures - globalOptions.saveTurns) * thumbRingMultiplier;
  } else {
    turns = (kolmafia.myAdventures() + sausageAdventures + pantsgivingAdventures + nightcapAdventures + thesisAdventures + unrealizedOrganAdventures) * thumbRingMultiplier;
  }
  return turns;
}

/**
 * Computes the estimated number of turns left that the user will use outside garbo
 * @returns A guess of how many turns will be used outside garbo
 */
function remainingUserTurns() {
  var dietAdventures = Math.max(potentialFullnessAdventures() + potentialInebrietyAdventures() + potentialNonOrganAdventures(), 0);
  var turns = kolmafia.myAdventures() + dietAdventures - estimatedGarboTurns() + globalOptions.saveTurns;
  return turns;
}
var estimatedTurnsTomorrow = 400 + clamp((get$2("valueOfAdventure") - 4000) / 8, 0, 600);
function potentialFullnessAdventures() {
  var distentionPillSpace = have$P($item`distention pill`) && !get$2("_distentionPillUsed") ? 1 : 0;
  return (kolmafia.fullnessLimit() - kolmafia.myFullness() + distentionPillSpace) * 8;
}
function potentialInebrietyAdventures() {
  var syntheticPillSpace = have$P($item`synthetic dog hair pill`) && !get$2("_syntheticDogHairPillUsed") ? 1 : 0;
  var shotglassSpace = have$P($item`mime army shotglass`) && !get$2("_mimeArmyShotglassUsed") ? 1 : 0;
  var sweatSpace = have$P($item`designer sweatpants`) ? 3 - get$2("_sweatOutSomeBoozeUsed") : 0;
  return (kolmafia.inebrietyLimit() - kolmafia.myInebriety() + syntheticPillSpace + sweatSpace + shotglassSpace) * 7;
}
function potentialNonOrganAdventures() {
  var borrowedTimeAdventures = globalOptions.ascend && !get$2("_borrowedTimeUsed") ? 20 : 0;
  var chocolateAdventures = (3 - get$2("_chocolatesUsed")) * (4 - get$2("_chocolatesUsed")) / 2;
  return borrowedTimeAdventures + chocolateAdventures;
}
function wanderingCopytargetsRemaining() {
  // Todo: account for not having enough turns to do all your nextweek fights
  return digitizedMonstersRemainingForTurns(estimatedGarboTurns()) + nextWeekFights();
}
function highMeatMonsterCount() {
  var meatTargets = targetingMeat() ? copyTargetCount() : 0;
  var embezzlers = embezzlerFights.apply(void 0, arguments);
  return meatTargets + embezzlers;
}

var guaranteedBullseye = () => get$2("everfullDartPerks").includes("25% Better bullseye targeting") && get$2("everfullDartPerks").includes("25% More Accurate bullseye targeting") && get$2("everfullDartPerks").includes("25% better chance to hit bullseyes");
var DARTS_KILL_BEFORE_RUN = 5;
var dartLevelTooHigh = () => get$2("everfullDartPerks").split(",").length >= DARTS_KILL_BEFORE_RUN;
var safeToAttemptBullseye = () => have$P($item`Everfull Dart Holster`) && (guaranteedBullseye() || (have$P($item`spring shoes`) || have$P($item`Roman Candelabra`)) && !dartLevelTooHigh());
var canBullseye = () => !have$P($effect`Everything Looks Red`) && (guaranteedBullseye() || !have$P($effect`Everything Looks Green`));

function timeToMeatify() {
  if (!have$P($familiar`Grey Goose`) || get$2("_meatifyMatterUsed") || kolmafia.myInebriety() > kolmafia.inebrietyLimit()) {
    return false;
  } else if ($familiar`Grey Goose`.experience >= 400) return true;else if (!globalOptions.ascend || kolmafia.myAdventures() > 50) return false;

  // Check Wanderers
  var totalTurns = kolmafia.totalTurnsPlayed();
  var usingLatte = have$P($item`latte lovers member's mug`) && get$2("latteModifier").split(",").includes("Meat Drop: 40");
  var nextProtonicGhost = have$P($item`protonic accelerator pack`) || kolmafia.mallPrice($item`almost-dead walkie-talkie`) < globalOptions.prefs.valueOfFreeFight ? Math.max(1, get$2("nextParanormalActivity") - totalTurns) : Infinity;
  var nextVoteMonster = have$P($item`"I Voted!" sticker`) && get$2("_voteFreeFights") < 3 ? Math.max(0, (totalTurns % 11 - 1) % 11) : Infinity;
  var nextVoidMonster = have$P($item`cursed magnifying glass`) && get$2("_voidFreeFights") < 5 && globalOptions.prefs.valueOfFreeFight / 13 > baseMeat() * (usingLatte ? 0.75 : 0.6) ? -get$2("cursedMagnifyingGlassCount") % 13 : Infinity;

  // If any of the above are 0, then
  // (1) We should be fighting a free fight
  // (2) We meatify if Grey Goose is sufficiently heavy and we don't have another free wanderer in our remaining turns

  var freeFightNow = get$2("questPAGhost") !== "unstarted" || nextVoteMonster === 0 || nextVoidMonster === 0;
  var delay = Math.min(nextProtonicGhost, nextVoteMonster === 0 ? get$2("_voteFreeFights") < 2 ? 11 : Infinity : nextVoteMonster, nextVoidMonster === 0 ? 13 : nextVoidMonster);
  if (delay < kolmafia.myAdventures()) return false;
  // We can wait for the next free fight
  else if (freeFightNow || $familiar`Grey Goose`.experience >= 121) return true;
  return false;
}
function canOpenRedPresent() {
  return have$P($familiar`Crimbo Shrub`) && !have$P($effect`Everything Looks Red`) && !have$P($skill`Free-For-All`) && !(safeToAttemptBullseye() && canBullseye()) && get$2("shrubGifts") === "meat" && kolmafia.myInebriety() <= kolmafia.inebrietyLimit();
}

/**
 * Rough estimate of the  number of barf combats we expect to do. Used for marginal familiar tabulation.
 * @returns A rough estimate of the number of barf combats we expect to do.
 */
function turnsAvailable() {
  var baseTurns = estimatedGarboTurns();
  var digitizes = wanderingCopytargetsRemaining();
  var mapTurns = globalOptions.ascend ? clamp(kolmafia.availableAmount($item`Map to Safety Shelter Grimace Prime`), 0, ESTIMATED_OVERDRUNK_TURNS) : 0;
  var barfTurns = baseTurns - digitizes - mapTurns;
  var barfCombatRate = 1 - 1 / FarmingStrategy.turnsToNC();
  return barfTurns * barfCombatRate;
}
function estimatedBarfExperience() {
  var _get$match;
  var sources = [1];
  if ([$skill`Curiosity of Br'er Tarrypin`, $effect`Curiosity of Br'er Tarrypin`].some(x => have$P(x))) {
    sources.push(1);
  }
  if (have$P($skill`Testudinal Teachings`)) sources.push(1 / 6);
  var voter = (_get$match = get$2("_voteModifier").match(/Experience \(familiar\): (\d+)/)) === null || _get$match === void 0 ? void 0 : _get$match[1];
  if (voter) sources.push(Number(voter));
  return sumNumbers(sources);
}
function snapperValue() {
  var item = phylumItem.get(globalOptions.target.phylum);
  if (!item) return 0;
  var denominator = 11 - (getTrackedPhylum() === globalOptions.target.phylum ? getProgress() : 0);
  if (denominator > copyTargetCount()) return 0;
  return garboValue(item) / denominator;
}
var getUsedTcbFamiliars = () => new Set(familiarsToday());
var tcbTurnsLeft = (f, used) => used.has(f) ? Infinity : currentFamiliar() === f ? clamp(5 - get$2("cupidBowFights"), 1, 5) : 5;
var amuletCoinValue = () => {
  var _ref = isFree(globalOptions.target) ? [0, estimatedGarboTurns()] : (() => {
      var copies = highMeatMonsterCount();
      return [copies, estimatedGarboTurns() - copies];
    })(),
    _ref2 = _slicedToArray(_ref, 2),
    copies = _ref2[0],
    barf = _ref2[1];
  return 0.5 * (barf * baseMeat() + copies * targetMeat());
};
var familiarEquipmentValue = f => {
  if (f === $familiar`Cornbeefadon`) {
    return have$P($item`amulet coin`) ? 0 : amuletCoinValue();
  }
  if ($familiars`Frozen Gravy Fairy, Flaming Gravy Fairy, Sleazy Gravy Fairy, Spooky Gravy Fairy, Stinky Gravy Fairy`.includes(f)) {
    return garboValue($item`lead necklace`);
  }
  return garboValue(kolmafia.familiarEquipment(f));
};
function tcbValue(familiar, tcbFamiliars, equipmentForced, includeAmuletCoinOpportunityCost) {
  if (equipmentForced) return 0;
  if (!have$f()) return 0;
  if (tcbFamiliars.has(familiar)) return 0;
  var leprechaunMultiplier = findLeprechaunMultiplier(familiar);
  // This is only used during barf so we can just use basemeat
  // Includes a lazy linearization of the value of its leprechaun-pounds
  var amuletCoin = includeAmuletCoinOpportunityCost && have$P($item`amulet coin`) ? (50 + 10 * (2 * leprechaunMultiplier + Math.sqrt(leprechaunMultiplier))) * baseMeat() / 100 : 0;
  return familiarEquipmentValue(familiar) / tcbTurnsLeft(familiar, tcbFamiliars) - amuletCoin;
}

function shouldRedigitize() {
  if (!have$I() || !canDigitize()) return false;
  var digitizesRemaining = getDigitizeUsesRemaining();
  var digitizeChunks = 1 + digitizesRemaining;
  var monsterCount = getDigitizeMonsterCount() + 1;
  // triangular number * 10 - 3
  var digitizeAdventuresUsed = monsterCount * (monsterCount + 1) * 5 - 3;
  // Redigitize if we've spent too many turns in this "chunk" of our digitize day
  return estimatedGarboTurns() / digitizeChunks < digitizeAdventuresUsed;
}
function shouldAffirmationHate() {
  if (!kolmafia.hippyStoneBroken()) return false;
  if (get$2("_affirmationHateUsed")) return false;
  if (garboValue($item`Daily Affirmation: Keep Free Hate in your Heart`) >= globalOptions.prefs.valueOfPvPFight * 3) {
    return false;
  }
  return true;
}
function preferMuscleCombat() {
  return kolmafia.myBuffedstat($stat`Muscle`) > 100 && kolmafia.myBuffedstat($stat`Muscle`) > kolmafia.myBuffedstat($stat`Mysticality`) && (kolmafia.currentHitStat() === $stat`Muscle` || kolmafia.itemType(kolmafia.equippedItem($slot`weapon`)) === "knife");
}
var Macro = /*#__PURE__*/function (_StrictMacro) {
  function Macro() {
    _classCallCheck(this, Macro);
    return _callSuper(this, Macro, arguments);
  }
  _inherits(Macro, _StrictMacro);
  return _createClass(Macro, [{
    key: "abortWithMsg",
    value: function abortWithMsg(errorMessage) {
      return this.step(`abort "${errorMessage}"`);
    }
  }, {
    key: "tryHaveSkill",
    value: function tryHaveSkill(skill) {
      if (!skill) return this;
      return this.externalIf(kolmafia.haveSkill(skill), Macro.trySkill(skill));
    }
  }, {
    key: "tryHaveItem",
    value: function tryHaveItem(item) {
      if (!item) return this;
      return this.externalIf(have$P(item), Macro.tryItem(item));
    }
  }, {
    key: "trySingAlong",
    value: function trySingAlong() {
      if (!have$C() || song() !== "Total Eclipse of Your Meat") {
        return this;
      }
      return this.tryHaveSkill($skill`Sing Along`);
    }
  }, {
    key: "ifInnateWanderer",
    value: function ifInnateWanderer(macro) {
      // if this monster appears without action on the part of the script

      var monsters = [].concat(_toConsumableArray(kolmafia.myPath() === path ? _toConsumableArray(wanderers) : []), _toConsumableArray(getTodaysHolidayWanderers()));
      return this.externalIf(monsters.length > 0, Macro.if_(monsters, macro));
    }
  }, {
    key: "familiarActions",
    value: function familiarActions() {
      return this.externalIf(kolmafia.myFamiliar() === $familiar`Grey Goose` && timeToMeatify(), Macro.trySkill($skill`Meatify Matter`)).externalIf(canOpenRedPresent() && kolmafia.myFamiliar() === $familiar`Crimbo Shrub`, Macro.trySkill($skill`Open a Big Red Present`)).externalIf(kolmafia.myFamiliar() === $familiar`Space Jellyfish`, Macro.externalIf(get$2("_spaceJellyfishDrops") < 5, Macro.if_($locations`Barf Mountain, Pirates of the Garbage Barges, Uncle Gator's Country Fun-Time Liquid Waste Sluice, The Toxic Teacups`.flatMap(l => kolmafia.getMonsters(l)), Macro.trySkill($skill`Extract Jelly`)), Macro.trySkill($skill`Extract Jelly`)));
    }
  }, {
    key: "tryCopier",
    value: function tryCopier(itemOrSkill) {
      switch (itemOrSkill) {
        case $item`Spooky Putty sheet`:
          return this.externalIf(get$2("spookyPuttyCopiesMade") + Math.max(1, get$2("_raindohCopiesMade")) < 6 && $items`Spooky Putty sheet, Spooky Putty monster`.some(item => have$P(item)), Macro.tryItem(itemOrSkill));
        case $item`Rain-Doh black box`:
          return this.externalIf(get$2("_raindohCopiesMade") + Math.max(1, get$2("spookyPuttyCopiesMade")) < 6 && $items`Rain-Doh black box, Rain-Doh box full of monster`.some(item => have$P(item)), Macro.tryItem(itemOrSkill));
        case $item`4-d camera`:
          return this.externalIf(!get$2("_cameraUsed") && !have$P($item`shaking 4-d camera`), Macro.tryHaveItem(itemOrSkill));
        case $item`crappy camera`:
          return this.externalIf(!get$2("_crappyCameraUsed") && !have$P($item`shaking crappy camera`), Macro.tryHaveItem(itemOrSkill));
        case $item`unfinished ice sculpture`:
          return this.externalIf(!get$2("_iceSculptureUsed") && !have$P($item`ice sculpture`), Macro.tryHaveItem(itemOrSkill));
        case $item`pulled green taffy`:
          return this.externalIf(!get$2("_envyfishEggUsed") && !have$P($item`envyfish egg`), Macro.tryHaveItem(itemOrSkill));
        case $item`print screen button`:
          return this.tryHaveItem(itemOrSkill);
        case $item`alpine watercolor set`:
          return this.tryHaveItem(itemOrSkill);
        case $item`LOV Enamorang`:
          return this.externalIf(get$2("_enamorangs") < 5 && !get$2("enamorangMonster"), Macro.tryHaveItem(itemOrSkill));
        case $skill`Club 'Em Into Next Week`:
          return this.externalIf(kolmafia.haveEquipped($item`legendary seal-clubbing club`), Macro.trySkill(itemOrSkill));
        case $skill`Digitize`:
          return this.externalIf(canDigitize(), Macro.trySkill(itemOrSkill));
      }

      // Unsupported item or skill
      return this;
    }
  }, {
    key: "delevel",
    value: function delevel() {
      return this.tryHaveSkill($skill`Curse of Weaksauce`).externalIf(shouldAffirmationHate(), Macro.tryHaveItem($item`Daily Affirmation: Keep Free Hate in your Heart`)).externalIf(have$P($skill`Meteor Lore`), Macro.trySkill($skill`Micrometeorite`)).tryHaveSkill($skill`Pocket Crumbs`).tryHaveItem($item`train whistle`).tryHaveSkill($skill`Entangling Noodles`).tryHaveItem($item`little red book`).tryHaveItem($item`Rain-Doh blue balls`).tryHaveItem($item`Rain-Doh indigo cup`);
    }
  }, {
    key: "meatKill",
    value: function meatKill() {
      var delevel = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : isStrongScaler(globalOptions.target);
      var sealClubberSetup = kolmafia.myClass() === $class`Seal Clubber` && have$P($skill`Furious Wallop`);
      var opsSetup = kolmafia.equippedAmount($item`Operation Patriot Shield`) > 0;
      var katanaSetup = kolmafia.equippedAmount($item`haiku katana`) > 0;
      var capeSetup = get$2("retroCapeSuperhero") === "robot" && get$2("retroCapeWashingInstructions") === "kill" && kolmafia.itemType(kolmafia.equippedItem($slot`weapon`)) === "pistol";
      var pigSkinnerSetup = have$P($skill`Head in the Game`);
      var bearArmsSetup = kolmafia.equippedAmount($item`right bear arm`) > 0 && kolmafia.equippedAmount($item`left bear arm`) > 0;
      var willCrit = (kolmafia.equippedAmount($item`mafia pointer finger ring`) > 0 || kolmafia.equippedAmount($item`bindlestocking`) > 0) && (sealClubberSetup || opsSetup || katanaSetup || capeSetup || pigSkinnerSetup || bearArmsSetup);
      var olfactMonster = FarmingStrategy.olfactMonster();
      this.externalIf(shouldRedigitize(), Macro.if_(globalOptions.target, Macro.trySkill($skill`Digitize`))).externalIf(delevel, Macro.if_(globalOptions.target, Macro.delevel())).externalIf(have$P($skill`Blow the Purple Candle!`), Macro.if_(globalOptions.target, Macro.trySkill($skill`Blow the Purple Candle!`))).if_($monster`tumbleweed`, Macro.abort()).trySingAlong().familiarActions().tryEgg().tryDrone().externalIf(have$P($skill`Extract Oil`) && get$2("_oilExtracted") < 15, Macro.if_($monster`garbage tourist`, Macro.trySkill($skill`Extract Oil`))).externalIf(wanderingCopytargetsRemaining() <= 5 - get$2("_meteorShowerUses") && have$P($skill`Meteor Lore`) && get$2("_meteorShowerUses") < 5, Macro.if_(globalOptions.target, Macro.trySkill($skill`Meteor Shower`))).externalIf(get$2("cosmicBowlingBallReturnCombats") < 1, Macro.trySkill($skill`Bowl Straight Up`));
      if (olfactMonster) {
        this.externalIf(have$P($skill`Transcendent Olfaction`) && (get$2("olfactedMonster") !== olfactMonster || !have$P($effect`On the Trail`)) && get$2("_olfactionsUsed") < 3, Macro.if_(olfactMonster, Macro.trySkill($skill`Transcendent Olfaction`))).externalIf(get$2("_gallapagosMonster") !== olfactMonster && have$P($skill`Gallapagosian Mating Call`), Macro.if_(olfactMonster, Macro.trySkill($skill`Gallapagosian Mating Call`))).externalIf(get$2("longConMonster") !== olfactMonster && get$2("_longConUsed") < 5 && have$P($skill`Long Con`), Macro.if_(olfactMonster, Macro.trySkill($skill`Long Con`))).externalIf(get$2("motifMonster") !== olfactMonster && have$P($skill`Motif`) && !have$P($effect`Everything Looks Blue`), Macro.if_(olfactMonster, Macro.trySkill($skill`Motif`))).externalIf(!get$2("_latteCopyUsed") && (get$2("_latteMonster") !== olfactMonster || get("Latte Monster") > 30) && have$P($item`latte lovers member's mug`), Macro.if_(olfactMonster, Macro.trySkill($skill`Offer Latte to Opponent`)));
      }
      return this.externalIf(get$2("_feelNostalgicUsed") < 3 && get$2("lastCopyableMonster") === $monster`garbage tourist` && have$P($skill`Feel Nostalgic`), Macro.if_(`!monsterid ${$monster`garbage tourist`.id}`, Macro.trySkill($skill`Feel Nostalgic`))).externalIf(opsSetup, Macro.trySkill($skill`Throw Shield`)).meatStasis(willCrit).externalIf(shouldAffirmationHate(), Macro.tryHaveItem($item`Daily Affirmation: Keep Free Hate in your Heart`)).externalIf(kolmafia.hippyStoneBroken() && monsterManuelAvailable(), Macro.if_(`(monsterid 1758 || monsterid 1759 || monsterid 1760) && monsterhpbelow ${Math.floor((100 + kolmafia.numericModifier("Monster Level")) / 5)}`, Macro.trySkill($skill`Feel Superior`))).externalIf(sealClubberSetup, Macro.trySkill($skill`Furious Wallop`)).externalIf(opsSetup, Macro.attack()).externalIf(katanaSetup, Macro.trySkill($skill`Summer Siesta`)).externalIf(capeSetup, Macro.trySkill($skill`Precision Shot`)).externalIf(bearArmsSetup, Macro.trySkill($skill`Kodiak Moment`)).externalIf(pigSkinnerSetup, Macro.attack()).externalIf(kolmafia.myClass() === $class`Disco Bandit`, Macro.trySkill($skill`Disco Dance of Doom`).trySkill($skill`Disco Dance II: Electric Boogaloo`).trySkill($skill`Disco Dance 3: Back in the Habit`)).externalIf(kolmafia.myClass() === $class`Cheese Wizard` && kolmafia.myFamiliar().experience < 400, Macro.trySkill($skill`Stilton Splatter`)).kill();
    }
  }, {
    key: "meatStasis",
    value: function meatStasis(checkPassive) {
      // We can't stasis without manuel's monsterhpabove if we want to crit
      if (checkPassive && !monsterManuelAvailable()) {
        return this;
      }
      var checkGet = i => have$P(i) && (kolmafia.itemAmount(i) > 0 || kolmafia.retrieveItem(i));
      var stasisItem = $items`facsimile dictionary, dictionary, seal tooth`.find(checkGet);
      var pinataCastsAvailable = Math.floor(currentCinch() / 5);
      var canPinata = have$n() && pinataCastsAvailable > 0 && monsterManuelAvailable();

      // We retrieve a seal tooth at the start of the day, so this is just to make sure nothing has gone awry.
      if (!stasisItem) {
        throw new Error("Acquire a seal tooth and run garbo again.");
      }

      // Construct the monster HP component of the stasis condition
      // Evaluate the passive damage
      var passiveDamage = maxPassiveDamage() + 5;
      // Are we aiming to crit? If so, we need to respect the passive damage
      // Also we need to respect our health total
      var hpCheck = checkPassive ? `!hppercentbelow 25 && monsterhpabove ${passiveDamage}` : "!hppercentbelow 25";
      // Same story but for the sixgun shot, which wants 40 more HP if possible
      var hpCheckSixgun = checkPassive ? `!hppercentbelow 25 && monsterhpabove ${passiveDamage + 40}` : "!hppercentbelow 25";
      // Same story but for Cincho's projectile pinata, which wants 50 more HP if possible
      var hpCheckCincho = checkPassive ? `!hppercentbelow 25 && monsterhpabove ${passiveDamage + 50}` : "!hppercentbelow 25";

      // Determine how long we'll be stasising for
      // By default there's no reason to stasis
      var stasisRounds = 0;
      if ([$familiar`Cocoabo`, $familiar`Feather Boa Constrictor`, $familiar`Ninja Pirate Zombie Robot`, $familiar`Stocking Mimic`].includes(kolmafia.myFamiliar())) {
        // Cocoabo-likes drop meat for the first ten rounds of combat
        stasisRounds = 10;
      }
      if (kolmafia.myFamiliar() === $familiar`Hobo Monkey` || kolmafia.myFamiliar() === $familiar`Jill-of-All-Trades` || kolmafia.haveEquipped($item`Buddy Bjorn`) || kolmafia.haveEquipped($item`Crown of Thrones`) || get$2("_bittycar")) {
        // These things can take a little longer to proc sometimes
        stasisRounds = FarmingStrategy.stasisRounds;
      }
      if (isQuickCombat()) {
        // long fights can be very slow
        stasisRounds = Math.min(5, stasisRounds);
      }

      // Ignore unexpected monsters, holiday scaling monsters seem to abort with monsterhpabove
      // Delevel the sausage goblins as otherwise they can kind of hurt
      return this.if_([].concat(_toConsumableArray(FarmingStrategy.monsters()), [globalOptions.target, $monster`sausage goblin`]), Macro.externalIf(have$P($item`Time-Spinner`), Macro.if_(`${hpCheck} && monstername sausage goblin`, Macro.tryHaveItem($item`Time-Spinner`))).externalIf(have$P($skill`Meteor Lore`), Macro.if_(`${hpCheck} && monstername sausage goblin`, Macro.tryHaveSkill($skill`Micrometeorite`))).externalIf(kolmafia.haveEquipped($item`Pantsgiving`), Macro.if_(`${hpCheck}`, Macro.trySkill($skill`Pocket Crumbs`))).externalIf(getSkills().includes($skill`Extract`), Macro.if_(`${hpCheck}`, Macro.trySkill($skill`Extract`))).externalIf(kolmafia.haveEquipped($item`vampyric cloake`) && get$2("_vampyreCloakeFormUses") < 10, Macro.if_(`${hpCheck}`, Macro.tryHaveSkill($skill`Become a Wolf`))).externalIf(kolmafia.haveEquipped($item`Cincho de Mayo`) && canPinata, Macro.while_(`!times ${maximumPinataCasts()} && ${hpCheckCincho} && ${Macro.makeBALLSPredicate($skill`Cincho: Projectile Piñata`)}`, Macro.trySkill($skill`Cincho: Projectile Piñata`))).externalIf(have$P($item`porquoise-handled sixgun`), Macro.if_(`${hpCheckSixgun}`, Macro.tryItem($item`porquoise-handled sixgun`))).while_(`${hpCheck} && !pastround ${stasisRounds}`, Macro.item(stasisItem)));
    }
  }, {
    key: "startCombat",
    value: function startCombat() {
      return this.trySingAlong().tryHaveSkill($skill`Curse of Weaksauce`).familiarActions().externalIf(get$2("cosmicBowlingBallReturnCombats") < 1, Macro.trySkill($skill`Bowl Straight Up`)).externalIf(kolmafia.haveEquipped($item`vampyric cloake`) && get$2("_vampyreCloakeFormUses") < 10, Macro.tryHaveSkill($skill`Become a Wolf`)).externalIf(kolmafia.haveEquipped($item`Pantsgiving`), Macro.trySkill($skill`Pocket Crumbs`)).externalIf(getSkills().includes($skill`Extract`), Macro.trySkill($skill`Extract`)).tryHaveItem($item`porquoise-handled sixgun`).externalIf(have$P($skill`Meteor Lore`), Macro.trySkill($skill`Micrometeorite`)).tryHaveItem($item`Time-Spinner`).tryHaveItem($item`Rain-Doh indigo cup`).tryHaveItem($item`Rain-Doh blue balls`).externalIf(kolmafia.haveEquipped($item`Buddy Bjorn`) || kolmafia.haveEquipped($item`Crown of Thrones`), Macro.while_("!pastround 3 && !hppercentbelow 25", Macro.item($item`seal tooth`))).externalIf([$familiar`Cocoabo`, $familiar`Feather Boa Constrictor`, $familiar`Ninja Pirate Zombie Robot`, $familiar`Stocking Mimic`].some(familiar => kolmafia.myFamiliar() === familiar), Macro.while_("!pastround 10 && !hppercentbelow 25", Macro.item($item`seal tooth`))).externalIf(kolmafia.myFamiliar() === $familiar`Hobo Monkey`, Macro.while_(`!match "shoulder, and hands you some Meat." && !pastround 5 && !hppercentbelow 25`, Macro.item($item`seal tooth`)));
    }
  }, {
    key: "kill",
    value: function kill() {
      var riftId = kolmafia.toInt($location`Shadow Rift`);
      var canPinata = kolmafia.haveEquipped($item`Cincho de Mayo`) && currentCinch() >= 5;
      return this.externalIf(kolmafia.myClass() === $class`Sauceror` && have$P($skill`Curse of Weaksauce`), Macro.trySkill($skill`Curse of Weaksauce`)).externalIf(canPinata, Macro.while_(`!times ${maximumPinataCasts()} && ${Macro.makeBALLSPredicate($skill`Cincho: Projectile Piñata`)} && !pastround 24 && !hppercentbelow 25`, Macro.trySkill($skill`Cincho: Projectile Piñata`))).tryHaveSkill($skill`Become a Wolf`).externalIf(!(kolmafia.myClass() === $class`Sauceror` && have$P($skill`Curse of Weaksauce`)), Macro.while_(`!pastround 24 && !hppercentbelow 25 && !missed 1 && !snarfblat ${riftId}`, Macro.attack())).killTarget().externalIf(preferMuscleCombat(), Macro.ifNot($element`Cold`, Macro.trySkillRepeat($skill`Northern Explosion`)).trySkillRepeat($skill`Lunging Thrust-Smack`, $skill`Saucegeyser`, $skill`Weapon of the Pastalord`, $skill`Cannelloni Cannon`, $skill`Wave of Sauce`, $skill`Saucestorm`), Macro.trySkillRepeat($skill`Saucegeyser`, $skill`Weapon of the Pastalord`, $skill`Cannelloni Cannon`, $skill`Wave of Sauce`, $skill`Saucestorm`).ifNot($element`Cold`, Macro.trySkillRepeat($skill`Northern Explosion`)).trySkillRepeat($skill`Lunging Thrust-Smack`)).attack().repeat();
    }
  }, {
    key: "killTarget",
    value:
    // Special handling for resistant targets
    function killTarget() {
      if (globalOptions.target.elementalResistance <= 75 && globalOptions.target.physicalResistance <= 75) {
        return this;
      }
      return this.if_(globalOptions.target, Macro.externalIf(globalOptions.target.elementalResistance > globalOptions.target.physicalResistance, Macro.externalIf(preferMuscleCombat(), Macro.trySkillRepeat($skill`Lunging Thrust-Smack`), Macro.trySkillRepeat($skill`Weapon of the Pastalord`)), Macro.externalIf(preferMuscleCombat() && globalOptions.target.defenseElement !== $element`cold`, Macro.trySkillRepeat($skill`Northern Explosion`)).trySkillRepeat($skill`Saucegeyser`, $skill`Wave of Sauce`, $skill`Saucestorm`)));
    }
  }, {
    key: "basicCombat",
    value: function basicCombat() {
      return this.startCombat().kill();
    }
  }, {
    key: "ghostBustin",
    value: function ghostBustin() {
      // Only bust ghosts if you have enough stunners to prevent getting hit
      var stunRounds = 0;
      var classStun = null;
      var extraStun = null;
      if (have$P($item`Rain-Doh blue balls`)) stunRounds++;
      if (get$2("lovebugsUnlocked")) stunRounds++;
      if (kolmafia.myClass() === $class`Seal Clubber` && have$P($skill`Club Foot`) && kolmafia.myMp() >= kolmafia.mpCost($skill`Club Foot`)) {
        var clubRounds = Math.min(kolmafia.myFury(), 3) + (kolmafia.itemType(kolmafia.equippedItem($slot`weapon`)) === "club" ? 1 : 0) - 1;
        if (stunRounds > 0) {
          classStun = $skill`Club Foot`;
          stunRounds += clubRounds;
        }
      } else if (kolmafia.myClass() === $class`Turtle Tamer` && have$P($skill`Shell Up`) && kolmafia.myMp() >= kolmafia.mpCost($skill`Shell Up`)) {
        var shellRounds = (have$P($effect`Blessing of the Storm Tortoise`) ? 2 : 0) + (have$P($effect`Grand Blessing of the Storm Tortoise`) ? 3 : 0) + (have$P($effect`Glorious Blessing of the Storm Tortoise`) ? 4 : 0);
        if (shellRounds > 0) {
          classStun = $skill`Shell Up`;
          stunRounds += shellRounds;
        }
      } else if (kolmafia.myClass() === $class`Pastamancer` && have$P($skill`Entangling Noodles`) && kolmafia.myMp() >= kolmafia.mpCost($skill`Entangling Noodles`)) {
        classStun = $skill`Entangling Noodles`;
        stunRounds += 2;
      } else if (kolmafia.myClass() === $class`Sauceror` && have$P($skill`Soul Bubble`) && kolmafia.mySoulsauce() >= 5) {
        classStun = $skill`Soul Bubble`;
        stunRounds += 2;
      } else if (kolmafia.myClass() === $class`Accordion Thief` && have$P($skill`Accordion Bash`) && kolmafia.itemType(kolmafia.equippedItem($slot`weapon`)) === "accordion" && kolmafia.myMp() >= kolmafia.mpCost($skill`Accordion Bash`)) {
        classStun = $skill`Accordion Bash`;
        stunRounds += 2;
      } else if (kolmafia.myClass() === $class`Disco Bandit`) ;

      // Don't use shadow noodles unless we really need it.
      if (stunRounds < 3 && classStun !== $skill`Entangling Noodles` && have$P($skill`Shadow Noodles`) && kolmafia.myMp() >= kolmafia.mpCost(classStun ?? $skill.none) + kolmafia.mpCost($skill`Shadow Noodles`)) {
        extraStun = $skill`Shadow Noodles`;
        stunRounds += 2;
      }

      // Lacking multi-round stuns
      if (stunRounds < 3) {
        return this.basicCombat();
      }
      return this.trySingAlong().familiarActions().tryHaveItem($item`Rain-Doh blue balls`).externalIf(get$2("lovebugsUnlocked"), Macro.trySkill($skill`Summon Love Gnats`)).tryHaveSkill(classStun).tryHaveSkill(extraStun).trySkill($skill`Shoot Ghost`).trySkill($skill`Shoot Ghost`).trySkill($skill`Shoot Ghost`).trySkill($skill`Trap Ghost`).kill();
    }
  }, {
    key: "tryDrone",
    value: function tryDrone() {
      return this.externalIf(kolmafia.myFamiliar() === $familiar`Grey Goose` && gooseDroneEligible() && get$2("gooseDronesRemaining") < copyTargetCount(), Macro.if_(globalOptions.target, Macro.trySkill($skill`Emit Matter Duplicating Drones`)));
    }
  }, {
    key: "tryEgg",
    value: function tryEgg() {
      return this.externalIf(kolmafia.myFamiliar() === $familiar`Chest Mimic` && $familiar`Chest Mimic`.experience >= 50, Macro.if_(globalOptions.target, Macro.trySkillRepeat($skill`%fn, lay an egg`)));
    }
  }, {
    key: "target",
    value: function target(action) {
      var doneHabitat = !have$P($skill`Just the Facts`) || get$2("_monsterHabitatsRecalled") === 3 && get$2("_monsterHabitatsFightsLeft") <= 1;
      return this.if_(globalOptions.target, Macro.externalIf(isStrongScaler(globalOptions.target), Macro.delevel()).if_($location`The Briny Deeps`, Macro.tryCopier($item`pulled green taffy`)).externalIf(kolmafia.myFamiliar() === $familiar`Reanimated Reanimator`, Macro.trySkill($skill`Wink at`)).externalIf(kolmafia.myFamiliar() === $familiar`Obtuse Angel`, Macro.trySkill($skill`Fire a badly romantic arrow`)).tryEgg().tryDrone().externalIf(doneHabitat && get$2("beGregariousCharges") > 0 && (get$2("beGregariousMonster") !== globalOptions.target || have$P($item`miniature crystal ball`) ? get$2("beGregariousFightsLeft") === 0 : get$2("beGregariousFightsLeft") <= 1), Macro.trySkill($skill`Be Gregarious`)).externalIf(have$P($skill`Just the Facts`) && get$2("_monsterHabitatsRecalled") < 3 && (get$2("_monsterHabitatsMonster") !== globalOptions.target || have$P($item`miniature crystal ball`) ? get$2("_monsterHabitatsFightsLeft") === 0 : get$2("_monsterHabitatsFightsLeft") <= 1), Macro.trySkill($skill`Recall Facts: Monster Habitats`)).externalIf(have$P($skill`Just the Facts`) && !get$2("_circadianRhythmsRecalled"), Macro.trySkill($skill`Recall Facts: %phylum Circadian Rhythms`)).externalIf(getDigitizeMonster() !== globalOptions.target || shouldRedigitize(), Macro.tryCopier($skill`Digitize`)).tryCopier($item`Spooky Putty sheet`).tryCopier($item`Rain-Doh black box`).tryCopier($item`4-d camera`).tryCopier($item`unfinished ice sculpture`).externalIf(get$2("_enamorangs") === 0, Macro.tryCopier($item`LOV Enamorang`)).tryCopier($skill`Club 'Em Into Next Week`).meatKill(false)).abortWithMsg(`Macro for ${action} expected ${globalOptions.target} but encountered something else.`);
    }
  }, {
    key: "duplicate",
    value: function duplicate() {
      return this.externalIf(kolmafia.haveEquipped($item`pro skateboard`) && !get$2("_epicMcTwistUsed"), Macro.trySkill($skill`Do an epic McTwist!`)).externalIf(have$I() && duplicateUsesRemaining() > 0, Macro.trySkill($skill`Duplicate`));
    }
  }, {
    key: "refractedGaze",
    value: function refractedGaze() {
      return this.externalIf(kolmafia.haveEquipped($item`Monodent of the Sea`) && kolmafia.haveEquipped($item`blood cubic zirconia`),
      // Assume if we have both equipped, we mean to refracted feesh
      Macro.if_("!monsterphylum Fish", Macro.trySkill($skill`Sea *dent: Talk to Some Fish`))).externalIf(kolmafia.haveEquipped($item`blood cubic zirconia`) && safeRefractedCasts() > 0, Macro.trySkill($skill`BCZ: Refracted Gaze`));
    }
  }], [{
    key: "abortWithMsg",
    value: function abortWithMsg(errorMessage) {
      return new Macro().step(`abort "${errorMessage}"`);
    }
  }, {
    key: "tryHaveSkill",
    value: function tryHaveSkill(skill) {
      return new Macro().tryHaveSkill(skill);
    }
  }, {
    key: "tryHaveItem",
    value: function tryHaveItem(item) {
      return new Macro().tryHaveItem(item);
    }
  }, {
    key: "trySingAlong",
    value: function trySingAlong() {
      return new Macro().trySingAlong();
    }
  }, {
    key: "ifInnateWanderer",
    value: function ifInnateWanderer(macro) {
      // if this monster appears without action on the part of the script
      return new Macro().ifInnateWanderer(macro);
    }
  }, {
    key: "familiarActions",
    value: function familiarActions() {
      return new Macro().familiarActions();
    }
  }, {
    key: "tryCopier",
    value: function tryCopier(itemOrSkill) {
      return new Macro().tryCopier(itemOrSkill);
    }
  }, {
    key: "delevel",
    value: function delevel() {
      return new Macro().delevel();
    }
  }, {
    key: "meatKill",
    value: function meatKill() {
      var delevel = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : isStrongScaler(globalOptions.target);
      return new Macro().meatKill(delevel);
    }
  }, {
    key: "meatStasis",
    value: function meatStasis(checkPassive) {
      return new Macro().meatStasis(checkPassive);
    }
  }, {
    key: "startCombat",
    value: function startCombat() {
      return new Macro().startCombat();
    }
  }, {
    key: "kill",
    value: function kill() {
      return new Macro().kill();
    }
  }, {
    key: "basicCombat",
    value: function basicCombat() {
      return new Macro().basicCombat();
    }
  }, {
    key: "ghostBustin",
    value: function ghostBustin() {
      return new Macro().ghostBustin();
    }
  }, {
    key: "tryDrone",
    value: function tryDrone() {
      return new Macro().tryDrone();
    }
  }, {
    key: "tryEgg",
    value: function tryEgg() {
      return new Macro().tryEgg();
    }
  }, {
    key: "target",
    value: function target(action) {
      return new Macro().target(action);
    }
  }, {
    key: "duplicate",
    value: function duplicate() {
      return new Macro().duplicate();
    }
  }, {
    key: "refractedGaze",
    value: function refractedGaze() {
      return new Macro().duplicate();
    }
  }]);
}(StrictMacro);
function customizeMacro(macro) {
  var _ref = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {},
    _ref$freeWanderer = _ref.freeWanderer,
    freeWanderer = _ref$freeWanderer === void 0 ? () => Macro.basicCombat() : _ref$freeWanderer,
    _ref$tentacle = _ref.tentacle,
    tentacle = _ref$tentacle === void 0 ? () => Macro.basicCombat() : _ref$tentacle,
    _ref$innateWanderer = _ref.innateWanderer,
    innateWanderer = _ref$innateWanderer === void 0 ? macro => Macro.externalIf(kolmafia.haveEquipped($item`backup camera`) && get$2("_backUpUses") < 11 && get$2("lastCopyableMonster") === globalOptions.target && (!targetingMeat() || kolmafia.myFamiliar() === meatFamiliar()), Macro.skill($skill`Back-Up to your Last Enemy`).step(macro), Macro.basicCombat()) : _ref$innateWanderer;
  return Macro.if_($monsters`giant rubber spider, time-spinner prank`, freeWanderer(macro)).externalIf(get$2("stopForUltraRare"), Macro.if_(ULTRA_RARE_MONSTERS, Macro.abort())).externalIf(have$P($effect`Eldritch Attunement`), Macro.if_($monster`Eldritch Tentacle`, tentacle(macro))).ifInnateWanderer(innateWanderer(macro)).step(macro);
}
function makeCcs(macro) {
  kolmafia.writeCcs(`[default]\n"${customizeMacro(macro).toString()}"`, "garbo");
  kolmafia.setCcs("garbo");
}
function runCombatBy(initiateCombatAction) {
  try {
    var result = initiateCombatAction();
    while (kolmafia.inMultiFight()) kolmafia.runCombat();
    if (kolmafia.choiceFollowsFight()) kolmafia.visitUrl("choice.php");
    return result;
  } catch (e) {
    throw `Combat exception! Last macro error: ${get$2("lastMacroError")}. Exception ${e}.`;
  }
}

/**
 * Attempt to perform a nonstandard combat-starting Action with a Macro
 * @param macro The Macro to attempt to use
 * @param action The combat-starting action to attempt
 * @param tryAuto Whether or not we should try to resolve the combat with an autoattack; autoattack macros can fail against special monsters, and thus we have to submit a macro via CCS regardless.
 * @returns The output of your specified action function (typically void)
 */
function withMacro(macro, action) {
  var tryAuto = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : false;
  if (kolmafia.getAutoAttack() !== 0) kolmafia.setAutoAttack(0);
  if (tryAuto) customizeMacro(macro).setAutoAttack();
  makeCcs(macro);
  try {
    return runCombatBy(action);
  } finally {
    if (tryAuto) kolmafia.setAutoAttack(0);
  }
}

/**
 * Adventure in a location and handle all combats with a given macro.
 *
 * @category Combat
 * @param loc Location to adventure in.
 * @param macro Macro to execute.
 */
function garboAdventure(loc, macro) {
  if (kolmafia.getAutoAttack() !== 0) kolmafia.setAutoAttack(0);
  makeCcs(macro);
  runCombatBy(() => kolmafia.adv1(loc, -1, ""));
}

/**
 * Adventure in a location and handle all combats with a given autoattack and manual macro.
 *
 * @category Combat
 * @param loc Location to adventure in.
 * @param autoMacro Macro to execute via KoL autoattack.
 * @param nextMacro Macro to execute manually after autoattack completes.
 */
function garboAdventureAuto(loc, autoMacro) {
  var nextMacro = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : Macro.abort();
  autoMacro.setAutoAttack();
  makeCcs(nextMacro);
  runCombatBy(() => kolmafia.adv1(loc, -1, ""));
}

var stashItems = get$2("garboStashItems", "").split(",").filter(x => x.trim().length > 0).map(id => kolmafia.toItem(id));
function withStash(itemsToTake, action) {
  var manager = new StashManager();
  try {
    manager.take.apply(manager, _toConsumableArray(itemsToTake));
    return action();
  } finally {
    manager.putBackAll();
  }
}
function withVIPClan(action) {
  var clanIdOrNameString = globalOptions.prefs.vipClan;
  var clanIdOrName = clanIdOrNameString.match(/^\d+$/) ? parseInt(clanIdOrNameString) : clanIdOrNameString;
  if (clanIdOrName === "" && have$P($item`Clan VIP Lounge key`)) {
    if (userConfirmDialog("The preference 'garbo_vipClan' is not set. Use the current clan as a VIP clan? (Defaults to yes in 15 seconds)", true, 15000)) {
      clanIdOrName = kolmafia.getClanId();
      globalOptions.prefs.vipClan = `${clanIdOrName}`;
      _set("garbo_vipClan", clanIdOrName);
    }
  }
  return withClan(clanIdOrName || kolmafia.getClanId(), action);
}
function withClan(clanIdOrName, action) {
  var startingClanId = kolmafia.getClanId();
  Clan.join(clanIdOrName);
  try {
    return action();
  } finally {
    Clan.join(startingClanId);
  }
}
var StashManager = /*#__PURE__*/function () {
  function StashManager() {
    _classCallCheck(this, StashManager);
    _defineProperty(this, "taken", new Map());
    var clanIdOrName = globalOptions.prefs.stashClan;
    this.clanIdOrName = clanIdOrName.match(/^\d+$/) ? parseInt(clanIdOrName) : clanIdOrName;
    this.enabled = ![0, "", "none"].some(id => id === this.clanIdOrName);
  }
  return _createClass(StashManager, [{
    key: "take",
    value: function take() {
      for (var _len = arguments.length, items = new Array(_len), _key = 0; _key < _len; _key++) {
        items[_key] = arguments[_key];
      }
      if (items.length === 0) {
        return;
      }
      if (!this.enabled) {
        kolmafia.print(`Stash access is disabled. Ignoring request to borrow "${items.map(value => value.name).join(", ")}" from clan stash.`, HIGHLIGHT);
        return;
      }
      withClan(this.clanIdOrName, () => {
        for (var _i = 0, _items = items; _i < _items.length; _i++) {
          var item = _items[_i];
          if (have$P(item)) continue;
          if (getFoldGroup(item).some(fold => have$P(fold))) {
            kolmafia.cliExecute(`fold ${item.name}`);
            continue;
          }
          var foldArray = [item].concat(_toConsumableArray(getFoldGroup(item)));
          kolmafia.refreshStash();
          var _iterator = _createForOfIteratorHelper(foldArray),
            _step;
          try {
            for (_iterator.s(); !(_step = _iterator.n()).done;) {
              var fold = _step.value;
              if (kolmafia.stashAmount(fold) > 0) {
                try {
                  if (kolmafia.takeStash(1, fold)) {
                    kolmafia.print(`Took ${fold.name} from stash in ${kolmafia.getClanName()}.`, HIGHLIGHT);
                    if (fold !== item) kolmafia.cliExecute(`fold ${item.name}`);
                    this.taken.set(item, (this.taken.get(item) ?? 0) + 1);
                    stashItems.push(fold);
                    break;
                  } else {
                    kolmafia.print(`Failed to take ${fold.name} from the stash. Do you have stash access in ${kolmafia.getClanName()}?`, "red");
                  }
                } catch (_unused) {
                  kolmafia.print(`Failed to take ${fold.name} from stash in ${kolmafia.getClanName()}.`, "red");
                }
              }
            }
          } catch (err) {
            _iterator.e(err);
          } finally {
            _iterator.f();
          }
          if (have$P(item)) continue;
        }
      });
    }

    /**
     * Ensure at least one of each of {items} in inventory.
     * @param items Items to take from the stash.
     */
  }, {
    key: "ensure",
    value: function ensure() {
      for (var _len2 = arguments.length, items = new Array(_len2), _key2 = 0; _key2 < _len2; _key2++) {
        items[_key2] = arguments[_key2];
      }
      this.take.apply(this, _toConsumableArray(items.filter(item => kolmafia.availableAmount(item) === 0)));
    }
  }, {
    key: "putBack",
    value: function putBack() {
      for (var _len3 = arguments.length, items = new Array(_len3), _key3 = 0; _key3 < _len3; _key3++) {
        items[_key3] = arguments[_key3];
      }
      if (items.length === 0) return;
      if (kolmafia.visitUrl("fight.php").includes("You're fighting")) {
        var _Macro$if_$if_;
        kolmafia.print("In fight, trying to get away to return items to stash...", HIGHLIGHT);
        (_Macro$if_$if_ = Macro.if_([globalOptions.target, $monster`giant giant crab`], Macro.attack().repeat()).if_(ULTRA_RARE_MONSTERS, Macro.abort())).tryItem.apply(_Macro$if_$if_, _toConsumableArray(unlimitedFreeRunList.filter(i => getAcquirePrice(i) < get$2("valueOfAdventure")).sort((a, b) => getAcquirePrice(a) - getAcquirePrice(b)))).step("runaway").submit();
      } else {
        kolmafia.visitUrl("main.php");
        if (kolmafia.handlingChoice()) {
          kolmafia.print(`I'm stuck in a choice, unfortunately, but were I not, I'd like to return the following items to your clan stash:`, "red");
          items.forEach(item => kolmafia.print(`${item.name},`, "red"));
        }
      }
      withClan(this.clanIdOrName, () => {
        for (var _i2 = 0, _items2 = items; _i2 < _items2.length; _i2++) {
          var item = _items2[_i2];
          var count = this.taken.get(item) ?? 0;
          if (count > 0) {
            kolmafia.retrieveItem(count, item);
            if (item === $item`Buddy Bjorn`) {
              kolmafia.visitUrl(`desc_item.php?whichitem=${$item`Buddy Bjorn`.descid}`);
              kolmafia.bjornifyFamiliar($familiar.none);
            }
            if (item === $item`Crown of Thrones`) {
              kolmafia.visitUrl(`desc_item.php?whichitem=${$item`Crown of Thrones`.descid}`);
              kolmafia.enthroneFamiliar($familiar.none);
            }
            var foldedForms = [item].concat(_toConsumableArray(getFoldGroup(item)));
            var _iterator2 = _createForOfIteratorHelper(foldedForms),
              _step2;
            try {
              for (_iterator2.s(); !(_step2 = _iterator2.n()).done;) {
                var fold = _step2.value;
                unequip(fold);
              }
            } catch (err) {
              _iterator2.e(err);
            } finally {
              _iterator2.f();
            }
            if (kolmafia.toSlot(item) === $slot`familiar` && !have$P(item, count)) {
              var _iterator3 = _createForOfIteratorHelper($familiars.all().filter(have$P)),
                _step3;
              try {
                for (_iterator3.s(); !(_step3 = _iterator3.n()).done;) {
                  var familiar = _step3.value;
                  if (kolmafia.familiarEquippedEquipment(familiar) === item) {
                    kolmafia.equip(familiar, $item.none);
                  }
                }
              } catch (err) {
                _iterator3.e(err);
              } finally {
                _iterator3.f();
              }
            }
            if (kolmafia.itemAmount(item) >= count && kolmafia.putStash(count, item)) {
              var index = stashItems.indexOf(item);
              if (index >= 0) stashItems.splice(stashItems.indexOf(item), 1);
              kolmafia.print(`Returned ${item.name} to stash in ${kolmafia.getClanName()}.`, HIGHLIGHT);
              this.taken.delete(item);
            } else {
              throw `Failed to return ${item.name} to stash.`;
            }
          }
        }
      });
    }

    /**
     * Put all items back in the stash.
     */
  }, {
    key: "putBackAll",
    value: function putBackAll() {
      this.putBack.apply(this, _toConsumableArray(this.taken.keys()));
    }
  }]);
}();

function bestLockPickChoice() {
  return 1 + kolmafia.toInt(maxBy($items`Boris's key lime, Jarlsberg's key lime, Sneaky Pete's key lime`, garboValue)) - kolmafia.toInt($item`Boris's key lime`);
}
var AscendingTasks = [{
  name: "Lock Picking",
  ready: () => have$P($skill`Lock Picking`) && globalOptions.ascend,
  completed: () => get$2("lockPicked"),
  do: () => {
    kolmafia.useSkill($skill`Lock Picking`);
    if (kolmafia.handlingChoice()) kolmafia.runChoice(-1);
  },
  choices: () => ({
    1414: bestLockPickChoice()
  }),
  spendsTurn: false
}].concat(_toConsumableArray([{
  key: $item`Boris's key`,
  lime: $item`Boris's key lime`
}, {
  key: $item`Jarlsberg's key`,
  lime: $item`Jarlsberg's key lime`
}, {
  key: $item`Sneaky Pete's key`,
  lime: $item`Sneaky Pete's key lime`
}, {
  key: $item`Richard's star key`,
  lime: $item`star key lime`
}].map(_ref => {
  var key = _ref.key,
    lime = _ref.lime;
  return {
    name: `Cook ${lime}`,
    completed: () => !have$P(key) || garboValue(lime) < garboValue($item`lime`),
    do: () => kolmafia.create(lime),
    ready: () => globalOptions.ascend && freeCrafts("food") > 0 || get$2("hasChef"),
    spendsTurn: false
  };
})));
var AscendingQuest = {
  name: "Ascend",
  tasks: AscendingTasks
};

var sessions = new Map();
/**
 * Start a new session, deleting any old session
 */
function startSession() {
  sessions.set("full", Session.current());
}

/**
 * Compute the difference between the current drops and starting session (if any)
 * @returns The difference
 */
function sessionSinceStart() {
  var session = sessions.get("full");
  if (session) {
    return Session.current().diff(session);
  }
  return Session.current();
}
var extraValue$1 = 0;
function trackMarginalTurnExtraValue(additionalValue) {
  extraValue$1 += additionalValue;
}
function trackMarginalMpa(remainingTurns) {
  var barf = sessions.get("barf");
  var current = Session.current();
  if (!barf) {
    sessions.set("barf", Session.current());
  } else {
    var turns = barf.diff(current).totalTurns;
    remainingTurns ?? (remainingTurns = estimatedGarboTurns());
    // track items if we have run at least 100 turns in barf mountain or we have less than 200 turns left in barf mountain
    var item = sessions.get("item-start");
    if (!item && (turns > 100 || estimatedGarboTurns() <= 200)) {
      sessions.set("item-start", current);
    }
    // start tracking meat if there are less than 75 turns left in barf mountain
    var meatStart = sessions.get("meat-start");
    if (!meatStart && remainingTurns <= 75) {
      sessions.set("meat-start", current);
    }

    // stop tracking meat if there are less than 25 turns left in barf moutain
    var meatEnd = sessions.get("meat-end");
    if (!meatEnd && remainingTurns <= 25) {
      sessions.set("meat-end", current);
    }
    var itemEnd = sessions.get("item-end");
    if (!itemEnd && remainingTurns <= 0) {
      sessions.set("item-end", current);
    }
  }
}
var outlierItemList = $items`Extrovermectin™, Volcoino, Poké-Gro fertilizer`;
function printMarginalSession() {
  var barf = sessions.get("barf");
  var meatStart = sessions.get("meat-start");
  var meatEnd = sessions.get("meat-end");
  var itemStart = sessions.get("item-start");
  var itemEnd = sessions.get("item-end");

  // we can only print out marginal items if we've started tracking for marginal value
  if (barf && meatStart && meatEnd) {
    var _barf$value = barf.value(garboValue),
      barfItemDetails = _barf$value.itemDetails;
    var isOutlier = detail => outlierItemList.includes(detail.item) || detail.quantity === 1 && detail.value >= 5000 && barfItemDetails.some(d => d.item === detail.item && d.quantity <= 2);
    var meatMpa = Session.computeMPA(meatStart, meatEnd, {
      value: garboValue,
      isOutlier
    });
    if (itemStart && itemEnd) {
      // MPA printout including maringal items
      var itemMpa = Session.computeMPA(itemStart, itemEnd, {
        value: garboValue,
        isOutlier,
        excludeValue: {
          item: extraValue$1
        }
      });
      kolmafia.print(`Outliers:`, HIGHLIGHT);
      var _iterator = _createForOfIteratorHelper(itemMpa.outlierItems),
        _step;
      try {
        for (_iterator.s(); !(_step = _iterator.n()).done;) {
          var detail = _step.value;
          kolmafia.print(`${detail.quantity} ${detail.item} worth ${detail.value.toFixed(0)} total`, HIGHLIGHT);
        }
      } catch (err) {
        _iterator.e(err);
      } finally {
        _iterator.f();
      }
      var effectiveMpa = itemMpa.mpa.effective - itemMpa.mpa.meat + meatMpa.mpa.meat;
      var totalMpa = itemMpa.mpa.total - itemMpa.mpa.meat + meatMpa.mpa.meat;
      kolmafia.print(`Marginal MPA: ${formatNumber(Math.round(meatMpa.mpa.meat * 100) / 100)} [raw] + ${formatNumber(Math.round(itemMpa.mpa.items * 100) / 100)} [items] (${formatNumber(Math.round((itemMpa.mpa.total - itemMpa.mpa.effective) * 100) / 100)} [outliers]) = ${formatNumber(Math.round(effectiveMpa * 100) / 100)} [total] (${formatNumber(Math.round(totalMpa * 100) / 100)} [w/ outliers])`, HIGHLIGHT);
    } else {
      // MPA printout excluding marginal items
      kolmafia.print("Warning: Insufficient turns were run, so this estimate is subject to large variance. Be careful when using these values as is.", "red");
      kolmafia.print(`Marginal MPA: ${formatNumber(Math.round(meatMpa.mpa.meat * 100) / 100)} [raw] + ${formatNumber(Math.round(meatMpa.mpa.items * 100) / 100)} [items] = ${formatNumber(Math.round(meatMpa.mpa.total * 100) / 100)} [total]`, HIGHLIGHT);
    }
  }
}
var garboResultsProperties = ["garboResultsMeat", "garboResultsItems", "garboResultsTurns"];
function getGarboDaily(property) {
  return get$2(property, 0);
}
function setGarboDaily(property, value) {
  _set(property, value);
}
function resetGarboDaily() {
  if (resetDailyPreference("garboResultsDate")) {
    var _iterator2 = _createForOfIteratorHelper(garboResultsProperties),
      _step2;
    try {
      for (_iterator2.s(); !(_step2 = _iterator2.n()).done;) {
        var prop = _step2.value;
        setGarboDaily(prop, 0);
      }
    } catch (err) {
      _iterator2.e(err);
    } finally {
      _iterator2.f();
    }
  }
}
function endSession() {
  var printLog = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : true;
  // force marginal mpa to always have a 0 turns remaining calculation
  trackMarginalMpa(0);
  resetGarboDaily();
  var message = (head, turns, meat, items) => kolmafia.print(`${head}, across ${formatNumber(turns)} turns you generated ${formatNumber(meat + items)} meat, with ${formatNumber(meat)} raw meat and ${formatNumber(items)} from items`, HIGHLIGHT);
  var _sessionSinceStart$va = sessionSinceStart().value(garboValue),
    meat = _sessionSinceStart$va.meat,
    items = _sessionSinceStart$va.items,
    itemDetails = _sessionSinceStart$va.itemDetails,
    turns = _sessionSinceStart$va.turns;
  var totalMeat = meat + getGarboDaily("garboResultsMeat");
  var totalItems = items + getGarboDaily("garboResultsItems");
  var totalTurns = turns + getGarboDaily("garboResultsTurns");
  if (printLog) {
    // list the top 3 gaining and top 3 losing items
    var losers = itemDetails.sort((a, b) => a.value - b.value).slice(0, 3);
    var winners = itemDetails.reverse().slice(0, 3);
    kolmafia.print(`Extreme Items:`, HIGHLIGHT);
    for (var _i = 0, _arr = [].concat(_toConsumableArray(winners), _toConsumableArray(losers)); _i < _arr.length; _i++) {
      var detail = _arr[_i];
      kolmafia.print(`${detail.quantity} ${detail.item} worth ${detail.value.toFixed(0)} total`, HIGHLIGHT);
    }
  }
  setGarboDaily("garboResultsMeat", totalMeat);
  setGarboDaily("garboResultsItems", totalItems);
  setGarboDaily("garboResultsTurns", totalTurns);
  if (printLog) {
    message("This run of garbo", turns, meat, items);
    message("So far today", totalTurns, totalMeat, totalItems);
    printMarginalSession();
  }
  if (globalOptions.loginvalidwishes) {
    if (failedWishes.length === 0) {
      kolmafia.print("No invalid wishes found.");
    } else {
      kolmafia.print("Found the following unwishable effects:");
      failedWishes.forEach(effect => kolmafia.print(`${effect}`));
    }
  }
}

var REPORT_RECIPIENT = "Jalen_Arbuckle";
var REPORT_KEYS = ["snootee", "microbrewery", "jickjar", "votemonster", "g9"];
function isReportKey(value) {
  return REPORT_KEYS.includes(value);
}
function sessionStorageKey() {
  return `garbo_reported_${kolmafia.daycount()}`;
}
function getReportedKeys() {
  return new Set((kolmafia.sessionStorage.getItem(sessionStorageKey()) ?? "").split(",").filter(isReportKey));
}
function markReported(key) {
  var reported = getReportedKeys();
  reported.add(key);
  kolmafia.sessionStorage.setItem(sessionStorageKey(), _toConsumableArray(reported).join(","));
}

/**
 * Send a daily report value via private message. Uses sessionStorage
 * to ensure each key is only reported once per gameday. The storage key
 * includes the gameday, so old entries are naturally orphaned on rollover.
 */
function reportDaily(key, value) {
  if (getReportedKeys().has(key)) return;
  kolmafia.chatPrivate(REPORT_RECIPIENT, `${key}:${value}`);
  markReported(key);
}
var PREF_WATCH_REPORTS = [{
  pref: "_dailySpecial",
  key: () => kolmafia.canadiaAvailable() ? "snootee" : kolmafia.gnomadsAvailable() ? "microbrewery" : null
}, {
  pref: "_jickJarAvailable",
  emptyValue: "unknown",
  key: "jickjar",
  value: prefValue => prefValue === "true" ? kolmafia.toInt(kolmafia.myId()) % 23 : null
}, {
  pref: "_voteMonster",
  key: "votemonster"
}, {
  pref: "_g9Effect",
  emptyValue: "0",
  key: "g9"
}];

/**
 * Check all pref-watch reports and send any that are newly filled.
 * Call this at any convenient hook point during the run.
 */
function checkPrefWatchReports() {
  var reported = getReportedKeys();
  var _iterator = _createForOfIteratorHelper(PREF_WATCH_REPORTS),
    _step;
  try {
    for (_iterator.s(); !(_step = _iterator.n()).done;) {
      var report = _step.value;
      var key = undelay(report.key);
      if (key === null) continue;
      if (reported.has(key)) continue;
      var prefValue = get$2(report.pref, "");
      if (prefValue === (report.emptyValue ?? "")) continue;
      var value = report.value ? report.value(prefValue) : prefValue;
      if (value === null) continue;
      reportDaily(key, value);
    }
  } catch (err) {
    _iterator.e(err);
  } finally {
    _iterator.f();
  }
}

var banishMethods = [{
  source: $skill`Spring Kick`,
  available: () => have$P($item`spring shoes`),
  macro: Macro.trySkill($skill`Spring Kick`).trySkill($skill`Spring Away`),
  equip: $item`spring shoes`
}, {
  source: $skill`Batter Up!`,
  available: () => kolmafia.myClass() === $class`Seal Clubber` && have$P($skill`Batter Up!`) && kolmafia.myFury() >= 5,
  macro: Macro.trySkill($skill`Batter Up!`),
  equip: $item`seal-clubbing club`
}, {
  source: $skill`Order a Kneecapping`,
  available: () => have$P($skill`Order a Kneecapping`) && !get$2("_kneecappingOrdered"),
  macro: Macro.trySkill($skill`Order a Kneecapping`)
}, {
  source: $item`human musk`,
  available: () => true,
  macro: Macro.tryItem($item`human musk`),
  retrieve: true
}, {
  source: $skill`Sea *dent: Throw a Lightning Bolt`,
  available: () => have$P($item`Monodent of the Sea`) && get$2("_seadentLightningUsed", 0) < 11,
  equip: $item`Monodent of the Sea`,
  macro: Macro.trySkill($skill`Sea *dent: Throw a Lightning Bolt`)
}];
function chooseBanish() {
  if (FarmingStrategy.monstersToBanish().length === 0) {
    return null;
  }
  var banishedMonsters = getBanishedMonsters();
  return banishMethods.find(method => method.available() && !FarmingStrategy.banishMonsters.includes(banishedMonsters.get(method.source) ?? $monster.none)) ?? null;
}

function logTargetFight(encounterType) {
  var isDigitize = encounterType.includes("Digitize Wanderer");
  if (isDigitize) {
    eventLog.digitizedCopyTargetsFought++;
  } else {
    eventLog.initialCopyTargetsFought++;
  }
  eventLog.copyTargetSources.push(isDigitize ? "Digitize" : "Unknown Source");
}

/** A base engine for Garbo!
 * Runs extra logic before executing all tasks.
 */
var BaseGarboContextEngine = /*#__PURE__*/function (_ContextualEngine) {
  function BaseGarboContextEngine(tasks, options) {
    var _this;
    _classCallCheck(this, BaseGarboContextEngine);
    var startTime = Date.now();
    _this = _callSuper(this, BaseGarboContextEngine, [tasks, options]);
    _defineProperty(_this, "history", []);
    if (globalOptions.history) {
      _this.history.push({
        name: "Engine/Construct",
        startTime,
        durationMs: Date.now() - startTime
      });
    }
    return _this;
  }
  _inherits(BaseGarboContextEngine, _ContextualEngine);
  return _createClass(BaseGarboContextEngine, [{
    key: "printExecutingMessage",
    value: function printExecutingMessage(task) {
      kolmafia.print(``);
      kolmafia.print(`Executing ${task.name}`, HIGHLIGHT);
    }
  }, {
    key: "destruct",
    value: function destruct() {
      var startTime = Date.now();
      _superPropGet(BaseGarboContextEngine, "destruct", this, 3)([]);
      if (globalOptions.history) {
        this.history.push({
          name: "Engine/Destruct",
          startTime,
          durationMs: Date.now() - startTime
        });
        var filename = `garbo_history_${kolmafia.todayToString()}.csv`;
        var buffer = kolmafia.fileToBuffer(filename).trim();
        var taskArray = [].concat(_toConsumableArray(buffer.split("\n")), _toConsumableArray(this.history.map(item => `${item.startTime},${item.name.replace(",", "")},${item.durationMs}`)));
        kolmafia.bufferToFile(taskArray.join("\n"), filename);
      }
    }
  }, {
    key: "available",
    value: function available(task) {
      safeInterrupt();
      var taskSober = undelay(task.sobriety, this.getContext(task));
      if (taskSober) {
        return (taskSober === "drunk" && !sober() || taskSober === "sober" && sober()) && _superPropGet(BaseGarboContextEngine, "available", this, 3)([task]);
      }
      return _superPropGet(BaseGarboContextEngine, "available", this, 3)([task]);
    }
  }, {
    key: "dress",
    value: function dress(task, outfit) {
      var duplicate = undelay(task.duplicate, this.getContext(task));
      if (duplicate && have$P($item`pro skateboard`) && !get$2("_epicMcTwistUsed")) {
        outfit.equip($item`pro skateboard`);
      }
      _superPropGet(BaseGarboContextEngine, "dress", this, 3)([task, outfit]);
      var canBreathe = () => kolmafia.booleanModifier("Adventure Underwater");
      if (outfit.modifier.includes("+sea") && !canBreathe()) {
        clearMaximizerCache();
        _superPropGet(BaseGarboContextEngine, "dress", this, 3)([task, outfit]);
        if (!canBreathe()) {
          throw new Error("Can't adventure underwater, figure it out.");
        }
      }
      if (kolmafia.itemAmount($item`tiny stillsuit`) > 0) {
        kolmafia.equip(kolmafia.myFamiliar() === $familiar`Cornbeefadon` ? $familiar`Mosquito` : $familiar`Cornbeefadon`, $item`tiny stillsuit`);
      }
    }
  }, {
    key: "prepare",
    value: function prepare(task) {
      if ("combat" in task) safeRestore();
      _superPropGet(BaseGarboContextEngine, "prepare", this, 3)([task]);
    }
  }, {
    key: "execute",
    value: function execute(task) {
      var startTime = Date.now();
      var spentTurns = kolmafia.totalTurnsPlayed();
      var context = this.getContext(task);
      var duplicate = undelay(task.duplicate, context);
      var before = getSkills();
      if (duplicate && have$I() && duplicateUsesRemaining() > 0) {
        educate([$skill`Extract`, $skill`Duplicate`]);
      }
      _superPropGet(BaseGarboContextEngine, "execute", this, 3)([task]);
      if (kolmafia.totalTurnsPlayed() !== spentTurns) {
        if (!undelay(task.spendsTurn, context)) {
          kolmafia.print(`Task ${task.name} spent a turn but was marked as not spending turns`);
        }
      }
      var foughtATarget = get$2("lastEncounter") === globalOptions.target.name;
      if (foughtATarget) logTargetFight(task.name);
      shrugBadEffects($effect`Feeling Lost`); // We deliberately use Feeling Lost sometimes
      wanderer().clear();
      sessionSinceStart().value(garboValue);
      if (duplicate && have$I()) {
        var _iterator = _createForOfIteratorHelper(before),
          _step;
        try {
          for (_iterator.s(); !(_step = _iterator.n()).done;) {
            var skill = _step.value;
            educate(skill);
          }
        } catch (err) {
          _iterator.e(err);
        } finally {
          _iterator.f();
        }
      }
      checkPrefWatchReports();
      if (globalOptions.history) {
        this.history.push({
          name: task.name,
          startTime,
          durationMs: Date.now() - startTime
        });
      }
    }
  }, {
    key: "markAttempt",
    value: function markAttempt(task) {
      _superPropGet(BaseGarboContextEngine, "markAttempt", this, 3)([task]);
      if (!!globalOptions.halt && task.name.localeCompare(globalOptions.halt, undefined, {
        sensitivity: "base"
      }) === 0) {
        throw new Error(`Task halt requested for "${task.name}". Stopping Garbage Collector.`);
      }
    }
  }]);
}(ContextualEngine);
_defineProperty(BaseGarboContextEngine, "defaultSettings", _objectSpread2(_objectSpread2({}, Engine.defaultSettings), {}, {
  choiceAdventureScript: "garbo_choice.js"
}));
var BaseGarboEngine = /*#__PURE__*/function (_BaseGarboContextEngi) {
  function BaseGarboEngine() {
    _classCallCheck(this, BaseGarboEngine);
    return _callSuper(this, BaseGarboEngine, arguments);
  }
  _inherits(BaseGarboEngine, _BaseGarboContextEngi);
  return _createClass(BaseGarboEngine, [{
    key: "getContext",
    value: function getContext() {
      // noop
    }
  }]);
}(BaseGarboContextEngine);
var _banish = /*#__PURE__*/new WeakMap();
var FarmTurnEngine = /*#__PURE__*/function (_BaseGarboContextEngi2) {
  function FarmTurnEngine() {
    var _this2;
    _classCallCheck(this, FarmTurnEngine);
    for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
      args[_key] = arguments[_key];
    }
    _this2 = _callSuper(this, FarmTurnEngine, [].concat(args));
    _classPrivateFieldInitSpec(_this2, _banish, null);
    return _this2;
  }
  _inherits(FarmTurnEngine, _BaseGarboContextEngi2);
  return _createClass(FarmTurnEngine, [{
    key: "getContext",
    value: function getContext() {
      return {
        banish: _classPrivateFieldGet2(_banish, this)
      };
    }
  }, {
    key: "getNextTask",
    value: function getNextTask() {
      _classPrivateFieldSet2(_banish, this, chooseBanish());
      return _superPropGet(FarmTurnEngine, "getNextTask", this, 3)([]);
    }
  }, {
    key: "execute",
    value: function execute(task) {
      _superPropGet(FarmTurnEngine, "execute", this, 3)([task]);
      _classPrivateFieldSet2(_banish, this, null);
    }
  }]);
}(BaseGarboContextEngine);

/**
 * A safe engine for Garbo!
 * Treats soft limits as tasks that should be skipped, with a default max of one attempt for any task.
 */
var SafeGarboContextEngine = /*#__PURE__*/function (_BaseGarboContextEngi3) {
  function SafeGarboContextEngine(tasks) {
    _classCallCheck(this, SafeGarboContextEngine);
    var options = new EngineOptions();
    options.default_task_options = {
      limit: {
        skip: 1
      }
    };
    return _callSuper(this, SafeGarboContextEngine, [tasks, options]);
  }
  _inherits(SafeGarboContextEngine, _BaseGarboContextEngi3);
  return _createClass(SafeGarboContextEngine);
}(BaseGarboContextEngine);
var SafeGarboEngine = /*#__PURE__*/function (_SafeGarboContextEngi) {
  function SafeGarboEngine() {
    _classCallCheck(this, SafeGarboEngine);
    return _callSuper(this, SafeGarboEngine, arguments);
  }
  _inherits(SafeGarboEngine, _SafeGarboContextEngi);
  return _createClass(SafeGarboEngine, [{
    key: "getContext",
    value: function getContext() {
      // noop
    }
  }]);
}(SafeGarboContextEngine);
function runQuests(quests,
// eslint-disable-next-line @typescript-eslint/no-explicit-any
garboEngine) {
  var engine = new garboEngine(getTasks(quests));
  try {
    engine.run();
  } finally {
    engine.destruct();
  }
}
function runSafeGarboQuests(quests) {
  runQuests(quests, SafeGarboEngine);
}
function runGarboQuests(quests) {
  runQuests(quests, BaseGarboEngine);
}
function runGarboFarmQuests(quests) {
  runQuests(quests, FarmTurnEngine);
}

function bestVykeaLevel() {
  var vykeas = [{
    level: 1,
    dowelCost: 0
  }, {
    level: 2,
    dowelCost: 1
  }, {
    level: 3,
    dowelCost: 11
  }]; // excluding 4 and 5 as per bean's suggestion
  var vykeaProfit = vykea => {
    var level = vykea.level,
      dowelCost = vykea.dowelCost;
    return estimatedGarboTurns() * baseMeat() * 0.1 * level - (5 * kolmafia.mallPrice($item`VYKEA rail`) + dowelCost * kolmafia.mallPrice($item`VYKEA dowel`) + 5 * kolmafia.mallPrice($item`VYKEA plank`) + 1 * kolmafia.mallPrice($item`VYKEA instructions`));
  };
  if (vykeas.some(vykea => vykeaProfit(vykea) > 0)) {
    return maxBy(vykeas, vykeaProfit).level;
  }
  return 0;
}
var PostFreeFightTasks = [{
  name: "Configure Vykea",
  ready: () => get$2("_VYKEACompanionLevel") === 0 && bestVykeaLevel() > 0,
  completed: () => get$2("_VYKEACompanionLevel") > 0,
  do: () => kolmafia.cliExecute(`create level ${bestVykeaLevel()} couch`),
  acquire: [{
    item: $item`VYKEA hex key`
  }],
  spendsTurn: false
}, {
  name: "Configure Thrall",
  ready: () => kolmafia.myClass() === $class`Pastamancer` && have$P($skill`Bind Lasagmbie`),
  completed: () => kolmafia.myThrall() === $thrall`Lasagmbie`,
  do: () => kolmafia.useSkill($skill`Bind Lasagmbie`),
  outfit: () => {
    if (kolmafia.myMaxmp() >= 200) return {};
    return {
      modifier: "MP"
    };
  },
  prepare: () => kolmafia.restoreMp(200),
  spendsTurn: false
}, {
  name: "Level Up Thrall",
  ready: () => kolmafia.myClass() === $class`Pastamancer` && have$P($item`experimental carbon fiber pasta additive`) && kolmafia.myThrall() !== $thrall.none,
  completed: () => get$2("_pastaAdditive") || kolmafia.myThrall().level >= 10,
  do: () => kolmafia.use($item`experimental carbon fiber pasta additive`),
  spendsTurn: false
}];
var PostFreeFightQuest = {
  name: "Post Free Fight",
  tasks: PostFreeFightTasks
};

function postFreeFightDailySetup() {
  runSafeGarboQuests([PostFreeFightQuest]);
}

var MIDNIGHTS = [{
  location: $location`Gingerbread Upscale Retail District`,
  choices: {
    1209: 2,
    1214: 1
  },
  available: () => kolmafia.haveOutfit("gingerbread best") && kolmafia.outfitPieces("gingerbread best").every(piece => kolmafia.canEquip(piece)) && kolmafia.itemAmount($item`high-end ginger wine`) < 11,
  value: () => {
    var best = bestConsumable("booze", true, $items`high-end ginger wine, astral pilsner`);
    var gingerWineValue = (0.5 * 30 * targetMeat() + getAverageAdventures($item`high-end ginger wine`) * get$2("valueOfAdventure")) / 2;
    var valueDif = gingerWineValue - best.value;
    return 2 * valueDif;
  }
}, {
  location: $location`Gingerbread Upscale Retail District`,
  available: () => kolmafia.haveOutfit("gingerbread best") && kolmafia.outfitPieces("gingerbread best").every(piece => kolmafia.canEquip(piece)) && have$P($item`sprinkles`, 300),
  choices: {
    1209: 2,
    1214: 2
  },
  value: () => garboValue($item`fancy chocolate sculpture`)
}, {
  location: $location`Gingerbread Upscale Retail District`,
  available: () => kolmafia.haveOutfit("gingerbread best") && kolmafia.outfitPieces("gingerbread best").every(piece => kolmafia.canEquip(piece)) && have$P($item`sprinkles`, 1000),
  choices: {
    1209: 2,
    1214: 3
  },
  value: () => garboValue($item`Pop Art: a Guide`)
}, {
  location: $location`Gingerbread Upscale Retail District`,
  available: () => kolmafia.haveOutfit("gingerbread best") && kolmafia.outfitPieces("gingerbread best").every(piece => kolmafia.canEquip(piece)) && have$P($item`sprinkles`, 1000),
  choices: {
    1209: 2,
    1214: 4
  },
  value: () => garboValue($item`No Hats as Art`)
}, {
  location: $location`Gingerbread Civic Center`,
  choices: {
    1203: 2
  },
  available: () => have$P($item`sprinkles`, 300) && !canJudgeFudge(),
  value: () => garboValue($item`counterfeit city`)
}, {
  location: $location`Gingerbread Civic Center`,
  choices: {
    1203: 4
  },
  available: () => have$P($item`sprinkles`, 5) && !canJudgeFudge(),
  value: () => 5 * garboValue($item`gingerbread cigarette`)
}];
var DEFAULT_MIDNIGHT = {
  location: $location`Gingerbread Train Station`,
  choices: {
    1205: 1
  },
  value: () => 0
};
function bestMidnightAvailable() {
  var availableMidnights = [].concat(_toConsumableArray(MIDNIGHTS.filter(_ref => {
    var location = _ref.location,
      available = _ref.available;
    return kolmafia.canAdventure(location) && available();
  })), [DEFAULT_MIDNIGHT]);
  return maxBy(availableMidnights, _ref2 => {
    var value = _ref2.value;
    return value();
  });
}

function desirableIngredients() {
  return have$P($skill`Head in the Game`) && have$P($item`mafia pointer finger ring`) ? ["msg", "cajun", "rawhide", "carrot"] : ["cajun", "rawhide", "carrot"];
}
function shouldUnlockIngredients() {
  if (!have$D()) return false;
  var shouldTryToUnlockIngredients = desirableIngredients().filter(i => ingredientsUnlocked().includes(i) || kolmafia.canAdventure(locationOf(i))).length >= 3;
  var doneUnlockingIngredients = desirableIngredients().filter(i => ingredientsUnlocked().includes(i)).length >= 3;
  return shouldTryToUnlockIngredients && !doneUnlockingIngredients;
}
function ingredientsToFillWith() {
  return [].concat(_toConsumableArray(desirableIngredients().filter(i => ingredientsUnlocked().includes(i))), _toConsumableArray(byStat({
    Muscle: ["vanilla", "pumpkin", "cinnamon"],
    Moxie: ["cinnamon", "pumpkin", "vanilla"],
    Mysticality: ["pumpkin", "vanilla", "cinnamon"]
  }))).splice(0, 3);
}
function latteMalformed() {
  return ["vanilla", "pumpkin", "cinnamon"].some(defaultIngredient => !ingredientsUnlocked().includes(defaultIngredient));
}

// Returns whether the latteUnlocks preference contains the default ingredients
function checkAndCorrectLatteMalformation() {
  if (!latteMalformed()) return true;
  kolmafia.visitUrl("main.php?latte=1", false);
  if (!latteMalformed()) return true;
  kolmafia.print("Can't access Latte Lover's Mug shop, disabling it", "red");
  _set("_latteBanishUsed", true);
  _set("_latteCopyUsed", true);
  _set("_latteRefillsUsed", 3);
  return false;
}
function shouldFillLatte() {
  if (!have$P($item`latte lovers member's mug`) || get$2("_latteRefillsUsed") >= 3) {
    return false;
  }
  if (get$2("_latteCopyUsed")) return true;
  if (get$2("_latteBanishUsed")) return true;
  if (checkAndCorrectLatteMalformation() && !setEqual(currentIngredients(), ingredientsToFillWith())) {
    return true;
  }
  return false;
}
function tryFillLatte() {
  return shouldFillLatte() && fill.apply(Latte, _toConsumableArray(ingredientsToFillWith())) && checkAndCorrectLatteMalformation();
}

var currentAdventures = () => distillateAdventures();
var nextDistillateSweat = () => Math.ceil((currentAdventures() + 0.5) ** (5 / 2));
var previousDistillateSweat = () => Math.ceil(Math.max(currentAdventures() - 0.5, 0) ** (5 / 2));
var adventuresPerSweat = () => 1 / (nextDistillateSweat() - previousDistillateSweat());
var turnsNeededForNextAdventure = function turnsNeededForNextAdventure() {
  var equipped = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : true;
  return Math.ceil((nextDistillateSweat() - get$2("familiarSweat")) / (equipped ? 3 : 1));
};

var famExpValue = new Map([[$familiar`Chest Mimic`, () => $familiar`Chest Mimic`.experience < mimicExperienceNeeded(true) ? MEAT_TARGET_MULTIPLIER() * get$2("valueOfAdventure") / 50 : 0], [$familiar`Pocket Professor`, () => $familiar`Pocket Professor`.experience < 400 && (!get$2("_thesisDelivered") || !globalOptions.ascend) ? 11 * get$2("valueOfAdventure") / 200 : 0], [$familiar`Grey Goose`, () => $familiar`Grey Goose`.experience < 400 && (!get$2("_meatifyMatterUsed") || !globalOptions.ascend) ? 15 ** 4 / 400 : 0]]);
function freeFightOutfit() {
  var spec = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : {};
  var adventure = arguments.length > 1 ? arguments[1] : undefined;
  var options = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : {};
  cleaverCheck();
  var _toAdventure = toAdventure(adventure),
    location = _toAdventure.location;
  var computedSpec = computeOutfitSpec(spec, location);
  validateGarbageFoldable(computedSpec);
  var outfit = Outfit.from(computedSpec, new Error(`Failed to construct outfit from spec ${JSON.stringify(spec)}!`));
  outfit.familiar ?? (outfit.familiar = freeFightFamiliar(adventure, computeFamiliarMenuOptions(options.familiarOptions, options.duplicate ?? false, outfit)));
  var mode = location === $location`The Deep Machine Tunnels` ? BonusEquipMode.DMT : BonusEquipMode.FREE;
  if (outfit.familiar !== $familiar`Patriotic Eagle`) {
    var familiarExpValue = undelay(famExpValue.get(outfit.familiar));
    outfit.modifier.push(familiarExpValue ? `${familiarExpValue} Familiar Experience` : "Familiar Weight");
  }
  var bjornChoice = chooseBjorn(mode, outfit.familiar);
  if (get$2("_vampyreCloakeFormUses") < 10) {
    outfit.setBonus($item`vampyric cloake`, 500);
  }
  outfit.addBonuses(bonusGear(mode));
  applyCheeseBonus(outfit, mode);
  if (!(globalOptions.ascend && !sober()) && turnsNeededForNextAdventure() <= estimatedGarboTurns()) {
    outfit.setBonus($item`tiny stillsuit`, get$2("valueOfAdventure") * 2 * adventuresPerSweat());
  }
  if (mode !== BonusEquipMode.DMT) {
    outfit.addBonuses(toyCupidBow(outfit.familiar));
  }
  if (location === getLocation() && turnsLeftOnQuest(false) === 1 && haveBooze()) {
    outfit.addBonus($item`Guzzlr pants`, expectedReward(true) - expectedReward(false));
  }
  var bjornalike = $items`Crown of Thrones, Buddy Bjorn`.find(item => outfit.canEquip(item));
  if (bjornalike) {
    outfit.setBonus(bjornalike, bjornChoice.value);
    var other = $items`Buddy Bjorn, Crown of Thrones`.filter(i => i !== bjornalike)[0];
    outfit.avoid.push(other);
    switch (bjornalike) {
      case $item`Buddy Bjorn`:
        outfit.bjornify(bjornChoice.familiar);
        break;
      case $item`Crown of Thrones`:
        outfit.enthrone(bjornChoice.familiar);
        break;
    }
  }
  outfit.setModes({
    snowsuit: "nose",
    parka: "dilophosaur"
  });
  return outfit;
}
function computeOutfitSpec(spec, location) {
  return _objectSpread2(_objectSpread2({}, spec), {}, {
    equip: [].concat(_toConsumableArray(spec.equip ?? []), _toConsumableArray(wanderer().getEquipment(location)))
  });
}
function computeFamiliarMenuOptions() {
  var options = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : {};
  var duplicate = arguments.length > 1 ? arguments[1] : undefined;
  var outfit = arguments.length > 2 ? arguments[2] : undefined;
  return _objectSpread2(_objectSpread2({}, options), {}, {
    allowAttackFamiliars: options.allowAttackFamiliars ?? !(duplicate && have$I() && duplicateUsesRemaining() > 0),
    equipmentForced: options.equipmentForced || !outfit.canEquip($item`toy Cupid bow`)
  });
}

var _autumnAtonManager;
var autumnAtonManager = () => _autumnAtonManager ?? (_autumnAtonManager = new AutumnAtonManager({
  averageItemValue: garboAverageValue,
  estimatedTurns: estimatedGarboTurns,
  estimatedTurnsTomorrow: () => globalOptions.ascend ? 0 : estimatedTurnsTomorrow
}));

function hotTubAvailable() {
  return have$P($item`Clan VIP Lounge key`) && get$2("_hotTubSoaks") < 5;
}

function lavaDogsAccessible() {
  return haveInCampground($item`haunted doghouse`) && !get$2("doghouseBoarded") && realmAvailable("hot");
}
function lavaDogsComplete() {
  return get$2("hallowienerVolcoino") || $location`The Bubblin' Caldera`.turnsSpent > 7 || $location`The Bubblin' Caldera`.noncombatQueue.includes("Lava Dogs");
}

function resultValue(result) {
  if (result instanceof kolmafia.Item) return garboValue(result);
  if (Array.isArray(result)) return garboAverageValue.apply(void 0, _toConsumableArray(result));
  return effectValue(result.effect, result.duration);
}
/*
 * @returns Whether `a` is strictly better than `b`
 */
function strictlyBetterThan(a, b) {
  return NEEDS.every(need => {
    var result = b.values[need];
    if (result === undefined) return true;
    var other = a.values[need];
    if (other === undefined) return false;
    if (other <= result) return false;
    return true;
  });
}
function getCoveredNeeds(_ref) {
  var furniture = _ref.furniture;
  return Object.keys(getStats(furniture));
}
function viableFurniture() {
  var discovered = discoveredFurniture();
  return [{
    furniture: "empty",
    values: {}
  }].concat(_toConsumableArray(discovered.map(furniture => ({
    furniture,
    values: Object.fromEntries(Object.entries(getStats(furniture)).map(_ref2 => {
      var _ref3 = _slicedToArray(_ref2, 2),
        need = _ref3[0],
        result = _ref3[1];
      return [need, resultValue(result)];
    }))
  })).filter((f, index, valuedDiscoveries) => !valuedDiscoveries.slice(index).some(futureFurniture => strictlyBetterThan(futureFurniture, f)))));
}
function valueCombination$1(combo) {
  var total = combo.reduce((acc, _ref4) => {
    var values = _ref4.values;
    return _objectSpread2(_objectSpread2({}, values), acc);
  }, {});
  return sum(NEEDS, need => total[need] ?? 0);
}
function buildCombination(combinations, furniture) {
  return combinations.flatMap(combination => {
    var coveredNeeds = new Set(combination.flatMap(getCoveredNeeds));
    var plausibleFurniture = furniture.filter(f => getCoveredNeeds(f).some(need => !coveredNeeds.has(need))); // Only furniture that cover at least one presently-uncovered need need apply
    return (plausibleFurniture.length ? plausibleFurniture : [{
      furniture: "empty",
      values: {}
    }]).map(furniture => [].concat(_toConsumableArray(combination), [furniture]));
  });
}
function getViableCombinations() {
  var furniture = viableFurniture();
  return Array(4).fill(null).reduce(acc => buildCombination(acc, furniture), [[]]);
}
function findBestCombination() {
  return maxBy(getViableCombinations(), valueCombination$1).map(_ref5 => {
    var furniture = _ref5.furniture;
    return furniture;
  });
}
var bestCombination;
var unlocked;
function getBestLeprecondoCombination() {
  if (unlocked !== get$2("leprecondoDiscovered")) {
    unlocked = get$2("leprecondoDiscovered");
    bestCombination = findBestCombination();
  }
  return bestCombination;
}
function leprecondoTask() {
  return {
    name: "Configure Leprecondo",
    ready: () => have$e() && rearrangesRemaining() > 0,
    completed: () => arrayEquals$1(installedFurniture(), getBestLeprecondoCombination()),
    do: () => setFurniture.apply(Leprecondo, _toConsumableArray(getBestLeprecondoCombination())),
    spendsTurn: false
  };
}

var STUFF_TO_CLOSET = $items`bowling ball, funky junk key`;
var STUFF_TO_USE = $items`Armory keycard, bottle-opener keycard, SHAWARMA Initiative Keycard`;
function closetStuff() {
  return {
    name: "Closet Stuff",
    completed: () => STUFF_TO_CLOSET.every(i => kolmafia.itemAmount(i) === 0),
    do: () => STUFF_TO_CLOSET.forEach(i => kolmafia.putCloset(kolmafia.itemAmount(i), i))
  };
}
function useStuff() {
  return {
    name: "Use Stuff",
    completed: () => STUFF_TO_USE.every(i => kolmafia.itemAmount(i) === 0),
    do: () => STUFF_TO_USE.forEach(i => kolmafia.use(kolmafia.itemAmount(i), i))
  };
}
// I should export this
var BARF_PLANTS = {
  unknown: [],
  none: [],
  outdoor: [Rutabeggar, SeltzerWatercress, LettuceSpray],
  indoor: [StealingMagnolia, Impatiens, PitcherPlant],
  underground: [HornOfPlenty, ShuffleTruffle, MaxHeadshroom],
  underwater: [Crookweed, Snori, UpSeaDaisy]
};
function floristFriars() {
  var barfPlants = BARF_PLANTS[FarmingStrategy.location.environment];
  return {
    name: "Florist Plants",
    completed: () => isFull(FarmingStrategy.location) || barfPlants.length === 0,
    ready: () => get$2("lastAdventure") === FarmingStrategy.location && have$L() && barfPlants.some(flower => flower.available(FarmingStrategy.location)),
    do: () => barfPlants.filter(flower => flower.available(FarmingStrategy.location)).forEach(flower => flower.plant()),
    available: () => have$L() && barfPlants.some(flower => flower.available(FarmingStrategy.location))
  };
}
function fillPantsgivingFullness() {
  return {
    name: "Fill Pantsgiving/Toilet Fullness",
    ready: () => !globalOptions.nodiet && sum($slots.all(), slot => get$1("Stomach Capacity", kolmafia.equippedItem(slot))) <= 0,
    completed: () => getRemainingStomach() <= 0,
    do: () => consumeDiet(computeDiet().pantsgiving(), "PANTSGIVING"),
    available: () => have$P($item`Pantsgiving`) || $item`Pork Elf toilet`.name in kolmafia.getCampground()
  };
}
function fillSweatyLiver() {
  return {
    name: "Fill Sweaty Liver",
    ready: () => !globalOptions.nodiet && canUseSkill($skill`Sweat Out Some Booze`) && kolmafia.myInebriety() > 0 && availableCasts$1($skill`Sweat Out Some Booze`) === potentialCasts($skill`Sweat Out Some Booze`) && sum($slots.all(), slot => get$1("Liver Capacity", kolmafia.equippedItem(slot))) <= 0,
    completed: () => $skill`Sweat Out Some Booze`.dailylimit === 0 || kolmafia.myInebriety() - potentialCasts($skill`Sweat Out Some Booze`) > kolmafia.inebrietyLimit(),
    do: () => {
      while (canUseSkill($skill`Sweat Out Some Booze`) && kolmafia.myInebriety() > 0) {
        useSkill($skill`Sweat Out Some Booze`);
      }
      consumeDiet(computeDiet().sweatpants(), "SWEATPANTS");
    },
    available: () => !globalOptions.nodiet && potentialCasts($skill`Sweat Out Some Booze`) > 0
  };
}
function numberology() {
  return {
    name: "Numberology",
    ready: () => Object.keys(kolmafia.reverseNumberology()).includes("69"),
    completed: () => get$2("_universeCalculated") >= get$2("skillLevel144"),
    do: () => kolmafia.cliExecute("numberology 69"),
    available: () => get$2("_universeCalculated") < get$2("skillLevel144")
  };
}
var juneCleaverSkipChoices;
function getJuneCleaverskipChoices() {
  if (skipsRemaining() > 0) {
    if (!juneCleaverSkipChoices) {
      juneCleaverSkipChoices = _toConsumableArray(choices).sort((a, b) => valueJuneCleaverOption(juneCleaverChoiceValues[a][bestJuneCleaverOption(a)]) - valueJuneCleaverOption(juneCleaverChoiceValues[b][bestJuneCleaverOption(b)])).splice(0, 3);
    }
    return _toConsumableArray(juneCleaverSkipChoices);
  }
  return [];
}
var juneCleaverChoices = () => Object.fromEntries(choices.map(choice => [choice, getJuneCleaverskipChoices().includes(choice) ? 4 : bestJuneCleaverOption(choice)]));
function juneCleaver() {
  return {
    name: "June Cleaver",
    ready: () => have$r() && teleportEffects.every(e => !have$P(e)) && kolmafia.myAdventures() > 0,
    completed: () => get$2("_juneCleaverFightsLeft") > 0,
    do: () => {
      var location = kolmafia.myInebriety() > kolmafia.inebrietyLimit() ? $location`Drunken Stupor` : $location`Noob Cave`;
      withProperty("recoveryScript", "", () => kolmafia.adv1(location, 1));
    },
    outfit: {
      weapon: $item`June cleaver`
    },
    combat: new GarboStrategy(() => Macro.abortWithMsg(`Expected June Cleaver non-combat but ended up in combat.`)),
    choices: juneCleaverChoices,
    available: () => have$r(),
    post: () => {
      if (["Poetic Justice", "Lost and Found"].includes(get$2("lastEncounter")) && have$P($effect`Beaten Up`)) {
        kolmafia.useSkill($skill`Tongue of the Walrus`);
      }
    }
  };
}
function fallbot() {
  return {
    name: "Autumn-Aton",
    completed: () => !available$1(),
    ready: () => globalOptions.ascend || turnsForQuest() < estimatedGarboTurns() + remainingUserTurns(),
    do: () => {
      sendTo(autumnAtonManager().bestLocation);
    },
    available: () => have$u(),
    post: () => {
      if (have$P($item`autumn-aton`)) {
        kolmafia.cliExecute("refresh inventory");
      }
    }
  };
}
function refillCinch() {
  return {
    name: "Refill Cinch",
    ready: () => have$n() && kolmafia.totalFreeRests() > get$2("timesRested"),
    completed: () => get$2("_cinchUsed") < cinchRestoredBy(),
    do: () => {
      var missingCinch = () => {
        return 100 - currentCinch();
      };
      // Only rest if we'll get full value out of the cinch
      // If our current cinch is less than the total available, it means we have free rests left.
      while (missingCinch() >= cinchRestoredBy() && currentCinch() < totalAvailableCinch()) {
        if (!freeRest()) break;
      }
    },
    available: () => have$n()
  };
}
var tokenBought = false;
function eightBitFatLoot() {
  return {
    name: "Check 8-Bit for Fat Loot",
    completed: () => tokenBought,
    ready: () => kolmafia.canAdventure($location`The Spooky Forest`) && get$2("8BitScore") >= 20_000,
    do: () => {
      kolmafia.visitUrl("place.php?whichplace=8bit&action=8treasure");
      if (kolmafia.availableChoiceOptions()[2]) {
        kolmafia.runChoice(2);
      }
      tokenBought = true;
    },
    available: () => tokenBought
  };
}
var funguyWorthIt = true;
function funGuySpores() {
  return {
    name: "Fun-Guy Spores",
    ready: () => funguyWorthIt && kolmafia.myLevel() >= 15 && (!globalOptions.ascend || kolmafia.myAdventures() > 11) && get$2("dinseyRollercoasterNext"),
    completed: () => have$P($effect`Mush-Mouth`),
    do: () => {
      // According to wiki, it has a 75% chance of being a stat mushroom and 25% chance of being another mushroom
      var value = 0.75 * garboAverageValue.apply(void 0, _toConsumableArray($items`Boletus Broletus mushroom, Omphalotus Omphaloskepsis mushroom, Gyromitra Dynomita mushroom`)) + 0.25 * garboAverageValue.apply(void 0, _toConsumableArray($items`Helvella Haemophilia mushroom, Stemonitis Staticus mushroom, Tremella Tarantella mushroom`));
      if (kolmafia.mallPrice($item`Fun-Guy spore`) < value && acquire(1, $item`Fun-Guy spore`, value, false) > 0) {
        kolmafia.use($item`Fun-Guy spore`);
      } else funguyWorthIt = false;
    },
    available: () => funguyWorthIt
  };
}
function leafResin() {
  return {
    name: "Leaf Resin",
    available: have$o(),
    ready: () => numberOfLeaves() > 75 && (estimatedGarboTurns() > 100 || !globalOptions.ascend),
    completed: () => have$P($effect`Resined`),
    acquire: [{
      item: $item`distilled resin`
    }],
    do: () => kolmafia.use($item`distilled resin`),
    post: () => {
      if (!have$P($effect`Resined`)) {
        throw new Error("Did not gain resined after using distilled resin. Mafia bug?");
      }
    }
  };
}
function wardrobeOMatic() {
  return {
    name: "Wardrobe-o-Matic",
    available: have$P($item`wardrobe-o-matic`),
    ready: () => kolmafia.myLevel() >= 20,
    completed: () => have$P($item`futuristic shirt`),
    do: () => kolmafia.use($item`wardrobe-o-matic`)
  };
}
function handleDrenchedInLava() {
  return {
    name: "Drenched In Lava Removal",
    available: () => lavaDogsAccessible(),
    ready: () => lavaDogsComplete(),
    completed: () => !have$P($effect`Drenched in Lava`),
    do: () => {
      if (hotTubAvailable()) {
        kolmafia.cliExecute("hottub");
      }
      if (have$P($effect`Drenched in Lava`)) {
        uneffect($effect`Drenched in Lava`);
      }
    }
  };
}
var bestAbortFreeRun = null;
function getBestAbortFreeRun() {
  if (bestAbortFreeRun === null) {
    var bestFreeRun = maxBy(unlimitedFreeRunList, getAcquirePrice, true);
    bestAbortFreeRun = getAcquirePrice(bestFreeRun) < get$2("valueOfAdventure") ? bestFreeRun : $item.none;
  }
  return bestAbortFreeRun;
}
function acquireAbortFreeRun() {
  return {
    name: "Acquire Best Free Run in Case of Abort",
    completed: () => getBestAbortFreeRun() === $item`none` || have$P(getBestAbortFreeRun()),
    do: () => acquire(1, getBestAbortFreeRun(), get$2("valueOfAdventure"), false)
  };
}
function uneffectAttunement() {
  return {
    name: "Uneffect Eldritch Attunement After 11 Tentacles",
    ready: () => get$2("_eldritchTentaclesFoughtToday") >= (have$P($skill`Evoke Eldritch Horror`) && !get$2("_eldritchHorrorEvoked") // Shrug at 10 if we plan to fight Eldritch Horror TODO: Remove this once we grimoirize everything and can re-order and do Eldritch Horror first
    ? 10 : 11),
    completed: () => !have$P($effect`Eldritch Attunement`),
    do: () => uneffect($effect`Eldritch Attunement`) // Shruggable
  };
}
function usePorkToilet() {
  return {
    name: "Use Pork Elf toilet",
    ready: () => kolmafia.myFullness() >= 2,
    completed: () => get$2("_porkElfToiletUsed"),
    do: () => {
      var startingFullness = kolmafia.myFullness();
      kolmafia.visitUrl("campground.php?action=rest");
      if (kolmafia.myFullness() >= startingFullness) {
        throw new Error("Using toilet did not reduce our fullness!");
      }
    },
    available: () => $item`Pork Elf toilet`.name in kolmafia.getCampground() && (!have$n() || kolmafia.totalFreeRests() <= get$2("timesRested"))
  };
}
function PostQuest(completed) {
  return {
    name: "Postcombat",
    completed,
    tasks: [uneffectAttunement(), acquireAbortFreeRun()].concat(_toConsumableArray(workshedTasks()), [handleDrenchedInLava(), fallbot(), closetStuff(), useStuff(), floristFriars(), numberology(), juneCleaver(), fillPantsgivingFullness(), fillSweatyLiver(), funGuySpores(), eightBitFatLoot(), refillCinch(), usePorkToilet(), leafResin(), wardrobeOMatic(), _objectSpread2(_objectSpread2({}, leprecondoTask()), {}, {
      available: have$e()
    })]).filter(_ref => {
      var available = _ref.available;
      return undelay(available ?? true);
    }).map(task => _objectSpread2(_objectSpread2({}, task), {}, {
      spendsTurn: false
    }))
  };
}

function postCombatActions() {
  runGarboQuests([PostQuest()]);
  safeRestore();
}

var taffyIsWorthIt = () => kolmafia.mallPrice($item`pulled green taffy`) < (targetingMeat() ? MEAT_TARGET_MULTIPLIER() * get$2("valueOfAdventure") : get$2("valueOfAdventure")) && kolmafia.retrieveItem($item`pulled green taffy`);
var wandererFailsafeMacro = () => Macro.externalIf(kolmafia.haveEquipped($item`backup camera`) && get$2("_backUpUses") < 11 && get$2("lastCopyableMonster") === globalOptions.target, Macro.if_(`!monsterid ${globalOptions.target.id}`, Macro.skill($skill`Back-Up to your Last Enemy`)));
var _macro = /*#__PURE__*/new WeakMap();
var _location = /*#__PURE__*/new WeakMap();
var _useAuto = /*#__PURE__*/new WeakMap();
var _action = /*#__PURE__*/new WeakMap();
var TargetFightRunOptions = /*#__PURE__*/function () {
  function TargetFightRunOptions(configOptions) {
    var _ref = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {},
      macro = _ref.macro,
      location = _ref.location,
      useAuto = _ref.useAuto,
      action = _ref.action;
    _classCallCheck(this, TargetFightRunOptions);
    _classPrivateFieldInitSpec(this, _macro, void 0);
    _classPrivateFieldInitSpec(this, _location, void 0);
    _classPrivateFieldInitSpec(this, _useAuto, void 0);
    _classPrivateFieldInitSpec(this, _action, void 0);
    this.configOptions = configOptions;
    _classPrivateFieldSet2(_action, this, action);
    _classPrivateFieldSet2(_macro, this, macro);
    _classPrivateFieldSet2(_location, this, location);
    _classPrivateFieldSet2(_useAuto, this, useAuto);
  }
  return _createClass(TargetFightRunOptions, [{
    key: "location",
    get: function get() {
      if (this.configOptions.location) return this.configOptions.location;
      var suggestion = this.configOptions.draggable && !_classPrivateFieldGet2(_location, this) && checkUnderwater() && taffyIsWorthIt() ? $location`The Briny Deeps` : _classPrivateFieldGet2(_location, this);
      if (this.configOptions.draggable && !suggestion || this.configOptions.draggable === "backup" && suggestion && suggestion.combatPercent < 100) {
        var wanderOptions = {
          wanderer: this.configOptions.draggable,
          allowEquipment: false
        };
        var targetLocation = wanderer().getTarget(wanderOptions).location;
        propertyManager.setChoices(wanderer().getChoices(targetLocation));
        return targetLocation;
      }
      return suggestion ?? $location`Noob Cave`;
    }
  }, {
    key: "macro",
    get: function get() {
      var baseMacro = _classPrivateFieldGet2(_macro, this) ?? Macro.target(this.action);
      return this.configOptions.draggable === "wanderer" ? wandererFailsafeMacro().step(baseMacro) : baseMacro;
    }
  }, {
    key: "useAuto",
    get: function get() {
      return _classPrivateFieldGet2(_useAuto, this) ?? true;
    }
  }, {
    key: "action",
    get: function get() {
      return _classPrivateFieldGet2(_action, this) ?? "???";
    }
  }]);
}();

function runTargetFight(fight) {
  var options = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {};
  var fullOptions = new TargetFightRunOptions(fight, options);
  fight.run(fullOptions);
}

var trickHats = $items`invisible bag, witch hat, beholed bedsheet, wolfman mask, pumpkinhead mask, mummy costume`;
var visitBlock = () => kolmafia.visitUrl(`place.php?whichplace=town&action=town_trickortreat`);
var visitHouse = house => kolmafia.runChoice(3, `whichhouse=${house.toFixed(0)}`);
function treatValue(outfit) {
  return sum(Object.entries(kolmafia.outfitTreats(outfit)), _ref => {
    var _ref2 = _slicedToArray(_ref, 2),
      candyName = _ref2[0],
      probability = _ref2[1];
    return probability * garboValue(kolmafia.toItem(candyName));
  });
}
function getTreatOutfit() {
  var availableOutfits = kolmafia.getOutfits().filter(name => kolmafia.outfitPieces(name).every(piece => kolmafia.canEquip(piece)));
  if (!availableOutfits.length) {
    kolmafia.print("You don't seem to actually have any trick-or-treating outfits available, my friend!");
  }
  return maxBy(availableOutfits, treatValue);
}
function treatOutfit() {
  var outfit = new Outfit();
  var bestTreatOutfit = getTreatOutfit();
  var pieces = kolmafia.outfitPieces(bestTreatOutfit);
  var _iterator = _createForOfIteratorHelper(pieces),
    _step;
  try {
    for (_iterator.s(); !(_step = _iterator.n()).done;) {
      var piece = _step.value;
      if (!outfit.equip(piece)) {
        kolmafia.print(`Could not equip all pieces of trick-or-treating outfit ${bestTreatOutfit}: aborted on ${piece}`);
      }
    }
  } catch (err) {
    _iterator.e(err);
  } finally {
    _iterator.f();
  }
  outfit.equip($familiar`Trick-or-Treating Tot`);
  return outfit;
}
function candyRichBlockValue() {
  var outfitCandyValue = treatValue(getTreatOutfit());
  var totOutfitCandyMultiplier = have$P($familiar`Trick-or-Treating Tot`) ? 1.6 : 1;
  var bowlValue = 1 / 5 * getSaleValue($item`huge bowl of candy`);
  var prunetsValue = have$P($familiar`Trick-or-Treating Tot`) ? 4 * 0.2 * getSaleValue($item`Prunets`) : 0;
  var outfitCandyTotal = 3 * outfitCandyValue * totOutfitCandyMultiplier;
  return outfitCandyTotal + bowlValue + prunetsValue + 5 * globalOptions.prefs.valueOfFreeFight;
}
function shouldAcquireCandyMap() {
  return !kolmafia.holiday().includes("Halloween") && kolmafia.mallPrice($item`map to a candy-rich block`) < 50000 &&
  // Sanity value to prevent mall shenanigans
  candyRichBlockValue() > kolmafia.mallPrice($item`map to a candy-rich block`);
}
function useCandyMapTask() {
  return {
    name: "Acquire Candy Map",
    ready: () => shouldAcquireCandyMap(),
    completed: () => get$2("_mapToACandyRichBlockUsed"),
    do: () => {
      if (acquire(1, $item`map to a candy-rich block`, candyRichBlockValue() - 1, false)) {
        withChoice(804, 2, () => kolmafia.use($item`map to a candy-rich block`));
      }
    },
    limit: {
      skip: 1
    },
    spendsTurn: false
  };
}
function doCandyTreat() {
  return {
    name: "Treat",
    completed: () => !["L", "S"].some(house => get$2("_trickOrTreatBlock").includes(house)),
    ready: () => !kolmafia.holiday().includes("Halloween") && get$2("_mapToACandyRichBlockUsed"),
    outfit: treatOutfit,
    do: () => {
      visitBlock();
      var houses = _toConsumableArray(get$2("_trickOrTreatBlock").split("").entries()).filter(_ref3 => {
        var _ref4 = _slicedToArray(_ref3, 2),
          house = _ref4[1];
        return ["L", "S"].includes(house);
      });
      // We do all treat houses in a row as one task for speed reasons
      var _iterator2 = _createForOfIteratorHelper(houses),
        _step2;
      try {
        for (_iterator2.s(); !(_step2 = _iterator2.n()).done;) {
          var _step2$value = _slicedToArray(_step2.value, 2),
            index = _step2$value[0],
            house = _step2$value[1];
          if (["L", "S"].includes(house)) {
            visitHouse(index);
            if (house === "S") {
              kolmafia.runChoice(2);
              visitBlock();
            }
          }
        }
      } catch (err) {
        _iterator2.e(err);
      } finally {
        _iterator2.f();
      }
    },
    spendsTurn: false,
    combat: new GarboStrategy(() => Macro.abortWithMsg("We were planning on Treating, but we've been Tricked!"))
  };
}
var MAX_HAT_PRICE = 100_000;
function obtainTrickHat() {
  return {
    name: "Obtain Trick Hat",
    completed: () => trickHats.some(hat => have$P(hat)),
    ready: () => trickHats.some(hat => kolmafia.mallPrice(hat) < MAX_HAT_PRICE),
    do: () => {
      var cheapestHat = maxBy(trickHats, kolmafia.mallPrice, true);
      acquire(1, cheapestHat, MAX_HAT_PRICE);
    },
    spendsTurn: false
  };
}
function candyMapDailyTasks() {
  return [useCandyMapTask(), doCandyTreat(), obtainTrickHat()];
}
function doCandyTrick() {
  return {
    name: "Trick",
    completed: () => !get$2("_trickOrTreatBlock").includes("D"),
    ready: () => !kolmafia.holiday().includes("Halloween") && get$2("_mapToACandyRichBlockUsed") && trickHats.some(hat => have$P(hat)),
    do: () => {
      visitBlock();
      var houseNumber = get$2("_trickOrTreatBlock").indexOf("D");
      if (houseNumber < 0) return;
      visitHouse(houseNumber);
    },
    outfit: () => {
      var hat = trickHats.find(hat => have$P(hat));
      if (!hat) {
        throw new Error("We thought we had an appropriate hat for tricking, but we did not.");
      }
      return freeFightOutfit({
        hat
      }, $location`Trick-or-Treating`);
    },
    combat: new GarboStrategy(() => Macro.basicCombat()),
    spendsTurn: false
  };
}

var ghostLocations = new Map([[$location`Cobb's Knob Treasury`, $monster`The ghost of Ebenoozer Screege`], [$location`The Haunted Conservatory`, $monster`The ghost of Lord Montague Spookyraven`], [$location`The Haunted Gallery`, $monster`The ghost of Waldo the Carpathian`], [$location`The Haunted Kitchen`, $monster`The Icewoman`], [$location`The Haunted Wine Cellar`, $monster`The ghost of Jim Unfortunato`], [$location`The Icy Peak`, $monster`The ghost of Sam McGee`], [$location`Inside the Palindome`, $monster`Emily Koops, a spooky lime`], [$location`Madness Bakery`, $monster`the ghost of Monsieur Baguelle`], [$location`The Old Landfill`, $monster`The ghost of Vanillica "Trashblossom" Gorton`], [$location`The Overgrown Lot`, $monster`the ghost of Oily McBindle`], [$location`The Skeleton Store`, $monster`boneless blobghost`], [$location`The Smut Orc Logging Camp`, $monster`The ghost of Richard Cockingham`], [$location`The Spooky Forest`, $monster`The Headless Horseman`]]);
function getGhost() {
  return ghostLocations.get(get$2("ghostLocation") ?? $location.none) ?? null;
}
function ghostAdventure() {
  return {
    location: get$2("ghostLocation") ?? $location.none,
    target: getGhost() ?? $monster.none
  };
}

var DEFAULT_FREE_FIGHT_TASK = {
  // GarboTask
  combat: new GarboStrategy(() => Macro.basicCombat()),
  spendsTurn: false,
  // GarboFreeFightTask
  combatCount: () => 1
};
function freeFightTask(fragment) {
  var outfit = () => freeFightOutfit({}, undelay(fragment.adventure)); // Todo: account for wanderers
  var fullTask = _objectSpread2(_objectSpread2({
    outfit
  }, DEFAULT_FREE_FIGHT_TASK), fragment);
  // Give us some padding
  return _objectSpread2(_objectSpread2({}, fullTask), {}, {
    limit: {
      skip: 5 + fullTask.combatCount()
    }
  });
}
function bestWitchessPiece() {
  return maxBy(pieces$1, monster => sum(kolmafia.itemDropsArray(monster), _ref => {
    var drop = _ref.drop;
    return garboValue(drop);
  }));
}
var locketMonster = () => findMonster(isFreeAndCopyable, valueDrops);
var locketsToSave = () => canReminisce(globalOptions.target) ? 1 : 0;
var maxSealsAvailable = () => kolmafia.retrieveItem(1, $item`Claw of the Infernal Seal`) ? 10 : 5;
function sealsAvailable() {
  var max = maxSealsAvailable();
  var available = kolmafia.guildStoreAvailable() ? Infinity : Math.floor(kolmafia.availableAmount($item`seal-blubber candle`) / 3);
  return Math.min(max, available);
}
var tearawayPantsFreeFightOutfit = adventure => () => freeFightOutfit({
  bonuses: new Map([[$item`tearaway pants`, get$2("valueOfAdventure") * plantsAdventureChance()]])
}, adventure, {
  familiarOptions: {
    canChooseMacro: false,
    allowAttackFamiliars: false
  }
});
function litLeafMacro(monster) {
  var tiedUpItem = new Map([[$monster`flaming leaflet`, $item`tied-up flaming leaflet`], [$monster`flaming monstera`, $item`tied-up flaming monstera`], [$monster`leaviathan`, $item`tied-up leaviathan`]]).get(monster);

  // Only convert lassos if we can funksling as the combat counts as a free loss
  return Macro.externalIf(kolmafia.haveEquipped($item`tearaway pants`), Macro.if_(monster, Macro.trySkill($skill`Tear Away your Pants!`))).externalIf(tiedUpItem !== undefined && kolmafia.itemAmount($item`lit leaf lasso`) >= 2 && have$P($skill`Ambidextrous Funkslinging`) && (garboValue(tiedUpItem) - kolmafia.mallPrice($item`lit leaf lasso`)) * 2 >= globalOptions.prefs.valueOfFreeFight, Macro.if_(monster, Macro.tryItem([$item`lit leaf lasso`, $item`lit leaf lasso`]))).basicCombat();
}
function dmtCommaValuable() {
  if (!have$M()) return false;
  var cost = kolmafia.mallPrice($item`Deep Machine Tunnels snowglobe`) + (currentFamiliar$1() === $familiar`Machine Elf` ? 0 : kolmafia.mallPrice($item`self-dribbling basketball`));
  return globalOptions.prefs.valueOfFreeFight * 5 > cost;
}
var stunDurations = new Map([[$skill`Blood Bubble`, 1], [$skill`Entangling Noodles`, () => kolmafia.myClass() === $class`Pastamancer` && !have$P($skill`Shadow Noodles`) ? 1 : 0], [$skill`Frost Bite`, 1], [$skill`Shadow Noodles`, 2], [$skill`Shell Up`, () => {
  if (kolmafia.myClass() !== $class`Turtle Tamer`) return 0;
  var _iterator = _createForOfIteratorHelper(new Map([[$effect`Glorious Blessing of the Storm Tortoise`, 4], [$effect`Grand Blessing of the Storm Tortoise`, 3], [$effect`Blessing of the Storm Tortoise`, 2]])),
    _step;
  try {
    for (_iterator.s(); !(_step = _iterator.n()).done;) {
      var _step$value = _slicedToArray(_step.value, 2),
        effect = _step$value[0],
        duration = _step$value[1];
      if (have$P(effect)) return duration;
    }
  } catch (err) {
    _iterator.e(err);
  } finally {
    _iterator.f();
  }
  return 0;
}], [$skill`Soul Bubble`, () => kolmafia.mySoulsauce() >= 5 ? 2 : 0], [$skill`Summon Love Gnats`, 1], [$item`Rain-Doh blue balls`, 1]]);
var RAW_FIGHTS = [{
  name: $item`protonic accelerator pack`.name,
  adventure: ghostAdventure,
  ready: () => get$2("ghostLocation") !== null,
  completed: () => get$2("questPAGhost") === "unstarted",
  choices: () => wanderer().getChoices(get$2("ghostLocation") ?? $location.none),
  do: () => get$2("ghostLocation") ?? $location.none,
  combat: new GarboStrategy(() => have$P($item`protonic accelerator pack`) ? Macro.ghostBustin() : Macro.basicCombat()),
  outfit: () => freeFightOutfit({
    back: have$P($item`protonic accelerator pack`) ? $item`protonic accelerator pack` : []
  }, get$2("ghostLocation") ?? $location.none),
  tentacle: true
}, {
  name: $item`molehill mountain`.name,
  adventure: $monster`Moleman`,
  ready: () => have$P($item`molehill mountain`) && (get$2("_thesisDelivered") || !have$P($familiar`Pocket Professor`)),
  completed: () => get$2("_molehillMountainUsed"),
  do: () => kolmafia.use($item`molehill mountain`),
  tentacle: true
}, {
  name: $skill`Aug. 8th: Cat Day!`.name,
  adventure: $monster.none,
  // TODO
  ready: () => shouldAugustCast($skill`Aug. 8th: Cat Day!`),
  completed: () => $skill`Aug. 8th: Cat Day!`.timescast > 0,
  do: () => kolmafia.useSkill($skill`Aug. 8th: Cat Day!`),
  tentacle: true
}, {
  name: $skill`Aug. 22nd: Tooth Fairy Day!`.name,
  adventure: $monster`Tooth Golem`,
  ready: () => shouldAugustCast($skill`Aug. 22nd: Tooth Fairy Day!`),
  completed: () => $skill`Aug. 22nd: Tooth Fairy Day!`.timescast > 0,
  do: () => kolmafia.useSkill($skill`Aug. 22nd: Tooth Fairy Day!`),
  tentacle: true
}, {
  name: "Tunnel of Love",
  adventure: $location`The Tunnel of L.O.V.E.`,
  ready: have$E,
  completed: isUsed$1,
  do: () => fightAll("LOV Epaulettes", "Open Heart Surgery", "LOV Extraterrestrial Chocolate"),
  // TODO: Get drops
  combatCount: () => 3,
  tentacle: false
}, {
  name: "Chateau Mantegna",
  adventure: () => paintingMonster() ?? $monster.none,
  ready: () => {
    var _ChateauMantegna$pain;
    return have$J() && (((_ChateauMantegna$pain = paintingMonster()) === null || _ChateauMantegna$pain === void 0 || (_ChateauMantegna$pain = _ChateauMantegna$pain.attributes) === null || _ChateauMantegna$pain === void 0 ? void 0 : _ChateauMantegna$pain.includes("FREE")) ?? false);
  },
  completed: paintingFought,
  do: () => fightPainting(),
  outfit: () => freeFightOutfit(have$P($familiar`Robortender`) && $phyla`elf, fish, hobo, penguin, constellation`.some(phylum => {
    var _ChateauMantegna$pain2;
    return phylum === ((_ChateauMantegna$pain2 = paintingMonster()) === null || _ChateauMantegna$pain2 === void 0 ? void 0 : _ChateauMantegna$pain2.phylum);
  }) ? {
    familiar: $familiar`Robortender`
  } : {}, paintingMonster() ?? $monster.none),
  tentacle: true
}, {
  name: "Eldritch Tentacle",
  adventure: $monster`Eldritch Tentacle`,
  ready: () => get$2("questL02Larva") !== "unstarted",
  completed: () => get$2("_eldritchTentacleFought") || get$2("_eldritchTentaclesFoughtToday") >= 11,
  do: () => {
    var haveEldritchEssence = kolmafia.itemAmount($item`eldritch essence`) !== 0;
    kolmafia.visitUrl("place.php?whichplace=forestvillage&action=fv_scientist", false);
    if (!kolmafia.handlingChoice()) throw "No choice?";
    kolmafia.runChoice(haveEldritchEssence ? 2 : 1);
  },
  tentacle: false
}, {
  name: $skill`Evoke Eldritch Horror`.name,
  adventure: $monster`Eldritch Tentacle`,
  ready: () => have$P($skill`Evoke Eldritch Horror`),
  completed: () => get$2("_eldritchHorrorEvoked") || get$2("_eldritchTentaclesFoughtToday") >= 11,
  do: () => {
    kolmafia.useSkill($skill`Evoke Eldritch Horror`);
    if (have$P($effect`Beaten Up`)) uneffect($effect`Beaten Up`);
  },
  effects: () => 11 / 200 * garboValue($item`eldritch ichor`) > kolmafia.mallPrice($item`crappy waiter disguise`) ? [$effect`Crappily Disguised as a Waiter`] : [],
  outfit: () => freeFightOutfit({
    bonuses: new Map($items`eldritch hat, eldritch pants, eldritch hammer`.map(item => [item, 11 / 200 * garboValue($item`eldritch effluvium`)]))
  }, $monster`Eldritch Tentacle`, {
    familiarOptions: {
      canChooseMacro: false
    }
  }),
  combat: new GarboStrategy(() => Macro.if_($monster`Sssshhsssblllrrggghsssssggggrrgglsssshhssslblgl`, Macro.externalIf(have$P($effect`Crappily Disguised as a Waiter`), Macro.externalIf(kolmafia.myBuffedstat($stat`Muscle`) > kolmafia.myBuffedstat($stat`Mysticality`) && (kolmafia.currentHitStat() === $stat`Muscle` || kolmafia.itemType(kolmafia.equippedItem($slot`weapon`)) === "knife"), Macro.trySkillRepeat($skill`Lunging Thrust-Smack`), Macro.trySkillRepeat($skill`Saucegeyser`))).attack().repeat()).basicCombat()),
  tentacle: false
}, {
  name: $item`lynyrd snare`.name,
  adventure: $monster`lynyrd`,
  ready: () => kolmafia.mallPrice($item`lynyrd snare`) <= globalOptions.prefs.valueOfFreeFight,
  completed: () => get$2("_lynyrdSnareUses") >= 3,
  do: () => kolmafia.use($item`lynyrd snare`),
  combat: new GarboStrategy(() => Macro.basicCombat()),
  combatCount: () => clamp(3 - get$2("_lynyrdSnareUses"), 0, 3),
  tentacle: false
}, {
  name: "[glitch season reward name]: retrocape edition",
  adventure: $monster`%monster%`,
  ready: () => (globalOptions.prefs.fightGlitch ?? false) && have$P($item`unwrapped knock-off retro superhero cape`) && sum(_toConsumableArray(stunDurations), _ref2 => {
    var _ref3 = _slicedToArray(_ref2, 2),
      thing = _ref3[0],
      duration = _ref3[1];
    return have$P(thing) ? undelay(duration) : 0;
  }) >= 5,
  completed: () => !!get$2("_glitchMonsterFights"),
  do: () => {
    kolmafia.visitUrl("inv_eat.php?pwd&whichitem=10207");
    kolmafia.runCombat();
  },
  combat: new GarboStrategy(() => Macro.trySkill($skill`Curse of Marinara`).trySkill($skill`Shell Up`).trySkill($skill`Shadow Noodles`).trySkill($skill`Entangling Noodles`).trySkill($skill`Summon Love Gnats`).trySkill($skill`Frost Bite`).trySkill($skill`Soul Bubble`).tryItem($item`Rain-Doh blue balls`).skill($skill`Blow a Robo-Kiss`).repeat()),
  outfit: () => freeFightOutfit({
    back: $items`unwrapped knock-off retro superhero cape`,
    modes: {
      retrocape: ["robot", "kiss"]
    },
    avoid: $items`mutant crown, mutant arm, mutant legs, shield of the Skeleton Lord`,
    modifier: kolmafia.numericModifier("Monster Level") >= 50 ? "-7 Monster Level" : "-Monster Level" // Above 50 ML, monsters resist stuns.
  }, $monster`%monster%`, {
    familiarOptions: {
      canChooseMacro: false,
      includeExperienceFamiliars: false // Experience familiars have a high modifier value for fam exp, causing us to not wear -ML
    }
  }),
  prepare: () => {
    kolmafia.restoreHp(kolmafia.myMaxhp());
    if (have$P($skill`Ruthless Efficiency`)) {
      ensureEffect($effect`Ruthlessly Efficient`);
    }
    if (have$P($skill`Mathematical Precision`)) {
      ensureEffect($effect`Mathematically Precise`);
    }
    if (have$P($skill`Blood Bubble`)) ensureEffect($effect`Blood Bubble`);
    kolmafia.retrieveItem($item`[glitch season reward name]`);
    if (kolmafia.numericModifier("Monster Level") >= 50 && (
    // Above 50 ML, monsters resist stuns.
    kolmafia.canadiaAvailable() || kolmafia.gnomadsAvailable() || have$P($item`detuned radio`))) {
      kolmafia.changeMcd(0);
    }
    if (have$P($effect`Ur-Kel's Aria of Annoyance`)) {
      uneffect($effect`Ur-Kel's Aria of Annoyance`);
    }
  },
  tentacle: false
}, {
  name: "[glitch season reward name]",
  ready: () => globalOptions.prefs.fightGlitch ?? false,
  adventure: $monster`%monster%`,
  completed: () => !!get$2("_glitchMonsterFights"),
  do: () => {
    kolmafia.visitUrl("inv_eat.php?pwd&whichitem=10207");
    kolmafia.runCombat();
  },
  combat: new GarboStrategy(() => Macro.trySkill($skill`Curse of Marinara`).trySkill($skill`Conspiratorial Whispers`).trySkill($skill`Shadow Noodles`).externalIf(get$2("glitchItemImplementationCount") * kolmafia.itemAmount($item`[glitch season reward name]`) >= 400, Macro.item([$item`gas can`, $item`gas can`])).externalIf(get$2("lovebugsUnlocked"), Macro.trySkill($skill`Summon Love Gnats`).trySkill($skill`Summon Love Mosquito`)).tryItem($item`train whistle`).trySkill($skill`Micrometeorite`).tryItem($item`Time-Spinner`).tryItem($item`little red book`).tryItem($item`Rain-Doh blue balls`).tryItem($item`Rain-Doh indigo cup`).trySkill($skill`Entangling Noodles`).trySkill($skill`Frost Bite`).kill()),
  outfit: () => freeFightOutfit({
    modifier: ["1000 mainstat"],
    avoid: $items`mutant crown, mutant arm, mutant legs, shield of the Skeleton Lord`
  }, $monster`%monster%`, {
    familiarOptions: {
      canChooseMacro: false
    }
  }),
  prepare: () => {
    kolmafia.restoreHp(kolmafia.myMaxhp());
    if (have$P($skill`Ruthless Efficiency`)) {
      ensureEffect($effect`Ruthlessly Efficient`);
    }
    if (have$P($skill`Mathematical Precision`)) {
      ensureEffect($effect`Mathematically Precise`);
    }
    if (have$P($skill`Blood Bubble`)) ensureEffect($effect`Blood Bubble`);
    kolmafia.retrieveItem($item`[glitch season reward name]`);
    if (get$2("glitchItemImplementationCount") * kolmafia.itemAmount($item`[glitch season reward name]`) >= 400) {
      kolmafia.retrieveItem($item`gas can`, 2);
    }
  },
  tentacle: false
}, {
  name: "Seal Clubbing",
  ready: () => kolmafia.myClass() === $class`Seal Clubber`,
  completed: () => sealsAvailable() <= 0 || get$2("_sealsSummoned") >= maxSealsAvailable(),
  adventure: () => kolmafia.guildStoreAvailable() ? $monster`Spawn of Wally` : $monster`hermetic seal`,
  do: () => {
    var _ref4 = kolmafia.guildStoreAvailable() ? [$item`figurine of a wretched-looking seal`, 1] : [$item`figurine of an ancient seal`, 3],
      _ref5 = _slicedToArray(_ref4, 2),
      figurine = _ref5[0],
      candlesNeeded = _ref5[1];
    kolmafia.retrieveItem(1, figurine);
    kolmafia.retrieveItem(candlesNeeded, $item`seal-blubber candle`);
    kolmafia.use(figurine);
  },
  outfit: () => {
    var clubs = kolmafia.Item.all().filter(i => have$P(i) && kolmafia.canEquip(i) && kolmafia.itemType(i) === "club");
    var club = clubs.find(i => kolmafia.weaponHands(i) === 1) ?? clubs.find(i => kolmafia.weaponHands(i) === 2) ?? $item`seal-clubbing club`;
    kolmafia.retrieveItem(club);
    return freeFightOutfit({
      weapon: club
    }, kolmafia.guildStoreAvailable() ? $monster`Spawn of Wally` : $monster`hermetic seal`);
  },
  combat: new GarboStrategy(() => Macro.startCombat().trySkill($skill`Furious Wallop`).trySkillRepeat($skill`Lunging Thrust-Smack`, $skill`Thrust-Smack`, $skill`Lunge Smack`).attack().repeat()),
  combatCount: sealsAvailable,
  tentacle: false
}, {
  name: "BRICKO",
  adventure: $monster`BRICKO ooze`,
  ready: () => kolmafia.mallPrice($item`BRICKO eye brick`) + 2 * kolmafia.mallPrice($item`BRICKO brick`) <= globalOptions.prefs.valueOfFreeFight,
  completed: () => get$2("_brickoFights") >= 10,
  do: () => kolmafia.use($item`BRICKO ooze`),
  outfit: () => freeFightOutfit({}, $monster`BRICKO ooze`, {
    familiarOptions: {
      canChooseMacro: false
    }
  }),
  combat: new GarboStrategy(() => Macro.basicCombat()),
  combatCount: () => clamp(10 - get$2("_brickoFights"), 0, 10),
  tentacle: false
}, {
  name: "Kramco",
  adventure: {
    target: $monster`sausage goblin`,
    location: "wanderer"
  },
  ready: () => have$P($item`Kramco Sausage-o-Matic™`),
  completed: () => !kramcoGuaranteed(),
  do: () => wanderer().getTarget({
    wanderer: "wanderer",
    allowEquipment: false
  }).location,
  outfit: () => freeFightOutfit({
    offhand: $item`Kramco Sausage-o-Matic™`
  }, {
    target: $monster`sausage goblin`,
    location: "wanderer"
  }),
  choices: () => wanderer().getChoices({
    wanderer: "wanderer",
    allowEquipment: false
  }),
  combat: new GarboStrategy(() => Macro.basicCombat()),
  combatCount: () => clamp(1 - get$2("_sausageFights"), 0, 1),
  tentacle: true
},
// Grimacia
// Saber
// Pygmys
{
  name: "Glark Cable",
  adventure: $location`The Red Zeppelin`,
  ready: () => kolmafia.canAdventure($location`The Red Zeppelin`) && get$2("questL11Ron") === "finished" && kolmafia.myInebriety() <= kolmafia.inebrietyLimit() && kolmafia.mallPrice($item`glark cable`) <= globalOptions.prefs.valueOfFreeFight,
  completed: () => get$2("_glarkCableUses") >= 5,
  do: $location`The Red Zeppelin`,
  outfit: () => freeFightOutfit({}, $location`The Red Zeppelin`, {
    familiarOptions: {
      canChooseMacro: false,
      allowAttackFamiliars: false
    }
  }),
  acquire: [{
    item: $item`glark cable`
  }],
  combatCount: () => clamp(5 - get$2("_glarkCableUses"), 0, 5),
  combat: new GarboStrategy(() => Macro.if_($monster`Eldritch Tentacle`, Macro.basicCombat()).item($item`glark cable`).abort()),
  tentacle: true
}, {
  name: "Mushroom garden",
  adventure: $location`Your Mushroom Garden`,
  ready: () => have$P($item`packet of mushroom spores`) || kolmafia.getCampground()["packet of mushroom spores"] !== undefined,
  completed: () => get$2("_mushroomGardenFights") > 0,
  prepare: () => {
    if (have$P($item`packet of mushroom spores`)) {
      kolmafia.use($item`packet of mushroom spores`);
    }
    if (have$I()) {
      educate([$skill`Extract`, $skill`Portscan`]);
    }
  },
  do: () => $location`Your Mushroom Garden`,
  post: () => {
    if (have$P($item`packet of tall grass seeds`)) {
      kolmafia.use($item`packet of tall grass seeds`);
    }
  },
  outfit: tearawayPantsFreeFightOutfit($location`Your Mushroom Garden`),
  combat: new GarboStrategy(() => Macro.externalIf(!doingGregFight(), Macro.if_($skill`Macrometeorite`, Macro.trySkill($skill`Portscan`))).externalIf(kolmafia.haveEquipped($item`tearaway pants`), Macro.trySkill($skill`Tear Away your Pants!`)).basicCombat()),
  combatCount: () => clamp(1 - get$2("_mushroomGardenFights"), 0, 1),
  tentacle: true
}, {
  name: "Portscan + Macrometeorite + Mushroom garden",
  adventure: $location`Your Mushroom Garden`,
  ready: () => (have$P($item`packet of mushroom spores`) || kolmafia.getCampground()["packet of mushroom spores"] !== undefined) && !doingGregFight() && have$P($skill`Macrometeorite`) && get$2("_macrometeoriteUses") < 10,
  completed: () => !exists("portscan.edu"),
  prepare: () => {
    if (have$P($item`packet of mushroom spores`)) {
      kolmafia.use($item`packet of mushroom spores`);
    }
    if (have$I()) {
      educate([$skill`Extract`, $skill`Portscan`]);
    }
  },
  do: $location`Your Mushroom Garden`,
  post: () => {
    if (have$P($item`packet of tall grass seeds`)) {
      kolmafia.use($item`packet of tall grass seeds`);
    }
  },
  outfit: tearawayPantsFreeFightOutfit($location`Your Mushroom Garden`),
  combat: new GarboStrategy(() => Macro.if_($monster`Government agent`, Macro.skill($skill`Macrometeorite`)).if_($monster`piranha plant`, Macro.if_($skill`Macrometeorite`, Macro.trySkill($skill`Portscan`))).externalIf(kolmafia.haveEquipped($item`tearaway pants`), Macro.trySkill($skill`Tear Away your Pants!`)).basicCombat()),
  combatCount: () => clamp(10 - get$2("_macrometeoriteUses"), 0, 10),
  tentacle: true
}, {
  name: "God Lobster",
  adventure: $monster`God Lobster`,
  ready: () => have$P($familiar`God Lobster`),
  completed: () => get$2("_godLobsterFights") >= 3,
  do: () => {
    kolmafia.restoreHp(kolmafia.myMaxhp());
    kolmafia.visitUrl("main.php?fightgodlobster=1");
    kolmafia.runCombat();
    kolmafia.visitUrl("choice.php");
    if (kolmafia.handlingChoice()) kolmafia.runChoice(-1);
  },
  choices: () => ({
    1310: !have$P($item`God Lobster's Crown`) ? 1 : 2
  }),
  // god lob equipment, then stats
  outfit: () => freeFightOutfit({
    familiar: $familiar`God Lobster`,
    bonuses: new Map([[$item`God Lobster's Scepter`, 1000], [$item`God Lobster's Ring`, 2000], [$item`God Lobster's Rod`, 3000], [$item`God Lobster's Robe`, 4000], [$item`God Lobster's Crown`, 5000]])
  }, $monster`God Lobster`),
  combatCount: () => clamp(3 - get$2("_godLobsterFights"), 0, 3),
  tentacle: false
}, {
  name: "Machine Elf",
  adventure: $location`The Deep Machine Tunnels`,
  ready: () => have$P($familiar`Machine Elf`) || dmtCommaValuable(),
  completed: () => get$2("_machineTunnelsAdv") >= 5,
  do: $location`The Deep Machine Tunnels`,
  prepare: () => {
    if (kolmafia.myFamiliar() === $familiar`Comma Chameleon`) {
      if (currentFamiliar$1() !== $familiar`Machine Elf`) {
        acquire(1, $item`self-dribbling basketball`, 10000);
        transform($familiar`Machine Elf`);
      }
      if (!kolmafia.canAdventure($location`The Deep Machine Tunnels`)) {
        acquire(1, $item`Deep Machine Tunnels snowglobe`, 2000);
        kolmafia.use($item`Deep Machine Tunnels snowglobe`);
      }
    }
    // We need an else here because if we're using Comma we don't get to convert items.
    if (garboValue($item`abstraction: certainty`) >= garboValue($item`abstraction: thought`)) {
      acquire(1, $item`abstraction: thought`, garboValue($item`abstraction: certainty`), false);
    }
    if (garboValue($item`abstraction: joy`) >= garboValue($item`abstraction: action`)) {
      acquire(1, $item`abstraction: action`, garboValue($item`abstraction: joy`), false);
    }
    if (garboValue($item`abstraction: motion`) >= garboValue($item`abstraction: sensation`)) {
      acquire(1, $item`abstraction: sensation`, garboValue($item`abstraction: motion`), false);
    }
  },
  choices: () => ({
    1119: 6
  }),
  // escape DMT
  combat: new GarboStrategy(() => Macro.externalIf(garboValue($item`abstraction: certainty`) >= garboValue($item`abstraction: thought`), Macro.if_($monster`Perceiver of Sensations`, Macro.tryItem($item`abstraction: thought`))).externalIf(garboValue($item`abstraction: joy`) >= garboValue($item`abstraction: action`), Macro.if_($monster`Thinker of Thoughts`, Macro.tryItem($item`abstraction: action`))).externalIf(garboValue($item`abstraction: motion`) >= garboValue($item`abstraction: sensation`), Macro.if_($monster`Performer of Actions`, Macro.tryItem($item`abstraction: sensation`))).basicCombat()),
  outfit: () => freeFightOutfit({
    familiar: have$P($familiar`Machine Elf`) ? $familiar`Machine Elf` : $familiar`Comma Chameleon`
  }, $location`The Deep Machine Tunnels`),
  tentacle: false,
  // Marked like this as 2 DMT fights get overriden by tentacles (could add +1 combat)
  combatCount: () => clamp(5 - get$2("_machineTunnelsAdv"), 0, 5)
}, {
  name: "Witchess",
  adventure: bestWitchessPiece,
  ready: () => have$H(),
  completed: () => fightsDone() >= 5 || pieces$1.includes(globalOptions.target),
  do: () => fightPiece(bestWitchessPiece()),
  tentacle: true,
  combatCount: () => clamp(5 - fightsDone(), 0, 5)
}, {
  name: "The X-32-F Combat Training Snowman",
  adventure: $location`The X-32-F Combat Training Snowman`,
  ready: () => get$2("snojoAvailable"),
  completed: () => get$2("_snojoFreeFights") >= 10,
  do: $location`The X-32-F Combat Training Snowman`,
  tentacle: false,
  combatCount: () => clamp(10 - get$2("_snojoFreeFights"), 0, 10)
},
// Neverending party
{
  name: "An Unusually Quiet Barroom Brawl",
  adventure: $location`An Unusually Quiet Barroom Brawl`,
  ready: () => get$2("ownsSpeakeasy"),
  completed: () => get$2("_speakeasyFreeFights") >= 3,
  do: $location`An Unusually Quiet Barroom Brawl`,
  tentacle: true,
  combatCount: () => clamp(3 - get$2("_speakeasyFreeFights"), 0, 3)
},
// killRobortCreaturesForFree
{
  name: $item`combat lover's locket`.name,
  adventure: () => locketMonster() ?? $monster.none,
  ready: () => have$t() && locketMonster() !== null,
  completed: () => reminiscesLeft() <= locketsToSave(),
  do: () => {
    var monster = locketMonster();
    if (!monster) return;
    reminisce(monster);
  },
  outfit: () => freeFightOutfit(have$P($familiar`Robortender`) ? {
    familiar: $familiar`Robortender`
  } : {}, locketMonster() ?? $monster.none),
  tentacle: true,
  combatCount: () => clamp(3 - reminiscesLeft() - locketsToSave(), 0, 3)
}, _objectSpread2(_objectSpread2({}, doCandyTrick()), {}, {
  adventure: $location`Trick-or-Treating`,
  combatCount: () => 5,
  tentacle: true
}),
// leaf burning fights
{
  name: "Burning Leaves Flaming Leaflet Fight",
  adventure: $monster`flaming leaflet`,
  ready: () => have$o() && numberOfLeaves() >= (burnFor.get($monster`flaming leaflet`) ?? Infinity),
  completed: () => get$2("_leafMonstersFought") >= 5,
  do: () => {
    var lassoCount = kolmafia.itemAmount($item`lit leaf lasso`);
    var result = burnSpecialLeaves($monster`flaming leaflet`);
    if (lassoCount > kolmafia.itemAmount($item`lit leaf lasso`)) {
      _set("_lastCombatLost", "false");
    }
    return result;
  },
  tentacle: true,
  combatCount: () => clamp(5 - get$2("_leafMonstersFought"), 0, 5),
  outfit: tearawayPantsFreeFightOutfit($monster`flaming leaflet`),
  combat: new GarboStrategy(() => litLeafMacro($monster`flaming leaflet`))
}, {
  name: $item`tied-up flaming leaflet`.name,
  adventure: $monster`flaming leaflet`,
  ready: () => kolmafia.mallPrice($item`tied-up flaming leaflet`) <= globalOptions.prefs.valueOfFreeFight,
  completed: () => get$2("_tiedUpFlamingLeafletFought"),
  acquire: () => [{
    item: $item`tied-up flaming leaflet`
  }],
  do: () => {
    var lassoCount = kolmafia.itemAmount($item`lit leaf lasso`);
    var result = kolmafia.use($item`tied-up flaming leaflet`);
    if (lassoCount > kolmafia.itemAmount($item`lit leaf lasso`)) {
      _set("_lastCombatLost", "false");
    }
    return result;
  },
  tentacle: true,
  combatCount: () => get$2("_tiedUpFlamingLeafletFought") ? 0 : 1,
  outfit: tearawayPantsFreeFightOutfit($monster`flaming leaflet`),
  combat: new GarboStrategy(() => litLeafMacro($monster`flaming leaflet`))
}, {
  name: $item`tied-up flaming monstera`.name,
  adventure: $monster`flaming monstera`,
  ready: () => kolmafia.mallPrice($item`tied-up flaming monstera`) <= globalOptions.prefs.valueOfFreeFight,
  completed: () => get$2("_tiedUpFlamingMonsteraFought"),
  acquire: () => [{
    item: $item`tied-up flaming monstera`
  }],
  do: () => {
    var lassoCount = kolmafia.itemAmount($item`lit leaf lasso`);
    var result = kolmafia.use($item`tied-up flaming monstera`);
    if (lassoCount > kolmafia.itemAmount($item`lit leaf lasso`)) {
      _set("_lastCombatLost", "false");
    }
    return result;
  },
  tentacle: true,
  combatCount: () => get$2("_tiedUpFlamingMonsteraFought") ? 0 : 1,
  outfit: tearawayPantsFreeFightOutfit($monster`flaming monstera`),
  combat: new GarboStrategy(() => litLeafMacro($monster`flaming monstera`))
},
// tied-up leaviathan (scaling, has 100 damage source cap and 2500 hp)
// li'l ninja costume
// closed-circuit pay phone (make into it's own Quest)
{
  name: "CyberRealm Overclock Fights",
  adventure: $location`Cyberzone 1`,
  ready: () => kolmafia.canAdventure($location`Cyberzone 1`) && have$P($item`zero-trust tanktop`) && have$P($skill`Torso Awareness`) && have$P($skill`OVERCLOCK(10)`),
  completed: () => get$2("_cyberFreeFights") >= 10,
  do: $location`Cyberzone 1`,
  // TODO Support other zones with better equipment and valuing hacker drops
  tentacle: false,
  choices: {
    1545: 1
  },
  // Take damage, get 0's
  outfit: () => freeFightOutfit({
    bonuses: new Map([[$item`familiar-in-the-middle wrapper`, garboValue($item`1`)], [$item`retro floppy disk`, garboValue($item`1`)], [$item`visual packet sniffer`, garboValue($item`1`) / 4] // unspaded droprate
    ]),
    shirt: $item`zero-trust tanktop`
  }, $location`Cyberzone 1`),
  combat: new GarboStrategy(() => Macro.if_($monsters`firewall, ICE barrier, corruption quarantine, parental controls, null container, zombie process, botfly, network worm, ICE man, rat (remote access trojan)`, Macro.trySkillRepeat($skill`Throw Cyber Rock`)).basicCombat()),
  combatCount: () => clamp(10 - get$2("_cyberFreeFights"), 0, 10)
}];
var FreeFightTasks = RAW_FIGHTS.map(freeFightTask);

// Possible additional free fights from tentacles
function possibleFreeFightQuestTentacleFights() {
  var availableFights = FreeFightTasks.filter(task => {
    var _task$ready2;
    return (((_task$ready2 = task.ready) === null || _task$ready2 === void 0 ? void 0 : _task$ready2.call(task)) ?? true) && !task.completed();
  });
  return sum(availableFights, _ref7 => {
    var combatCount = _ref7.combatCount,
      tentacle = _ref7.tentacle;
    return combatCount() * (tentacle ? 1 : 0);
  });
}
var FreeFightQuest = {
  name: "Free Fight",
  tasks: FreeFightTasks,
  ready: () => sober() && !have$P($effect`Feeling Lost`)
};

function sandwormFamiliar() {
  if (have$P($familiar`Trick-or-Treating Tot`) && have$P($item`li'l ninja costume`) && !have$P($item`toy Cupid bow`)) {
    return $familiar`Trick-or-Treating Tot`;
  }
  var viableFairies = kolmafia.Familiar.all().filter(f => have$P(f) && findFairyMultiplier(f) && f !== $familiar`Steam-Powered Cheerleader` && !f.physicalDamage && !f.elementalDamage);
  var highestFairyMult = findFairyMultiplier(maxBy(viableFairies, f => f === $familiar`Jill-of-All-Trades` && have$P($item`toy Cupid bow`) ? 1 // Ignore LED candle if we have TCB
  : findFairyMultiplier(f)));
  var goodFairies = viableFairies.filter(f => findFairyMultiplier(f) === highestFairyMult);
  if (have$P($familiar`Reagnimated Gnome`) && !have$P($item`gnomish housemaid's kgnee`) && !get$2("_garbo_triedForKgnee", false)) {
    var current = kolmafia.myFamiliar();
    kolmafia.useFamiliar($familiar`Reagnimated Gnome`);
    kolmafia.visitUrl("arena.php");
    kolmafia.runChoice(4);
    kolmafia.useFamiliar(current);
    _set("_garbo_triedForKgnee", true);
  }
  if (have$P($item`gnomish housemaid's kgnee`) && highestFairyMult === 1 && !have$P($item`toy Cupid bow`)) {
    goodFairies.push($familiar`Reagnimated Gnome`);
  }
  var bonuses = [].concat(_toConsumableArray(menu$1($monster`giant sandworm`, {
    canChooseMacro: false
  })), [{
    familiar: $familiar`Reagnimated Gnome`,
    expectedValue: get$2("valueOfAdventure") * 70 / 1000,
    leprechaunMultiplier: 0,
    limit: "none"
  }]);
  var tcbFamiliars = getUsedTcbFamiliars();
  var bestNonCheerleaderFairy = maxBy(goodFairies, f => {
    var _bonuses$find;
    return ((_bonuses$find = bonuses.find(_ref => {
      var familiar = _ref.familiar;
      return familiar === f;
    })) === null || _bonuses$find === void 0 ? void 0 : _bonuses$find.expectedValue) ?? tcbValue(f, tcbFamiliars);
  });
  if (have$P($familiar`Steam-Powered Cheerleader`) && findFairyMultiplier($familiar`Steam-Powered Cheerleader`) > findFairyMultiplier(bestNonCheerleaderFairy)) {
    return $familiar`Steam-Powered Cheerleader`;
  }
  return bestNonCheerleaderFairy;
}

function sandwormSpec() {
  var spec = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : {};
  var outfit = Outfit.from(spec, new Error(`Failed to construct outfit from spec ${JSON.stringify(spec)}`));
  var itemDropBonus = 0.1 / 10000 * garboValue($item`spice melange`);
  outfit.modifier.push(`${itemDropBonus.toFixed(2)} Item Drop 10000 max`);
  if (have$P($item`January's Garbage Tote`) && have$P($item`broken champagne bottle`) && get$2("garbageChampagneCharge") > 0) {
    outfit.equip($item`broken champagne bottle`);
  }
  if (have$P($item`Lil' Doctor™ bag`) && get$2("_otoscopeUsed") < 3) {
    outfit.equip($item`Lil' Doctor™ bag`);
  }
  outfit.equip(sandwormFamiliar());
  if (outfit.familiar === $familiar`Reagnimated Gnome`) {
    outfit.addBonus($item`gnomish housemaid's kgnee`, kolmafia.familiarWeight($familiar`Reagnimated Gnome`) * get$2("valueOfAdventure") / 1000);
  }
  if (outfit.familiar === $familiar`Jill-of-All-Trades`) {
    outfit.setModes({
      jillcandle: "disco"
    });
  }
  outfit.equip($item`toy Cupid bow`);
  return outfit.spec();
}
function sandwormOutfit() {
  var spec = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : {};
  return freeFightOutfit(sandwormSpec(spec), kolmafia.Location.none);
}
var DEFAULT_SANDWORM_TASK = {
  combat: new GarboStrategy(() => Macro.abort()),
  prepare: () => {
    if (!have$P($item`drum machine`, expectedFreeGiantSandwormQuestFights())) {
      acquire(expectedFreeGiantSandwormQuestFights(), $item`drum machine`, sandwormValue() - 1, false);
    }
  },
  effects: () => [].concat(_toConsumableArray(have$P($skill`Emotionally Chipped`) && get$2("_feelLostUsed") < 3 ? $effects`Feeling Lost` : []), _toConsumableArray(have$P($skill`Steely-Eyed Squint`) && !get$2("_steelyEyedSquintUsed") ? $effects`Steely-Eyed Squint` : [])),
  do: () => kolmafia.use($item`drum machine`),
  outfit: () => sandwormOutfit(),
  spendsTurn: false,
  tentacle: true
};
function sandwormTask(fragment) {
  var fullTask = _objectSpread2(_objectSpread2({}, DEFAULT_SANDWORM_TASK), fragment);
  return _objectSpread2(_objectSpread2({}, fullTask), {}, {
    limit: {
      skip: 20
    }
  });
}
var NON_SANDWORM_TASK = {
  combat: new GarboStrategy(() => Macro.abort()),
  outfit: () => new Outfit(),
  spendsTurn: false,
  combatCount: () => 0,
  tentacle: false
};
function nonSandwormTask(fragment) {
  var fullTask = _objectSpread2(_objectSpread2({}, NON_SANDWORM_TASK), fragment);
  return _objectSpread2(_objectSpread2({}, fullTask), {}, {
    limit: {
      skip: 1
    }
  });
}
var sandwormMacro = () => Macro.trySingAlong().tryHaveSkill($skill`Otoscope`);
var SANDWORM_TASK_DEFINITIONS = [{
  name: $skill`Chest X-Ray`.name,
  ready: () => drumMachineWorthIt() && have$P($item`Lil' Doctor™ bag`),
  completed: () => get$2("_chestXRayUsed") >= 3,
  combat: new GarboStrategy(() => sandwormMacro().trySkill($skill`Chest X-Ray`)),
  outfit: () => sandwormOutfit({
    equip: $items`Lil' Doctor™ bag`
  }),
  combatCount: () => clamp(3 - get$2("_chestXRayUsed"), 0, 3)
}, {
  name: $item`Apriling band quad tom`.name,
  ready: () => have$P($item`Apriling band quad tom`),
  completed: () => $item`Apriling band quad tom`.dailyusesleft === 0,
  do: () => {
    play("Apriling band quad tom");
    kolmafia.visitUrl("main.php");
    return kolmafia.runCombat();
  },
  combat: new GarboStrategy(() => sandwormMacro().basicCombat()),
  combatCount: () => $item`Apriling band quad tom`.dailyusesleft
}, {
  name: $skill`Asdon Martin: Missile Launcher`.name,
  ready: () => drumMachineWorthIt() && kolmafia.getWorkshed() === $item`Asdon Martin keyfob (on ring)`,
  completed: () => get$2("_missileLauncherUsed"),
  prepare: () => fillTo(100),
  combat: new GarboStrategy(() => sandwormMacro().trySkill($skill`Asdon Martin: Missile Launcher`)),
  combatCount: () => !get$2("_missileLauncherUsed") ? 1 : 0
}, {
  name: $skill`Gingerbread Mob Hit`.name,
  ready: () => drumMachineWorthIt() && have$P($skill`Gingerbread Mob Hit`),
  completed: () => get$2("_gingerbreadMobHitUsed"),
  combat: new GarboStrategy(() => sandwormMacro().trySkill($skill`Gingerbread Mob Hit`)),
  combatCount: () => !get$2("_gingerbreadMobHitUsed") ? 1 : 0
}, {
  name: $skill`Shattering Punch`.name,
  ready: () => drumMachineWorthIt() && have$P($skill`Shattering Punch`),
  completed: () => get$2("_shatteringPunchUsed") >= 3,
  combat: new GarboStrategy(() => sandwormMacro().trySkill($skill`Shattering Punch`)),
  combatCount: () => clamp(3 - get$2("_shatteringPunchUsed"), 0, 3)
}, {
  name: $item`replica bat-oomerang`.name,
  ready: () => drumMachineWorthIt() && have$P($item`replica bat-oomerang`),
  completed: () => get$2("_usedReplicaBatoomerang") >= 3,
  combat: new GarboStrategy(() => sandwormMacro().tryItem($item`replica bat-oomerang`)),
  combatCount: () => clamp(3 - get$2("_usedReplicaBatoomerang"), 0, 3)
}, {
  name: $skill`Shocking Lick`.name,
  ready: () => drumMachineWorthIt() && globalOptions.ascend && get$2("shockingLickCharges") > 0,
  completed: () => get$2("shockingLickCharges") === 0,
  combat: new GarboStrategy(() => sandwormMacro().trySkill($skill`Shocking Lick`)),
  combatCount: () => get$2("shockingLickCharges")
}, {
  name: $skill`Lightning Strike`.name,
  ready: () => drumMachineWorthIt() && have$P($skill`Lightning Strike`) && kolmafia.myLightning() >= 20,
  completed: () => kolmafia.myLightning() < 20,
  combat: new GarboStrategy(() => sandwormMacro().trySkill($skill`Lightning Strike`)),
  combatCount: () => kolmafia.floor(kolmafia.myLightning() / 20)
}, {
  name: $skill`Free-For-All`.name,
  ready: () => drumMachineWorthIt() && have$P($skill`Free-For-All`),
  completed: () => have$P($effect`Everything Looks Red`),
  combat: new GarboStrategy(() => sandwormMacro().trySkill($skill`Free-For-All`)),
  combatCount: () => !have$P($effect`Everything Looks Red`) ? 1 : 0
}, {
  name: $item`shadow brick`.name,
  ready: () => drumMachineROI() > kolmafia.mallPrice($item`shadow brick`),
  completed: () => get$2("_shadowBricksUsed") >= 13,
  combat: new GarboStrategy(() => sandwormMacro().tryItem($item`shadow brick`)),
  combatCount: () => drumMachineROI() > kolmafia.mallPrice($item`shadow brick`) ? clamp(13 - get$2("_shadowBricksUsed"), 0, 13) : 0,
  acquire: () => [{
    item: $item`shadow brick`,
    price: drumMachineROI()
  }]
}, {
  name: $skill`BCZ: Sweat Bullets`.name,
  ready: () => have$P($item`blood cubic zirconia`) && safeSweatBulletCasts(drumMachineROI()) >= 0,
  completed: () => safeSweatBulletCasts(drumMachineROI()) <= 0,
  combat: new GarboStrategy(() => sandwormMacro().trySkill($skill`BCZ: Sweat Bullets`)),
  outfit: () => sandwormOutfit({
    equip: $items`blood cubic zirconia`
  }),
  combatCount: () => safeSweatBulletCasts(drumMachineROI())
}, {
  name: "Yellow Ray",
  ready: () => drumMachineWorthIt() && (have$P($skill`Fondeluge`) || have$P($item`Jurassic Parka`)),
  completed: () => have$P($effect`Everything Looks Yellow`),
  combat: new GarboStrategy(() => sandwormMacro().tryHaveSkill($skill`Fondeluge`).trySkill($skill`Spit jurassic acid`)),
  outfit: () => sandwormOutfit(have$P($skill`Fondeluge`) ? {} : have$P($item`Jurassic Parka`) ? {
    shirt: $items`Jurassic Parka`,
    modes: {
      parka: "dilophosaur"
    }
  } : {}),
  combatCount: () => !have$P($effect`Everything Looks Yellow`) ? 1 : 0
}];
var SandwormTasks = [].concat(_toConsumableArray([{
  name: "Ensure Beach Access",
  ready: () => get$2("lastDesertUnlock") !== kolmafia.myAscensions(),
  completed: () => have$P($item`bitchin' meatcar`) || have$P($item`Desert Bus pass`) || kolmafia.myPath() === $path`Actually Ed the Undying`,
  do: () => kolmafia.create($item`bitchin' meatcar`)
}, {
  name: "Fold broken champagne bottle",
  ready: () => have$P($item`January's Garbage Tote`) && !have$P($item`broken champagne bottle`) && get$2("garbageChampagneCharge") > 0,
  completed: () => have$P($item`broken champagne bottle`),
  do: () => {
    _set("_garbageItemChanged", true);
    return kolmafia.cliExecute("fold broken champagne bottle");
  }
}].map(nonSandwormTask)), _toConsumableArray(SANDWORM_TASK_DEFINITIONS.map(sandwormTask)), _toConsumableArray([{
  name: "Fold wad of used tape",
  ready: () => have$P($item`January's Garbage Tote`),
  completed: () => have$P($item`wad of used tape`),
  do: () => {
    _set("_garbageItemChanged", true);
    return kolmafia.cliExecute("fold wad of used tape");
  }
}, {
  name: "Uneffect Feeling Lost",
  ready: () => have$P($effect`Feeling Lost`),
  completed: () => !have$P($effect`Feeling Lost`),
  do: () => uneffect($effect`Feeling Lost`)
}].map(nonSandwormTask)));
function expectedFreeGiantSandwormQuestFights() {
  return sum(SANDWORM_TASK_DEFINITIONS.filter(t => {
    var _t$ready;
    return (((_t$ready = t.ready) === null || _t$ready === void 0 ? void 0 : _t$ready.call(t)) ?? true) && !t.completed();
  }), t => t.combatCount());
}
var _hasWorms;
function hasWorms() {
  return _hasWorms ?? (_hasWorms = expectedFreeGiantSandwormQuestFights() > 0);
}
var REJECTION = 1 / 10;
var BASE_RATE = 1 / 100;
var _sandwormValue;
function sandwormValue() {
  if (_sandwormValue) return _sandwormValue;
  sandwormOutfit().dress();
  var squint = have$P($skill`Steely-Eyed Squint`) && !have$P($effect`Steely-Eyed Squint`) ? 2 : 1;
  var dropRate = clamp(BASE_RATE * (1 + get$1("Item Drop") * squint / 100), 0, 1);
  var rate = REJECTION * (1 - (1 - dropRate) ** (kolmafia.haveEquipped($item`toy Cupid bow`) ? 2 : 1));
  return _sandwormValue = rate * garboValue($item`spice melange`) + globalOptions.prefs.valueOfFreeFight;
}
function drumMachineROI() {
  return sandwormValue() - kolmafia.mallPrice($item`drum machine`);
}
function drumMachineWorthIt() {
  return drumMachineROI() > 0;
}
var FreeGiantSandwormQuest = {
  name: "Free Giant Sandworm",
  tasks: SandwormTasks,
  ready: () => sober() && hasWorms()
};

function getRun() {
  return tryFindFreeRunOrBanish(freeRunConstraints()) ?? ltbRun();
}
var BuffExtensionTasks = [{
  name: "Platinum Yendorian Express Card",
  completed: () => get$2("expressCardUsed"),
  do: () => {
    withStash($items`Platinum Yendorian Express Card`, () => {
      if (have$P($item`Platinum Yendorian Express Card`)) {
        burnLibrams();
        kolmafia.use($item`Platinum Yendorian Express Card`);
      }
    });
  },
  spendsTurn: false,
  limit: {
    skip: 1
  }
}, {
  name: "Bag o' Tricks",
  completed: () => get$2("_bagOTricksUsed"),
  do: () => {
    withStash($items`Bag o' Tricks`, () => {
      if (have$P($item`Bag o' Tricks`)) {
        kolmafia.use($item`Bag o' Tricks`);
      }
    });
  },
  spendsTurn: false,
  limit: {
    skip: 1
  }
}, {
  name: "License to Chill",
  ready: () => have$P($item`License to Chill`),
  completed: () => get$2("_licenseToChillUsed"),
  do: () => {
    burnLibrams();
    kolmafia.use($item`License to Chill`);
  },
  spendsTurn: false,
  limit: {
    skip: 1
  }
}, {
  name: "Such Great Heights",
  ready: () => globalOptions.ascend && sober() && !have$P($effect`Lucky!`) && questStep$1("questM16Temple") > 0 && kolmafia.mallPrice($item`stone wool`) < 3 * get$2("valueOfAdventure") + effectExtenderValue(3, 10) && !!getRun(),
  completed: () => get$2("lastTempleAdventures") >= kolmafia.myAscensions(),
  prepare: () => {
    var _getRun$constraints$p, _getRun$constraints;
    (_getRun$constraints$p = (_getRun$constraints = getRun().constraints).preparation) === null || _getRun$constraints$p === void 0 || _getRun$constraints$p.call(_getRun$constraints);
    ensureEffect($effect`Stone-Faced`);
  },
  do: $location`The Hidden Temple`,
  combat: new GarboStrategy(() => Macro.step(getRun().macro).abortWithMsg("Failed to freerun while attempting to reach Such Great Heights for buff extension")),
  outfit: () => freeFightOutfit(toSpec(getRun()), $location`The Hidden Temple`),
  choices: {
    582: 1,
    579: 3
  },
  spendsTurn: false,
  limit: {
    tries: 5
  } // Stone-faced should immediately get us the Fitting In adventure. If we try this many times, something has gone wrong
}];
var PostBuffExtensionTasks = [{
  name: "Free 70s Mining",
  ready: () => realmAvailable("hot"),
  completed: () => countFreeMines() <= 0,
  do: () => kolmafia.cliExecute("oreo 0"),
  spendsTurn: false
}
// TODO Add Shadow Rift here if we ever grimoirize it
];
var BuffExtensionQuest = {
  name: "Buff Extension",
  tasks: BuffExtensionTasks
};
var PostBuffExtensionQuest = {
  name: "Post Buff Extension",
  tasks: PostBuffExtensionTasks
};

function queryEggNetIncomplete() {
  try {
    var status = JSON.parse(kolmafia.visitUrl("https://eggnet.loathers.net/status"));
    var lastUpdate = new Date(status.lastUpdate);
    var daysSince = (Date.now() - lastUpdate.getTime()) / (24 * 60 * 60 * 1000);
    var max = daysSince < 0.5 ? 100 : 100 - 10 * daysSince;
    return new Map(Object.entries(status.eggs).filter(entry => entry[1] > 0 && entry[1] < max).map(_ref => {
      var _ref2 = _slicedToArray(_ref, 2),
        id = _ref2[0],
        count = _ref2[1];
      return [kolmafia.Monster.get(id), count];
    }));
  } catch (_unused) {
    return new Map();
  }
}
function queryEggNetPriority() {
  try {
    var monsters = JSON.parse(kolmafia.visitUrl("https://eggnet.loathers.net/monsters"));
    return new Map(monsters.filter(entry => entry.eggs < 100 && entry.priority > 0).map(entry => [kolmafia.Monster.get(entry.id), entry.priority]));
  } catch (_unused2) {
    return new Map();
  }
}
function findDonateMonster(onlyFree) {
  var incomplete = queryEggNetIncomplete();
  var priority = queryEggNetPriority();
  if (incomplete.size === 0) return undefined;
  var maxMonsterId = $monster`beef bodyguard bat`.id; // Last Update Feb 24 2026
  var banned = new Set([].concat(_toConsumableArray($monsters.all().filter(x => x.attributes.includes("BOSS") || x.attributes.includes("NOCOPY") || onlyFree && !x.attributes.includes("FREE"))), _toConsumableArray($monsters`quadfaerie, cursed villager, plywood cultists, barrow wraith?, Source Agent`)));
  // Find the monster that needs the most eggs, adding in a small amount of variance as a tiebreaker
  var monster = findMonster(m => m.id <= maxMonsterId && incomplete.has(m) && !banned.has(m), m => 100 - (incomplete.get(m ?? kolmafia.Monster.none) ?? 0) + (priority.get(m) ?? 0) * 1000 + Math.sin((kolmafia.toInt(kolmafia.myId()) << 5) + kolmafia.myDaycount() + m.id));
  var count = incomplete.get(monster ?? kolmafia.Monster.none) ?? 0;
  return !!monster && monster !== kolmafia.Monster.none && count > 0 ? {
    monster,
    count
  } : undefined;
}
function mimicEscape() {
  var constraints = _objectSpread2(_objectSpread2({}, freeRunConstraints()), {}, {
    noFamiliar: () => true,
    maximumCost: () => globalOptions.prefs.valueOfAdventure ?? globalOptions.prefs.valueOfFreeFight
  });
  return tryFindFreeRunOrBanish(constraints) ?? undefined;
}
function shouldDelevel(monster) {
  return monster.attributes.includes("Scale:") || kolmafia.myBuffedstat($stat`Moxie`) < monster.baseAttack + 10 || have$P($skill`Hero of the Half-Shell`) && kolmafia.itemType(kolmafia.equippedItem($slot`offhand`)) === "shield" && kolmafia.myBuffedstat($stat`Muscle`) < monster.baseAttack + 10;
}
function monsterRequirements(monster) {
  var maximize = ["-100 Thorns", "-100 Sporadic Thorns", "-100 Damage Aura", "-100 Sporadic Damage Aura"];
  var options = {
    preventEquip: $items`carnivorous potted plant, Kramco Sausage-o-Matic™`
  };
  switch (monster) {
    default:
      maximize.push("100 Avoid Attack");
      options.bonusEquip = new Map([[$item`unwrapped knock-off retro superhero cape`, 300], [$item`navel ring of navel gazing`, 50], [$item`ancient stone head`, 33], [$item`asteroid belt`, 25], [$item`attorney's badge`, 20], [$item`propeller beanie`, 10], [$item`Mayflower bouquet`, 6.5]]);
      break;
  }
  return new Requirement(maximize, options);
}
function monsterEffects(monster) {
  var effects = [];
  return effects;
}
function mimicEggDonation() {
  var escape = mimicEscape();
  var donation = findDonateMonster(!escape);
  if (!donation) {
    return [];
  }
  return [{
    name: `Donate mimic egg`,
    ready: () => eggMonsters().has(donation.monster),
    completed: () => get$2("_mimicEggsDonated") >= 3,
    outfit: {
      familiar: $familiar`Chest Mimic`
    },
    do: () => donate(donation.monster),
    limit: {
      skip: 3
    },
    spendsTurn: false
  }, {
    name: `Harvest mimic eggs`,
    ready: () => canReminisce(donation.monster) && (!!escape || donation.monster.attributes.includes("FREE")) && $familiar`Chest Mimic`.experience > 50,
    completed: () => get$2("_mimicEggsObtained") >= 11 || get$2("_mimicEggsDonated") >= 3 || eggMonsters().has(donation.monster),
    do: () => reminisce(donation.monster),
    combat: new GarboStrategy(() => Macro.externalIf(shouldDelevel(donation.monster), Macro.delevel()).externalIf(Math.min(100 - donation.count, 3 - get$2("_mimicEggsDonated")) > 0, Macro.trySkill($skill`%fn, lay an egg`)).externalIf(Math.min(100 - donation.count, 3 - get$2("_mimicEggsDonated")) > 1, Macro.trySkill($skill`%fn, lay an egg`)).externalIf(Math.min(100 - donation.count, 3 - get$2("_mimicEggsDonated")) > 2, Macro.trySkill($skill`%fn, lay an egg`)).externalIf(!!escape && !donation.monster.attributes.includes("FREE"), Macro.step((escape === null || escape === void 0 ? void 0 : escape.macro) ?? "")).kill(), () => Macro.kill()),
    prepare: () => {
      kolmafia.useFamiliar($familiar`Chest Mimic`);
      escape === null || escape === void 0 || escape.prepare(monsterRequirements(donation.monster));
      if (kolmafia.haveEquipped($item`unwrapped knock-off retro superhero cape`)) {
        set("heck", "hold");
      }
      if (have$P($skill`Blood Bubble`)) ensureEffect($effect`Blood Bubble`);
      kolmafia.restoreHp(kolmafia.myMaxhp());
      kolmafia.restoreMp(safeRestoreMpTarget());
    },
    effects: () => monsterEffects(donation.monster),
    limit: {
      skip: 1
    },
    spendsTurn: false,
    sobriety: "sober"
  }];
}
var FreeMimicEggDonationQuest = () => ({
  name: "Free Mimic Egg Donation",
  tasks: _toConsumableArray(mimicEggDonation()),
  ready: () => globalOptions.prefs.beSelfish !== true && have$j() && have$t(),
  completed: () => get$2("_mimicEggsDonated") >= 3
});

var firstChainMacro = () => Macro.if_(globalOptions.target, Macro.externalIf(isStrongScaler(globalOptions.target), Macro.delevel()).if_("hppercentbelow 30", Macro.tryItem($item`New Age healing crystal`)).if_(`!${Macro.makeBALLSPredicate($skill`lecture on relativity`)}`, Macro.externalIf(getDigitizeMonster() !== globalOptions.target, Macro.tryCopier($skill`Digitize`)).tryCopier($item`Spooky Putty sheet`).tryCopier($item`Rain-Doh black box`).tryCopier($item`4-d camera`).tryCopier($item`unfinished ice sculpture`).externalIf(get$2("_enamorangs") === 0, Macro.tryCopier($item`LOV Enamorang`)).tryCopier($skill`Club 'Em Into Next Week`)).trySkill($skill`lecture on relativity`).meatKill(false)).abort();
var secondChainMacro = () => Macro.if_(globalOptions.target, Macro.externalIf(isStrongScaler(globalOptions.target), Macro.delevel()).if_("hppercentbelow 30", Macro.tryItem($item`New Age healing crystal`)).if_(`!${Macro.makeBALLSPredicate($skill`lecture on relativity`)}`, Macro.trySkill($skill`Meteor Shower`)).if_(`!${Macro.makeBALLSPredicate($skill`lecture on relativity`)}`, Macro.externalIf(get$2("_sourceTerminalDigitizeMonster") !== globalOptions.target, Macro.tryCopier($skill`Digitize`)).tryCopier($item`Spooky Putty sheet`).tryCopier($item`Rain-Doh black box`).tryCopier($item`4-d camera`).tryCopier($item`unfinished ice sculpture`).externalIf(get$2("_enamorangs") === 0, Macro.tryCopier($item`LOV Enamorang`)).tryCopier($skill`Club 'Em Into Next Week`)).trySkill($skill`lecture on relativity`).meatKill(false)).abort();
function meatTargetSetup() {
  kolmafia.setLocation($location`Friar Ceremony Location`);
  potionSetup(false);
  kolmafia.maximize("MP", false);
  meatMood(true, targetMeat()).execute(highMeatMonsterCount());
  safeRestore();
  freeFightMood().execute(50);
  runGarboQuests([BuffExtensionQuest, PostBuffExtensionQuest]);
  burnLibrams(400);
  bathroomFinance(highMeatMonsterCount());
  if (have$I()) {
    educate([$skill`Extract`, $skill`Digitize`]);
  }
  if (!get$2("_cameraUsed") && !have$P($item`shaking 4-d camera`) && expectedTargetProfit() > kolmafia.mallPrice($item`4-d camera`)) {
    withProperty("autoSatisfyWithCloset", true, () => kolmafia.retrieveItem($item`4-d camera`));
  }
  if (!get$2("_iceSculptureUsed") && !have$P($item`ice sculpture`) && expectedTargetProfit() > (kolmafia.mallPrice($item`snow berries`) + kolmafia.mallPrice($item`ice harvest`)) * 3) {
    withProperty("autoSatisfyWithCloset", true, () => {
      kolmafia.cliExecute("refresh inventory");
      kolmafia.retrieveItem($item`unfinished ice sculpture`);
    });
  }
  if (!get$2("_enamorangs") && !kolmafia.itemAmount($item`LOV Enamorang`) && expectedTargetProfit() > 20000) {
    kolmafia.retrieveItem($item`LOV Enamorang`);
  }
  if (doingGregFight()) {
    initializeExtrovermectinZones();
  }
}
function startWandererCounter() {
  var nextFight = getNextCopyTargetFight();
  if (!nextFight || nextFight.canInitializeWandererCounters || nextFight.draggable) {
    return;
  }
  var digitizeNeedsStarting = get("Digitize Monster") === Infinity && getDigitizeUses() !== 0;
  var romanceNeedsStarting = get$2("_romanticFightsLeft") > 0 && get("Romantic Monster window begin") === Infinity && get("Romantic Monster window end") === Infinity;
  if (digitizeNeedsStarting || romanceNeedsStarting) {
    if (digitizeNeedsStarting) {
      kolmafia.print("Starting digitize counter by visiting the Haunted Kitchen!");
    }
    if (romanceNeedsStarting) {
      kolmafia.print("Starting romance counter by visiting the Haunted Kitchen!");
    }
    do {
      var run = void 0;
      if (gregReady()) {
        var _run$constraints$prep, _run$constraints;
        kolmafia.print("You still have gregs active, so we're going to wear your meat outfit.");
        run = ltbRun();
        (_run$constraints$prep = (_run$constraints = run.constraints).preparation) === null || _run$constraints$prep === void 0 || _run$constraints$prep.call(_run$constraints);
        meatTargetOutfit().dress();
      } else {
        var _run$constraints$prep2, _run$constraints2;
        kolmafia.print("You do not have gregs active, so this is a regular free run.");
        run = tryFindFreeRunOrBanish(freeRunConstraints()) ?? ltbRun();
        (_run$constraints$prep2 = (_run$constraints2 = run.constraints).preparation) === null || _run$constraints$prep2 === void 0 || _run$constraints$prep2.call(_run$constraints2);
        freeFightOutfit(toSpec(run), $location`The Haunted Kitchen`).dress();
      }
      garboAdventure($location`The Haunted Kitchen`, Macro.if_(globalOptions.target, Macro.target("wanderer")).step(run.macro));
    } while (get$2("lastCopyableMonster") === $monster`Government agent` || lastAdventureWasWeird({
      extraEncounters: ["Lights Out in the Kitchen"]
    }));
  }
}
function pygmyOptions() {
  var equip = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : [];
  return {
    spec: () => ({
      equip,
      avoid: $items`Staff of Queso Escusado, stinky cheese sword`,
      bonuses: new Map([[$item`garbage sticker`, 100]].concat(_toConsumableArray(magnifyingGlass())))
    }),
    macroAllowsFamiliarActions: false,
    location: $location`The Hidden Bowling Alley`,
    monster: $monster`drunk pygmy`
  };
}
function familiarSpec(underwater, fight) {
  if (have$j() && $familiar`Chest Mimic`.experience >= 50 && get$2("_mimicEggsObtained") < 11 &&
  // switchmonster doesn't apply ML, meaning the target monsters die too quickly to get multiple eggs in
  !["Macrometeorite", "Powerful Glove", "Backup"].includes(fight.name)) {
    return {
      familiar: $familiar`Chest Mimic`
    };
  }
  if (get$2("_badlyRomanticArrows") === 0) {
    if (!underwater && have$P($familiar`Obtuse Angel`) && (kolmafia.familiarEquippedEquipment($familiar`Obtuse Angel`) === $item`quake of arrows` || kolmafia.retrieveItem($item`quake of arrows`))) {
      return {
        familiar: $familiar`Obtuse Angel`,
        famequip: $item`quake of arrows`
      };
    }
    if (have$P($familiar`Reanimated Reanimator`)) {
      return {
        familiar: $familiar`Reanimated Reanimator`
      };
    }
  }
  if (isFreeAndCopyable(globalOptions.target)) {
    if (fight.gregariousReplace) {
      return {
        familiar: freeFightFamiliar(fight.location ?? globalOptions.target, {
          mode: "target",
          excludeFamiliar: [$familiar`Red-Nosed Snapper`]
        })
      };
    }
    return {
      familiar: freeFightFamiliar(fight.location ?? globalOptions.target, {
        mode: "target"
      })
    };
  }
  return {
    familiar: meatFamiliar()
  };
}
function dailyFights() {
  if (kolmafia.myInebriety() > kolmafia.inebrietyLimit()) return;
  if (copyTargetSources.some(source => source.potential())) {
    withStash($items`Spooky Putty sheet`, () => {
      // check if user wants to wish for the copy target before doing setup
      if (!getNextCopyTargetFight()) return;
      meatTargetSetup();
      if (targetingMeat()) runGarboQuests([EmbezzlerFightsQuest]);

      // PROFESSOR COPIES
      if (have$P($familiar`Pocket Professor`)) {
        var potentialPocketProfessorLectures = [{
          shouldDo: targetingMeat(),
          property: "_garbo_meatChain",
          macro: firstChainMacro,
          goalMaximize: spec => meatTargetOutfit(spec).dress()
        }, {
          shouldDo: true,
          property: "_garbo_weightChain",
          macro: secondChainMacro,
          goalMaximize: spec => Outfit.from(_objectSpread2(_objectSpread2({}, spec), {}, {
            modifier: ["Familiar Weight"]
          }), new Error(`Unable to build outfit for weight chain!`)).dress()
        }];
        for (var _i = 0, _potentialPocketProfe = potentialPocketProfessorLectures; _i < _potentialPocketProfe.length; _i++) {
          var potentialLecture = _potentialPocketProfe[_i];
          var _property = potentialLecture.property,
            macro = potentialLecture.macro,
            goalMaximize = potentialLecture.goalMaximize,
            shouldDo = potentialLecture.shouldDo;
          if (!shouldDo) continue;
          var fightSource = getNextCopyTargetFight();
          if (!fightSource) return;
          if (get$2(_property, false)) continue;
          if (fightSource.gregariousReplace) {
            var crateIsSabered = get$2("_saberForceMonster") === $monster`crate`;
            var notEnoughCratesSabered = get$2("_saberForceMonsterCount") < 2;
            var weWantToSaberCrates = !crateIsSabered || notEnoughCratesSabered;
            if (weWantToSaberCrates) saberCrateIfSafe();
          }
          var chip = $item`Pocket Professor memory chip`;
          var jacks = $item`box of Familiar Jacks`;
          kolmafia.useFamiliar($familiar`Pocket Professor`);
          if (!have$P(chip)) {
            if (kolmafia.mallPrice(jacks) < kolmafia.mallPrice(chip)) {
              kolmafia.retrieveItem(jacks);
              kolmafia.use(jacks);
            } else {
              kolmafia.retrieveItem(chip);
            }
          }
          var profSpec = {
            familiar: $familiar`Pocket Professor`,
            avoid: $items`Roman Candelabra, legendary seal-clubbing club`
          };
          if (have$P(chip)) {
            profSpec.famequip = chip;
          }
          goalMaximize(_objectSpread2(_objectSpread2({}, profSpec), fightSource.spec));
          if (get$2("_pocketProfessorLectures") < totalAvailableLectures()) {
            var _eventLog$copyTargetS;
            var startLectures = get$2("_pocketProfessorLectures");
            runTargetFight(fightSource, {
              macro: macro(),
              useAuto: false,
              action: "Pocket Professor"
            });
            eventLog.initialCopyTargetsFought += 1 + get$2("_pocketProfessorLectures") - startLectures;
            eventLog.copyTargetSources.push(fightSource.name);
            (_eventLog$copyTargetS = eventLog.copyTargetSources).push.apply(_eventLog$copyTargetS, _toConsumableArray(new Array(get$2("_pocketProfessorLectures") - startLectures).fill("Pocket Professor")));
          }
          _set(_property, true);
          postCombatActions();
          var predictedNextFight = getNextCopyTargetFight();
          if (!(predictedNextFight !== null && predictedNextFight !== void 0 && predictedNextFight.draggable)) doSausage();
          doGhost();
          startWandererCounter();
        }
      }
      kolmafia.useFamiliar(meatFamiliar());

      // REMAINING TARGET MONSTER FIGHTS
      var nextFight = getNextCopyTargetFight();
      while (nextFight !== null && kolmafia.myAdventures()) {
        kolmafia.print(`Running fight ${nextFight.name}`);
        var startTurns = kolmafia.totalTurnsPlayed();
        if (nextFight.draggable === "backup" && have$P($skill`Musk of the Moose`) && !have$P($effect`Musk of the Moose`)) {
          kolmafia.useSkill($skill`Musk of the Moose`);
        }
        if (nextFight.gregariousReplace) {
          var _crateIsSabered = get$2("_saberForceMonster") === $monster`crate`;
          var _notEnoughCratesSabered = get$2("_saberForceMonsterCount") < 2;
          var _weWantToSaberCrates = !_crateIsSabered || _notEnoughCratesSabered;
          if (_weWantToSaberCrates) saberCrateIfSafe();
        }
        var location = new TargetFightRunOptions(nextFight).location;
        var underwater = location.environment === "underwater";
        var famSpec = familiarSpec(underwater, nextFight);
        if (famSpec.familiar === $familiar`Red-Nosed Snapper` && getTrackedPhylum() !== globalOptions.target.phylum) {
          trackPhylum(globalOptions.target.phylum);
        }
        kolmafia.setLocation(location);
        meatTargetOutfit(_objectSpread2(_objectSpread2({}, nextFight.spec), famSpec), location).dress();
        runTargetFight(nextFight, {
          action: nextFight.name
        });
        postCombatActions();
        kolmafia.print(`Finished ${nextFight.name}`);
        if (kolmafia.totalTurnsPlayed() - startTurns === 1 && get$2("lastCopyableMonster") === globalOptions.target && (nextFight.wrongEncounterName || get$2("lastEncounter") === globalOptions.target.name)) {
          eventLog.initialCopyTargetsFought++;
          eventLog.copyTargetSources.push(nextFight.name);
        }
        nextFight = getNextCopyTargetFight();
        if (romanticMonsterImpossible() && (!nextFight || !nextFight.draggable)) {
          doSausage();
        }
        doGhost();
        startWandererCounter();
      }
    });
  }
}
var consecutiveNonFreeFights = 0;
var FreeFight = /*#__PURE__*/function () {
  function FreeFight(available, run, tentacle, options) {
    _classCallCheck(this, FreeFight);
    this.available = available;
    this.run = run;
    this.tentacle = tentacle;
    this.options = options;
  }
  return _createClass(FreeFight, [{
    key: "destination",
    value: function destination() {
      return this.options.wandererDetails ?? undelay(this.options.location) ?? $location.none;
    }
  }, {
    key: "outfit",
    value: function outfit(spec) {
      var additonalOptions = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {};
      return freeFightOutfit(spec, this.options.monster ? {
        location: this.destination(),
        target: undelay(this.options.monster)
      } : this.destination(), additonalOptions);
    }
  }, {
    key: "isAvailable",
    value: function isAvailable() {
      if (kolmafia.myAdventures() === 0) return false;
      var avail = this.available();
      return typeof avail === "number" ? avail > 0 : avail;
    }
  }, {
    key: "getSpec",
    value: function getSpec() {
      var noncombat = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : false;
      var spec = undelay(this.options.spec ?? {});
      if (noncombat) delete spec.familiar;
      return spec;
    }
  }, {
    key: "runAll",
    value: function runAll() {
      var _this$options$cost, _this$options;
      if (!this.isAvailable()) return;
      if ((((_this$options$cost = (_this$options = this.options).cost) === null || _this$options$cost === void 0 ? void 0 : _this$options$cost.call(_this$options)) ?? 0) > globalOptions.prefs.valueOfFreeFight) {
        return;
      }
      while (this.isAvailable()) {
        var _this$options2, _this$options2$noncom, _this$options$effects, _this$options3;
        voidMonster();
        var noncombat = !!((_this$options2 = this.options) !== null && _this$options2 !== void 0 && (_this$options2$noncom = _this$options2.noncombat) !== null && _this$options2$noncom !== void 0 && _this$options2$noncom.call(_this$options2));
        var effects = ((_this$options$effects = (_this$options3 = this.options).effects) === null || _this$options$effects === void 0 ? void 0 : _this$options$effects.call(_this$options3)) ?? [];
        freeFightMood.apply(void 0, _toConsumableArray(effects)).execute();
        this.outfit(this.getSpec(noncombat)).dress();
        safeRestore();
        var curTurncount = kolmafia.myTurncount();
        withMacro(Macro.basicCombat(), this.run);
        if (kolmafia.myTurncount() > curTurncount) consecutiveNonFreeFights++;else consecutiveNonFreeFights = 0;
        if (consecutiveNonFreeFights >= 5) {
          throw new Error("The last 5 FreeRunFights were not free!");
        }
        postCombatActions();
        // Slot in our Professor Thesis if it's become available
        if (!have$P($effect`Feeling Lost`)) deliverThesisIfAble();
      }
    }
  }]);
}();
var FreeRunFight = /*#__PURE__*/function (_FreeFight) {
  function FreeRunFight(available, run, options) {
    var _this;
    var freeRunPicker = arguments.length > 3 && arguments[3] !== undefined ? arguments[3] : {};
    _classCallCheck(this, FreeRunFight);
    _this = _callSuper(this, FreeRunFight, [available, () => null, false, _objectSpread2(_objectSpread2({}, options), {}, {
      macroAllowsFamiliarActions: false
    })]);
    _this.freeRun = run;
    _this.constraints = freeRunPicker;
    return _this;
  }
  _inherits(FreeRunFight, _FreeFight);
  return _createClass(FreeRunFight, [{
    key: "runAll",
    value: function runAll() {
      var _this2 = this;
      if (!this.isAvailable()) return;
      if ((this.options.cost ? this.options.cost() : 0) > globalOptions.prefs.valueOfFreeFight) {
        return;
      }
      var _loop = function _loop() {
        var _runSource$constraint, _runSource$constraint2, _this2$options$effect, _this2$options;
        var initialSpec = undelay(_this2.options.spec ?? {});
        var constraints = _objectSpread2(_objectSpread2({}, freeRunConstraints(initialSpec)), {}, {
          noFamiliar: () => "familiar" in initialSpec
        }, _this2.constraints);
        var runSource = tryFindFreeRunOrBanish(constraints);
        if (!runSource) return 1; // break
        (_runSource$constraint = (_runSource$constraint2 = runSource.constraints).preparation) === null || _runSource$constraint === void 0 || _runSource$constraint.call(_runSource$constraint2);
        var mergingOutfit = Outfit.from(initialSpec, new Error(`Failed to build outfit from ${JSON.stringify(initialSpec)}`));
        mergingOutfit.equip(toSpec(runSource));
        _this2.outfit(mergingOutfit.spec(), {
          familiarOptions: {
            mode: "run"
          }
        }).dress();
        freeFightMood.apply(void 0, _toConsumableArray(((_this2$options$effect = (_this2$options = _this2.options).effects) === null || _this2$options$effect === void 0 ? void 0 : _this2$options$effect.call(_this2$options)) ?? []));
        safeRestore();
        var curTurncount = kolmafia.myTurncount();
        withMacro(Macro.step(runSource.macro), () => _this2.freeRun(runSource));
        if (kolmafia.myTurncount() > curTurncount) consecutiveNonFreeFights++;else consecutiveNonFreeFights = 0;
        if (consecutiveNonFreeFights >= 5) {
          throw new Error("The last 5 FreeRunFights were not free!");
        }
        postCombatActions();
      };
      while (this.isAvailable()) {
        if (_loop()) break;
      }
    }
  }]);
}(FreeFight);
var pygmyBanishHandlers = [{
  pygmy: $monster`pygmy bowler`,
  skill: $skill`Snokebomb`,
  check: "_snokebombUsed",
  limit: getUsingFreeBunnyBanish() ? 1 : 3,
  item: $item`Louder Than Bomb`
}, {
  pygmy: $monster`pygmy orderlies`,
  skill: $skill`Feel Hatred`,
  check: "_feelHatredUsed",
  limit: 3,
  item: $item`divine champagne popper`
}, {
  pygmy: $monster`pygmy janitor`,
  skill: undefined,
  check: undefined,
  limit: 0,
  item: $item`tennis ball`
}];
var sniffSources = ["_gallapagosMonster", "olfactedMonster", "_latteMonster", "motifMonster", "longConMonster"];
var pygmySniffed = () => sniffSources.some(source => pygmyBanishHandlers.some(_ref => {
  var pygmy = _ref.pygmy;
  return pygmy === get$2(source);
}));
var pygmyMacro = () => Macro.step.apply(Macro, _toConsumableArray(pygmyBanishHandlers.map(_ref2 => {
  var pygmy = _ref2.pygmy,
    skill = _ref2.skill,
    item = _ref2.item,
    check = _ref2.check,
    limit = _ref2.limit;
  return Macro.externalIf((check ? get$2(check) : Infinity) < limit, Macro.if_(pygmy, skill ? Macro.trySkill(skill).item(item) : Macro.item(item)), Macro.if_(pygmy, Macro.item(item)));
}))).if_($monster`drunk pygmy`, Macro.trySkill($skill`Extract`).trySingAlong()).ifInnateWanderer(Macro.basicCombat()).abort();
function getStenchLocation() {
  return $locations`Uncle Gator's Country Fun-Time Liquid Waste Sluice, The Hippy Camp (Bombed Back to the Stone Age), The Dark and Spooky Swamp`.find(l => kolmafia.canAdventure(l)) ?? $location.none;
}
function bowlOfScorpionsAvailable() {
  if (get$2("hiddenTavernUnlock") === kolmafia.myAscensions()) {
    return true;
  } else if (globalOptions.triedToUnlockHiddenTavern) {
    return false;
  } else {
    globalOptions.triedToUnlockHiddenTavern = true;
    kolmafia.retrieveItem($item`book of matches`);
    if (have$P($item`book of matches`)) {
      kolmafia.use($item`book of matches`);
    }
    return get$2("hiddenTavernUnlock") === kolmafia.myAscensions() || kolmafia.mallPrice($item`Bowl of Scorpions`) < 1000;
  }
}
function molemanReady() {
  return have$P($item`molehill mountain`) && !get$2("_molehillMountainUsed");
}
var freeFightSources = [new FreeFight(() => wantPills() ? 5 - get$2("_saberForceUses") : 0, () => {
  if (have$P($familiar`Red-Nosed Snapper`)) {
    kolmafia.cliExecute(`snapper ${$phylum`dude`}`);
  }
  setChoice(1387, 3);
  if (have$P($skill`Comprehensive Cartography`) && get$2("_monstersMapped") < (getBestItemStealZone(true) && get$2("_fireExtinguisherCharge") >= 10 ? 2 : 3) // Save a map to use for polar vortex
  ) {
    withMacro(Macro.skill($skill`Use the Force`), () => {
      mapMonster($location`Domed City of Grimacia`, $monster`grizzled survivor`);
      kolmafia.runCombat();
      kolmafia.runChoice(-1);
    });
  } else {
    if (kolmafia.numericModifier($item`Grimacite guayabera`, "Monster Level") < 40) {
      kolmafia.retrieveItem(1, $item`tennis ball`);
      kolmafia.retrieveItem(1, $item`handful of split pea soup`);
      kolmafia.retrieveItem(1, $item`divine champagne popper`);
    }
    var snokeLimit = getUsingFreeBunnyBanish() ? 1 : 3;
    garboAdventure($location`Domed City of Grimacia`, Macro.if_($monster`alielf`, Macro.trySkill($skill`Asdon Martin: Spring-Loaded Front Bumper`).tryItem($item`handful of split pea soup`)).if_($monster`cat-alien`, get$2("_snokebombUsed") < snokeLimit ? Macro.trySkill($skill`Snokebomb`).item($item`tennis ball`) : Macro.item($item`tennis ball`)).if_($monster`dog-alien`, Macro.trySkill($skill`Feel Hatred`).tryItem($item`divine champagne popper`)).step("pickpocket").skill($skill`Use the Force`));
  }
}, false, {
  spec: () => {
    var canPickPocket = kolmafia.myClass() === $class`Accordion Thief` || kolmafia.myClass() === $class`Disco Bandit`;
    var bestPickpocketItem = $items`tiny black hole, mime army infiltration glove`.find(item => have$P(item) && kolmafia.canEquip(item));
    var spec = {
      modifier: ["1000 Pickpocket Chance"],
      equip: $items`Fourth of May Cosplay Saber`
    };
    if (have$P($familiar`Red-Nosed Snapper`)) {
      spec.familiar = $familiar`Red-Nosed Snapper`;
    }
    if (!canPickPocket && bestPickpocketItem) {
      var _spec$equip;
      (_spec$equip = spec.equip) === null || _spec$equip === void 0 || _spec$equip.push(bestPickpocketItem);
    }
    return spec;
  },
  effects: () => $effects`Transpondent`,
  macroAllowsFamiliarActions: false,
  location: $location`Domed City of Grimacia`
}),
// Initial 9 Pygmy fights
new FreeFight(() => get$2("questL11Worship") !== "unstarted" && bowlOfScorpionsAvailable() && !pygmySniffed() ? clamp(9 - get$2("_drunkPygmyBanishes"), 0, 9) : 0, () => {
  kolmafia.putCloset(kolmafia.itemAmount($item`bowling ball`), $item`bowling ball`);
  kolmafia.retrieveItem(clamp(9 - get$2("_drunkPygmyBanishes"), 0, 9), $item`Bowl of Scorpions`);
  kolmafia.retrieveItem($item`Louder Than Bomb`);
  kolmafia.retrieveItem($item`tennis ball`);
  kolmafia.retrieveItem($item`divine champagne popper`);
  garboAdventure($location`The Hidden Bowling Alley`, pygmyMacro());
}, true, {
  cost: () => {
    var banishers = pygmyBanishHandlers.filter(_ref3 => {
      var skill = _ref3.skill,
        check = _ref3.check,
        limit = _ref3.limit;
      return !skill || !have$P(skill) || check && get$2(check) >= limit;
    }).map(_ref4 => {
      var item = _ref4.item;
      return item;
    });
    return kolmafia.retrievePrice($item`Bowl of Scorpions`) + sum(banishers, kolmafia.mallPrice) / 11;
  },
  location: $location`The Hidden Bowling Alley`,
  monster: $monster`drunk pygmy`
}),
// 10th Pygmy fight. If we have an orb, equip it for this fight, to save for later
new FreeFight(() => get$2("questL11Worship") !== "unstarted" && get$2("_drunkPygmyBanishes") === 9 && !pygmySniffed(), () => {
  kolmafia.putCloset(kolmafia.itemAmount($item`bowling ball`), $item`bowling ball`);
  kolmafia.retrieveItem($item`Bowl of Scorpions`);
  garboAdventure($location`The Hidden Bowling Alley`, pygmyMacro());
}, true, pygmyOptions($items`miniature crystal ball`.filter(item => have$P(item)))),
// 11th pygmy fight if we lack a saber
new FreeFight(() => get$2("questL11Worship") !== "unstarted" && get$2("_drunkPygmyBanishes") === 10 && (!have$P($item`Fourth of May Cosplay Saber`) || crateStrategy() === "Saber") && !pygmySniffed(), () => {
  kolmafia.putCloset(kolmafia.itemAmount($item`bowling ball`), $item`bowling ball`);
  kolmafia.retrieveItem($item`Bowl of Scorpions`);
  garboAdventureAuto($location`The Hidden Bowling Alley`, pygmyMacro());
}, true, pygmyOptions()),
// 11th+ pygmy fight if we have a saber- saber friends
new FreeFight(() => {
  var rightTime = have$P($item`Fourth of May Cosplay Saber`) && crateStrategy() !== "Saber" && get$2("_drunkPygmyBanishes") >= 10;
  var saberedMonster = get$2("_saberForceMonster");
  var wrongPygmySabered = saberedMonster && $monsters`pygmy orderlies, pygmy bowler, pygmy janitor`.includes(saberedMonster);
  var drunksCanAppear = get$2("_drunkPygmyBanishes") === 10 || saberedMonster === $monster`drunk pygmy` && get$2("_saberForceMonsterCount");
  return get$2("questL11Worship") !== "unstarted" && rightTime && !wrongPygmySabered && drunksCanAppear && !pygmySniffed();
}, () => {
  if ((get$2("_saberForceMonster") !== $monster`drunk pygmy` || get$2("_saberForceMonsterCount") === 1) && get$2("_saberForceUses") < 5) {
    kolmafia.putCloset(kolmafia.itemAmount($item`bowling ball`), $item`bowling ball`);
    kolmafia.putCloset(kolmafia.itemAmount($item`Bowl of Scorpions`), $item`Bowl of Scorpions`);
    garboAdventure($location`The Hidden Bowling Alley`, Macro.skill($skill`Use the Force`));
  } else {
    if (kolmafia.closetAmount($item`Bowl of Scorpions`) > 0) {
      kolmafia.takeCloset(kolmafia.closetAmount($item`Bowl of Scorpions`), $item`Bowl of Scorpions`);
    } else kolmafia.retrieveItem($item`Bowl of Scorpions`);
    garboAdventure($location`The Hidden Bowling Alley`, pygmyMacro());
  }
}, false, pygmyOptions($items`Fourth of May Cosplay Saber`)),
// Finally, saber or not, if we have a drunk pygmy in our crystal ball, let it out.
new FreeFight(() => get$2("questL11Worship") !== "unstarted" && ponder().get($location`The Hidden Bowling Alley`) === $monster`drunk pygmy` && get$2("_drunkPygmyBanishes") >= 11 && !pygmySniffed(), () => {
  kolmafia.putCloset(kolmafia.itemAmount($item`bowling ball`), $item`bowling ball`);
  kolmafia.retrieveItem(1, $item`Bowl of Scorpions`);
  garboAdventure($location`The Hidden Bowling Alley`, Macro.if_($monster`drunk pygmy`, pygmyMacro()).abort());
}, true, pygmyOptions($items`miniature crystal ball`.filter(item => have$P(item)))), new FreeFight(() => !timeSpinnerRefused($monster`drunk pygmy`) && have$P($item`Time-Spinner`) && !doingGregFight() && $location`The Hidden Bowling Alley`.combatQueue.includes("drunk pygmy") && get$2("_timeSpinnerMinutesUsed") < 8, () => {
  kolmafia.retrieveItem($item`Bowl of Scorpions`);
  Macro.trySkill($skill`Extract`).trySingAlong().setAutoAttack();
  kolmafia.visitUrl(`inv_use.php?whichitem=${kolmafia.toInt($item`Time-Spinner`)}`);
  travelToRecentFight($monster`drunk pygmy`);
}, true, pygmyOptions()), new FreeFight(() => get$2("neverendingPartyAlways") && questStep("_questPartyFair") < 999 ? clamp(10 - get$2("_neverendingPartyFreeTurns") - (!molemanReady() && !get$2("_thesisDelivered") && have$P($familiar`Pocket Professor`) ? 1 : 0), 0, 10) : 0, () => {
  var constructedMacro = Macro.tryHaveSkill($skill`Feel Pride`).basicCombat();
  setNepQuestChoicesAndPrepItems();
  garboAdventure($location`The Neverending Party`, constructedMacro);
}, true, {
  spec: () => ({
    modifier: get$2("_questPartyFairQuest") === "trash" ? ["100 Item Drop"] : get$2("_questPartyFairQuest") === "dj" ? ["100 Meat Drop"] : [],
    equip: get$2("_partyHard") ? $items`PARTY HARD T-shirt` : have$P($item`January's Garbage Tote`) && (!have$P($item`broken champagne bottle`) || get$2("garbageChampagneCharge") === 0) && (!have$P($item`deceased crimbo tree`) || get$2("garbageTreeCharge") === 0) ? $items`makeshift garbage shirt` : []
  }),
  location: $location`The Neverending Party`
}),
// Get a li'l ninja costume for 150% item drop
new FreeFight(() => !have$P($item`li'l ninja costume`) && have$P($familiar`Trick-or-Treating Tot`) && !get$2("_firedJokestersGun") && have$P($item`The Jokester's gun`) && kolmafia.canEquip($item`The Jokester's gun`) && questStep("questL08Trapper") >= 2, () => garboAdventure($location`Lair of the Ninja Snowmen`, Macro.skill($skill`Fire the Jokester's Gun`).abort()), true, {
  spec: {
    equip: $items`The Jokester's gun`
  },
  macroAllowsFamiliarActions: false,
  location: $location`Lair of the Ninja Snowmen`
}),
// Fallback for li'l ninja costume if Lair of the Ninja Snowmen is unavailable
new FreeFight(() => !have$P($item`li'l ninja costume`) && have$P($familiar`Trick-or-Treating Tot`) && !get$2("_firedJokestersGun") && have$P($item`The Jokester's gun`) && kolmafia.canEquip($item`The Jokester's gun`) && availableMaps() > 0, () => {
  try {
    Macro.skill($skill`Fire the Jokester's Gun`).abort().setAutoAttack();
    mapMonster($location`The Haiku Dungeon`, $monster`amateur ninja`);
  } finally {
    kolmafia.setAutoAttack(0);
  }
}, true, {
  spec: {
    equip: $items`The Jokester's gun`
  },
  macroAllowsFamiliarActions: false,
  location: $location`The Haiku Dungeon`,
  monster: $monster`amateur ninja`
}), new FreeFight(() => {
  if (!have$P($item`closed-circuit pay phone`)) return false;
  // Check if we have or can get Shadow Affinity
  if (have$P($effect`Shadow Affinity`)) return true;
  if (!get$2("_shadowAffinityToday") && !rufusTarget()) {
    return true;
  }
  if (get$2("rufusQuestType") === "items" || get$2("rufusQuestType") === "entity") {
    // TODO: Skip bosses for now, until we can fight them
    return false; // We deemed it unprofitable to complete the quest in potionSetup
  }
  if (get$2("encountersUntilSRChoice") === 0) {
    // Target is either an artifact or a boss
    return true; // Get the artifact or kill the boss immediately for free
  }

  // Consider forcing noncombats below:
  if (get$2("noncombatForcerActive")) return true; // If it's already forced, no problem
  if (willYachtzee()) return false; // NCs are better when yachtzeeing, probably
  // TODO: With the KoL update, is there a function for checking if an NC is already forced?
  if (shouldClara("shadow waters")) {
    return true;
  }

  // TODO: Calculate forcing for shadow waters against using the +5 fam weight buff
  if (have$n() && totalAvailableCinch() >= 60) {
    return true;
  }
  return false; // It costs turns to do anything else here
}, () => {
  if (have$P($item`Rufus's shadow lodestone`)) {
    setChoice(1500, 2); // Turn in lodestone if you have it
    kolmafia.adv1(bestShadowRift(), -1, "");
  }
  if (!get$2("_shadowAffinityToday") && !rufusTarget()) {
    chooseQuest(() => 2); // Choose an artifact (not supporting boss for now)
    aprilFoolsRufus();
  }
  runShadowRiftTurn();
  if (get$2("encountersUntilSRChoice") === 0 || get$2("noncombatForcerActive")) {
    if (have$m() && !rufusTarget()) {
      chooseQuest(() => 2);
      aprilFoolsRufus();
    }
    kolmafia.adv1(bestShadowRift(), -1, ""); // grab the NC
  }
  if (questStep("questRufus") === 1) {
    withChoice(1498, 1, () => kolmafia.use($item`closed-circuit pay phone`));
  }
  if (have$P($item`Rufus's shadow lodestone`)) {
    setChoice(1500, 2); // Check for lodestone at the end again
    kolmafia.adv1(bestShadowRift(), -1, "");
  }
  if (!have$P($effect`Shadow Affinity`) && get$2("encountersUntilSRChoice") !== 0) {
    kolmafia.setLocation($location.none); // Reset location to not affect mafia's item drop calculations
  }
}, true, {
  location: $location`Shadow Rift`
})];
var priorityFreeRunFightSources = [new FreeRunFight(() => have$P($familiar`Patriotic Eagle`) && !have$P($effect`Citizen of a Zone`) && $locations`Barf Mountain, The Fun-Guy Mansion, The Dire Warren`.some(l => kolmafia.canAdventure(l)), runSource => {
  var location = $locations`Barf Mountain, The Fun-Guy Mansion, The Dire Warren`.find(l => kolmafia.canAdventure(l));
  if (!location) {
    throw new Error("Somehow, we can't adventure in the Dire Warren.");
  }
  garboAdventure(location, Macro.skill($skill`%fn, let's pledge allegiance to a Zone`).step(runSource.macro));
}, {
  spec: {
    familiar: $familiar`Patriotic Eagle`,
    famequip: $items`little bitty bathysphere, das boot`,
    modifier: ["ML 100 Max", "-Familiar Weight"],
    avoid: $items`Drunkula's wineglass`
  },
  location: kolmafia.canAdventure($location`Barf Mountain`) ? $location`Barf Mountain` : $location`The Dire Warren`
})];
function latteFight(ingredient) {
  return new FreeRunFight(() => shouldUnlockIngredients() && !ingredientsUnlocked().includes(ingredient) && kolmafia.canAdventure(locationOf(ingredient)), runSource => {
    var targetLocation = locationOf(ingredient);
    propertyManager.setChoices(wanderer().getChoices(targetLocation));
    garboAdventure(targetLocation, runSource.macro);
  }, {
    spec: {
      equip: $items`latte lovers member's mug`
    },
    location: locationOf(ingredient)
  }, freeRunConstraints({
    equip: $items`latte lovers member's mug`
  }));
}
var freeRunFightSources = [].concat(_toConsumableArray(["cajun", "rawhide", "carrot"].map(latteFight)), [
// Fire Extinguisher on best available target.
new FreeRunFight(() => (have$P($item`industrial fire extinguisher`) && get$2("_fireExtinguisherCharge") >= 10 || have$P($familiar`XO Skeleton`) && get$2("_xoHugsUsed") < 11 || have$P($item`bat wings`) && get$2("_batWingsSwoopUsed") < 11 || have$P($skill`Perpetrate Mild Evil`) && get$2("_mildEvilPerpetrated") < 3) && get$2("_VYKEACompanionLevel") === 0 &&
// don't attempt this in case you re-run garbo after making a vykea furniture
getBestItemStealZone() !== null, runSource => {
  setupItemStealZones();
  var best = getBestItemStealZone();
  if (!best) throw `Unable to find fire extinguisher zone?`;
  var mappingMonster = availableMaps() > 0 && best.location.wanderers;
  var monsters = asArray(best.monster);
  if (best.preReq) best.preReq();
  var hasXO = kolmafia.myFamiliar() === $familiar`XO Skeleton`;
  if (kolmafia.myThrall() !== $thrall.none) kolmafia.useSkill($skill`Dismiss Pasta Thrall`);
  withMacro(Macro.if_(monsters.map(m => `!monsterid ${m.id}`).join(" && "), runSource.macro).externalIf(hasXO && get$2("_xoHugsUsed") < 11, Macro.skill($skill`Hugs and Kisses!`)).externalIf(!best.requireMapTheMonsters && hasXO && get$2("_xoHugsUsed") < 10, Macro.step(itemStealOlfact(best))).trySkillRepeat($skill`Fire Extinguisher: Polar Vortex`, $skill`Perpetrate Mild Evil`, $skill`Swoop like a Bat`).step(runSource.macro), () => {
    if (mappingMonster) {
      mapMonster(best.location, monsters[0]);
    } else {
      kolmafia.adv1(best.location, -1, "");
    }
  }, true);
}, {
  spec: () => {
    var zone = getBestItemStealZone();
    var spec = have$P($familiar`XO Skeleton`) && get$2("_xoHugsUsed") < 11 ? {
      familiar: $familiar`XO Skeleton`,
      equip: []
    } : {
      equip: []
    };
    if (have$P($item`industrial fire extinguisher`) && get$2("_fireExtinguisherCharge") >= 10) {
      var _spec$equip2;
      (_spec$equip2 = spec.equip) === null || _spec$equip2 === void 0 || _spec$equip2.push($item`industrial fire extinguisher`);
    }
    if (swoopsRemaining() > 0) {
      var _spec$equip3;
      (_spec$equip3 = spec.equip) === null || _spec$equip3 === void 0 || _spec$equip3.push($item`bat wings`);
    }
    spec.modifier = (zone === null || zone === void 0 ? void 0 : zone.maximize) ?? [];
    return spec;
  },
  location: () => {
    var _getBestItemStealZone;
    return ((_getBestItemStealZone = getBestItemStealZone()) === null || _getBestItemStealZone === void 0 ? void 0 : _getBestItemStealZone.location) ?? $location.none;
  },
  monster: () => {
    var _getBestItemStealZone2;
    var monsterTarget = (_getBestItemStealZone2 = getBestItemStealZone()) === null || _getBestItemStealZone2 === void 0 ? void 0 : _getBestItemStealZone2.monster;
    if (!monsterTarget) return $monster.none;
    return Array.isArray(monsterTarget) ? monsterTarget[0] : monsterTarget;
  }
}), new FreeRunFight(() => have$P($familiar`Space Jellyfish`) && get$2("_spaceJellyfishDrops") < 5 && getStenchLocation() !== $location.none, runSource => {
  garboAdventure(getStenchLocation(), Macro.trySkill($skill`Extract Jelly`).step(runSource.macro));
}, {
  spec: {
    familiar: $familiar`Space Jellyfish`
  },
  location: getStenchLocation
}), new FreeRunFight(() => !doingGregFight() && have$P($familiar`Space Jellyfish`) && have$P($skill`Meteor Lore`) && get$2("_macrometeoriteUses") < 10 && getStenchLocation() !== $location.none, runSource => {
  garboAdventure(getStenchLocation(), Macro.while_(`!pastround 28 && ${Macro.makeBALLSPredicate($skill`Macrometeorite`)}`, Macro.skill($skill`Extract Jelly`).skill($skill`Macrometeorite`)).trySkill($skill`Extract Jelly`).step(runSource.macro));
}, {
  spec: {
    familiar: $familiar`Space Jellyfish`
  },
  location: getStenchLocation
}), new FreeRunFight(() => !doingGregFight() && have$P($familiar`Space Jellyfish`) && have$P($item`Powerful Glove`) && get$2("_powerfulGloveBatteryPowerUsed") < 91 && getStenchLocation() !== $location.none, runSource => {
  garboAdventure(getStenchLocation(), Macro.while_(`!pastround 28 && ${Macro.makeBALLSPredicate($skill`CHEAT CODE: Replace Enemy`)}`, Macro.skill($skill`Extract Jelly`).skill($skill`CHEAT CODE: Replace Enemy`)).trySkill($skill`Extract Jelly`).step(runSource.macro));
}, {
  spec: {
    familiar: $familiar`Space Jellyfish`,
    equip: $items`Powerful Glove`
  },
  location: getStenchLocation
}), new FreeFight(() => available$3() && get$2("gingerAdvanceClockUnlocked") && !get$2("_gingerbreadClockVisited") && get$2("_gingerbreadCityTurns") <= 3, () => {
  propertyManager.setChoices({
    1215: 1 // Gingerbread Civic Center advance clock
  });
  garboAdventure($location`Gingerbread Civic Center`, Macro.abortWithMsg(`Expected "Setting the Clock" but ended up in combat.`));
}, false, {
  noncombat: () => true,
  location: $location`Gingerbread Civic Center`
}), new FreeRunFight(() => available$3() && minutesToNoon() > 0, runSource => {
  propertyManager.setChoices({
    1215: 1 // Gingerbread Civic Center advance clock
  });
  garboAdventure($location`Gingerbread Civic Center`, runSource.macro);
  if (["Even Tamer Than Usual", "Never Break the Chain", "Close, but Yes Cigar", "Armchair Quarterback"].includes(get$2("lastEncounter"))) {
    _set("_gingerbreadCityTurns", 1 + get$2("_gingerbreadCityTurns"));
  }
}, {
  spec: {
    bonuses: new Map([[$item`carnivorous potted plant`, 100]])
  },
  location: $location`Gingerbread Civic Center`
}), new FreeFight(() => available$3() && minutesToNoon() === 0, () => {
  propertyManager.setChoices({
    1204: 1 // Gingerbread Train Station Noon random candy
  });
  garboAdventure($location`Gingerbread Train Station`, Macro.abortWithMsg(`Expected "Noon at the Train Station" but ended up in combat.`));
}, false, {
  noncombat: () => true,
  location: $location`Gingerbread Train Station`
}), new FreeRunFight(() => available$3() && minutesToMidnight() > 0 && minutesToNoon() < 0 && (kolmafia.availableAmount($item`sprinkles`) > 5 || kolmafia.haveOutfit("gingerbread best")), runSource => {
  propertyManager.setChoices({
    1215: 1 // Gingerbread Civic Center advance clock
  });
  garboAdventure($location`Gingerbread Civic Center`, runSource.macro);
  if (["Even Tamer Than Usual", "Never Break the Chain", "Close, but Yes Cigar", "Armchair Quarterback"].includes(get$2("lastEncounter"))) {
    _set("_gingerbreadCityTurns", 1 + get$2("_gingerbreadCityTurns"));
  }
}, {
  spec: {
    bonuses: new Map([[$item`carnivorous potted plant`, 100]])
  },
  location: $location`Gingerbread Civic Center`
}), new FreeFight(() => available$3() && minutesToMidnight() === 0 && (kolmafia.availableAmount($item`sprinkles`) > 5 || kolmafia.haveOutfit("gingerbread best")), () => {
  var _bestMidnightAvailabl = bestMidnightAvailable(),
    choices = _bestMidnightAvailabl.choices,
    location = _bestMidnightAvailabl.location;
  propertyManager.setChoices(choices);
  if (location === $location`Gingerbread Upscale Retail District`) {
    kolmafia.outfit("gingerbread best");
  }
  garboAdventure(location, Macro.abortWithMsg("We thought it was Midnight here in Gingerbread City, but we're in a fight!"));
}, false, {
  noncombat: () => true,
  location: () => bestMidnightAvailable().location
}),
// Try for an ultra-rare with mayfly runs and pickpocket if we have a manuel to detect monster hp ;)
new FreeRunFight(() => monsterManuelAvailable() && have$P($item`mayfly bait necklace`) && kolmafia.canAdventure($location`Cobb's Knob Menagerie, Level 1`) && get$2("_mayflySummons") < 30, runSource => {
  var willSurvivePassive = `monsterhpabove ${maxPassiveDamage()}`;
  garboAdventure($location`Cobb's Knob Menagerie, Level 1`, Macro.if_($monster`QuickBASIC elemental`, Macro.basicCombat()).if_($monster`BASIC Elemental`, Macro.if_(willSurvivePassive, Macro.step("pickpocket")).externalIf(have$P($skill`Transcendent Olfaction`) && get$2("_olfactionsUsed") < 1, Macro.if_(willSurvivePassive, Macro.trySkill($skill`Transcendent Olfaction`))).externalIf(have$P($skill`Gallapagosian Mating Call`) && get$2("_gallapagosMonster") !== $monster`BASIC Elemental`, Macro.if_(willSurvivePassive, Macro.skill($skill`Gallapagosian Mating Call`))).trySkill($skill`Summon Mayfly Swarm`)).step(runSource.macro));
}, {
  spec: () => {
    var canPickPocket = kolmafia.myClass() === $class`Accordion Thief` || kolmafia.myClass() === $class`Disco Bandit`;
    var bestPickpocketItem = $items`tiny black hole, mime army infiltration glove`.find(item => have$P(item) && kolmafia.canEquip(item));
    // Base drop is 30%, so 1% pickpocket gives .003
    var pickPocketValue = 0.003 * garboValue($item`GOTO`);
    var spec = {
      equip: $items`mayfly bait necklace`,
      bonuses: new Map([[$item`carnivorous potted plant`, 100]]),
      familiar: freeFightFamiliar($location`Cobb's Knob Menagerie, Level 1`, {
        allowAttackFamiliars: false,
        mode: "run"
      })
    };
    if (!canPickPocket && bestPickpocketItem) {
      var _spec$equip4;
      (_spec$equip4 = spec.equip) === null || _spec$equip4 === void 0 || _spec$equip4.push(bestPickpocketItem);
    }
    if (canPickPocket || bestPickpocketItem) {
      spec.modifier = [`${pickPocketValue} Pickpocket Chance`];
    }
    return spec;
  },
  location: $location`Cobb's Knob Menagerie, Level 1`
}),
// Try for mini-hipster\goth kid free fights with any remaining non-familiar free runs
new FreeRunFight(() => get$2("_hipsterAdv") < 7 && (have$P($familiar`Mini-Hipster`) || have$P($familiar`Artistic Goth Kid`)), runSource => {
  var targetLocation = wanderer().getTarget("backup").location;
  propertyManager.setChoices(wanderer().getChoices(targetLocation));
  garboAdventure(targetLocation, Macro.if_(`(monsterid 969) || (monsterid 970) || (monsterid 971) || (monsterid 972) || (monsterid 973) || (monstername Black Crayon *)`, Macro.basicCombat()).step(runSource.macro));
}, {
  spec: () => {
    if (have$P($familiar`Mini-Hipster`)) {
      return {
        familiar: $familiar`Mini-Hipster`,
        bonuses: new Map([[$item`ironic moustache`, garboValue($item`mole skin notebook`)], [$item`chiptune guitar`, garboValue($item`ironic knit cap`)], [$item`fixed-gear bicycle`, garboValue($item`ironic oversized sunglasses`)]])
      };
    } else {
      return {
        familiar: $familiar`Artistic Goth Kid`
      };
    }
  },
  wandererDetails: "backup"
}),
// Try to accelerate the shadow nc, if you're able to do a quest
new FreeRunFight(() => have$P($item`closed-circuit pay phone`) && get$2("rufusQuestType") !== "items" && !have$P($effect`Shadow Affinity`) && get$2("encountersUntilSRChoice") > 0, runSource => garboAdventure(bestShadowRift(), runSource.macro), {
  location: bestShadowRift
}),
// Try for an ultra-rare with mayfly runs if we didn't have a manuel ;)
new FreeRunFight(() => have$P($item`mayfly bait necklace`) && kolmafia.canAdventure($location`Cobb's Knob Menagerie, Level 1`) && get$2("_mayflySummons") < 30, runSource => {
  garboAdventure($location`Cobb's Knob Menagerie, Level 1`, Macro.if_($monster`QuickBASIC elemental`, Macro.basicCombat()).if_($monster`BASIC Elemental`, Macro.trySkill($skill`Summon Mayfly Swarm`)).step(runSource.macro));
}, {
  spec: {
    equip: $items`mayfly bait necklace`,
    bonuses: new Map([[$item`carnivorous potted plant`, 100]])
  },
  location: $location`Cobb's Knob Menagerie, Level 1`
})]);
function targetCopiesInProgress() {
  return get$2("beGregariousFightsLeft") > 0 || get$2("_monsterHabitatsFightsLeft") > 0 || !romanticMonsterImpossible() || get("Digitize Monster") <= 0;
}
function freeRunFights() {
  if (kolmafia.myInebriety() > kolmafia.inebrietyLimit()) return;
  if (targetCopiesInProgress()) return;
  propertyManager.setChoices({
    1387: 2,
    // "You will go find two friends and meet me here."
    1324: 5 // Fight a random partier
  });
  var onlyPriorityRuns = globalOptions.prefs.yachtzeechain && !get$2("_garboYachtzeeChainCompleted", false);
  var stashRun = kolmafia.stashAmount($item`navel ring of navel gazing`) ? $items`navel ring of navel gazing` : kolmafia.stashAmount($item`Greatest American Pants`) ? $items`Greatest American Pants` : [];
  kolmafia.refreshStash();
  withStash(stashRun, () => {
    var _iterator = _createForOfIteratorHelper(priorityFreeRunFightSources),
      _step;
    try {
      for (_iterator.s(); !(_step = _iterator.n()).done;) {
        var priorityRunFight = _step.value;
        priorityRunFight.runAll();
      }
    } catch (err) {
      _iterator.e(err);
    } finally {
      _iterator.f();
    }
    if (onlyPriorityRuns) return;
    var _iterator2 = _createForOfIteratorHelper(freeRunFightSources),
      _step2;
    try {
      for (_iterator2.s(); !(_step2 = _iterator2.n()).done;) {
        var freeRunFightSource = _step2.value;
        freeRunFightSource.runAll();
      }
    } catch (err) {
      _iterator2.e(err);
    } finally {
      _iterator2.f();
    }
  });
}
function freeFights() {
  if (kolmafia.myInebriety() > kolmafia.inebrietyLimit()) return;
  if (targetCopiesInProgress()) return;
  propertyManager.setChoices({
    1387: 2,
    // "You will go find two friends and meet me here."
    1324: 5 // Fight a random partier
  });
  freeRunFights();
  killRobortCreaturesForFree();

  // TODO: Run unconverted free fights
  // TODO: Once all is grimoirized, move Eldritch Horror free fight to the start and update the uneffect task, so that we can optimize Generic Summer Holiday tentacles.
  var _iterator3 = _createForOfIteratorHelper(freeFightSources),
    _step3;
  try {
    for (_iterator3.s(); !(_step3 = _iterator3.n()).done;) {
      var freeFightSource = _step3.value;
      freeFightSource.runAll();
    }

    // TODO: Run grimorized free fights until all are converted
    // TODO: freeFightMood()
  } catch (err) {
    _iterator3.e(err);
  } finally {
    _iterator3.f();
  }
  runGarboQuests([PostQuest(), FreeFightQuest, FreeGiantSandwormQuest]);

  // Run any community endeavors
  runGarboQuests([PostQuest(), undelay(FreeMimicEggDonationQuest)]);
  tryFillLatte();
  postFreeFightDailySetup();
}
function setNepQuestChoicesAndPrepItems() {
  var quest = get$2("_questPartyFairQuest");
  if (quest === "food") {
    if (!questStep("_questPartyFair")) {
      setChoice(1324, 2); // Check out the kitchen
      setChoice(1326, 3); // Talk to the woman
    } else if (get$2("choiceAdventure1324") !== 5) {
      setChoice(1324, 5);
      kolmafia.print("Found Geraldine!", HIGHLIGHT);
      // Format of this property is count, space, item ID.
      var partyFairInfo = get$2("_questPartyFairProgress").split(" ");
      logMessage(`Geraldine wants ${partyFairInfo[0]} ${kolmafia.toItem(partyFairInfo[1]).plural}, please!`);
    }
  } else if (quest === "booze") {
    if (!questStep("_questPartyFair")) {
      setChoice(1324, 3); // Go to the back yard
      setChoice(1327, 3); // Find Gerald
    } else if (get$2("choiceAdventure1324") !== 5) {
      setChoice(1324, 5);
      kolmafia.print("Found Gerald!", HIGHLIGHT);
      var _partyFairInfo = get$2("_questPartyFairProgress").split(" ");
      logMessage(`Gerald wants ${_partyFairInfo[0]} ${kolmafia.toItem(_partyFairInfo[1]).plural}, please!`);
    }
  } else {
    setChoice(1324, 5); // Pick a fight
  }
}
function thesisReady() {
  return !get$2("_thesisDelivered") && have$P($familiar`Pocket Professor`) && $familiar`Pocket Professor`.experience >= 400;
}
function deliverThesisIfAble() {
  if (!thesisReady()) return;
  var requiredThesisHP = 1296;
  var thesisLocation = $location`Uncle Gator's Country Fun-Time Liquid Waste Sluice`;
  var requiredMuscle = requiredThesisHP / 0.75 - 5;
  if (molemanReady()) {
    requiredMuscle = requiredThesisHP / 1.5 - 15;
    thesisLocation = $location`Noob Cave`; // We can trivially always adventure here
  } else if ((get$2("neverendingPartyAlways") || get$2("_neverEndingPartyToday")) && questStep("_questPartyFair") < 999 && !get$2("_partyHard")) {
    // Set up NEP if we haven't yet
    setNepQuestChoicesAndPrepItems();
    thesisLocation = $location`The Neverending Party`;
    requiredMuscle = requiredThesisHP / 0.75 + 10;
  }
  // if running nobarf, might not have access to Uncle Gator's. Space is cheaper.
  else if (!kolmafia.canAdventure(thesisLocation)) {
    if (!have$P($item`transporter transponder`)) {
      acquire(1, $item`transporter transponder`, 10000);
    }
    kolmafia.use($item`transporter transponder`);
    thesisLocation = $location`Hamburglaris Shield Generator`;
    requiredMuscle = requiredThesisHP / 0.75 - 1;
  }
  freeFightOutfit({
    modifier: ["100 Muscle"],
    familiar: $familiar`Pocket Professor`
  }, molemanReady() ? $monster`Moleman` : thesisLocation).dress();
  safeRestore();
  if (kolmafia.myBuffedstat($stat`Muscle`) < requiredMuscle && have$P($item`Powerful Glove`) && !have$P($effect`Triple-Sized`) && get$2("_powerfulGloveBatteryPowerUsed") <= 95 && (
  // We only get triple-sized if it doesn't lose us a replace enemy use
  get$2("_powerfulGloveBatteryPowerUsed") % 10 === 5 || !doingGregFight())) {
    kolmafia.cliExecute("checkpoint");
    kolmafia.equip($slot`acc1`, $item`Powerful Glove`);
    ensureEffect($effect`Triple-Sized`);
    kolmafia.outfit("checkpoint");
  }
  kolmafia.cliExecute(`try; gain ${requiredMuscle} muscle`);
  if (molemanReady()) {
    withMacro(Macro.skill($skill`deliver your thesis!`), () => kolmafia.use($item`molehill mountain`), true);
  } else {
    garboAdventure(thesisLocation, Macro.skill($skill`deliver your thesis!`));
  }
  postCombatActions();
}
function doSausage() {
  if (!kramcoGuaranteed()) {
    return;
  }
  freeFightOutfit({
    equip: $items`Kramco Sausage-o-Matic™`
  }, {
    location: "wanderer",
    target: $monster`sausage goblin`
  }).dress();
  var currentSausages = get$2("_sausageFights");
  do {
    var targetLocation = wanderer().getTarget("wanderer").location;
    propertyManager.setChoices(wanderer().getChoices(targetLocation));
    var goblin = $monster`sausage goblin`;
    freeFightOutfit({
      equip: $items`Kramco Sausage-o-Matic™`
    }, {
      location: "wanderer",
      target: $monster`sausage goblin`
    }).dress();
    garboAdventureAuto(targetLocation, Macro.if_(goblin, Macro.basicCombat()).ifInnateWanderer(Macro.basicCombat()).abortWithMsg(`Expected ${goblin} but got something else.`));
  } while (get$2("_sausageFights") === currentSausages); // Try again if we hit an NC that didn't take a turn
  if (kolmafia.getAutoAttack() !== 0) kolmafia.setAutoAttack(0);
  postCombatActions();
}
function doGhost() {
  if (!have$P($item`protonic accelerator pack`) || get$2("questPAGhost") === "unstarted") {
    return;
  }
  var ghostLocation = get$2("ghostLocation");
  if (!ghostLocation) return;
  propertyManager.setChoices(wanderer().getChoices(ghostLocation));
  freeFightOutfit({
    equip: $items`protonic accelerator pack`
  }, ghostLocation).dress();
  var currentTurncount;
  do {
    currentTurncount = kolmafia.myTurncount();
    garboAdventure(ghostLocation, Macro.ghostBustin());
  } while (get$2("ghostLocation") !== $location.none && currentTurncount === kolmafia.myTurncount());
  // Try again if we hit an NC that didn't take a turn
  postCombatActions();
}
var itemStealZones = [{
  location: $location`The Deep Dark Jungle`,
  monster: $monster`smoke monster`,
  item: $item`transdermal smoke patch`,
  dropRate: 1,
  maximize: [],
  requireMapTheMonsters: false,
  isOpen: () => get$2("_spookyAirportToday") || get$2("spookyAirportAlways"),
  openCost: () => 0,
  preReq: null
}, {
  location: $location`The Ice Hotel`,
  monster: $monster`ice bartender`,
  item: $item`perfect ice cube`,
  dropRate: 1,
  maximize: [],
  requireMapTheMonsters: false,
  isOpen: () => get$2("_coldAirportToday") || get$2("coldAirportAlways"),
  openCost: () => 0,
  preReq: null
}, {
  location: $location`The Haunted Library`,
  monster: $monster`bookbat`,
  item: $item`tattered scrap of paper`,
  dropRate: 1,
  maximize: ["99 monster level 100 max"],
  // Bookbats need up to +100 ML to survive the polar vortices
  requireMapTheMonsters: false,
  isOpen: () => have$P($item`[7302]Spookyraven library key`),
  openCost: () => 0,
  preReq: null
}, {
  location: $location`The Stately Pleasure Dome`,
  monster: $monster`toothless mastiff bitch`,
  item: $item`disintegrating spiky collar`,
  dropRate: 1,
  maximize: ["99 muscle 100 max"],
  // Ensure mastiff is at least 100 hp
  requireMapTheMonsters: false,
  isOpen: () => true,
  openCost: () => !have$P($effect`Absinthe-Minded`) ? kolmafia.mallPrice($item`tiny bottle of absinthe`) : 0,
  preReq: () => {
    if (!have$P($effect`Absinthe-Minded`)) {
      if (!have$P($item`tiny bottle of absinthe`)) {
        kolmafia.buy(1, $item`tiny bottle of absinthe`);
      }
      kolmafia.use($item`tiny bottle of absinthe`);
    }
  }
}, {
  location: $location`Twin Peak`,
  monster: $monsters`bearpig topiary animal, elephant (meatcar?) topiary animal, spider (duck?) topiary animal`,
  item: $item`rusty hedge trimmers`,
  dropRate: 0.5,
  maximize: ["99 monster level 11 max"],
  // Topiary animals need an extra 11 HP to survive polar vortices
  requireMapTheMonsters: false,
  isOpen: () => kolmafia.myLevel() >= 9 && get$2("chasmBridgeProgress") >= 30 && get$2("twinPeakProgress") >= 15,
  openCost: () => 0,
  preReq: null
}, {
  location: $location`The Hidden Temple`,
  monster: $monster`baa-relief sheep`,
  item: $item`stone wool`,
  requireMapTheMonsters: true,
  dropRate: 1,
  maximize: ["99 monster level 100 max"],
  // Sheeps need up to +100 ML to survive the polar vortices
  isOpen: () => get$2("lastTempleUnlock") === kolmafia.myAscensions(),
  openCost: () => 0,
  preReq: null
}].concat(_toConsumableArray($locations`Shadow Rift (The Ancient Buried Pyramid), Shadow Rift (The Hidden City), Shadow Rift (The Misspelled Cemetary)`.map(location => ({
  location,
  monster: $monster`shadow slab`,
  item: $item`shadow brick`,
  requireMapTheMonsters: false,
  dropRate: 1,
  isOpen: () => kolmafia.canAdventure(location),
  openCost: () => 0,
  preReq: null
}))));
function getBestItemStealZone() {
  var canMapMonster = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : availableMaps() > 0 || currentlyMapping();
  var targets = itemStealZones.filter(zone => zone.isOpen() && (canMapMonster || !zone.requireMapTheMonsters && asArray(zone.monster).some(m => !kolmafia.isBanished(m))));
  var vorticesAvail = have$P($item`industrial fire extinguisher`) ? Math.floor(get$2("_fireExtinguisherCharge") / 10) : 0;
  var hugsAvail = have$P($familiar`XO Skeleton`) ? clamp(11 - get$2("_xoHugsUsed"), 0, 11) : 0;
  var swoopsAvail = swoopsRemaining();
  var value = zone => {
    // We have to divide hugs by 2 - will likely use a banish as a free run so we will be alternating zones.
    return zone.dropRate * garboValue(zone.item) * (vorticesAvail + swoopsAvail + hugsAvail / 2) - zone.openCost();
  };
  return targets.length ? maxBy(targets, value) : null;
}
function setupItemStealZones() {
  // Haunted Library is full of free noncombats
  propertyManager.setChoices({
    163: 4,
    164: 3,
    165: 4,
    166: 1,
    888: 5,
    889: 5
  });
}
function itemStealOlfact(best) {
  // banishes and sniffs do not work in the shadow rifts
  return best.location.zone === "Shadow Rift" ? new Macro() : Macro.externalIf(have$P($skill`Transcendent Olfaction`) && get$2("_olfactionsUsed") < 1 && itemStealZones.every(zone => !asArray(zone.monster).includes(get$2("olfactedMonster"))), Macro.skill($skill`Transcendent Olfaction`)).externalIf(have$P($skill`Gallapagosian Mating Call`) && get$2("_gallapagosMonster") !== best.monster, Macro.skill($skill`Gallapagosian Mating Call`));
}
var haveEnoughPills = clamp(kolmafia.availableAmount($item`synthetic dog hair pill`), 0, 100) + clamp(kolmafia.availableAmount($item`distention pill`), 0, 100) + kolmafia.availableAmount($item`Map to Safety Shelter Grimace Prime`) < 200 && kolmafia.availableAmount($item`Map to Safety Shelter Grimace Prime`) < ESTIMATED_OVERDRUNK_TURNS;
function wantPills() {
  return have$P($item`Fourth of May Cosplay Saber`) && crateStrategy() !== "Saber" && haveEnoughPills;
}
function voidMonster() {
  if (get$2("cursedMagnifyingGlassCount") < 13 || !have$P($item`cursed magnifying glass`) || get$2("_voidFreeFights") >= 5) {
    return;
  }
  freeFightOutfit({
    equip: $items`cursed magnifying glass`
  }, "wanderer").dress();
  var targetLocation = wanderer().getTarget("wanderer").location;
  propertyManager.setChoices(wanderer().getChoices(targetLocation));
  garboAdventure(targetLocation, Macro.basicCombat());
  postCombatActions();
}
var BAD_CLL_MONSTERS = $monsters`alert mariachi`;
var freeKills = [{
  spec: {
    equip: $items`The Jokester's gun`
  },
  macro: $skill`Fire the Jokester's Gun`,
  used: () => get$2("_firedJokestersGun")
}, {
  spec: {
    equip: $items`Lil' Doctor™ bag`
  },
  macro: $skill`Chest X-Ray`,
  used: () => get$2("_chestXRayUsed") >= 3
}, {
  macro: $skill`Shattering Punch`,
  used: () => get$2("_shatteringPunchUsed") >= 3
}, {
  macro: $skill`Gingerbread Mob Hit`,
  used: () => get$2("_gingerbreadMobHitUsed")
}, {
  macro: $item`replica bat-oomerang`,
  used: () => get$2("_usedReplicaBatoomerang") >= 3
}];
var canUseSource = _ref5 => {
  var _spec$equip5;
  var spec = _ref5.spec,
    macro = _ref5.macro,
    used = _ref5.used;
  return ((spec === null || spec === void 0 || (_spec$equip5 = spec.equip) === null || _spec$equip5 === void 0 ? void 0 : _spec$equip5.every(i => have$P(i))) ?? have$P(macro)) && !used();
};
function findFreeKill() {
  return freeKills.find(canUseSource) ?? null;
}
function killRobortCreaturesForFree() {
  if (!have$P($familiar`Robortender`)) return;
  var currentHeads = kolmafia.availableAmount($item`fish head`);
  var freeKill = findFreeKill();
  while (freeKill && kolmafia.canAdventure($location`The Copperhead Club`) && availableMaps() > 0) {
    if (have$P($effect`Crappily Disguised as a Waiter`)) {
      setChoice(855, 4);
      garboAdventure($location`The Copperhead Club`, Macro.abort());
    }
    freeFightOutfit(_objectSpread2(_objectSpread2({}, freeKill.spec), {}, {
      familiar: $familiar`Robortender`
    }), {
      location: $location`The Copperhead Club`,
      target: $monster`Mob Penguin Capo`
    }).dress();
    withMacro(freeKill.macro instanceof kolmafia.Item ? Macro.item(freeKill.macro) : Macro.skill(freeKill.macro), () => {
      mapMonster($location`The Copperhead Club`, $monster`Mob Penguin Capo`);
      kolmafia.runCombat();
    }, true);
    freeKill = findFreeKill();
  }
  var _loop2 = function _loop2() {
    var roboTarget = findMonster(monster => !BAD_CLL_MONSTERS.includes(monster), monster => valueDrops(monster) + garboValue(dropFrom(monster)) * dropChance());
    if (!roboTarget) return 1; // break
    var regularTarget = findMonster(monster => !BAD_CLL_MONSTERS.includes(monster), valueDrops);
    var familiar = regularTarget === roboTarget ? freeFightFamiliar(regularTarget, {
      canChooseMacro: roboTarget.attributes.includes("FREE")
    }) : $familiar`Robortender`;
    freeFightOutfit(roboTarget.attributes.includes("FREE") ? {
      familiar
    } : _objectSpread2(_objectSpread2({}, freeKill.spec), {}, {
      familiar
    }), roboTarget).dress();
    withMacro(isFree(roboTarget) ? Macro.basicCombat() : freeKill.macro instanceof kolmafia.Item ? Macro.item(freeKill.macro) : Macro.skill(freeKill.macro), () => reminisce(roboTarget), true);
    freeKill = findFreeKill();
  };
  while (freeKill && have$t() && reminiscesLeft() > 1) {
    if (_loop2()) break;
  }
  if (!currentDrinks().includes($item`drive-by shooting`) && kolmafia.availableAmount($item`fish head`) > currentHeads && (!globalOptions.nobarf || targetingMeat()) && userConfirmDialog("Garbo managed to rustle up a fish head, would you like it to use it to make a drive-by shooting so you can benefit from your robortender? Sorry for flip-flopping on this, life is hard.", true)) {
    if (!have$P($item`drive-by shooting`)) kolmafia.create($item`drive-by shooting`);
    feed($item`drive-by shooting`);
    setBestLeprechaunAsMeatFamiliar();
  }
}

// Possible additional free fights from Eldritch Attunement
function estimatedAttunementTentacles() {
  var totalFreeFights = sum(freeFightSources, source => {
    var avail = source.tentacle ? source.available() : 0;
    return typeof avail === "number" ? avail : kolmafia.toInt(avail);
  }) + possibleFreeFightQuestTentacleFights() + expectedFreeGiantSandwormQuestFights();
  return clamp(totalFreeFights, 0, Math.max(0, 11 -
  // Capped at 11,
  get$2("_eldritchTentaclesFoughtToday") - (
  // minus what we've already fought,
  get$2("questL02Larva") !== "unstarted" ? 1 : 0) - (
  // minus one if we have access to Science Tent
  have$P($skill`Evoke Eldritch Horror`) && !get$2("_eldritchHorrorEvoked") // minus one if we have Evoke Eldritch Horror
  ? 1 : 0)));
}
function runShadowRiftTurn() {
  // we can probably have a better name
  if (get$2("encountersUntilSRChoice") === 0) return;
  if (willYachtzee() || get$2("rufusQuestType") === "items" || get$2("rufusQuestType") === "entity" // We can't handle bosses... yet
  ) {
    kolmafia.adv1(bestShadowRift(), -1, ""); // We shouldn't be using NC forcers
    return;
  }
  if (shouldClara("shadow waters")) {
    kolmafia.use($item`Clara's bell`);
  } else if (have$n() && totalAvailableCinch() >= 60) {
    var lastAcc = kolmafia.equippedItem($slot`acc3`);
    kolmafia.equip($slot`acc3`, $item`Cincho de Mayo`);
    while (currentCinch() < 60) {
      if (!freeRest()) throw new Error("We are out of free rests!");
    }
    kolmafia.useSkill($skill`Cincho: Fiesta Exit`);
    kolmafia.equip($slot`acc3`, lastAcc); // Re-equip last item
  } else if (have$P($item`Jurassic Parka`) && get$2("_spikolodonSpikeUses") < 5 && have$P($effect`Shadow Affinity`) && get$2("encountersUntilSRChoice") >= 2) {
    freeFightOutfit({
      shirt: $item`Jurassic Parka`
    }, bestShadowRift()).dress();
    kolmafia.cliExecute("parka spikolodon");
    var macro = Macro.skill($skill`Launch spikolodon spikes`).basicCombat();
    garboAdventureAuto(bestShadowRift(), macro);
  } else {
    kolmafia.adv1(bestShadowRift(), -1, ""); // We wanted to use NC forcers, but none are suitable now
  }
}

var photoBoothItems = $items`Sheriff badge, Sheriff pistol, Sheriff moustache, feather boa, oversized monocle on a stick, fake huge beard`;
var closetItems = $items`4-d camera, sand dollar, unfinished ice sculpture`;
var retrieveItems = $items`Half a Purse, seal tooth, The Jokester's gun`;
var latteRefreshed = false;
var snojoConfigured = false;
var speakeasyPoolTableVisited = false;

// For this valuation, we are using the rough approximated value of different
//   voting initiatives. They are relatively straghtforward:
//
//     - Meat Drop -- Add 30% meat to all fights
//     - Item Drop -- Add the value of extra garbage bags
//     - Advs      -- Add +1 of VOA
//  ==== BELOW THIS LINE, THEY ARE PRIORITY RATHER THAN VALUATION ===========
//     - FamXP     -- Helps level up your grey goose & pocket prof & robort, for loopers
//     - ML        -- Helps stasis longer
//     - Primestat -- Helps make combat easier
//     - Exp       -- Technically helps you cast more librams,  maybe?
//     - Meat -30% -- Lowers your meat drop; negative priority, so never used
//     - Item -15% -- Lowers your item drop; negative priority, so never used
//     - FamXP -2  -- Lowers your fam XP; negative priority, so never used

function voterSetup() {
  var initPriority = new Map([["Meat Drop: +30", 0.3 * ((1000 + songboomMeat()) * embezzlerFights() + targetMeat() * copyTargetCount() + baseMeat() * (estimatedGarboTurns() - copyTargetCount() - embezzlerFights()))], ["Item Drop: +15", 0.15 * (3 * 200 * 0.15 * (estimatedGarboTurns() - copyTargetCount()))], ["Adventures: +1", globalOptions.ascend ? 0 : get$2("valueOfAdventure")], ["Familiar Experience: +2", 8], ["Monster Level: +10", 5], [`${kolmafia.myPrimestat()} Percent: +25`, 3], [`Experience (${kolmafia.myPrimestat()}): +4`, 2], ["Meat Drop: -30", -2], ["Item Drop: -15", -2], ["Familiar Experience: -2", -2]]);
  if (!get$2("voteAlways") && !get$2("_voteToday")) {
    var availableInitiatives = new Map(Object.keys(kolmafia.votingBoothInitiatives(kolmafia.myClass(), kolmafia.myPath(), kolmafia.myDaycount())).map(init => {
      var val = initPriority.get(init) ?? 0;
      return [init, val];
    }));
    var initiativeValue = 2 * Math.max.apply(Math, _toConsumableArray(availableInitiatives.values()));
    var fightValue = 3 * globalOptions.prefs.valueOfFreeFight;
    var ballotValue = initiativeValue + fightValue;
    if (ballotValue > kolmafia.mallPrice($item`absentee voter ballot`) && acquire(1, $item`absentee voter ballot`, ballotValue, false)) {
      kolmafia.visitUrl(`inv_use.php?which=3&whichitem=9991&pwd=${kolmafia.myHash()}`);
    } else return;
  }

  // We do this funny logic on annoyed snake & slime blob because they both suck for profits
  // And because we don't want to lock people out of grabbing an outfit
  var voterValueTable = [{
    monster: $monster`terrible mutant`,
    value: garboValue($item`glob of undifferentiated tissue`) + 10
  }, {
    monster: $monster`angry ghost`,
    value: garboValue($item`ghostly ectoplasm`) * 1.11
  }, {
    monster: $monster`government bureaucrat`,
    value: garboValue($item`absentee voter ballot`) * 0.05 + 75 * 0.25 + 50
  }, {
    monster: $monster`annoyed snake`,
    value: kolmafia.gamedayToInt()
  }, {
    monster: $monster`slime blob`,
    value: 95 - kolmafia.gamedayToInt()
  }];
  kolmafia.visitUrl("place.php?whichplace=town_right&action=townright_vote");
  var votingMonsterPriority = voterValueTable.sort((a, b) => b.value - a.value).map(element => element.monster.name);
  var monsterVote = votingMonsterPriority.indexOf(get$2("_voteMonster1")) < votingMonsterPriority.indexOf(get$2("_voteMonster2")) ? 1 : 2;
  var voteLocalPriorityArr = [1, 2, 3, 4].map(index => ({
    urlString: index - 1,
    value: initPriority.get(get$2(`_voteLocal${index}`)) ?? (get$2(`_voteLocal${index}`).includes("-") ? -1 : 1)
  }));
  var init = maxBy(voteLocalPriorityArr, "value").urlString;
  kolmafia.visitUrl(`choice.php?option=1&whichchoice=1331&g=${monsterVote}&local[]=${init}&local[]=${init}`);
}
function pantogram() {
  if (!have$G() || havePants()) return;
  var pantogramValue;
  if (have$P($item`repaid diaper`) && have$P($familiar`Robortender`)) {
    var expectedBarfTurns = globalOptions.nobarf ? 0 : estimatedGarboTurns() - wanderingCopytargetsRemaining() - highMeatMonsterCount();
    pantogramValue = 100 * expectedBarfTurns;
  } else {
    var lepMult = findLeprechaunMultiplier(meatFamiliar());
    var lepBonus = 2 * lepMult + Math.sqrt(lepMult);
    var totalPantsValue = pants => get$1("Meat Drop", pants) + get$1("Familiar Weight", pants) * lepBonus;
    var alternativePants = kolmafia.Item.all().filter(item => have$P(item) && kolmafia.toSlot(item) === $slot`pants`).map(pants => totalPantsValue(pants));
    var bestPantsValue = Math.max.apply(Math, [0].concat(_toConsumableArray(alternativePants)));
    pantogramValue = (100 + 0.6 * baseMeat() - bestPantsValue * baseMeat() / 100) * estimatedGarboTurns();
  }
  var cloverPrice = Math.min.apply(Math, _toConsumableArray($items`ten-leaf clover, disassembled clover`.map(item => kolmafia.mallPrice(item))));
  if (cloverPrice + kolmafia.mallPrice($item`porquoise`) > pantogramValue) {
    return;
  }
  acquire(1, $item`porquoise`, pantogramValue - cloverPrice, false);
  if (!have$P($item`porquoise`)) return;
  kolmafia.retrieveItem($item`ten-leaf clover`);
  kolmafia.retrieveItem($item`bubblin' crude`);
  makePants(kolmafia.myPrimestat().toString(), "Sleaze Resistance: 2", "MP Regen Max: 15", "Drops Items: true", "Meat Drop: 60");
}
function nepQuest() {
  if (!(get$2("neverendingPartyAlways") || get$2("_neverendingPartyToday"))) return;

  // Do not enable hard mode
  if (kolmafia.haveEquipped($item`PARTY HARD T-shirt`)) {
    unequip($item`PARTY HARD T-shirt`);
  }
  if (get$2("_questPartyFair") === "unstarted") {
    kolmafia.visitUrl(kolmafia.toUrl($location`The Neverending Party`));
    if (["food", "booze", "trash", "dj"].includes(get$2("_questPartyFairQuest"))) {
      kolmafia.runChoice(1); // Accept quest
    } else {
      kolmafia.runChoice(2); // Decline quest
    }
  }
  if (["food", "booze"].includes(get$2("_questPartyFairQuest"))) {
    kolmafia.print("Gerald/ine quest!", HIGHLIGHT);
  }
}
function configureSnojo() {
  if (snojoConfigured) return;

  // if we're ascending, pick whichever consumable has the best price
  // each consumable takes 7 turns and we can spend 10 per day
  var options = new Map([[10 / 7 * garboValue($item`ancient medicinal herbs`), 1], [10 / 7 * garboValue($item`ice rice`), 2], [10 / 7 * garboValue($item`iced plum wine`), 3]]);
  // otherwise, assume we're in for at least five days and consider scrolls
  // we get 7 consumables in 5 days, plus a scroll
  if (!globalOptions.ascend) {
    if (get$2("snojoMuscleWins") < 50) {
      options.set((7 * garboValue($item`ancient medicinal herbs`) + garboValue($item`training scroll:  Shattering Punch`)) / 5, 1);
    }
    if (get$2("snojoMysticalityWins") < 50) {
      options.set((7 * garboValue($item`ice rice`) + garboValue($item`training scroll:  Snokebomb`)) / 5, 2);
    }
    if (get$2("snojoMoxieWins") < 50) {
      options.set((7 * garboValue($item`iced plum wine`) + garboValue($item`training scroll:  Shivering Monkey Technique`)) / 5, 3);
    }
  }
  var bestProfit = Math.max.apply(Math, _toConsumableArray(options.keys()));
  var option = options.get(bestProfit);
  if (option) {
    kolmafia.visitUrl("place.php?whichplace=snojo&action=snojo_controller");
    kolmafia.runChoice(option);
    snojoConfigured = true;
  }
}
function freddiesProfitable() {
  var valuesWithoutFreddy = luckyGoldRingDropValues(false, false);
  var valuesWithFreddy = luckyGoldRingDropValues(false, true);
  return sumNumbers(valuesWithFreddy) / valuesWithFreddy.length > sumNumbers(valuesWithoutFreddy) / valuesWithoutFreddy.length;
}
var DailyTasks = [{
  name: "Chibi Buddy",
  ready: () => have$P($item`ChibiBuddy™ (on)`) || have$P($item`ChibiBuddy™ (off)`),
  completed: () => get$2("_chibiChanged", true),
  do: () => kolmafia.cliExecute("chibi chat"),
  spendsTurn: false
}, {
  name: "Refresh Latte",
  ready: () => have$P($item`latte lovers member's mug`),
  completed: () => latteRefreshed,
  do: () => {
    kolmafia.visitUrl("main.php?latte=1", false);
    checkAndCorrectLatteMalformation();
    latteRefreshed = true;
  },
  spendsTurn: false
}, {
  name: "Unlock Cemetery",
  ready: () => kolmafia.guildStoreAvailable(),
  completed: () => kolmafia.canAdventure($location`The Unquiet Garves`),
  do: () => kolmafia.visitUrl("guild.php?place=scg"),
  limit: {
    skip: 3
  },
  // Sometimes need to cycle through some dialogue
  spendsTurn: false
}, {
  name: "Unlock Woods",
  ready: () => kolmafia.guildStoreAvailable() && have$P($item`bitchin' meatcar`),
  completed: () => kolmafia.canAdventure($location`The Spooky Forest`),
  do: () => {
    kolmafia.visitUrl("guild.php?place=paco");
    if (kolmafia.handlingChoice()) kolmafia.runChoice(1);
    kolmafia.visitUrl("woods.php"); // Without visiting woods, other visitUrls will not register woods as being unlocked.
  },
  limit: {
    skip: 3
  },
  // Sometimes need to cycle through some dialogue
  spendsTurn: false
}, {
  name: "Check Florist",
  ready: () => get$2("ownsFloristFriar"),
  completed: () => kolmafia.floristAvailable(),
  after: ["Daily/Unlock Woods"],
  do: () => {
    kolmafia.visitUrl("woods.php"); // Without visiting woods, other visitUrls will not register woods as being unlocked.
    kolmafia.visitUrl("place.php?whichplace=forestvillage&action=fv_friar");
    kolmafia.runChoice(4);
  },
  spendsTurn: false
}, {
  name: "Continuum Transfunctioner",
  ready: () => kolmafia.canAdventure($location`The Spooky Forest`),
  completed: () => have$P($item`continuum transfunctioner`),
  after: ["Daily/Unlock Woods"],
  do: () => {
    kolmafia.visitUrl("woods.php"); // Without visiting woods, other visitUrls will not register woods as being unlocked.
    kolmafia.visitUrl("place.php?whichplace=forestvillage&action=fv_mystic");
    kolmafia.runChoice(1); // Sure, old man.  Tell me all about it
    kolmafia.runChoice(1); // Against my better judgement, yes
    kolmafia.runChoice(1); // Er, sure, I guess so
  },
  spendsTurn: false
}, {
  name: "Free Goofballs",
  ready: () => kolmafia.canAdventure($location`The Spooky Forest`) && get$2("questL03Rat") !== "unstarted",
  completed: () => get$2("lastGoofballBuy") === kolmafia.myAscensions(),
  after: ["Daily/Unlock Woods"],
  do: () => {
    if (kolmafia.itemAmount($item`gloomy black mushroom`) > 0) {
      kolmafia.putCloset(kolmafia.itemAmount($item`gloomy black mushroom`), $item`gloomy black mushroom`);
    }
    kolmafia.visitUrl("woods.php"); // Without visiting woods, other visitUrls will not register woods as being unlocked.
    kolmafia.visitUrl("tavern.php?action=buygoofballs");
  },
  spendsTurn: false
}, {
  name: "Configure I Voted! Sticker",
  completed: () => have$P($item`"I Voted!" sticker`),
  do: voterSetup,
  spendsTurn: false
}, {
  name: "Configure Pantogram",
  ready: () => have$G(),
  completed: () => havePants(),
  do: pantogram,
  spendsTurn: false
}, {
  name: "Configure Fourth of May Cosplay Saber",
  ready: () => have$P($item`Fourth of May Cosplay Saber`),
  completed: () => get$2("_saberMod") !== 0,
  do: () => {
    kolmafia.visitUrl("main.php?action=may4");
    // Familiar weight
    kolmafia.runChoice(4);
  },
  spendsTurn: false
}, {
  name: "Bastille Battalion",
  ready: () => have$P($item`Bastille Battalion control rig`),
  completed: () => get$2("_bastilleGames") !== 0,
  do: () => {
    var potionOptions = [{
      identifier: "sharks",
      item: $item`sharkfin gumbo`
    }, {
      identifier: "lava",
      item: $item`boiling broth`
    }, {
      identifier: "truth",
      item: $item`interrogative elixir`
    }];
    var bestPotionIdentifier = maxBy(potionOptions, potion => garboValue(potion.item)).identifier;
    kolmafia.cliExecute(`bastille myst brutalist ${bestPotionIdentifier} gesture`);
  },
  spendsTurn: false
}, {
  name: "11th Precinct",
  ready: () => get$2("hasDetectiveSchool"),
  completed: () => get$2("_detectiveCasesCompleted") >= 3,
  do: () => kolmafia.cliExecute("Detective Solver.ash"),
  spendsTurn: false
}, {
  name: "Clan Photo Booth",
  ready: () => kolmafia.getClanLounge()["photo booth sized crate"] !== undefined && photoBoothItems.some(item => !have$P(item)),
  completed: () => get$2("_photoBoothEquipment") >= 3,
  do: () => photoBoothItems.forEach(item => get$2("_photoBoothEquipment") >= 3 || have$P(item) || kolmafia.retrieveItem(item)),
  spendsTurn: false
}, {
  name: "Getaway Campsite Buffs",
  ready: () => get$2("getawayCampsiteUnlocked"),
  completed: () => get$2("_campAwayCloudBuffs") + get$2("_campAwaySmileBuffs") === 4,
  do: () => kolmafia.visitUrl("place.php?whichplace=campaway&action=campaway_sky"),
  limit: {
    skip: 4
  },
  spendsTurn: false
}, {
  name: "Verify Horsery",
  completed: () => get$2("horseryAvailable"),
  do: () => {
    kolmafia.visitUrl("place.php?whichplace=town_right");
  },
  spendsTurn: false
}, {
  name: "Prepare Horsery",
  after: ["Verify Horsery"],
  ready: () => get$2("horseryAvailable"),
  completed: () => get$2("_horsery") === "dark horse",
  do: () => kolmafia.cliExecute("horsery dark"),
  spendsTurn: false
}, {
  name: "Beach Comb One-Day",
  ready: () => have$P($item`piece of driftwood`) && !have$P($item`Beach Comb`),
  completed: () => have$P($item`driftwood beach comb`),
  do: () => kolmafia.use($item`piece of driftwood`),
  spendsTurn: false
}, {
  name: "Beach Comb Buff",
  ready: () => available$2(),
  completed: () => !headAvailable("FAMILIAR") || freeCombs() < 1,
  do: () => tryHead($effect`Do I Know You From Somewhere?`),
  spendsTurn: false
}, {
  name: "Beach Comb Free Walks",
  ready: () => available$2(),
  completed: () => freeCombs() < 1,
  do: () => kolmafia.cliExecute(`combo ${11 - get$2("_freeBeachWalksUsed")}`),
  spendsTurn: false
}, {
  name: "Clan Fortune Buff",
  ready: () => have$P($item`Clan VIP Lounge key`) && kolmafia.getClanLounge()["Clan Carnival Game"] !== undefined,
  completed: () => get$2("_clanFortuneBuffUsed"),
  do: () => kolmafia.cliExecute("fortune buff meat"),
  spendsTurn: false
}, {
  name: $item`defective Game Grid token`.name,
  completed: () => get$2("_defectiveTokenUsed"),
  do: () => withStash([$item`defective Game Grid token`], () => kolmafia.use(1, $item`defective Game Grid token`)),
  spendsTurn: false
}, {
  name: $item`Glenn's golden dice`.name,
  ready: () => have$P($item`Glenn's golden dice`),
  completed: () => get$2("_glennGoldenDiceUsed"),
  do: () => kolmafia.use($item`Glenn's golden dice`),
  spendsTurn: false
}, {
  name: "Speakeasy pool table",
  ready: () => get$2("ownsSpeakeasy"),
  completed: () => speakeasyPoolTableVisited,
  do: () => {
    kolmafia.visitUrl("place.php?whichplace=speakeasy&action=olivers_pooltable");
    speakeasyPoolTableVisited = true;
  },
  outfit: {
    modifier: "Pool Skill"
  },
  spendsTurn: false
}, {
  name: "Clan pool table",
  ready: () => kolmafia.getClanLounge()["Clan pool table"] !== undefined,
  completed: () => get$2("_poolGames") >= 3,
  do: () => kolmafia.cliExecute("pool aggressive"),
  limit: {
    skip: 3
  },
  spendsTurn: false
}, {
  name: "Daycare Buff",
  ready: () => get$2("daycareOpen") || get$2("_daycareToday"),
  completed: () => get$2("_daycareSpa"),
  do: () => kolmafia.cliExecute("daycare mysticality"),
  spendsTurn: false
}, {
  name: "Daycare Free Scavenge",
  ready: () => get$2("daycareOpen") || get$2("_daycareToday"),
  completed: () => get$2("_daycareGymScavenges") > 0,
  do: () => kolmafia.cliExecute("daycare scavenge free"),
  spendsTurn: false
}, {
  name: $item`redwood rain stick`.name,
  ready: () => have$P($item`redwood rain stick`),
  completed: () => get$2("_redwoodRainStickUsed"),
  do: () => kolmafia.use($item`redwood rain stick`),
  spendsTurn: false
}, {
  name: "Witchess Puzzle Champ",
  ready: () => have$H(),
  completed: () => get$2("_witchessBuff"),
  do: () => kolmafia.cliExecute("up Puzzle Champ"),
  spendsTurn: false
}, {
  name: "Friar's Blessing",
  ready: () => questStep$1("questL06Friar") === 999,
  completed: () => get$2("friarsBlessingReceived"),
  do: () => kolmafia.cliExecute("friars familiar"),
  spendsTurn: false
}, {
  name: $item`The Legendary Beat`.name,
  ready: () => have$P($item`The Legendary Beat`),
  completed: () => get$2("_legendaryBeat"),
  do: () => kolmafia.use($item`The Legendary Beat`),
  spendsTurn: false
}, {
  name: $item`portable steam unit`.name,
  ready: () => have$P($item`portable steam unit`),
  completed: () => get$2("_portableSteamUnitUsed"),
  do: () => kolmafia.use($item`portable steam unit`),
  spendsTurn: false
}, {
  name: "Pastamancer Barrel Shrine",
  ready: () => get$2("barrelShrineUnlocked") && kolmafia.myClass() === $class`Pastamancer`,
  completed: () => get$2("_barrelPrayer"),
  do: () => kolmafia.cliExecute("barrelprayer buff"),
  spendsTurn: false
}, {
  name: "Clan Fortune Consults",
  ready: () => have$P($item`Clan VIP Lounge key`) && kolmafia.getClanLounge()["Clan Carnival Game"] !== undefined && kolmafia.isOnline("OnlyFax") && Clan.getWhitelisted().find(c => c.name === "Bonus Adventures from Hell") !== undefined,
  completed: () => get$2("_clanFortuneConsultUses") >= 3,
  do: () => {
    Clan.with("Bonus Adventures from Hell", () => kolmafia.cliExecute(`fortune ${kolmafia.getPlayerId("OnlyFax")}`));
    if (get$2("_clanFortuneConsultUses") < 3) kolmafia.wait(10);
  },
  limit: {
    skip: 3
  },
  spendsTurn: false
}, {
  name: "Summon Demon",
  ready: () => !!get$2("demonName2") && get$2("questL11Manor") === "finished",
  completed: () => get$2("demonSummoned"),
  do: () => kolmafia.cliExecute("summon Preternatural Greed"),
  spendsTurn: false
}, {
  name: "Source Terminal Enhance",
  ready: () => have$I(),
  completed: () => enhanceUsesRemaining() === 0,
  do: () => enhance($effect`meat.enh`),
  limit: {
    skip: 3
  },
  spendsTurn: false
}, {
  name: "Source Terminal Enquire",
  ready: () => have$I(),
  completed: () => get$2("sourceTerminalEnquiry") === "familiar.enq",
  do: () => enquiry($effect`familiar.enq`),
  spendsTurn: false
}, {
  name: "Mad Tea Party Buff",
  ready: () => !get$2("_madTeaParty"),
  completed: () => get$2("_madTeaParty"),
  do: () => kolmafia.cliExecute("hatter 22"),
  acquire: [{
    item: $item`filthy knitted dread sack`
  }],
  effects: [$effect`Down the Rabbit Hole`],
  spendsTurn: false
}, {
  name: "SongBoom Buff",
  ready: () => have$C() && songChangesLeft() > 0 && kolmafia.myInebriety() <= kolmafia.inebrietyLimit(),
  completed: () => song() === "Total Eclipse of Your Meat",
  do: () => setSong("Total Eclipse of Your Meat"),
  spendsTurn: false
}, {
  name: "SongBoom Buff (Drunk)",
  ready: () => have$C() && songChangesLeft() > 0 && kolmafia.myInebriety() > kolmafia.inebrietyLimit(),
  completed: () => song() === "Food Vibrations",
  do: () => setSong("Food Vibrations"),
  spendsTurn: false
}, {
  name: "Set Mind Control Device",
  ready: () => kolmafia.canadiaAvailable() || kolmafia.gnomadsAvailable() || have$P($item`detuned radio`),
  completed: () => kolmafia.currentMcd() === (kolmafia.canadiaAvailable() ? 11 : 10),
  do: () => kolmafia.changeMcd(kolmafia.canadiaAvailable() ? 11 : 10),
  spendsTurn: false
}, {
  name: "Implement [glitch season reward name]",
  ready: () => have$P($item`[glitch season reward name]`),
  completed: () => get$2("_glitchItemImplemented"),
  do: () => kolmafia.use($item`[glitch season reward name]`),
  spendsTurn: false
}, {
  name: "Use BittyCar MeatCart",
  ready: () => get$2("_bittycar") !== "meatcar",
  completed: () => get$2("_bittycar") === "meatcar",
  do: () => withStash([$item`BittyCar MeatCar`], () => kolmafia.use(1, $item`BittyCar MeatCar`)),
  spendsTurn: false
}, {
  name: "Use BittyCar SoulCar",
  ready: () => get$2("_bittycar") !== "meatcar" && get$2("_bittycar") !== "soulcar",
  completed: () => get$2("_bittycar") === "soulcar",
  do: () => withStash([$item`BittyCar SoulCar`], () => kolmafia.use(1, $item`BittyCar SoulCar`)),
  spendsTurn: false
}, {
  name: "Holiday Eldritch Attunement",
  ready: () => kolmafia.holiday().includes("Generic Summer Holiday") && !have$P($effect`Eldritch Attunement`) && estimatedAttunementTentacles() * globalOptions.prefs.valueOfFreeFight > get$2("valueOfAdventure"),
  completed: () => have$P($effect`Eldritch Attunement`),
  do: () => kolmafia.adv1($location`Generic Summer Holiday Swimming!`),
  acquire: [{
    item: $item`water wings`
  }],
  outfit: () => kolmafia.myInebriety() > kolmafia.inebrietyLimit() && have$P($item`Drunkula's wineglass`) && kolmafia.canEquip($item`Drunkula's wineglass`) ? {
    offhand: $item`Drunkula's wineglass`,
    acc1: $item`water wings`,
    avoid: $items`June cleaver, cursed magnifying glass, Kramco Sausage-o-Matic™`
  } : {
    acc1: $item`water wings`,
    avoid: $items`June cleaver, cursed magnifying glass, Kramco Sausage-o-Matic™`
  },
  spendsTurn: false,
  combat: new GarboStrategy(() => Macro.if_([$monster`giant rubber spider`, $monster`time-spinner prank`].concat(_toConsumableArray(getTodaysHolidayWanderers())), Macro.basicCombat()).abortWithMsg("Unexpected combat encounter while attempting to get Eldritch Attunment from Generic Summer Holiday"))
}, {
  name: "Check Neverending Party Quest",
  ready: () => (get$2("neverendingPartyAlways") || get$2("_neverendingPartyToday")) && get$2("_questPartyFair") === "unstarted",
  completed: () => get$2("_questPartyFair") !== "unstarted",
  do: nepQuest,
  outfit: () => kolmafia.myInebriety() > kolmafia.inebrietyLimit() && have$P($item`Drunkula's wineglass`) && kolmafia.canEquip($item`Drunkula's wineglass`) ? {
    offhand: $item`Drunkula's wineglass`,
    avoid: $items`June cleaver, PARTY HARD T-shirt`
  } : {
    avoid: $items`June cleaver, PARTY HARD T-shirt`
  },
  spendsTurn: false
}, {
  name: "Check Barf Mountain Quest",
  ready: () => get$2("stenchAirportAlways") || get$2("_stenchAirportToday"),
  completed: () => !attemptCompletingBarfQuest(),
  do: checkBarfQuest,
  spendsTurn: false
}, {
  name: "Configure Snojo",
  ready: () => get$2("snojoAvailable") && get$2("_snojoFreeFights") < 10,
  completed: () => snojoConfigured,
  do: configureSnojo,
  spendsTurn: false
},
// Final tasks
{
  name: "Closet Items",
  ready: () => closetItems.some(item => kolmafia.itemAmount(item)),
  completed: () => closetItems.every(item => kolmafia.itemAmount(item) === 0),
  do: () => closetItems.forEach(item => kolmafia.putCloset(kolmafia.itemAmount(item), item)),
  spendsTurn: false
}, {
  name: "Closet Hobo Nickels",
  ready: () => have$P($familiar`Hobo Monkey`) || have$P($item`hobo nickel`, 1000),
  completed: () => kolmafia.itemAmount($item`hobo nickel`) === 0,
  do: () => kolmafia.putCloset(kolmafia.itemAmount($item`hobo nickel`), $item`hobo nickel`),
  spendsTurn: false
}, {
  name: "Closet Freddies",
  ready: () => !freddiesProfitable(),
  completed: () => kolmafia.itemAmount($item`Freddy Kruegerand`) === 0,
  do: () => kolmafia.putCloset(kolmafia.itemAmount($item`Freddy Kruegerand`), $item`Freddy Kruegerand`),
  spendsTurn: false
}, {
  name: "Retrieve Items",
  ready: () => retrieveItems.some(item => kolmafia.itemAmount(item) === 0),
  completed: () => retrieveItems.every(item => kolmafia.itemAmount(item) > 0),
  do: () => retrieveItems.forEach(item => kolmafia.retrieveItem(item)),
  spendsTurn: false
}, {
  name: "Dispose of Park Garbage",
  ready: () => realmAvailable("stench") && 3 * garboValue($item`FunFunds™`) > kolmafia.mallPrice($item`bag of park garbage`),
  completed: () => get$2("_dinseyGarbageDisposed"),
  do: () => {
    kolmafia.print("Disposing of garbage.", HIGHLIGHT);
    kolmafia.retrieveItem($item`bag of park garbage`);
    kolmafia.visitUrl("place.php?whichplace=airport_stench&action=airport3_tunnels");
    kolmafia.runChoice(6);
    kolmafia.cliExecute("refresh inv");
  },
  spendsTurn: false
}, {
  name: "Volcano Quest",
  ready: () => realmAvailable("hot"),
  completed: () => get$2("_volcanoItemRedeemed"),
  do: checkVolcanoQuest,
  spendsTurn: false
}, {
  name: "Free Volcoino",
  ready: () => realmAvailable("hot"),
  completed: () => get$2("_infernoDiscoVisited"),
  do: () => {
    kolmafia.visitUrl("place.php?whichplace=airport_hot&action=airport4_zone1");
    kolmafia.runChoice(7);
  },
  acquire: () => $items`smooth velvet pocket square, smooth velvet socks, smooth velvet hat, smooth velvet shirt, smooth velvet hanky, smooth velvet pants`.map(x => ({
    item: x
  })),
  outfit: {
    modifier: "disco style"
  },
  spendsTurn: false
}, {
  name: "Use Walkie Talkie for Ghost",
  ready: () => kolmafia.mallPrice($item`almost-dead walkie-talkie`) < globalOptions.prefs.valueOfFreeFight && get$2("nextParanormalActivity") <= kolmafia.totalTurnsPlayed(),
  completed: () => have$P($item`protonic accelerator pack`) || get$2("questPAGhost") === "started",
  do: () => {
    if (acquire(1, $item`almost-dead walkie-talkie`, globalOptions.prefs.valueOfFreeFight, false)) {
      kolmafia.use($item`almost-dead walkie-talkie`);
    }
  },
  spendsTurn: false
}];
var DailyQuest = {
  name: "Daily",
  tasks: DailyTasks
};

function drivebyValue() {
  var targetCount = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : 0;
  var targets = targetCount;
  var tourists = (estimatedGarboTurns() - targets) * FarmingStrategy.ncAdjustment();
  var marginalRoboWeight = 50;
  var meatPercentDelta = Math.sqrt(220 * 2 * marginalRoboWeight) - Math.sqrt(220 * 2 * marginalRoboWeight) + 2 * marginalRoboWeight;
  return meatPercentDelta / 100 * (targetMeat() * targets + baseMeat() * tourists);
}
function entendreValue() {
  var targetCount = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : 0;
  var targets = targetCount;
  var tourists = (estimatedGarboTurns() - targets) * FarmingStrategy.ncAdjustment();
  var marginalRoboWeight = 50;
  var itemPercent = Math.sqrt(55 * marginalRoboWeight) + marginalRoboWeight - 3;
  var garbageBagsDropRate = 0.15 * 3; // 3 bags each with a 15% drop chance

  return itemPercent / 100 * (garbageBagsDropRate * tourists * garbageTouristRatio);
}
function worthFeedingRobortender() {
  if (!globalOptions.nobarf) return true;
  if (isFree(globalOptions.target)) return false;
  return (globalOptions.target.maxMeat + globalOptions.target.minMeat) / 2 >= 300;
}
function prepRobortender() {
  if (!have$P($familiar`Robortender`)) return;
  var targetCount = highMeatMonsterCount("Scepter"); // Scepter can cause circular logic
  var roboDrinks = {
    "Drive-by shooting": {
      priceCap: drivebyValue(targetCount),
      mandatory: true
    },
    Newark: {
      priceCap: newarkValue() * 0.25 * estimatedGarboTurns(),
      mandatory: false
    },
    "Feliz Navidad": {
      priceCap: felizValue() * 0.25 * estimatedGarboTurns(),
      mandatory: false
    },
    "Bloody Nora": {
      priceCap: get$2("_envyfishEggUsed") ? targetMeat() * (0.5 + (4 + Math.sqrt(110 / 100)) * 30 / 100) + baseMeat() * (0.5 + (4 + Math.sqrt(110 / 100)) * 30 / 100) * estimatedGarboTurns() : FarmingStrategy.isUnderwater() ? baseMeat() * (0.5 + (4 + Math.sqrt(110 / 100)) * 30 / 100) * estimatedGarboTurns() : 0,
      mandatory: false
    },
    "Single entendre": {
      priceCap: entendreValue(targetCount),
      mandatory: false
    }
  };
  var _loop = function _loop() {
      var _Object$entries$_i = _slicedToArray(_Object$entries[_i], 2),
        drinkName = _Object$entries$_i[0],
        _Object$entries$_i$ = _Object$entries$_i[1],
        priceCap = _Object$entries$_i$.priceCap,
        mandatory = _Object$entries$_i$.mandatory;
      if (get$2("_roboDrinks").toLowerCase().includes(drinkName.toLowerCase())) {
        return 0; // continue
      }
      kolmafia.useFamiliar($familiar`Robortender`);
      var drink = kolmafia.toItem(drinkName);
      if (kolmafia.retrievePrice(drink) > priceCap) {
        if (mandatory) {
          setBestLeprechaunAsMeatFamiliar();
          if (!userConfirmDialog(`Garbo cannot find a reasonably priced drive-by-shooting (price cap: ${priceCap}), and will not be using your robortender. Is that cool with you?`, true)) {
            kolmafia.abort("Alright, then, I guess you should try to find a reasonbly priced drive-by-shooting. Or do different things with your day.");
          }
          return 1; // break
        }
        return 0; // continue
      }
      withProperty("autoBuyPriceLimit", priceCap, () => kolmafia.retrieveItem(1, drink));
      if (have$P(drink)) feed(drink);
    },
    _ret;
  for (var _i = 0, _Object$entries = Object.entries(roboDrinks); _i < _Object$entries.length; _i++) {
    _ret = _loop();
    if (_ret === 0) continue;
    if (_ret === 1) break;
  }
}
var DailyFamiliarTasks = [{
  name: "Prepare Shorter-Order Cook",
  ready: () => have$P($familiar`Shorter-Order Cook`) && have$P($item`blue plate`),
  completed: () => kolmafia.familiarEquippedEquipment($familiar`Shorter-Order Cook`) === $item`blue plate`,
  do: () => {
    kolmafia.retrieveItem($item`blue plate`);
    kolmafia.equip($familiar`Shorter-Order Cook`, $item`blue plate`);
  },
  spendsTurn: false
}, {
  name: "Prepare Robortender",
  ready: () => have$P($familiar`Robortender`) && worthFeedingRobortender(),
  completed: () => get$2("_roboDrinks").toLowerCase().includes("drive-by shooting"),
  do: prepRobortender,
  spendsTurn: false
}, {
  name: "Acquire box of Familiar Jacks",
  ready: () => have$P($familiar`Cornbeefadon`),
  completed: () => have$P($item`box of Familiar Jacks`) || have$P($item`amulet coin`),
  do: () =>
  // TODO: acquire max price calculation
  acquire(1, $item`box of Familiar Jacks`, get$2("autoBuyPriceLimit")),
  spendsTurn: false
}, {
  name: "Acquire amulet coin",
  ready: () => have$P($familiar`Cornbeefadon`) && have$P($item`box of Familiar Jacks`) && !have$f() && amuletCoinValue() >= kolmafia.mallPrice($item`box of Familiar Jacks`),
  completed: () => have$P($item`amulet coin`),
  do: () => {
    kolmafia.use($item`box of Familiar Jacks`);
  },
  outfit: {
    familiar: $familiar`Cornbeefadon`
  },
  spendsTurn: false
}, {
  name: "Acquire box of old Crimbo decorations",
  ready: () => have$P($familiar`Crimbo Shrub`),
  completed: () => have$P($item`box of old Crimbo decorations`),
  do: () => {
    kolmafia.useFamiliar($familiar`Crimbo Shrub`);
  },
  outfit: {
    familiar: $familiar`Crimbo Shrub`
  },
  spendsTurn: false
}, {
  name: "Decorate Crimbo Shrub",
  ready: () => have$P($item`box of old Crimbo decorations`),
  completed: () => get$2("_shrubDecorated"),
  do: () => decorate(kolmafia.myPrimestat().toString(), "Stench Damage", kolmafia.hippyStoneBroken() ? "PvP Fights" : "HP Regen", "Red Ray"),
  outfit: {
    familiar: $familiar`Crimbo Shrub`
  },
  spendsTurn: false
}, {
  name: "Mummery Meat",
  ready: () => have$P($item`mumming trunk`),
  completed: () => get$2("_mummeryMods").includes("Meat Drop"),
  do: () => kolmafia.cliExecute("mummery meat"),
  outfit: {
    familiar: meatFamiliar()
  },
  spendsTurn: false
}, {
  name: "Mummery Item",
  ready: () => have$P($item`mumming trunk`) && have$P($familiar`Trick-or-Treating Tot`),
  completed: () => get$2("_mummeryMods").includes("Item Drop"),
  do: () => kolmafia.cliExecute("mummery item"),
  outfit: {
    familiar: $familiar`Trick-or-Treating Tot`
  },
  spendsTurn: false
}, {
  name: "Moveable feast",
  ready: () => have$P($item`moveable feast`) || globalOptions.prefs.stashClan !== "none",
  completed: () => get$2("_feastUsed") > 0,
  do: () => {
    withStash($items`moveable feast`, () => {
      if (have$P($item`moveable feast`)) {
        [].concat(_toConsumableArray($familiars`Pocket Professor, Frumious Bandersnatch, Pair of Stomping Boots`), [meatFamiliar()]).forEach(tryFeast);
      }
    });
  },
  spendsTurn: false
}];
var DailyFamiliarsQuest = {
  name: "Daily Familiars",
  tasks: DailyFamiliarTasks
};

var instruments = [{
  instrument: "Apriling band tuba",
  value: () => realmAvailable("sleaze") ? (20000 - get$2("valueOfAdventure")) * 3 : 0
}, {
  instrument: "Apriling band quad tom",
  value: () => (globalOptions.prefs.valueOfFreeFight + 0.02 * garboValue($item`spice melange`)) * 3
}, {
  instrument: "Apriling band saxophone",
  value: () => getBestLuckyAdventure().value() * 3
}, {
  instrument: "Apriling band piccolo",
  value: () => Math.max.apply(Math, [0].concat(_toConsumableArray(getExperienceFamiliars("barf").map(_ref => {
    var familiar = _ref.familiar,
      expectedValue = _ref.expectedValue;
    var usesAllowed = clamp(Math.floor((400 - familiar.experience) / 40), 0, 3);
    return expectedValue / estimatedBarfExperience() * 40 * usesAllowed;
  }))))
}];
function getBestAprilInstruments() {
  var available = clamp(2 - get$2("_aprilBandInstruments"), 0, 2);
  return instruments.filter(_ref2 => {
    var instrument = _ref2.instrument;
    return !have$P(kolmafia.toItem(instrument));
  }).sort((a, b) => b.value() - a.value()).splice(0, available).map(_ref3 => {
    var instrument = _ref3.instrument;
    return instrument;
  });
}

function archaeologySpadeTask() {
  return {
    name: "Use Archaeologist's Spade",
    ready: () => have$P($item`Archaeologist's Spade`) && get$2("_archSpadeDigs", 0) < 11,
    completed: () => get$2("_archSpadeDigs", 0) >= 11,
    do: () => {
      var spadeTargets = [{
        price: garboAverageValue.apply(void 0, _toConsumableArray($items`ancient Pork Elf pottery shard, dinosaur bone fragment, 2015 landfill detritus`)),
        tuner: undefined
      }, {
        price: garboValue($item`ancient Pork Elf pottery shard`),
        tuner: $item`Pork Elf neti pot`
      }, {
        price: garboValue($item`dinosaur bone fragment`),
        tuner: $item`giant gnawing bone`
      }, {
        price: garboValue($item`2015 landfill detritus`),
        tuner: $item`Fleek™ mascara`
      }].filter(t => !t.tuner || have$P(t.tuner));
      if (spadeTargets.length > 0) {
        var target = maxBy(spadeTargets, "price");
        if (target.tuner) kolmafia.use(target.tuner);
      }
      directlyUse($item`Archaeologist's Spade`);
      while ("2" in kolmafia.availableChoiceOptions()) {
        kolmafia.runChoice(2);
      }
      kolmafia.visitUrl("main.php");
    },
    spendsTurn: false
  };
}

// Stats assigned a value of 1, to discern from the Truly Useless
// MP restore assigned a value of 2, because it's better than stats!
var MAYAM_RING_VALUES = {
  yam1: () => garboValue($item`yam`),
  sword: () => 1,
  vessel: () => 2,
  eye: () => effectValue($effect`Big Eyes`, 100),
  fur: () => Math.max.apply(Math, [0].concat(_toConsumableArray(getExperienceFamiliars("free").map(_ref => {
    var expectedValue = _ref.expectedValue;
    return expectedValue / 12;
  })))) * 100,
  chair: () => have$n() ? 3 * 5 * felizValue() : 0,
  // TODO Account for reaching a Yachtzee NC breakpoint
  yam2: () => garboValue($item`yam`),
  lightning: () => 1,
  bottle: () => 0,
  wood: () => 0,
  wall: () => 0,
  cheese: () => garboValue($item`goat cheese`),
  eyepatch: () => 1,
  meat: () => clamp(kolmafia.myLevel() * 100, 100, 1500),
  yam3: () => garboValue($item`yam`),
  yam4: () => garboValue($item`yam`),
  explosion: () => 0,
  clock: () => 5 * get$2("valueOfAdventure")
};
function valueSymbol(symbol) {
  return MAYAM_RING_VALUES[symbol]();
}
function valueResonance(combination) {
  var result = getResonanceResult(combination);
  if (!result) return 0;
  if (result instanceof kolmafia.Item) {
    if (result === $item`yamtility belt`) return 0; // yamtilityValue();
    return garboValue(result);
  }
  return effectValue(result, 30);
}
function valueCombination(combination) {
  return sum(toCombination([combination]), valueSymbol) + valueResonance(combination);
}
function getAvailableResonances(forbiddenSymbols, indexCap) {
  return RESONANCE_KEYS.filter((combination, index) => index < indexCap && !toCombination([combination]).some(sym => forbiddenSymbols.includes(sym)));
}
function getBestAvailableSymbolFromRing(ring, forbiddenSymbols) {
  return maxBy(RINGS[ring].filter(sym => !forbiddenSymbols.includes(sym)), valueSymbol);
}
function getBestGreedyCombination(forbiddenSymbols) {
  return toCombinationString([getBestAvailableSymbolFromRing(0, forbiddenSymbols), getBestAvailableSymbolFromRing(1, forbiddenSymbols), getBestAvailableSymbolFromRing(2, forbiddenSymbols), getBestAvailableSymbolFromRing(3, forbiddenSymbols)]);
}
var resonanceIndex = resonance => RESONANCE_KEYS.indexOf(resonance);
function expandCombinationGroup(group) {
  var forbiddenSymbols = [].concat(_toConsumableArray(group.flatMap(combinationString => toCombination([combinationString]))), _toConsumableArray(symbolsUsed()));
  return [].concat(_toConsumableArray(getAvailableResonances(forbiddenSymbols, Math.min.apply(Math, _toConsumableArray(group.map(resonanceIndex)))).map(resonance => [].concat(_toConsumableArray(group), [resonance]))), [[].concat(_toConsumableArray(group), [getBestGreedyCombination(forbiddenSymbols)])]);
}
function getBestMayamCombinations() {
  return maxBy(new Array(remainingUses()).fill(null).reduce(acc => acc.flatMap(combinationGroup => expandCombinationGroup(combinationGroup)), [[]]), group => sum(group, valueCombination));
}
function mayamCalendarSummon() {
  return {
    name: "Mayam Summons",
    completed: () => remainingUses() === 0,
    ready: () => have$i(),
    do: () => {
      var startingFamiliar = kolmafia.myFamiliar();
      var _iterator = _createForOfIteratorHelper(getBestMayamCombinations()),
        _step;
      try {
        for (_iterator.s(); !(_step = _iterator.n()).done;) {
          var combination = _step.value;
          if (combination.includes("fur")) {
            var famList = getExperienceFamiliars("free");
            var bestFamiliar = famList.length > 0 ? maxBy(famList, "expectedValue").familiar : meatFamiliar();
            kolmafia.useFamiliar(bestFamiliar);
          }
          submit(combination);
        }
      } catch (err) {
        _iterator.e(err);
      } finally {
        _iterator.f();
      }
      kolmafia.useFamiliar(startingFamiliar);
    },
    spendsTurn: false
  };
}

var SummonTomes = $skills`Summon Snowcones, Summon Stickers, Summon Sugar Sheets, Summon Rad Libs, Summon Smithsness`;
var Wads = $items`twinkly wad, cold wad, stench wad, hot wad, sleaze wad, spooky wad`;
var _shouldClearRufusQuest = null;
var smashedBarrels = false;
function drawBestCards() {
  var cardsLeft = Math.floor(3 - get$2("_deckCardsDrawn") / 5);
  var cardsSeen = get$2("_deckCardsSeen").toLowerCase();
  var bestCards = [{
    card: "Island",
    item: $item`blue mana`
  }, {
    card: "Ancestral Recall",
    item: $item`blue mana`
  }, {
    card: "Plains",
    item: $item`white mana`
  }, {
    card: "Healing Salve",
    item: $item`white mana`
  }, {
    card: "Swamp",
    item: $item`black mana`
  }, {
    card: "Dark Ritual",
    item: $item`black mana`
  }, {
    card: "Mountain",
    item: $item`red mana`
  }, {
    card: "Lightning bolt",
    item: $item`red mana`
  }, {
    card: "Forest",
    item: $item`green mana`
  }, {
    card: "Giant Growth",
    item: $item`green mana`
  }, {
    card: "Gift Card",
    item: $item`gift card`
  }, {
    card: "Mickey",
    item: $item`1952 Mickey Mantle card`
  }].filter(_ref => {
    var card = _ref.card;
    return !cardsSeen.includes(card.toLowerCase());
  }).sort((a, b) => garboValue(b.item) - garboValue(a.item)).splice(0, cardsLeft).map(_ref2 => {
    var card = _ref2.card;
    return card;
  });
  var _iterator = _createForOfIteratorHelper(bestCards),
    _step;
  try {
    for (_iterator.s(); !(_step = _iterator.n()).done;) {
      var card = _step.value;
      kolmafia.cliExecute(`cheat ${card}`);
    }
  } catch (err) {
    _iterator.e(err);
  } finally {
    _iterator.f();
  }
}
function bestExtrude() {
  return maxBy($items`browser cookie, hacked gibson`, garboValue);
}
function pickCargoPocket() {
  var picked = kolmafia.pickedPockets();
  var items = kolmafia.itemPockets();
  var meats = kolmafia.meatPockets();
  var scraps = kolmafia.scrapPockets();
  function pocketValue(pocket) {
    var value = 0;
    if (pocket in picked) {
      return value;
    }
    if (pocket in items) {
      value += sum(Object.entries(kolmafia.pocketItems(pocket)), _ref3 => {
        var _ref4 = _slicedToArray(_ref3, 2),
          item = _ref4[0],
          count = _ref4[1];
        return garboValue(kolmafia.toItem(item)) * count;
      });
    }
    if (pocket in meats) {
      value += sum(Object.values(kolmafia.pocketMeat(pocket)), x => parseInt(x));
    }
    if (pocket in scraps) {
      value += 200;
    }
    return value;
  }
  var pockets = [];
  for (var i = 1; i <= 666; i++) {
    var value = pocketValue(i);
    if (value > 0) {
      pockets.push([i, value]);
    }
  }
  if (pockets.length > 0) {
    kolmafia.cliExecute(`cargo ${Math.trunc(maxBy(pockets, 1)[0])}`);
  }
}
function bestDevilerCandy() {
  // These are replenishable notrade nodiscard items that we would prefer to use
  var priorityUntradeableNoDiscardList = $items`sugar shotgun, sugar shillelagh, sugar shank, sugar chapeau, sugar shorts, sugar shield, sugar shirt`.filter(i => kolmafia.itemAmount(i) > 1);
  if (priorityUntradeableNoDiscardList.length > 0) {
    return maxBy(priorityUntradeableNoDiscardList, kolmafia.itemAmount);
  }
  var bestCandyFromMall = maxBy(kolmafia.Item.all().filter(i => i.candy && i.tradeable && kolmafia.mallPrice(i) > 0), kolmafia.mallPrice, true);
  // These are notrade items that have an autosell value that we don't mind using if they are the cheapest
  var safeUntradeableCandies = $items`Comet Pop, black candy heart, peanut brittle shield`;
  // Find the best candy from inventory, accounting for value of autosell when mall min
  var inventoryCandies = kolmafia.Item.all().filter(i => i.candy && (have$P(i) && !i.tradeable ? safeUntradeableCandies.includes(i) : true));
  var bestInventoryCandy = (() => {
    if (inventoryCandies.length === 0) return null;
    var naiveBest = maxBy(inventoryCandies, garboValue, true);
    return maxBy(inventoryCandies.filter(c => garboValue(c) === garboValue(naiveBest)), kolmafia.itemAmount);
  })();
  return !!bestInventoryCandy && garboValue(bestInventoryCandy) < kolmafia.mallPrice(bestCandyFromMall) ? bestInventoryCandy : bestCandyFromMall;
}
var cachedbestDevilerCandy = null;
function getBestDevilerCandy() {
  if (cachedbestDevilerCandy === null) {
    cachedbestDevilerCandy = bestDevilerCandy();
  }
  return cachedbestDevilerCandy;
}
var chooseAprilFamiliar = () => {
  var experienceFamiliars = getExperienceFamiliars("free").filter(_ref5 => {
    var familiar = _ref5.familiar;
    return familiar.experience <= 360;
  });
  if (experienceFamiliars.length) {
    return maxBy(experienceFamiliars, "expectedValue").familiar;
  }
  return meatFamiliar().experience <= 360 ? meatFamiliar() : null;
};
var SummonTasks = _toConsumableArray(SummonTomes.map(skill => ({
  name: `${skill}`,
  ready: () => have$P(skill),
  completed: () => skill.dailylimit === 0,
  do: () => kolmafia.useSkill(skill, skill.dailylimit),
  spendsTurn: false
})));
var triedForest = false;
var DailyItemTasks = [{
  name: "Summon Clip Art",
  ready: () => have$P($skill`Summon Clip Art`),
  completed: () => $skill`Summon Clip Art`.dailylimit === 0,
  do: () => {
    var best = $item.none;
    for (var itemId = 5224; itemId <= 5283; itemId++) {
      var current = kolmafia.Item.get(`[${itemId}]`);
      if (garboValue(current) > garboValue(best)) {
        best = current;
      }
    }
    if (best !== $item.none) {
      kolmafia.cliExecute(`try; create ${$skill`Summon Clip Art`.dailylimit} ${best}`);
    }
  },
  spendsTurn: false
}, {
  name: "Defective Game Grid Token",
  ready: () => get$2("lastArcadeAscension") === kolmafia.myAscensions() || have$P($item`Game Grid token`) || have$P($item`Game Grid ticket`),
  completed: () => get$2("_defectiveTokenChecked"),
  do: () => {
    kolmafia.visitUrl("place.php?whichplace=arcade", false);
    kolmafia.visitUrl("place.php?whichplace=arcade&action=arcade_plumber", false);
  },
  spendsTurn: false
}, {
  name: "2002 Mr. Store",
  ready: () => have$P($item`2002 Mr. Store Catalog`),
  completed: () => get$2("availableMrStore2002Credits") === 0 && get$2("_2002MrStoreCreditsCollected"),
  do: () => {
    var bestItem = maxBy(kolmafia.Item.all().filter(i => kolmafia.sellsItem($coinmaster`Mr. Store 2002`, i)), garboValue);
    kolmafia.buy($coinmaster`Mr. Store 2002`, get$2("availableMrStore2002Credits"), bestItem);
  },
  spendsTurn: false
}, {
  name: "Spend Sept-Ember Embers",
  ready: () => have$P($item`Sept-Ember Censer`) && globalOptions.ascend,
  completed: () => get$2("availableSeptEmbers") === 0,
  do: () => {
    var itemsWithCosts = kolmafia.Item.all().filter(i => kolmafia.sellsItem($coinmaster`Sept-Ember Censer`, i)).map(item => ({
      item,
      cost: kolmafia.sellPrice($coinmaster`Sept-Ember Censer`, item),
      value: garboValue(item) / kolmafia.sellPrice($coinmaster`Sept-Ember Censer`, item)
    }));
    while (get$2("availableSeptEmbers") > 0) {
      var _maxBy = maxBy(itemsWithCosts.filter(_ref6 => {
          var cost = _ref6.cost;
          return cost <= get$2("availableSeptEmbers");
        }), "value"),
        item = _maxBy.item,
        cost = _maxBy.cost;
      var toBuy = Math.floor(get$2("availableSeptEmbers") / cost);
      kolmafia.buy($coinmaster`Sept-Ember Censer`, toBuy, item);
    }
  },
  spendsTurn: false
}, {
  name: "Chateau Mantegna Desk",
  ready: () => have$J(),
  completed: () => get$2("_chateauDeskHarvested"),
  do: () => kolmafia.visitUrl("place.php?whichplace=chateau&action=chateau_desk2", false),
  spendsTurn: false
}, {
  name: "Kremlin's Greatest Briefcase Collect",
  ready: () => have$P($item`Kremlin's Greatest Briefcase`),
  completed: () => get$2("_kgbClicksUsed") > 17 || get$2("_kgbDispenserUses") >= 3,
  do: () => kolmafia.cliExecute("Briefcase collect"),
  spendsTurn: false
}, {
  name: "Ice Cold April Shower",
  ready: () => have$P($item`Clan VIP Lounge key`) && kolmafia.getClanLounge()["Clan shower"] !== undefined,
  completed: () => get$2("_aprilShower"),
  do: () => kolmafia.cliExecute("try; shower ice"),
  spendsTurn: false
}, {
  name: "Swimming Pool Item",
  ready: () => have$P($item`Clan VIP Lounge key`) && kolmafia.getClanLounge()["Olympic-sized Clan crate"] !== undefined,
  completed: () => get$2("_olympicSwimmingPoolItemFound"),
  do: () => kolmafia.cliExecute("swim item"),
  spendsTurn: false
}, {
  name: "Cheat Deck of Every Card",
  ready: () => have$P($item`Deck of Every Card`),
  completed: () => Math.floor(3 - get$2("_deckCardsDrawn") / 5) === 0,
  do: drawBestCards,
  spendsTurn: false
}, {
  name: "Source Terminal Extrude",
  ready: () => have$I(),
  completed: () => get$2("_sourceTerminalExtrudes") === 3 || garboValue(bestExtrude()) < garboValue($item`Source essence`) * 10,
  do: () => extrude(bestExtrude()),
  acquire: [{
    item: $item`Source essence`,
    num: 10
  }],
  limit: {
    skip: 3
  },
  spendsTurn: false
}, {
  name: "Internet Meme Shop viral video",
  completed: () => get$2("_internetViralVideoBought") || garboValue($item`viral video`) < garboValue($item`BACON`) * kolmafia.sellPrice($coinmaster`Internet Meme Shop`, $item`viral video`),
  do: () => kolmafia.buy($coinmaster`Internet Meme Shop`, 1, $item`viral video`),
  acquire: [{
    item: $item`BACON`,
    num: kolmafia.sellPrice($coinmaster`Internet Meme Shop`, $item`viral video`)
  }],
  spendsTurn: false
}, {
  name: "Internet Meme Shop plus one",
  completed: () => get$2("_internetPlusOneBought") || garboValue($item`plus one`) < garboValue($item`BACON`) * kolmafia.sellPrice($coinmaster`Internet Meme Shop`, $item`plus one`),
  do: () => kolmafia.buy($coinmaster`Internet Meme Shop`, 1, $item`plus one`),
  acquire: [{
    item: $item`BACON`,
    num: kolmafia.sellPrice($coinmaster`Internet Meme Shop`, $item`plus one`)
  }],
  spendsTurn: false
}, {
  name: "Internet Meme Shop gallon of milk",
  completed: () => get$2("_internetGallonOfMilkBought") || garboValue($item`gallon of milk`) < garboValue($item`BACON`) * kolmafia.sellPrice($coinmaster`Internet Meme Shop`, $item`gallon of milk`),
  do: () => kolmafia.buy($coinmaster`Internet Meme Shop`, 1, $item`gallon of milk`),
  acquire: [{
    item: $item`BACON`,
    num: kolmafia.sellPrice($coinmaster`Internet Meme Shop`, $item`gallon of milk`)
  }],
  spendsTurn: false
}, {
  name: "Internet Meme Shop print screen button",
  completed: () => get$2("_internetPrintScreenButtonBought") || garboValue($item`print screen button`) < garboValue($item`BACON`) * kolmafia.sellPrice($coinmaster`Internet Meme Shop`, $item`print screen button`),
  do: () => kolmafia.buy($coinmaster`Internet Meme Shop`, 1, $item`print screen button`),
  acquire: [{
    item: $item`BACON`,
    num: kolmafia.sellPrice($coinmaster`Internet Meme Shop`, $item`print screen button`)
  }],
  spendsTurn: false
}, {
  name: "Internet Meme Shop daily dungeon malware",
  completed: () => get$2("_internetDailyDungeonMalwareBought") || garboValue($item`daily dungeon malware`) < garboValue($item`BACON`) * kolmafia.sellPrice($coinmaster`Internet Meme Shop`, $item`daily dungeon malware`),
  do: () => kolmafia.buy($coinmaster`Internet Meme Shop`, 1, $item`daily dungeon malware`),
  acquire: [{
    item: $item`BACON`,
    num: kolmafia.sellPrice($coinmaster`Internet Meme Shop`, $item`daily dungeon malware`)
  }],
  spendsTurn: false
}, {
  name: "Rainbow Gravitation",
  ready: () => have$P($skill`Rainbow Gravitation`),
  completed: () => get$2("prismaticSummons") === 3 || garboValue($item`prismatic wad`) < sum(Wads, garboValue),
  do: () => kolmafia.useSkill($skill`Rainbow Gravitation`, 3 - get$2("prismaticSummons")),
  acquire: () => Wads.map(x => ({
    item: x,
    num: 3 - get$2("prismaticSummons")
  })),
  spendsTurn: false
}, {
  name: "Request Sandwich",
  ready: () => have$P($skill`Request Sandwich`),
  completed: () => get$2("_requestSandwichSucceeded"),
  do: () => kolmafia.useSkill($skill`Request Sandwich`),
  limit: {
    skip: 10
  },
  spendsTurn: false
}, {
  name: "Demand Sandwich",
  ready: () => have$P($skill`Demand Sandwich`),
  completed: () => get$2("_demandSandwich") > 0,
  do: () => kolmafia.useSkill($skill`Demand Sandwich`),
  spendsTurn: false
}, {
  name: "Tea Tree",
  ready: () => kolmafia.getCampground()["potted tea tree"] !== undefined,
  completed: () => get$2("_pottedTeaTreeUsed"),
  do: () => {
    var teas = $items`cuppa Activi tea, cuppa Alacri tea, cuppa Boo tea, cuppa Chari tea, cuppa Craft tea, cuppa Cruel tea, cuppa Dexteri tea, cuppa Feroci tea, cuppa Flamibili tea, cuppa Flexibili tea, cuppa Frost tea, cuppa Gill tea, cuppa Impregnabili tea, cuppa Improprie tea, cuppa Insani tea, cuppa Irritabili tea, cuppa Loyal tea, cuppa Mana tea, cuppa Mediocri tea, cuppa Monstrosi tea, cuppa Morbidi tea, cuppa Nas tea, cuppa Net tea, cuppa Neuroplastici tea, cuppa Obscuri tea, cuppa Physicali tea, cuppa Proprie tea, cuppa Royal tea, cuppa Serendipi tea, cuppa Sobrie tea, cuppa Toast tea, cuppa Twen tea, cuppa Uncertain tea, cuppa Vitali tea, cuppa Voraci tea, cuppa Wit tea, cuppa Yet tea`;
    var bestTea = maxBy(teas, garboValue);
    var shakeVal = 3 * garboAverageValue.apply(void 0, _toConsumableArray(teas));
    var teaAction = shakeVal > garboValue(bestTea) ? "shake" : bestTea.name;
    kolmafia.cliExecute(`teatree ${teaAction}`);
  },
  spendsTurn: false
}, {
  name: "Check Jick Jar",
  ready: () => have$P($item`psychoanalytic jar`),
  completed: () => get$2("_jickJarAvailable") !== "unknown",
  do: () => kolmafia.visitUrl("showplayer.php?who=1"),
  spendsTurn: false
}, {
  name: "Acquire Jick Jar",
  ready: () => have$P($item`psychoanalytic jar`, 2) && get$2("_jickJarAvailable") === "true",
  completed: () => get$2("_psychoJarFilled"),
  do: () => kolmafia.visitUrl("showplayer.php?who=1&action=jung&whichperson=jick"),
  spendsTurn: false
}, {
  name: "Cargo Shorts Pocket",
  ready: () => have$P($item`Cargo Cultist Shorts`),
  completed: () => get$2("_cargoPocketEmptied"),
  do: pickCargoPocket,
  spendsTurn: false
}, {
  name: "Time-Spinner Gin",
  ready: () => have$P($item`Time-Spinner`) && !doingGregFight() && get$2("timeSpinnerMedals") >= 5 && get$2("_timeSpinnerMinutesUsed") <= 8,
  completed: () => get$2("_timeSpinnerReplicatorUsed"),
  do: () => kolmafia.cliExecute("FarFuture drink"),
  spendsTurn: false
}, {
  name: $skill`That's Not a Knife`.name,
  ready: () => have$P($skill`That's Not a Knife`),
  completed: () => get$2("_discoKnife"),
  do: () => {
    var knives = $items`boot knife, broken beer bottle, sharpened spoon, candy knife, soap knife`;
    knives.forEach(item => kolmafia.putCloset(kolmafia.itemAmount(item), item));
    kolmafia.useSkill($skill`That's Not a Knife`);
  },
  spendsTurn: false
}, {
  name: "FantasyRealm Hat",
  ready: () => get$2("frAlways") || get$2("_frToday"),
  completed: () => have$P($item`FantasyRealm G. E. M.`),
  do: () => {
    kolmafia.visitUrl("place.php?whichplace=realm_fantasy&action=fr_initcenter");
    kolmafia.runChoice(-1);
  },
  choices: {
    1280: 1
  },
  spendsTurn: false
}, {
  name: "Lodestone",
  ready: () => have$P($item`lodestone`) && !get$2("_lodestoneUsed"),
  completed: () => get$2("_lodestoneUsed"),
  do: () => kolmafia.use($item`lodestone`),
  spendsTurn: false
}, {
  name: "Learn About Bugs",
  ready: () => have$P($item`S.I.T. Course Completion Certificate`),
  completed: () => get$2("_sitCourseCompleted") || have$P($skill`Insectologist`),
  do: () => kolmafia.use($item`S.I.T. Course Completion Certificate`),
  choices: {
    1494: 2
  },
  spendsTurn: false
}, {
  name: "Rake Leaves",
  ready: () => have$o(),
  completed: () => have$P($item`rake`),
  do: () => {
    kolmafia.visitUrl("campground.php?preaction=leaves");
    kolmafia.visitUrl("main.php"); // Mafia not marking as can walk away
  },
  spendsTurn: false
}, {
  name: "Burning Leaves lit leaf lasso",
  ready: () => have$o() && numberOfLeaves() >= (burnFor.get($item`lit leaf lasso`) ?? Infinity),
  completed: () => get$2("_leafLassosCrafted") >= 3,
  do: () => burnSpecialLeaves($item`lit leaf lasso`),
  limit: {
    skip: 3
  },
  spendsTurn: false
}, {
  name: "Burning Leaves day shortener",
  ready: () => have$o() && numberOfLeaves() >= (burnFor.get($item`day shortener`) ?? Infinity),
  completed: () => get$2("_leafDayShortenerCrafted"),
  do: () => burnSpecialLeaves($item`day shortener`),
  spendsTurn: false
}, {
  name: "Candy cane sword cane Shrine Meat",
  ready: () => have$P($item`candy cane sword cane`) && kolmafia.canAdventure($location`An Overgrown Shrine (Northeast)`) && (!(kolmafia.myInebriety() > kolmafia.inebrietyLimit()) || have$P($item`Drunkula's wineglass`) && kolmafia.canEquip($item`Drunkula's wineglass`)),
  completed: () => get$2("_candyCaneSwordOvergrownShrine"),
  do: () => {
    kolmafia.visitUrl("adventure.php?snarfblat=348");
    kolmafia.runChoice(4);
    kolmafia.runChoice(6);
  },
  outfit: () => ({
    weapon: $item`candy cane sword cane`,
    offhand: kolmafia.myInebriety() > kolmafia.inebrietyLimit() ? $item`Drunkula's wineglass` : undefined
  }),
  limit: {
    skip: 3
  },
  spendsTurn: false
}, {
  name: "Clear Existing Rufus Quest",
  completed: () => get$2("_shadowAffinityToday") || _shouldClearRufusQuest !== null,
  do: () => {
    var value = rufusPotion.value(highMeatMonsterCount());
    var price = rufusPotion.price(false);
    _shouldClearRufusQuest = value.some(value => (!globalOptions.nobarf || value.name === "target") && value.value - price > 0);
    if (_shouldClearRufusQuest) {
      var target = rufusTarget();
      if (get$2("rufusQuestType") === "items") {
        if (acquire(3, target, 2 * kolmafia.mallPrice(target), false, 100000)) {
          withChoice(1498, 1, () => kolmafia.use($item`closed-circuit pay phone`));
        }
      } else if (get$2("rufusQuestType") === "artifact") {
        if (have$P(target)) {
          withChoice(1498, 1, () => kolmafia.use($item`closed-circuit pay phone`));
        }
      }
    }
  },
  spendsTurn: false
}, {
  name: "Accept Rufus Quest for Forest",
  ready: () => have$m() && !rufusTarget(),
  completed: () => get$2("_shadowForestLooted") || have$P($item`Rufus's shadow lodestone`),
  do: () => {
    chooseQuest(() => 3);
    aprilFoolsRufus();
  },
  spendsTurn: false
}, {
  name: "Acquire Rufus Items",
  ready: () => {
    if (!have$m()) return false;
    var target = rufusTarget();
    return target instanceof kolmafia.Item && target.tradeable;
  },
  completed: () => get$2("_shadowForestLooted") || have$P($item`Rufus's shadow lodestone`) || triedForest,
  do: () => {
    var target = rufusTarget();
    var bestRift = chooseRift({
      canAdventure: true,
      sortBy: l => sum(kolmafia.getMonsters(l), m => sum(kolmafia.itemDropsArray(m), _ref7 => {
        var drop = _ref7.drop;
        return garboValue(drop);
      }))
    });
    if (!bestRift) kolmafia.abort("Failed to choose rift for Shadow Forest");
    var value = (6 + 9) / 2 * sum(kolmafia.getMonsters(bestRift), m => sum(kolmafia.itemDropsArray(m), _ref8 => {
      var drop = _ref8.drop;
      return garboValue(drop);
    })) / 3;
    if (target instanceof kolmafia.Item && target.tradeable) {
      if (acquire(3, target, value, false) < 3) {
        kolmafia.print(`Our Rufus quest is ${target}, which costs too much to do the Forest!`, "red");
      }
      triedForest = true;
    }
  },
  spendsTurn: false
}, {
  name: "Turn In Rufus Quest for Forest",
  ready: () => questStep$1("questRufus") === 1,
  completed: () => get$2("_shadowForestLooted") || have$P($item`Rufus's shadow lodestone`),
  do: () => submitQuest(),
  spendsTurn: false
}, {
  name: "Shadow Forest",
  ready: () => have$P($item`Rufus's shadow lodestone`),
  completed: () => get$2("_shadowForestLooted"),
  do: () => chooseRift({
    canAdventure: true,
    sortBy: l => sum(kolmafia.getMonsters(l), m => sum(kolmafia.itemDropsArray(m), _ref9 => {
      var drop = _ref9.drop;
      return garboValue(drop);
    }))
  }) ?? kolmafia.abort("Failed to find appropriate rift for Shadow Forest"),
  choices: {
    1500: 3
  },
  spendsTurn: false
}].concat(_toConsumableArray(augustSummonTasks()), _toConsumableArray(candyMapDailyTasks()), [{
  name: "Get April Instruments",
  completed: () => !canJoinSection(),
  do: () => getBestAprilInstruments().forEach(instrument => joinSection(instrument)),
  spendsTurn: false
}, {
  name: "Play the April piccolo",
  ready: () => have$P($item`Apriling band piccolo`),
  do: () => {
    var familiar = chooseAprilFamiliar();
    while (familiar && canPlay($item`Apriling band piccolo`)) {
      kolmafia.useFamiliar(familiar);
      play($item`Apriling band piccolo`);
      familiar = chooseAprilFamiliar();
    }
  },
  completed: () => !canPlay($item`Apriling band piccolo`),
  spendsTurn: false
}, mayamCalendarSummon(), {
  name: "Devil Cheapest Candy",
  ready: () => have$P($item`candy egg deviler`),
  // TODO: Support guild stash
  completed: () => get$2("_candyEggsDeviled") >= 3 || garboValue($item`deviled candy egg`) < garboValue(getBestDevilerCandy()),
  do: () => {
    acquire(1, getBestDevilerCandy(), garboValue($item`deviled candy egg`), true);
    kolmafia.print(`${getBestDevilerCandy()} will be deviled for expected profit of ${garboValue($item`deviled candy egg`) - garboValue(getBestDevilerCandy())}`);
    kolmafia.visitUrl(`inventory.php?action=eggdevil&pwd`);
    kolmafia.runChoice(1, `a=${kolmafia.toInt(getBestDevilerCandy())}`);
    cachedbestDevilerCandy = null;
  },
  limit: {
    skip: 3
  },
  spendsTurn: false
}, leprecondoTask(), archaeologySpadeTask(), {
  name: "Smash Barrels",
  ready: () => get$2("barrelShrineUnlocked"),
  completed: () => smashedBarrels,
  do: () => {
    var page = kolmafia.visitUrl("barrel.php");
    var regexp = /<div class="ex">(?!<div class="mimic">!<\/div>)<a class="spot" href="(choice.php\?whichchoice=1099&pwd=[a-f0-9]+&option=1&slot=\d+)"><img title="A barrel"/g;
    for (var match; match = regexp.exec(page); null) {
      var url = match[1];
      kolmafia.visitUrl(url);
    }
    smashedBarrels = true;
  },
  spendsTurn: false
}]);
var DailyItemsQuest = {
  name: "Daily Items",
  tasks: [].concat(_toConsumableArray(SummonTasks), _toConsumableArray(DailyItemTasks))
};

var DailySeaTasks = [{
  name: "Unlock The Sea",
  ready: () => kolmafia.myLevel() >= 11,
  completed: () => have$P($item`little bitty bathysphere`),
  do: () => {
    kolmafia.visitUrl("place.php?whichplace=sea_oldman");
    kolmafia.visitUrl("place.php?whichplace=sea_oldman&action=oldman_oldman");
  },
  spendsTurn: false,
  limit: {
    skip: 1
  }
}, {
  name: $item`sea jelly`.name,
  ready: () => have$P($familiar`Space Jellyfish`) && have$P($item`little bitty bathysphere`),
  completed: () => get$2("_seaJellyHarvested"),
  do: () => {
    kolmafia.visitUrl("place.php?whichplace=thesea&action=thesea_left2");
    kolmafia.runChoice(1);
  },
  outfit: {
    familiar: $familiar`Space Jellyfish`
  },
  spendsTurn: false,
  limit: {
    skip: 1
  }
}];
var DailySeaQuest = {
  name: "Daily Sea",
  tasks: DailySeaTasks
};

function dailySetup() {
  runSafeGarboQuests([DailyFamiliarsQuest, DailyQuest, DailyItemsQuest, DailySeaQuest, AscendingQuest]);
}

function dessertIslandWorthIt() {
  // estimating value of giant giant crab at 5*VOA, it has 2000 base meat
  return garboValue($item`cocoa of youth`) > 5 * get$2("valueOfAdventure");
}
function crewRoleValue(crewmate) {
  // Cuisinier is highest value if cocoa of youth is more meat than expected from giant crab
  if (dessertIslandWorthIt() && crewmate.includes("Cuisinier")) {
    return 50;
  }
  // Coxswain helps save turns if we run from storms
  if (crewmate.includes("Coxswain")) return 40;
  // Harquebusier gives us extra fun from combats
  if (crewmate.includes("Harquebusier")) return 30;
  // Crypto, Cuisinier (if cocoa not worth it), and Mixologist have small bonuses we care about less
  return 0;
}
function crewAdjectiveValue(crewmate) {
  // Wide-Eyed give us bonus fun when counting birds in smooth sailing, and we'll mostly be doing that rather than spending limited grub/grog
  if (crewmate.includes("Wide-Eyed")) return 5;
  // Gluttonous can help when running out of grub, even though we usually shouldn't?
  if (crewmate.includes("Gluttonous")) return 4;
  // Beligerent, Dipsomaniacal, and Pinch-Fisted don't make much difference
  return 0;
}
function bestCrewmate() {
  return maxBy([1, 2, 3], choiceOption => {
    var crewmatePref = `_pirateRealmCrewmate${choiceOption}`;
    var crewmate = get$2(crewmatePref);
    var roleValue = crewRoleValue(crewmate);
    var adjectiveValue = crewAdjectiveValue(crewmate);
    return roleValue + adjectiveValue;
  });
}
function outfitBonuses() {
  var funPointValue = garboValue($item`PirateRealm guest pass`) / 600;
  return new Map([[$item`carnivorous potted plant`, get$2("valueOfAdventure") / (20 + get$2("_carnivorousPottedPlantWins"))], [$item`Red Roger's red left foot`, funPointValue], [$item`PirateRealm party hat`, funPointValue]]);
}

var CockroachSetup = {
  name: "Setup Cockroach Target",
  ready: () => doingGregFight() && globalOptions.target === $monster`cockroach` && kolmafia.myInebriety() <= kolmafia.inebrietyLimit(),
  completed: () => get$2("_lastPirateRealmIsland") === $location`Trash Island`,
  tasks: [{
    name: "40 Adventure Failsafe",
    ready: () => kolmafia.myAdventures() <= 40,
    completed: () => have$P($item`PirateRealm eyepatch`),
    do: () => {
      if (userConfirmDialog("You don't have enough adventures to do piraterealm; would you like us to automatically change your copy target to a Knob Goblin Guard? Otherwise, we're going to abort.", true)) {
        globalOptions.target = $monster`Knob Goblin Elite Guard Captain`;
      } else {
        kolmafia.abort("Unable to start piraterealm but you're hellbent on doing cockroaches!");
      }
    },
    spendsTurn: false
  }, {
    name: "Get PirateRealm Eyepatch",
    completed: () => have$P($item`PirateRealm eyepatch`),
    do: () => kolmafia.visitUrl("place.php?whichplace=realm_pirate&action=pr_port"),
    limit: {
      tries: 1
    },
    spendsTurn: false
  }, {
    name: "Start PirateRealm Journey",
    ready: () => have$P($item`PirateRealm eyepatch`),
    completed: () => questStep$1("_questPirateRealm") > 0,
    do: () => {
      kolmafia.visitUrl("place.php?whichplace=realm_pirate&action=pr_port");
      kolmafia.runChoice(1); // Head to Groggy's
      kolmafia.runChoice(bestCrewmate()); // Choose our crew
      if (!(4 in kolmafia.availableChoiceOptions())) {
        kolmafia.abort("You need the anemometer unlocked to fight cockroaches in garbo!");
      }
      kolmafia.runChoice(4); // Choose anemometer for trash island
      var bestBoat = get$2("pirateRealmUnlockedClipper") ? 4 : 3; // Swift Clipper or Speedy Caravel
      kolmafia.runChoice(bestBoat);
      kolmafia.runChoice(1); // Head for the sea
    },
    outfit: {
      equip: $items`PirateRealm eyepatch`,
      modifier: kolmafia.Stat.all().map(stat => `-${stat}`)
    },
    limit: {
      tries: 1
    },
    spendsTurn: false
  }, {
    name: "Choose First Island",
    ready: () => questStep$1("_questPirateRealm") === 1,
    completed: () => questStep$1("_questPirateRealm") > 1,
    do: $location`Sailing the PirateRealm Seas`,
    outfit: {
      equip: $items`PirateRealm eyepatch`,
      modifier: kolmafia.Stat.all().map(stat => `-${stat}`)
    },
    choices: () => ({
      1352: dessertIslandWorthIt() && get$2("_pirateRealmCrewmate").includes("Cuisinier") ? 6 : 1
    }),
    limit: {
      tries: 1
    },
    spendsTurn: false,
    combat: new GarboStrategy(() => Macro.abortWithMsg("Hit a combat while sailing the high seas!"))
  }, {
    name: "Sail to first Island",
    ready: () => questStep$1("_questPirateRealm") === 2,
    completed: () => questStep$1("_questPirateRealm") > 2,
    do: $location`Sailing the PirateRealm Seas`,
    outfit: () => ({
      equip: $items`PirateRealm eyepatch, PirateRealm party hat, Red Roger's red right foot`.filter(i => have$P(i)),
      modifier: kolmafia.Stat.all().map(stat => `-${stat}`)
    }),
    choices: () => ({
      1365: 1,
      1364: 2,
      1361: 1,
      1357: get$2("_pirateRealmGold") >= 50 ? 3 : 4,
      1360: 6,
      // Will need to add shop handling, perhaps to choice adventure script
      1356: 3,
      1362: get$2("_pirateRealmShipSpeed") - get$2("_pirateRealmSailingTurns") >= 2 ? 2 : 1,
      1363: 2,
      1359: 1,
      // Emergency grog adventure, choice one seems more consistent?
      1358: 1,
      // Emergency grub adventure, choice one seems more consistent?
      1367: 1 // Wrecked ship, this uses glue, need a pref for glue to make this not break if we don't have glue
    }),
    post: () => {
      // Escape wrecked ship, if no glue is available
      if (kolmafia.handlingChoice() && kolmafia.lastChoice() === 1367) {
        kolmafia.runChoice(2);
      }
    },
    limit: {
      tries: 8
    },
    spendsTurn: true,
    combat: new GarboStrategy(() => Macro.abortWithMsg("Hit a combat while sailing the high seas!"))
  }, {
    name: "Land Ho (First Island)",
    ready: () => questStep$1("_questPirateRealm") === 3,
    completed: () => questStep$1("_questPirateRealm") > 3,
    do: $location`Sailing the PirateRealm Seas`,
    combat: new GarboStrategy(() => Macro.abortWithMsg("Expected Land Ho! but hit a combat")),
    choices: {
      1355: 1
    },
    // Land ho!
    outfit: {
      equip: $items`PirateRealm eyepatch`,
      modifier: kolmafia.Stat.all().map(stat => `-${stat}`)
    },
    limit: {
      tries: 1
    },
    spendsTurn: false
  }, {
    name: "Standard Island Combats (Island 1)",
    ready: () => questStep$1("_questPirateRealm") === 4,
    completed: () => questStep$1("_questPirateRealm") > 4,
    prepare: () => {
      if (kolmafia.mallPrice($item`windicle`) < 3 * get$2("valueOfAdventure") && !get$2("_pirateRealmWindicleUsed")) {
        acquire(1, $item`windicle`, 3 * get$2("valueOfAdventure"), true);
      }
    },
    do: () => get$2("_lastPirateRealmIsland", $location`none`),
    outfit: () => freeFightOutfit({
      equip: $items`PirateRealm eyepatch`,
      bonuses: outfitBonuses(),
      familiar: freeFightFamiliar(get$2("_lastPirateRealmIsland", $location`none`), {
        canChooseMacro: false,
        allowAttackFamiliars: true,
        mode: "free"
      }),
      avoid: $items`Roman Candelabra`
    }, get$2("_lastPirateRealmIsland", $location`none`)),
    combat: new GarboStrategy(() => Macro.externalIf(kolmafia.mallPrice($item`windicle`) < 3 * get$2("valueOfAdventure") && !get$2("_pirateRealmWindicleUsed") && get$2("_pirateRealmIslandMonstersDefeated") <= 1, Macro.item($item`windicle`)).basicCombat()),
    limit: {
      tries: 8
    },
    spendsTurn: true
  }, {
    name: "Final Island Encounter (Island 1 (Dessert))",
    ready: () => questStep$1("_questPirateRealm") === 5 && get$2("_lastPirateRealmIsland") === $location`Dessert Island`,
    completed: () => questStep$1("_questPirateRealm") > 5,
    do: $location`PirateRealm Island`,
    outfit: () => ({
      equip: $items`PirateRealm eyepatch`,
      modifier: kolmafia.Stat.all().map(stat => `-${stat}`)
    }),
    choices: {
      1385: 1
    },
    // Take cocoa of youth
    combat: new GarboStrategy(() => Macro.abortWithMsg("Hit a combat when we expected cocoa of youth!")),
    limit: {
      tries: 1
    },
    spendsTurn: true
  }, {
    name: "Final Island Encounter (Island 1 (Giant Giant Crab))",
    ready: () => questStep$1("_questPirateRealm") === 5 && get$2("_lastPirateRealmIsland") === $location`Crab Island`,
    completed: () => questStep$1("_questPirateRealm") > 5,
    prepare: () => {
      kolmafia.restoreHp(kolmafia.myMaxhp());
    },
    do: $location`Crab Island`,
    outfit: () => meatTargetOutfit({
      modifier: ["-Muscle", "-Mysticality", "-Moxie"],
      equip: $items`PirateRealm eyepatch`,
      avoid: $items`Roman Candelabra`,
      beforeDress: [() => meatMood(false, targetMeat()).execute(highMeatMonsterCount()),
      // meatMood is currently difficult to sort for things that give +stats
      () => potionSetup(false, true) // run potionSetup while avoiding stats. We do not avoid limited use buffs that may still increase stats like paw wishes or pill keeper.
      ]
    }, $location`Crab Island`),
    choices: {
      1368: 1
    },
    // fight crab
    combat: new GarboStrategy(() => Macro.tryHaveSkill($skill`Curse of Weaksauce`).meatKill()),
    limit: {
      tries: 1
    },
    spendsTurn: true
  }, {
    name: "Choose Trash Island",
    ready: () => questStep$1("_questPirateRealm") === 6,
    completed: () => questStep$1("_questPirateRealm") > 6,
    do: $location`Sailing the PirateRealm Seas`,
    outfit: {
      equip: $items`PirateRealm eyepatch`
    },
    choices: {
      1353: 5
    },
    // Trash Island
    limit: {
      tries: 1
    },
    spendsTurn: false,
    combat: new GarboStrategy(() => Macro.abortWithMsg("Hit a combat while sailing the high seas!")),
    post: () => unequip($item`PirateRealm eyepatch`) // Unequip the eyepatch when we're done, to avoid mana issues during diet etc
  }, {
    name: "Stop Being Beaten Up",
    completed: () => !have$P($effect`Beaten Up`),
    do: () => kolmafia.useSkill($skill`Tongue of the Walrus`),
    spendsTurn: false,
    post: unignoreBeatenUp
  }]
};

function canContinue() {
  return kolmafia.myAdventures() > globalOptions.saveTurns && (globalOptions.stopTurncount === null || kolmafia.myTurncount() < globalOptions.stopTurncount);
}
var lastParachuteFailure = 0;
var shouldCheckParachute = () => kolmafia.totalTurnsPlayed() !== lastParachuteFailure;
var updateParachuteFailure = () => lastParachuteFailure = kolmafia.totalTurnsPlayed();

var MEAT_DROP_VALUE = () => baseMeat() / 100;
function familiarNeedsBoot(familiar) {
  return FarmingStrategy.isUnderwater() && !have$P($effect`Driving Waterproofly`) && !familiar.underwater;
}
var outfitCache = new Map();
var outfitSlots = $slots`hat, back, shirt, weapon, off-hand, pants, acc1, acc2, acc3, familiar`;
var SPECIAL_FAMILIARS_FOR_CACHING = new Map([[
// Derives its value from famexp and requires _much_ more famexp than other exp familiars
$familiar`Chest Mimic`, {
  extraValue: _ref => {
    var famexp = _ref.famexp;
    return famexp * MEAT_TARGET_MULTIPLIER() * get$2("valueOfAdventure") / 50;
  }
}],
// Uniquely required to equip its fam equip to meaningfully have value
[$familiar`Jill-of-All-Trades`, {
  equip: $item`LED candle`
}],
// Derives its value irregularly from +famweight
[$familiar`Mini Kiwi`, {
  extraValue: _ref2 => {
    var weight = _ref2.weight;
    return clamp((weight + totalFamiliarWeight($familiar`Mini Kiwi`, false)) * 0.005, 0, 1) * garboValue($item`mini kiwi`);
  }
}]]);
function outfitCacheKey(familiar) {
  if (SPECIAL_FAMILIARS_FOR_CACHING.has(familiar)) {
    return familiar;
  }
  var lepMultiplier = findLeprechaunMultiplier(familiar);
  if (!FarmingStrategy.isUnderwater()) {
    return lepMultiplier;
  }
  return `${lepMultiplier}:${familiar.underwater}`;
}
function getCachedOutfitValues(fam) {
  var cacheKey = outfitCacheKey(fam);
  var currentValue = outfitCache.get(cacheKey);
  var needsBoot = familiarNeedsBoot(fam);
  if (currentValue) return currentValue;
  var current = kolmafia.myFamiliar();
  kolmafia.cliExecute("checkpoint");
  try {
    computeBarfOutfit({
      familiar: fam,
      equip: needsBoot ? $items`das boot` : [],
      avoid: $items`Kramco Sausage-o-Matic™, cursed magnifying glass, protonic accelerator pack, "I Voted!" sticker, li'l pirate costume, bag of many confections, bat wings, toy Cupid bow`
    }, true).dress();
    var outfit = outfitSlots.map(slot => kolmafia.equippedItem(slot));
    var bonuses = bonusGear(BonusEquipMode.MEAT_TARGET, false);
    var values = {
      weight: sum(outfit, eq => get$1("Familiar Weight", eq)),
      meat: sum(outfit, eq => get$1("Meat Drop", eq)),
      item: sum(outfit, eq => get$1("Item Drop", eq)),
      famexp: sum(outfit, eq => get$1("Familiar Experience", eq)),
      bonus: sum(outfit, eq => bonuses.get(eq) ?? 0)
    };
    outfitCache.set(cacheKey, values);
    return values;
  } finally {
    kolmafia.useFamiliar(current);
    kolmafia.cliExecute("outfit checkpoint");
  }
}
var nonOutfitWeightBonus = () => kolmafia.weightAdjustment() - sum(outfitSlots, slot => get$1("Familiar Weight", kolmafia.equippedItem(slot)));
function familiarModifier(familiar, modifier) {
  var cachedOutfitWeight = getCachedOutfitValues(familiar).weight;
  var totalWeight = totalFamiliarWeight(familiar, false) + nonOutfitWeightBonus() + cachedOutfitWeight;
  var _ref3 = SPECIAL_FAMILIARS_FOR_CACHING.get(familiar) ?? {},
    equip = _ref3.equip;
  return equip ? kolmafia.numericModifier(familiar, modifier, totalWeight - kolmafia.numericModifier(equip, "Familiar Weight"), equip) : kolmafia.numericModifier(familiar, modifier, totalWeight, $item.none);
}
function familiarAbilityValue(familiar) {
  return familiarModifier(familiar, "Meat Drop") * MEAT_DROP_VALUE() + familiarModifier(familiar, "Item Drop") * FarmingStrategy.itemDropValue();
}
function totalFamiliarValue(_ref4) {
  var expectedValue = _ref4.expectedValue,
    outfitValue = _ref4.outfitValue,
    familiar = _ref4.familiar;
  return expectedValue + outfitValue + familiarAbilityValue(familiar);
}
function turnsNeededFromBaseline(baselineToCompareAgainst, tcbFamiliars) {
  return _ref5 => {
    var familiar = _ref5.familiar,
      limit = _ref5.limit,
      outfitValue = _ref5.outfitValue,
      bonusTurns = _ref5.bonusTurns;
    switch (limit) {
      case "drops":
        return sum(getAllDrops(familiar).filter(_ref6 => {
          var expectedValue = _ref6.expectedValue;
          return outfitValue + familiarAbilityValue(familiar) + expectedValue > totalFamiliarValue(baselineToCompareAgainst);
        }), "expectedTurns") - (bonusTurns ?? 0);
      case "experience":
        return getExperienceFamiliarLimit(familiar) - (bonusTurns ?? 0);
      case "none":
        return 0;
      case "cupid":
        return tcbTurnsLeft(familiar, tcbFamiliars) - (bonusTurns ?? 0);
      case "special":
        return getSpecialFamiliarLimit({
          familiar,
          outfitValue,
          baselineToCompareAgainst
        }) - (bonusTurns ?? 0);
    }
  };
}
function calculateOutfitValue(f) {
  var _SPECIAL_FAMILIARS_FO, _SPECIAL_FAMILIARS_FO2;
  var outfit = getCachedOutfitValues(f.familiar);
  var outfitValue = outfit.bonus + outfit.meat * MEAT_DROP_VALUE() + outfit.item * FarmingStrategy.itemDropValue() + (((_SPECIAL_FAMILIARS_FO = SPECIAL_FAMILIARS_FOR_CACHING.get(f.familiar)) === null || _SPECIAL_FAMILIARS_FO === void 0 || (_SPECIAL_FAMILIARS_FO2 = _SPECIAL_FAMILIARS_FO.extraValue) === null || _SPECIAL_FAMILIARS_FO2 === void 0 ? void 0 : _SPECIAL_FAMILIARS_FO2.call(_SPECIAL_FAMILIARS_FO, outfit)) ?? 0);
  var outfitWeight = outfit.weight;
  return _objectSpread2(_objectSpread2({}, f), {}, {
    outfitValue,
    outfitWeight
  });
}
function extraValue(target, meat, jellyfish) {
  var targetValue = totalFamiliarValue(target);
  var meatFamiliarValue = totalFamiliarValue(meat);
  var jelly = FarmingStrategy.monsters().every(monster => monster.attackElement === $element`Stench`) ? $item`stench jelly` : kolmafia.Item.none;
  var jellyfishValue = jellyfish ? garboValue(jelly) / 20 + familiarAbilityValue(jellyfish.familiar) + jellyfish.outfitValue : 0;
  return Math.max(targetValue - Math.max(meatFamiliarValue, jellyfishValue), 0);
}
var familiarPrintout = _ref7 => {
  var expectedValue = _ref7.expectedValue,
    familiar = _ref7.familiar,
    outfitValue = _ref7.outfitValue;
  return `(expected value of ${expectedValue.toFixed(1)} from familiar drops, ${familiarAbilityValue(familiar).toFixed(1)} from familiar abilities and ${outfitValue.toFixed(1)} from outfit)`;
};
function barfFamiliar(equipmentForced) {
  // Meatify is basically always the most valuable thing we can do, to the point of not even counting as a marginal familiar
  if (timeToMeatify()) {
    return {
      familiar: $familiar`Grey Goose`,
      extraValue: 0
    };
  }

  // Luddite mode users get off here
  if (get$2("garbo_IgnoreMarginalFamiliars", false)) {
    return {
      familiar: meatFamiliar(),
      extraValue: 0
    };
  }
  var meat = meatFamiliar();
  var usedTcbFamiliars = getUsedTcbFamiliars();
  var fullMenu = menu$1(FarmingStrategy.location, {
    canChooseMacro: true,
    includeExperienceFamiliars: true,
    mode: "barf"
  }).flatMap(generalFamiliar => {
    // Here we do two things:
    // * transform `GeneralFamiliar`s into `MarginalFamiliar`s, which carry with them the total value of the outfit you'd wear
    // * "double up" on familiars for which the toy Cupid bow is available
    var normal = calculateOutfitValue(generalFamiliar);
    if (normal.limit === "cupid" ||
    // If we're already dealing with one of our generated toy cupid bow picks
    equipmentForced ||
    // If we're unable to equip the toy cupid bow
    !have$f() ||
    // If we don't have the toy cupid bow
    usedTcbFamiliars.has(generalFamiliar.familiar) // If we've already gotten the thing
    ) {
      return normal;
    }
    var tcb = calculateOutfitValue(_objectSpread2(_objectSpread2({}, generalFamiliar), {}, {
      expectedValue: generalFamiliar.expectedValue + tcbValue(generalFamiliar.familiar, usedTcbFamiliars, false, true),
      limit: "cupid"
    }));
    if (tcb.expectedValue >= normal.expectedValue) {
      return [tcb, _objectSpread2(_objectSpread2({}, normal), {}, {
        // Account for the already-burned TCB turns when calculating the limit for the "normal" entry for the familiar
        bonusTurns: tcbTurnsLeft(generalFamiliar.familiar, usedTcbFamiliars)
      })];
    }
    return normal;
  });
  var meatFamiliarEntry = fullMenu.find(_ref8 => {
    var familiar = _ref8.familiar,
      limit = _ref8.limit;
    return familiar === meat && limit !== "cupid";
  });
  if (!meatFamiliarEntry) {
    throw new Error("Something went wrong when initializing familiars!");
  }
  var meatFamiliarValue = totalFamiliarValue(meatFamiliarEntry);
  // Ultimately, using our meat familiar all day is the default behavior
  // so any familiar worse than that isn't worth spending any time thinking about
  var viableMenu = fullMenu.filter(f => totalFamiliarValue(f) >= meatFamiliarValue);
  if (viableMenu.length === 0) {
    return {
      familiar: meat,
      extraValue: 0
    };
  }

  // Determine the baseline for how good a familiar needs to be to be run--either an unlimited familiar, or our meat familiar
  var unlimitedCruisingFamiliars = viableMenu.filter(_ref9 => {
    var limit = _ref9.limit;
    return limit === "none";
  });
  var cruisingFamiliar = unlimitedCruisingFamiliars.length ? maxBy(unlimitedCruisingFamiliars, totalFamiliarValue) : meatFamiliarEntry;
  var cruisingFamiliarValue = totalFamiliarValue(cruisingFamiliar);

  // Only consider familiars better than our best unlimited familiar
  var finalMenu = viableMenu.filter(f => totalFamiliarValue(f) >= cruisingFamiliarValue);

  // If the shrub beats our cruising familiar, use it
  var shrubAvailable = finalMenu.some(_ref0 => {
    var familiar = _ref0.familiar;
    return familiar === $familiar`Crimbo Shrub`;
  });
  if (shrubAvailable) {
    return {
      familiar: $familiar`Crimbo Shrub`,
      extraValue: 0
    };
  }
  var turnsNeeded = sum(finalMenu, turnsNeededFromBaseline(cruisingFamiliar, usedTcbFamiliars));

  // If we have enough turns to get all the drops we need, prioritize by using our best leprechauns first
  // Otherwise, prioritize by using the most valuable familiar
  var best = turnsNeeded < turnsAvailable() ? maxBy(finalMenu, "leprechaunMultiplier") : maxBy(finalMenu, totalFamiliarValue);
  kolmafia.print(`Choosing to use ${best.familiar} ${familiarPrintout(best)} over ${meatFamiliarEntry.familiar} ${familiarPrintout(meatFamiliarEntry)}.`, HIGHLIGHT);
  var jellyfish = fullMenu.find(_ref1 => {
    var familiar = _ref1.familiar;
    return familiar === $familiar`Space Jellyfish`;
  });
  return {
    familiar: best.familiar,
    extraValue: extraValue(best, meatFamiliarEntry, jellyfish)
  };
}
function getSpecialFamiliarLimit(_ref10) {
  var familiar = _ref10.familiar,
    outfitValue = _ref10.outfitValue,
    baselineToCompareAgainst = _ref10.baselineToCompareAgainst;
  switch (familiar) {
    case $familiar`Space Jellyfish`:
      return sum(getAllJellyfishDrops().filter(_ref11 => {
        var expectedValue = _ref11.expectedValue;
        return outfitValue + familiarAbilityValue(familiar) + expectedValue > totalFamiliarValue(baselineToCompareAgainst);
      }), "turnsAtValue");
    case $familiar`Crimbo Shrub`:
      return Math.ceil(estimatedGarboTurns() / 100);
    case $familiar`Skeleton of Crimbo Past`:
      return clamp(100 - get$2("_knuckleboneDrops"), 0, 100) / expectedBones(FarmingStrategy.location);
    default:
      return 0;
  }
}

function chooseGun() {
  if (have$P($item`love`)) {
    return $item`love`;
  }
  if (!have$P($item`ice nine`)) {
    kolmafia.cliExecute("refresh inventory");
    kolmafia.retrieveItem($item`ice nine`);
  }
  return have$P($item`ice nine`) ? $item`ice nine` : null;
}
function gunSpec(outfit) {
  if (!outfit.canEquip($item`unwrapped knock-off retro superhero cape`)) {
    return {
      available: false,
      items: []
    };
  }
  var gun = chooseGun();
  if (!gun) return {
    available: false,
    items: []
  };
  return {
    available: true,
    items: {
      back: $item`unwrapped knock-off retro superhero cape`,
      weapon: gun,
      equip: $items`mafia pointer finger ring`,
      modes: {
        retrocape: ["robot", "kill"]
      }
    }
  };
}
var POINTER_RING_SPECS = outfit => [{
  available: have$P($skill`Furious Wallop`) && kolmafia.myFury() > 0,
  items: $items`mafia pointer finger ring`
}, {
  available: have$P($skill`Head in the Game`),
  items: $items`mafia pointer finger ring`
}, {
  available: kolmafia.myClass() === $class`Turtle Tamer`,
  items: $items`Operation Patriot Shield, mafia pointer finger ring`
}, {
  available: true,
  items: $items`haiku katana, mafia pointer finger ring`
}, () => gunSpec(outfit), {
  available: true,
  items: $items`Operation Patriot Shield, mafia pointer finger ring`
}, {
  available: true,
  items: $items`left bear arm, right bear arm, mafia pointer finger ring`
}];
var trueInebrietyLimit = () => kolmafia.inebrietyLimit() - (kolmafia.myFamiliar() === $familiar`Stooper` ? 1 : 0);
function computeBarfOutfit(spec) {
  var sim = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : false;
  cleaverCheck();
  validateGarbageFoldable(spec);
  var outfit = Outfit.from(spec, new Error(`Failed to construct outfit from spec ${JSON.stringify(spec)}!`));
  outfit.addBonuses(bonusGear(BonusEquipMode.BARF, !sim));
  applyCheeseBonus(outfit, BonusEquipMode.BARF);
  if (outfit.familiar === $familiar`Jill-of-All-Trades`) {
    outfit.equip($item`LED candle`);
    outfit.setModes({
      jillcandle: "ultraviolet"
    });
  }
  if (outfit.familiar === $familiar`Chest Mimic` && $familiar`Chest Mimic`.experience < 550) {
    var famExpValue = MEAT_TARGET_MULTIPLIER() * get$2("valueOfAdventure") / 50;
    outfit.modifier.push(`${famExpValue} Familiar Experience`);
  }
  var bjornChoice = chooseBjorn(BonusEquipMode.BARF, spec.familiar, sim);
  if (FarmingStrategy.isUnderwater()) {
    outfit.modifier.push(`+sea`);
  }
  outfit.modifier.push(`${modeValueOfMeat(BonusEquipMode.BARF)} Meat Drop`, `${modeValueOfItem(BonusEquipMode.BARF)} Item Drop`, "-tie");
  if (kolmafia.myInebriety() > trueInebrietyLimit()) {
    if (!outfit.equip($item`Drunkula's wineglass`)) {
      throw new Error("We're overdrunk but have found ourself unable to equip a wineglass!");
    }
  } else {
    if (have$P($item`protonic accelerator pack`) && get$2("questPAGhost") === "unstarted" && get$2("nextParanormalActivity") <= kolmafia.totalTurnsPlayed()) {
      outfit.equip($item`protonic accelerator pack`);
    }
    var _iterator = _createForOfIteratorHelper(POINTER_RING_SPECS(outfit)),
      _step;
    try {
      for (_iterator.s(); !(_step = _iterator.n()).done;) {
        var _spec = _step.value;
        var _undelay = undelay(_spec),
          available = _undelay.available,
          items = _undelay.items;
        if (available && outfit.tryEquip(items)) break;
      }
    } catch (err) {
      _iterator.e(err);
    } finally {
      _iterator.f();
    }
  }
  if (getKramcoWandererChance() > 0.05) {
    outfit.equip($item`Kramco Sausage-o-Matic™`);
  }
  if (!sim) {
    outfit.addBonuses(toyCupidBow(spec.familiar));
  }
  var bjornalike = bestBjornalike(outfit);
  if (bjornalike) {
    outfit.setBonus(bjornalike, bjornChoice.value);
    var other = $items`Buddy Bjorn, Crown of Thrones`.filter(i => i !== bjornalike)[0];
    outfit.avoid.push(other);
    switch (bjornalike) {
      case $item`Buddy Bjorn`:
        outfit.bjornify(bjornChoice.familiar);
        break;
      case $item`Crown of Thrones`:
        outfit.enthrone(bjornChoice.familiar);
        break;
    }
  }
  outfit.setModes({
    snowsuit: "nose",
    parka: "kachungasaur"
  });
  return outfit;
}
function barfOutfit(spec) {
  var _spec$equip;
  var sim = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : false;
  var _barfFamiliar = barfFamiliar(Boolean(spec.famequip || ((_spec$equip = spec.equip) === null || _spec$equip === void 0 ? void 0 : _spec$equip.some(equipment => kolmafia.toSlot(equipment) === $slot`familiar`)))),
    familiar = _barfFamiliar.familiar,
    extraValue = _barfFamiliar.extraValue;
  try {
    return computeBarfOutfit(_objectSpread2({
      familiar
    }, spec), sim);
  } finally {
    trackMarginalTurnExtraValue(extraValue);
  }
}

function FarmTurnQuest() {
  return {
    name: `${FarmingStrategy.location}`,
    tasks: [{
      name: "Parachute",
      ready: () => have$g() && shouldCheckParachute() && kolmafia.myLocation() === FarmingStrategy.location && FarmingStrategy.shouldOlfact,
      completed: () => have$P($effect`Everything looks Beige`) || kolmafia.myAdventures() === 0,
      outfit: context => barfOutfit(FarmingStrategy.outfit(context)),
      do: () => fight(undelay(FarmingStrategy.targetMonster)),
      combat: FarmingStrategy.combat,
      post: () => {
        var _FarmingStrategy$post;
        (_FarmingStrategy$post = FarmingStrategy.post) === null || _FarmingStrategy$post === void 0 || _FarmingStrategy$post.call(FarmingStrategy);
        if (!have$P($effect`Everything looks Beige`)) updateParachuteFailure();
        trackMarginalMpa();
      },
      spendsTurn: true
    }, {
      name: "Farm",
      completed: () => kolmafia.myAdventures() === 0,
      prepare: context => {
        var _context$banish;
        if (redTaffyWorth() && FarmingStrategy.isUnderwater()) {
          kolmafia.retrieveItem($item`pulled red taffy`);
        }
        meatMood().execute(estimatedGarboTurns());
        if ((_context$banish = context.banish) !== null && _context$banish !== void 0 && _context$banish.retrieve && context.banish.source instanceof kolmafia.Item) {
          kolmafia.retrieveItem(context.banish.source);
        }
      },
      outfit: context => barfOutfit(FarmingStrategy.outfit(context)),
      do: FarmingStrategy.location,
      combat: FarmingStrategy.combat,
      post: () => {
        var _FarmingStrategy$post2;
        (_FarmingStrategy$post2 = FarmingStrategy.post) === null || _FarmingStrategy$post2 === void 0 || _FarmingStrategy$post2.call(FarmingStrategy);
        trackMarginalMpa();
        if (kolmafia.toMonster(get$2("lastEncounter")) === $monster`tumbleweed`) {
          throw new Error("You encountered a tumbleweed and should not have, resolve your banishes");
        }
        if (FarmingStrategy.monstersToBanish().includes(kolmafia.toMonster(get$2("lastEncounter")))) {
          throw new Error("You encountered a banishable monster and didn't banish it, sort your life out!");
        }
      },
      spendsTurn: true
    }],
    completed: () => !canContinue()
  };
}

var DailyExtrasQuest = {
  name: "Daily Extras",
  tasks: [mayamCalendarSummon()]
};

function bestFamUnderwaterGear(fam) {
  // Returns best familiar gear for yachtzee chaining
  return fam.underwater || have$P($effect`Driving Waterproofly`) || have$P($effect`Wet Willied`) ? have$P($item`amulet coin`) ? $item`amulet coin` : $item`filthy child leash` : have$P($item`das boot`) ? $item`das boot` : $item`little bitty bathysphere`;
}
function equipmentlessFamiliarWeight(fam) {
  return totalFamiliarWeight(fam, true) - kolmafia.numericModifier(kolmafia.equippedItem($slot`familiar`), "Familiar Weight");
}
function bestYachtzeeFamiliar() {
  var haveUnderwaterFamEquipment = familiarWaterBreathingEquipment.some(item => have$P(item));
  var availableUnderwaterFamiliars = kolmafia.Familiar.all().filter(fam => have$P(fam) && findLeprechaunMultiplier(fam) > 0 && fam !== $familiar`Ghost of Crimbo Commerce` && fam !== $familiar`Robortender` && (fam.underwater || haveUnderwaterFamEquipment)).map(familiar => ({
    familiar,
    meat: kolmafia.numericModifier(familiar, "Meat Drop", equipmentlessFamiliarWeight(familiar), bestFamUnderwaterGear(familiar))
  }));
  kolmafia.print(`Familiar bonus meat%:`, "blue");
  availableUnderwaterFamiliars.forEach(_ref => {
    var familiar = _ref.familiar,
      meat = _ref.meat;
    kolmafia.print(`${familiar} (${meat.toFixed(2)}%)`, "blue");
  });
  if (availableUnderwaterFamiliars.length === 0) return $familiar.none;
  var best = maxBy(availableUnderwaterFamiliars, "meat").familiar;
  kolmafia.print(`Best Familiar: ${best}`, "blue");
  return best;
}

function yachtzeeBuffValue(obj) {
  return clamp(2000 * (get$1("Meat Drop", obj) + get$1("Familiar Weight", obj) * 2.5) / 100, 0, 20000);
}
function getBestWaterBreathingEquipment(yachtzeeTurns) {
  var waterBreathingEquipmentCosts = waterBreathingEquipment.map(it => ({
    item: it,
    cost: have$P(it) && kolmafia.canEquip(it) ? yachtzeeTurns * yachtzeeBuffValue(kolmafia.equippedItem(kolmafia.toSlot(it))) : Infinity
  }));
  var bestWaterBreathingEquipment = waterBreathingEquipment.some(item => kolmafia.haveEquipped(item)) ? {
    item: $item.none,
    cost: 0
  } : maxBy(waterBreathingEquipmentCosts, "cost", true);
  return bestWaterBreathingEquipment;
}

var yachtzeeQuest = [{
  name: "Yachtzee",
  completed: () => !willYachtzee() && !get$2("noncombatForcerActive"),
  tasks: [{
    name: "Yachtzee",
    completed: () => !get$2("noncombatForcerActive"),
    ready: () => have$P($effect`Fishy`),
    choices: {
      918: 2
    },
    do: $location`The Sunken Party Yacht`,
    outfit: () => {
      var _outfit$avoid;
      var outfit = new Outfit();
      var overdrunk = kolmafia.myInebriety() > kolmafia.inebrietyLimit();
      var yachtzeeFamiliar = bestYachtzeeFamiliar();
      outfit.modifier.push("20 Meat Drop", "-tie");
      if (!(yachtzeeFamiliar.underwater || have$P($effect`Driving Waterproofly`) || have$P($effect`Wet Willied`))) {
        outfit.modifier.push("underwater familiar");
      }
      outfit.equip(getBestWaterBreathingEquipment(maximumYachtzees()).item);
      outfit.equip(bestFamUnderwaterGear(yachtzeeFamiliar));
      if (overdrunk) outfit.equip($item`Drunkula's wineglass`);
      (_outfit$avoid = outfit.avoid).push.apply(_outfit$avoid, _toConsumableArray($items`anemoney clip, cursed magnifying glass, Kramco Sausage-o-Matic™, cheap sunglasses`));
      if (outfit.haveEquipped($item`The Crown of Ed the Undying`)) {
        outfit.setModes({
          edpiece: "fish"
        });
      }
      outfit.familiar = yachtzeeFamiliar;
      return outfit;
    },
    combat: new GarboStrategy(() => Macro.abortWithMsg("Unexpected combat while attempting yachtzee adventure")),
    turns: maximumYachtzees,
    spendsTurn: true
  }, {
    name: "Use Fishy Pipe for Yachtzee",
    completed: () => have$P($effect`Fishy`),
    ready: () => have$P($item`fishy pipe`) && !get$2("_fishyPipeUsed"),
    do: () => kolmafia.use($item`fishy pipe`),
    turns: 0,
    sobriety: () => willDrunkAdventure() ? "drunk" : "sober",
    spendsTurn: false
  }, {
    name: "Use Skatepark for Yachtzee",
    completed: () => have$P($effect`Fishy`),
    ready: () => get$2("skateParkStatus") === "ice" && !get$2("_skateBuff1"),
    do: () => kolmafia.cliExecute("skate lutz"),
    turns: 0,
    sobriety: () => willDrunkAdventure() ? "drunk" : "sober",
    spendsTurn: false
  }, {
    name: "Apriling Band Tuba Yachtzee NC Force",
    completed: () => get$2("noncombatForcerActive"),
    ready: () => have$P($item`Apriling band tuba`) && $item`Apriling band tuba`.dailyusesleft > 0 && have$P($effect`Fishy`),
    do: () => play($item`Apriling band tuba`),
    turns: 0,
    sobriety: () => willDrunkAdventure() ? "drunk" : "sober",
    spendsTurn: false
  }, {
    name: "Clara Yachtzee NC Force",
    completed: () => get$2("noncombatForcerActive"),
    ready: () => shouldClara("yachtzee") && have$P($effect`Fishy`),
    do: () => {
      kolmafia.use($item`Clara's bell`);
    },
    turns: 0,
    sobriety: () => willDrunkAdventure() ? "drunk" : "sober",
    spendsTurn: false
  }, {
    name: "Cincho Yachtzee NC Force",
    completed: () => get$2("noncombatForcerActive"),
    ready: () => currentCinch() >= 60 && have$P($effect`Fishy`),
    do: () => kolmafia.useSkill($skill`Cincho: Fiesta Exit`),
    turns: 0,
    sobriety: () => willDrunkAdventure() ? "drunk" : "sober",
    spendsTurn: false
  }]
}];

var EMPTY_CONTEXT = {
  banish: null
};

function dailyDungeon(additionalReady) {
  return {
    completed: () => get$2("dailyDungeonDone"),
    ready: () => additionalReady() && garboValue($item`fat loot token`) > get$2("valueOfAdventure") * clamp(15 - get$2("_lastDailyDungeonRoom"), 0, 3),
    choices: () => ({
      689: 1,
      690: 2,
      691: kolmafia.haveEquipped($item`candy cane sword cane`) && !get$2("candyCaneSwordDailyDungeon") ? 4 : 2,
      692: 3,
      693: 2
    }),
    acquire: $items`ring of Detect Boring Doors, eleven-foot pole, Pick-O-Matic lockpicks`.map(i => ({
      item: i
    })),
    do: $location`The Daily Dungeon`,
    combat: new GarboStrategy(() => Macro.kill()),
    turns: () => clamp(15 - get$2("_lastDailyDungeonRoom"), 0, 3),
    outfit: {
      equip: $items`candy cane sword cane`
    },
    spendsTurn: true
  };
}
function lavaDogs(additionalReady, baseSpec) {
  return {
    completed: () => lavaDogsComplete(),
    ready: () => additionalReady() && globalOptions.ascend && lavaDogsAccessible() && garboValue($item`Volcoino`) > 6 * get$2("valueOfAdventure") + (hotTubAvailable() ? 0 : kolmafia.mallPrice($item`soft green echo eyedrop antidote`)) && $items`June cleaver, Space Trip safety headphones`.some(i => have$P(i) && kolmafia.canEquip(i)),
    prepare: () => {
      var metalValue = get$2("_volcanoSuperduperheatedMetal") ? garboValue($item`superheated metal`) : sum([[$item`superheated metal`, 0.95], [$item`superduperheated metal`, 0.05]], _ref => {
        var _ref2 = _slicedToArray(_ref, 2),
          item = _ref2[0],
          rate = _ref2[1];
        return rate * garboValue(item);
      });
      if (metalValue > kolmafia.mallPrice($item`heat-resistant sheet metal`)) {
        acquire(1, $item`heat-resistant sheet metal`, metalValue);
      }
    },
    do: $location`The Bubblin' Caldera`,
    outfit: () => {
      var avoid = $items`carnivorous potted plant, mutant crown, mutant arm, mutant legs, shield of the Skeleton Lord`;
      if (!have$P($effect`Drenched in Lava`)) return baseSpec;
      var weapon = have$P($item`June cleaver`) ? $item`June cleaver` : [];
      var modifier = ["Muscle"];
      if (!have$P($item`June cleaver`)) modifier.push(`-7 Monster Level`);
      return freeFightOutfit(_objectSpread2(_objectSpread2({}, baseSpec), {}, {
        modifier,
        weapon,
        avoid
      }), $location`The Bubblin' Caldera`);
    },
    combat: new GarboStrategy(() => Macro.kill()),
    turns: () => clamp(7 - $location`The Bubblin' Caldera`.turnsSpent, 0, 7),
    spendsTurn: true
  };
}
function luckyTasks(sobriety, additionalReady) {
  return [{
    name: `Lucky Adventure (${sobriety})`,
    completed: () => !have$P($effect`Lucky!`),
    ready: () => additionalReady(),
    do: () => getBestLuckyAdventure().location,
    outfit: () => sobriety === "drunk" ? {
      offhand: $item`Drunkula's wineglass`
    } : {},
    combat: new GarboStrategy(() => Macro.abortWithMsg("Unexpected combat while attempting Lucky! adventure")),
    sobriety,
    spendsTurn: true,
    turns: 0 // Turns spent is handled by Lucky Sources
  }, _objectSpread2(_objectSpread2({}, embezzlerFightTask), {}, {
    name: `Lucky Embezzler (${sobriety})`,
    ready: () => additionalReady() && embezzlerFightTask.ready()
  })].concat(_toConsumableArray(luckySourceTasks.map(task => _objectSpread2(_objectSpread2({}, task), {}, {
    ready: () => additionalReady() && getBestLuckyAdventure().phase === "barf" && getBestLuckyAdventure().value() > get$2("valueOfAdventure") && task.ready()
  }))));
}
function vampOut(additionalReady) {
  return {
    ready: () => additionalReady() && have$P($item`plastic vampire fangs`) && garboValue($item`Interview With You (a Vampire)`) > get$2("valueOfAdventure"),
    completed: () => get$2("_interviewMasquerade"),
    choices: () => ({
      546: 12
    }),
    do: () => {
      kolmafia.visitUrl("place.php?whichplace=town&action=town_vampout");
      kolmafia.runChoice(-1);
    },
    outfit: () => freeFightOutfit({
      equip: $items`plastic vampire fangs`
    }, kolmafia.Location.none),
    spendsTurn: true,
    turns: () => 1
  };
}
var bestDupeItem = null;
function getBestDupeItem() {
  if (bestDupeItem === null || !have$P(bestDupeItem)) {
    // Machine elf can dupe PVPable food, booze, spleen item or potion
    var validItems = kolmafia.Item.all().filter(i => i.tradeable && i.discardable && (i.inebriety || i.fullness || i.potion || i.spleen) && have$P(i));
    if (globalOptions.prefs.dmtDupeItem && validItems.includes(globalOptions.prefs.dmtDupeItem)) {
      bestDupeItem = globalOptions.prefs.dmtDupeItem;
    } else {
      bestDupeItem = maxBy(validItems, garboValue);
    }
  }
  return bestDupeItem;
}
function canForceNoncombat() {
  return get$2("noncombatForcerActive") || !get$2("_claraBellUsed") && have$P($item`Clara's bell`);
}
function canGetFusedFuse() {
  return realmAvailable("hot") && [1, 2, 3].some(it => get$2(`_volcanoItem${it}`) === $item`fused fuse`.id) && canForceNoncombat();
}
var peridotZone = () => getAvailableUltraRareZones().find(l => canImperil(l) && !unperidotableZones.includes(l));
var NonBarfTurnTasks = [{
  name: "Make Mimic Eggs (whatever we can)",
  ready: () => have$P($familiar`Chest Mimic`),
  completed: () => get$2("_mimicEggsObtained") >= 11 || $familiar`Chest Mimic`.experience < minimumMimicExperience(),
  do: () => {
    if (!differentiableQuantity(globalOptions.target)) {
      receive(globalOptions.target);
    }
    differentiate(globalOptions.target);
  },
  outfit: () => meatTargetOutfit({
    familiar: $familiar`Chest Mimic`
  }),
  combat: new GarboStrategy(() => Macro.meatKill()),
  turns: () => globalOptions.ascend ? clamp(Math.floor($familiar`Chest Mimic`.experience / 50) - 1, 1, 11 - get$2("_mimicEggsObtained")) : 0,
  spendsTurn: () => !globalOptions.target.attributes.includes("FREE")
}, {
  name: "Machine Elf Dupe",
  ready: () => have$P($familiar`Machine Elf`) &&
  // Dupe at end of day even if not ascending, encountersUntilDMTChoice does not reset on rollover
  willDrunkAdventure() === !sober() && get$2("encountersUntilDMTChoice") === 0 && garboValue(getBestDupeItem()) > get$2("valueOfAdventure"),
  completed: () => get$2("lastDMTDuplication") === kolmafia.myAscensions(),
  do: $location`The Deep Machine Tunnels`,
  prepare: () => {
    if (kolmafia.itemAmount(getBestDupeItem()) === 0) {
      withProperty("autoSatisfyWithMall", false, () => kolmafia.retrieveItem(getBestDupeItem()));
    }
  },
  outfit: () => sober() ? {
    avoid: $items`Kramco Sausage-o-Matic™`,
    familiar: $familiar`Machine Elf`
  } : {
    offhand: $item`Drunkula's wineglass`,
    familiar: $familiar`Machine Elf`
  },
  combat: new GarboStrategy(() => Macro.abortWithMsg("Hit unexpected combat!")),
  turns: () => 1,
  spendsTurn: true,
  choices: () => ({
    1119: 4,
    1125: `1&iid=${getBestDupeItem().id}`
  })
}, _objectSpread2(_objectSpread2({
  name: "Lava Dogs (drunk)"
}, lavaDogs(() => willDrunkAdventure(), {
  offhand: $item`Drunkula's wineglass`
})), {}, {
  sobriety: "drunk"
}), _objectSpread2(_objectSpread2({
  name: "Lava Dogs (sober)"
}, lavaDogs(() => !willDrunkAdventure(), {})), {}, {
  sobriety: "sober"
})].concat(_toConsumableArray(getTasks(yachtzeeQuest)), [// Use NC forces and adventure to get the Yachtzee NC
_objectSpread2(_objectSpread2({
  name: "Daily Dungeon (drunk)"
}, dailyDungeon(() => willDrunkAdventure())), {}, {
  outfit: () => freeFightOutfit({
    offhand: $item`Drunkula's wineglass`,
    equip: $items`ring of Detect Boring Doors`
  }, $location`The Daily Dungeon`),
  sobriety: "drunk"
}), _objectSpread2(_objectSpread2({
  name: "Daily Dungeon (sober)"
}, dailyDungeon(() => !willDrunkAdventure())), {}, {
  outfit: () => freeFightOutfit({
    equip: $items`ring of Detect Boring Doors`
  }, $location`The Daily Dungeon`),
  sobriety: "sober"
}), _objectSpread2(_objectSpread2({
  name: "Vamp Out (drunk)"
}, vampOut(() => willDrunkAdventure())), {}, {
  sobriety: "drunk"
}), _objectSpread2(_objectSpread2({
  name: "Vamp Out (sober)"
}, vampOut(() => !willDrunkAdventure())), {}, {
  sobriety: "sober"
}), {
  name: "Fused Fuse",
  completed: () => get$2("_volcanoItemRedeemed"),
  ready: canGetFusedFuse,
  do: $location`LavaCo™ Lamp Factory`,
  prepare: () => get$2("noncombatForcerActive") || kolmafia.use($item`Clara's bell`),
  post: () => {
    kolmafia.visitUrl("place.php?whichplace=airport_hot&action=airport4_questhub");
    var option = [1, 2, 3].find(it => get$2(`_volcanoItem${it}`) === $item`fused fuse`.id);
    if (option) kolmafia.runChoice(option);
    kolmafia.visitUrl("main.php");
  },
  outfit: () => sober() ? {
    avoid: $items`Kramco Sausage-o-Matic™`
  } : {
    offhand: $item`Drunkula's wineglass`
  },
  combat: new GarboStrategy(() => Macro.abortWithMsg("Hit unexpected combat!")),
  turns: () => 1,
  spendsTurn: true,
  choices: {
    1091: 7
  }
}], _toConsumableArray(luckyTasks("sober", () => !willDrunkAdventure())), _toConsumableArray(luckyTasks("drunk", () => willDrunkAdventure())), [{
  name: "Map for Pills",
  completed: () => kolmafia.availableAmount($item`Map to Safety Shelter Grimace Prime`) === 0,
  choices: () => ({
    536: kolmafia.availableAmount($item`distention pill`) < kolmafia.availableAmount($item`synthetic dog hair pill`) + kolmafia.availableAmount($item`Map to Safety Shelter Grimace Prime`) ? 1 : 2
  }),
  do: () => withProperty("choiceAdventureScript", "", () => {
    ensureEffect($effect`Transpondent`);
    kolmafia.use($item`Map to Safety Shelter Grimace Prime`);
    return true;
  }),
  spendsTurn: true,
  sobriety: "drunk",
  turns: () => kolmafia.availableAmount($item`Map to Safety Shelter Grimace Prime`)
}, {
  name: "Peridot Fish for UR",
  ready: () => have$P($item`Peridot of Peril`) && !(willDrunkAdventure() && sober()),
  completed: () => !peridotZone(),
  do: peridotZone,
  outfit: () => freeFightOutfit(sober() ? {
    acc1: $item`Peridot of Peril`
  } : {
    acc1: $item`Peridot of Peril`,
    offhand: $item`Drunkula's wineglass`
  }, peridotZone() ?? kolmafia.Location.none),
  turns: () => peridotZone() ? 1 : 0,
  spendsTurn: false,
  combat: new GarboStrategy(() => Macro.kill()),
  choices: () => _objectSpread2(_objectSpread2({}, wanderer().getChoices(peridotZone() ?? $location.none)), {}, {
    1557: 2
  })
}, {
  name: "Use Day Shorteners (drunk)",
  ready: () => globalOptions.ascend && garboValue($item`extra time`) > kolmafia.mallPrice($item`day shortener`) + 5 * get$2("valueOfAdventure"),
  completed: () => get$2(`_garboDayShortenersUsed`, 0) >= 3,
  // Arbitrary cap at 3, since using 3 results in only 1 adventure
  do: () => {
    if (acquire(1, $item`day shortener`, garboValue($item`extra time`) - 5 * get$2("valueOfAdventure"), false)) {
      kolmafia.use($item`day shortener`);
    }
    _set(`_garboDayShortenersUsed`, get$2(`_garboDayShortenersUsed`, 0) + 1);
  },
  spendsTurn: true,
  sobriety: "drunk",
  turns: () => 5 * (3 - get$2(`_garboDayShortenersUsed`, 0))
}, {
  name: "Use Day Shorteners (sober)",
  ready: () => !globalOptions.ascend && garboValue($item`extra time`) > kolmafia.mallPrice($item`day shortener`) + 5 * get$2("valueOfAdventure"),
  completed: () => get$2(`_garboDayShortenersUsed`, 0) >= 3,
  // Arbitrary cap at 3, since using 3 results in only 1 adventure
  do: () => {
    if (acquire(1, $item`day shortener`, garboValue($item`extra time`) - 5 * get$2("valueOfAdventure"), false)) {
      kolmafia.use($item`day shortener`);
    }
    _set(`_garboDayShortenersUsed`, get$2(`_garboDayShortenersUsed`, 0) + 1);
  },
  spendsTurn: true,
  sobriety: "sober",
  turns: () => 5 * (3 - get$2(`_garboDayShortenersUsed`, 0))
}]);
function nonBarfTurns() {
  return sum(NonBarfTurnTasks.filter(t => {
    var _t$ready;
    return (((_t$ready = t.ready) === null || _t$ready === void 0 ? void 0 : _t$ready.call(t, EMPTY_CONTEXT)) ?? true) && !t.completed(EMPTY_CONTEXT);
  }), t => undelay(t.turns));
}
var startedNonBarf = false;
var NonBarfTurnQuest = {
  name: "Non Barf Turn",
  tasks: NonBarfTurnTasks,
  ready: () => {
    if (!startedNonBarf) {
      startedNonBarf = clamp(kolmafia.myAdventures() - wanderingCopytargetsRemaining(), 1, kolmafia.myAdventures()) <= nonBarfTurns() + globalOptions.saveTurns;
    }
    return startedNonBarf;
  },
  completed: () => !canContinue()
};

var TurnGenTasks = [{
  name: "Sausage",
  ready: () => kolmafia.myAdventures() <= 1 + globalOptions.saveTurns,
  completed: () => howManySausagesCouldIEat() === 0,
  prepare: () => kolmafia.maximize("MP", false),
  do: () => kolmafia.eat(howManySausagesCouldIEat(), $item`magical sausage`),
  spendsTurn: false
}, {
  name: "Sweatpants",
  ready: () => !globalOptions.nodiet && canUseSkill($skill`Sweat Out Some Booze`) && kolmafia.myInebriety() > 0 && kolmafia.myAdventures() <= 1 + globalOptions.saveTurns,
  completed: () => $skill`Sweat Out Some Booze`.dailylimit === 0 || kolmafia.myInebriety() - potentialCasts($skill`Sweat Out Some Booze`) > kolmafia.inebrietyLimit(),
  do: () => {
    while (canUseSkill($skill`Sweat Out Some Booze`) && kolmafia.myInebriety() > 0) {
      useSkill($skill`Sweat Out Some Booze`);
    }
    consumeDiet(computeDiet().sweatpants(), "SWEATPANTS");
  },
  spendsTurn: false
}, {
  name: "Law of Averages",
  ready: () => kolmafia.myAdventures() <= Math.min(1 + globalOptions.saveTurns, 199),
  completed: () => $item`Law of Averages`.dailyusesleft === 0,
  do: () => kolmafia.use($item`Law of Averages`),
  spendsTurn: false
}];
var TurnGenQuest = {
  name: "Turn Gen",
  tasks: TurnGenTasks
};

var isGhost = () => get$2("_voteMonster") === $monster`angry ghost`;
var isMutant = () => get$2("_voteMonster") === $monster`terrible mutant`;
var isSteve = () => get$2("nextSpookyravenStephenRoom") === $location`The Haunted Laboratory`;
function createWandererOutfit(details, spec, additionalOutfitOptions) {
  var wanderTarget = wanderer().getTarget(undelay(details));
  var needPeridot = wanderTarget.peridotMonster !== $monster.none;
  var needBCZ = wanderTarget.useRefractedGaze ?? false;
  var needMonodent = wanderTarget.useFeesh ?? false;
  var sourceOutfit = Outfit.from(undelay(spec), new Error(`Failed to build outfit for Wanderer from ${JSON.stringify(undelay(spec))}`));
  if (wanderTarget.familiar !== $familiar`none`) {
    sourceOutfit.familiar = wanderTarget.familiar;
  }
  if (needPeridot) sourceOutfit.equip($item`Peridot of Peril`);
  if (needBCZ) sourceOutfit.equip($item`blood cubic zirconia`);
  if (needMonodent) sourceOutfit.equip($item`Monodent of the Sea`);
  return freeFightOutfit(sourceOutfit.spec(), undelay(details), additionalOutfitOptions);
}
function wanderTask(details, spec, base) {
  var additionalOutfitOptions = arguments.length > 3 && arguments[3] !== undefined ? arguments[3] : {};
  return _objectSpread2({
    do: () => wanderer().getTarget(undelay(details)).location,
    choices: () => wanderer().getChoices(undelay(details)),
    outfit: () => createWandererOutfit(details, spec, additionalOutfitOptions),
    spendsTurn: false,
    combat: new GarboStrategy(() => Macro.basicCombat())
  }, base);
}
function shouldGoUnderwater() {
  if (!sober()) return false;
  if (kolmafia.myLevel() < 11) return false;
  if (questStep$1("questS01OldGuy") === -1) {
    kolmafia.visitUrl("place.php?whichplace=sea_oldman&action=oldman_oldman");
  }
  if (!get$1("Adventure Underwater") && waterBreathingEquipment.every(item => !have$P(item) || !kolmafia.canEquip(item))) {
    return false;
  }
  if (!get$1("Underwater Familiar") && familiarWaterBreathingEquipment.every(item => !have$P(item))) {
    return false;
  }
  if (have$P($item`envyfish egg`) || globalOptions.ascend && get$2("_envyfishEggUsed")) {
    return false;
  }
  if (!kolmafia.canAdventure($location`The Briny Deeps`)) return false;

  // TODO: if you didn't digitize a target, this equation may not be right
  if (kolmafia.mallPrice($item`pulled green taffy`) > (targetingMeat() ? MEAT_TARGET_MULTIPLIER() * get$2("valueOfAdventure") : get$2("valueOfAdventure"))) {
    return false;
  }
  if (have$P($effect`Fishy`)) return true;
  if (willYachtzee()) return false;
  if (have$P($item`fishy pipe`) && !get$2("_fishyPipeUsed")) {
    kolmafia.use($item`fishy pipe`);
    if (have$P($effect`Fishy`)) return true;
  }
  if (get$2("skateParkStatus") === "ice" && !get$2("_skateBuff1")) {
    kolmafia.cliExecute("skate lutz");
    if (have$P($effect`Fishy`)) return true;
  }
  return false;
}

/**
 * Creates autoattack and postAuto macros for digitize wanderer fights.
 * @param targetKillMacro - Macro to use when digitize monster is the target
 * @param nonTargetKillMacro - Macro to use otherwise (defaults to Macro.kill())
 */
function digitizeMacros(prepend) {
  var makeMacro = useAutoattackCondition => () => {
    var digitizeMonster = getDigitizeMonster() ?? $monster.none;
    var condition = useAutoattackCondition ? digitizeMonster : `(monsterid ${digitizeMonster.id}) && !gotjump && !(pastround 2)`;
    return Macro.if_(condition, Macro.externalIf(digitizeMonster === globalOptions.target, (undelay(prepend) ?? new Macro()).meatKill(), Macro.kill())).abortWithMsg(`Expected a digitized ${digitizeMonster}, but encountered something else.`);
  };
  return [makeMacro(true), makeMacro(false)];
}
var mimicSpec = () => get$2("_mimicEggsObtained") < 11 && $familiar`Chest Mimic`.experience > (wanderingCopytargetsRemaining() === 1 ? 50 : (11 - get$2("_mimicEggsObtained")) * 50) ? {
  familiar: $familiar`Chest Mimic`
} : {};
var digitizedTarget = () => have$I() && getDigitizeMonster() === globalOptions.target;
var BarfTurnTasks = [{
  name: "Latte",
  completed: () => !shouldFillLatte(),
  do: () => tryFillLatte(),
  spendsTurn: false
}, {
  name: "Lights Out",
  ready: () => kolmafia.canAdventure(get$2("nextSpookyravenStephenRoom") ?? $location`none`) && get$2("nextSpookyravenStephenRoom") !== get$2("ghostLocation") && kolmafia.totalTurnsPlayed() % 37 === 0,
  completed: () => kolmafia.totalTurnsPlayed() === get$2("lastLightsOutTurn"),
  do: () => get$2("nextSpookyravenStephenRoom"),
  outfit: () => meatTargetOutfit(sober() ? {} : {
    offhand: $item`Drunkula's wineglass`
  }),
  spendsTurn: isSteve,
  combat: new GarboStrategy(() => Macro.if_($monster`Stephen Spookyraven`, Macro.basicCombat()).abortWithMsg("Expected to fight Stephen Spookyraven, but didn't!"))
}, {
  name: "Use Walkie Talkie for Ghost",
  ready: () => kolmafia.mallPrice($item`almost-dead walkie-talkie`) < globalOptions.prefs.valueOfFreeFight && get$2("nextParanormalActivity") <= kolmafia.totalTurnsPlayed(),
  completed: () => have$P($item`protonic accelerator pack`) || get$2("questPAGhost") === "started",
  do: () => {
    if (acquire(1, $item`almost-dead walkie-talkie`, globalOptions.prefs.valueOfFreeFight, false)) {
      kolmafia.use($item`almost-dead walkie-talkie`);
    }
  },
  spendsTurn: false,
  limit: {
    skip: 40
  } // Safeguard to avoid infinite loops if mallPrice can bug
}, {
  name: "Proton Ghost",
  ready: () => !!get$2("ghostLocation"),
  completed: () => get$2("questPAGhost") === "unstarted",
  do: () => get$2("ghostLocation"),
  outfit: () => freeFightOutfit({
    modifier: get$2("ghostLocation") === $location`The Icy Peak` ? ["Cold Resistance 5 min"] : [],
    back: have$P($item`protonic accelerator pack`) ? $item`protonic accelerator pack` : []
  }, get$2("ghostLocation")),
  choices: () => wanderer().getChoices(get$2("ghostLocation") ?? $location.none),
  combat: new GarboStrategy(() => have$P($item`protonic accelerator pack`) ? Macro.ghostBustin() : Macro.basicCombat()),
  spendsTurn: false,
  // Ghost fights are currently hard
  // and they resist physical attacks!
  sobriety: "sober"
}, wanderTask(() => ({
  wanderer: "wanderer",
  drunkSafe: !isGhost()
}), () => ({
  equip: [$item`"I Voted!" sticker`].concat(_toConsumableArray(!sober() && !isGhost() ? $items`Drunkula's wineglass` : []), _toConsumableArray(!have$P($item`mutant crown`) && isMutant() ? $items`mutant arm, mutant legs`.filter(i => have$P(i)) : []))
}), {
  name: "Vote Wanderer",
  ready: () => have$P($item`"I Voted!" sticker`) && kolmafia.totalTurnsPlayed() % 11 === 1 && get$2("_voteFreeFights") < 3,
  completed: () => get$2("lastVoteMonsterTurn") >= kolmafia.totalTurnsPlayed(),
  sobriety: () => isGhost() ? "sober" : undefined
}), {
  name: "Thesis",
  ready: () => have$P($familiar`Pocket Professor`) && kolmafia.myAdventures() === 1 + globalOptions.saveTurns && $familiar`Pocket Professor`.experience >= 400,
  completed: () => get$2("_thesisDelivered"),
  do: () => deliverThesisIfAble(),
  sobriety: "sober",
  spendsTurn: true
}, {
  name: "Digitize Wanderer (Underwater, for Green Taffy)",
  completed: () => get("Digitize Monster") > 0,
  ready: shouldGoUnderwater,
  acquire: () => [{
    item: $item`pulled green taffy`
  }],
  do: $location`The Briny Deeps`,
  outfit: () => meatTargetOutfit({}, $location`The Briny Deeps`),
  combat: _construct(GarboStrategy, _toConsumableArray(digitizeMacros(Macro.item($item`pulled green taffy`)))),
  sobriety: "sober",
  spendsTurn: true
}, {
  name: "Digitize Wanderer",
  completed: () => get("Digitize Monster") > 0,
  outfit: () => digitizedTarget() ? meatTargetOutfit(mimicSpec(), {
    wanderer: "wanderer",
    allowEquipment: false
  }) : freeFightOutfit(undefined, wanderer().getTarget({
    wanderer: "wanderer",
    allowEquipment: false
  }).location),
  do: () => wanderer().getTarget({
    wanderer: "wanderer",
    allowEquipment: false
  }).location,
  choices: () => wanderer().getChoices({
    wanderer: "wanderer",
    allowEquipment: false
  }),
  combat: _construct(GarboStrategy, _toConsumableArray(digitizeMacros())),
  spendsTurn: () => {
    var _SourceTerminal$getDi;
    return !((_SourceTerminal$getDi = getDigitizeMonster()) !== null && _SourceTerminal$getDi !== void 0 && _SourceTerminal$getDi.attributes.includes("FREE"));
  }
}, {
  name: "Club Into Next Week Monster",
  completed: () => turnsUntilNextWeekFight() > 0,
  outfit: () => clubIntoNextWeekMonster() === globalOptions.target ? meatTargetOutfit(mimicSpec(), {
    wanderer: "wanderer",
    allowEquipment: false
  }) : freeFightOutfit({}, {
    wanderer: "wanderer",
    allowEquipment: false
  }),
  do: () => wanderer().getTarget({
    wanderer: "wanderer",
    allowEquipment: false
  }).location,
  choices: () => wanderer().getChoices({
    wanderer: "wanderer",
    allowEquipment: false
  }),
  combat: new GarboStrategy(() => Macro.target("club 'em into next week")),
  spendsTurn: () => {
    var _LegendarySealClubbin;
    return !((_LegendarySealClubbin = clubIntoNextWeekMonster()) !== null && _LegendarySealClubbin !== void 0 && _LegendarySealClubbin.attributes.includes("FREE"));
  }
}, wanderTask("wanderer", {
  offhand: $item`Kramco Sausage-o-Matic™`
}, {
  name: "Guaranteed Kramco",
  ready: () => romanticMonsterImpossible(),
  completed: () => !kramcoGuaranteed()
}), wanderTask("wanderer", {
  offhand: $item`cursed magnifying glass`
}, {
  name: "Void Monster",
  ready: () => have$P($item`cursed magnifying glass`) && get$2("_voidFreeFights") < 5,
  completed: () => get$2("cursedMagnifyingGlassCount") !== 13
}), {
  name: "Envyfish Egg",
  ready: () => have$P($item`envyfish egg`) && get$2("envyfishMonster") === globalOptions.target,
  completed: () => get$2("_envyfishEggUsed"),
  do: () => kolmafia.use($item`envyfish egg`),
  spendsTurn: true,
  outfit: () => meatTargetOutfit(),
  combat: new GarboStrategy(() => Macro.target("envyfish egg"))
}, wanderTask("yellow ray", {}, {
  name: "Cheese Wizard Fondeluge",
  ready: () => have$P($skill`Fondeluge`) && romanticMonsterImpossible(),
  completed: () => have$P($effect`Everything Looks Yellow`),
  combat: new GarboStrategy(() => Macro.if_(globalOptions.target, Macro.meatKill()).refractedGaze().familiarActions().duplicate().skill($skill`Fondeluge`)),
  duplicate: true,
  sobriety: "sober"
}), wanderTask("yellow ray", {
  shirt: $items`Jurassic Parka`,
  modes: {
    parka: "dilophosaur"
  }
}, {
  name: "Spit Acid",
  ready: () => have$P($item`Jurassic Parka`) && romanticMonsterImpossible(),
  completed: () => have$P($effect`Everything Looks Yellow`),
  combat: new GarboStrategy(() => Macro.if_(globalOptions.target, Macro.meatKill()).refractedGaze().familiarActions().duplicate().skill($skill`Spit jurassic acid`)),
  sobriety: "sober",
  duplicate: true
}), wanderTask("freefight (no items)", () => ({
  weapon: $item`Sheriff pistol`,
  acc1: $item`Sheriff badge`,
  acc2: $item`Sheriff moustache`
}), {
  name: "Assert your Authority",
  ready: () => have$P($item`Sheriff pistol`) && have$P($item`Sheriff badge`) && have$P($item`Sheriff moustache`) && romanticMonsterImpossible(),
  completed: () => get$2("_assertYourAuthorityCast") >= 3,
  combat: new GarboStrategy(() => Macro.if_(globalOptions.target, Macro.meatKill()).familiarActions().skill($skill`Assert your Authority`)),
  sobriety: "sober"
}), wanderTask("freefight", {}, {
  name: "Pig Skinner Free-For-All",
  ready: () => have$P($skill`Free-For-All`) && romanticMonsterImpossible(),
  completed: () => have$P($effect`Everything Looks Red`),
  combat: new GarboStrategy(() => Macro.if_(globalOptions.target, Macro.meatKill()).refractedGaze().familiarActions().skill($skill`Free-For-All`)),
  sobriety: "sober",
  duplicate: true
}), wanderTask("conditional freefight", {
  offhand: guaranteedBullseye() || have$P($item`spring shoes`) ? [] : $item`Roman Candelabra`,
  acc1: $item`Everfull Dart Holster`,
  acc2: guaranteedBullseye() || !have$P($item`spring shoes`) ? [] : $item`spring shoes`,
  modifier: guaranteedBullseye() ? [] : "Monster Level"
}, {
  name: "Darts: Bullseye",
  ready: safeToAttemptBullseye,
  completed: () => !canBullseye(),
  combat: new GarboStrategy(() => Macro.if_(globalOptions.target, Macro.meatKill()).familiarActions().skill($skill`Darts: Aim for the Bullseye`).trySkill($skill`Spring Away`).trySkill($skill`Blow the Green Candle!`)),
  sobriety: "sober",
  duplicate: true
}), wanderTask("freefight", {}, {
  name: "Heavy Rains Lightning Strike",
  ready: () => have$P($skill`Lightning Strike`) && kolmafia.myLightning() >= 20,
  completed: () => kolmafia.myLightning() < 20,
  combat: new GarboStrategy(() => Macro.if_(globalOptions.target, Macro.meatKill()).refractedGaze().familiarActions().skill($skill`Lightning Strike`)),
  duplicate: true
}), wanderTask("yellow ray", {}, {
  name: "Shocking Lick",
  ready: () => romanticMonsterImpossible(),
  completed: () => get$2("shockingLickCharges") === 0,
  combat: new GarboStrategy(() => Macro.if_(globalOptions.target, Macro.meatKill()).refractedGaze().familiarActions().duplicate().skill($skill`Shocking Lick`)),
  duplicate: true,
  sobriety: "sober"
}), wanderTask("freerun", () => ({
  equip: $items`spring shoes, carnivorous potted plant`.filter(i => have$P(i))
}), {
  name: "Spring Shoes Freerun",
  ready: () => have$P($item`spring shoes`) && romanticMonsterImpossible() && (kolmafia.getWorkshed() !== $item`model train set` || next() !== Station.GAIN_MEAT) && (guaranteedBullseye() || !safeToAttemptBullseye() || have$P($skill`Free-For-All`) || have$P($effect`Everything Looks Red`, 30)),
  completed: () => have$P($effect`Everything Looks Green`),
  combat: new GarboStrategy(() => Macro.if_(globalOptions.target, Macro.meatKill()).familiarActions().skill($skill`Spring Away`), undefined, () => !have$P($item`carnivorous potted plant`) // Do not use autoattack with carn plant, it will cancel the swallow
  ),
  sobriety: "sober"
}, {
  familiarOptions: {
    mode: "run"
  }
}), {
  name: "Gingerbread Noon",
  completed: () => minutesToNoon() !== 0,
  do: $location`Gingerbread Train Station`,
  choices: {
    1204: 1
  },
  combat: new GarboStrategy(() => Macro.abortWithMsg("We thought it was noon here in Gingerbread City, but we're in a fight!")),
  outfit: () => sober() ? {} : {
    offhand: $item`Drunkula's wineglass`
  },
  spendsTurn: false
}, {
  name: "Gingerbread Midnight",
  completed: () => minutesToMidnight() !== 0,
  do: () => bestMidnightAvailable().location,
  choices: () => bestMidnightAvailable().choices,
  outfit: () => ({
    equip: bestMidnightAvailable().location === $location`Gingerbread Upscale Retail District` ? kolmafia.outfitPieces("Gingerbread Best") : [],
    offhand: sober() ? undefined : $item`Drunkula's wineglass`
  }),
  combat: new GarboStrategy(() => Macro.abortWithMsg("We thought it was Midnight here in Gingerbread City, but we're in a fight!")),
  spendsTurn: false
}, {
  name: "Rain Man",
  ready: () => kolmafia.myRain() >= 50 && have$P($skill`Rain Man`),
  completed: () => kolmafia.myRain() < 50,
  do: () => {
    rainMan(globalOptions.target);
  },
  combat: new GarboStrategy(() => Macro.meatKill()),
  spendsTurn: () => !globalOptions.target.attributes.includes("FREE"),
  outfit: () => meatTargetOutfit()
}, {
  name: "Make Mimic Eggs (maximum eggs)",
  ready: () => shouldMakeEgg(),
  completed: () => get$2("_mimicEggsObtained") >= 11,
  do: () => {
    if (differentiableQuantity(globalOptions.target) < 1) {
      receive(globalOptions.target);
    }
    differentiate(globalOptions.target);
  },
  combat: new GarboStrategy(() => Macro.meatKill()),
  spendsTurn: () => !globalOptions.target.attributes.includes("FREE"),
  outfit: () => meatTargetOutfit({
    familiar: $familiar`Chest Mimic`
  })
}, {
  name: "Fight Mimic Eggs",
  ready: () => globalOptions.ascend,
  completed: () => differentiableQuantity(globalOptions.target) === 0,
  do: () => differentiate(globalOptions.target),
  outfit: () => meatTargetOutfit(),
  combat: new GarboStrategy(() => Macro.meatKill()),
  spendsTurn: () => !globalOptions.target.attributes.includes("FREE")
}, {
  name: "Liana Parachute",
  ready: () => (sober() || have$P($item`Drunkula's wineglass`) && kolmafia.canEquip($item`Drunkula's wineglass`)) && have$g() && shouldCheckParachute() && questStep$1("questL11Worship") > 3 && have$P($item`antique machete`),
  // TODO Support other machete's
  completed: () => have$P($effect`Everything looks Beige`),
  outfit: () => freeFightOutfit({
    weapon: $item`antique machete`
  }, kolmafia.Location.none),
  do: () => fight($monster`dense liana`),
  combat: new GarboStrategy(() => Macro.abortWithMsg("Did not instantly kill the Liana, check what went wrong")),
  prepare: () => {
    if (!sober()) {
      freeFightOutfit({
        offhand: $item`Drunkula's wineglass`
      }, kolmafia.Location.none).dress();
    }
    withChoice(785, 6, () => kolmafia.adv1($location`An Overgrown Shrine (Northeast)`, -1, ""));
    if (!sober()) {
      freeFightOutfit({
        weapon: $item`antique machete`
      }, kolmafia.Location.none).dress();
    }
  },
  post: () => {
    if (!have$P($effect`Everything looks Beige`)) updateParachuteFailure();
  },
  spendsTurn: false
}, {
  name: "Fight Cookbookbat Quest Target",
  ready: () => {
    var questMonster = get$2("_cookbookbatQuestMonster");
    if (!questMonster || hasNameCollision(questMonster)) return false;
    var questLocation = get$2("_cookbookbatQuestLastLocation");
    if (!questLocation || !canAdventureOrUnlock(questLocation, false)) {
      return false;
    }
    var questReward = get$2("_cookbookbatQuestIngredient");
    return have$d() && !!questReward && 3 * garboValue(questReward) > get$2("valueOfAdventure");
  },
  completed: () => {
    var questLocation = get$2("_cookbookbatQuestLastLocation");
    return !questLocation || !canImperil(questLocation) || unperidotableZones.includes(questLocation);
  },
  choices: () => {
    var _get;
    return _objectSpread2({
      1557: `1&bandersnatch=${((_get = get$2("_cookbookbatQuestMonster")) === null || _get === void 0 ? void 0 : _get.id) ?? 0}`
    }, wanderer().getChoices(get$2("_cookbookbatQuestLastLocation") ?? $location.none));
  },
  outfit: () => freeFightOutfit({
    equip: sober() ? $items`Peridot of Peril` : $items`Peridot of Peril, Drunkula's wineglass`,
    familiar: $familiar`Cookbookbat`
  }, get$2("_cookbookbatQuestLastLocation") ?? kolmafia.Location.none),
  do: () => get$2("_cookbookbatQuestLastLocation"),
  combat: new GarboStrategy(() => Macro.basicCombat()),
  spendsTurn: true
}];
var WandererQuest = {
  name: "Wanderers",
  tasks: BarfTurnTasks,
  completed: () => !canContinue()
};

var FarmQuests = () => [TurnGenQuest, DailyExtrasQuest, WandererQuest, NonBarfTurnQuest, FarmTurnQuest()];

function getAutosellableMeltingJunk() {
  return kolmafia.Item.all().filter(i => (get$1("Lasts Until Rollover", i) || globalOptions.ascend && i.quest) && kolmafia.itemAmount(i) && kolmafia.autosellPrice(i) > 0 && (globalOptions.ascend || !["Adventures", "PvP Fights", "Rollover Effect Duration"].some(mod => get$1(mod))));
}
var FinishUpQuest = {
  name: "Finish Up",
  tasks: [{
    name: "Open MayDay™ supply package",
    ready: () => globalOptions.ascend,
    completed: () => kolmafia.availableAmount($item`MayDay™ supply package`) === 0,
    spendsTurn: false,
    do: () => kolmafia.use($item`MayDay™ supply package`, kolmafia.availableAmount($item`MayDay™ supply package`))
  }, {
    name: "Autosell Melting Junk",
    completed: () => getAutosellableMeltingJunk().length === 0,
    spendsTurn: false,
    do: () => getAutosellableMeltingJunk().forEach(i => kolmafia.autosell(i, kolmafia.itemAmount(i)))
  }, {
    name: "Buy one-day ticket to Dinseylandfill",
    ready: () => globalOptions.prefs.buyPass === true && kolmafia.availableAmount($item`FunFunds™`) >= 20,
    completed: () => have$P($item`one-day ticket to Dinseylandfill`),
    spendsTurn: false,
    do: () => kolmafia.buy($coinmaster`The Dinsey Company Store`, 1, $item`one-day ticket to Dinseylandfill`)
  }]
};

var SetupTargetCopyQuest = {
  name: "SetupTargetCopy",
  ready: () => kolmafia.myInebriety() <= kolmafia.inebrietyLimit(),
  tasks: [{
    // Need the daily dungeon to either be totally finished or to be on a NC we can walk away from
    name: "Setup Daily Dungeon",
    outfit: () => freeFightOutfit({
      equip: $items`ring of Detect Boring Doors`
    }, $location`The Daily Dungeon`),
    // walk away from any nc we can walk away from, skip boring doors, open the final chest
    choices: () => ({
      689: 1,
      690: 2,
      691: 2,
      692: 8,
      693: 3
    }),
    acquire: [{
      item: $item`ring of Detect Boring Doors`
    }],
    ready: () => getChangeLastAdvLocationMethod() === "dailydungeon" && have$w() && doingGregFight(),
    completed: () => get$2("dailyDungeonDone") || ["It's Almost Certainly a Trap", "I Wanna Be a Door"].includes(get$2("_lastDailyDungeonEncounter")),
    do: $location`The Daily Dungeon`,
    post: () => _set("_lastDailyDungeonEncounter", get$2("lastEncounter")),
    spendsTurn: true,
    combat: new GarboStrategy(() => Macro.kill())
  }, {
    name: "Fold Spooky Putty sheet",
    ready: () => !have$P($item`Spooky Putty monster`) && getFoldGroup($item`Spooky Putty sheet`).some(item => have$P(item)),
    completed: () => have$P($item`Spooky Putty sheet`),
    do: () => kolmafia.cliExecute("fold spooky putty sheet"),
    spendsTurn: false,
    limit: {
      skip: 1
    }
  },
  // Fix invalid copiers (caused by ascending or combat text-effects)
  {
    name: "Fix Spooky Putty monster",
    ready: () => have$P($item`Spooky Putty monster`),
    completed: () => !!get$2("spookyPuttyMonster") || !have$P($item`Spooky Putty monster`),
    do: () => {
      examine($item`Spooky Putty monster`);
      if (!get$2("spookyPuttyMonster")) {
        // Still invalid, use it to turn back into the spooky putty sheet
        kolmafia.use($item`Spooky Putty monster`);
      }
    },
    spendsTurn: false,
    limit: {
      skip: 1
    }
  }, {
    name: "Fix Rain-Doh box full of monster",
    ready: () => have$P($item`Rain-Doh box full of monster`),
    completed: () => !!get$2("rainDohMonster"),
    do: () => examine($item`Rain-Doh box full of monster`),
    spendsTurn: false,
    limit: {
      skip: 1
    }
  }, {
    name: "Fix shaking 4-d camera",
    ready: () => have$P($item`shaking 4-d camera`),
    completed: () => !!get$2("cameraMonster"),
    do: () => examine($item`shaking 4-d camera`),
    spendsTurn: false,
    limit: {
      skip: 1
    }
  }, {
    name: "Fix envyfish egg",
    ready: () => have$P($item`envyfish egg`),
    completed: () => !!get$2("envyfishMonster"),
    do: () => examine($item`envyfish egg`),
    spendsTurn: false,
    limit: {
      skip: 1
    }
  }, {
    name: "Fix ice sculpture",
    ready: () => have$P($item`ice sculpture`),
    completed: () => !!get$2("iceSculptureMonster"),
    do: () => examine($item`ice sculpture`),
    spendsTurn: false,
    limit: {
      skip: 1
    }
  }, {
    name: "Fix photocopied monster",
    ready: () => have$P($item`photocopied monster`),
    completed: () => !!get$2("photocopyMonster"),
    do: () => examine($item`photocopied monster`),
    spendsTurn: false,
    limit: {
      skip: 1
    }
  }, {
    name: `Discard Fax`,
    ready: () => !get$2("_photocopyUsed") && get$2("photocopyMonster") !== globalOptions.target && have$P($item`Clan VIP Lounge key`) && kolmafia.getClanLounge()["deluxe fax machine"] !== undefined && kolmafia.canFaxbot(globalOptions.target),
    completed: () => !have$P($item`photocopied monster`),
    do: () => kolmafia.cliExecute("fax send"),
    spendsTurn: false,
    limit: {
      skip: 2
    }
  },
  // Fax the copy target before starting, to prevent an abort in case the faxbot networks are down
  {
    name: `Faxbot copytarget`,
    ready: () => !get$2("_photocopyUsed") && have$P($item`Clan VIP Lounge key`) && kolmafia.getClanLounge()["deluxe fax machine"] !== undefined && kolmafia.canFaxbot(globalOptions.target),
    completed: () => have$P($item`photocopied monster`) && get$2("photocopyMonster") === globalOptions.target,
    do: () => {
      kolmafia.faxbot(globalOptions.target);
      for (var i = 0; i < 3; i++) {
        kolmafia.wait(10 + i * 2);
        if (!have$P($item`photocopied monster`)) {
          kolmafia.cliExecute("fax receive");
          examine($item`photocopied monster`);
          if (get$2("photocopyMonster") === globalOptions.target) {
            break;
          }
        }
      }
    },
    spendsTurn: false,
    limit: {
      skip: 1
    }
  }, {
    name: "Acquire amulet coin",
    ready: () => have$P($familiar`Cornbeefadon`) && have$P($item`box of Familiar Jacks`) && amuletCoinValue() >= kolmafia.mallPrice($item`box of Familiar Jacks`),
    completed: () => have$P($item`amulet coin`),
    do: () => {
      kolmafia.use($item`box of Familiar Jacks`);
    },
    outfit: {
      familiar: $familiar`Cornbeefadon`
    },
    spendsTurn: false
  }]
};

// Max price for tickets. You should rethink whether Barf is the best place if they're this expensive.
var TICKET_MAX_PRICE = 500000;
function ensureBarfAccess() {
  if (!(get$2("stenchAirportAlways") || get$2("_stenchAirportToday"))) {
    var ticket = $item`one-day ticket to Dinseylandfill`;
    // TODO: Get better item acquisition logic that e.g. checks own mall store.
    if (!have$P(ticket)) kolmafia.buy(1, ticket, TICKET_MAX_PRICE);
    kolmafia.use(ticket);
  }
}
function defaultTarget() {
  if ($skills`Curse of Weaksauce, Saucegeyser`.every(s => have$P(s))) {
    return maxBy($monsters.all().filter(m => m.wishable && isFreeAndCopyable(m)), valueDrops);
  }
  return $monster`Knob Goblin Elite Guard Captain`;
}
function main() {
  var argString = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : "";
  sinceKolmafiaRevision(29250); // lgr limit properties
  checkGithubVersion();
  Args.fill(globalOptions, argString);
  // Instant returns placed before visiting anything.
  if (globalOptions.version) return; // Since we always print the version, all done!
  if (globalOptions.help) {
    Args.showHelp(globalOptions);
    return;
  }

  // Cowo is for professionals only
  if (globalOptions.prefs.farmingMethod === FarmingMethod.THE_CORAL_CORRAL && (kolmafia.effectFact($monster`sea cow`) !== $effect`Fishy` || get$2("seahorseName") === "")) {
    globalOptions.prefs.farmingMethod = FarmingMethod.BARF_MOUNTAIN;
  }

  // Hit up main.php to get out of easily escapable choices
  kolmafia.visitUrl("main.php");
  if (kolmafia.currentRound() > 0) {
    kolmafia.abort("It seems like you're a bit busy right now. Don't run garbo when you're in combat!");
  }
  if (kolmafia.handlingChoice()) {
    kolmafia.abort("It seems like you're a bit busy right now. Don't run garbo when you're in the middle of a choice adventure.");
  }
  kolmafia.cliExecute("mallcheck.js");
  if (globalOptions.target === $monster.none) {
    globalOptions.target = defaultTarget();
  }
  globalOptions.prefs.yachtzeechain = false;
  if (globalOptions.turns) {
    if (globalOptions.turns >= 0) {
      globalOptions.stopTurncount = kolmafia.myTurncount() + globalOptions.turns;
    } else {
      globalOptions.saveTurns = -globalOptions.turns;
    }
  }
  if (globalOptions.prefs.autoUserConfirm) {
    kolmafia.print("I have set auto-confirm to true and accept all ramifications that come with that.", "red");
  }
  if (stashItems.length > 0) {
    if (globalOptions.returnstash || userConfirmDialog(`Garbo has detected that you have the following items still out of the stash from a previous run of garbo: ${stashItems.map(item => item.name).join(", ")}. Would you like us to return these to the stash now?`, true)) {
      startSession();
      try {
        var clanIdOrName = globalOptions.prefs.stashClan;
        var parsedClanIdOrName = clanIdOrName !== "none" ? clanIdOrName.match(/^\d+$/) ? parseInt(clanIdOrName) : clanIdOrName : null;
        if (parsedClanIdOrName) {
          Clan.with(parsedClanIdOrName, () => {
            for (var _i = 0, _arr = _toConsumableArray(stashItems); _i < _arr.length; _i++) {
              var item = _arr[_i];
              var equipped = [item].concat(_toConsumableArray(getFoldGroup(item))).find(i => kolmafia.haveEquipped(i));
              if (equipped) unequip(equipped);
              if (getFoldGroup(item).some(i => have$P(i))) {
                kolmafia.cliExecute(`fold ${item}`);
              }
              var retrieved = kolmafia.retrieveItem(item);
              if (item === $item`Spooky Putty sheet` && !retrieved && have$P($item`Spooky Putty monster`)) {
                continue;
              }
              kolmafia.print(`Returning ${item} to ${kolmafia.getClanName()} stash.`, HIGHLIGHT);
              if (kolmafia.putStash(item, 1)) {
                stashItems.splice(stashItems.indexOf(item), 1);
              }
            }
          });
        } else throw new Error("Error: No garbo_stashClan set.");
      } finally {
        endSession(false);
      }
    } else {
      if (userConfirmDialog("Are you a responsible friend who has already returned their stash clan items, or promise to do so manually at a later time?", false)) {
        stashItems.splice(0);
      }
    }
  }
  if (globalOptions.returnstash) {
    _set("garboStashItems", stashItems.map(item => kolmafia.toInt(item).toFixed(0)).join(","));
    return;
  }
  if (!$classes`Seal Clubber, Turtle Tamer, Pastamancer, Sauceror, Disco Bandit, Accordion Thief, Cow Puncher, Snake Oiler, Beanslinger, Pig Skinner, Cheese Wizard, Jazz Agent`.includes(kolmafia.myClass())) {
    throw new Error("Garbo does not support this class. It barely supports WOL/SOL avatar classes");
  }
  if (!get$2("kingLiberated") || kolmafia.myLevel() < 13 || kolmafia.Stat.all().some(s => kolmafia.myBasestat(s) < 75)) {
    if (globalOptions.prefs.skipAscensionCheck) {
      kolmafia.logprint("This player is a silly goose, who ignored our warnings about being underleveled.");
    } else {
      var proceedRegardless = userConfirmDialog("Looks like your ascension may not be done, or you may be severely underleveled. Running garbo in an unintended character state can result in serious injury and even death. Are you sure you want to garbologize?", true);
      if (!proceedRegardless) {
        throw new Error("User interrupt requested. Stopping Garbage Collector.");
      } else {
        kolmafia.logprint("This player is a silly goose, who ignored our warnings about being underleveled.");
      }
    }
  }
  if (globalOptions.prefs.valueOfAdventure && globalOptions.prefs.valueOfAdventure <= 3500) {
    throw `Your valueOfAdventure is set to ${globalOptions.prefs.valueOfAdventure}, which is too low for barf farming to be worthwhile. If you forgot to set it, use "set valueOfAdventure = XXXX" to set it to your marginal turn meat value.`;
  }
  if (!globalOptions.nobarf && globalOptions.prefs.valueOfAdventure && globalOptions.prefs.valueOfAdventure >= (globalOptions.nobarf ? 20_000 : 10_000)) {
    throw `Your valueOfAdventure is set to ${globalOptions.prefs.valueOfAdventure}, which is definitely incorrect. Please set it to your reliable marginal turn value.`;
  }
  if (kolmafia.myInebriety() > kolmafia.inebrietyLimit() && (!have$P($item`Drunkula's wineglass`) || !kolmafia.canEquip($item`Drunkula's wineglass`))) {
    throw new Error("Go home, you're drunk. And don't own (or can't equip) Drunkula's wineglass. Consider either being sober or owning Drunkula's wineglass and being able to equip it.");
  }
  var completedProperty = "_garboCompleted";
  _set(completedProperty, "");

  // re-align sweat (useful for diet and outfit)
  examine($item`designer sweatpants`);
  startSession();
  if (!globalOptions.nobarf && !globalOptions.simdiet && FarmingStrategy.ensureBarfAccess) {
    ensureBarfAccess();
  }
  if (globalOptions.simdiet) {
    propertyManager.set({
      logPreferenceChange: true,
      autoSatisfyWithMall: true,
      autoSatisfyWithNPCs: true,
      autoSatisfyWithCoinmasters: true,
      autoSatisfyWithStash: false,
      maximizerFoldables: true,
      autoTuxedo: true,
      autoPinkyRing: true,
      autoGarish: true,
      valueOfInventory: 2,
      suppressMallPriceCacheMessages: true,
      shadowLabyrinthGoal: "effects",
      lightsOutAutomation: 1,
      errorOnAmbiguousFold: false
    });
    runDiet();
    propertyManager.resetAll();
    return;
  }
  var gardens = $items`packet of pumpkin seeds, Peppermint Pip Packet, packet of dragon's teeth, packet of beer seeds, packet of winter seeds, packet of thanksgarden seeds, packet of tall grass seeds, packet of mushroom spores, packet of rock seeds`;
  var startingGarden = gardens.find(garden => Object.getOwnPropertyNames(kolmafia.getCampground()).includes(garden.name));
  if (startingGarden && !$items`packet of tall grass seeds, packet of mushroom spores`.includes(startingGarden) && kolmafia.getCampground()[startingGarden.name] && $items`packet of tall grass seeds, packet of mushroom spores`.some(gardenSeed => have$P(gardenSeed))) {
    if (startingGarden === $item`packet of rock seeds`) {
      kolmafia.visitUrl("campground.php?action=rgarden1&pwd");
      kolmafia.visitUrl("campground.php?action=rgarden2&pwd");
      kolmafia.visitUrl("campground.php?action=rgarden3&pwd");
    } else {
      kolmafia.visitUrl("campground.php?action=garden&pwd");
    }
  }
  var combatFlags = getCombatFlags(["aabosses", "bothcombatinterf"]);
  try {
    kolmafia.print("Collecting garbage!", HIGHLIGHT);
    if (globalOptions.stopTurncount !== null) {
      kolmafia.print(`Stopping in ${globalOptions.stopTurncount - kolmafia.myTurncount()}`, HIGHLIGHT);
    }
    kolmafia.print();
    if (have$P($item`packet of tall grass seeds`) && kolmafia.myGardenType() !== "grass" && kolmafia.myGardenType() !== "mushroom") {
      kolmafia.use($item`packet of tall grass seeds`);
    }
    kolmafia.setAutoAttack(0);
    setCombatFlags({
      flag: "aabosses",
      value: true
    }, {
      flag: "bothcombatinterf",
      value: false
    });
    var maximizerCombinationLimit = isQuickGear() ? 100000 : get$2("maximizerCombinationLimit");
    var bannedAutoRestorers = have$P($item`Cincho de Mayo`) ? ["sleep on your clan sofa", "rest in your campaway tent", "rest at the chateau", "rest at your campground", "free rest"] : [];
    var hpItems = get$2("hpAutoRecoveryItems").split(";").filter(s => !bannedAutoRestorers.includes(s)).join(";");
    var mpItems = get$2("mpAutoRecoveryItems").split(";").filter(s => !bannedAutoRestorers.includes(s)).join(";");
    propertyManager.set({
      logPreferenceChange: true,
      logPreferenceChangeFilter: _toConsumableArray(new Set([].concat(_toConsumableArray(get$2("logPreferenceChangeFilter").split(",")), ["libram_savedMacro", "maximizerMRUList", "testudinalTeachings", "garboTargetDate", "garboTargetCount", "garboTargetSources", "spadingData"]))).sort().filter(a => a).join(","),
      battleAction: "custom combat script",
      customCombatScript: "garbo",
      autoSatisfyWithMall: true,
      autoSatisfyWithNPCs: true,
      autoSatisfyWithCoinmasters: true,
      autoSatisfyWithStash: false,
      dontStopForCounters: true,
      maximizerFoldables: true,
      hpAutoRecovery: -0.05,
      hpAutoRecoveryTarget: 0.0,
      mpAutoRecovery: -0.05,
      mpAutoRecoveryTarget: 0.0,
      hpAutoRecoveryItems: hpItems,
      mpAutoRecoveryItems: mpItems,
      afterAdventureScript: "",
      betweenBattleScript: "",
      choiceAdventureScript: "garbo_choice.js",
      counterScript: "",
      familiarScript: "",
      currentMood: "apathetic",
      autoTuxedo: true,
      autoPinkyRing: true,
      autoGarish: true,
      allowNonMoodBurning: !globalOptions.ascend,
      allowSummonBurning: true,
      libramSkillsSoftcore: "none",
      // Don't cast librams when mana burning, handled manually based on sale price
      valueOfInventory: 2,
      suppressMallPriceCacheMessages: true,
      maximizerCombinationLimit: maximizerCombinationLimit,
      allowNegativeTally: true,
      spadingScript: "excavator.js",
      lastChanceBurn: "",
      errorOnAmbiguousFold: false
    });
    var bestHalloweiner = 0;
    if (haveInCampground($item`haunted doghouse`)) {
      var halloweinerOptions = [[$items`bowl of eyeballs, bowl of mummy guts, bowl of maggots`, 1], [$items`blood and blood, Jack-O-Lantern beer, zombie`, 2], [$items`wind-up spider, plastic nightmare troll, Telltale™ rubber heart`, 3]].map(_ref => {
        var _ref2 = _slicedToArray(_ref, 2),
          halloweinerOption = _ref2[0],
          choiceId = _ref2[1];
        return {
          price: garboAverageValue.apply(void 0, _toConsumableArray(halloweinerOption)),
          choiceId: choiceId
        };
      });
      bestHalloweiner = maxBy(halloweinerOptions, "price").choiceId;
    }
    propertyManager.setChoices({
      1106: 3,
      // Ghost Dog Chow
      1107: 1,
      // tennis ball
      1108: bestHalloweiner,
      1340: 1,
      // Accept the doctor quest
      1341: 1 // Cure her poison
    });
    if (have$r()) {
      propertyManager.setChoices(Object.fromEntries(choices.map(choice => [choice, bestJuneCleaverOption(choice)])));
    }
    propertyManager.set({
      shadowLabyrinthGoal: "effects"
    }); // Automate Shadow Labyrinth Quest

    var equipmentFamiliars = $familiars`Left-Hand Man, Disembodied Hand, Mad Hatrack, Fancypants Scarecrow`;
    var _iterator = _createForOfIteratorHelper(equipmentFamiliars.filter(have$P)),
      _step;
    try {
      for (_iterator.s(); !(_step = _iterator.n()).done;) {
        var familiar = _step.value;
        kolmafia.equip(familiar, $item.none);
      }
    } catch (err) {
      _iterator.e(err);
    } finally {
      _iterator.f();
    }
    safeRestore();
    if (questStep("questM23Meatsmith") === -1) {
      kolmafia.visitUrl("shop.php?whichshop=meatsmith&action=talk");
      kolmafia.runChoice(1);
    }
    if (questStep("questM24Doc") === -1) {
      kolmafia.visitUrl("shop.php?whichshop=doc&action=talk");
      kolmafia.runChoice(1);
    }
    if (questStep("questM25Armorer") === -1) {
      kolmafia.visitUrl("shop.php?whichshop=armory&action=talk");
      kolmafia.runChoice(1);
    }

    // unlock the sea
    if (kolmafia.myLevel() >= 11 && questStep("questS01OldGuy") === -1) {
      kolmafia.visitUrl("place.php?whichplace=sea_oldman&action=oldman_oldman");
    }
    if (kolmafia.myClass() === $class`Seal Clubber` && !have$P($skill`Furious Wallop`) && kolmafia.guildStoreAvailable()) {
      kolmafia.visitUrl("guild.php?action=buyskill&skillid=32", true);
    }
    var _stashItems = $items`repaid diaper, Buddy Bjorn, Crown of Thrones, Pantsgiving, mafia pointer finger ring, Mayflower bouquet`;
    if (kolmafia.myInebriety() <= kolmafia.inebrietyLimit() && (kolmafia.myClass() !== $class`Seal Clubber` || !have$P($skill`Furious Wallop`)) && !have$P($skill`Head in the Game`)) {
      _stashItems.push.apply(_stashItems, _toConsumableArray($items`haiku katana, Operation Patriot Shield`));
    }
    if (!have$P($item`Jurassic Parka`) && have$P($skill`Torso Awareness`)) {
      _stashItems.push($item`origami pasties`);
    }

    // Prepare Daily Affirmation for PvP fights if desired
    if (shouldAffirmationHate()) {
      acquire(1, $item`Daily Affirmation: Keep Free Hate in your Heart`, globalOptions.prefs.valueOfPvPFight * 3 * 1.1);
    }

    // FIXME: Dynamically figure out pointer ring approach.
    withStash(_stashItems, () => {
      withVIPClan(() => {
        // Prepare pirate realm if our copy target is cockroach
        // How do we handle if garbo was started without enough turns left without dieting to prep?
        if (globalOptions.target === $monster`cockroach` && !globalOptions.simdiet) {
          if (!globalOptions.nodiet) nonOrganAdventures();
          runSafeGarboQuests([DailyFamiliarsQuest]); // Prep robortender ahead of time in case it's a giant crab
          withProperty("removeMalignantEffects", false, () => runGarboQuests([CockroachSetup]));
        }
        // 0. diet stuff.
        if (globalOptions.nodiet || get$2("_garboYachtzeeChainCompleted", false)) {
          kolmafia.print("We should not be yachtzee chaining", "red");
          globalOptions.prefs.yachtzeechain = false;
        }
        if (!globalOptions.nodiet && (!globalOptions.prefs.yachtzeechain || get$2("_garboYachtzeeChainCompleted", false))) {
          runDiet();
        } else if (!globalOptions.simdiet) {
          nonOrganAdventures();
        }

        // 1. make an outfit (amulet coin, pantogram, etc), misc other stuff (VYKEA, songboom, robortender drinks)
        dailySetup();
        var preventEquip = $items`broken champagne bottle, Spooky Putty snake, Spooky Putty mitre, Spooky Putty leotard, Spooky Putty ball, papier-mitre, papier-mâchéte, papier-mâchine gun, papier-masque, papier-mâchuridars, smoke ball, stinky fannypack, dice-shaped backpack, Amulet of Perpetual Darkness`;
        if (isQuickGear()) {
          // Brimstone equipment explodes the number of maximize combinations
          preventEquip.push.apply(preventEquip, _toConsumableArray($items`Brimstone Bludgeon, Brimstone Bunker, Brimstone Brooch, Brimstone Bracelet, Brimstone Boxers, Brimstone Beret`));
        }
        // TODO: Until properly handled ban equipment that expands organs
        preventEquip.push.apply(preventEquip, _toConsumableArray($items`devilbone rosary, devilbone greaves, devilbone corset, angelbone totem, angelbone chopsticks, angelbone dice`));
        setDefaultMaximizeOptions({
          preventEquip: preventEquip,
          preventSlot: $slots`buddy-bjorn, crown-of-thrones`
        });

        // 2. do some target copy stuff
        freeFights();
        runGarboQuests([SetupTargetCopyQuest]);
        dailyFights();
        if (!globalOptions.nobarf) {
          // 3. burn turns at barf
          potionSetup(false);
          kolmafia.maximize("MP", false);
          meatMood().execute(estimatedGarboTurns());
          runGarboQuests([BuffExtensionQuest, PostBuffExtensionQuest]);
          if (!targetingMeat()) runGarboQuests([EmbezzlerFightsQuest]);
          try {
            runGarboFarmQuests([PostQuest()].concat(_toConsumableArray(FarmQuests())));
            runGarboQuests([FinishUpQuest]);
          } finally {
            kolmafia.setAutoAttack(0);
          }
        } else kolmafia.setAutoAttack(0);
      });
    });
  } finally {
    kolmafia.cliExecute("spade autoconfirm");
    propertyManager.resetAll();
    _set("garboStashItems", stashItems.map(item => kolmafia.toInt(item).toFixed(0)).join(","));
    setCombatFlags.apply(void 0, _toConsumableArray(combatFlags));
    if (startingGarden && have$P(startingGarden)) kolmafia.use(startingGarden);
    printEventLog();
    endSession();
    printLog(HIGHLIGHT);
  }
  _set(completedProperty, ["garbo", argString].filter(Boolean).join(" "));
}

exports.main = main;
